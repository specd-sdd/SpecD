{
  "title": "Change Create",
  "description": "Users need a way to start tracking a new unit of spec work before any artifacts exist. `specd change create <name> [--spec <id>...]` creates a new change and persists it to the repository, optionally scoping it to one or more specs upfront.",
  "dependsOn": ["cli:entrypoint", "core:change", "core:spec-id-format"],
  "context": [
    "Users need a way to start tracking a new unit of spec work before any artifacts exist. `specd change create <name> [--spec <id>...]` creates a new change and persists it to the repository, optionally scoping it to one or more specs upfront."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```\nspecd change create <name> [--spec <id>...] [--format text|json|toon]\n```\n\n- `<name>` — required positional; the unique slug name for the new change (e.g. `add-auth-flow`)\n- `--spec <id>` — optional repeatable flag; one or more spec IDs being created or modified. Each `<id>` is `[<workspace>:]<capability-path>` — the workspace qualifier is optional and defaults to `default` when omitted (e.g. `--spec auth/login` means `default:auth/login`, `--spec billing/invoices` with an explicit workspace would be `--spec billing-ws:billing/invoices`). When omitted, the change is created with an empty specIds list — specs can be added later via `change edit`.\n- `--description <text>` — optional; a short free-text description of the change's purpose, stored in the manifest\n- `--format text|json|toon` — optional; output format, defaults to `text`"
      ]
    },
    {
      "requirement": "Workspace resolution",
      "rules": [
        "The workspace IDs for the new change are derived from the workspace prefix of each `--spec` value. If the workspace prefix is omitted, `default` is used. If a named workspace is not declared in `specd.yaml`, the command fails with exit code 1 and prints an `error:` message to stderr."
      ]
    },
    {
      "requirement": "ReadOnly workspace rejection",
      "rules": [
        "After resolving the workspace for each `--spec` value, the CLI MUST check the workspace's `ownership` from `SpecdConfig`. If any spec's workspace has `readOnly` ownership, the command MUST exit with code 1 and print an error message to stderr:\n\n```text\nerror: Cannot add spec \"<specId>\" to change — workspace \"<workspace>\" is readOnly.\n\nReadOnly workspaces are protected: their specs and code cannot be modified by changes.\n```\n\nThis check MUST occur before invoking the `CreateChange` use case. If multiple specs target readOnly workspaces, one error per spec MUST be printed. The error message MUST NOT suggest remediation steps."
      ]
    },
    {
      "requirement": "Schema name and version",
      "rules": [
        "The CLI MUST NOT call `getActiveSchema` before `CreateChange`. Schema name and version are resolved inside the `CreateChange` use case from the project's active configuration.\n\nThe CLI MUST invoke `kernel.changes.create.execute` without `schemaName` or `schemaVersion` unless a future flag explicitly requests an override (none in this change).\n\nThe created change's manifest MUST still record the effective `schemaName` and `schemaVersion` from the `created` event produced by `CreateChange`."
      ]
    },
    {
      "requirement": "Overlap warning delegation",
      "rules": [
        "When `specIds` is non-empty, the CLI MUST pass `includeOverlapCheck: true` to `CreateChange.execute`.\n\nWhen `result.overlapReport?.hasOverlap` is `true`, the CLI MUST format and write the same stderr warning as today:\n\n```text\nwarning: spec overlap detected:\n  <specId> — also targeted by: <change-name> (<state>), ...\n```\n\nThe CLI MUST NOT call `kernel.changes.detectOverlap` directly in the create command handler.\n\nOverlap warning emission MUST NOT cause a non-zero exit code."
      ]
    },
    {
      "requirement": "Output on success",
      "rules": [
        "On success, output depends on `--format`:\n\n- `text` (default): prints a single line to stdout:\n- `json` or `toon`: outputs the following to stdout (encoded in the respective format): The `changePath` is the absolute filesystem path to the change directory where artifacts should be written.\n\nNo further output is produced."
      ]
    },
    {
      "requirement": "JSON output includes changePath",
      "rules": [
        "When `--format json` is specified:\n\n- stdout is valid JSON with `result: \"ok\"`, `name: \"<name>\"`, `state: \"drafting\"`, and `changePath: \"<absolute-path>\"`\n- `changePath` points to the change directory under `.specd/changes/`"
      ]
    },
    {
      "requirement": "JSON output on success",
      "rules": [
        "When `--format json` succeeds:\n\n- stdout is valid JSON with `result: \"ok\"`, `state: \"drafting\"`, and `name: \"<name>\"`\n- the process exits with code 0"
      ]
    },
    {
      "requirement": "Duplicate name error",
      "rules": [
        "If a change with the given name already exists (`ChangeAlreadyExistsError`), the command exits with code 1 and prints an `error:` message to stderr."
      ]
    }
  ],
  "constraints": [
    "--spec is optional; when omitted, the change is created with an empty specIds list",
    "The change name must be a valid kebab-case slug; the CLI validates the format before invoking the use case",
    "The workspace prefix in --spec defaults to default when omitted; workspace IDs are never a separate flag",
    "Specs targeting readOnly workspaces are rejected before the use case is invoked",
    "The CLI does not resolve or pass schema identity — CreateChange owns active schema resolution"
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Missing name argument",
      "when": ["specd change create is run without a positional name"],
      "then": ["the command exits with code 1 and prints a usage error to stderr"]
    },
    {
      "requirement": "Command signature",
      "name": "No --spec flag creates change with empty specIds",
      "when": ["specd change create my-change is run with no --spec flag"],
      "then": [
        "the change is created with an empty specIds list",
        "AND stdout contains created change my-change",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Workspace resolution",
      "name": "Workspace prefix omitted defaults to default",
      "when": ["specd change create my-change --spec auth/login is run with no workspace prefix"],
      "then": [
        "the spec is treated as default:auth/login",
        "AND the change is created with workspace default"
      ]
    },
    {
      "requirement": "Workspace resolution",
      "name": "Explicit workspace prefix used",
      "given": ["specd.yaml declares a workspace named billing-ws"],
      "when": ["specd change create my-change --spec billing-ws:invoices is run"],
      "then": ["the change is created with workspace billing-ws and specId billing-ws:invoices"]
    },
    {
      "requirement": "Workspace resolution",
      "name": "Unknown workspace prefix",
      "when": ["specd change create my-change --spec nonexistent-ws:some/path is run"],
      "then": ["the command exits with code 1 and prints an error: message to stderr"]
    },
    {
      "requirement": "ReadOnly workspace rejection",
      "name": "Spec targeting readOnly workspace rejected",
      "given": ["workspace platform is declared with ownership: readOnly in specd.yaml"],
      "when": ["specd change create my-change --spec platform:auth/tokens is run"],
      "then": [
        "the command exits with code 1",
        "AND stderr contains Cannot add spec \"platform:auth/tokens\" to change — workspace \"platform\" is readOnly.",
        "AND no change is created"
      ]
    },
    {
      "requirement": "ReadOnly workspace rejection",
      "name": "Multiple readOnly specs each produce an error",
      "given": ["workspace platform is readOnly"],
      "when": [
        "specd change create my-change --spec platform:auth/tokens --spec platform:auth/sessions is run"
      ],
      "then": ["stderr contains one error message per readOnly spec", "AND no change is created"]
    },
    {
      "requirement": "ReadOnly workspace rejection",
      "name": "Mixed owned and readOnly specs rejected",
      "given": ["workspace default is owned and workspace platform is readOnly"],
      "when": [
        "specd change create my-change --spec default:auth/login --spec platform:auth/tokens is run"
      ],
      "then": ["the command exits with code 1 due to the readOnly spec", "AND no change is created"]
    },
    {
      "requirement": "ReadOnly workspace rejection",
      "name": "Owned workspace specs accepted normally",
      "given": ["workspace default is owned"],
      "when": ["specd change create my-change --spec default:auth/login is run"],
      "then": ["the change is created successfully"]
    },
    {
      "requirement": "Schema name and version",
      "name": "Schema resolved inside CreateChange",
      "given": ["the active project schema resolves to name 'spec-driven' and version 1"],
      "when": ["specd change create my-change is run"],
      "then": [
        "getActiveSchema is not called in the CLI handler",
        "AND the created change's manifest includes schemaName: 'spec-driven' and schemaVersion: 1"
      ]
    },
    {
      "requirement": "Schema name and version",
      "name": "Manifest still records effective schema identity",
      "given": ["the active project schema resolves to name 'spec-driven' and version 1"],
      "when": ["specd change create my-change is run"],
      "then": [
        "the created change's manifest includes schemaName: 'spec-driven' and schemaVersion: 1"
      ]
    },
    {
      "requirement": "Overlap warning delegation",
      "name": "CLI passes includeOverlapCheck when specs present",
      "given": ["specd change create my-change --spec default:auth/login is run"],
      "when": ["the CLI invokes CreateChange.execute"],
      "then": ["the input includes includeOverlapCheck: true"]
    },
    {
      "requirement": "Overlap warning delegation",
      "name": "Overlap warning emitted from result report",
      "given": ["another active change already targets default:auth/login"],
      "when": ["specd change create my-change --spec default:auth/login succeeds"],
      "then": [
        "stderr contains warning: spec overlap detected",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Overlap warning delegation",
      "name": "CLI does not call detectOverlap directly",
      "when": ["specd change create my-change --spec default:auth/login is run"],
      "then": ["the CLI handler does not invoke kernel.changes.detectOverlap.execute"]
    },
    {
      "requirement": "Overlap warning delegation",
      "name": "No overlap check when specIds empty",
      "when": ["specd change create my-change is run with no --spec flags"],
      "then": ["includeOverlapCheck is not passed as true"]
    },
    {
      "requirement": "Output on success",
      "name": "Successful creation",
      "when": ["specd change create add-login --spec auth/login succeeds"],
      "then": [
        "stdout contains exactly created change add-login",
        "AND stderr is empty",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "JSON output includes changePath",
      "name": "JSON output contains changePath",
      "when": ["specd change create my-change --format json succeeds"],
      "then": [
        "the JSON output includes changePath as an absolute path string",
        "AND the path points to the change directory under .specd/changes/"
      ]
    },
    {
      "requirement": "Duplicate name error",
      "name": "Name already exists",
      "given": ["a change named my-change already exists"],
      "when": ["specd change create my-change --spec auth/login is run"],
      "then": [
        "the command exits with code 1",
        "AND stderr contains an error: message mentioning the duplicate name"
      ]
    },
    {
      "requirement": "JSON output on success",
      "name": "JSON output on success",
      "when": ["specd change create add-login --spec auth/login --format json succeeds"],
      "then": [
        "stdout is valid JSON with result equal to \"ok\" and state equal to \"drafting\"",
        "AND name is \"add-login\"",
        "AND the process exits with code 0"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "cli:change-create",
        "file": "cli:src/commands/change/create.ts",
        "symbol": "registerChangeCreate"
      },
      {
        "specId": "cli:change-create",
        "file": "cli:test/commands/change-create.spec.ts",
        "symbol": "Integration (real kernel)"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:b7a63119df6ec17d837aa869697c69641c483e9a07fc071acacb60111587f542",
    "verify.md": "sha256:a002b3153bc71756fcaef2699dd3dc3bede21c13a8ebcd1c33b1ca302a5a3e04"
  },
  "generatedBy": "core"
}
