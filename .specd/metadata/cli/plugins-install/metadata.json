{
  "title": "cli:plugins-install",
  "description": "Defines the CLI contract for installing specd plugins. The command reads the current plugin configuration, validates each requested plugin, installs it via the plugin manager, and records the installation in the project configuration.",
  "dependsOn": [
    "plugin-manager:install-plugin-use-case",
    "plugin-manager:load-plugin-use-case",
    "core:config-writer-port",
    "core:get-config"
  ],
  "context": [
    "Defines the CLI contract for installing specd plugins. The command reads the current plugin configuration, validates each requested plugin, installs it via the plugin manager, and records the installation in the project configuration."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "The command MUST accept one or more plugin names as positional arguments:\n\n```bash\nspecd plugins install <plugin> [<plugin>...]\n```\n\nEach `plugin` is an npm package name (e.g., `@specd/plugin-agent-claude`)."
      ]
    },
    {
      "requirement": "Declaration source",
      "rules": [
        "When the command needs the set of declared plugins (for example to detect already-installed plugins), it MUST read from the loaded `SpecdConfig.plugins` field. It MUST NOT call `kernel.project.listPlugins` or `ConfigWriter.listPlugins` when a config snapshot is already available.\n\nFor already-installed detection, the command MUST consult `config.plugins.agents` (or the appropriate type bucket)."
      ]
    },
    {
      "requirement": "Display name",
      "rules": [
        "The command MUST display a suitable header above any tabular output. The header MUST be `Installed plugins` with a trailing colon, followed by newline-separated details."
      ]
    },
    {
      "requirement": "Already-installed handling",
      "rules": [
        "When a plugin name is already declared in `specd.yaml` under the appropriate type:\n\n- The command MUST emit a warning message containing the string `already installed` and the word `update` (in that order).\n- The command SHOULD suggest using `update` to reinstall.\n- The command MUST NOT re-install the plugin."
      ]
    },
    {
      "requirement": "Installation workflow",
      "rules": [
        "For each plugin name that is not already installed:\n\n1. The command MUST call the `InstallPlugin` use case from `@specd/plugin-manager`.\n2. On success, the command MUST call `ConfigWriter.addPlugin()` to record the plugin in `specd.yaml`.\n3. On failure, the command MUST emit the error message and continue with the remaining plugins."
      ]
    },
    {
      "requirement": "Exit code",
      "rules": [
        "The command MUST exit with code 1 if any plugin fails to install, but continue processing the remaining plugins."
      ]
    },
    {
      "requirement": "Output format",
      "rules": [
        "The command MUST output:\n\n- Plugin name\n- Installation status (`installed`, `skipped`, or error message)\n- Any additional details\n\nThe format MUST be machine-parseable when invoked with `--format json`."
      ]
    }
  ],
  "constraints": [
    "The command MUST NOT mutate specd.yaml for plugins that are already installed.",
    "The command MUST validate plugin names before attempting installation."
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Valid plugin names",
      "when": ["specd plugins install @specd/plugin-agent-claude is invoked"],
      "then": ["the command attempts to install the plugin"]
    },
    {
      "requirement": "Command signature",
      "name": "Multiple plugin names",
      "when": [
        "specd plugins install @specd/plugin-agent-claude @specd/plugin-agent-copilot is invoked"
      ],
      "then": ["the command attempts to install both plugins"]
    },
    {
      "requirement": "Declaration source",
      "name": "Already-installed check uses config.plugins",
      "given": ["@specd/plugin-agent-claude is declared under plugins.agents in the loaded config"],
      "when": ["specd plugins install @specd/plugin-agent-claude is invoked"],
      "then": [
        "the command detects the plugin as already installed from config.plugins",
        "AND it does not call kernel.project.listPlugins"
      ]
    },
    {
      "requirement": "Declaration source",
      "name": "Declaration read does not re-read disk via ConfigWriter",
      "given": ["a loaded SpecdConfig snapshot is available"],
      "when": ["the install workflow checks declared plugins"],
      "then": ["it reads config.plugins in memory", "AND it does not call ConfigWriter.listPlugins"]
    },
    {
      "requirement": "Already-installed handling",
      "name": "Plugin already installed",
      "given": ["@specd/plugin-agent-claude is already in specd.yaml"],
      "when": ["specd plugins install @specd/plugin-agent-claude is invoked"],
      "then": [
        "a warning containing \"already installed\" and \"update\" is emitted",
        "AND the plugin is NOT re-installed"
      ]
    },
    {
      "requirement": "Display name",
      "name": "Output displays appropriate header",
      "when": ["specd plugins install @specd/plugin-agent-claude is invoked"],
      "then": ["output displays a header Installed plugins: followed by plugin details"]
    },
    {
      "requirement": "Installation workflow",
      "name": "Calls InstallPlugin use case and records in config",
      "when": [
        "specd plugins install @specd/plugin-agent-claude is invoked with plugin not installed"
      ],
      "then": [
        "InstallPlugin use case is called",
        "AND ConfigWriter.addPlugin() is called to record the plugin in specd.yaml"
      ]
    },
    {
      "requirement": "Exit code",
      "name": "Installation failure",
      "when": ["any plugin fails to install"],
      "then": ["the command exits with code 1", "AND continues processing the remaining plugins"]
    },
    {
      "requirement": "Output format",
      "name": "JSON format",
      "when": ["--format json is passed"],
      "then": ["output is machine-parseable JSON"]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "cli:plugins-install",
        "file": "cli:src/commands/plugins/get-declared-plugins.ts",
        "symbol": "getDeclaredPlugins"
      },
      {
        "specId": "cli:plugins-install",
        "file": "cli:src/commands/plugins/install.ts",
        "symbol": "installPluginsWithKernel"
      },
      {
        "specId": "cli:plugins-install",
        "file": "cli:src/commands/plugins/install.ts",
        "symbol": "registerPluginsInstall"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:4b483f11253c6502163b963ff2f568419a36aa49b96d7147d70d06a4e108f751",
    "verify.md": "sha256:02338dafd24c88b1fcf5dcd6eafef25dee691a8138d35bd704542a66067e9f04"
  },
  "generatedBy": "core"
}
