{
  "title": "Graph Stats",
  "description": "Without a quick overview of the code graph's contents, users and agents cannot tell whether the graph is populated, how large it is, or when it was last updated. The `specd graph stats` command displays summary statistics from the code graph so users can assess coverage and freshness at a glance.",
  "dependsOn": [
    "cli:entrypoint",
    "core:config",
    "code-graph:staleness-detection",
    "core:list-workspaces",
    "code-graph:get-graph-health",
    "sdk:with-open-graph-provider",
    "sdk:host-context"
  ],
  "context": [
    "Without a quick overview of the code graph's contents, users and agents cannot tell whether the graph is populated, how large it is, or when it was last updated. The `specd graph stats` command displays summary statistics from the code graph so users can assess coverage and freshness at a glance."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```\nspecd graph stats [--config <path> | --path <path>] [--format text|json|toon]\n```\n\n- `--config <path>` — optional; explicit path to `specd.yaml`, matching the standard CLI meaning\n- `--path <path>` — optional; repo-root bootstrap mode\n- `--format text|json|toon` — optional; output format, defaults to `text`\n\n`--config` and `--path` are mutually exclusive.\n\n`--path` and no-config fallback are bootstrap mechanisms for setup and repository exploration, not the intended steady-state mode for configured projects."
      ]
    },
    {
      "requirement": "Statistics retrieval",
      "rules": [
        "The command obtains host context via `openSpecdHost` from `@specd/sdk`, opens the graph provider through the SDK lifecycle helper, and delegates health computation to `createGetGraphHealth`.\n\nFor `--config`, the command uses the explicit configuration path. For `--path` and the no-config fallback, it calls `openSpecdHost` with `allowBootstrapFallback: true` so the SDK first discovers a project configuration and otherwise creates a synthetic graph host from the resolved VCS root. `graph stats` MUST NOT call `resolveGraphCliContext`.\n\nThe command MUST pass `ListWorkspaces` results when the host has a configured kernel; synthetic bootstrap uses its generated default workspace."
      ]
    },
    {
      "requirement": "Concurrent indexing guard",
      "rules": [
        "`graph stats` MUST NOT perform a host-managed pre-open lock probe before opening the provider.\n\nInstead, the command relies on provider-owned availability checks surfaced through `GetGraphHealth` and provider reads."
      ]
    },
    {
      "requirement": "Output format",
      "rules": [
        "In `text` mode (default), the output is a labelled summary:\n\n```text\nFiles:     459\nDocuments: 18\nSymbols:   1497\nSpecs:     122\nLanguages: javascript, typescript\nRelations:\n  IMPORTS: 1227\n  DEFINES: 1497\nLast indexed: 2026-03-14T10:38:30.178Z\n```\n\n- `Files`, `Documents`, `Symbols`, `Specs` show `fileCount`, `documentCount`, `symbolCount`, `specCount`\n- `Languages` shows the `languages` array joined by `, `\n- `Relations` shows only non-zero relation counts from `relationCounts`, each on its own indented line\n- `Last indexed` shows `lastIndexedAt` as an ISO 8601 timestamp\n\nIf the graph is stale, a warning line SHALL be appended after `Last indexed`:\n\n```text\n⚠ Graph is stale (indexed at <short-ref>, current: <short-ref>)\n```\n\nWhere `<short-ref>` is the first 7 characters of the ref. If `lastIndexedRef` is `null`, no staleness line is shown.\n\nIf the stored derivation fingerprint differs from the fingerprint computed for the current effective graph configuration, a warning line SHALL be appended to stderr in text mode:\n\n```text\n⚠ Derivation fingerprint mismatch — code-graph version or workspace configuration changed since last index\n```\n\nThis warning is independent from the VCS staleness line. Both MAY appear when the graph is stale by ref and mismatched by derivation fingerprint.\n\nIn `json` or `toon` mode, the full `GraphStatistics` object is output as-is, with three additional fields:\n\n- `stale: boolean | null` — `true` if stale, `false` if fresh, `null` if unknown\n- `currentRef: string | null` — the current VCS ref, or `null` if unavailable\n- `fingerprintMismatch: boolean | null` — `true` when the stored derivation fingerprint differs from the current effective graph configuration, `false` when it matches, `null` when the comparison cannot be computed"
      ]
    },
    {
      "requirement": "Error cases",
      "rules": [
        "If the provider reports `GRAPH_BUSY`, the command SHALL fail with the standard graph-busy infrastructure error path and exit code 3.\n\nIf the provider reports `GRAPH_PROVIDER_STALE`, the command SHALL fail with the standard infrastructure error path and exit code 3.\n\nIf the provider cannot be opened or statistics retrieval fails due to another infrastructure error, the command SHALL exit with code 3."
      ]
    }
  ],
  "constraints": [
    "Health orchestration runs inside withOpenGraphProvider from @specd/sdk; the CLI does not manage provider lifecycle inline",
    "process.exit(0) is called explicitly after the SDK wrapper closes the provider",
    "Zero-value relation counts are omitted from text output for readability",
    "Bootstrap mode (--path) resolves host context through openSpecdHost with the appropriate config path input",
    "Graph-busy and provider-stale availability semantics are owned by the provider, not by a CLI-managed pre-open lock probe"
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Explicit config path bypasses discovery",
      "given": ["the current directory would autodiscover a different specd.yaml"],
      "when": ["specd graph stats --config /tmp/other/specd.yaml is run"],
      "then": ["the command uses /tmp/other/specd.yaml directly"]
    },
    {
      "requirement": "Command signature",
      "name": "Explicit path enters bootstrap mode",
      "given": ["a specd.yaml exists under the current repository"],
      "when": ["specd graph stats --path /tmp/repo is run"],
      "then": [
        "config discovery is ignored",
        "AND the command opens the graph for a synthetic single workspace default rooted at /tmp/repo"
      ]
    },
    {
      "requirement": "Command signature",
      "name": "Mutually exclusive context flags fail fast",
      "when": ["specd graph stats --config ./specd.yaml --path . is run"],
      "then": ["the command exits with code 1 before any graph provider is opened"]
    },
    {
      "requirement": "Statistics retrieval",
      "name": "Command delegates health to GetGraphHealth via SDK",
      "when": ["specd graph stats is executed in configured mode with kernel available"],
      "then": [
        "it obtains host context via openSpecdHost from @specd/sdk",
        "AND it opens the provider via withOpenGraphProvider from @specd/sdk",
        "AND it calls GetGraphHealth.execute() with workspaces from ListWorkspaces",
        "AND it does not call resolveGraphCliContext"
      ]
    },
    {
      "requirement": "Statistics retrieval",
      "name": "Path and no-config stats use opt-in SDK bootstrap fallback",
      "given": ["a VCS repository without a discoverable specd.yaml"],
      "when": ["specd graph stats --path <repo> or specd graph stats runs there"],
      "then": [
        "stats calls openSpecdHost with allowBootstrapFallback: true",
        "AND it obtains graph health without resolveGraphCliContext"
      ]
    },
    {
      "requirement": "Statistics retrieval",
      "name": "Successful stats exits after provider cleanup",
      "when": ["specd graph stats completes successfully"],
      "then": [
        "process.exit(0) is called only after withOpenGraphProvider completes its close path"
      ]
    },
    {
      "requirement": "Statistics retrieval",
      "name": "Command obtains orchestrated project structure",
      "when": ["specd graph stats is executed in configured mode"],
      "then": [
        "it calls ListWorkspaces to obtain the rich workspace list",
        "AND it passes that list to GetGraphHealth for fingerprint comparison"
      ]
    },
    {
      "requirement": "Statistics retrieval",
      "name": "Host context from openSpecdHost",
      "when": ["specd graph stats is executed"],
      "then": ["it obtains host context via openSpecdHost from @specd/sdk"]
    },
    {
      "requirement": "Concurrent indexing guard",
      "name": "Stats surface provider busy after open",
      "given": ["the provider reports GRAPH_BUSY while serving graph health"],
      "when": ["specd graph stats is run"],
      "then": [
        "the command exits with code 3",
        "AND it uses the infrastructure error path rather than a separate pre-open lock probe"
      ]
    },
    {
      "requirement": "Output format",
      "name": "Text output with fresh graph",
      "given": ["lastIndexedRef equals the current VCS ref"],
      "when": ["graph stats is run in text mode"],
      "then": ["no staleness warning line SHALL be shown"]
    },
    {
      "requirement": "Output format",
      "name": "Text output with stale graph",
      "given": ["lastIndexedRef is \"abc1234def\" and the current VCS ref is \"fff9999aaa\""],
      "when": ["graph stats is run in text mode"],
      "then": [
        "a line ⚠ Graph is stale (indexed at abc1234, current: fff9999) SHALL appear after Last indexed"
      ]
    },
    {
      "requirement": "Output format",
      "name": "Text output with null ref",
      "given": ["lastIndexedRef is null"],
      "when": ["graph stats is run in text mode"],
      "then": ["no staleness line SHALL be shown"]
    },
    {
      "requirement": "Output format",
      "name": "Text output with derivation fingerprint mismatch",
      "given": [
        "the stored derivation fingerprint differs from the fingerprint computed for the current effective graph configuration"
      ],
      "when": ["graph stats is run in text mode"],
      "then": [
        "stderr contains ⚠ Derivation fingerprint mismatch — code-graph version or workspace configuration changed since last index"
      ]
    },
    {
      "requirement": "Output format",
      "name": "Text output includes document counts",
      "given": ["the graph contains 459 files, 18 documents, 1497 symbols, and 122 specs"],
      "when": ["graph stats is run in text mode"],
      "then": [
        "stdout contains Files:     459",
        "AND stdout contains Documents: 18",
        "AND stdout contains Symbols:   1497"
      ]
    },
    {
      "requirement": "Output format",
      "name": "JSON output includes staleness fields",
      "when": ["graph stats --format json is run"],
      "then": [
        "the output SHALL include stale (boolean or null), currentRef (string or null), and fingerprintMismatch"
      ]
    },
    {
      "requirement": "Output format",
      "name": "JSON stale field values",
      "given": ["the graph is stale"],
      "when": ["graph stats --format json is run"],
      "then": ["stale SHALL be true", "AND currentRef SHALL be the current VCS ref string"]
    },
    {
      "requirement": "Error cases",
      "name": "Infrastructure error exits with code 3",
      "given": [
        "the provider cannot be opened",
        "OR the provider reports GRAPH_BUSY",
        "OR the provider reports GRAPH_PROVIDER_STALE"
      ],
      "when": ["specd graph stats is run"],
      "then": [
        "stderr contains a fatal: prefixed error message",
        "AND the process exits with code 3"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "cli:graph-stats",
        "file": "cli:test/commands/graph-stats.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "cli:graph-stats",
        "file": "cli:src/commands/graph/stats.ts",
        "symbol": "registerGraphStats"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:08de5423d7040554fcd2711796151bef11a415ea12078da4c629e352875f4a0f",
    "verify.md": "sha256:8c8bd63d5ff05755c4fa90a707fa1381dafb8851ba74f2593080f96a91326e21"
  },
  "generatedBy": "core"
}
