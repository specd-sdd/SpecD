{
  "title": "Change Discard",
  "description": "When a change is no longer viable, it should be explicitly abandoned rather than left rotting in the active list. `specd change discard <name> --reason <text>` permanently moves a change to `discarded/`, requiring a reason so the decision is auditable.",
  "dependsOn": ["cli:cli/entrypoint", "core:core/change"],
  "context": [
    "When a change is no longer viable, it should be explicitly abandoned rather than left rotting in the active list. `specd change discard <name> --reason <text>` permanently moves a change to `discarded/`, requiring a reason so the decision is auditable."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```\nspecd change discard <name> --reason <text> [--force] [--format text|json|toon]\n```\n\n- `<name>` — required positional; the name of the change to discard\n- `--reason <text>` — required; a human-readable explanation for discarding the change\n- `--force` — optional; bypasses the historical implementation guard when the change has previously reached `implementing`\n- `--format text|json|toon` — optional; output format, defaults to `text`"
      ]
    },
    {
      "requirement": "Behaviour",
      "rules": [
        "The command moves the change from `changes/` or `drafts/` to `discarded/` and appends a `discarded` event to history. This operation is irreversible.\n\nIf the change has ever reached `implementing`, the command SHALL fail by default because implementation may already exist and abandoning the workflow would risk leaving permanent specs and code out of sync. The operation MAY proceed only when `--force` is provided."
      ]
    },
    {
      "requirement": "Output on success",
      "rules": [
        "On success, output depends on `--format`:\n\n- `text` (default): prints a single line to stdout:\n- `json` or `toon`: outputs the following to stdout (encoded in the respective format):"
      ]
    },
    {
      "requirement": "Error cases",
      "rules": [
        "- If the change does not exist, the command exits with code 1 and prints an `error:` message.\n- `--reason` is mandatory; omitting it is a CLI usage error (exit code 1).\n- If the change has ever reached `implementing` and `--force` is not provided, the command exits with code 1 and prints an `error:` message explaining that implementation may already exist and discarding the change could leave specs and code out of sync."
      ]
    }
  ],
  "constraints": [
    "Discarding is irreversible — there is no undo command",
    "A change may be discarded from changes/ or drafts/; it cannot be discarded from discarded/ (already there) or from the archive",
    "Once a change has ever reached implementing, discarding requires --force",
    "The reason is stored in the discarded event and must be a non-empty string",
    "--force bypasses only the historical implementation guard; it does not make discarding reversible"
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Missing reason flag",
      "when": ["specd change discard my-change is run without --reason"],
      "then": ["the command exits with code 1 and prints a usage error to stderr"]
    },
    {
      "requirement": "Command signature",
      "name": "Empty reason rejected",
      "when": ["specd change discard my-change --reason \"\" is run"],
      "then": ["the command exits with code 1 and prints a usage error to stderr"]
    },
    {
      "requirement": "Command signature",
      "name": "Force flag is accepted",
      "when": [
        "specd change discard my-change --reason \"cleanup\" --force is run with a valid change name"
      ],
      "then": ["the command accepts the flag as part of its input contract"]
    },
    {
      "requirement": "Behaviour",
      "name": "Active change discarded",
      "given": ["an active change old-experiment in changes/"],
      "when": ["specd change discard old-experiment --reason \"superseded\" is run"],
      "then": [
        "the change is moved to discarded/",
        "AND a discarded event with the given reason is appended to history"
      ]
    },
    {
      "requirement": "Behaviour",
      "name": "Drafted change discarded",
      "given": ["a drafted change old-experiment in drafts/"],
      "when": ["specd change discard old-experiment --reason \"abandoned\" is run"],
      "then": ["the change is moved to discarded/", "AND a discarded event is appended to history"]
    },
    {
      "requirement": "Behaviour",
      "name": "Historically implemented change can still be discarded when forced",
      "given": ["old-experiment has previously reached implementing"],
      "when": ["specd change discard old-experiment --reason \"workflow cleanup\" --force is run"],
      "then": ["the change is moved to discarded/", "AND a discarded event is appended to history"]
    },
    {
      "requirement": "Output on success",
      "name": "Success message",
      "when": ["specd change discard old-experiment --reason \"done\" succeeds"],
      "then": [
        "stdout contains discarded change old-experiment",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Change not found",
      "when": ["specd change discard nonexistent --reason \"done\" is run"],
      "then": ["the command exits with code 1", "AND stderr contains an error: message"]
    },
    {
      "requirement": "Error cases",
      "name": "Historically implemented change requires force",
      "given": ["old-experiment has previously reached implementing"],
      "when": ["specd change discard old-experiment --reason \"done\" is run without --force"],
      "then": [
        "the command exits with code 1",
        "AND stderr explains that implementation may already exist and discarding the change could leave specs and code out of sync"
      ]
    },
    {
      "requirement": "Output on success",
      "name": "JSON output on success",
      "when": ["specd change discard old-experiment --reason \"done\" --format json succeeds"],
      "then": [
        "stdout is valid JSON with result equal to \"ok\" and name equal to \"old-experiment\"",
        "AND the process exits with code 0"
      ]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:be5b0c5c7af512bcbb98d1d0a7fd9f852765afc7e27b0607df06d1a80eeb3776",
    "verify.md": "sha256:4cfa631cac38cb1685d1f313fd9d5deb64075da660c5a512080e978d7708e557"
  },
  "generatedBy": "core"
}
