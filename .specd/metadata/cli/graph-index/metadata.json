{
  "title": "Graph Index",
  "description": "Without an up-to-date code graph, all graph-based queries return stale or empty results. The `specd graph index` command indexes the current workspace into the code graph, reporting what was discovered, indexed, skipped, and removed so users and agents can verify completeness.",
  "dependsOn": [
    "cli:entrypoint",
    "core:config",
    "code-graph:composition",
    "code-graph:graph-store",
    "core:list-workspaces"
  ],
  "context": [
    "Without an up-to-date code graph, all graph-based queries return stale or empty results. The `specd graph index` command indexes the current workspace into the code graph, reporting what was discovered, indexed, skipped, and removed so users and agents can verify completeness."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```text\nspecd graph index [--force] [--exclude-path <pattern>] [--config <path> | --path <path>] [--format text|json|toon]\n```\n\n- `--force` — optional; recreate the graph store from scratch\n- `--exclude-path <pattern>` — optional, repeatable; additional patterns to exclude during file discovery\n- `--config <path>` — optional; explicit path to `specd.yaml`\n- `--path <path>` — optional; repo-root bootstrap mode\n- `--format text|json|toon` — optional; output format, defaults to `text`\n\n`--config` and `--path` are mutually exclusive."
      ]
    },
    {
      "requirement": "Indexing behaviour",
      "rules": [
        "The command obtains the workspace targets and spec sources from the project configuration.\nIt delegates bootstrap configuration creation, effective project graph config merging, lock acquisition, and index execution entirely to the `@specd/code-graph` provider.\nThe command SHALL pass an `onProgress` callback to the provider's `index()` method. When running in `text` mode, this callback prints the current progress percentage and indexing phase to stdout.\n\nUnless `SPECD_GRAPH_INDEX_NO_WORKER` is set to `true` (test-only bypass), the parent CLI process SHALL spawn a child worker via `child_process.spawn` reusing the same CLI arguments. The parent acquires the shared graph indexing lock before spawning. The worker receives `SPECD_GRAPH_INDEX_WORKER=true` and `SPECD_GRAPH_INDEX_LOCK_HELD=true` in its environment, performs the actual indexing work, and inherits stdio from the parent. The parent forwards `SIGINT` and `SIGTERM` to the worker, releases the lock when the worker exits, and propagates the worker exit code (or reports worker failure on non-zero exit).\n\nWhen `SPECD_GRAPH_INDEX_NO_WORKER` is `true`, indexing runs in the current process without spawning a worker. This bypass exists only for automated tests."
      ]
    },
    {
      "requirement": "Output format",
      "rules": [
        "In `text` mode (default), the output is a summary block:\n\n```text\nIndexed 387 file(s) in 1234ms\n  discovered: 459\n  documents:  18\n  skipped:    72\n  removed:    0\n  specs:      122\n  errors:     3\n    path/to/file.ts: error message\n```\n\nThe first line shows `filesIndexed` and `duration`. The indented lines show `filesDiscovered`, `documentsIndexed`, `filesSkipped`, `filesRemoved`, `specsIndexed`, and error count. When errors are present, each error is listed below with its file path and message, indented further.\n\nWhen the result includes per-workspace breakdowns, text mode also lists each workspace with discovered, indexed, document, skipped, and removed counts.\n\nIn `json` or `toon` mode, the full `IndexResult` object is output as-is, including `documentsIndexed` and the per-workspace `documentsIndexed` breakdown."
      ]
    },
    {
      "requirement": "Error cases",
      "rules": [
        "If the provider cannot be opened or indexing fails due to an infrastructure error (I/O, database), the command exits with code 3.\n\nIf the shared graph indexing lock cannot be acquired because another indexing run is in progress, the command exits with code 3 after printing a user-facing retry-later message.\n\nPer-file indexing errors (parse failures, unsupported syntax) do not cause a non-zero exit — they are reported in the `errors` array of `IndexResult` and the command exits with code 0."
      ]
    },
    {
      "requirement": "CLI reference documentation",
      "rules": [
        "The `specd graph` command group SHALL be fully documented in `docs/cli/cli-reference.md` under a `## graph` section. The reference MUST cover all five subcommands: `index`, `search`, `hotspots`, `stats`, and `impact`.\n\nFor `graph index`, the documentation MUST include:\n\n- Full command signature with all flags\n- Description of `--exclude-path` flag: repeatable, gitignore-syntax, merges on top of config's `graph.excludePaths`\n- Description of `--config` and `--path` behavior, including that they are mutually exclusive\n- Description of bootstrap mode and no-config fallback as setup/bootstrap mechanisms rather than the normal configured mode\n- Description of `graph.excludePaths` and `graph.respectGitignore` workspace config fields and their effect on indexing\n- The built-in default exclusion list (applied when `graph.excludePaths` is not configured)\n- Replace semantics: specifying `graph.excludePaths` replaces built-in defaults\n- Example showing how to re-include a subdirectory of an otherwise-excluded dir using negation\n\nFor each other subcommand (`search`, `hotspots`, `stats`, `impact`), the documentation MUST include: command signature, flag descriptions, at least one usage example, and the graph CLI context model (`--config`, `--path`, bootstrap-only fallback semantics) when the command supports those flags."
      ]
    }
  ],
  "constraints": [
    "The CLI does not contain indexing logic, lock management, or configuration building — it delegates entirely to @specd/code-graph",
    "process.exit(0) is called explicitly after closing the provider to prevent LadybugDB native threads from keeping the process alive",
    "--force delegates destructive backend recreation to the graph-store contract",
    "The withProvider helper manages lifecycle and registers SIGINT/SIGTERM signal handlers",
    "Workspace targets and spec sources are derived from SpecdConfig and Kernel",
    "Worker subprocess isolation keeps native graph-store threads out of the parent CLI process; tests bypass spawning via SPECD_GRAPH_INDEX_NO_WORKER"
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Minimal invocation with defaults",
      "when": ["specd graph index is run"],
      "then": ["it accepts no positional arguments", "AND output format defaults to text"]
    },
    {
      "requirement": "Output format",
      "name": "Text output shows summary including documents",
      "given": [
        "indexing discovers 459 files, indexes 387, indexes 18 documents, skips 72, removes 0, indexes 122 specs, and has 0 errors in 1234ms"
      ],
      "when": ["specd graph index is run"],
      "then": [
        "stdout contains Indexed 387 file(s) in 1234ms",
        "AND stdout contains discovered: 459",
        "AND stdout contains documents:  18",
        "AND stdout contains skipped:    72",
        "AND stdout contains errors:     0"
      ]
    },
    {
      "requirement": "Output format",
      "name": "Text output with errors",
      "given": ["indexing completes with 2 per-file errors"],
      "when": ["specd graph index is run"],
      "then": [
        "stdout shows errors:     2",
        "AND each error is listed below with its file path and message",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Output format",
      "name": "JSON output",
      "given": ["indexing completes successfully"],
      "when": ["specd graph index --format json is run"],
      "then": [
        "stdout is valid JSON containing filesDiscovered, filesIndexed, documentsIndexed, filesSkipped, filesRemoved, specsIndexed, errors, and duration"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Mutually exclusive context flags fail fast",
      "when": ["specd graph index --config ./specd.yaml --path . is run"],
      "then": [
        "stderr contains a CLI error about incompatible flags",
        "AND no graph provider is opened",
        "AND the process exits with code 1"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Infrastructure error exits with code 3",
      "given": ["the provider cannot be opened (e.g. database I/O error)"],
      "when": ["specd graph index is run"],
      "then": [
        "stderr contains a fatal: prefixed error message",
        "AND the process exits with code 3"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Lock acquisition failure exits with code 3",
      "given": ["another process currently holds the shared graph indexing lock"],
      "when": ["specd graph index is run"],
      "then": ["stderr contains a retry-later message", "AND the process exits with code 3"]
    },
    {
      "requirement": "Error cases",
      "name": "Per-file errors do not cause non-zero exit",
      "given": ["indexing encounters parse failures in some files"],
      "when": ["specd graph index is run"],
      "then": [
        "the errors are included in the errors array of the output",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "CLI reference documentation",
      "name": "graph section present in CLI reference",
      "when": ["docs/cli/cli-reference.md is inspected"],
      "then": ["a ## graph section exists covering index, search, hotspots, stats, and impact"]
    },
    {
      "requirement": "CLI reference documentation",
      "name": "graph index flags documented",
      "when": ["the ### graph index subsection is read"],
      "then": [
        "--exclude-path, --force, --config, --path, and --format are documented with descriptions",
        "AND project-global graph.includePaths, global graph.excludePaths, and workspace graph.respectGitignore / graph.allowedPaths behavior are explained",
        "AND the bootstrap-only nature of --path and no-config fallback is stated"
      ]
    },
    {
      "requirement": "Indexing behaviour",
      "name": "Command passes progress callback in text mode",
      "when": ["specd graph index is run in text mode"],
      "then": [
        "the command passes an onProgress callback to the provider",
        "AND when the callback is invoked, it prints the progress percentage and phase to stdout"
      ]
    },
    {
      "requirement": "Indexing behaviour",
      "name": "Command delegates configuration and locking",
      "when": ["specd graph index is executed"],
      "then": [
        "it delegates bootstrap config fallback, effective project config merging, lock acquisition, and index execution to the provider"
      ]
    },
    {
      "requirement": "Indexing behaviour",
      "name": "Worker subprocess performs indexing in isolation",
      "given": ["SPECD_GRAPH_INDEX_NO_WORKER is not set"],
      "when": ["specd graph index is run"],
      "then": [
        "the parent process spawns a child worker with SPECD_GRAPH_INDEX_WORKER=true",
        "AND the parent forwards SIGINT and SIGTERM to the worker",
        "AND the parent releases the indexing lock when the worker exits"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "cli:graph-index",
        "file": "cli:src/commands/graph/resolve-graph-cli-context.ts"
      }
    ],
    "symbols": [
      {
        "specId": "cli:graph-index",
        "file": "cli:src/commands/graph/index-graph.ts",
        "symbol": "registerGraphIndex"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:3092ba43653b2446db066788950712ea4dcd0dabf077c9dfabd73b7050d14b8e",
    "verify.md": "sha256:6455a805e8ac58bfd3736ab86a6e43ac5b62a936d3a1d88857e37328243df072"
  },
  "generatedBy": "core"
}
