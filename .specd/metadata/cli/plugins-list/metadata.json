{
  "title": "cli:plugins-list",
  "description": "Defines the CLI contract for listing installed plugins. The command reads the plugin configuration and reports on each declared plugin's status.",
  "dependsOn": ["plugin-manager:load-plugin-use-case", "core:get-config"],
  "context": [
    "Defines the CLI contract for listing installed plugins. The command reads the plugin configuration and reports on each declared plugin's status."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "The command MUST accept an optional `--type` flag to filter by plugin type:\n\n```bash\nspecd plugins list [--type <type>]\n```"
      ]
    },
    {
      "requirement": "Declaration source",
      "rules": [
        "The command MUST enumerate declared plugins from the loaded `SpecdConfig.plugins` field (via `loadConfig` or an equivalent config snapshot). It MUST NOT call `kernel.project.listPlugins` or re-read `specd.yaml` through `ConfigWriter.listPlugins` when a config snapshot is already available.\n\nWhen `--type` is omitted, the command MUST default to type `agents`. When `--type <type>` is provided, the command MUST read only `config.plugins.<type>` (or an empty list when that type is absent)."
      ]
    },
    {
      "requirement": "Plugin status detection",
      "rules": [
        "For each plugin declared in `specd.yaml`:\n\n- **installed**: The plugin is in config and can be loaded via `LoadPlugin`.\n- **not\\_found**: The plugin is in config but the npm package is not installed.\n- **error**: The plugin is in config but fails to load (show error message)."
      ]
    },
    {
      "requirement": "Output format",
      "rules": [
        "The command MUST output:\n\n- Plugin name\n- Plugin type (from the type key in config)\n- Version (if available from the npm package)\n- Status (`installed`, `not_found`, or `error`)\n\nThe format MUST be machine-parseable when invoked with `--format json`."
      ]
    }
  ],
  "constraints": ["The command MUST NOT modify any state."],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "List all plugins",
      "when": ["specd plugins list is invoked"],
      "then": ["all declared plugins are listed"]
    },
    {
      "requirement": "Command signature",
      "name": "Filter by type",
      "when": ["specd plugins list --type agents is invoked"],
      "then": ["only plugins of type \"agents\" are listed"]
    },
    {
      "requirement": "Declaration source",
      "name": "Declarations read from loaded config snapshot",
      "given": ["plugins are declared in specd.yaml under plugins.agents"],
      "when": ["specd plugins list is invoked"],
      "then": [
        "the command enumerates plugins from the loaded SpecdConfig.plugins field",
        "AND it does not call kernel.project.listPlugins",
        "AND it does not call ConfigWriter.listPlugins"
      ]
    },
    {
      "requirement": "Declaration source",
      "name": "Default type is agents when --type omitted",
      "given": ["plugins.agents and plugins.other are both declared"],
      "when": ["specd plugins list is invoked without --type"],
      "then": ["only plugins under plugins.agents are considered"]
    },
    {
      "requirement": "Declaration source",
      "name": "Unknown type yields empty declaration list",
      "given": ["specd.yaml has no plugins.missing entries"],
      "when": ["specd plugins list --type missing is invoked"],
      "then": ["no plugins are enumerated for that type", "AND the command completes without error"]
    },
    {
      "requirement": "Plugin status detection",
      "name": "Plugin installed and loadable",
      "given": ["a plugin is in config and the npm package is installed"],
      "when": ["specd plugins list is invoked"],
      "then": ["status shows \"installed\""]
    },
    {
      "requirement": "Plugin status detection",
      "name": "Plugin not found",
      "given": ["a plugin is in config but npm package is not installed"],
      "when": ["specd plugins list is invoked"],
      "then": ["status shows \"not_found\""]
    },
    {
      "requirement": "Plugin status detection",
      "name": "Plugin load error",
      "given": ["a plugin is in config but fails to load"],
      "when": ["specd plugins list is invoked"],
      "then": ["status shows \"error\" with error message"]
    },
    {
      "requirement": "Output format",
      "name": "Text output includes plugin details",
      "when": ["specd plugins list is invoked"],
      "then": ["output includes plugin name, type, version, and status"]
    },
    {
      "requirement": "Output format",
      "name": "JSON output is machine-parseable",
      "when": ["specd plugins list --format json is invoked"],
      "then": ["output is machine-parseable JSON"]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "cli:plugins-list",
        "file": "cli:src/commands/plugins/get-declared-plugins.ts",
        "symbol": "getDeclaredPlugins"
      },
      {
        "specId": "cli:plugins-list",
        "file": "cli:src/commands/plugins/get-declared-plugins.ts",
        "symbol": "DeclaredPluginEntry"
      },
      {
        "specId": "cli:plugins-list",
        "file": "cli:src/commands/plugins/list.ts",
        "symbol": "registerPluginsList"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:86684fd0adf04cbe4deaad445f8328807f7291fad46fdc7907752034935044bb",
    "verify.md": "sha256:b2fd1f7b17687f334899f67d84cf508dbce0150ac11c5f4e6010c817f81729f1"
  },
  "generatedBy": "core"
}
