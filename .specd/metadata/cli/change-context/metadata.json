{
  "title": "Change Context",
  "description": "AI agents need a single command to retrieve relevant working context — spec content and project context entries — so they can operate on a change without manual assembly. `specd change context <name> <step>` compiles and prints the context block an agent receives when entering a lifecycle step for a named change. Lifecycle state and readiness are separate concerns retrieved through `specd change status`; artifact instructions and step hook instructions remain separate concerns retrieved via `specd change artifact-instruction` and `specd change hook-instruction` respectively.",
  "dependsOn": [
    "cli:entrypoint",
    "cli:change-spec-preview",
    "core:compile-context",
    "core:config",
    "core:get-artifact-instruction",
    "core:get-hook-instructions",
    "core:refresh-implementation-tracking"
  ],
  "context": [
    "AI agents need a single command to retrieve relevant working context — spec content and project context entries — so they can operate on a change without manual assembly. `specd change context <name> <step>` compiles and prints the context block an agent receives when entering a lifecycle step for a named change. Lifecycle state and readiness are separate concerns retrieved through `specd change status`; artifact instructions and step hook instructions remain separate concerns retrieved via `specd change artifact-instruction` and `specd change hook-instruction` respectively."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```\nspecd change context <name> <step>\n  [--rules] [--constraints] [--scenarios]\n  [--include-change-specs]\n  [--follow-deps [--depth <n>]]\n  [--optimized] [--no-optimized]\n  [--fingerprint <hash>]\n  [--format text|json|toon]\n```\n\n- `<name>` — required positional; the name of the change to compile context for\n- `<step>` — required positional; the lifecycle step being entered (e.g. `designing`, `implementing`, `verifying`)\n- `--rules` — when present, includes only the rules sections of full-mode spec content in the output\n- `--constraints` — when present, includes only the constraints sections of full-mode spec content in the output\n- `--scenarios` — when present, includes only the scenarios sections of full-mode spec content in the output\n- `--include-change-specs` — when present, directly includes the change's `specIds` in the collected context. By default, direct `specIds` inclusion is disabled; those specs may still appear if selected by include patterns or dependency traversal.\n- `--follow-deps` — when present, follows `dependsOn` links from `.specd-metadata.yaml` transitively to discover additional specs. By default (without this flag) `dependsOn` traversal is **not** performed.\n- `--depth <n>` — optional; only valid with `--follow-deps`; limits dependency traversal to N levels (1 = direct deps only); defaults to unlimited when `--follow-deps` is passed without `--depth`\n- `--optimized` — optional; force prefer optimized context\n- `--no-optimized` — optional; suppress preference for optimized context\n- `--fingerprint <hash>` — optional; when provided, the CLI compares this value against the current context fingerprint. If the fingerprint matches, returns `status: \"unchanged\"` without the full context. If omitted or the fingerprint does not match, returns the full context with the new fingerprint.\n- `--format text|json|toon` — optional; output format, defaults to `text`\n\nWhen none of `--rules`, `--constraints`, or `--scenarios` are passed, all available full-mode sections are included. When one or more are passed, only those sections appear in each full-mode spec's content block. Section flags have no effect on list-mode or summary-mode entries."
      ]
    },
    {
      "requirement": "Optimization warning signal",
      "rules": [
        "When `llmOptimizedContext: true` is active, if any spec in the context (or the project context itself) is missing optimized fields or has stale project metadata, the command SHALL surface a warning.\n\nThe warning MUST include an instruction on how to generate the missing metadata (e.g., using `specd-spec-metadata` or a project-level equivalent).\n\nFor structured formats (`json`, `toon`), the warning state and instructions SHALL be included alongside the context data."
      ]
    },
    {
      "requirement": "Implementation tracking refresh before context compilation",
      "rules": [
        "Before invoking `CompileContext`, the command MUST call `RefreshImplementationTracking` for the same change name.\n\nThe CLI MUST NOT invoke `ImplementationDetector` directly and MUST NOT duplicate detection merge logic.\n\nWhen `--fingerprint` short-circuits the command with an unchanged context response, refresh MUST still run before the fingerprint comparison so tracked implementation state is current for that check."
      ]
    },
    {
      "requirement": "Behaviour",
      "rules": [
        "The command invokes the `CompileContext` use case.\n\nThe CLI MUST NOT construct a `CompileContextConfig` object inline from `SpecdConfig`. Yaml-derived context configuration is baked into the kernel-wired `CompileContext` instance at composition time.\n\nThe CLI MUST pass only runtime overrides to `CompileContext.execute`:\n\n- `name` and `step` from positional arguments\n- `contextMode` from `--mode` or the CLI's effective-mode derivation (section flags may force `hybrid` when yaml default is not `full`/`hybrid`)\n- `llmOptimizedContext` only when explicitly resolved via `--optimized` or `--no-optimized` and the resolved value differs from the yaml default (omit the field when it matches the baked default)\n- `includeChangeSpecs`, `followDeps`, `depth`, `sections`, and `fingerprint` from the corresponding CLI flags\n\nThe effective `llmOptimizedContext` value for explicit CLI overrides is determined as follows:\n\n- If `--no-optimized` is passed, it is `false`.\n- If `--optimized` is passed, it is `true`.\n- Otherwise, the CLI does not pass `llmOptimizedContext` on `execute` — the baked yaml default applies.\n\nOptimization bypass when only a subset of sections is requested (for example `--rules` without `--constraints`) is enforced by `CompileContext` from the forwarded `sections` input and the baked `llmOptimizedContext` default. The CLI MUST NOT recompute or override `llmOptimizedContext` based on section flags.\n\nWhen `--include-change-specs` is omitted, the command passes `includeChangeSpecs: false`; the use case does not directly seed `change.specIds`, but those specs may still be included by include patterns or dependency traversal.\n\nWhen `--fingerprint` is provided, the CLI first checks whether the provided fingerprint matches the current context fingerprint calculated by `CompileContext`. If they match, the CLI returns a minimal response indicating unchanged status without the full context. If they do not match (or `--fingerprint` is omitted), the CLI returns the full context with the new fingerprint."
      ]
    },
    {
      "requirement": "Output",
      "rules": [
        "The CLI MUST assemble the final output from the structured `CompileContextResult` returned by the use case.\n\n**In `text` mode** (default):\n\n1. The first rendered line is `Context Fingerprint: <sha256...>`, before any project context or spec content.\n2. Project context entries are rendered next, each preceded by its source label (for example `**Source: <path>**` for file entries and `**Source: instruction**` for instruction entries). Entries are separated by `---`.\n3. Spec entries follow with explicit mode labels:\n   - **Full-mode specs** (`mode: 'full'`) are rendered under a `### Spec: <specId>` heading and MUST include an explicit mode label.\n   - **Summary-mode specs** (`mode: 'summary'`) are rendered under `## Available context specs` with spec ID, title, description, source, and an explicit summary label.\n   - **List-mode specs** (`mode: 'list'`) are rendered under `## Available context specs` with spec ID, source, and an explicit list label.\n   - Non-full sections include this instruction: `Use \\`specd change spec-preview <change-name> <specId>\\` to load the merged full content of any change spec you need.\\`\n   - Specs from `dependsOnTraversal` source MUST be visually distinguished from `includePattern` specs.\n\nWhen `--fingerprint` is provided and matches the current fingerprint, text mode still begins with `Context Fingerprint: <sha256...>` and then outputs a brief unchanged message. The full context is not printed.\n\n**In `json` or `toon` mode**, the output is the structured context-only result directly. List entries include `specId`, `source`, and `mode`. Summary entries additionally include `title` and `description`. Full entries additionally include `content`. The output MUST NOT include lifecycle state, requested-step availability, blocking artifacts, or per-step availability."
      ]
    },
    {
      "requirement": "Context warnings",
      "rules": [
        "Any warnings from the `CompileContext` use case (for example stale metadata, missing specs, or unknown workspaces) are printed to stderr as `warning:` lines. The context block is still printed to stdout and the process exits with code 0."
      ]
    },
    {
      "requirement": "Error cases",
      "rules": [
        "- If the change does not exist, exits with code 1.\n- If the schema cannot be resolved, exits with code 3."
      ]
    }
  ],
  "constraints": [
    "In text mode, the first line is Context Fingerprint: <sha256...>, before project context entries.",
    "In text mode, project context entries appear before spec entries.",
    "Text output MUST label each rendered spec entry explicitly as list, summary, or full content without relying on title rewriting.",
    "Non-full specs in text output MUST include a note instructing the agent to use specd change spec-preview <change-name> <specId> for merged full content when applicable.",
    "Summary-mode and list-mode specs from dependsOnTraversal MUST be rendered under a separate sub-heading from includePattern specs.",
    "Section flags apply only to full-mode spec content.",
    "All warnings go to stderr; the assembled output goes to stdout.",
    "dependsOn traversal is opt-in via --follow-deps; without the flag, deps are not followed.",
    "--include-change-specs is opt-in; without the flag, change.specIds are not direct seeds.",
    "--depth without --follow-deps is a CLI usage error (exit code 1).",
    "The CLI MUST NOT derive or render lifecycle readiness; callers MUST use change status for lifecycle state and blockers.",
    "Optimization warnings for missing or stale optimized fields are suppressed whenever the effective llmOptimizedContext is false."
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Missing step argument",
      "when": ["specd change context my-change is run"],
      "then": ["the command exits with code 1", "AND stderr contains a usage error"]
    },
    {
      "requirement": "Command signature",
      "name": "--mode flag accepted",
      "when": ["specd change context my-change designing --mode summary is run"],
      "then": ["the command proceeds normally and overrides the config mode"]
    },
    {
      "requirement": "Command signature",
      "name": "--depth without --follow-deps",
      "when": ["specd change context my-change designing --depth 2 is run without --follow-deps"],
      "then": ["the command exits with code 1 and prints a usage error to stderr"]
    },
    {
      "requirement": "Command signature",
      "name": "--fingerprint flag accepted",
      "when": ["specd change context my-change designing --fingerprint sha256:abc123... is run"],
      "then": ["the command proceeds normally"]
    },
    {
      "requirement": "Command signature",
      "name": "--include-change-specs flag accepted",
      "when": ["specd change context my-change designing --include-change-specs is run"],
      "then": ["the command proceeds normally and requests direct change-spec seeding"]
    },
    {
      "requirement": "Command signature",
      "name": "--optimized and --no-optimized flags accepted",
      "when": [
        "specd change context my-change designing --optimized is run",
        "specd change context my-change designing --no-optimized is run"
      ],
      "then": [
        "the command proceeds normally and forces optimization preference",
        "the command proceeds normally and suppresses optimization preference"
      ]
    },
    {
      "requirement": "Implementation tracking refresh before context compilation",
      "name": "Context command refreshes before CompileContext",
      "given": ["specd change context <name> <step> is executed"],
      "when": ["the command handler runs"],
      "then": ["it calls RefreshImplementationTracking before CompileContext"]
    },
    {
      "requirement": "Implementation tracking refresh before context compilation",
      "name": "Fingerprint short-circuit still refreshes first",
      "given": ["--fingerprint is provided"],
      "when": ["the command handler runs"],
      "then": ["it calls RefreshImplementationTracking before comparing fingerprints"]
    },
    {
      "requirement": "Output",
      "name": "Text output begins with fingerprint before context sections",
      "given": ["the current context fingerprint is sha256:abc123..."],
      "when": ["specd change context my-change designing --format text is called"],
      "then": ["the first rendered line is Context Fingerprint: sha256:abc123..."]
    },
    {
      "requirement": "Output",
      "name": "Text output shows unchanged message when fingerprint matches",
      "given": ["the current context fingerprint is sha256:abc123..."],
      "when": [
        "specd change context my-change designing --fingerprint sha256:abc123... --format text is called"
      ],
      "then": [
        "the output includes Context unchanged since last call.",
        "AND no spec content is printed"
      ]
    },
    {
      "requirement": "Output",
      "name": "Text output labels full-mode specs explicitly",
      "given": ["CompileContext returns a full-mode spec entry"],
      "when": ["specd change context <name> <step> --format text is called"],
      "then": ["the rendered block includes an explicit full-mode label"]
    },
    {
      "requirement": "Output",
      "name": "Text output labels summary-mode specs explicitly",
      "given": ["CompileContext returns a summary-mode spec entry"],
      "when": ["specd change context <name> <step> --format text is called"],
      "then": ["the rendered block includes an explicit summary-mode label"]
    },
    {
      "requirement": "Output",
      "name": "Text output labels list-mode specs explicitly",
      "given": ["CompileContext returns a list-mode spec entry"],
      "when": ["specd change context <name> <step> --format text is called"],
      "then": ["the rendered block includes an explicit list-mode label"]
    },
    {
      "requirement": "Output",
      "name": "Text output omits lifecycle availability",
      "given": ["the change has unavailable workflow steps"],
      "when": ["specd change context <name> <step> --format text is called"],
      "then": [
        "stdout contains no available-step section, lifecycle state, or blocker list",
        "AND stderr contains no lifecycle-availability warning"
      ]
    },
    {
      "requirement": "Output",
      "name": "Structured output omits lifecycle fields",
      "when": ["specd change context <name> <step> --format toon is called"],
      "then": [
        "the result contains context fingerprint, context entries, specs, and context warnings",
        "AND it omits stepAvailable, blockingArtifacts, and availableSteps"
      ]
    },
    {
      "requirement": "Output",
      "name": "Non-full output instructs spec-preview usage",
      "given": ["the output contains summary-mode or list-mode entries"],
      "when": ["text output is rendered"],
      "then": [
        "it includes guidance to run specd change spec-preview <change-name> <specId> for merged full content"
      ]
    },
    {
      "requirement": "Output",
      "name": "JSON output includes list-mode entries",
      "given": ["CompileContext returns mode: \"list\" entries"],
      "when": ["specd change context <name> <step> --format json is called"],
      "then": ["JSON includes those entries with mode and source fields"]
    },
    {
      "requirement": "Output",
      "name": "Section flags have no effect on list and summary entries",
      "given": ["the result is rendered in list mode or summary mode"],
      "when": ["--rules or --constraints are passed"],
      "then": ["output remains list/summary shaped without full content blocks"]
    },
    {
      "requirement": "Output",
      "name": "include-change-specs false still allows reinjection",
      "given": [
        "--include-change-specs is omitted",
        "AND a change spec matches include patterns or traversal"
      ],
      "when": ["the command is executed"],
      "then": ["that spec can still appear in emitted context entries"]
    },
    {
      "requirement": "Output",
      "name": "Full mode defaults to Description + Rules + Constraints",
      "given": ["a spec is rendered in full mode"],
      "when": ["specd change context is run without section flags"],
      "then": ["output includes Description, Rules, and Constraints for that spec"]
    },
    {
      "requirement": "Context warnings",
      "name": "Stale metadata warning",
      "given": ["a spec included in context has stale metadata"],
      "when": ["specd change context my-change designing is run"],
      "then": [
        "stderr contains a warning: line for the stale spec",
        "AND the context output is still printed to stdout",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Context warnings",
      "name": "dependsOn cycles do not produce warning lines",
      "given": ["dependency traversal encounters a dependsOn cycle while compiling context"],
      "when": ["specd change context my-change designing --follow-deps is run"],
      "then": [
        "stderr does not include a warning line solely for the cycle",
        "AND the command still returns the compiled context"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Change not found",
      "when": ["specd change context nonexistent designing is run"],
      "then": ["the command exits with code 1", "AND stderr contains an error: message"]
    },
    {
      "requirement": "Behaviour",
      "name": "CLI does not build CompileContextConfig inline",
      "when": ["specd change context is run"],
      "then": [
        "the command does not construct a CompileContextConfig object from SpecdConfig",
        "AND it invokes CompileContext.execute with runtime overrides only"
      ]
    },
    {
      "requirement": "Behaviour",
      "name": "CLI forwards traversal and fingerprint options to CompileContext",
      "when": [
        "specd change context is run with flags such as --include-change-specs, --follow-deps, --depth, or --fingerprint"
      ],
      "then": ["the command forwards those options into the CompileContext.execute input"]
    },
    {
      "requirement": "Behaviour",
      "name": "--no-optimized suppresses preference for optimized context",
      "given": ["llmOptimizedContext: true in configuration"],
      "when": ["specd change context my-change designing --no-optimized is executed"],
      "then": [
        "the CLI passes llmOptimizedContext: false as a runtime override on CompileContext.execute"
      ]
    },
    {
      "requirement": "Behaviour",
      "name": "--optimized forces preference for optimized context",
      "given": ["llmOptimizedContext: false in configuration"],
      "when": ["specd change context my-change designing --optimized is executed"],
      "then": [
        "the CLI passes llmOptimizedContext: true as a runtime override on CompileContext.execute"
      ]
    },
    {
      "requirement": "Behaviour",
      "name": "Section flags forwarded without CLI llmOptimizedContext override",
      "given": ["llmOptimizedContext: true in configuration"],
      "when": ["specd change context my-change designing --rules is executed"],
      "then": [
        "the CLI forwards sections: ['rules'] on CompileContext.execute",
        "AND the CLI does not pass llmOptimizedContext when it matches the yaml default",
        "AND optimization bypass behaviour is verified by core:compile-context"
      ]
    },
    {
      "requirement": "Optimization warning signal",
      "name": "Displays warnings when optimizations are missing",
      "given": [
        "llmOptimizedContext: true",
        "AND some specs (or the project) are missing optimization"
      ],
      "when": ["specd change context is run"],
      "then": [
        "standard CLI warnings are emitted",
        "AND they include remediation instructions for the agent"
      ]
    },
    {
      "requirement": "Optimization warning signal",
      "name": "Warnings suppressed when raw sections or --no-optimized are requested",
      "given": ["llmOptimizedContext: true", "AND some specs are missing optimization"],
      "when": [
        "specd change context my-change designing --no-optimized or specd change context my-change designing --rules is executed"
      ],
      "then": [
        "stale-optimization warnings for missing or stale spec optimized fields are suppressed"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "cli:change-context",
        "file": "cli:test/commands/change-context.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "cli:change-context",
        "file": "cli:src/commands/change/context.ts",
        "symbol": "registerChangeContext"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:ffc11b43019c37f89483db22bf064a846a4cebb3567384c0ae6b512cc1976611",
    "verify.md": "sha256:e0d007cd8d307daea0c071c1e66f267e953fae55e778e1ee54cfe99b85b03bdd"
  },
  "generatedBy": "core"
}
