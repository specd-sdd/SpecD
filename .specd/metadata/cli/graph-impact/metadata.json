{
  "title": "Graph Impact",
  "description": "Before modifying code, developers and agents need to understand the blast radius — which other files and symbols depend on the change. The `specd graph impact` command runs impact analysis on a file, symbol, or set of changed files, reporting risk level, dependency counts, and affected files.",
  "dependsOn": [
    "cli:entrypoint",
    "core:config",
    "code-graph:composition",
    "code-graph:traversal",
    "code-graph:workspace-integration"
  ],
  "context": [
    "Before modifying code, developers and agents need to understand the blast radius — which other files and symbols depend on the change. The `specd graph impact` command runs impact analysis on a file, symbol, or set of changed files, reporting risk level, dependency counts, and affected files."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```text\nspecd graph impact [--file <paths...>] [--symbol <name>] [--spec <id>] [--direction dependents|dependencies|upstream|downstream|both] [--depth <n>] [--config <path> | --path <path>] [--format text|json|toon]\n```\n\nExactly one of `--file`, `--symbol`, or `--spec` must be provided.\n\n- `--file` — analyze impact of one or more files. Each path MAY be workspace-prefixed, config-relative, or absolute.\n- `--symbol` — analyze impact of a symbol by name. If multiple symbols match, all are analyzed and results listed\n- `--spec` — analyze impact of one spec by fully-qualified spec id\n- `--direction` — optional; analysis direction, defaults to `upstream`. Values:\n  - `dependents` — find symbols, files, and specs that depend on the target; alias of `upstream`\n  - `dependencies` — find symbols, files, and specs that the target depends on; alias of `downstream`\n  - `upstream` — compatibility value for `dependents`\n  - `downstream` — compatibility value for `dependencies`\n  - `both` — combined dependents and dependencies analysis\n- `--depth` — optional; maximum traversal depth, defaults to `3`. Must be a positive integer. Passed through to `analyzeImpact`, `analyzeFileImpact`, or `analyzeSpecImpact` as `maxDepth`\n- `--config <path>` — optional; explicit path to `specd.yaml`, matching the standard CLI meaning\n- `--path <path>` — optional; repo-root bootstrap mode\n- `--format text|json|toon` — optional; output format, defaults to `text`\n\nThe CLI SHALL normalize `dependents` to `upstream` and `dependencies` to `downstream` before calling graph impact analysis. Invalid direction values SHALL fail with a CLI usage error before opening the graph provider.\n\nUser-facing documentation for this command MUST prefer the concrete aliases `dependents` and `dependencies` before the compatibility graph-theory terms `upstream` and `downstream`. Documentation MAY mention the compatibility values, but it MUST NOT describe `downstream` as dependents.\n\n`--config` and `--path` are mutually exclusive.\n\n`--path` and no-config fallback are bootstrap mechanisms for setup and early repository exploration, not the intended steady-state mode for configured projects."
      ]
    },
    {
      "requirement": "File impact analysis",
      "rules": [
        "When `--file` is provided:\n\n1. Resolves graph context using explicit config, autodetected config, or bootstrap mode according to the graph CLI precedence rules\n2. Creates a `CodeGraphProvider` from the resolved graph context\n3. Opens the provider\n4. Resolves each requested file path to canonical graph files through the provider's selector normalization service. It MUST support absolute paths, project-relative paths, and workspace-prefixed paths.\n5. For a single resolved file, calls `analyzeFileImpact(file, direction)` and outputs the `FileImpactResult`\n6. For multiple resolved files, computes per-file file impact with the same semantics as single-file analysis, then aggregates changed symbols, affected symbols, affected files, and overall risk across the requested files\n7. Outputs the aggregated result including changed-symbol detail and per-file impact breakdown\n8. Closes the provider and exits"
      ]
    },
    {
      "requirement": "Symbol impact analysis",
      "rules": [
        "When `--symbol` is provided:\n\n1. Resolves graph context using explicit config, autodetected config, or bootstrap mode according to the graph CLI precedence rules\n2. Searches for symbols matching the name via `findSymbols({ name })`\n3. If no symbol is found, outputs `No symbol found matching \"<name>\".`\n4. If one symbol is found, calls `analyzeImpact(symbolId, direction)` and outputs the result\n5. If multiple symbols match, analyzes each one and outputs all results"
      ]
    },
    {
      "requirement": "Spec impact analysis",
      "rules": [
        "When `--spec` is provided:\n\n1. Resolves graph context using explicit config, autodetected config, or bootstrap mode according to the graph CLI precedence rules\n2. Creates a `CodeGraphProvider` from the resolved graph context\n3. Opens the provider\n4. Calls `analyzeSpecImpact(specId, direction)`\n5. Outputs the resulting `ImpactResult`\n6. Closes the provider and exits\n\nSpec impact MUST reflect requirement-aware graph relations, not just code-structure traversal. At minimum it includes:\n\n- `DEPENDS_ON` between specs\n- `COVERS_FILE` between specs and files\n- `COVERS_SYMBOL` between specs and symbols"
      ]
    },
    {
      "requirement": "Concurrent indexing guard",
      "rules": [
        "Before attempting to open the provider or execute graph-backed analysis, `graph impact` SHALL check the shared graph indexing lock used by `graph index`.\n\nIf indexing is currently in progress, the command SHALL fail fast with a short user-facing retry-later message indicating that the graph is being indexed and should be queried again in a few seconds.\n\nThis guard exists so the command does not surface backend lock failures opportunistically while another CLI process is rebuilding the graph."
      ]
    },
    {
      "requirement": "Output format",
      "rules": [
        "All file paths rendered in the impact analysis output (both text and JSON) SHALL be formatted relative to the project root for consistency across multi-workspace projects.\n\n**File impact** in `text` mode:\n\n```text\nImpact analysis for src/auth.ts (depth=5)\n  Risk level:       HIGH\n  Direct deps:      6\n  Indirect deps:    3\n  Transitive deps:  1\n  Affected files:   8\n\nChanged symbols:\n  validate:10\n  AuthService:20\n\nAffected files:\n  packages/core/src/login.ts: handleLogin:12 (d=1), validateSession:45 (d=1)\n  packages/core/src/session.ts: createSession:8 (d=2)\n\nPer-symbol breakdown:\n  src/auth.ts:function:validate:10:0  risk=HIGH direct=4\n  src/auth.ts:class:AuthService:20:0  risk=MEDIUM direct=2\n```\n\nFor multiple files, text output SHALL show one aggregated summary plus a `Changed symbols:` block grouped by input file, an `Affected files:` block, and a `Per-file breakdown:` or equivalent structure that lets a human see which input files contributed to the overall result.\n\nWhen affected symbols are available, each file line shows the symbols grouped after a colon with depth indicators: `path: name:line (d=N), name:line (d=N)`. All paths are rendered relative to the project root. Files reached only via IMPORTS (file-level) are listed without symbols. The depth value `d=N` indicates the distance from the analysis target (1 = direct, 2 = indirect, 3+ = transitive).\n\nWhen `--direction` is omitted or set to `dependents` / `upstream`, human-facing documentation and help text SHOULD explain these counts as dependent counts. When `--direction dependencies` / `downstream` is used, documentation SHOULD explain that the same fields represent dependencies reached from the target.\n\nWhen `--depth` is not the default (3), the header line includes `(depth=N)` to show the configured depth.\n\n**Symbol impact** in `text` mode:\n\n```text\nImpact analysis for function createKernel (packages/core/src/composition/kernel.ts:147) (depth=5)\n  Risk level:       CRITICAL\n  Direct deps:      1\n  Indirect deps:    1\n  Transitive deps:  38\n  Affected files:   40\n\nAffected files:\n  packages/cli/src/kernel.ts: wireKernel:5 (d=1)\n  ...\n```\n\n**Spec impact** in `text` mode:\n\n```text\nImpact analysis for spec core:change (depth=3)\n  Risk level:       HIGH\n  Direct deps:      2\n  Indirect deps:    1\n  Transitive deps:  0\n  Affected files:   4\n```\n\nSpec impact output MUST surface files reached through `COVERS_FILE` and symbols reached through `COVERS_SYMBOL` in the same result structure used by other impact targets.\n\nWhen multiple symbols match, each analysis is listed sequentially.\n\nIn `json` or `toon` mode, the full result object is object as-is. The `AffectedSymbol` entries in JSON include the `depth` field.\n\nThe JSON output MUST include aggregate impact fields to support automated risk assessment:\n\n- `riskLevel`\n- `directDepsCount`\n- `indirectDepsCount`\n- `transitiveDepsCount`\n- `affectedFilesCount`"
      ]
    },
    {
      "requirement": "Error cases",
      "rules": [
        "Exactly one of `--file`, `--symbol`, or `--spec` must be provided. If none or more than one is given, the command exits with code 1 and prints `error: provide exactly one of --file, --symbol, or --spec`.\n\nIf both `--config` and `--path` are passed, the command SHALL fail with a CLI error before attempting graph access.\n\nIf a `--file` input without a workspace prefix does not resolve by config-relative path, the command SHALL fail with a not-found error that includes the normalized config-relative path it searched.\n\nIf a `--file` input without a workspace prefix resolves to more than one canonical graph file, the command SHALL fail with an ambiguity error listing the matching workspace-prefixed paths rather than guessing a workspace.\n\nIf `--spec` references no indexed spec node, the command SHALL fail with a not-found error that includes the requested spec id.\n\nIf the shared graph indexing lock is present, the command SHALL exit with code 3 after printing a user-facing retry-later message.\n\nIf the provider cannot be opened, the command exits with code 3."
      ]
    }
  ],
  "constraints": [
    "The CLI does not contain impact analysis logic — it delegates entirely to @specd/code-graph",
    "process.exit(0) is called explicitly after closing the provider",
    "Canonical graph file identities are workspace-prefixed, but user-facing --file inputs MAY be workspace-prefixed, config-relative, or absolute",
    "--direction applies to --file, --symbol, and --spec",
    "--depth applies to all selectors",
    "--depth must be a positive integer; invalid values exit with code 1",
    "Context resolution SHALL use the shared graph CLI model rather than command-local path semantics",
    "The command checks the shared graph indexing lock before opening the provider and fails fast while indexing is in progress"
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Explicit config path bypasses discovery",
      "given": ["the current directory would autodiscover a different specd.yaml"],
      "when": ["specd graph impact --file src/auth.ts --config /tmp/other/specd.yaml is run"],
      "then": ["the command uses /tmp/other/specd.yaml directly"]
    },
    {
      "requirement": "Command signature",
      "name": "Explicit path enters bootstrap mode",
      "given": ["a specd.yaml exists under the current repository"],
      "when": ["specd graph impact --file src/auth.ts --path /tmp/repo is run"],
      "then": [
        "config discovery is ignored",
        "AND the command analyzes impact against a synthetic single workspace default rooted at /tmp/repo"
      ]
    },
    {
      "requirement": "Command signature",
      "name": "Invalid direction fails before provider access",
      "when": ["specd graph impact --file src/auth.ts --direction sideways is run"],
      "then": [
        "the command exits with code 1",
        "AND stderr explains that the direction value is invalid",
        "AND no graph provider is opened"
      ]
    },
    {
      "requirement": "Command signature",
      "name": "Removed --changes flag is rejected",
      "when": ["specd graph impact --changes src/auth.ts is run"],
      "then": [
        "the command exits with code 1",
        "AND stderr does not present --changes as a supported selector"
      ]
    },
    {
      "requirement": "Command signature",
      "name": "Spec selector enters requirement impact mode",
      "given": ["spec core:change is indexed in the graph"],
      "when": ["specd graph impact --spec core:change is run"],
      "then": [
        "the command analyzes requirement impact for that spec",
        "AND no file or symbol selector is required"
      ]
    },
    {
      "requirement": "File impact analysis",
      "name": "Upstream file analysis with defaults",
      "given": ["core:src/auth.ts is indexed in the graph with upstream dependents"],
      "when": ["specd graph impact --file core:src/auth.ts is run"],
      "then": [
        "stdout shows Impact analysis for core:src/auth.ts with risk level, dependency counts, affected files with their affected symbols, and per-symbol breakdown",
        "AND the analysis direction is upstream",
        "AND the default depth is 3",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "File impact analysis",
      "name": "Unprefixed relative file resolves through configRelativePath",
      "given": [
        "file core:src/auth.ts is indexed with configRelativePath packages/core/src/auth.ts"
      ],
      "when": [
        "specd graph impact --file packages/core/src/auth.ts is run from the same configured project"
      ],
      "then": [
        "the command resolves the canonical graph file core:src/auth.ts",
        "AND runs file impact analysis for that canonical file"
      ]
    },
    {
      "requirement": "File impact analysis",
      "name": "Absolute file path normalizes before lookup",
      "given": [
        "file core:src/auth.ts is indexed with configRelativePath packages/core/src/auth.ts"
      ],
      "when": ["specd graph impact --file /repo/packages/core/src/auth.ts is run"],
      "then": [
        "the command normalizes the absolute path to packages/core/src/auth.ts",
        "AND resolves the canonical graph file core:src/auth.ts"
      ]
    },
    {
      "requirement": "File impact analysis",
      "name": "Ambiguous unprefixed selector fails with canonical matches",
      "given": ["two indexed files share config-relative path src/index.ts"],
      "when": ["specd graph impact --file src/index.ts is run"],
      "then": [
        "the command exits with code 1",
        "AND stderr lists the matching canonical workspace-prefixed files"
      ]
    },
    {
      "requirement": "File impact analysis",
      "name": "Multi-file analysis aggregates file impact semantics",
      "given": ["core:src/auth.ts and cli:src/auth.ts are indexed with dependents"],
      "when": ["specd graph impact --file core:src/auth.ts cli:src/auth.ts is run"],
      "then": [
        "the command analyzes each file with file-impact semantics",
        "AND aggregates changed symbols, affected symbols, affected files, and overall risk across both files"
      ]
    },
    {
      "requirement": "File impact analysis",
      "name": "File selectors resolved via provider normalization",
      "when": ["specd graph impact --file is run with an absolute or project-relative path"],
      "then": [
        "it resolves correctly to the canonical graph identity through the provider",
        "AND analysis proceeds without error"
      ]
    },
    {
      "requirement": "Symbol impact analysis",
      "name": "Single symbol match",
      "given": ["createKernel exists once in the graph"],
      "when": ["specd graph impact --symbol createKernel is run"],
      "then": [
        "stdout shows Impact analysis for function createKernel (...) with risk level and affected files"
      ]
    },
    {
      "requirement": "Symbol impact analysis",
      "name": "Multiple symbol matches",
      "given": ["validate exists in 3 different files"],
      "when": ["specd graph impact --symbol validate is run"],
      "then": [
        "stdout shows 3 symbols match \"validate\": followed by separate impact reports for each"
      ]
    },
    {
      "requirement": "Symbol impact analysis",
      "name": "Symbol not found",
      "given": ["nonExistentSymbol does not exist in the graph"],
      "when": ["specd graph impact --symbol nonExistentSymbol is run"],
      "then": [
        "stdout shows No symbol found matching \"nonExistentSymbol\".",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Symbol impact analysis",
      "name": "Custom depth for symbol analysis",
      "given": ["createKernel exists with transitive callers at depth 5"],
      "when": ["specd graph impact --symbol createKernel --depth 5 is run"],
      "then": [
        "the analysis includes dependents up to depth 5",
        "AND each affected symbol in the text output shows (d=N) with its depth"
      ]
    },
    {
      "requirement": "Spec impact analysis",
      "name": "Downstream spec analysis shows covered files and symbols",
      "given": [
        "spec core:change covers file core:src/change.ts and symbol core:Change.transition"
      ],
      "when": ["specd graph impact --spec core:change --direction downstream is run"],
      "then": ["stdout shows the covered file and covered symbol in the impacted result"]
    },
    {
      "requirement": "Spec impact analysis",
      "name": "Upstream spec analysis shows dependent specs",
      "given": ["spec core:archive-change depends on core:spec-lock"],
      "when": ["specd graph impact --spec core:spec-lock --direction upstream is run"],
      "then": ["stdout shows core:archive-change as an impacted spec"]
    },
    {
      "requirement": "Spec impact analysis",
      "name": "Missing spec reports cleanly",
      "given": ["spec missing:spec does not exist in the graph"],
      "when": ["specd graph impact --spec missing:spec is run"],
      "then": [
        "stdout shows No spec found matching \"missing:spec\".",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Concurrent indexing guard",
      "name": "Impact analysis fails fast while the indexing lock is present",
      "given": ["a graph index process currently holds the shared graph indexing lock"],
      "when": ["specd graph impact --file src/auth.ts is run"],
      "then": [
        "the command exits with code 3 before opening the provider",
        "AND it prints a short retry-later message explaining that the graph is currently being indexed"
      ]
    },
    {
      "requirement": "Output format",
      "name": "Text output shows risk level and counts",
      "given": [
        "core:src/auth.ts has risk level HIGH with 6 direct, 3 indirect, and 1 transitive dependents"
      ],
      "when": ["specd graph impact --file core:src/auth.ts is run"],
      "then": ["stdout contains Risk level:       HIGH", "AND stdout contains Direct deps:      6"]
    },
    {
      "requirement": "Output format",
      "name": "Text output shows changed symbols for file impact",
      "when": ["specd graph impact --file core:src/auth.ts is run"],
      "then": ["text output includes a Changed symbols: block before the affected files list"]
    },
    {
      "requirement": "Output format",
      "name": "Multi-file text output shows grouped changed symbols",
      "when": ["specd graph impact --file core:src/auth.ts cli:src/user.ts is run"],
      "then": [
        "text output includes one aggregated summary",
        "AND a Changed symbols: block grouped by input file",
        "AND a per-file breakdown section"
      ]
    },
    {
      "requirement": "Output format",
      "name": "JSON output includes changedSymbols for file impact",
      "when": ["specd graph impact --file core:src/auth.ts --format json is run"],
      "then": ["stdout is valid JSON containing changedSymbols, affectedSymbols, and affectedFiles"]
    },
    {
      "requirement": "Output format",
      "name": "JSON output for symbol impact",
      "when": ["specd graph impact --symbol createKernel --format json is run"],
      "then": ["stdout is valid JSON containing symbol and impact objects"]
    },
    {
      "requirement": "Output format",
      "name": "JSON output includes aggregate impact fields",
      "when": ["specd graph impact --file <path> --format json is run"],
      "then": [
        "stdout is valid JSON",
        "AND it contains riskLevel, directDepsCount, indirectDepsCount, transitiveDepsCount, and affectedFilesCount"
      ]
    },
    {
      "requirement": "Output format",
      "name": "Impact paths are rendered relative to project root",
      "when": ["specd graph impact is run"],
      "then": [
        "all file paths in the output (e.g., packages/core/src/index.ts) are relative to the project root",
        "AND they do not include workspace-prefixed identities unless explicitly requested"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "No selector provided",
      "when": ["specd graph impact is run without --file, --symbol, or --spec"],
      "then": [
        "stderr contains error: provide exactly one of --file, --symbol, or --spec",
        "AND the process exits with code 1"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Multiple selectors provided",
      "when": ["specd graph impact --file core:src/auth.ts --spec core:change is run"],
      "then": [
        "stderr contains error: provide exactly one of --file, --symbol, or --spec",
        "AND the process exits with code 1"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Missing unprefixed selector reports normalized lookup",
      "when": ["specd graph impact --file packages/core/src/missing.ts is run"],
      "then": [
        "the command exits with code 1",
        "AND stderr includes the normalized config-relative path that was searched"
      ]
    },
    {
      "requirement": "Error cases",
      "name": "Mutually exclusive context flags fail fast",
      "when": ["specd graph impact --file core:src/auth.ts --config ./specd.yaml --path . is run"],
      "then": ["the command exits with code 1 before any graph provider is opened"]
    },
    {
      "requirement": "Error cases",
      "name": "Infrastructure error exits with code 3",
      "given": ["the provider cannot be opened"],
      "when": ["specd graph impact --file core:src/auth.ts is run"],
      "then": [
        "stderr contains a fatal: prefixed error message",
        "AND the process exits with code 3"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "cli:graph-impact",
        "file": "cli:test/commands/graph-impact.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "cli:graph-impact",
        "file": "cli:src/commands/graph/impact.ts",
        "symbol": "registerGraphImpact"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:c8a38d655845b350409d48738b1e8bfc86090c1412ae979ac0292ddcd6607cad",
    "verify.md": "sha256:49a24eb65a442ab111f05a5d47236b0fbaedc30ab8a31c904c9d93d3432a8a5a"
  },
  "generatedBy": "core"
}
