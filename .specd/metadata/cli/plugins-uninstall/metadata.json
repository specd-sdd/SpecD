{
  "title": "cli:plugins-uninstall",
  "description": "Defines the CLI contract for uninstalling plugins. The command calls the plugin's uninstall hook and removes the plugin declaration from configuration.",
  "dependsOn": [
    "plugin-manager:uninstall-plugin-use-case",
    "plugin-manager:load-plugin-use-case",
    "core:composition",
    "core:config-writer-port"
  ],
  "context": [
    "Defines the CLI contract for uninstalling plugins. The command calls the plugin's uninstall hook and removes the plugin declaration from configuration."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "The command MUST accept one or more plugin names as positional arguments:\n\n```bash\nspecd plugins uninstall <plugin> [<plugin>...]\n```"
      ]
    },
    {
      "requirement": "Uninstall workflow",
      "rules": [
        "For each plugin name:\n\n1. The command MUST load the plugin via `LoadPlugin` use case.\n2. The command MUST call the plugin's `uninstall()` method.\n3. The command MUST call `createConfigWriter().removePlugin(configPath, type, name)` to remove from `specd.yaml`.\n\nThe command MUST NOT call `kernel.project.removePlugin`."
      ]
    },
    {
      "requirement": "Exit code",
      "rules": [
        "The command MUST exit with code 1 if any plugin fails, but continue processing the remaining plugins."
      ]
    },
    {
      "requirement": "Output",
      "rules": [
        "For each plugin, the command MUST output:\n\n- Plugin name\n- Uninstall status (`uninstalled` or error message)\n\nThe format MUST be machine-parseable when invoked with `--format json`."
      ]
    }
  ],
  "constraints": [
    "The command MUST remove the plugin declaration from config on success.",
    "The command MUST call the plugin's uninstall hook before removing from config."
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Uninstall multiple plugins",
      "when": [
        "specd plugins uninstall @specd/plugin-agent-claude @specd/plugin-agent-copilot is invoked"
      ],
      "then": ["both plugins are uninstalled"]
    },
    {
      "requirement": "Exit code",
      "name": "Uninstall failure",
      "when": ["any plugin fails to uninstall"],
      "then": ["the command exits with code 1", "AND continues processing the remaining plugins"]
    },
    {
      "requirement": "Uninstall workflow",
      "name": "Uninstall calls plugin hook and removes from config",
      "when": ["specd plugins uninstall @specd/plugin-agent-claude is invoked"],
      "then": [
        "the plugin is loaded via LoadPlugin use case",
        "AND the plugin's uninstall() method is called",
        "AND createConfigWriter().removePlugin is called to remove the plugin from specd.yaml",
        "AND kernel.project.removePlugin is not called"
      ]
    },
    {
      "requirement": "Output",
      "name": "Machine-parseable JSON output",
      "when": ["specd plugins uninstall @specd/plugin-agent-claude --format json is invoked"],
      "then": ["output is machine-parseable JSON with plugin name and status"]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "cli:plugins-uninstall",
        "file": "cli:test/commands/plugins.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "cli:plugins-uninstall",
        "file": "cli:src/commands/plugins/uninstall.ts",
        "symbol": "registerPluginsUninstall"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:2c0cda8891c1da7f55bcf6daba0f4faeb87b495c362c1d538bff46ac1ed35878",
    "verify.md": "sha256:a8f01c91c4a04fe2f45b2f5a00c3951dd533adede5cf47907466ca9a2c8693e3"
  },
  "generatedBy": "core"
}
