{
  "title": "Change List",
  "description": "Teams need a quick overview of all in-flight work to coordinate and avoid conflicts. `specd change list` lists all active changes in the project (those in `changes/`), sorted by creation date.",
  "dependsOn": ["cli:cli/entrypoint", "core:core/change"],
  "context": [
    "Teams need a quick overview of all in-flight work to coordinate and avoid conflicts. `specd change list` lists all active changes in the project (those in `changes/`), sorted by creation date."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "```\nspecd change list [--format text|json|toon]\n```\n\n- `--format text|json|toon` — optional; output format, defaults to `text`"
      ]
    },
    {
      "requirement": "Output format",
      "rules": [
        "The command prints a human-readable table to stdout showing only active changes (in `changes/`). Changes are sorted by `createdAt` ascending (oldest first).\n\nThe output has an inverse-video column header row `NAME  STATE  SPECS  SCHEMA` above the data rows. Column widths are fixed at render time, computed from the widest value across all rows for each column (global, not per-group).\n\nEach data row shows:\n\n```\n  <name>  <state>  <specIds>  <schema-name>@<schema-version>\n```\n\nwhere `<specIds>` is a comma-separated list of the spec IDs on the change.\n\nWhen a description is set on the change, a dim indented description line is printed below the main row:\n\n```\n    <description>\n```\n\nDescription sub-rows are NOT part of the column structure — they are printed outside the fixed-width grid.\n\nIn `json` or `toon` format, the output is an array of objects:\n\n```json\n[{\"name\":\"...\",\"state\":\"...\",\"specIds\":[...],\"schema\":{\"name\":\"...\",\"version\":N},\"description\":\"...\"}]\n```\n\n`description` is omitted from the JSON object when not set."
      ]
    },
    {
      "requirement": "Empty output",
      "rules": [
        "If there are no active changes, the command prints `no changes` to stdout in `text` mode, or `[]` as valid JSON in `json`/`toon` mode. The process exits with code 0."
      ]
    }
  ],
  "constraints": [
    "Only active changes (in changes/) are listed; drafted and discarded changes have their own noun groups",
    "No filtering by state or workspace in v1"
  ],
  "scenarios": [
    {
      "requirement": "Output format",
      "name": "Only active changes shown",
      "given": ["the project has one active change in changes/ and one drafted change in drafts/"],
      "when": ["specd change list is run"],
      "then": ["stdout shows only the active change", "AND the drafted change is not shown"]
    },
    {
      "requirement": "Output format",
      "name": "Discarded changes not shown",
      "given": ["the project has one discarded change in discarded/ and no active changes"],
      "when": ["specd change list is run"],
      "then": ["stdout does not show the discarded change", "AND stdout contains no changes"]
    },
    {
      "requirement": "Output format",
      "name": "Rows contain name, state, specIds, and schema",
      "given": [
        "a change named add-login in state designing with specIds ['auth/login'] and schema std version 1"
      ],
      "when": ["specd change list is run"],
      "then": ["the row for add-login shows add-login, designing, auth/login, and std@1"]
    },
    {
      "requirement": "Output format",
      "name": "JSON format output",
      "given": [
        "one active change add-login in state designing with specId auth/login and schema std version 1"
      ],
      "when": ["specd change list --format json is run"],
      "then": [
        "stdout is a valid JSON array containing one object with name, state, specIds, and schema",
        "AND schema has name equal to \"std\" and version equal to 1",
        "AND the process exits with code 0"
      ]
    },
    {
      "requirement": "Empty output",
      "name": "No active changes — text mode",
      "when": ["specd change list is run with no active changes in changes/"],
      "then": ["stdout contains no changes", "AND the process exits with code 0"]
    },
    {
      "requirement": "Empty output",
      "name": "No active changes — JSON mode",
      "when": ["specd change list --format json is run with no active changes"],
      "then": ["stdout is []", "AND the process exits with code 0"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:f8384d969888dee9029d106de15149912becfa2d75b96a290f6ecdbac2075b70",
    "verify.md": "sha256:7a089cf30a3c0b8fbe7b83de9863d70735f49b55598f0bd8721d9ad4def7998b"
  },
  "generatedBy": "core"
}
