{
  "title": "cli:plugins-update",
  "description": "Defines the CLI contract for updating installed plugins. The command reinstalls plugins to pick up new versions or configuration changes.",
  "dependsOn": ["plugin-manager:update-plugin-use-case", "core:get-config"],
  "context": [
    "Defines the CLI contract for updating installed plugins. The command reinstalls plugins to pick up new versions or configuration changes."
  ],
  "rules": [
    {
      "requirement": "Command signature",
      "rules": [
        "The command MUST accept optional plugin names as positional arguments:\n\n```bash\nspecd plugins update [<plugin>...]\n```"
      ]
    },
    {
      "requirement": "Declaration source",
      "rules": [
        "The command MUST enumerate declared plugins from the loaded `SpecdConfig.plugins` field (via `loadConfig` or an equivalent config snapshot). It MUST NOT call `kernel.project.listPlugins` or `ConfigWriter.listPlugins` when a config snapshot is already available.\n\nWhen no plugin names are provided, the command MUST derive the update set from all declared plugin buckets (`plugins.agents` and `plugins.ui`).\n\nWhen plugin names are provided, the command MUST resolve each name against declared plugins in any supported bucket before updating."
      ]
    },
    {
      "requirement": "Update behavior",
      "rules": [
        "- **Without arguments**: Updates all declared plugins in `specd.yaml` across supported type buckets (`agents` and `ui`).\n- **With plugin names**: Updates only the specified plugins when declared in any supported bucket.\n- The command MUST be idempotent — running update multiple times produces the same result.\n- The command MUST NOT mutate the plugin configuration in `specd.yaml`."
      ]
    },
    {
      "requirement": "UI plugin update enumeration",
      "rules": [
        "When no plugin names are provided, the command MUST include plugins declared under `plugins.ui` in the update set alongside `plugins.agents`.\n\nWhen plugin names are provided, the command MUST resolve each name against declared plugins in `plugins.agents` or `plugins.ui` before updating."
      ]
    },
    {
      "requirement": "Exit code",
      "rules": ["The command MUST exit with code 1 if any plugin fails to update."]
    },
    {
      "requirement": "Output",
      "rules": [
        "For each plugin, the command MUST output:\n\n- Plugin name\n- Update status (`updated`, `skipped`, or error message)\n\nThe format MUST be machine-parseable when invoked with `--format json`."
      ]
    }
  ],
  "constraints": [
    "The command MUST be idempotent.",
    "The command MUST NOT add or remove plugins from config."
  ],
  "scenarios": [
    {
      "requirement": "Command signature",
      "name": "Update all plugins",
      "when": ["specd plugins update is invoked with no arguments"],
      "then": ["all declared plugins are updated"]
    },
    {
      "requirement": "Command signature",
      "name": "Update specific plugins",
      "when": ["specd plugins update @specd/plugin-agent-claude is invoked"],
      "then": ["only the specified plugin is updated"]
    },
    {
      "requirement": "Declaration source",
      "name": "Update-all uses config.plugins.agents",
      "given": ["multiple plugins are declared under plugins.agents in the loaded config"],
      "when": ["specd plugins update is invoked with no arguments"],
      "then": [
        "all plugins from config.plugins.agents are selected for update",
        "AND the command does not call kernel.project.listPlugins"
      ]
    },
    {
      "requirement": "Declaration source",
      "name": "Declaration read does not use ConfigWriter.listPlugins",
      "given": ["a loaded SpecdConfig snapshot is available"],
      "when": ["specd plugins update resolves the declared plugin set"],
      "then": ["it reads config.plugins in memory", "AND it does not call ConfigWriter.listPlugins"]
    },
    {
      "requirement": "Exit code",
      "name": "Update failure",
      "when": ["any plugin fails to update"],
      "then": ["the command exits with code 1", "AND continues processing the remaining plugins"]
    },
    {
      "requirement": "Update behavior",
      "name": "Update all plugins when no arguments provided",
      "when": ["specd plugins update is invoked with no arguments"],
      "then": ["all declared plugins in specd.yaml are updated"]
    },
    {
      "requirement": "Update behavior",
      "name": "Update behavior is idempotent",
      "given": ["specd plugins update @specd/plugin-agent-claude has been run successfully"],
      "when": ["specd plugins update @specd/plugin-agent-claude is run again"],
      "then": [
        "the command succeeds and does not produce an error",
        "AND specd.yaml is not modified"
      ]
    },
    {
      "requirement": "UI plugin update enumeration",
      "name": "Update all includes plugins.ui",
      "given": [
        "@specd/plugin-ui-studio is declared under plugins.ui",
        "AND @specd/plugin-agent-claude is declared under plugins.agents"
      ],
      "when": ["specd plugins update is invoked with no plugin names"],
      "then": ["both plugins are updated"]
    },
    {
      "requirement": "UI plugin update enumeration",
      "name": "Update single UI plugin by name",
      "given": ["@specd/plugin-ui-studio is declared under plugins.ui"],
      "when": ["specd plugins update @specd/plugin-ui-studio is invoked"],
      "then": ["UpdatePlugin is called for @specd/plugin-ui-studio"]
    },
    {
      "requirement": "Output",
      "name": "Machine-parseable JSON output",
      "when": ["specd plugins update --format json is invoked"],
      "then": ["output is machine-parseable JSON with plugin names and status"]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "cli:plugins-update",
        "file": "cli:test/commands/plugins-update.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "cli:plugins-update",
        "file": "cli:src/commands/plugins/update.ts",
        "symbol": "updatePluginsWithKernel"
      },
      {
        "specId": "cli:plugins-update",
        "file": "cli:src/commands/plugins/plugin-bucket.ts",
        "symbol": "listDeclaredPlugins"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:d00fd2bb077872b027bfdf6d6b15320159997be40bf6cdded59ce832dd675409",
    "verify.md": "sha256:f8f7a45f1b96624da64f10d0db24532bc9f96a6c4bc9bbe130bf6164ac047d12"
  },
  "generatedBy": "core"
}
