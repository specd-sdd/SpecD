{
  "title": "plugin-manager:agent-plugin-type",
  "description": "Defines the agent plugin interface that extends `SpecdPlugin` for agent integrations (Claude, Copilot, Codex, etc.).",
  "dependsOn": ["core:config", "plugin-manager:specd-plugin-type"],
  "context": [
    "Defines the agent plugin interface that extends `SpecdPlugin` for agent integrations (Claude, Copilot, Codex, etc.)."
  ],
  "rules": [
    {
      "requirement": "AgentPlugin extends SpecdPlugin",
      "rules": [
        "`AgentPlugin` MUST extend `SpecdPlugin` with `type: 'agent'` and add:\n\n- `install(config: SpecdConfig, options?: AgentInstallOptions): Promise<AgentInstallResult>`\n- `uninstall(config: SpecdConfig, options?: AgentInstallOptions): Promise<void>`\n\nThe `AgentPlugin` SHALL receive the full `SpecdConfig` object to ensure it has access to the project root and any necessary workspace or storage configuration."
      ]
    },
    {
      "requirement": "AgentInstallOptions",
      "rules": [
        "```typescript\ntype TemplateVariable =\n  | string\n  | number\n  | boolean\n  | readonly TemplateVariable[]\n  | { readonly [key: string]: TemplateVariable }\n\ninterface AgentInstallOptions {\n  skills?: string[] // specific skill names; omit = all\n  variables?: Record<string, TemplateVariable> // recursive template variables, including variables.frontmatter and variables.sharedFolder\n  capabilities?: string[] // capability identifiers supported by the target runtime\n}\n```\n\n`capabilities` MUST be supplied as a simple collection of capability identifiers by the agent plugin at install time. The initial required capability identifiers are `mcp`, `agents`, and `frontmatter`.\n\nTheir initial required meanings are:\n\n- `mcp`: the target runtime supports MCP-oriented template branches\n- `agents`: the target runtime supports delegated-agent-oriented template branches\n- `frontmatter`: the target runtime expects frontmatter to be composed and inserted from `variables.frontmatter`\n\nFrontmatter data MUST be passed through `variables.frontmatter`. Plugins MUST NOT pass a prebuilt YAML frontmatter document through `AgentInstallOptions`.\n\n`variables.sharedFolder`, when provided, MUST be relative to the project root and is subject to normalization and validation by `@specd/skills`."
      ]
    },
    {
      "requirement": "AgentInstallResult",
      "rules": [
        "```typescript\ninterface AgentInstallResult {\n  installed: Array<{ skill: string; path: string }>\n  skipped: Array<{ skill: string; reason: string }>\n}\n```"
      ]
    },
    {
      "requirement": "isAgentPlugin type guard",
      "rules": [
        "The spec MUST export a pure function `isAgentPlugin(value: SpecdPlugin): value is AgentPlugin` that validates:\n\n1. `value.type` is `'agent'`\n2. `value.install` is a function\n3. `value.uninstall` is a function\n\nThis type guard lives in the domain layer so that application use cases and infrastructure adapters can import it without creating layer violations."
      ]
    }
  ],
  "constraints": [
    "Agent plugins specialize the base plugin type for agent integrations.",
    "Install-time capability data MUST be passed separately from template variables.",
    "The initial required capability identifiers are mcp, agents, and frontmatter.",
    "Agent plugins MUST NOT pass pre-normalized capability objects into @specd/skills.",
    "Install-time frontmatter data MUST be passed through variables.frontmatter, not as a prebuilt YAML document.",
    "Agent plugins MAY pass variables.sharedFolder, but @specd/skills owns its normalization and safety validation."
  ],
  "scenarios": [
    {
      "requirement": "AgentPlugin extends SpecdPlugin",
      "name": "Has install and uninstall",
      "given": ["a valid SpecdConfig is provided"],
      "when": ["install(config, options) or uninstall(config, options) is called"],
      "then": ["the plugin executes the requested operation using the provided configuration"]
    },
    {
      "requirement": "isAgentPlugin type guard",
      "name": "Rejects plugin without install method",
      "given": ["a SpecdPlugin with type: 'agent' but no install method"],
      "when": ["isAgentPlugin is called"],
      "then": ["it returns false"]
    },
    {
      "requirement": "isAgentPlugin type guard",
      "name": "Rejects plugin with wrong type",
      "given": ["a SpecdPlugin with install and uninstall methods but type is not 'agent'"],
      "when": ["isAgentPlugin is called"],
      "then": ["it returns false"]
    },
    {
      "requirement": "AgentInstallOptions",
      "name": "Options include skills filter, recursive variables, and capability identifiers",
      "when": ["AgentInstallOptions is used"],
      "then": [
        "it accepts an optional skills array",
        "AND it accepts optional recursive variables",
        "AND it accepts optional capabilities as a list of strings"
      ]
    },
    {
      "requirement": "AgentInstallOptions",
      "name": "Initial capability identifiers are required by the contract",
      "when": ["the install-time agent capability contract is reviewed"],
      "then": [
        "it defines mcp, agents, and frontmatter as the initial required capability identifiers"
      ]
    },
    {
      "requirement": "AgentInstallOptions",
      "name": "Plugins do not pass pre-normalized capability objects",
      "when": ["an agent plugin prepares install options"],
      "then": [
        "it passes capability identifiers only",
        "AND it does not pass pre-normalized capability objects into @specd/skills"
      ]
    },
    {
      "requirement": "AgentInstallOptions",
      "name": "Frontmatter data travels through variables.frontmatter",
      "when": ["an agent plugin prepares install options"],
      "then": [
        "frontmatter data is stored under variables.frontmatter",
        "AND nested frontmatter data remains addressable as template variables"
      ]
    },
    {
      "requirement": "AgentInstallOptions",
      "name": "sharedFolder travels through variables.sharedFolder",
      "when": ["an agent plugin overrides the shared template install location"],
      "then": [
        "it passes that value through variables.sharedFolder",
        "AND the value remains relative to the project root"
      ]
    },
    {
      "requirement": "AgentInstallResult",
      "name": "Result tracks installed and skipped skills",
      "when": ["AgentInstallResult is returned"],
      "then": [
        "it contains installed array with skill and path",
        "AND skipped array with skill and reason"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "plugin-manager:agent-plugin-type",
        "file": "plugin-manager:test/domain/types/is-agent-plugin.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "plugin-manager:agent-plugin-type",
        "file": "plugin-manager:src/domain/types/agent-plugin.ts",
        "symbol": "AgentInstallOptions"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:fe1d85c237ee0bd7f81257a778987c74ea9cffdad6c62c9aeee214cb04477887",
    "verify.md": "sha256:e00ad11eece5e0e376156b03029a4ac9bba2bb609efb0d4f69208f6efc16914e"
  },
  "generatedBy": "agent",
  "optimizedDescription": "Defines the agent plugin interface that extends SpecdPlugin for agent integrations (Claude, Copilot, Codex, etc.).",
  "optimizedContext": "# plugin-manager:agent-plugin-type\n\n## Rules\n- AgentPlugin extends SpecdPlugin: AgentPlugin MUST extend SpecdPlugin with type: 'agent' and add: - install(config: SpecdConfig, options?: AgentInstallOptions): Promise<AgentInstallResult - uninstall(config:…\n- AgentInstallOptions: capabilities MUST be supplied as a simple collection of capability identifiers by the agent plugin at install time.\n- AgentInstallResult: See source artifacts.\n- isAgentPlugin type guard: The spec MUST export a pure function isAgentPlugin(value: SpecdPlugin): value is AgentPlugin that validates: 1.\n- Dependencies: core:config, plugin-manager:specd-plugin-type\n\n## Constraints\n- Agent plugins specialize the base plugin type for agent integrations.\n- Install-time capability data MUST be passed separately from template variables.\n- The initial required capability identifiers are mcp, agents, and frontmatter.\n- Agent plugins MUST NOT pass pre-normalized capability objects into @specd/skills.\n- Install-time frontmatter data MUST be passed through variables.frontmatter, not as a prebuilt YAML document.\n- Agent plugins MAY pass variables.sharedFolder, but @specd/skills owns its normalization and safety validation.\n"
}
