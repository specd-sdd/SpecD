{
  "title": "plugin-manager:list-plugins-use-case",
  "description": "Use case that returns the plugin inventory — the status of each requested plugin.",
  "dependsOn": ["plugin-manager:plugin-loader"],
  "context": ["Use case that returns the plugin inventory — the status of each requested plugin."],
  "rules": [
    {
      "requirement": "Input",
      "rules": [
        "```typescript\ninterface ListPluginsInput {\n  readonly pluginNames: string[]\n}\n```"
      ]
    },
    {
      "requirement": "Output",
      "rules": [
        "```typescript\ninterface ListPluginsOutput {\n  readonly plugins: Array<{\n    name: string\n    status: 'loaded' | 'not_found' | 'error'\n    plugin?: SpecdPlugin\n    error?: string\n  }>\n}\n```"
      ]
    },
    {
      "requirement": "Behavior",
      "rules": [
        "For each plugin name:\n\n- Attempt to load via `PluginLoader`\n- If loaded successfully → status: `'loaded'`\n- If npm package not found → status: `'not_found'`\n- If load fails → status: `'error'` with error message"
      ]
    }
  ],
  "constraints": ["Read-only operation, no state modification."],
  "scenarios": [
    {
      "requirement": "Input",
      "name": "Input includes plugin names array",
      "when": ["ListPlugins is executed"],
      "then": ["the input includes pluginNames array"]
    },
    {
      "requirement": "Output",
      "name": "Returns plugin statuses",
      "when": ["ListPlugins is executed with plugin names"],
      "then": ["it returns array with name, status, and optional plugin/error"]
    },
    {
      "requirement": "Behavior",
      "name": "Not found status when package cannot be resolved",
      "given": ["an npm package that cannot be resolved"],
      "when": ["ListPlugins is executed with that plugin name"],
      "then": ["status is 'not_found'"]
    },
    {
      "requirement": "Behavior",
      "name": "Error status when load fails",
      "given": ["a plugin that exists but fails to load"],
      "when": ["ListPlugins is executed with that plugin name"],
      "then": ["status is 'error' with error message"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:b55b58f01a7b3275b9b3a5a4d9f97c419cee96f9b8446451112b81c76b562e76",
    "verify.md": "sha256:09a9bc952bb92f930a3d87552119ac9b0de4c19706eab0b140d3e4cac3963f73"
  },
  "generatedBy": "core"
}
