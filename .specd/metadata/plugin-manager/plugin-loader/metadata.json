{
  "title": "plugin-manager:plugin-loader",
  "description": "Infrastructure component for dynamically loading plugins. Handles package resolution, manifest validation, and plugin instantiation.",
  "dependsOn": [
    "core:core/config",
    "plugin-manager:specd-plugin-type",
    "plugin-manager:plugin-errors"
  ],
  "context": [
    "Infrastructure component for dynamically loading plugins. Handles package resolution, manifest validation, and plugin instantiation."
  ],
  "rules": [
    {
      "requirement": "Load workflow",
      "rules": [
        "The loader MUST:\n\n1. **Package availability**: The package must be in node\\_modules (installed via npm/pnpm or linked as workspace)\n2. **Resolve path**: Use `import.meta.resolve()` or `require.resolve()` to get package path\n3. **Read manifest**: Read `specd-plugin.json` from the package root\n4. **Validate manifest**: Validate with Zod schema (including `version` field)\n5. **Dynamic import**: `import(name)` as ESM module\n6. **Factory call**: Call `create()` factory function, passing the loader options\n7. **Validate interface**: Use `isSpecdPlugin` and `isAgentPlugin` from the domain layer to validate the returned object"
      ]
    },
    {
      "requirement": "Manifest schema",
      "rules": [
        "```typescript\nconst SpecdPluginManifestSchema = z.object({\n  schemaVersion: z.number().min(1),\n  name: z.string().min(1),\n  version: z.string().min(1),\n  description: z.string().optional(),\n  pluginType: z.enum(['agent']),\n  minCoreVersion: z.string().default('*'),\n})\n```"
      ]
    },
    {
      "requirement": "Error handling",
      "rules": [
        "Validation failures MUST throw `PluginValidationError`:\n\n- Base contract violation (missing `name`, `type`, `version`, `configSchema`, `init`, `destroy`) — caught by `isSpecdPlugin`\n- Unknown plugin type (type not in `PLUGIN_TYPES`) — caught by `isSpecdPlugin`\n- Agent-specific contract violation (missing `install` or `uninstall` when `pluginType` is `'agent'`) — caught by `isAgentPlugin`"
      ]
    },
    {
      "requirement": "Factory function",
      "rules": [
        "The npm package MUST export `create(options: PluginLoaderOptions): SpecdPlugin` as default or named export.\n\nThe `PluginLoaderOptions` SHALL contain the fully-resolved `SpecdConfig`:\n\n```typescript\ninterface PluginLoaderOptions {\n  readonly config: SpecdConfig\n}\n```"
      ]
    }
  ],
  "constraints": [
    "All I/O happens here in the infrastructure layer.",
    "Zod validation happens at the boundary before reaching domain."
  ],
  "scenarios": [
    {
      "requirement": "Load workflow",
      "name": "Package available",
      "given": ["a valid SpecdConfig is provided", "AND a plugin package is in node_modules"],
      "when": ["PluginLoader loads it"],
      "then": ["it returns the plugin instance"]
    },
    {
      "requirement": "Zod validation",
      "name": "Invalid manifest",
      "given": ["specd-plugin.json has invalid schema"],
      "when": ["PluginLoader loads it"],
      "then": ["PluginValidationError is thrown"]
    },
    {
      "requirement": "Zod validation",
      "name": "Manifest missing version",
      "given": ["specd-plugin.json has no version field"],
      "when": ["PluginLoader loads it"],
      "then": ["PluginValidationError is thrown"]
    },
    {
      "requirement": "Zod validation",
      "name": "Unknown plugin type rejected at runtime",
      "given": [
        "a plugin whose manifest declares pluginType: 'agent' but the runtime object's type is not in PLUGIN_TYPES"
      ],
      "when": ["PluginLoader validates the interface"],
      "then": ["isSpecdPlugin returns false and PluginValidationError is thrown"]
    },
    {
      "requirement": "Zod validation",
      "name": "Agent plugin missing install method",
      "given": [
        "a plugin whose manifest declares pluginType: 'agent' but the runtime object lacks install or uninstall"
      ],
      "when": ["PluginLoader validates the interface"],
      "then": ["isAgentPlugin returns false and PluginValidationError is thrown"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:2cf9821ea5f6c9579fa699fb32bcaa3fe4cc3c62cbc8a59ab8cc32cd7c68cd4a",
    "verify.md": "sha256:0a5ae4933041d3300cb8794382ff7a1275cc56ed92823c4b615b9193d25cd632"
  },
  "generatedBy": "core"
}
