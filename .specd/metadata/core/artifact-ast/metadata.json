{
  "title": "Artifact AST",
  "description": "Parsers, the delta engine, selectors, and validation rules all operate on artifact content, and without a shared tree format each would depend on parser internals, making them tightly coupled to specific file types. The normalized AST solves this by defining a plain JSON-serializable tree of nodes that every `ArtifactParser` adapter must produce and consume. This format is the contract between all these components; adapter internals (which parsing library is used, how the native AST is built) are not specified here.",
  "dependsOn": ["core:delta-format", "core:selector-model"],
  "context": [
    "Parsers, the delta engine, selectors, and validation rules all operate on artifact content, and without a shared tree format each would depend on parser internals, making them tightly coupled to specific file types. The normalized AST solves this by defining a plain JSON-serializable tree of nodes that every `ArtifactParser` adapter must produce and consume. This format is the contract between all these components; adapter internals (which parsing library is used, how the native AST is built) are not specified here."
  ],
  "rules": [
    {
      "requirement": "Common node schema",
      "rules": [
        "Every node in the normalized AST is a plain object with the following fields:\n\n- `type` (string, required) — the node type; values are format-specific and defined per format below\n- `label` (string, optional) — the identifying value of the node; this is the field evaluated by `matches` in selectors; present on nodes that have a primary identifier (section heading text, pair key name, etc.)\n- `value` (string | number | boolean | null, optional) — the scalar content of a leaf node; mutually exclusive with `children`\n- `children` (array, optional) — ordered child nodes; mutually exclusive with `value`\n\nA node may have neither `value` nor `children` (e.g. a thematic break). A node may not have both."
      ]
    },
    {
      "requirement": "Markdown AST",
      "rules": [
        "A markdown adapter must normalize to the following node types:\n\n- `document` — root node; no `label`; has `children`\n- `section` — a heading and all content until the next heading of equal or lesser depth; `label` is the heading text without leading `#` characters and surrounding whitespace (e.g. `\"Requirement: Login\"`); has `level` (integer 1–6) reflecting the heading depth; has `children`\n- `paragraph` — a prose block; no `label`; has `value` (the paragraph text as plain markdown inline content)\n- `list` — a bullet or numbered list; no `label`; has `ordered` (boolean); has `children`\n- `list-item` — an individual list entry; `label` is the item text; may have `children` for nested lists (in which case `label` holds the item text and `children` holds the nested list nodes)\n- `code-block` — a fenced or indented code block; `label` is the language identifier if present; has `value` (the code content)\n- `thematic-break` — a horizontal rule; no `label`; no `value`; no `children`\n\nSections nest by relative heading depth: each section becomes a child of the nearest preceding section whose `level` is strictly lower. Levels need not be contiguous — a `level: 4` section becomes a child of the nearest preceding `level: 1`, `level: 2`, or `level: 3` section, whichever appears last.\n\nExample — the following markdown:\n\n```markdown\n## Requirements\n\n### Requirement: Login\n\nThe system must authenticate users.\n\n- Email and password\n- Rate-limited to 5 attempts\n```\n\nNormalizes to:\n\n```json\n{\n  \"type\": \"document\",\n  \"children\": [\n    {\n      \"type\": \"section\",\n      \"label\": \"Requirements\",\n      \"level\": 2,\n      \"children\": [\n        {\n          \"type\": \"section\",\n          \"label\": \"Requirement: Login\",\n          \"level\": 3,\n          \"children\": [\n            {\n              \"type\": \"paragraph\",\n              \"value\": \"The system must authenticate users.\"\n            },\n            {\n              \"type\": \"list\",\n              \"ordered\": false,\n              \"children\": [\n                { \"type\": \"list-item\", \"label\": \"Email and password\" },\n                { \"type\": \"list-item\", \"label\": \"Rate-limited to 5 attempts\" }\n              ]\n            }\n          ]\n        }\n      ]\n    }\n  ]\n}\n```"
      ]
    },
    {
      "requirement": "YAML AST",
      "rules": [
        "A YAML adapter must normalize to the following node types:\n\n- `document` — root node; no `label`; has `children` (the top-level mapping's pairs, or a single sequence)\n- `mapping` — a YAML mapping; no `label`; has `children` (array of `pair` nodes)\n- `pair` — a key–value entry; `label` is the key name as a string; has `value` for scalar values, or `children` containing a single `mapping` or `sequence` node for nested structures\n- `sequence` — a YAML sequence; no `label`; has `children` (array of `sequence-item` nodes)\n- `sequence-item` — an item in a sequence; no `label`; has `value` for scalar items, or `children` containing a single `mapping` or `sequence` node for structured items\n\nExample — the following YAML:\n\n```yaml\nschema: spec-driven\nllm:\n  model: claude-opus-4-6\n  maxTokens: 4096\n```\n\nNormalizes to:\n\n```json\n{\n  \"type\": \"document\",\n  \"children\": [\n    { \"type\": \"pair\", \"label\": \"schema\", \"value\": \"spec-driven\" },\n    {\n      \"type\": \"pair\",\n      \"label\": \"llm\",\n      \"children\": [\n        {\n          \"type\": \"mapping\",\n          \"children\": [\n            { \"type\": \"pair\", \"label\": \"model\", \"value\": \"claude-opus-4-6\" },\n            { \"type\": \"pair\", \"label\": \"maxTokens\", \"value\": 4096 }\n          ]\n        }\n      ]\n    }\n  ]\n}\n```"
      ]
    },
    {
      "requirement": "JSON AST",
      "rules": [
        "A JSON adapter must normalize to the following node types:\n\n- `document` — root node; no `label`; has `children` containing a single root node\n- `object` — a JSON object; no `label`; has `children` (array of `property` nodes)\n- `property` — a key–value entry; `label` is the key name; has `value` for scalar values, or `children` containing a single `object` or `array` node\n- `array` — a JSON array; no `label`; has `children` (array of `array-item` nodes)\n- `array-item` — an item in an array; no `label`; has `value` for scalar items, or `children` containing a single `object` or `array` node"
      ]
    },
    {
      "requirement": "Plaintext AST",
      "rules": [
        "A plaintext adapter must normalize to the following node types:\n\n- `document` — root node; no `label`; has `children`\n- `paragraph` — a block of text separated by blank lines; no `label`; has `value`\n- `line` — a single line of text; no `label`; has `value`"
      ]
    },
    {
      "requirement": "Round-trip fidelity",
      "rules": [
        "Each adapter must be able to serialize a normalized AST back to the artifact's native format. The round-trip contract:\n\n- All `label`, `value`, and `children` content is preserved exactly\n- For markdown: heading levels, list types (`ordered`), code block languages, and nesting structure are preserved\n- For markdown: inline formatting intent represented in AST node metadata (such as inline code, emphasis, strong, and link structure) is preserved across parse/apply/serialize for nodes not explicitly modified by delta entries\n- For markdown: serializer style markers (`bullet`, `emphasis`, `strong`) should follow the source document when unambiguous; when ambiguous, output follows deterministic project markdown conventions\n- For YAML: key order within mappings is preserved; comments are preserved where the underlying library supports it (CST-level round-trip)\n- For JSON: key order is preserved; formatting is normalized to two-space indentation\n- Content not representable in the normalized format (e.g. inline markdown formatting such as bold or italic) is preserved as-is within `value` strings — the adapter does not decompose inline nodes"
      ]
    }
  ],
  "constraints": [
    "value and children are mutually exclusive on any node",
    "label is only present on node types that have a primary identifier; it is absent on document, paragraph, list, sequence, array, object, thematic-break",
    "All field values must be JSON-serializable — no functions, no circular references, no class instances",
    "The adapter is the only component that may parse or serialize the native format; all other components operate exclusively on the normalized AST"
  ],
  "scenarios": [
    {
      "requirement": "Common node schema",
      "name": "value and children mutually exclusive",
      "when": ["a node has both value and children"],
      "then": ["the adapter must not produce such a node — it is an invalid AST"]
    },
    {
      "requirement": "Common node schema",
      "name": "label absent on non-identifying nodes",
      "when": ["a paragraph, list, sequence, object, or document node is produced"],
      "then": ["the node has no label field"]
    },
    {
      "requirement": "Markdown AST",
      "name": "Heading becomes section with label and level",
      "given": ["a markdown file containing ### Requirement: Login"],
      "when": ["the markdown adapter parses it"],
      "then": [
        "the resulting AST contains a node { type: \"section\", label: \"Requirement: Login\", level: 3, children: [...] }"
      ]
    },
    {
      "requirement": "Markdown AST",
      "name": "Section nests by heading level",
      "given": [
        "a markdown file with a ## Requirements heading followed by ### Requirement: Login"
      ],
      "when": ["the markdown adapter parses it"],
      "then": ["the Requirement: Login section node is a child of the Requirements section node"]
    },
    {
      "requirement": "Markdown AST",
      "name": "Non-contiguous heading levels nest correctly",
      "given": [
        "a markdown file with a # Overview heading followed by #### Detail (no intermediate levels)"
      ],
      "when": ["the markdown adapter parses it"],
      "then": [
        "the Detail section node (level: 4) is a child of the Overview section node (level: 1)"
      ]
    },
    {
      "requirement": "Markdown AST",
      "name": "Paragraph becomes leaf node with value",
      "given": ["a markdown file containing a prose paragraph"],
      "when": ["the markdown adapter parses it"],
      "then": ["the resulting node is { type: \"paragraph\", value: \"<text>\" } with no children"]
    },
    {
      "requirement": "Markdown AST",
      "name": "Fenced code block preserves lang and content",
      "given": ["a markdown file with ```typescript\\nconst x = 1\\n```"],
      "when": ["the markdown adapter parses it"],
      "then": [
        "the resulting node is { type: \"code-block\", label: \"typescript\", value: \"const x = 1\" }"
      ]
    },
    {
      "requirement": "Markdown AST",
      "name": "Unordered list",
      "given": ["a markdown file with a bullet list"],
      "when": ["the markdown adapter parses it"],
      "then": [
        "the resulting node is { type: \"list\", ordered: false, children: [{ type: \"list-item\", label: \"...\" }, ...] }"
      ]
    },
    {
      "requirement": "Markdown AST",
      "name": "Round-trip preserves structure",
      "given": ["a markdown AST produced by parsing a spec file"],
      "when": ["the adapter serializes it back to markdown"],
      "then": ["re-parsing the output produces an identical AST"]
    },
    {
      "requirement": "Markdown AST",
      "name": "Delta apply preserves inline metadata of untouched markdown nodes",
      "given": [
        "a markdown AST where paragraph nodes keep adapter inline metadata for serialization",
        "AND a delta modifies a sibling section only"
      ],
      "when": ["the delta is applied and the AST is serialized"],
      "then": [
        "untouched paragraph nodes still preserve equivalent inline formatting intent after re-parse"
      ]
    },
    {
      "requirement": "Markdown AST",
      "name": "Mixed markdown style normalizes deterministically",
      "given": ["markdown content with mixed markers for unordered lists or emphasis"],
      "when": ["the markdown adapter serializes a parsed AST"],
      "then": ["output uses deterministic project markdown style for ambiguous constructs"]
    },
    {
      "requirement": "YAML AST",
      "name": "Scalar pair",
      "given": ["a YAML file containing schema: spec-driven"],
      "when": ["the YAML adapter parses it"],
      "then": [
        "the resulting AST contains { type: \"pair\", label: \"schema\", value: \"spec-driven\" }"
      ]
    },
    {
      "requirement": "YAML AST",
      "name": "Nested mapping under pair",
      "given": ["a YAML file with llm:\\n  model: claude-opus-4-6"],
      "when": ["the YAML adapter parses it"],
      "then": [
        "the llm pair has children containing a mapping node, which has a pair child with label: \"model\""
      ]
    },
    {
      "requirement": "YAML AST",
      "name": "Sequence of scalars",
      "given": ["a YAML file with a sequence of strings under a key"],
      "when": ["the YAML adapter parses it"],
      "then": [
        "the pair has children containing a sequence node with sequence-item children each having value"
      ]
    },
    {
      "requirement": "YAML AST",
      "name": "Sequence of objects",
      "given": ["a YAML file with a sequence of mappings (e.g. workflow steps)"],
      "when": ["the YAML adapter parses it"],
      "then": ["each sequence-item has children containing a mapping node with pair children"]
    },
    {
      "requirement": "YAML AST",
      "name": "Round-trip preserves comments",
      "given": ["a YAML file with inline comments"],
      "when": ["the YAML adapter parses and re-serializes it without modification"],
      "then": ["the output preserves all comments"]
    },
    {
      "requirement": "JSON AST",
      "name": "Object property",
      "given": ["a JSON file containing { \"version\": \"1.0.0\" }"],
      "when": ["the JSON adapter parses it"],
      "then": ["the AST contains a property node with label: \"version\" and value: \"1.0.0\""]
    },
    {
      "requirement": "JSON AST",
      "name": "Nested object",
      "given": ["a JSON file with a nested object value"],
      "when": ["the JSON adapter parses it"],
      "then": ["the property has children containing an object node with property children"]
    },
    {
      "requirement": "JSON AST",
      "name": "Array of scalars",
      "given": ["a JSON file with \"keywords\": [\"specd\", \"spec-driven\"]"],
      "when": ["the JSON adapter parses it"],
      "then": [
        "the keywords property has children containing an array node with array-item children each having value"
      ]
    },
    {
      "requirement": "Plaintext AST",
      "name": "Document node is root",
      "given": ["a plaintext file"],
      "when": ["the plaintext adapter parses it"],
      "then": ["the root is a document node with children containing block nodes"]
    },
    {
      "requirement": "Plaintext AST",
      "name": "Paragraph and line nodes",
      "given": ["a plaintext file with multiple paragraphs"],
      "when": ["the plaintext adapter parses it"],
      "then": ["each paragraph becomes a paragraph node with line children"]
    },
    {
      "requirement": "Round-trip fidelity",
      "name": "Markdown round-trip preserves headings",
      "given": ["a markdown file with headings at multiple levels"],
      "when": ["it is parsed to AST, then serialized back to markdown"],
      "then": ["heading levels and labels are preserved exactly"]
    },
    {
      "requirement": "Round-trip fidelity",
      "name": "YAML round-trip preserves key order",
      "given": ["a YAML file with multiple keys"],
      "when": ["it is parsed to AST, then serialized back to YAML"],
      "then": ["key order is preserved"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:2a02f64e98a4187863106180d2057b68ec5d514361c8c883a7c9a2a627ec438c",
    "verify.md": "sha256:0999b3b7fa2795411473ab84c98ab3296b2d3804d7b994b7a496ce280816d09d"
  },
  "generatedBy": "agent",
  "optimizedDescription": "Parsers, delta engine, selectors, validation rules all operate artifact content, shared tree format each would depend parser internals, making tightl…",
  "optimizedContext": "# Artifact AST\n\n## Rules\n- Common node schema: Every node normalized AST plain object following fields: - type (string, required) — node type; values format-specific defined per format below - label (string, optional) — identifying value node; field evaluated matches selectors; present nodes have primary identifier (section heading text, pair key name, etc. ) - value (string | number | boolean | null, optional) — scalar content leaf node; mutually exclusive children - children (array, optional) — ordered child nodes; mutually exclusive value node may have neither value nor children (e. g. thematic break). node may not have both.\n- Markdown AST: markdown adapter must normalize following node types: - document — root node; no label; has children - section — heading all content until next heading equal lesser depth; label heading text leading # characters surrounding whitespace (e. g. \"Requirement: Login\"); has level (integer 1–6) reflecting heading depth; has children - paragraph — prose block; no label; has value paragraph text plain markdown inline content) - list — bullet numbered list; no label; has ordered (boolean); has children - list-item — individual list entry; label item text; may have children nested lists which case label holds item text children holds nested list nodes) - code-block — fenced indented code block; label…\n- YAML AST: YAML adapter must normalize following node types: - document — root node; no label; has children top-level mapping's pairs, single sequence) - mapping — YAML mapping; no label; has children (array pair nodes) - pair — key–value entry; label key name string; has value scalar values, children containing single mapping sequence node nested structures - sequence — YAML sequence; no label; has children (array sequence-item nodes) - sequence-item — item sequence; no label; has value scalar items, children containing single mapping sequence node structured items Example — following YAML: Normalizes to:\n- JSON AST: JSON adapter must normalize following node types: - document — root node; no label; has children containing single root node - object — JSON object; no label; has children (array property nodes) - property — key–value entry; label key name; has value scalar values, children containing single object array node - array — JSON array; no label; has children (array array-item nodes) - array-item — item array; no label; has value scalar items, children containing single object array node\n- Plaintext AST: plaintext adapter must normalize following node types: - document — root node; no label; has children - paragraph — block text separated blank lines; no label; has value - line — single line text; no label; has value\n- Round-trip fidelity: Each adapter must able serialize normalized AST back artifact's native format. round-trip contract: - All label, value, children content preserved exactly - markdown: heading levels, list types (ordered), code block languages, nesting structure preserved - markdown: inline formatting intent represented AST node metadata (such inline code, emphasis, strong, link structure) preserved across parse/apply/serialize nodes not explicitly modified delta entries - markdown: serializer style markers (bullet, emphasis, strong) should follow source document when unambiguous; when ambiguous, output follows deterministic project markdown conventions - YAML: key order within mappings preserved; comments p…"
}
