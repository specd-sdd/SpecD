{
  "title": "Spec Metadata",
  "description": "Tooling and AI agents need a compact, machine-readable summary of each spec — its dependencies, content hashes, rules, and scenarios — without parsing the full artifact files every time. Each spec has a corresponding `metadata.json` file stored under `.specd/metadata/<workspace>/<capability-path>/`, separate from the spec's content artifacts.\n\n`metadata.json` is the canonical normalized consumer form of a persisted spec. It unifies heterogeneous schemas and companion-file layouts into one stable shape for consumers even when the underlying schema expresses the same information across different files, section names, or sidecars. It is generated deterministically by core at archive time using the schema's `metadataExtraction` engine together with repository semantic persisted state; content hashes and canonical projection checks enable staleness detection so consumers know when to regenerate.",
  "dependsOn": [
    "core:config",
    "core:change",
    "core:schema-format",
    "core:content-extraction",
    "core:spec-repository-port",
    "core:spec-lock",
    "core:spec-optimization"
  ],
  "context": [
    "Tooling and AI agents need a compact, machine-readable summary of each spec — its dependencies, content hashes, rules, and scenarios — without parsing the full artifact files every time. Each spec has a corresponding `metadata.json` file stored under `.specd/metadata/<workspace>/<capability-path>/`, separate from the spec's content artifacts.\n\n`metadata.json` is the canonical normalized consumer form of a persisted spec. It unifies heterogeneous schemas and companion-file layouts into one stable shape for consumers even when the underlying schema expresses the same information across different files, section names, or sidecars. It is generated deterministically by core at archive time using the schema's `metadataExtraction` engine together with repository semantic persisted state; content hashes and canonical projection checks enable staleness detection so consumers know when to regenerate."
  ],
  "rules": [
    {
      "requirement": "File location and naming",
      "rules": [
        "`metadata.json` lives under the `.specd/metadata/` directory, organized by workspace then capability path:\n\n```\n.specd/metadata/core/config/\n└── metadata.json\n\n.specd/metadata/skills/get-skill/\n└── metadata.json\n\n.specd/metadata/plugin-manager/agent-plugin-type/\n└── metadata.json\n```\n\nThe path structure is: `.specd/metadata/<workspace>/<capability-path>/metadata.json`, where:\n\n- `<workspace>` is the workspace name from `specd.yaml`\n- `<capability-path>` is the spec's capability path (with prefix segments if workspace has a prefix)\n\nThe metadata root path is configured per workspace via `specs.fs.metadataPath` in `specd.yaml`. When not set, the composition layer auto-derives it from the VCS root of the workspace's specs path: `createVcsAdapter(specs.path).rootDir()` + `/.specd/metadata/`. This works across heterogeneous VCS setups (git, hg, svn). When `NullVcsAdapter` is returned (specs path is not inside any VCS), the fallback is `.specd/metadata/` relative to the specs root parent directory. The `FsSpecRepository` adapter receives the resolved path as config — it does not perform VCS detection itself.\n\nThe file's absence is not an error — a spec with no `metadata.json` is treated as having no declared dependencies and no recorded content hash."
      ]
    },
    {
      "requirement": "Sidecar separation",
      "rules": [
        "The metadata generation process SHALL obtain persisted schema, dependencies, and implementation links through the `SpecRepository` semantic operations. It MUST NOT read the underlying sidecar files directly."
      ]
    },
    {
      "requirement": "Spec.generatedMetadataStamp is a stamp only",
      "rules": [
        "`Spec.generatedMetadataStamp` returned by `SpecRepository.get()` MUST expose only\n`{ present, lastModified }` for the generated `metadata.json` file. It MUST NOT be\ntreated as authored spec content and MUST NOT replace `metadata()`.\n\nParsed metadata content, structural validation, freshness classification\n(`fresh` / `stale` via contentHashes and dependency projection), and overwrite\nprotection remain available only through `metadata()` / `saveMetadata()` and the\ngeneration/save use cases defined by this spec and related ports."
      ]
    },
    {
      "requirement": "File format",
      "rules": [
        "`metadata.json` is a JSON file (per ADR-0019: machine-generated files use JSON).\nAll fields are optional — an empty object or absent file is valid:\n\n```json\n{\n  \"title\": \"Change\",\n  \"description\": \"The central domain entity in specd.\",\n  \"dependsOn\": [\"core:storage\", \"core:delta-format\"],\n  \"contentHashes\": {\n    \"spec.md\": \"sha256:abc123...\"\n  },\n  \"rules\": [{ \"requirement\": \"Lifecycle states\", \"rules\": [\"...\"] }],\n  \"constraints\": [\"...\"],\n  \"scenarios\": [{ \"requirement\": \"...\", \"name\": \"...\", \"given\": [], \"when\": [], \"then\": [] }],\n  \"optimizedDescription\": \"AI optimized description\",\n  \"optimizedContext\": \"AI optimized context\",\n  \"generatedBy\": \"core\",\n  \"provenance\": {\n    \"artifacts\": {\n      \"spec.md\": { \"hash\": \"sha256:...\", \"lastModified\": \"2026-07-24T00:00:00.000Z\" }\n    },\n    \"persistedStateHash\": null,\n    \"schema\": { \"name\": \"specd/std\", \"version\": 1 },\n    \"projectionVersion\": 1,\n    \"projectionFingerprint\": \"sha256:...\"\n  }\n}\n```\n\nFields:\n\n- `title` — human-readable display title\n- `description` — short summary of the spec's purpose\n- `keywords` — lowercase hyphen-separated discovery tokens\n- `dependsOn` — array of spec IDs this spec depends on; this is a normalized\n  projection of the spec's persisted dependency state, not an independently\n  authored value\n- `contentHashes` — `{ filename: \"sha256:<hex>\" }` for staleness detection\n- `rules` — extracted requirements grouped by heading\n- `constraints` — extracted constraint bullets\n- `scenarios` — extracted verification scenarios\n- `context` — freeform context strings\n- `optimizedDescription` — a normalized projection of `spec-lock.json`'s\n  `optimizations.optimizedDescription.value`. It is included only when that\n  field's persisted baseline is fresh against the current artifacts and schema\n  identity; it MUST be omitted, not fabricated, when stale or absent.\n- `optimizedContext` — the same projection of\n  `optimizations.optimizedContext.value`, subject to the same freshness gate.\n- `generatedBy` — always `\"core\"`; metadata content is always produced by\n  deterministic generation. A legacy document with `generatedBy: \"agent\"` MAY\n  still be read leniently for migration, but no generation path emits that\n  value.\n- `provenance` — the `SpecMetadataProvenance` record described in Requirement:\n  Source provenance, used to determine whether this document is fresh relative\n  to current source state"
      ]
    },
    {
      "requirement": "Source provenance",
      "rules": [
        "Generated metadata MUST record a `provenance` object describing the exact\nsource state it was produced from:\n\n- `artifacts` — a record keyed by artifact filename, each entry containing the\n  `hash` (content hash) and diagnostic `lastModified` stamp of that artifact at\n  generation time\n- `persistedStateHash` — the lock sidecar's `persistedStateHash` at generation\n  time, or `null` when no persisted lock state exists for the spec\n- `schema` — the `PersistedSchemaIdentity` (`{ name, version }`) in effect at\n  generation time\n- `projectionVersion` — the integer version of the metadata generation/\n  projection contract used\n- `projectionFingerprint` — a hash of the effective `metadataExtraction`\n  configuration, registered extractor transforms, schema extends/plugins/\n  overrides that affect extraction, and the generator algorithm version; it is\n  a hash of the resolved projection contract, not a copy of the resolved schema\n\n`lastModified` values recorded in `provenance.artifacts` are diagnostic only.\nFreshness comparisons MUST use `hash`, `persistedStateHash`, `schema`,\n`projectionVersion`, and `projectionFingerprint` — never `lastModified` alone.\n\n`provenance` is the complete input to freshness comparison. `contentHashes`\nremains present as a simpler consumer-facing convenience field but MUST be\nderivable from `provenance.artifacts` and MUST NOT diverge from it."
      ]
    },
    {
      "requirement": "Structural validation before persistence",
      "rules": [
        "The internal metadata-persistence guard (`PersistSpecMetadata`) validates\ngenerated JSON content against the `strictSpecMetadataSchema` Zod schema before\nwriting. The content must be a JSON object. `title` and `description` are\nrequired; other fields are optional but when present must conform to their\ndeclared types and formats:\n\n- `title` (required) must be a non-empty string\n- `description` (required) must be a non-empty string\n- `keywords` must be an array of non-empty lowercase strings\n- `dependsOn` must be an array of strings, each matching a valid spec ID pattern\n- `contentHashes` (required) must be a non-empty record of filename to hash\n  string\n- `rules` must be an array of objects with `requirement` and `rules`\n- `constraints` must be a non-empty array of non-empty strings\n- `scenarios` must be an array of objects with `requirement`, `name`, `when`,\n  `then`, and `given`\n- `optimizedDescription` must be a non-empty string\n- `optimizedContext` must be a non-empty string\n- `provenance` must conform to `SpecMetadataProvenance`\n\nIf validation fails, persistence is rejected with a typed validation error and\nthe file is not written. This guard is not a public editor: it is invoked only\ninternally by materialization, never by an external caller supplying arbitrary\ncontent.\n\nUnknown top-level keys are allowed (`.passthrough()`) to support\nforward-compatible extensions."
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "rules": [
        "`metadata.json` is generated deterministically by core as part of the `ArchiveChange` use case, but every metadata-related archive check that can still fail the archive attempt MUST complete during the full archive-batch preflight before canonical publication begins for any spec.\n\nArchive-time flow for modified specs:\n\n1. Prepare the merged canonical artifact content for every affected spec in memory.\n2. Determine the final persisted `dependsOn` set for each archive-target spec.\n3. Run `extractMetadata()` over the prepared merged content for each relevant spec and evaluate archive-time consistency requirements such as `dependsOn` mismatch detection.\n4. Determine the final `spec-lock.json` content for each spec that will publish a sidecar.\n5. Confirm that every metadata-related archive check across the full archive batch has succeeded.\n6. Only then publish the canonical spec artifacts plus `spec-lock.json` for each spec as staged publication units.\n7. After publication succeeds for a spec, run `GenerateSpecMetadata` against the canonical persisted spec.\n8. Compute `contentHashes` for the required persisted spec artifacts and persist `metadata.json`.\n\nThe archive-owned persisted dependency rules are:\n\n- If `change.specDependsOn` has an entry for the spec, that value is the final persisted `dependsOn` set for the archive attempt.\n- If `metadataExtraction.dependsOn` returns a value during the pre-publication extraction pass, the extracted value MUST match the final persisted `dependsOn` set being sealed for that spec or archive fails.\n- This mismatch rule applies both when a canonical `spec-lock.json` already exists and when the archive is creating `spec-lock.json` for the first time.\n- If `metadataExtraction.dependsOn` is omitted, `metadata.json.dependsOn` MUST still be written from the final persisted dependency set.\n- `metadata.json.dependsOn` is therefore the canonical normalized dependency field exposed to consumers, regardless of whether the underlying schema expresses dependencies in `spec.md`, another artifact, or only through persisted sidecar state.\n- Outside archive, a legacy spec with no sidecar MAY still remain on extraction-backed metadata flows until opportunistic backfill succeeds.\n\n`metadata.json.dependsOn` remains a supported consumer surface, but for persisted specs it is a projection of archive-owned persisted dependency state rather than an independent source of truth."
      ]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "rules": [
        "Metadata freshness MUST be computed only by the pure application/domain\nfunction `assessMetadataFreshness(persisted: SpecMetadata, current:\nSpecMetadataSourceState): MetadataFreshnessAssessment`. `SpecRepository` MUST\nNOT compute, return, or classify freshness itself; `readMetadataSnapshot()`\ndistinguishes only `missing`, `invalid`, and `present` persistence/parse\nstates.\n\n`assessMetadataFreshness` compares:\n\n- the exact artifact filename set and per-artifact content hashes in\n  `provenance.artifacts` against the current artifact state\n- `provenance.persistedStateHash` against the spec's current\n  `persistedStateHash(spec)`, including the transition to or from lock absence\n- `provenance.schema` against the spec's current persisted schema identity\n- `provenance.projectionVersion` against the generator's current projection\n  version\n- `provenance.projectionFingerprint` against the current effective projection\n  contract fingerprint\n\nMetadata is stale when any of these comparisons differs. `lastModified` values\nare diagnostic only and MUST NOT by themselves make metadata stale when the\ncorresponding hash is unchanged.\n\nThis comparison is used internally by `MaterializeSpecMetadata` to decide reuse\nversus regeneration. It is not a repository responsibility and MUST NOT be\nreimplemented independently by any repository adapter or by ordinary consumers."
      ]
    },
    {
      "requirement": "Use by CompileContext",
      "rules": [
        "`CompileContext` and other context-oriented consumers obtain the canonical\nnormalized representation of persisted specs through Core metadata\nmaterialization (`GetSpecMetadata` / `MaterializeSpecMetadata`), never by\ncalling `SpecRepository.readMetadataSnapshot()` directly to make a freshness\ndecision.\n\nMaterialization guarantees that a consumer requiring the normalized projection\nreceives a metadata value that is either confirmed fresh or was just\nregenerated from current source state; consumers MUST NOT implement their own\nmissing/stale fallback logic against a raw repository snapshot.\n\n`SpecRepository.readMetadataSnapshot()` remains available for materialization\ninternals and diagnostics that intentionally need the raw persisted/parse\nstate, but is not the path ordinary consumers use to obtain usable metadata."
      ]
    },
    {
      "requirement": "Version control",
      "rules": [
        "`metadata.json` is a disposable generated cache, not authored content. New\nprojects initialize their metadata cache directory and add\n`/.specd/metadata/` (rooted, so similarly named nested directories are\nunaffected) to the project-root `.gitignore`.\n\nExisting projects that already track generated metadata are not automatically\nuntracked — adding an ignore entry does not remove already-tracked files from\nthe Git index. Removing previously tracked metadata from version control is an\nexplicit one-time repository migration, not something runtime silently\nperforms.\n\nWhen a project configures a custom filesystem `metadataPath`, keeping that path\nout of version control is the operator's responsibility; runtime does not\nrewrite `.gitignore` for arbitrary custom paths outside the default location."
      ]
    },
    {
      "requirement": "Implementation projection",
      "rules": [
        "Generated metadata SHALL include an `implementation` property when the spec is linked to code files or symbols. This data is projected from the repository's persisted implementation semantics.\n\n- **File-level links** are projected as `implementation.files: Array<{ specId, file }>`.\n- **Symbol-level links** are projected as `implementation.symbols: Array<{ specId, file, symbol }>`.\n\nIf a spec has no persisted implementation links, the `implementation` property SHALL be omitted from the generated metadata."
      ]
    }
  ],
  "constraints": [
    "metadata.json is not a schema artifact — it is never listed in\nrequiredSpecArtifacts, never validated by ValidateArtifacts, and never\ntracked in the change manifest's artifacts array",
    "Its absence is not an error at any point — all reads of metadata treat a\nmissing file as empty",
    "dependsOn paths must not form cycles; if a cycle is detected during\ntraversal, specd breaks the cycle and emits a warning",
    "Staleness is an internal materialization decision, not a public status;\nordinary consumers self-heal through materialization instead of acting on a\nraw staleness flag",
    "No process may include the spec itself in its own dependsOn list",
    "The internal metadata-persistence guard must validate content against the\nstructural contract before writing — structurally invalid content is\nrejected and not written",
    "dependsOn in generated metadata is never independently authored or subject\nto overwrite comparison — it is always a direct projection of the spec's\ncurrent persisted dependency state",
    "Reading metadata (parseMetadata) remains lenient — it returns {} on\ninvalid input so that downstream operations are never blocked by a malformed\nfile on disk",
    "Metadata is accessed exclusively via SpecRepository.readMetadataSnapshot()\nand SpecRepository.writeMetadataSnapshot() — never via the generic\nartifact() / save() methods",
    "Metadata content, including optimizedDescription and optimizedContext, is\nnever a source of truth — spec-lock.json owns dependency, implementation,\nand optimization state; metadata is always a regenerable projection"
  ],
  "scenarios": [
    {
      "requirement": "File location and naming",
      "name": "Metadata file present",
      "given": [
        "a spec at core:config with a metadata.json at .specd/metadata/core/config/metadata.json"
      ],
      "when": ["metadata(spec) is called"],
      "then": ["the result contains the parsed JSON fields"]
    },
    {
      "requirement": "File location and naming",
      "name": "Metadata file absent",
      "given": ["no metadata.json exists for the spec"],
      "when": ["metadata(spec) is called"],
      "then": ["the result is null"]
    },
    {
      "requirement": "File location and naming",
      "name": "Workspace without prefix stores metadata correctly",
      "given": ["a workspace skills with no prefix configured", "AND a spec at skills:get-skill"],
      "when": ["metadata is saved for that spec"],
      "then": ["the file is stored at .specd/metadata/skills/get-skill/metadata.json"]
    },
    {
      "requirement": "File location and naming",
      "name": "Workspace with prefix stores metadata correctly",
      "given": ["a workspace core with prefix core configured", "AND a spec at core:config"],
      "when": ["metadata is saved for that spec"],
      "then": ["the file is stored at .specd/metadata/core/core/config/metadata.json"]
    },
    {
      "requirement": "File location and naming",
      "name": "Explicit metadataPath in workspace config",
      "given": ["a workspace config with specs.fs.metadataPath: .specd/metadata"],
      "when": ["metadata is requested for a spec in that workspace"],
      "then": [
        "the adapter reads from the configured path at <metadataPath>/<workspace>/<specPath>/metadata.json"
      ]
    },
    {
      "requirement": "File location and naming",
      "name": "Auto-derived metadataPath from VCS root",
      "given": [
        "a workspace config with no explicit specs.fs.metadataPath",
        "AND the specs path is inside a VCS repository"
      ],
      "when": ["the kernel boots"],
      "then": [
        "the composition layer resolves the VCS root of specs.path and derives <vcsRoot>/.specd/metadata/ as the metadata path"
      ]
    },
    {
      "requirement": "File location and naming",
      "name": "NullVcsAdapter fallback for metadataPath",
      "given": [
        "a workspace config with no explicit specs.fs.metadataPath",
        "AND the specs path is not inside any VCS (NullVcsAdapter returned)"
      ],
      "when": ["the kernel boots"],
      "then": [
        "the composition layer falls back to .specd/metadata/ relative to the specs root parent"
      ]
    },
    {
      "requirement": "File format",
      "name": "Valid metadata file with all fields",
      "given": [
        "a metadata.json containing {\"title\":\"Config\",\"description\":\"Project configuration\",\"dependsOn\":[\"core:storage\"],\"contentHashes\":{\"spec.md\":\"sha256:abc...\"},\"generatedBy\":\"core\"}"
      ],
      "when": ["it is parsed by the lenient schema"],
      "then": ["all fields are present in the result"]
    },
    {
      "requirement": "File format",
      "name": "Title absent — fallback to path",
      "when": ["metadata.json has no title field"],
      "then": ["tooling displays the spec's path (e.g. core/change) instead of a title"]
    },
    {
      "requirement": "File format",
      "name": "Valid metadata file with only dependsOn",
      "when": [
        "metadata.json contains \"dependsOn\": [\"core:storage\"] and contentHashes with entries for each file"
      ],
      "then": [
        "specd parses core:storage as a dependency and uses the per-file hashes for staleness detection"
      ]
    },
    {
      "requirement": "File format",
      "name": "Empty dependsOn",
      "when": ["metadata.json contains \"dependsOn\": []"],
      "then": ["the spec has no declared dependencies — no traversal occurs from this spec"]
    },
    {
      "requirement": "File format",
      "name": "Cross-workspace dependency",
      "when": ["dependsOn contains billing:payments/invoices"],
      "then": ["specd resolves the spec from the billing workspace's specs root"]
    },
    {
      "requirement": "File format",
      "name": "Unqualified path resolves to same workspace",
      "when": ["a spec in the default workspace has \"dependsOn\": [\"auth/login\"]"],
      "then": ["specd resolves auth/login within the default workspace"]
    },
    {
      "requirement": "File format",
      "name": "Generated metadata includes provenance",
      "when": ["metadata is generated for a spec"],
      "then": [
        "the document includes a provenance object describing the source artifact hashes, persistedStateHash, schema identity, projectionVersion, and projectionFingerprint used to produce it"
      ]
    },
    {
      "requirement": "File format",
      "name": "optimizedDescription is omitted when its lock baseline is stale",
      "given": [
        "the spec-lock optimization baseline for optimizedDescription is stale against current artifacts or schema identity"
      ],
      "when": ["metadata is generated"],
      "then": ["optimizedDescription is omitted from the generated document rather than fabricated"]
    },
    {
      "requirement": "File format",
      "name": "generatedBy is always core for newly generated documents",
      "when": ["metadata is generated"],
      "then": [
        "generatedBy is always \"core\"",
        "AND a legacy document with generatedBy: \"agent\" may still be read leniently for migration"
      ]
    },
    {
      "requirement": "Source provenance",
      "name": "Provenance records per-artifact hash and diagnostic lastModified",
      "when": ["metadata is generated"],
      "then": [
        "provenance.artifacts records, for each artifact filename, its content hash and a diagnostic lastModified stamp"
      ]
    },
    {
      "requirement": "Source provenance",
      "name": "Provenance records persistedStateHash or null when lock absent",
      "given": ["a spec with no persisted lock state"],
      "when": ["metadata is generated"],
      "then": ["provenance.persistedStateHash is null"]
    },
    {
      "requirement": "Source provenance",
      "name": "Provenance records schema identity, projection version, and projection fingerprint",
      "when": ["metadata is generated"],
      "then": [
        "provenance.schema records the PersistedSchemaIdentity in effect at generation time",
        "AND provenance.projectionVersion and provenance.projectionFingerprint record the generation/projection contract used"
      ]
    },
    {
      "requirement": "Source provenance",
      "name": "contentHashes remains derivable from provenance.artifacts",
      "when": ["metadata is generated"],
      "then": [
        "the simpler contentHashes convenience field does not diverge from provenance.artifacts"
      ]
    },
    {
      "requirement": "Source provenance",
      "name": "lastModified alone is diagnostic, not a freshness input",
      "given": ["an artifact whose lastModified differs but whose content hash is unchanged"],
      "when": ["freshness is compared using provenance"],
      "then": [
        "the comparison relies on hash, persistedStateHash, schema, projectionVersion, and projectionFingerprint — never lastModified alone"
      ]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Valid metadata accepted",
      "given": [
        "a JSON object with title: 'Config', description: 'Handles config', keywords: ['lifecycle'], dependsOn: ['core:storage'], and contentHashes: { 'spec.md': 'sha256:a3f1...64hex' }"
      ],
      "when": ["PersistSpecMetadata validates that content before writing"],
      "then": ["the file is written successfully"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Missing title rejected",
      "given": ["a JSON object with description: 'Some description' but no title"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": ["a typed validation error is thrown", "AND the file is not written"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Missing description rejected",
      "given": ["a JSON object with title: 'Test' but no description"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": ["a typed validation error is thrown", "AND the file is not written"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Invalid keywords rejected",
      "given": ["a JSON object with keywords: ['Valid', 123]"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": [
        "a typed validation error is thrown indicating keywords must be lowercase strings",
        "AND the file is not written"
      ]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Invalid dependsOn format rejected",
      "given": ["a JSON object with dependsOn: ['not a valid id!']"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": [
        "a typed validation error is thrown indicating the spec ID format is invalid",
        "AND the file is not written"
      ]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Invalid contentHashes format rejected",
      "given": ["a JSON object with contentHashes: { 'spec.md': 'md5:abc' }"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": [
        "a typed validation error is thrown indicating the hash format is invalid",
        "AND the file is not written"
      ]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Invalid rules structure rejected",
      "given": ["a JSON object with rules: [{ requirement: '' }]"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": ["a typed validation error is thrown", "AND the file is not written"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Invalid scenarios structure rejected",
      "given": [
        "a JSON object with scenarios: [{ requirement: 'X', name: 'Y' }] (missing when and then)"
      ],
      "when": ["PersistSpecMetadata validates that content"],
      "then": ["a typed validation error is thrown", "AND the file is not written"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Unknown top-level keys allowed",
      "given": [
        "a JSON object with title: 'Test', description: 'A test', and customField: 'value'"
      ],
      "when": ["PersistSpecMetadata validates that content"],
      "then": ["the file is written successfully — unknown keys are passed through"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Empty content rejected",
      "given": ["an empty JSON object (no fields)"],
      "when": ["PersistSpecMetadata validates that content"],
      "then": [
        "a typed validation error is thrown — content must be a JSON object with at least title and description"
      ]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Read path remains lenient",
      "given": ["a metadata.json on disk with keywords: [123, true] (invalid types)"],
      "when": ["parseMetadata reads the file"],
      "then": ["it returns {} without throwing — read path never blocks operations"]
    },
    {
      "requirement": "Structural validation before persistence",
      "name": "Schema supports optimized fields and provenance",
      "given": [
        "a metadata object with optimizedDescription, optimizedContext, and a well-formed provenance object"
      ],
      "when": ["validated against strictSpecMetadataSchema"],
      "then": ["validation passes"]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Metadata generated after archive",
      "given": ["a change modifies the spec core:change"],
      "when": ["ArchiveChange completes the delta merge and spec sync"],
      "then": [
        "core generates metadata for core:change using the schema's metadata extraction engine",
        "AND the file contains title, description, dependsOn, contentHashes, and any rules, constraints, scenarios extracted from the spec content"
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Manifest dependsOn takes priority over extracted",
      "given": ["a change has specDependsOn entries for a spec"],
      "when": ["metadata is generated for that spec"],
      "then": [
        "dependsOn in the written metadata comes from change.specDependsOn, not from the extraction engine"
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Pre-publication extraction checks final persisted dependsOn during full-batch preflight",
      "given": ["archive has prepared the merged canonical content for a modified spec"],
      "when": ["archive determines the final persisted dependsOn set for that spec"],
      "then": [
        "it runs extractMetadata() against the prepared content during the full archive-batch preflight",
        "AND that check completes before canonical publication begins for any spec in the batch"
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Metadata-related failure in one spec blocks publication of earlier specs",
      "given": [
        "a multi-spec archive batch where one spec has already passed metadata-related checks",
        "AND a later spec in the same batch will fail metadata-related archive validation"
      ],
      "when": ["ArchiveChange.execute completes metadata-related preflight for the batch"],
      "then": [
        "the later failure aborts the archive before canonical publication begins for the earlier spec"
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Omitted extraction falls back to the final persisted dependency set",
      "given": [
        "a persisted spec is being archived",
        "AND the schema omits metadataExtraction.dependsOn for that spec"
      ],
      "when": ["archive regenerates metadata"],
      "then": ["metadata.json.dependsOn is written from the final persisted dependency set"]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Legacy spec may still derive metadata dependsOn from extraction",
      "given": ["a persisted legacy spec has no spec-lock.json", "AND extraction yields dependsOn"],
      "when": [
        "a non-archive metadata flow regenerates metadata before opportunistic backfill succeeds"
      ],
      "then": [
        "metadata.json.dependsOn may still be derived from extraction until sidecar backfill succeeds"
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Mismatched extracted dependsOn blocks archive before any batch publication starts",
      "given": [
        "archive is sealing the final persisted dependency state for a modified spec",
        "AND the spec may or may not already have a canonical spec-lock.json",
        "AND extraction yields a different dependsOn value"
      ],
      "when": ["archive performs the pre-publication consistency check"],
      "then": [
        "archive fails for that spec before canonical publication begins for any spec in the batch"
      ]
    },
    {
      "requirement": "Deterministic generation at archive time",
      "name": "Generation failure does not block archive",
      "given": ["extraction produces no title for a spec"],
      "when": ["SaveSpecMetadata rejects the write"],
      "then": [
        "the archive is not aborted",
        "AND the spec path is reported in staleMetadataSpecPaths"
      ]
    },
    {
      "requirement": "Sidecar separation",
      "name": "Metadata and sidecar live in different storage locations",
      "given": ["a persisted spec has both metadata.json and spec-lock.json"],
      "when": ["their storage locations are inspected"],
      "then": [
        "metadata.json lives under the configured metadata root",
        "AND spec-lock.json lives alongside the canonical persisted scope: spec artifacts"
      ]
    },
    {
      "requirement": "Sidecar separation",
      "name": "Legacy spec may have metadata without sidecar",
      "given": ["a legacy persisted spec predates spec-lock.json"],
      "when": ["tooling reads its metadata state"],
      "then": ["missing spec-lock.json is tolerated until opportunistic backfill creates it"]
    },
    {
      "requirement": "Spec.generatedMetadataStamp is a stamp only",
      "name": "get stamp does not replace metadata()",
      "given": ["a spec with generated metadata.json on disk"],
      "when": ["a caller uses Spec.generatedMetadataStamp from get()"],
      "then": [
        "only present and lastModified are available",
        "AND parsed metadata content is obtained only via metadata()"
      ]
    },
    {
      "requirement": "Spec.generatedMetadataStamp is a stamp only",
      "name": "Stamp absence does not imply empty metadata document",
      "given": ["a spec with no generated metadata file"],
      "when": ["get() returns generatedMetadataStamp.present === false"],
      "then": [
        "callers MUST treat that as absence of the sidecar",
        "AND MUST NOT interpret it as an empty parsed metadata document"
      ]
    },
    {
      "requirement": "Implementation projection",
      "name": "Metadata projects archived implementation links from spec-lock",
      "given": ["a persisted spec-lock.json with file-level and symbol-level implementation links"],
      "when": ["metadata is generated for that spec"],
      "then": [
        "metadata.json includes an implementation projection derived from the sidecar",
        "AND the sidecar remains the authoritative source"
      ]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "Content hash mismatch marks metadata stale",
      "given": [
        "persisted metadata's provenance.artifacts hash for a file differs from that file's current content hash"
      ],
      "when": ["assessMetadataFreshness compares persisted and current source state"],
      "then": ["the result is stale"]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "persistedStateHash transition to or from absence marks metadata stale",
      "given": [
        "persisted metadata's provenance.persistedStateHash differs from the spec's current persistedStateHash(spec), including a transition to or from lock absence"
      ],
      "when": ["assessMetadataFreshness runs"],
      "then": ["the result is stale"]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "Schema identity change marks metadata stale",
      "given": [
        "persisted metadata's provenance.schema differs from the spec's current persisted schema identity"
      ],
      "when": ["assessMetadataFreshness runs"],
      "then": ["the result is stale"]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "Projection version or fingerprint change marks metadata stale",
      "given": [
        "persisted metadata's provenance.projectionVersion or provenance.projectionFingerprint differs from the generator's current values"
      ],
      "when": ["assessMetadataFreshness runs"],
      "then": ["the result is stale"]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "lastModified alone does not cause staleness",
      "given": [
        "a persisted artifact's lastModified value differs from its current value but its content hash is unchanged"
      ],
      "when": ["assessMetadataFreshness runs"],
      "then": ["the metadata is not treated as stale solely because of lastModified"]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "Repository never classifies freshness",
      "when": ["SpecRepository.readMetadataSnapshot(spec) is called"],
      "then": [
        "the returned kind is one of missing, invalid, or present",
        "AND the repository does not compute or return fresh/stale"
      ]
    },
    {
      "requirement": "Freshness assessment is application-owned",
      "name": "Freshness comparison is used only by MaterializeSpecMetadata",
      "when": ["a repository adapter or ordinary consumer needs a freshness decision"],
      "then": [
        "it does not reimplement assessMetadataFreshness independently",
        "AND MaterializeSpecMetadata is the internal caller that uses the comparison to decide reuse versus regeneration"
      ]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "dependsOn adds context beyond excludes",
      "when": [
        "specd.yaml has contextExcludeSpecs: ['core/storage'] and a spec's metadata lists core/storage in dependsOn"
      ],
      "then": ["CompileContext includes core/storage — dependsOn overrides project-level excludes"]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "Transitive traversal",
      "when": [
        "change has specIds: ['default:auth/login'] and auth/login metadata lists auth/jwt in dependsOn, and auth/jwt metadata lists crypto/keys"
      ],
      "then": ["CompileContext includes all three: auth/login, auth/jwt, and crypto/keys"]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "Canonical metadata dependency projection works without extraction",
      "given": [
        "a persisted spec has metadata.json.dependsOn: ['core:storage']",
        "AND its schema omits metadataExtraction.dependsOn"
      ],
      "when": ["a context consumer traverses dependencies"],
      "then": [
        "core:storage is still discovered from metadata",
        "AND no direct sidecar artifact read is required"
      ]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "Materialization returns a confirmed-fresh or just-regenerated value",
      "given": ["a spec whose persisted metadata may be missing, invalid, or stale"],
      "when": [
        "a consumer requests its metadata through GetSpecMetadata / MaterializeSpecMetadata"
      ],
      "then": [
        "the consumer receives a value that is either confirmed fresh or was just regenerated from current source state",
        "AND the consumer does not implement its own missing/stale fallback logic"
      ]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "readMetadataSnapshot is not used by ordinary consumers for freshness decisions",
      "when": [
        "CompileContext or another context-oriented consumer needs the canonical normalized representation of a persisted spec"
      ],
      "then": [
        "it obtains it through Core metadata materialization",
        "AND it does not call SpecRepository.readMetadataSnapshot() directly to make a freshness decision"
      ]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "Missing spec in dependsOn skipped with warning",
      "when": ["dependsOn references a spec ID that does not exist on disk"],
      "then": ["specd emits a warning and skips that entry — context compilation continues"]
    },
    {
      "requirement": "Use by CompileContext",
      "name": "Cycle in dependsOn broken with warning",
      "when": ["spec A lists spec B in dependsOn and spec B lists spec A in dependsOn"],
      "then": [
        "specd detects the cycle, breaks it, emits a warning, and includes both specs only once"
      ]
    },
    {
      "requirement": "Version control",
      "name": "New project gitignores the metadata cache directory",
      "given": ["a newly initialized project"],
      "when": ["the metadata cache directory is initialized"],
      "then": ["a rooted /.specd/metadata/ entry is added to the project-root .gitignore"]
    },
    {
      "requirement": "Version control",
      "name": "Rooted ignore entry does not affect similarly named nested directories",
      "given": [
        "a nested directory elsewhere in the repo that happens to be named .specd/metadata"
      ],
      "when": ["the root .gitignore entry /.specd/metadata/ is evaluated"],
      "then": ["only the project-root metadata cache directory is ignored"]
    },
    {
      "requirement": "Version control",
      "name": "Existing tracked metadata is not automatically untracked",
      "given": ["a project that already tracks generated metadata files in Git"],
      "when": ["the gitignore entry is added"],
      "then": [
        "previously tracked metadata files remain tracked until an explicit one-time migration removes them"
      ]
    },
    {
      "requirement": "Version control",
      "name": "Custom metadataPath is the operator's responsibility",
      "given": [
        "a project configures a custom filesystem metadataPath outside the default location"
      ],
      "when": ["that path is used for generated metadata"],
      "then": [
        "runtime does not rewrite .gitignore for that custom path",
        "AND keeping it out of version control is the operator's responsibility"
      ]
    },
    {
      "requirement": "Version control",
      "name": "Implementation files projected into metadata",
      "given": ["a spec with file-level implementation links in the semantic repository"],
      "when": ["spec metadata is generated"],
      "then": ["the implementation.files array contains the linked file paths"]
    },
    {
      "requirement": "Version control",
      "name": "Implementation symbols projected into metadata",
      "given": ["a spec with symbol-level implementation links in the semantic repository"],
      "when": ["spec metadata is generated"],
      "then": ["the implementation.symbols array contains the linked symbol identities"]
    },
    {
      "requirement": "Version control",
      "name": "Semantic repository source prioritized",
      "given": ["a spec with implementation links in the semantic repository"],
      "when": ["metadata is generated"],
      "then": [
        "the implementation links are sourced from the repository semantic operations, NOT by parsing sidecar files"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "core:spec-metadata",
        "file": "core:src/domain/services/parse-metadata.ts",
        "symbol": "SpecMetadataProvenance"
      },
      {
        "specId": "core:spec-metadata",
        "file": "core:src/domain/services/parse-metadata.ts",
        "symbol": "SpecMetadata"
      },
      {
        "specId": "core:spec-metadata",
        "file": "core:src/domain/services/parse-metadata.ts",
        "symbol": "MetadataSnapshot"
      },
      {
        "specId": "core:spec-metadata",
        "file": "core:src/domain/services/assess-metadata-freshness.ts",
        "symbol": "assessMetadataFreshness"
      }
    ]
  },
  "optimizedDescription": "metadata.json: deterministic normalized projection of spec; provenance freshness; lock owns truth.",
  "optimizedContext": "# Spec Metadata\n## Rules\n### Location\n- `.specd/metadata/<workspace>/<capability-path>/metadata.json` via `specs.fs.metadataPath` or VCS-root derive / NullVcs fallback.\n- Absence = no deps / no hash — not error.\n### Sidecar / stamp\n- Get schema/deps/impl via SpecRepository semantic ops — not raw sidecar files.\n- `generatedMetadataStamp` = `{ present, lastModified }` only — not substitute for `metadata()`.\n### Format (optional fields; empty/absent OK)\n- title, description, keywords, dependsOn (projection of persisted deps), contentHashes, rules, constraints, scenarios, context, optimizedDescription/Context (from lock optimizations when fresh — omit if stale/absent), generatedBy always `\"core\"`, provenance.\n### Provenance\n- artifacts{hash,lastModified}, persistedStateHash, schema{name,version}, projectionVersion, projectionFingerprint. Freshness uses hashes/schema/version/fingerprint — never lastModified alone. contentHashes MUST match provenance.artifacts.\n### Persist guard\n- `PersistSpecMetadata` validates `strictSpecMetadataSchema` before write (title/description/contentHashes required). Internal only. Unknown keys passthrough.\n### Archive-time generation\n- Preflight extract+dependsOn consistency before any publish; post-publish GenerateSpecMetadata + persist. dependsOn from change.specDependsOn; extraction must match or fail.\n### Freshness\n- Only `assessMetadataFreshness(persisted, current)` — SpecRepository does not classify freshness. MaterializeSpecMetadata decides reuse vs regen.\n### Consumers\n- CompileContext etc. via GetSpecMetadata/MaterializeSpecMetadata — not raw readMetadataSnapshot for usability.\n### VCS / implementation\n- Disposable cache; default `/.specd/metadata/` gitignored. implementation.files / .symbols projected from persisted links when present.\n## Constraints\n- Not a schema artifact; never ValidateArtifacts/manifest-tracked.\n- dependsOn projection only — never independently authored. No self-deps; cycles broken+warn on traversal.\n- Access via readMetadataSnapshot/writeMetadataSnapshot — not artifact()/save(). Lock owns deps/impl/optimizations; metadata regenerable projection.\n- parseMetadata lenient → {} on invalid.",
  "contentHashes": {
    "spec.md": "sha256:dac1bf779c810ceaa16396bd66bc9b3f259366ddb55ae90c88cbbbfd718c9c34",
    "verify.md": "sha256:d70a500cd45538e2069183858db04535f4dfd690686600a4500c96c591c3b3d1"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:dac1bf779c810ceaa16396bd66bc9b3f259366ddb55ae90c88cbbbfd718c9c34",
        "lastModified": "2026-07-25T11:53:31.147Z"
      },
      "verify.md": {
        "hash": "sha256:d70a500cd45538e2069183858db04535f4dfd690686600a4500c96c591c3b3d1",
        "lastModified": "2026-07-25T11:53:31.147Z"
      }
    },
    "persistedStateHash": "sha256:4430c4e158667903fc517babd6dc03c34e2539cdd6a61e9fb83cfe0354caf963",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
