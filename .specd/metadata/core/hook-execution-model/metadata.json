{
  "title": "Hook Execution Model",
  "description": "Workflow steps declare hooks via `instruction:` and `run:` entries, but the schema format only defines their YAML structure — not who executes each type, when execution happens, or what failure means in each context. Without a unified execution model, hook behaviour is scattered across use cases and undocumented agent conventions. This spec defines the complete hook execution model: the two hook types and their consumers, hook execution for transitions and archives, failure semantics for each phase, hook ordering rules, the `--skip-hooks` pattern for manual control, and the boundary between compile-time and runtime concerns.",
  "dependsOn": [
    "core:workflow-model",
    "core:schema-format",
    "core:hook-runner-port",
    "core:transition-change",
    "core:archive-change",
    "core:run-step-hooks",
    "core:get-hook-instructions",
    "core:config",
    "cli:change-transition",
    "cli:change-archive"
  ],
  "context": [
    "Workflow steps declare hooks via `instruction:` and `run:` entries, but the schema format only defines their YAML structure — not who executes each type, when execution happens, or what failure means in each context. Without a unified execution model, hook behaviour is scattered across use cases and undocumented agent conventions. This spec defines the complete hook execution model: the two hook types and their consumers, hook execution for transitions and archives, failure semantics for each phase, hook ordering rules, the `--skip-hooks` pattern for manual control, and the boundary between compile-time and runtime concerns."
  ],
  "rules": [
    {
      "requirement": "Two hook types",
      "rules": [
        "Workflow hooks come in exactly two types, distinguished by their key:\n\n- **`instruction:`** — a text block consumed by `GetHookInstructions` at query time. It is returned as contextual guidance for the agent or external tool. `instruction:` hooks are never executed at runtime — they have no process, exit code, or side effects.\n- **`run:`** — a shell command executed by the `HookRunner` port. It produces an exit code, stdout, and stderr. `run:` hooks are never injected into agent context — they are operational commands.\n\nEvery hook entry declares exactly one of these two keys alongside its `id`. An entry with both keys or neither key is a `SchemaValidationError` (enforced by schema validation, not by this model).\n\nAll workflow steps can declare both `instruction:` and `run:` hooks — there are no restrictions by step type."
      ]
    },
    {
      "requirement": "External hooks are explicit workflow entries",
      "rules": [
        "Workflow hooks SHALL support an explicit external hook entry type in addition to `instruction:` and shell `run:` hooks.\n\nExternal hooks are distinct from shell `run:` hooks:\n\n- `HookRunner` continues to execute shell `run:` hooks only\n- explicit external hook entries are dispatched to external hook runners\n- external hook dispatch is determined by whether a registered external runner declares support for the hook's `external.type`\n\nThe workflow shape for an explicit external hook entry is `external: { type, config }`.\n\nIf no external runner accepts the hook's `external.type`, execution MUST fail with a clear error."
      ]
    },
    {
      "requirement": "External hooks follow workflow phase semantics",
      "rules": [
        "Explicit external hooks SHALL follow the same pre-phase and post-phase workflow semantics as shell `run:` hooks:\n\n- pre-phase failures are fail-fast\n- post-phase failures are reported without rolling back completed work\n\nThe difference is the dispatch backend, not the lifecycle semantics."
      ]
    },
    {
      "requirement": "instruction hooks are passive text",
      "rules": [
        "`instruction:` hooks are passive text blocks — they are never executed as processes. They have no exit code, stdout, stderr, or side effects. `TransitionChange`, `ArchiveChange`, and `RunStepHooks` MUST skip `instruction:` entries.\n\n`instruction:` hooks are consumed exclusively by `GetHookInstructions`, which returns instruction text for a specific step+phase, optionally filtered by hook ID. This enables both native specd skills and external agent tools (e.g. Claude Code hooks) to retrieve instruction text independently from context compilation. `CompileContext` does NOT include instruction hooks — it assembles context (specs, metadata, artifact rules), not step instructions."
      ]
    },
    {
      "requirement": "Default hook execution for transitions and archives",
      "rules": [
        "By default, `TransitionChange` and `ArchiveChange` auto-execute `run:` hooks at step boundaries:\n\n- **`TransitionChange`** — when transitioning to a state that has a workflow step with `run:` hooks, executes pre-hooks before the state change and post-hooks after. Pre-hook failure aborts the transition (fail-fast). Post-hook failures are collected but do not roll back (fail-soft). Hook execution is delegated to `RunStepHooks`.\n- **`ArchiveChange`** — executes pre-archive hooks before any file modifications and post-archive hooks after the archive completes. Pre-hook failure aborts the archive. Post-hook failures are collected. Hook execution is delegated to `RunStepHooks`.\n\nBoth use cases delegate hook execution to `RunStepHooks`, which handles hook collection, variable expansion, and execution semantics."
      ]
    },
    {
      "requirement": "Two execution modes for run hooks",
      "rules": [
        "`RunStepHooks` supports **fail-fast** for pre-hooks and **fail-soft** for post-hooks."
      ]
    },
    {
      "requirement": "change transition does not execute hooks",
      "rules": [
        "The `Change` entity itself MUST NOT execute hooks. Hook execution is an application-layer responsibility managed by use cases (like `TransitionChange`)."
      ]
    },
    {
      "requirement": "Manual hook control with skipHooks",
      "rules": [
        "Manual hook control uses phase selectors rather than a separate boolean flag.\n\n- `TransitionChange` accepts `skipHookPhases` values `'source.pre'`, `'source.post'`, `'target.pre'`, `'target.post'`, and `'all'`.\n- `ArchiveChange` accepts `skipHookPhases` values `'pre'`, `'post'`, and `'all'`.\n\nWhen the selector set contains all phases for the current use case, `TransitionChange` or `ArchiveChange` skips every `run:` hook. This gives the caller (typically an LLM agent) manual control over hook timing:\n\n1. Call `specd change hook-instruction <name> <step> --phase pre` to read `instruction:` hooks\n2. Call `specd change run-hooks <name> <step> --phase pre` to execute `run:` pre-hooks\n3. Call the lifecycle command with `--skip-hooks ...` to perform the transition or archive without the skipped auto-hooks\n4. Call `specd change run-hooks <name> <step> --phase post` to execute `run:` post-hooks\n5. Call `specd change hook-instruction <name> <step> --phase post` to read post-step instructions\n\nThe `--skip-hooks` CLI flag maps to the `skipHookPhases` selector set in the corresponding use case input. The agent is responsible for invoking hooks at the appropriate time."
      ]
    },
    {
      "requirement": "Pre-hook failure semantics",
      "rules": [
        "When a `run:` pre-hook exits with a non-zero code:\n\n- **`TransitionChange`** — throws `HookFailedError`. No state transition occurs.\n- **`ArchiveChange`** — throws `HookFailedError`. No files are modified.\n- **`RunStepHooks` (standalone)** — returns immediately with the failure result. The CLI exits with code 2. The agent SHOULD NOT proceed with the step's work and SHOULD offer to fix the problem before retrying.\n\nIn all cases, pre-hooks use **fail-fast** semantics: execution stops at the first failure, and subsequent hooks in the list are not run."
      ]
    },
    {
      "requirement": "Post-hook failure semantics",
      "rules": [
        "When a `run:` post-hook exits with a non-zero code:\n\n- **`TransitionChange`** — collects the failure in `postHookFailures` but does not roll back the transition. All remaining post-hooks continue to execute.\n- **`ArchiveChange`** — collects the failure in `postHookFailures` but does not roll back the archive. All remaining post-hooks continue to execute.\n- **`RunStepHooks` (standalone)** — continues executing remaining post-hooks, collecting all failures. The CLI exits with code 2 if any post-hook failed.\n\nIn all cases, post-hooks use **fail-soft** semantics: all hooks execute regardless of individual failures, and failures are collected rather than thrown."
      ]
    },
    {
      "requirement": "Hook ordering",
      "rules": [
        "Hooks are executed in the following order within each phase (`pre` or `post`):\n\n1. **Schema-level hooks** — entries from the schema's `workflow[step].hooks.pre` or `workflow[step].hooks.post`, in declaration order\n2. **Project-level hooks** — entries from the project's `schemaOverrides` targeting `workflow[step].hooks.pre` or `workflow[step].hooks.post`, in declaration order\n\nSchema hooks always precede project hooks within the same phase. Within each level, declaration order is preserved."
      ]
    },
    {
      "requirement": "Template variable expansion",
      "rules": [
        "Before executing a `run:` hook command, `HookRunner` expands `{{key.path}}` template variables using a `HookVariables` object. The variables available are:\n\n- `{{change.name}}` — the change name\n- `{{change.workspace}}` — the primary workspace name\n- `{{change.path}}` — the absolute path to the change directory\n- `{{project.root}}` — the absolute path to the project root\n\nUnknown variable paths are left unexpanded (the original `{{key.path}}` token is preserved). All substituted values are shell-escaped to prevent injection attacks."
      ]
    }
  ],
  "constraints": [
    "instruction: hooks are never executed — they are passive text consumed exclusively by GetHookInstructions",
    "run: hooks are never injected into agent context — they are commands executed by HookRunner",
    "All workflow steps can have both instruction: and run: hooks — no restrictions by step type",
    "Pre-hooks use fail-fast semantics in all execution contexts",
    "Post-hooks use fail-soft semantics in all execution contexts",
    "Schema-level hooks always precede project-level hooks within the same phase",
    "TransitionChange and ArchiveChange delegate hook execution to RunStepHooks",
    "RunStepHooks is the single hook execution engine used by all use cases and the CLI",
    "Template variable expansion and shell escaping are handled by HookRunner, not by callers"
  ],
  "scenarios": [
    {
      "requirement": "Two hook types",
      "name": "instruction hook is not executed by RunStepHooks",
      "given": [
        "a workflow step with hooks pre: [{ id: \"guidance\", instruction: \"Read the tasks\" }, { id: \"lint\", run: \"pnpm lint\" }]"
      ],
      "when": ["RunStepHooks executes pre-hooks for this step"],
      "then": [
        "only the lint hook is executed via HookRunner",
        "AND the guidance instruction hook is skipped entirely"
      ]
    },
    {
      "requirement": "External hooks are explicit workflow entries",
      "name": "External hook dispatch uses accepted-type declarations",
      "given": [
        "a workflow step with an explicit external hook entry whose external.type is docker",
        "AND a registered external hook runner declares support for docker"
      ],
      "when": ["the step's executable hooks are run"],
      "then": [
        "the hook is dispatched to that external runner",
        "AND HookRunner is not used for that entry"
      ]
    },
    {
      "requirement": "External hooks are explicit workflow entries",
      "name": "No runner accepts the external hook type",
      "given": [
        "a workflow step with an explicit external hook entry whose external.type is webhook",
        "AND no registered external hook runner declares support for webhook"
      ],
      "when": ["the step's executable hooks are run"],
      "then": ["execution fails with a clear unknown external hook type error"]
    },
    {
      "requirement": "External hooks follow workflow phase semantics",
      "name": "Pre-phase external hook failure is fail-fast",
      "given": ["pre-hooks include an explicit external hook that fails"],
      "when": ["the phase is executed"],
      "then": [
        "subsequent hooks in that pre phase are not run",
        "AND the failure aborts the phase just like a failing shell run: hook"
      ]
    },
    {
      "requirement": "External hooks follow workflow phase semantics",
      "name": "Post-phase external hook failure is reported without rollback",
      "given": ["post-hooks include a failing explicit external hook followed by another hook"],
      "when": ["the phase is executed"],
      "then": [
        "later post-hooks still run",
        "AND the failure is reported without rolling back completed work"
      ]
    },
    {
      "requirement": "instruction hooks are passive text",
      "name": "CompileContext does not include instruction hooks",
      "given": [
        "a workflow step with hooks.pre: [{ id: \"read-tasks\", instruction: \"Read pending tasks\" }]"
      ],
      "when": ["CompileContext compiles context for this step"],
      "then": [
        "the instruction text does NOT appear in the instruction block",
        "AND instruction hooks are only available via GetHookInstructions"
      ]
    },
    {
      "requirement": "instruction hooks are passive text",
      "name": "ArchiveChange skips instruction hooks",
      "given": [
        "the archiving step with hooks.pre: [{ id: \"review\", instruction: \"Review deltas\" }, { id: \"test\", run: \"pnpm test\" }]"
      ],
      "when": ["ArchiveChange executes pre-archive hooks"],
      "then": ["only pnpm test is executed", "AND the review instruction hook is skipped"]
    },
    {
      "requirement": "instruction hooks are passive text",
      "name": "Individual instruction query by hook ID",
      "given": [
        "a workflow step with hooks.pre: [{ id: \"read-tasks\", instruction: \"Read pending tasks\" }, { id: \"lint\", run: \"pnpm lint\" }]"
      ],
      "when": ["GetHookInstructions is called with step, phase: \"pre\", and only: \"read-tasks\""],
      "then": [
        "only the text \"Read pending tasks\" is returned",
        "AND the lint run hook is not included"
      ]
    },
    {
      "requirement": "instruction hooks are passive text",
      "name": "All instructions returned when no filter",
      "given": [
        "a step with hooks.pre: [{ id: \"a\", instruction: \"First\" }, { id: \"b\", run: \"cmd\" }, { id: \"c\", instruction: \"Second\" }]"
      ],
      "when": ["GetHookInstructions is called with phase: \"pre\" and no only filter"],
      "then": [
        "both instruction hooks are returned in order: a then c",
        "AND the run: hook b is not included"
      ]
    },
    {
      "requirement": "Two execution modes for run hooks",
      "name": "Transition skips all hooks",
      "given": ["a change transition command"],
      "when": ["the agent runs specd change transition <name> implementing --skip-hooks all"],
      "then": ["the transition command skips all transition hook execution"]
    },
    {
      "requirement": "Two execution modes for run hooks",
      "name": "Archive skips all hooks",
      "given": ["an archive command"],
      "when": ["the agent runs specd change archive <name> --skip-hooks all"],
      "then": ["the archive command skips all archive hook execution"]
    },
    {
      "requirement": "Two execution modes for run hooks",
      "name": "Archive skips only pre hooks",
      "given": ["the archiving step has both pre and post run: hooks"],
      "when": ["the agent runs specd change archive <name> --skip-hooks pre"],
      "then": ["pre hooks are skipped", "AND post hooks remain enabled"]
    },
    {
      "requirement": "Two execution modes for run hooks",
      "name": "Archive skips only post hooks",
      "given": ["the archiving step has both pre and post run: hooks"],
      "when": ["the agent runs specd change archive <name> --skip-hooks post"],
      "then": ["post hooks are skipped", "AND pre hooks remain enabled"]
    },
    {
      "requirement": "Pre-hook failure semantics",
      "name": "Deterministic pre-hook failure aborts archive",
      "given": [
        "the archiving step with pre-hooks [{ id: \"test\", run: \"pnpm test\" }, { id: \"lint\", run: \"pnpm lint\" }]",
        "AND pnpm test exits with code 1"
      ],
      "when": ["ArchiveChange executes pre-hooks"],
      "then": [
        "HookFailedError is thrown with command pnpm test, exit code 1, and stderr",
        "AND pnpm lint is never executed",
        "AND no files are modified"
      ]
    },
    {
      "requirement": "Pre-hook failure semantics",
      "name": "Agent-driven pre-hook failure exits code 2",
      "given": [
        "the implementing step with pre-hooks [{ id: \"check\", run: \"pnpm typecheck\" }]",
        "AND pnpm typecheck exits with code 1"
      ],
      "when": ["the agent runs specd change run-hooks <name> implementing --phase pre"],
      "then": ["the CLI exits with code 2", "AND the result includes the failed hook details"]
    },
    {
      "requirement": "Post-hook failure semantics",
      "name": "Deterministic post-hook failure does not rollback",
      "given": [
        "the archiving step with post-hooks [{ id: \"branch\", run: \"git checkout -b x\" }, { id: \"notify\", run: \"curl webhook\" }]",
        "AND git checkout -b x exits with code 1"
      ],
      "when": ["ArchiveChange executes post-hooks"],
      "then": [
        "curl webhook is still executed",
        "AND both results are collected",
        "AND postHookFailures includes the branch hook failure",
        "AND the archive is NOT rolled back"
      ]
    },
    {
      "requirement": "Post-hook failure semantics",
      "name": "Agent-driven post-hook failure continues execution",
      "given": [
        "the implementing step with post-hooks [{ id: \"test\", run: \"pnpm test\" }, { id: \"lint\", run: \"pnpm lint\" }]",
        "AND pnpm test exits with code 1"
      ],
      "when": ["the agent runs specd change run-hooks <name> implementing --phase post"],
      "then": [
        "pnpm lint is still executed",
        "AND the CLI exits with code 2 (at least one hook failed)",
        "AND results for both hooks are returned"
      ]
    },
    {
      "requirement": "Hook ordering",
      "name": "Schema hooks execute before project hooks",
      "given": [
        "schema-level pre-hooks [{ id: \"schema-lint\", run: \"pnpm lint\" }]",
        "AND project-level pre-hooks [{ id: \"project-check\", run: \"pnpm typecheck\" }]"
      ],
      "when": ["hooks are executed for this step's pre phase"],
      "then": ["pnpm lint executes first", "AND pnpm typecheck executes second"]
    },
    {
      "requirement": "change transition does not execute hooks",
      "name": "Transition with hooks defined does not run them",
      "given": ["the implementing step has run: pre-hooks defined"],
      "when": ["the agent runs specd change transition <name> implementing"],
      "then": [
        "the transition succeeds",
        "AND no hooks are executed",
        "AND the change state is updated to implementing"
      ]
    },
    {
      "requirement": "Default hook execution for transitions and archives",
      "name": "TransitionChange executes pre-hooks before state change",
      "given": ["a transition to implementing step with pre-hooks"],
      "when": ["TransitionChange.execute is called"],
      "then": [
        "pre-hooks are executed before the state transition",
        "AND pre-hook failure aborts the transition"
      ]
    },
    {
      "requirement": "Default hook execution for transitions and archives",
      "name": "TransitionChange executes post-hooks after state change",
      "given": ["a transition to implementing step with post-hooks"],
      "when": ["TransitionChange.execute is called"],
      "then": [
        "post-hooks are executed after the state transition",
        "AND post-hook failures are collected without rollback"
      ]
    },
    {
      "requirement": "Default hook execution for transitions and archives",
      "name": "ArchiveChange executes pre-hooks before archive",
      "given": ["archiving step with pre-hooks"],
      "when": ["ArchiveChange.execute is called"],
      "then": [
        "pre-hooks are executed before any file modifications",
        "AND pre-hook failure aborts the archive"
      ]
    },
    {
      "requirement": "Default hook execution for transitions and archives",
      "name": "ArchiveChange executes post-hooks after archive",
      "given": ["archiving step with post-hooks"],
      "when": ["ArchiveChange.execute is called"],
      "then": [
        "post-hooks are executed after the archive completes",
        "AND post-hook failures are collected without rollback"
      ]
    },
    {
      "requirement": "Default hook execution for transitions and archives",
      "name": "Hook execution delegated to RunStepHooks",
      "given": ["TransitionChange needs to execute hooks"],
      "when": ["hook execution is triggered"],
      "then": ["RunStepHooks is used for collection, variable expansion, and execution"]
    },
    {
      "requirement": "Manual hook control with skipHooks",
      "name": "TransitionChange accepts skipHookPhases selector",
      "when": ["TransitionChange.execute is called with skipHookPhases: ['all']"],
      "then": ["all run: hooks are skipped for that transition"]
    },
    {
      "requirement": "Manual hook control with skipHooks",
      "name": "ArchiveChange accepts skipHookPhases selector",
      "when": ["ArchiveChange.execute is called with skipHookPhases: ['pre']"],
      "then": ["pre-hooks are skipped for that archive"]
    },
    {
      "requirement": "Manual hook control with skipHooks",
      "name": "Phase selector skips specific phases",
      "given": [
        "TransitionChange accepts phase selectors 'source.pre', 'source.post', 'target.pre', 'target.post'"
      ],
      "when": ["skipHookPhases: ['target.pre'] is provided"],
      "then": ["only target pre-hooks are skipped"]
    },
    {
      "requirement": "Template variable expansion",
      "name": "HookRunner expands template variables in run commands",
      "given": ["a run: hook with command \"echo {{change.name}}\""],
      "when": ["the hook is executed via HookRunner"],
      "then": ["{{change.name}} is substituted with the change name value"]
    },
    {
      "requirement": "Template variable expansion",
      "name": "Unknown template variables left unexpanded",
      "given": ["a run: hook with command \"echo {{unknown.variable}}\""],
      "when": ["the hook is executed"],
      "then": ["the literal {{unknown.variable}} token is preserved in the command"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:70a36036b7fe87acb54c95fcecb3d9e7845c1b15284cec14d7ff5e2f16ad4b96",
    "verify.md": "sha256:351f4e7ea9e422c71e9869e306f340a153eecb5dc0727bc42652388bd2dd7dd3"
  },
  "generatedBy": "agent",
  "optimizedDescription": "Workflow steps declare hooks via instruction: run: entries, but schema format only defines YAML structure — not who executes each type, when executio…",
  "optimizedContext": "# Hook Execution Model\n\n## Rules\n- Two hook types: Workflow hooks come exactly two types, distinguished key: - instruction: — text block consumed GetHookInstructions query time. returned contextual guidance agent external tool. instruction: hooks never executed runtime — have no process, exit code, side effects. - run: — shell command executed HookRunner port. produces exit code, stdout, stderr. run: hooks never injected agent context — operational commands. Every hook entry declares exactly one two keys alongside id. entry both keys neither key SchemaValidationError (enforced schema validation, not model). All workflow steps can declare both instruction: run: hooks — there no restrictions step type.\n- External hooks are explicit workflow entries: Workflow hooks SHALL support explicit external hook entry type addition instruction: shell run: hooks. External hooks distinct shell run: hooks: - HookRunner continues execute shell run: hooks only - explicit external hook entries dispatched external hook runners - external hook dispatch determined whether registered external runner declares support hook's external. type workflow shape explicit external hook entry external: { type, config }. If no external runner accepts hook's external. type, execution MUST fail clear error.\n- External hooks follow workflow phase semantics: Explicit external hooks SHALL follow same pre-phase post-phase workflow semantics shell run: hooks: - pre-phase failures fail-fast - post-phase failures reported rolling back completed work difference dispatch backend, not lifecycle semantics.\n- instruction hooks are passive text: instruction: hooks passive text blocks — never executed processes. have no exit code, stdout, stderr, side effects. TransitionChange, ArchiveChange, RunStepHooks MUST skip instruction: entries. instruction: hooks consumed exclusively GetHookInstructions, which returns instruction text specific step+phase, optionally filtered hook ID. enables both native specd skills external agent tools (e. g. Claude Code hooks) retrieve instruction text independently context compilation. CompileContext does NOT include instruction hooks — assembles context (specs, metadata, artifact rules), not step instructions.\n- Default hook execution for transitions and archives: default, TransitionChange ArchiveChange auto-execute run: hooks step boundaries: - TransitionChange — when transitioning state has workflow step run: hooks, executes pre-hooks before state change post-hooks after. Pre-hook failure aborts transition (fail-fast). Post-hook failures collected but do not roll back (fail-soft). Hook execution delegated RunStepHooks. - ArchiveChange — executes pre-archive hooks before any file modifications post-archive hooks after archive completes. Pre-hook failure aborts archive. Post-hook failures collected. Hook execution delegated RunStepHooks. Both use cases delegate hook execution RunStepHooks, which handles hook collection, variable expansion, execution…\n- Two execution modes for run hooks: RunStepHooks supports fail-fast pre-hooks fail-soft post-hooks.\n- change transition does not execute hooks: Change entity itself MUST NOT execute hooks. Hook execution application-layer responsibility managed use cases (like TransitionChange).\n- Manual hook control with skipHooks: Manual hook control uses phase selectors rather than separate boolean flag. - TransitionChange accepts skipHookPhases values 'source. pre', 'source. post', 'target. pre', 'target. post', 'all'. - ArchiveChange accepts skipHookPhases values 'pre', 'post', 'all'. When selector set contains all phases current use case, TransitionChange ArchiveChange skips every run: hook. gives caller (typically LLM agent) manual control over hook timing: 1. Call specd change hook-instruction <name> <step> --phase pre read instruction: hooks 2. Call specd change run-hooks <name> <step> --phase pre execute run: pre-hooks 3. Call lifecycle command --skip-hooks. . . perform transition archive skipped auto-hooks 4…\n- Pre-hook failure semantics: When run: pre-hook exits non-zero code: - TransitionChange — throws HookFailedError. No state transition occurs. - ArchiveChange — throws HookFailedError. No files modified. - RunStepHooks (standalone) — returns immediately failure result. CLI exits code 2. agent SHOULD NOT proceed step's work SHOULD offer fix problem before retrying. all cases, pre-hooks use fail-fast semantics: execution stops first failure, subsequent hooks list not run.\n- Post-hook failure semantics: When run: post-hook exits non-zero code: - TransitionChange — collects failure postHookFailures but does not roll back transition. All remaining post-hooks continue execute. - ArchiveChange — collects failure postHookFailures but does not roll back archive. All remaining post-hooks continue execute. - RunStepHooks (standalone) — continues executing remaining post-hooks, collecting all failures. CLI exits code 2 if any post-hook failed. all cases, post-hooks use fail-soft semantics: all hooks execute regardless individual failures, failures collected rather than thrown.\n- Hook ordering: Hooks executed following order within each phase (pre post): 1. Schema-level hooks — entries schema's workflow[step]. hooks. pre workflow[step]. hooks. post, declaration order 2. Project-level hooks — entries project's schemaOverrides targeting workflow[step]. hooks. pre workflow[step]. hooks. post, declaration order Schema hooks always precede project hooks within same phase. Within each level, declaration order preserved.\n- Template variable expansion: Before executing run: hook command, HookRunner expands {{key. path}} template variables using HookVariables object. variables available are: - {{change. name}} — change name - {{change. workspace}} — primary workspace name - {{change. path}} — absolute path change directory - {{project. root}} — absolute path project root Unknown variable paths left unexpanded original {{key. path}} token preserved). All substituted values shell-escaped prevent injection attacks."
}
