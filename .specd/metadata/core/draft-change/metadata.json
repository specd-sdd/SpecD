{
  "title": "DraftChange",
  "description": "Users need to pause work on a change without losing its state or history — for example, to context-switch or wait on a dependency. The `DraftChange` use case shelves an existing change by appending a `drafted` event to its history, and the repository implementation moves the change directory from `changes/` to `drafts/` when persisted.",
  "dependsOn": ["core:change", "default:_global/architecture", "core:drafted-change-view"],
  "context": [
    "Users need to pause work on a change without losing its state or history — for example, to context-switch or wait on a dependency. The `DraftChange` use case shelves an existing change by appending a `drafted` event to its history, and the repository implementation moves the change directory from `changes/` to `drafts/` when persisted."
  ],
  "rules": [
    {
      "requirement": "Input contract",
      "rules": [
        "`DraftChange.execute` SHALL accept a `DraftChangeInput` with the following fields:\n\n- `name` (string, required) — the change to shelve\n- `reason` (string, optional) — explanation for shelving the change\n- `force` (boolean, optional) — explicit override for the historical implementation guard"
      ]
    },
    {
      "requirement": "Change must exist",
      "rules": [
        "The use case MUST load the change from active storage via `ChangeRepository.get(name)`. If no active change exists with the given name, it MUST throw `ChangeNotFoundError`.\n\nThe use case MUST NOT load a change that exists only under `drafts/`."
      ]
    },
    {
      "requirement": "Actor resolution",
      "rules": [
        "The use case MUST resolve the current actor identity via the `ActorResolver` port. The resolved actor is recorded in the `drafted` event."
      ]
    },
    {
      "requirement": "Historical implementation guard",
      "rules": [
        "`DraftChange` SHALL respect the `Change` entity's historical implementation guard.\n\nIf the loaded change has ever reached `implementing`, `DraftChange.execute` SHALL fail by default instead of shelving it, because implementation may already exist and moving the change to `drafts/` would risk leaving permanent specs and code out of sync.\n\nThe use case MAY proceed only when `input.force === true`, in which case the guard is bypassed intentionally."
      ]
    },
    {
      "requirement": "Drafted event appended to history",
      "rules": [
        "The use case MUST call `change.draft(actor, reason, force)` to append a `drafted` event to the change's history after the historical implementation guard has been satisfied or explicitly bypassed. The event records:\n\n- `type`: `'drafted'`\n- `at`: the current timestamp\n- `by`: the resolved actor identity\n- `reason`: included only when provided in the input"
      ]
    },
    {
      "requirement": "Persistence",
      "rules": [
        "After appending the drafted event, the use case MUST persist the change through `ChangeRepository.mutate(name, fn)`.\n\nInside the mutation callback, the repository supplies the fresh persisted active `Change`; the use case calls `change.draft(actor, reason, force)` on that instance and returns it. When the callback resolves, the repository persists the updated manifest and performs the required directory relocation to `drafts/`.\n\n`DraftChange.execute` MUST return a `DraftedChangeView` for the drafted change, produced from the persisted drafted state (via `getDraft(name)` or an equivalent repository mapper). It MUST NOT return a mutable `Change` to callers."
      ]
    },
    {
      "requirement": "Dependencies",
      "rules": [
        "`DraftChange` depends on two ports injected via constructor:\n\n- `ChangeRepository` — for loading and persistence\n- `ActorResolver` — for resolving the current actor identity"
      ]
    }
  ],
  "constraints": [
    "The use case MUST NOT perform any state transitions — drafting is orthogonal to lifecycle state",
    "The use case delegates directory relocation entirely to the repository layer",
    "The isDrafted property on the Change entity is derived from the most recent drafted or restored event in history"
  ],
  "scenarios": [
    {
      "requirement": "Change must exist",
      "name": "Non-existent change is rejected",
      "when": ["DraftChange.execute is called with a name that does not exist in active storage"],
      "then": ["it throws ChangeNotFoundError"]
    },
    {
      "requirement": "Change must exist",
      "name": "Drafted-only name is rejected",
      "given": ["a change exists only under drafts/"],
      "when": ["DraftChange.execute is called with its name"],
      "then": ["it throws ChangeNotFoundError"]
    },
    {
      "requirement": "Change must exist",
      "name": "Discarded-only name is rejected",
      "given": ["a change exists only under discarded/"],
      "when": ["DraftChange.execute is called with its name"],
      "then": ["it throws ChangeNotFoundError"]
    },
    {
      "requirement": "Actor resolution",
      "name": "Actor identity is recorded in the drafted event",
      "when": ["DraftChange.execute is called"],
      "then": [
        "the drafted event's by field matches the identity returned by ActorResolver.identity()"
      ]
    },
    {
      "requirement": "Historical implementation guard",
      "name": "Historically implemented change is rejected by default",
      "given": ["the loaded change has previously reached implementing"],
      "when": ["DraftChange.execute is called without force"],
      "then": [
        "it fails without appending a drafted event",
        "AND the error explains that implementation may already exist and specs and code could be left out of sync"
      ]
    },
    {
      "requirement": "Historical implementation guard",
      "name": "Force bypasses the historical implementation guard",
      "given": ["the loaded change has previously reached implementing"],
      "when": ["DraftChange.execute is called with force: true"],
      "then": ["the use case succeeds", "AND it appends a drafted event"]
    },
    {
      "requirement": "Drafted event appended to history",
      "name": "Reason is included when provided",
      "when": ["DraftChange.execute is called with reason: 'blocked on dependency'"],
      "then": [
        "the last event in the change's history is a drafted event with reason === 'blocked on dependency'"
      ]
    },
    {
      "requirement": "Drafted event appended to history",
      "name": "Reason is omitted when not provided",
      "when": ["DraftChange.execute is called without a reason field"],
      "then": [
        "the last event in the change's history is a drafted event without a reason property"
      ]
    },
    {
      "requirement": "Drafted event appended to history",
      "name": "Change becomes drafted after execution",
      "when": ["DraftChange.execute returns"],
      "then": ["the result satisfies DraftedChangeView", "AND result.isDrafted === true"]
    },
    {
      "requirement": "Persistence",
      "name": "Change is persisted through serialized mutation",
      "when": ["DraftChange.execute completes successfully"],
      "then": [
        "ChangeRepository.mutate(input.name, fn) is called",
        "AND the callback records the drafted event on the fresh persisted Change",
        "AND the resulting change is relocated to the drafts area by the repository"
      ]
    },
    {
      "requirement": "Persistence",
      "name": "Result is not a mutable Change",
      "given": ["an active change is drafted successfully"],
      "when": ["DraftChange.execute returns"],
      "then": ["the return value is not an instance of domain Change"]
    },
    {
      "requirement": "Persistence",
      "name": "Result is loadable via getDraft after execute",
      "given": ["DraftChange.execute returns a view for name parked-feature"],
      "when": ["ChangeRepository.getDraft('parked-feature') is called"],
      "then": ["a DraftedChangeView is returned with isDrafted === true"]
    },
    {
      "requirement": "Persistence",
      "name": "mutate relocates before view mapping",
      "given": ["an active change named parked-feature"],
      "when": ["DraftChange.execute completes"],
      "then": [
        "ChangeRepository.mutate was called for that name",
        "AND the change directory exists under drafts/ after completion"
      ]
    },
    {
      "requirement": "Input contract",
      "name": "execute accepts DraftChangeInput",
      "when": ["DraftChange.execute is called"],
      "then": [
        "it accepts DraftChangeInput with name (required), reason (optional), force (optional)"
      ]
    },
    {
      "requirement": "Dependencies",
      "name": "Uses ChangeRepository port",
      "when": ["DraftChange is instantiated"],
      "then": ["it requires a ChangeRepository port in its constructor"]
    },
    {
      "requirement": "Dependencies",
      "name": "Uses ActorResolver port",
      "when": ["DraftChange is instantiated"],
      "then": ["it requires an ActorResolver port in its constructor"]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "core:draft-change",
        "file": "core:src/application/use-cases/draft-change.ts",
        "symbol": "DraftChange"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:aad623324259d102e16a2897bd5301d5ba19f396eec35817045ff50e6db011f9",
    "verify.md": "sha256:eaf34977a91b1fa58ffca27cf3d9b49bcb697e9c2ae3fc647b8f50e5e290cdfd"
  },
  "generatedBy": "agent",
  "optimizedDescription": "`DraftChange` shelves active change by appending `drafted` history and moving persisted storage from `changes/` to `drafts/`.",
  "optimizedContext": "# DraftChange\n\n## Rules\n- `DraftChangeInput` requires `name`; accepts optional `reason` and `force`.\n- Load active change only via `ChangeRepository.get(name)`; if missing throw `ChangeNotFoundError`; drafted-only names do not qualify.\n- Resolve actor via `ActorResolver` and record it in `drafted` event.\n- Respect historical implementation guard: if change ever reached `implementing`, fail unless `force === true`.\n- Append drafted history via `change.draft(actor, reason, force)`.\n- Persist through `ChangeRepository.mutate(name, fn)`; repository writes fresh manifest and relocates directory from `changes/` to `drafts/`.\n\n## Constraints\n- No lifecycle transition; drafting is orthogonal to lifecycle state.\n- Directory relocation belongs to repository layer.\n- `isDrafted` derives from latest drafted/restored history event.\n"
}
