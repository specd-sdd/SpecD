{
  "title": "core:spec-lock",
  "description": "Archived specs need a durable sidecar that preserves spec identity, persisted\ndependencies, implementation traceability, and per-field LLM optimization state\nindependently of metadata regeneration or graph re-indexing. `spec-lock.json` is\nthat sidecar: the canonical persisted record for spec schema identity,\ndependencies, implementation links, and optional optimization baselines.\n\nA spec's canonical artifacts MAY exist before this sidecar does. Persisted state\ncomes into being deliberately — through archive publication, an explicit\none-time initialization/adoption operation, or an explicit persisted-state\nmutation — never as an incidental guess constructed from a stale cache.",
  "dependsOn": ["core:spec-id-format", "core:storage"],
  "context": [
    "Archived specs need a durable sidecar that preserves spec identity, persisted\ndependencies, implementation traceability, and per-field LLM optimization state\nindependently of metadata regeneration or graph re-indexing. `spec-lock.json` is\nthat sidecar: the canonical persisted record for spec schema identity,\ndependencies, implementation links, and optional optimization baselines.\n\nA spec's canonical artifacts MAY exist before this sidecar does. Persisted state\ncomes into being deliberately — through archive publication, an explicit\none-time initialization/adoption operation, or an explicit persisted-state\nmutation — never as an incidental guess constructed from a stale cache."
  ],
  "rules": [
    {
      "requirement": "Sidecar location and naming",
      "rules": [
        "The sidecar SHALL be a file named `spec-lock.json` located in the canonical persisted spec directory next to that spec's archived artifacts."
      ]
    },
    {
      "requirement": "Durable schema identity",
      "rules": [
        "`spec-lock.json` MUST store the spec schema identity under `schema`.\n\n- `schema.name` is required.\n- `schema.version` is required.\n\nOnce recorded for a persisted spec, this schema identity MUST remain immutable\nexcept through an explicit, guarded schema-reassignment operation (see\nRequirement: Guarded schema reassignment). No other write path — including\narchive-time materialization, dependency mutation, implementation mutation, or\noptimization mutation — MAY replace an existing `schema` value."
      ]
    },
    {
      "requirement": "Guarded schema reassignment",
      "rules": [
        "An explicit schema-reassignment operation MAY replace the `schema` recorded for\nan already-initialized spec. Reassignment MUST:\n\n- resolve and load the target schema's declared canonical artifacts and verify\n  they parse under that schema before replacing `schema`\n- preserve current `dependsOn` unchanged when the target schema does not declare\n  dependency extraction\n- require that dependencies extracted under the target schema equal the current\n  canonical `dependsOn`, failing the operation rather than silently changing\n  dependencies, when the target schema does declare dependency extraction\n- preserve `implementation` and any `optimizations` values and baselines\n  unchanged\n\nBecause each optimization baseline records the schema identity in effect when it\nwas captured, a schema reassignment makes every existing optimization stale\nrelative to the new schema even though its artifact bytes and `value` are\nunchanged. Reassignment MUST NOT clear or regenerate optimization values as a\nside effect; staleness is derived, not enforced by deletion.\n\nSelecting the schema identity already recorded is a semantic no-op and MUST NOT\nbe treated as a failure."
      ]
    },
    {
      "requirement": "Persistent dependencies",
      "rules": [
        "`spec-lock.json` MUST store the final persisted `dependsOn` list for the archived spec as canonical spec IDs.\n\nThis sidecar list is the durable archived dependency record. Archive-time metadata generation and later metadata regeneration flows SHALL treat it as the authoritative persisted dependency state when projecting canonical `metadata.json.dependsOn`, including schemas that do not declare dependency extraction in their spec artifacts."
      ]
    },
    {
      "requirement": "Archived implementation links",
      "rules": [
        "`spec-lock.json` MUST store archived implementation traceability under an `implementation` array.\n\nEach entry MUST contain:\n\n- `file` — the canonical implementation file identity in `workspace:path` form, where `workspace` is the workspace of `specId` and `path` is relative to that workspace `codeRoot`\n\nEach entry MAY additionally contain:\n\n- `symbols` — a non-empty array of symbol names when the archived link is symbol-level\n\nWhen `symbols` is absent, the entry represents a file-level implementation link. When `symbols` is present, the entry represents symbol-level implementation traceability for that file."
      ]
    },
    {
      "requirement": "Optional persisted optimization state",
      "rules": [
        "`spec-lock.json` MAY store per-field LLM optimization state under an optional\n`optimizations` object.\n\nEach present field (`optimizedDescription`, `optimizedContext`) MUST record:\n\n- `value` — the optimized string content\n- `schema` — the schema identity in effect when the field was captured, in the\n  same `{ name, version }` shape as the lock's own `schema`\n- `artifactState` — a map of artifact filename to `{ hash, lastModified }`\n  describing the exact canonical artifact set the value was produced from\n\nEach optimized field owns an independent artifact and schema baseline. Setting\nor clearing one field MUST NOT change the baseline recorded for the other. When\nthe last present field is cleared, `optimizations` MUST be omitted entirely\nrather than retained as an empty object.\n\nA lock written before this requirement existed and containing no `optimizations`\nblock remains valid. No migration MAY synthesize `optimizations` from previously\nagent-generated `metadata.json` fields; only an explicit optimization-setting\noperation creates this state."
      ]
    },
    {
      "requirement": "Lock-less specs and explicit initialization",
      "rules": [
        "A spec MAY have canonical artifacts without a `spec-lock.json`. This lock-less\nstate is valid and MUST NOT be silently upgraded by an ordinary read.\n\nPersisted state for a lock-less spec MUST be created only by:\n\n- archive publication of a change targeting that spec, or\n- an explicit initialization operation that adopts existing artifacts into\n  persisted state, or\n- an explicit persisted-state mutation (dependency, implementation, or\n  optimization) that is itself a create-worthy operation for that spec\n\nExplicit initialization is a one-time adoption: it MUST fail when persisted\nstate already exists for the target spec, regardless of whether the requested\nschema identity matches. Explicit initialization MUST derive `dependsOn` from\nthe current canonical artifacts under the resolved schema rather than from any\ncached projection, default `implementation` to an empty array, and omit\n`optimizations`.\n\nSemantic no-op mutations against a lock-less spec (e.g. removing a dependency\nthat is not present, removing an implementation link that is not present, or\nclearing an optimization field that is not set) MUST NOT create persisted state."
      ]
    },
    {
      "requirement": "Archive-time materialization",
      "rules": [
        "`spec-lock.json` SHALL be written or updated only by archive-time\nmaterialization, explicit persisted-state mutation operations (dependency,\nimplementation, optimization, and schema-reassignment updates), explicit\ninitialization of a lock-less spec, and explicit integrity-maintenance flows.\n\nArchive-time materialization MUST:\n\n- read raw project-relative implementation paths from the active change state\n- validate that each linked file belongs to the workspace implied by the\n  archived `specId`\n- ignore entries whose raw file path falls under that workspace's\n  `graph.excludePaths`\n- discard entries that cannot be normalized into a valid `workspace:path`\n  identity\n- fail archive when a confirmed link points outside the workspace `codeRoot`\n  implied by `specId`\n\nEvery non-archive writer listed above MUST construct the complete persisted\nstate through the shared pure patch/construction rules for this document rather\nthan hand-assembling partial JSON, and MUST use a conditional write guarded by\nthe observed revision so concurrent writers cannot silently overwrite each\nother."
      ]
    },
    {
      "requirement": "Sidecar is the durable source of truth",
      "rules": [
        "`spec-lock.json` MUST be the durable archived source of truth for\nimplementation traceability and, when present, per-field LLM optimization\nstate.\n\n`metadata.json` MAY project or cache this information for faster consumption,\nbut metadata regeneration MUST NOT invent, mutate, or delete implementation\nlinks or optimization values independently of the sidecar. A generated\nprojection MUST include an optimized field only when the sidecar's baseline for\nthat field is fresh against current canonical artifacts and schema identity."
      ]
    },
    {
      "requirement": "Repository hash of persisted lock state",
      "rules": [
        "The SHA-256 of the durable lock sidecar bytes MUST be the value returned by\n`SpecRepository.persistedStateMeta(spec, { includeHash: true })?.hash`\n(formerly exposed via a dedicated `specHash` / `persistedStateHash` helper).\nApplication callers MUST obtain that digest only through the repository port —\nnot by reading `spec-lock.json` directly. There MUST NOT be a separate\n`persistedStateHash(spec)` method on the port.\n\nPresence and last-modified of the lock sidecar for cheap freshness MUST surface on\n`Spec.persistedStateStamp` from `get()`, not as schema artifact filenames."
      ]
    }
  ],
  "constraints": [
    "spec-lock.json MUST be valid JSON.",
    "Canonical implementation file identities MUST use forward-slash-normalized workspace:path values.",
    "symbols, when present, MUST be non-empty.",
    "Sidecar maintenance MUST preserve file-level and symbol-level links as distinct archived traceability forms."
  ],
  "scenarios": [
    {
      "requirement": "Sidecar location and naming",
      "name": "Sidecar is written next to archived spec artifacts",
      "given": ["an archived spec with canonical persisted artifacts"],
      "when": ["archive materializes its sidecar"],
      "then": ["spec-lock.json is written in that same persisted spec directory"]
    },
    {
      "requirement": "Durable schema identity",
      "name": "Schema identity remains immutable after first persistence",
      "given": [
        "an existing spec-lock.json with schema: { \"name\": \"schema-std\", \"version\": 1 }"
      ],
      "when": ["the active project schema later changes"],
      "then": ["the sidecar keeps the original stored schema identity"]
    },
    {
      "requirement": "Durable schema identity",
      "name": "Non-reassignment write paths cannot replace schema",
      "given": ["a persisted spec with an existing schema value"],
      "when": [
        "archive-time materialization, dependency mutation, implementation mutation, or optimization mutation runs against it"
      ],
      "then": ["the existing schema value is not replaced"]
    },
    {
      "requirement": "Guarded schema reassignment",
      "name": "Reassignment validates target schema before replacing schema",
      "when": ["a schema-reassignment operation targets a new schema"],
      "then": [
        "it resolves and loads the target schema's declared canonical artifacts and verifies they parse under that schema before replacing schema"
      ]
    },
    {
      "requirement": "Guarded schema reassignment",
      "name": "Reassignment preserves dependsOn when target schema has no dependency extraction",
      "given": ["a target schema that does not declare dependency extraction"],
      "when": ["schema reassignment is performed"],
      "then": ["the current canonical dependsOn is preserved unchanged"]
    },
    {
      "requirement": "Guarded schema reassignment",
      "name": "Reassignment fails when extracted dependencies differ from canonical dependsOn",
      "given": ["a target schema that declares dependency extraction"],
      "when": [
        "dependencies extracted under the target schema do not equal the current canonical dependsOn"
      ],
      "then": ["the reassignment operation fails instead of silently changing dependencies"]
    },
    {
      "requirement": "Guarded schema reassignment",
      "name": "Reassignment preserves implementation and optimization values",
      "when": ["a schema-reassignment operation completes"],
      "then": ["implementation and any optimizations values and baselines remain unchanged"]
    },
    {
      "requirement": "Guarded schema reassignment",
      "name": "Reassignment makes existing optimizations stale without deleting them",
      "given": ["a persisted spec with existing optimization baselines"],
      "when": ["its schema is reassigned"],
      "then": [
        "the existing optimization baselines are not cleared or regenerated",
        "AND they become stale relative to the new schema"
      ]
    },
    {
      "requirement": "Guarded schema reassignment",
      "name": "Reassigning to the currently recorded schema is a no-op",
      "given": [
        "a persisted spec whose recorded schema already matches the requested target schema"
      ],
      "when": ["a schema-reassignment operation selects that same schema identity"],
      "then": ["the operation succeeds as a semantic no-op and is not treated as a failure"]
    },
    {
      "requirement": "Persistent dependencies",
      "name": "Sidecar dependencies are used as durable fallback",
      "given": [
        "a persisted spec whose spec-lock.json contains dependsOn",
        "AND dependency extraction later omits dependsOn"
      ],
      "when": ["metadata is regenerated"],
      "then": ["the sidecar dependsOn value is used as the authoritative fallback"]
    },
    {
      "requirement": "Archived implementation links",
      "name": "File-level link is stored without symbols",
      "given": ["an archived implementation link for core:src/index.ts with no symbol refinements"],
      "when": ["the sidecar is saved and reloaded"],
      "then": [
        "the implementation entry contains only the canonical file",
        "AND it is interpreted as a file-level link"
      ]
    },
    {
      "requirement": "Archived implementation links",
      "name": "Symbol-level link preserves symbol list",
      "given": [
        "an archived implementation link for core:src/index.ts with symbols [\"createUser\", \"deleteUser\"]"
      ],
      "when": ["the sidecar is saved and reloaded"],
      "then": [
        "the implementation entry preserves the canonical file",
        "AND it preserves the exact non-empty symbols list"
      ]
    },
    {
      "requirement": "Optional persisted optimization state",
      "name": "Optimized field records value, schema baseline, and artifact state",
      "when": ["an optimization-setting operation sets optimizedDescription or optimizedContext"],
      "then": [
        "the field records value, schema, and artifactState describing the exact canonical artifact set it was produced from"
      ]
    },
    {
      "requirement": "Optional persisted optimization state",
      "name": "Independent field baselines",
      "given": ["a lock with both optimizedDescription and optimizedContext present"],
      "when": ["one field is set or cleared"],
      "then": ["the baseline recorded for the other field does not change"]
    },
    {
      "requirement": "Optional persisted optimization state",
      "name": "Clearing the last optimized field omits the optimizations block",
      "given": ["a lock with exactly one optimization field present"],
      "when": ["that field is cleared"],
      "then": ["optimizations is omitted entirely rather than retained as an empty object"]
    },
    {
      "requirement": "Optional persisted optimization state",
      "name": "Pre-existing lock without optimizations remains valid",
      "given": [
        "a spec-lock.json written before this requirement existed with no optimizations block"
      ],
      "when": ["it is read"],
      "then": ["it remains valid"]
    },
    {
      "requirement": "Optional persisted optimization state",
      "name": "No migration synthesizes optimizations from agent-generated metadata",
      "when": ["metadata regeneration or any migration flow runs"],
      "then": [
        "it does not synthesize optimizations from previously agent-generated metadata.json fields",
        "AND only an explicit optimization-setting operation creates this state"
      ]
    },
    {
      "requirement": "Lock-less specs and explicit initialization",
      "name": "Lock-less spec with canonical artifacts is valid",
      "given": ["a spec with canonical artifacts and no spec-lock.json"],
      "when": ["it is read"],
      "then": ["the lock-less state is valid and is not silently upgraded by the read"]
    },
    {
      "requirement": "Lock-less specs and explicit initialization",
      "name": "Explicit initialization adopts existing artifacts",
      "given": ["a lock-less spec"],
      "when": ["an explicit initialization operation adopts its artifacts into persisted state"],
      "then": [
        "dependsOn is derived from the current canonical artifacts under the resolved schema",
        "AND implementation defaults to an empty array",
        "AND optimizations is omitted"
      ]
    },
    {
      "requirement": "Lock-less specs and explicit initialization",
      "name": "Explicit initialization fails when persisted state already exists",
      "given": ["a spec that already has persisted state"],
      "when": ["an explicit initialization operation targets that spec"],
      "then": ["it fails regardless of whether the requested schema identity matches"]
    },
    {
      "requirement": "Lock-less specs and explicit initialization",
      "name": "Semantic no-op mutation does not create persisted state",
      "given": ["a lock-less spec"],
      "when": [
        "a mutation removes a dependency, implementation link, or optimization field that is not present or set"
      ],
      "then": ["persisted state is not created for that spec"]
    },
    {
      "requirement": "Archive-time materialization",
      "name": "Excluded path is ignored during materialization",
      "given": [
        "a confirmed raw implementation link falls under the target workspace graph.excludePaths"
      ],
      "when": ["archive materializes implementation links"],
      "then": [
        "that link is skipped for spec-lock.json",
        "AND archive does not fail solely because of that excluded path"
      ]
    },
    {
      "requirement": "Archive-time materialization",
      "name": "Workspace-boundary mismatch fails archive",
      "given": [
        "a confirmed implementation link whose raw file path falls outside the codeRoot of the workspace implied by specId"
      ],
      "when": ["archive attempts materialization"],
      "then": ["archive fails instead of writing an invalid canonical workspace:path"]
    },
    {
      "requirement": "Archive-time materialization",
      "name": "Non-archive writers use conditional revision-guarded writes",
      "given": [
        "a persisted-state mutation, explicit initialization, or schema-reassignment operation"
      ],
      "when": ["it writes spec-lock.json"],
      "then": [
        "it constructs the complete persisted state through the shared pure patch/construction rules rather than hand-assembling partial JSON",
        "AND it uses a conditional write guarded by the observed revision so concurrent writers cannot silently overwrite each other"
      ]
    },
    {
      "requirement": "Sidecar is the durable source of truth",
      "name": "Metadata regeneration does not mutate sidecar implementation links",
      "given": ["an existing spec-lock.json with archived implementation links"],
      "when": ["metadata is regenerated later"],
      "then": [
        "spec-lock.json content remains unchanged",
        "AND metadata only projects from the sidecar"
      ]
    },
    {
      "requirement": "Sidecar is the durable source of truth",
      "name": "Generated projection excludes a stale optimization field",
      "given": [
        "a sidecar optimization baseline that is stale against current canonical artifacts or schema identity"
      ],
      "when": ["metadata is regenerated"],
      "then": ["the generated projection excludes that optimized field"]
    },
    {
      "requirement": "Repository hash of persisted lock state",
      "name": "persistedStateMeta hash matches lock sidecar bytes",
      "given": ["a durable spec-lock.json with known content"],
      "when": ["SpecRepository.persistedStateMeta(spec, { includeHash: true }) is called"],
      "then": ["the returned hash is the SHA-256 of those sidecar bytes"]
    },
    {
      "requirement": "Repository hash of persisted lock state",
      "name": "Presence stamps do not expose lock as an artifact",
      "given": ["a lock sidecar present on disk"],
      "when": ["get() returns the Spec"],
      "then": [
        "Spec.persistedStateStamp.present is true",
        "AND spec-lock.json does not appear in Spec.artifacts"
      ]
    },
    {
      "requirement": "Repository hash of persisted lock state",
      "name": "there is no persistedStateHash method on the port",
      "when": ["the SpecRepository port surface is inspected"],
      "then": ["it does not declare persistedStateHash as a method"]
    },
    {
      "requirement": "Sidecar is not a schema artifact",
      "name": "Sidecar is omitted from generic artifact metadata",
      "given": ["an archived spec persisted with spec.md, verify.md, and"],
      "when": ["the repository returns the spec's normal artifact metadata"],
      "then": [
        "Spec.artifacts includes only schema-declared artifacts",
        "AND spec-lock.json is not exposed as a normal artifact filename"
      ]
    },
    {
      "requirement": "Sidecar is not a schema artifact",
      "name": "Generic artifact reads reject the sidecar",
      "given": ["an archived spec persisted with spec-lock.json"],
      "when": ["application logic attempts to load spec-lock.json through the generic"],
      "then": [
        "the repository rejects the request",
        "AND callers must use persisted-state semantic operations instead"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "core:spec-lock",
        "file": "core:src/domain/services/parse-spec-lock.ts",
        "symbol": "SpecLockData"
      },
      {
        "specId": "core:spec-lock",
        "file": "core:src/domain/services/parse-spec-lock.ts",
        "symbol": "specLockSchema"
      },
      {
        "specId": "core:spec-lock",
        "file": "core:src/domain/services/parse-spec-lock.ts",
        "symbol": "parseSpecLock"
      },
      {
        "specId": "core:spec-lock",
        "file": "core:src/domain/services/apply-persisted-spec-state-patch.ts",
        "symbol": "applyPersistedSpecStatePatch"
      },
      {
        "specId": "core:spec-lock",
        "file": "core:src/domain/services/apply-persisted-spec-state-patch.ts",
        "symbol": "PersistedSpecState"
      },
      {
        "specId": "core:spec-lock",
        "file": "core:src/domain/errors/persisted-spec-state-schema-replacement-error.ts",
        "symbol": "PersistedSpecStateSchemaReplacementError"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:0f811fee6fae100caff9ab7ac1cf70ed4778d19a97cc7285327f1d5b404876ad",
    "verify.md": "sha256:756e0476d04b063af10f47a0a17b556150017ab165d64fda7db70182be90556f"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:0f811fee6fae100caff9ab7ac1cf70ed4778d19a97cc7285327f1d5b404876ad",
        "lastModified": "2026-07-25T11:53:31.094Z"
      },
      "verify.md": {
        "hash": "sha256:756e0476d04b063af10f47a0a17b556150017ab165d64fda7db70182be90556f",
        "lastModified": "2026-07-25T11:53:31.095Z"
      }
    },
    "persistedStateHash": "sha256:da02e64b30aaa897f65b292e776ce9144937ac2f10af4e71ca923477ee9b9a8a",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
