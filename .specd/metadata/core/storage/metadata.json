{
  "title": "Storage",
  "description": "The domain and application layers must remain agnostic to where changes, drafts, and archives physically live, so that future storage backends can be swapped without touching business logic. specd's storage layer achieves this through port interfaces in `@specd/core` with adapter-specific implementations; the v1 implementation ships one adapter for all storage ports: `fs` (local filesystem).",
  "dependsOn": [
    "default:_global/architecture",
    "core:change",
    "core:change-manifest",
    "default:_global/logging"
  ],
  "context": [
    "The domain and application layers must remain agnostic to where changes, drafts, and archives physically live, so that future storage backends can be swapped without touching business logic. specd's storage layer achieves this through port interfaces in `@specd/core` with adapter-specific implementations; the v1 implementation ships one adapter for all storage ports: `fs` (local filesystem)."
  ],
  "rules": [
    {
      "requirement": "Change directory naming",
      "rules": [
        "Active change directories must use the format `YYYYMMDD-HHmmss-<name>`, where the timestamp is the moment the change was created. The prefix must be a filesystem convention only — it must not appear in the domain model, the change manifest, or any CLI argument. `FsChangeRepository` must resolve a change by name using a glob pattern `*-<name>`."
      ]
    },
    {
      "requirement": "Change directory listing order",
      "rules": [
        "`ChangeRepository.list()` MUST return active changes in canonical sort order: `createdAt` ascending (oldest first). With the `fs` adapter, this order is owned by the active-changes index helper under `{configPath}/tmp/fs-cache/changes/` — not by sorting directory entries at list time."
      ]
    },
    {
      "requirement": "Artifact status derivation",
      "rules": [
        "Artifact status (`missing`, `in-progress`, `complete`, `skipped`) must be derived at load time — it must not be stored directly in the manifest. The manifest stores only `validatedHash` per artifact. `FsChangeRepository` must compute status using this precedence:\n\n1. `validatedHash === \"__skipped__\"` → `skipped` (only valid for `optional: true` artifacts)\n2. File absent (and no sentinel) → `missing`\n3. File present and cleaned hash matches `validatedHash` → `complete`\n4. File present but hash differs or `validatedHash` is `null` → `in-progress`\n\n\"Cleaned hash\" means: read the file content, apply the artifact type's `preHashCleanup` rules in order (the same rules `ValidateArtifacts` applies before computing `validatedHash`), then compute SHA-256 of the result. `FsChangeRepository` must have access to the schema's artifact types at load time so that it can retrieve the `preHashCleanup` array for each artifact type. If no `preHashCleanup` rules are defined for an artifact type, the raw content is hashed directly.\n\nTo prevent false-drift detection and unnecessary write operations on uninitialized repositories, status derivation (including comparing current hashes to `validatedHash`) and drift invalidations must only be performed when the repository is fully initialized with resolved artifact types (i.e. `artifactTypes.length > 0`). If the repository is not initialized with artifact types, drift detection is bypassed."
      ]
    },
    {
      "requirement": "Artifact dependency cascade",
      "rules": [
        "`Change.effectiveStatus(type)` must cascade through the artifact dependency graph. An artifact whose own hash matches its `validatedHash` must still be reported as `in-progress` if any artifact in its `requires` chain is neither `complete` nor `skipped`. A `skipped` optional artifact satisfies the dependency — it does not block downstream artifacts."
      ]
    },
    {
      "requirement": "ValidateArtifacts is the sole path to complete",
      "rules": [
        "`Artifact.markComplete(hash)` must only be called by the `ValidateArtifacts` use case. `Artifact.markSkipped()` must only be called by the skip use case (sets `validatedHash` to `\"__skipped__\"`). No other code path may set these values."
      ]
    },
    {
      "requirement": "Archive pattern configuration",
      "rules": [
        "The `fs` archive adapter must support a configurable `pattern` field in `specd.yaml` under `storage.archive.pattern`. The pattern controls the directory structure within the archive root. Supported variables: `{{year}}`, `{{month}}`, `{{day}}`, `{{change.name}}`, `{{change.archivedName}}`. The default pattern must be `{{change.archivedName}}`."
      ]
    },
    {
      "requirement": "Scope excluded from archive pattern",
      "rules": [
        "`{{change.scope}}` must not be a supported archive pattern variable. Scope paths use `/` as a segment separator, which produces ambiguous slugs when normalized for use in directory names."
      ]
    },
    {
      "requirement": "Workspace excluded from archive pattern",
      "rules": [
        "`{{change.workspace}}` MUST NOT be a supported archive pattern variable. A change has no single primary workspace — only workspaces touched via `specIds` (see [`core:change`](../change/spec.md)). The normative supported-variable catalog for `storage.archive.pattern` is `{{year}}`, `{{month}}`, `{{day}}`, `{{change.name}}`, `{{change.archivedName}}`; it MUST NOT include any per-change workspace token.\n\nImplementations MUST reject a configured pattern containing `{{change.workspace}}` the same way they reject `{{change.scope}}` — by throwing `UnsupportedPatternError` at construction time, not by leaving the token unexpanded or substituting a fallback value."
      ]
    },
    {
      "requirement": "Archive index",
      "rules": [
        "Filesystem-backed archive listing MUST use a list index under `{configPath}/tmp/fs-cache/archive/`, not a root-local index at the archive storage root.\n\nEach fs-cache bucket directory contains:\n\n- `.specd-index.jsonl` — one JSON object per line with wire shape `{ entry, sourceMtime?, sourceFiles? }` where `entry` is the public list-entry payload for that bucket and freshness fields are helper-only (never returned from `list()`).\n- `.specd-index-meta.json` — `{ totalCount, generatedAt, isInvalidated }`.\n\nFor archive, each line's `entry` is an `ArchiveListEntry`; `sourceMtime` records the manifest mtime used for freshness.\n\n`ArchiveRepository.reindex()` MUST rebuild the archive list index in `fs-cache/archive/` by scanning archived manifests. It MUST NOT write or maintain `.specd-index.jsonl` / `.specd-index-meta.json` at the archive root as part of normal list/count operation.\n\n`get(name)` MAY still resolve archive paths by scanning stored manifests or other adapter-specific lookup — it MUST NOT depend on a root-local JSONL index for routine reads.\n\nOn first use or forced rebuild, implementations MUST migrate from any legacy root-local `.specd-index.jsonl` / `.specd-index-meta.json` by rebuilding into `fs-cache/archive/` (migrate and forget — no dual-read compatibility).\n\nOrphan cleanup: when `reindex()` or the first full rebuild materializes `fs-cache/archive/`, delete legacy `.specd-index.jsonl` and `.specd-index-meta.json` from the archive root if present (ignore ENOENT). Normal `list()` / `count()` cache hits MUST NOT scan or delete root-local legacy files.\n\n`specd storage reindex` invokes port `reindex()` methods only — see [`cli:storage-reindex`](../../cli/storage-reindex/spec.md). Storage specs MUST NOT require the CLI to know JSONL layout."
      ]
    },
    {
      "requirement": "Archive runtime ignore hygiene",
      "rules": [
        "Fs-backed archive storage MUST maintain an archive-local `.gitignore` for runtime archive artifacts.\n\n`FsArchiveRepository` MUST ensure that the archive root ignores `.staging`.\n\nLegacy root-local index files (`.specd-index.jsonl`, `.specd-index-meta.json`) are obsolete; orphan cleanup removes them on rebuild/migration only. Runtime archive behavior MUST NOT re-add index-only ignore lines for those files after migration.\n\nThis guarantee MUST be exercised by runtime archive behavior rather than relying on project bootstrap state alone, so archive ignore hygiene remains correct after archive directory relocation, recreation, or index recovery.\n\n`FsArchiveRepository` MAY centralize this behavior in a shared internal archive-directory preparation helper, but the runtime guarantee MUST cover archive creation and staged commit paths."
      ]
    },
    {
      "requirement": "Named storage factories",
      "rules": [
        "Kernel composition SHALL support named storage factories for repository-backed capabilities. A storage factory SHALL be selected by adapter name and SHALL be responsible for creating the repository implementation needed for that storage mode.\n\nWhen storage selection requires workspace-specific VCS or null-VCS handling, that responsibility SHALL remain within the selected storage factory rather than leaking into unrelated composition paths."
      ]
    },
    {
      "requirement": "Archive pattern date variables are zero-padded",
      "rules": [
        "`{{month}}` and `{{day}}` must be zero-padded to two digits. `{{year}}` is four digits. This ensures lexicographic sort produces chronological order."
      ]
    },
    {
      "requirement": "Change manifest format",
      "rules": [
        "The format of `manifest.json` — its fields, event shapes, and schema version behavior — is defined in [`core:change-manifest`](../change-manifest/spec.md). `FsChangeRepository` reads and writes the manifest according to that format and must write it atomically (temp file + rename) to prevent partial reads."
      ]
    },
    {
      "requirement": "Repository path confinement",
      "rules": [
        "Fs-backed repositories MUST treat path confinement as a storage invariant.\n\nChange, spec, archive, and metadata operations MUST reject any derived path that would escape their configured storage root or address an arbitrary untracked file outside the permitted artifact set for the operation."
      ]
    },
    {
      "requirement": "Staged archive persistence",
      "rules": [
        "Fs-backed archive persistence MUST prefer staged commit semantics over ad hoc rollback.\n\nWhen archive requires multiple durable updates, storage behavior MUST prepare the complete result first and only then expose the committed archive result, so failures before commit do not leave partially visible permanent archive state."
      ]
    },
    {
      "requirement": "Storage debug logging",
      "rules": [
        "Fs-backed repositories SHOULD emit debug-level logs for tracked artifact resolution, path-confinement rejections, staged archive commit progress, and archive failure diagnostics.\n\nThese logs MUST follow the project's global logging conventions."
      ]
    },
    {
      "requirement": "Change locks directory placement",
      "rules": [
        "The `FsChangeRepository` implementation derives its change lock directory path internally\nfrom the `configPath` field: `{configPath}/tmp/change-locks`. This directory is distinct\nfrom the changes storage root and allows per-change lock files to be co-located with\nother config-scoped temporary artifacts.\n\nThe `configPath` field is part of the `ChangeRepositoryConfig` port contract, provided\nat construction time. The repository derives the locks directory internally as\n`path.join(configPath, 'tmp', 'change-locks')`."
      ]
    },
    {
      "requirement": "Filesystem list index cache layout",
      "rules": [
        "Fs-backed list/count implementations MUST store derived list indexes under:\n\n```text\n{configPath}/tmp/fs-cache/\n  archive/\n  changes/\n  drafts/\n  discarded/\n  specs/<workspace>/\n  validate-specs/<workspace>/\n```\n\nEach list-index bucket directory contains `.specd-index.jsonl` and\n`.specd-index-meta.json` as defined in the archive index requirement. Change buckets\nstore `sourceMtime` from `manifest.json`. Spec buckets store `sourceFiles` with\nper-file mtimes used for freshness.\n\nRepositories MUST NOT read or write these list-index cache files directly except\nthrough dedicated index helper classes (`FsChangeIndexCache`, `FsSpecIndexCache`).\nHelpers own canonical sort, pagination, freshness, regeneration, and per-bucket\nlocking.\n\nIndex entries store the full CLI-usable list-entry payload; port `include*` flags are\nresponse projection only.\n\nThe `validate-specs/<workspace>/` bucket is owned by the filesystem adapter of\n[`core:validation-result-cache-port`](../validation-result-cache-port/spec.md), not by\n`FsSpecRepository` list/count helpers. Its normative meta and row shapes are defined\nin Requirement: Validation result cache bucket layout."
      ]
    },
    {
      "requirement": "Validation result cache bucket layout",
      "rules": [
        "The filesystem adapter for `ValidationResultCache` MUST persist one bucket per\nworkspace at `{configPath}/tmp/fs-cache/validate-specs/<workspace>/` containing:\n\n- `.specd-index-meta.json` — validate-bucket meta that **extends** the shared list-index\n  meta shape `{ totalCount, generatedAt, isInvalidated }` with:\n  - `schemaFingerprint` (string)\n  - `engineVersion` (non-negative integer)\n- `.specd-index.jsonl` — one JSON object per line with wire shape\n  `{ entry, stamps, cacheFingerprint }` where:\n  - `entry` is the cached `SpecValidationEntry` (or equivalent)\n  - `stamps` records freshness from `Spec` (`SpecArtifactEntry` lastModified values,\n    `persistedStateStamp`, and `generatedMetadataStamp`, with absence represented explicitly)\n  - `cacheFingerprint` is\n    `hash(sortedKeyJson({ specFingerprint, metadataContentHash | \"__absent__\" }))`\n    per [`core:validation-result-cache-port`](../validation-result-cache-port/spec.md)\n\nShared list-index buckets MUST NOT be required to carry `schemaFingerprint` or\n`engineVersion`. Those fields are validate-bucket-specific extensions.\n\nDelivery hosts MUST NOT be required to know this bucket exists. `specd storage reindex`\nand other host commands MUST NOT gain validate-cache-specific surfaces solely for this\nbucket."
      ]
    },
    {
      "requirement": "configPath tmp gitignore",
      "rules": [
        "Fs-backed repositories and project initialisation MUST ensure `{configPath}/tmp/.gitignore` exists with normative contents:\n\n```gitignore\n*\n!.gitignore\n```\n\nMeaning: ignore all tmp artifacts (`fs-cache/`, change-locks, and other runtime files) while allowing the ignore rule file itself to remain un-ignored.\n\nRuntime repository behaviour MUST create or update this file idempotently when tmp paths are first used. `initProject` MUST create the same file for new projects (see [`core:config-writer-port`](../config-writer-port/spec.md))."
      ]
    }
  ],
  "constraints": [
    "Manifest files must be written atomically (write to temp file, then rename) to prevent partial reads",
    "Fs-cache index entries MUST use forward slashes as path separators in serialized path fields regardless of host OS",
    "The timestamp in a change directory name must be derived from change.createdAt, not from the system clock at write time",
    "Derived list indexes under {configPath}/tmp/fs-cache/ are runtime caches and MUST NOT be committed to version control"
  ],
  "scenarios": [
    {
      "requirement": "Change directory naming",
      "name": "Change created",
      "when": ["a new change named add-auth is created at 2024-03-15T10:30:00"],
      "then": ["its directory is named 20240315-103000-add-auth"]
    },
    {
      "requirement": "Change directory naming",
      "name": "Change resolved by name",
      "when": ["FsChangeRepository.get('add-auth') is called"],
      "then": ["it resolves the directory by globbing *-add-auth, not by storing the full path"]
    },
    {
      "requirement": "Change directory listing order",
      "name": "Multiple changes listed in createdAt order",
      "given": ["two active changes with createdAt timestamps where alpha is older than beta"],
      "when": ["ChangeRepository.list() is called through the fs adapter"],
      "then": [
        "alpha appears before beta in canonical sort order",
        "AND the order is produced by the active-changes index helper under {configPath}/tmp/fs-cache/changes/ without re-sorting at list time"
      ]
    },
    {
      "requirement": "Change directory listing order",
      "name": "List order matches index helper not directory walk",
      "given": ["change directories on disk whose lexical order differs from createdAt ascending"],
      "when": ["ChangeRepository.list() is called"],
      "then": [
        "returned items follow createdAt ascending from the fs-cache index",
        "AND the adapter does not sort directory entries at list time"
      ]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "Valid hash matches file content",
      "given": [
        "an artifact was validated with content some content (hash stored as validatedHash)"
      ],
      "when": ["the file is loaded"],
      "then": ["its status becomes complete"]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "Hash mismatch triggers in-progress status",
      "given": ["an artifact was validated with content some content"],
      "when": ["the file is edited to different content"],
      "then": ["its status becomes in-progress"]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "Missing validatedHash defaults to in-progress",
      "given": ["a file present on disk"],
      "when": ["all validatedHash values are cleared"],
      "then": ["its status becomes in-progress — file present but no valid hash"]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "preHashCleanup normalized edit preserves complete status",
      "given": [
        "an artifact type with preHashCleanup: [{ pattern: \"- \\\\[x\\\\]\", replacement: \"- [ ]\" }]",
        "AND the artifact was validated with content - [ ] task one (cleaned hash stored as validatedHash)"
      ],
      "when": ["the file is edited to - [x] task one"],
      "then": [
        "FsChangeRepository applies the cleanup before hashing, producing the same hash",
        "AND the derived status is complete"
      ]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "Non-normalized edit still triggers in-progress",
      "given": [
        "an artifact type with preHashCleanup: [{ pattern: \"- \\\\[x\\\\]\", replacement: \"- [ ]\" }]",
        "AND the artifact was validated with content - [ ] task one"
      ],
      "when": ["the file is edited to - [ ] task one\\n- [ ] task two"],
      "then": [
        "the cleaned hash differs from validatedHash",
        "AND the derived status is in-progress"
      ]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "No preHashCleanup rules hashes raw content",
      "given": [
        "an artifact type with no preHashCleanup rules",
        "AND the artifact was validated with content some content"
      ],
      "when": ["the file content is unchanged"],
      "then": ["sha256(rawContent) === validatedHash", "AND the derived status is complete"]
    },
    {
      "requirement": "Artifact status derivation",
      "name": "Status derivation bypassed when repository is uninitialized",
      "given": ["a repository initialized without artifact types (artifactTypes.length === 0)"],
      "when": ["a change is loaded"],
      "then": [
        "drift detection and status derivation are bypassed",
        "AND files do not report drift or trigger auto-invalidations"
      ]
    },
    {
      "requirement": "Artifact dependency cascade",
      "name": "Upstream artifact edited",
      "when": [
        "artifact A is complete but its upstream dependency B is edited (becomes in-progress)"
      ],
      "then": ["Change.effectiveStatus('a') returns in-progress"]
    },
    {
      "requirement": "Artifact dependency cascade",
      "name": "Upstream artifact skipped — downstream unblocked",
      "given": [
        "artifact A requires optional artifact B, and B has validatedHash: \"__skipped__\""
      ],
      "when": ["Change.effectiveStatus('a') is called"],
      "then": ["it returns A's own derived status — B's skipped state does not block A"]
    },
    {
      "requirement": "ValidateArtifacts is the sole path to complete",
      "name": "Attempt to mark complete outside ValidateArtifacts",
      "when": ["any code other than ValidateArtifacts calls artifact.markComplete(hash)"],
      "then": ["it violates this requirement — the call must be removed"]
    },
    {
      "requirement": "ValidateArtifacts is the sole path to complete",
      "name": "Attempt to mark skipped outside skip use case",
      "when": ["any code other than the skip use case sets validatedHash to \"__skipped__\""],
      "then": ["it violates this requirement — the call must be removed"]
    },
    {
      "requirement": "Archive pattern configuration",
      "name": "Custom archive pattern",
      "when": ["specd.yaml sets storage.archive.pattern: \"{{year}}/{{change.archivedName}}\""],
      "then": ["archived changes are placed under <archive-root>/2024/<archivedName>/"]
    },
    {
      "requirement": "Archive pattern configuration",
      "name": "Default archive pattern",
      "when": ["no pattern is configured in specd.yaml"],
      "then": ["archives use {{change.archivedName}} as the directory name"]
    },
    {
      "requirement": "Scope excluded from archive pattern",
      "name": "Pattern uses scope variable",
      "when": ["storage.archive.pattern contains {{change.scope}}"],
      "then": ["FsArchiveRepository must reject it as an unsupported variable"]
    },
    {
      "requirement": "Workspace excluded from archive pattern",
      "name": "Pattern uses workspace variable",
      "when": ["storage.archive.pattern contains {{change.workspace}}"],
      "then": [
        "FsArchiveRepository must reject it as an unsupported variable, throwing UnsupportedPatternError at construction time — the same as {{change.scope}}"
      ]
    },
    {
      "requirement": "Workspace excluded from archive pattern",
      "name": "Normative catalog omits any workspace token",
      "given": ["the normative supported-variable catalog for storage.archive.pattern"],
      "when": ["the catalog is inspected"],
      "then": [
        "it contains only {{year}}, {{month}}, {{day}}, {{change.name}}, {{change.archivedName}}",
        "AND it contains no per-change workspace token"
      ]
    },
    {
      "requirement": "Archive index",
      "name": "Change archived upserts fs-cache index entry",
      "when": ["archive(change) completes successfully"],
      "then": [
        "an ArchiveListEntry row is upserted in {configPath}/tmp/fs-cache/archive/.specd-index.jsonl",
        "AND no root-local .specd-index.jsonl is written at the archive storage root"
      ]
    },
    {
      "requirement": "Archive index",
      "name": "Archive list serves from fs-cache index",
      "given": ["archived changes exist on disk and the fs-cache archive index is fresh"],
      "when": ["ArchiveRepository.list() is called"],
      "then": [
        "entries are returned from {configPath}/tmp/fs-cache/archive/ without reading every manifest",
        "AND results are ordered by archivedAt descending (newest first)"
      ]
    },
    {
      "requirement": "Archive index",
      "name": "Archive reindex rebuilds fs-cache index",
      "when": ["ArchiveRepository.reindex() is invoked after manual filesystem changes"],
      "then": [
        "{configPath}/tmp/fs-cache/archive/.specd-index.jsonl is rebuilt from archived manifests",
        "AND meta.totalCount reflects the full archive count"
      ]
    },
    {
      "requirement": "Archive index",
      "name": "Legacy root index migrated on rebuild",
      "given": ["legacy .specd-index.jsonl exists at the archive storage root"],
      "when": [
        "ArchiveRepository.reindex() or the first full rebuild materializes fs-cache/archive/"
      ],
      "then": [
        "the legacy root-local index files are deleted (ENOENT ignored)",
        "AND listing uses only the fs-cache index thereafter"
      ]
    },
    {
      "requirement": "Archive index",
      "name": "get does not depend on root-local index",
      "given": ["no root-local archive index file exists"],
      "when": ["ArchiveRepository.get(name) is called for an archived change"],
      "then": ["the change is resolved without requiring a root-local JSONL index"]
    },
    {
      "requirement": "Archive runtime ignore hygiene",
      "name": "Runtime archive behavior ensures staging is ignored",
      "given": ["the archive root exists without an archive-local .gitignore"],
      "when": [
        "FsArchiveRepository performs archive creation or index rebuild that touches the archive root"
      ],
      "then": [
        "the archive root .gitignore is created or updated",
        "AND it contains an entry for .staging",
        "AND it does not require entries for .specd-index.jsonl or .specd-index-meta.json"
      ]
    },
    {
      "requirement": "Archive runtime ignore hygiene",
      "name": "Shared helper preserves staging ignore across runtime paths",
      "when": ["archive creation, reindex(), or staged commit paths prepare the archive directory"],
      "then": ["each path preserves the archive-local ignore entry for .staging"]
    },
    {
      "requirement": "Named storage factories",
      "name": "Repository capability selects a factory by adapter name",
      "given": ["registered storage factories for fs and git"],
      "when": ["kernel composition resolves a workspace configured with adapter: git"],
      "then": [
        "the git storage factory is used to create that repository-backed capability",
        "AND the fs factory remains available for workspaces still configured with fs"
      ]
    },
    {
      "requirement": "Named storage factories",
      "name": "Workspace-specific VCS handling stays inside the selected factory",
      "given": [
        "a storage mode whose repository creation needs VCS detection or null-VCS fallback"
      ],
      "when": ["the named storage factory is invoked"],
      "then": [
        "the factory owns that VCS or null-VCS decision internally",
        "AND unrelated kernel composition paths do not hardcode storage-specific VCS handling"
      ]
    },
    {
      "requirement": "Archive pattern date variables are zero-padded",
      "name": "January archive",
      "when": ["a change is archived on January 5th"],
      "then": ["{{month}} resolves to \"01\" and {{day}} resolves to \"05\""]
    },
    {
      "requirement": "Change manifest format",
      "name": "Manifest structure matches specification",
      "given": ["an active change with history events"],
      "when": ["the change is persisted to disk"],
      "then": [
        "manifest.json contains the fields and structure defined in the change-manifest spec"
      ]
    },
    {
      "requirement": "Repository path confinement",
      "name": "Change storage rejects paths outside the change root",
      "when": ["a change-storage operation derives a path outside its configured root"],
      "then": ["the operation is rejected"]
    },
    {
      "requirement": "Repository path confinement",
      "name": "Spec storage rejects untracked or escaping artifact paths",
      "when": [
        "a spec-storage operation targets an artifact path outside the permitted artifact set or outside the spec root"
      ],
      "then": ["the operation is rejected"]
    },
    {
      "requirement": "Repository path confinement",
      "name": "Archive storage rejects archive paths outside the archive root",
      "when": ["an archive-storage operation derives a path outside the configured archive root"],
      "then": ["the operation is rejected"]
    },
    {
      "requirement": "Staged archive persistence",
      "name": "Failure before staged commit leaves no partially visible archive result",
      "given": [
        "fs-backed archive persistence has prepared some intermediate data",
        "AND a failure occurs before commit"
      ],
      "when": ["the archive attempt aborts"],
      "then": ["the permanent archive view does not expose a partially committed result"]
    },
    {
      "requirement": "Staged archive persistence",
      "name": "Permanent archive view changes only after successful commit",
      "when": ["fs-backed archive persistence completes successfully"],
      "then": ["the permanent archive view changes only after the staged commit succeeds"]
    },
    {
      "requirement": "Storage debug logging",
      "name": "Debug logs cover storage diagnostics",
      "when": ["debug logging is enabled for fs-backed storage adapters"],
      "then": [
        "logs include tracked artifact resolution, path-confinement rejections, staged archive commit progress, and archive failure diagnostics"
      ]
    },
    {
      "requirement": "Change locks directory placement",
      "name": "Locks derived from configPath field",
      "given": ["ChangeRepositoryConfig includes configPath: /project/.specd/config"],
      "when": ["a change lock is acquired"],
      "then": ["the lock file is created under /project/.specd/config/tmp/change-locks/"]
    },
    {
      "requirement": "Change locks directory placement",
      "name": "Default configPath",
      "given": [
        "specd.yaml without explicit configPath",
        "AND config loader defaults to .specd/config"
      ],
      "when": ["FsChangeRepository is composed"],
      "then": ["locks resolve to {configPath}/tmp/change-locks"]
    },
    {
      "requirement": "Change locks directory placement",
      "name": "Locks separated from changes storage",
      "given": ["storage.changes.path: .specd/changes", "AND configPath: .specd/config (default)"],
      "when": ["locks are acquired"],
      "then": [
        "lock files are under .specd/config/tmp/change-locks/",
        "AND not under .specd/changes/"
      ]
    },
    {
      "requirement": "Filesystem list index cache layout",
      "name": "List indexes stored under fs-cache buckets",
      "given": [
        "an fs-backed project with active changes, drafts, discarded changes, archive entries, and workspace specs"
      ],
      "when": ["list/count operations populate derived indexes"],
      "then": [
        "index files exist under {configPath}/tmp/fs-cache/ in bucket directories changes/, drafts/, discarded/, archive/, and specs/<workspace>/",
        "AND each of those list-index buckets contains .specd-index.jsonl and .specd-index-meta.json",
        "AND those list/count operations do not require writing validate-specs/<workspace>/"
      ]
    },
    {
      "requirement": "Filesystem list index cache layout",
      "name": "Index meta exposes totalCount for count operations",
      "given": ["a populated fs-cache bucket with totalCount: 5 in meta"],
      "when": ["the corresponding repository count() is called with a fresh index"],
      "then": ["the returned count is 5 without materializing full list entries"]
    },
    {
      "requirement": "Filesystem list index cache layout",
      "name": "Validate-specs bucket is sibling not owned by SpecRepository helpers",
      "when": ["FsSpecRepository list/count helpers run"],
      "then": [
        "they MUST NOT own or mutate {configPath}/tmp/fs-cache/validate-specs/<workspace>/",
        "AND that bucket remains owned by the ValidationResultCache filesystem adapter"
      ]
    },
    {
      "requirement": "Validation result cache bucket layout",
      "name": "Bucket files live under validate-specs workspace path",
      "given": ["a filesystem ValidationResultCache adapter for workspace core"],
      "when": ["it upserts a validation result row"],
      "then": [
        "files are written under {configPath}/tmp/fs-cache/validate-specs/core/",
        "AND the directory contains .specd-index-meta.json and .specd-index.jsonl"
      ]
    },
    {
      "requirement": "Validation result cache bucket layout",
      "name": "Meta extends IndexMeta with schema and engine fields",
      "when": ["validate-bucket meta is written"],
      "then": [
        "it includes totalCount, generatedAt, and isInvalidated",
        "AND it includes schemaFingerprint and engineVersion",
        "AND shared list-index buckets are not required to include those two extra fields"
      ]
    },
    {
      "requirement": "Validation result cache bucket layout",
      "name": "Rows carry entry stamps and cache fingerprint",
      "when": ["a JSONL row is written for a validated spec"],
      "then": [
        "the row includes entry, stamps, and cacheFingerprint",
        "AND stamps includes artifacts (SpecArtifactEntry lastModified values),\npersistedStateStamp, and generatedMetadataStamp (with explicit absence when\nsidecars are missing)",
        "AND cacheFingerprint follows\n[core:validation-result-cache-port](../validation-result-cache-port/spec.md)"
      ]
    },
    {
      "requirement": "Validation result cache bucket layout",
      "name": "Hosts gain no validate-cache-specific reindex surface",
      "when": ["host storage reindex commands are considered for this change"],
      "then": ["they MUST NOT gain a validate-cache-only argument or documentation"]
    },
    {
      "requirement": "configPath tmp gitignore",
      "name": "Runtime ensures tmp gitignore contents",
      "given": ["{configPath}/tmp/ is first used by an fs-backed repository"],
      "when": ["runtime tmp hygiene runs"],
      "then": [
        "{configPath}/tmp/.gitignore exists with contents ignoring * and un-ignoring !.gitignore"
      ]
    },
    {
      "requirement": "configPath tmp gitignore",
      "name": "Fs-cache artifacts remain gitignored",
      "given": ["{configPath}/tmp/.gitignore is present"],
      "when": ["{configPath}/tmp/fs-cache/ indexes are generated"],
      "then": ["those cache files are ignored by git via the tmp gitignore rule"]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "core:storage",
        "file": "core:src/infrastructure/fs/fs-validation-result-cache.ts",
        "symbol": "FsValidationResultCache"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:23cdac37abf9959dd2ee9f0905da6f7b8c50fe05699a02aaed8de536f8c61bc1",
    "verify.md": "sha256:3b4f5e4a624158b300dcae0e8da6a23976b8383d98976b6c34330e82ae100137"
  },
  "generatedBy": "core"
}
