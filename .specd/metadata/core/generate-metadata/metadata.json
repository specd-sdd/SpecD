{
  "title": "Generate Spec Metadata",
  "description": "Metadata must be producible deterministically from spec content so that every project gets a consistent baseline without requiring LLM involvement. The `GenerateSpecMetadata` use case achieves this by resolving a spec ID, loading its artifacts, parsing them into ASTs, running the schema's `metadataExtraction` engine, computing content hashes, and returning the assembled `.specd-metadata.yaml` content.",
  "dependsOn": [
    "core:spec-metadata",
    "core:content-extraction",
    "core:schema-format",
    "core:spec-id-format",
    "core:spec-repository-port",
    "core:composition-resolver",
    "core:spec-optimization"
  ],
  "context": [
    "Metadata must be producible deterministically from spec content so that every project gets a consistent baseline without requiring LLM involvement. The `GenerateSpecMetadata` use case achieves this by resolving a spec ID, loading its artifacts, parsing them into ASTs, running the schema's `metadataExtraction` engine, computing content hashes, and returning the assembled `.specd-metadata.yaml` content."
  ],
  "rules": [
    {
      "requirement": "Input and output",
      "rules": [
        "The use case takes a `specId` string (e.g. `'core/change'` or\n`'billing:invoices/create'`). It returns `{ metadata: SpecMetadata,\nhasExtraction: boolean }`. `metadata` contains the extracted fields, content\nhashes, canonical `dependsOn`, and a `provenance` record capturing the exact\nartifact hashes/lastModified, `persistedStateHash`, schema identity, projection\nversion, and projection fingerprint used to produce this result. `hasExtraction`\nindicates whether the active schema declares any `metadataExtraction` rules.\n\nThe returned `provenance` MUST reflect the exact loaded artifact set and lock\nsnapshot used during this call — not a subsequently re-read state — so that\ncallers such as `MaterializeSpecMetadata` can reuse it without re-hashing\nartifact content."
      ]
    },
    {
      "requirement": "Schema resolution",
      "rules": [
        "The use case obtains the active schema via `SchemaProvider.get()`. If the schema cannot be resolved, `get()` throws `SchemaNotFoundError` or `SchemaValidationError` — the use case does not catch these. If the resolved schema has no `metadataExtraction` declarations, the use case returns `{ metadata: {}, hasExtraction: false }` immediately — there is nothing to extract."
      ]
    },
    {
      "requirement": "Spec resolution",
      "rules": [
        "The use case parses `specId` via `parseSpecId()` to obtain a workspace name and capability path. It looks up the workspace's `SpecRepository` from the injected map. If the workspace is unknown, it throws `WorkspaceNotFoundError`. If the spec is not found via `SpecRepository.get()`, it throws `SpecNotFoundError`."
      ]
    },
    {
      "requirement": "Artifact loading and parsing",
      "rules": [
        "The use case iterates over the schema's artifact type declarations, filtering to `scope: 'spec'` artifacts. For each artifact:\n\n1. Resolves the filename from the artifact's `output` field (last path segment)\n2. Infers the format via `inferFormat()`, falling back to `'plaintext'` if no format is declared or inferred\n3. Looks up a parser from `ArtifactParserRegistry` for the format\n4. Loads the artifact content via `SpecRepository.artifact()`\n\nArtifacts with no matching parser or no content on disk are silently skipped."
      ]
    },
    {
      "requirement": "Metadata extraction",
      "rules": [
        "The use case calls `extractMetadata()` with:\n\n- The schema's `metadataExtraction` declarations\n- Parsed ASTs keyed by artifact type ID\n- Renderers from the artifact parsers\n- The shared extractor-transform registry assembled by kernel composition\n- Caller-owned origin context for each extracted artifact, including the values needed by transforms such as `resolveSpecPath`\n\nThe extraction engine produces fields including `title`, `description`, `dependsOn`, `keywords`, `rules`, `constraints`, `scenarios`, and `context`.\n\nWhen the schema declares transforms for those fields, the use case awaits the extraction runtime and the extracted metadata returned from `extractMetadata()` is already normalized by that runtime transform path.\n\nThe use case may satisfy that normalization through repository-backed transform execution assembled by the caller, rather than through filesystem-specific path inference embedded in the extraction engine."
      ]
    },
    {
      "requirement": "dependsOn resolution",
      "rules": [
        "`GenerateSpecMetadata` MUST canonicalize `dependsOn` from persisted spec\nsemantics before returning metadata.\n\nResolution rules:\n\n1. Run schema extraction normally. If extraction yields `dependsOn`, treat that\n   as the candidate extracted dependency set.\n2. Read the repository's aggregate persisted state through\n   `SpecRepository.readPersistedState(spec)`.\n3. If persisted state exists, the returned `metadata.dependsOn` MUST use its\n   `dependsOn` value, regardless of what extraction yielded.\n4. If persisted state does not exist (a lock-less spec), the returned\n   `metadata.dependsOn` MUST be derived from the current canonical artifacts:\n   use the extracted value when extraction yields one, otherwise `[]`. This is a\n   live projection of current artifacts, not a cached or previously observed\n   value.\n5. If persisted state exists and its `dependsOn` differs from a non-empty\n   extracted value, metadata generation MUST fail explicitly instead of\n   silently choosing one.\n\nIf `dependsOn` entries require normalization from artifact-local strings (for\nexample relative spec links) to canonical spec IDs, that behavior must be\ndeclared through the schema's extractor transform model and executed during\n`extractMetadata()`.\n\nThe use case supplies the origin context and repository-backed transform\nruntime needed by those registered transforms, awaits the transformed\nextraction output, and accepts that output as final before comparing it against\npersisted state.\n\nIf extraction finds dependency values but transform execution cannot normalize\nthem, metadata generation fails explicitly. It does not silently drop those\nfound values and continue with an incomplete `dependsOn` set."
      ]
    },
    {
      "requirement": "One consistent lock snapshot or explicit absence",
      "rules": [
        "`GenerateSpecMetadata` MUST read persisted state at most once per generation\nattempt and use that single observation — the complete\n`PersistedSpecStateSnapshot` or explicit absence (`null`) — consistently for\n`dependsOn`, `implementation`, `optimizations`, and\n`provenance.persistedStateHash`.\n\nWhen a hash is needed for `provenance.persistedStateHash` (or equivalent\nfingerprint inputs), it MUST obtain that hash via\n`SpecRepository.persistedStateMeta(spec, { includeHash: true })?.hash ?? null`\n(or from the same single observation's `originalHash` when that is already part\nof the read snapshot). It MUST NOT call a removed `persistedStateHash(spec)`\nmethod.\n\nIt MUST NOT mix fields read from two different persisted-state observations\nwithin one generation attempt, and MUST NOT re-read persisted state partway\nthrough assembling the result."
      ]
    },
    {
      "requirement": "Fresh lock-owned optimizations only",
      "rules": [
        "When the persisted state snapshot includes an `optimizations` block,\n`GenerateSpecMetadata` MUST include `optimizedDescription` and/or\n`optimizedContext` in the generated metadata only for fields whose persisted\nartifact and schema baseline is fresh against the artifacts and schema identity\nloaded during this same generation attempt.\n\nA stale or absent optimization field MUST be omitted from the generated\nmetadata rather than included with a stale value or a placeholder.\n`GenerateSpecMetadata` MUST NOT regenerate, invalidate, or otherwise mutate\npersisted optimization state — it only decides whether to project an existing\nvalue."
      ]
    },
    {
      "requirement": "Content hashes",
      "rules": [
        "After extraction, the use case computes a SHA-256 hash for each artifact file that was successfully loaded, using `ContentHasher.hash()`. The resulting `contentHashes` map is keyed by the resolved filename (e.g. `spec.md`, `verify.md`). Only artifacts with content on disk are included."
      ]
    },
    {
      "requirement": "Assembled result",
      "rules": [
        "The final metadata object merges:\n\n- all fields from `extractMetadata()` output\n- canonical `dependsOn` determined by the dependency-resolution rules above\n- implementation projection from persisted repository semantics\n- fresh lock-owned optimization fields, per Requirement: Fresh lock-owned\n  optimizations only\n- `contentHashes` from the hashing step\n- `provenance` — the exact artifact hashes/lastModified, `persistedStateHash`\n  (or `null` for a lock-less spec), schema identity, projection version, and\n  projection fingerprint used during this generation attempt\n- `generatedBy: 'core'`\n\nThe result is returned with `hasExtraction: true`."
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveGenerateSpecMetadataDeps",
      "rules": [
        "The config-based `createGenerateSpecMetadata(config, options?)` form MUST derive `GenerateSpecMetadataDeps` through `resolveGenerateSpecMetadataDeps(resolver)` and then delegate to canonical `createGenerateSpecMetadata(deps)`.\n\n`resolveGenerateSpecMetadataDeps(resolver)` MUST resolve:\n\n- `listWorkspaces: ListWorkspaces`\n- `schemaProvider: SchemaProvider`\n- `parsers: ArtifactParserRegistry`\n- `hasher: ContentHasher`\n- `extractorTransforms: ExtractorTransformRegistry`\n- `workspaceRoutes: readonly SpecWorkspaceRoute[]`\n\nThe helper is the only use-case-specific composition entry for config-based bootstrap. The factory MUST NOT reconstruct fs-shaped wiring inline."
      ]
    }
  ],
  "constraints": [
    "No LLM involvement — extraction is purely deterministic via the schema's\nmetadataExtraction engine",
    "Delegates to async extractMetadata() domain service for all extraction\nlogic — the use case orchestrates but does not implement extraction",
    "GenerateSpecMetadata MUST NOT write to disk, mutate persisted state, or call\nSpecRepository.writeMetadataSnapshot() / writePersistedState() under any\ncircumstance — persistence is exclusively MaterializeSpecMetadata and its\ninternal PersistSpecMetadata collaborator's responsibility",
    "Content hashes only cover artifacts that were successfully loaded from disk",
    "GenerateSpecMetadata never decides whether a persisted optimization is\nfresh independently of the artifacts and schema loaded during the same call —\nfreshness for optimization projection uses only that same-call state, not a\nseparately re-read observation"
  ],
  "scenarios": [
    {
      "requirement": "Schema resolution",
      "name": "Schema resolution failure propagates",
      "when": ["SchemaProvider.get() throws SchemaNotFoundError"],
      "then": ["the error propagates — the use case does not catch it"]
    },
    {
      "requirement": "Schema resolution",
      "name": "Schema has no metadataExtraction",
      "when": ["the resolved schema has no metadataExtraction declarations"],
      "then": ["the use case returns { metadata: {}, hasExtraction: false }"]
    },
    {
      "requirement": "Spec resolution",
      "name": "Unknown workspace",
      "given": ["the specId references a workspace not present in the injected spec repositories"],
      "when": ["GenerateSpecMetadata is executed"],
      "then": ["it throws WorkspaceNotFoundError"]
    },
    {
      "requirement": "Spec resolution",
      "name": "Spec not found",
      "given": ["the workspace exists but the spec path does not resolve to a spec"],
      "when": ["GenerateSpecMetadata is executed"],
      "then": ["it throws SpecNotFoundError"]
    },
    {
      "requirement": "Artifact loading and parsing",
      "name": "Artifact with unknown parser format skipped",
      "given": ["a scope: 'spec' artifact has a format with no registered parser"],
      "when": ["GenerateSpecMetadata is executed"],
      "then": [
        "that artifact is silently skipped and extraction proceeds with the remaining artifacts"
      ]
    },
    {
      "requirement": "Artifact loading and parsing",
      "name": "Artifact not on disk skipped",
      "given": ["a scope: 'spec' artifact is declared in the schema but has no content on disk"],
      "when": ["GenerateSpecMetadata is executed"],
      "then": [
        "that artifact is silently skipped and extraction proceeds with the remaining artifacts"
      ]
    },
    {
      "requirement": "Metadata extraction",
      "name": "Fields extracted from valid spec",
      "given": [
        "a spec with artifacts that conform to the schema's metadataExtraction declarations"
      ],
      "when": ["GenerateSpecMetadata is executed"],
      "then": [
        "the result metadata contains extracted fields such as title, description, dependsOn, keywords, rules, constraints, and scenarios"
      ]
    },
    {
      "requirement": "Metadata extraction",
      "name": "Extraction uses shared transform registry with origin context",
      "given": [
        "a metadata extractor declares transform: resolveSpecPath",
        "AND the current spec origin is available to the use case"
      ],
      "when": ["GenerateSpecMetadata executes extraction"],
      "then": [
        "it supplies the shared extractor-transform registry and origin context to extractMetadata"
      ]
    },
    {
      "requirement": "Metadata extraction",
      "name": "Extraction awaits async transform resolution",
      "given": [
        "a metadata extractor declares a transform whose runtime implementation returns a promise"
      ],
      "when": ["GenerateSpecMetadata executes extraction"],
      "then": ["it awaits that promise before assembling the final metadata object"]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Relative spec path resolved during extraction",
      "given": [
        "the current spec origin is workspace core and capability path core/change",
        "AND extraction yields the relative link ../storage/spec.md",
        "AND the schema declares transform: resolveSpecPath"
      ],
      "when": ["GenerateSpecMetadata executes extraction"],
      "then": [
        "the transformed dependsOn value is core:storage",
        "AND no separate post-extraction repair step runs afterward"
      ]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Cross-workspace relative spec path resolves through repository-backed normalization",
      "given": [
        "the current spec origin is workspace core and capability path core/actor-resolver-port",
        "AND extraction yields the relative link ../../_global/architecture/spec.md",
        "AND the registered resolveSpecPath runtime uses repository-backed resolution across configured workspaces"
      ],
      "when": ["GenerateSpecMetadata executes extraction"],
      "then": [
        "the transformed dependsOn value is default:_global/architecture",
        "AND it is not normalized to core:_global/architecture"
      ]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Persisted state overrides omitted extracted dependsOn",
      "given": [
        "extraction does not yield dependsOn",
        "AND SpecRepository.readPersistedState(spec) returns a snapshot with dependsOn: ['core:storage']"
      ],
      "when": ["GenerateSpecMetadata assembles metadata"],
      "then": ["the returned metadata.dependsOn is ['core:storage']"]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Mismatched extracted and persisted dependencies fail generation",
      "given": [
        "extraction yields dependsOn: ['core:config']",
        "AND SpecRepository.readPersistedState(spec) returns a snapshot with dependsOn: ['core:storage']"
      ],
      "when": ["GenerateSpecMetadata executes"],
      "then": ["metadata generation fails explicitly instead of silently choosing one set"]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Lock-less spec derives dependsOn as a live projection of current artifacts",
      "given": ["SpecRepository.readPersistedState(spec) returns null for a lock-less spec"],
      "when": ["GenerateSpecMetadata assembles metadata"],
      "then": [
        "metadata.dependsOn is derived from the current canonical artifacts — the extracted value when extraction yields one, otherwise []",
        "AND it is not read from any cached or previously observed value"
      ]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Unresolvable dependency value fails extraction instead of being omitted",
      "given": [
        "extraction yields a dependsOn value like https://example.com",
        "AND the registered transform cannot normalize that value"
      ],
      "when": ["GenerateSpecMetadata executes extraction"],
      "then": ["extraction fails explicitly instead of silently omitting that dependency"]
    },
    {
      "requirement": "dependsOn resolution",
      "name": "Canonical spec ID may pass through resolveSpecPath when enabled by args",
      "given": [
        "extraction yields the canonical spec ID core:storage",
        "AND the schema declares transform: { name: \"resolveSpecPath\", args: [\"true\"] }"
      ],
      "when": ["GenerateSpecMetadata executes extraction"],
      "then": ["the final dependsOn value remains core:storage"]
    },
    {
      "requirement": "One consistent lock snapshot or explicit absence",
      "name": "Single persisted-state read is reused across all dependent fields",
      "given": ["a generation attempt that reads persisted state once"],
      "when": [
        "GenerateSpecMetadata assembles dependsOn, implementation, optimizations, and provenance.persistedStateHash"
      ],
      "then": [
        "all four use that same single PersistedSpecStateSnapshot observation (or explicit null)"
      ]
    },
    {
      "requirement": "One consistent lock snapshot or explicit absence",
      "name": "Persisted state is not re-read partway through assembly",
      "when": ["GenerateSpecMetadata is executing a single generation attempt"],
      "then": [
        "it does not call SpecRepository.readPersistedState(spec) more than once for that attempt"
      ]
    },
    {
      "requirement": "One consistent lock snapshot or explicit absence",
      "name": "Explicit absence is treated as one consistent observation",
      "given": ["SpecRepository.readPersistedState(spec) returns null"],
      "when": ["GenerateSpecMetadata assembles the result"],
      "then": [
        "dependsOn, implementation, and provenance.persistedStateHash all reflect that same absence rather than a mix of observations"
      ]
    },
    {
      "requirement": "One consistent lock snapshot or explicit absence",
      "name": "provenance.persistedStateHash uses persistedStateMeta includeHash or snapshot originalHash",
      "given": ["a generation attempt with present persisted state"],
      "when": ["GenerateSpecMetadata assembles provenance.persistedStateHash"],
      "then": [
        "the hash is obtained via persistedStateMeta(..., { includeHash: true })",
        "AND the use case does not call a persistedStateHash repository method"
      ]
    },
    {
      "requirement": "Fresh lock-owned optimizations only",
      "name": "Fresh optimization field is projected",
      "given": [
        "the persisted optimizations.optimizedDescription baseline is fresh against the artifacts and schema loaded during this generation attempt"
      ],
      "when": ["GenerateSpecMetadata assembles metadata"],
      "then": ["optimizedDescription is included with its persisted value"]
    },
    {
      "requirement": "Fresh lock-owned optimizations only",
      "name": "Stale optimization field is omitted, not fabricated",
      "given": [
        "the persisted optimizations.optimizedContext baseline is stale against the artifacts or schema loaded during this generation attempt"
      ],
      "when": ["GenerateSpecMetadata assembles metadata"],
      "then": [
        "optimizedContext is omitted from the generated metadata rather than included with a stale value or placeholder"
      ]
    },
    {
      "requirement": "Fresh lock-owned optimizations only",
      "name": "Generation never mutates persisted optimization state",
      "when": ["GenerateSpecMetadata evaluates optimization freshness"],
      "then": [
        "it does not regenerate, invalidate, or otherwise mutate the persisted optimizations block",
        "AND it only decides whether to project an existing value"
      ]
    },
    {
      "requirement": "Content hashes",
      "name": "Hashes computed for loaded artifacts",
      "given": ["two artifacts (spec.md and verify.md) were loaded successfully"],
      "when": ["GenerateSpecMetadata is executed"],
      "then": ["contentHashes contains SHA-256 entries for both spec.md and verify.md"]
    },
    {
      "requirement": "Content hashes",
      "name": "Missing artifacts excluded from hashes",
      "given": ["spec.md exists on disk but verify.md does not"],
      "when": ["GenerateSpecMetadata is executed"],
      "then": ["contentHashes contains only the entry for spec.md"]
    },
    {
      "requirement": "Assembled result",
      "name": "Result includes generatedBy marker",
      "when": ["GenerateSpecMetadata completes successfully with extraction"],
      "then": ["metadata.generatedBy is 'core'"]
    },
    {
      "requirement": "Assembled result",
      "name": "Result merges extracted fields and hashes",
      "when": ["GenerateSpecMetadata completes successfully"],
      "then": [
        "the returned metadata contains both the extractMetadata() output fields and contentHashes",
        "AND hasExtraction is true"
      ]
    },
    {
      "requirement": "Assembled result",
      "name": "Result includes fresh optimization fields and provenance",
      "given": ["a generation attempt with a fresh optimizations baseline present"],
      "when": ["GenerateSpecMetadata completes successfully"],
      "then": [
        "the returned metadata includes the fresh optimization fields, contentHashes, and a provenance record for this generation attempt"
      ]
    },
    {
      "requirement": "Input and output",
      "name": "execute accepts specId string",
      "when": ["GenerateSpecMetadata.execute is called"],
      "then": [
        "it accepts a specId string parameter (e.g. 'core/change' or 'billing:invoices/create')"
      ]
    },
    {
      "requirement": "Input and output",
      "name": "Returns metadata and hasExtraction flag",
      "when": ["GenerateSpecMetadata.execute completes successfully"],
      "then": ["it returns { metadata: SpecMetadata, hasExtraction: boolean }"]
    },
    {
      "requirement": "Input and output",
      "name": "metadata contains extracted fields and hashes",
      "when": ["GenerateSpecMetadata.execute completes with extraction"],
      "then": [
        "the returned metadata contains fields from extractMetadata() plus contentHashes and generatedBy: 'core'"
      ]
    },
    {
      "requirement": "Input and output",
      "name": "Result includes a provenance record reflecting the exact call state",
      "when": ["GenerateSpecMetadata completes successfully"],
      "then": [
        "the returned metadata.provenance reflects the exact artifact hashes/lastModified, persistedStateHash, schema identity, projection version, and projection fingerprint loaded during this call",
        "AND MaterializeSpecMetadata can reuse it without re-hashing artifact content"
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveGenerateSpecMetadataDeps",
      "name": "createGenerateSpecMetadata config form derives GenerateSpecMetadataDeps through resolveGenerateSpecMetadataDeps",
      "when": ["createGenerateSpecMetadata(config, options?) is invoked"],
      "then": [
        "it creates a composition resolver for that composition session",
        "AND it derives GenerateSpecMetadataDeps through resolveGenerateSpecMetadataDeps(resolver)",
        "AND resolveGenerateSpecMetadataDeps(resolver) resolves:",
        "AND the factory delegates to canonical createGenerateSpecMetadata(deps)"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "core:generate-metadata",
        "file": "core:src/application/use-cases/generate-spec-metadata.ts",
        "symbol": "GenerateSpecMetadata"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:6230999bdc9edb95d3e39b00dc83c08cd0da4208754c4a98a37557671345d391",
    "verify.md": "sha256:ea7fcded17be8b31be1f40c007428b6d49aa8a6ee90710ba0216d3c61b96134c"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:6230999bdc9edb95d3e39b00dc83c08cd0da4208754c4a98a37557671345d391",
        "lastModified": "2026-07-25T11:53:31.159Z"
      },
      "verify.md": {
        "hash": "sha256:ea7fcded17be8b31be1f40c007428b6d49aa8a6ee90710ba0216d3c61b96134c",
        "lastModified": "2026-07-25T11:53:31.160Z"
      }
    },
    "persistedStateHash": "sha256:97c0dcafe8dc6b7ed47d098bddb6324a2e5f31bf09f760cc8b1709048fefd7ee",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
