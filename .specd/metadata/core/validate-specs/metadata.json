{
  "title": "Validate Specs",
  "description": "Spec artifacts can drift from the schema's structural expectations over time, and there is no other mechanism to catch these violations before they cause downstream failures. The `ValidateSpecs` use case validates spec artifacts against the active schema's structural rules, supporting three scoping modes: a single spec by qualified path, all specs in a workspace, or all specs across all workspaces. Only spec-scoped artifact types from the schema are validated; change-scoped artifacts are excluded.",
  "dependsOn": [
    "core:validate-artifacts",
    "core:schema-format",
    "core:storage",
    "core:workspace",
    "core:spec-id-format",
    "core:composition-resolver",
    "core:validation-result-cache-port",
    "core:spec-lock",
    "core:spec-repository-port",
    "core:spec-optimization",
    "core:get-spec-metadata"
  ],
  "context": [
    "Spec artifacts can drift from the schema's structural expectations over time, and there is no other mechanism to catch these violations before they cause downstream failures. The `ValidateSpecs` use case validates spec artifacts against the active schema's structural rules, supporting three scoping modes: a single spec by qualified path, all specs in a workspace, or all specs across all workspaces. Only spec-scoped artifact types from the schema are validated; change-scoped artifacts are excluded."
  ],
  "rules": [
    {
      "requirement": "Resolve the active schema",
      "rules": [
        "The use case SHALL obtain the schema via `SchemaProvider.get()`. If the schema cannot be resolved, `get()` throws `SchemaNotFoundError` or `SchemaValidationError` — the use case does not catch these."
      ]
    },
    {
      "requirement": "Filter to spec-scoped artifact types",
      "rules": [
        "The use case SHALL call `schema.artifacts()` and retain only those artifact types where `scope === 'spec'`. Change-scoped artifact types MUST be excluded from validation."
      ]
    },
    {
      "requirement": "Single spec validation mode",
      "rules": [
        "When `input.specPath` is provided (format: `workspace:capabilityPath`), the use case SHALL:\n\n1. Parse the spec identifier via `parseSpecId`.\n2. Look up the workspace's `SpecRepository`. If not found, throw `WorkspaceNotFoundError`.\n3. Load the spec via `repo.get(specPath)`. If not found, throw `SpecNotFoundError`.\n4. Validate only that single spec."
      ]
    },
    {
      "requirement": "Workspace validation mode",
      "rules": [
        "When `input.workspace` is provided (and `input.specPath` is not), the use case SHALL:\n\n1. Look up the workspace's `SpecRepository`. If not found, throw `WorkspaceNotFoundError`.\n2. List all specs in the workspace via `repo.list(undefined, { includeMeta: true })`\n   so Meta stamps are available for validation-result-cache hard hits.\n3. Validate each listed spec."
      ]
    },
    {
      "requirement": "All-workspaces validation mode",
      "rules": [
        "When neither `input.specPath` nor `input.workspace` is provided, the use case SHALL\niterate all configured workspaces and, for each workspace repository, list specs via\n`repo.list(undefined, { includeMeta: true })`, then validate every listed spec."
      ]
    },
    {
      "requirement": "Per-spec artifact validation",
      "rules": [
        "For each spec, the use case SHALL check every spec-scoped artifact type from the schema:\n\n1. Determine the expected filename from `path.basename(artifactType.output)`.\n2. If the file is missing and the artifact type is not optional, record a `ValidationFailure` with description indicating the required artifact is missing.\n3. If the file is missing and the artifact type is optional, skip silently.\n4. If the file exists and the artifact type has no validation rules, parse it when a matching `crossArtifactValidations` rule may need it; otherwise skip.\n5. If the file exists and has local rules: infer the format (from `artifactType.format` or filename), obtain the parser from `ArtifactParserRegistry`, parse the content into an AST, and evaluate the schema's validation rules via `evaluateRules`.\n6. Any artifact whose local validation passes and whose AST is available becomes an eligible participant input for Requirement: Per-spec cross-artifact validation."
      ]
    },
    {
      "requirement": "Per-spec cross-artifact validation",
      "rules": [
        "After local per-artifact validation for one spec, `ValidateSpecs` SHALL evaluate the same schema `crossArtifactValidations` engine used by `ValidateArtifacts` for all rules whose `scope` is `spec`.\n\nEvaluation rules:\n\n- rules are evaluated independently for each spec being validated\n- only participants belonging to the current spec are considered\n- participant key extraction and relation semantics MUST match `ValidateArtifacts`\n- `ValidateSpecs` MUST reuse the same underlying cross-artifact evaluation machinery as `ValidateArtifacts` rather than redefining a separate comparison model\n\nParticipant readiness rules:\n\n- a participant is ready only when its artifact file exists, its artifact has passed local structural validation for this spec, and its parsed AST is available\n- if every participant for a rule is ready, the rule MUST be evaluated and any mismatch recorded as a `ValidationFailure`\n- if one or more participants are not ready, the rule MUST be deferred for that spec and surfaced as a non-failing validation output entry"
      ]
    },
    {
      "requirement": "Aggregated result",
      "rules": [
        "The use case SHALL return a `ValidateSpecsResult` containing:\n\n- `entries` — array of `SpecValidationEntry` objects, one per validated spec.\n- `totalSpecs` — total number of specs validated.\n- `passed` — number of specs with zero failures.\n- `failed` — number of specs with one or more failures.\n\nEach `SpecValidationEntry` MUST include:\n\n- `spec` — qualified label in `workspace:capabilityPath` format.\n- `passed` — `true` if `failures` is empty.\n- `failures` — array of `ValidationFailure` objects from both local artifact validation and per-spec cross-artifact validation.\n- `warnings` — array of `ValidationWarning` objects, including deferred cross-artifact validation notices."
      ]
    },
    {
      "requirement": "Format inference and parser resolution",
      "rules": [
        "When an artifact type does not specify an explicit `format`, the use case SHALL infer it from the filename via `inferFormat`. If no parser is found for the resolved format, the artifact MUST be skipped without recording a failure or warning."
      ]
    },
    {
      "requirement": "Metadata materialization and validation",
      "rules": [
        "After per-artifact and cross-artifact validation for one spec, `ValidateSpecs`\nSHALL obtain that spec's metadata through `GetSpecMetadata.execute({ specId })`\nbefore validating its normalized content. `ValidateSpecs` MUST NOT read\n`SpecRepository.readMetadataSnapshot()` directly to decide pass/fail on its own.\n\nValidation rules:\n\n1. Missing, invalid, or stale persisted metadata is NOT itself a validation\n   failure as long as materialization successfully produces a valid in-memory\n   projection — self-healing on read is expected behaviour, not a defect being\n   reported.\n2. If materialization cannot produce a valid metadata projection at all (for\n   example, a source read failure or a projection that fails structural\n   validation even after regeneration), record a `ValidationFailure` indicating\n   that metadata could not be materialized for the spec.\n3. Once metadata is materialized, validate its normalized `dependsOn` against\n   the spec's current persisted dependency state\n   (`SpecRepository.readPersistedState(spec).dependsOn`, or `[]` for a\n   lock-less spec). A mismatch after materialization indicates an internal\n   inconsistency and MUST be recorded as a `ValidationFailure`.\n4. If the active schema declares `metadataExtraction.dependsOn` for the spec and\n   extraction yields a dependency set that differs from the persisted\n   dependency state, record a `ValidationFailure`.\n5. If the schema omits dependency extraction, validation MUST accept the\n   materialized `dependsOn` when it matches the persisted dependency state.\n\nA `metadata-cache-write-failed` warning returned by materialization MUST be\nsurfaced as a `ValidationWarning` on the spec's entry rather than a\n`ValidationFailure` — a cache-write failure does not mean the normalized\nprojection used for validation was invalid.\n\nThese checks validate canonical metadata as a self-healing cache of persisted\nspec semantics without treating `spec-lock.json` as a normal schema artifact."
      ]
    },
    {
      "requirement": "Persisted optimization staleness is an independent failure",
      "rules": [
        "Independently of metadata materialization, `ValidateSpecs` SHALL check the\nfreshness of any persisted optimization fields (`optimizedDescription`,\n`optimizedContext`) recorded in the spec's persisted state against its current\nartifacts and schema identity.\n\nIf any present optimization field is stale, `ValidateSpecs` MUST record a\n`ValidationFailure` for that spec identifying the stale field(s) and reason(s).\nThis failure MUST be reported even when metadata materialization otherwise\nsucceeds and the spec's other validations pass, because Core cannot author a\nreplacement optimization value automatically — a human or an explicit optimizer\ninvocation must refresh it.\n\nA spec with no persisted optimizations, or with only fresh optimization fields,\nMUST NOT fail this check."
      ]
    },
    {
      "requirement": "Transparent validation result cache",
      "rules": [
        "`ValidateSpecs` MUST use a workspace `ValidationResultCache` for each selected spec\nbefore running full per-spec validation, and MUST upsert after a completed full\nvalidation on cache miss.\n\nBehaviour:\n\n1. Compute the active `schemaFingerprint` and `engineVersion` for the resolved schema\n   and evaluation engine (see fingerprint rules below).\n2. For workspace and all-workspaces discovery, list specs via\n   `repo.list(undefined, { includeMeta: true })` (or the equivalent prefix-less call).\n   The projected Meta stamps (`artifacts`, `persistedStateMeta`,\n   `generatedMetadataMeta`) MUST be passed into cache `lookup` as optional current\n   stamps so a warm **hard hit** does not require N×`repo.get()`.\n3. For each selected spec, call cache\n   `lookup({ spec, schemaFingerprint, engineVersion, stamps? })` where `stamps`\n   are derived from list Meta when available. The cache owns the freshness cascade\n   defined by\n   [`core:validation-result-cache-port`](../validation-result-cache-port/spec.md).\n4. On hard hit, use the returned `SpecValidationEntry` as the outcome for that\n   spec without re-running artifact parse, local rules, cross-artifact rules, or\n   metadata consistency checks, and without calling `get()` solely to obtain stamps.\n5. On soft hit or miss, the existing content / fingerprint / full validation path\n   applies (including any `get()` or content I/O the cache or use case already\n   requires for soft-hit/`cacheFingerprint`).\n6. On miss, after full per-spec validation,\n   `upsert({ entry, spec, schemaFingerprint, engineVersion })` only — not stamps\n   or fingerprints invented by ValidateSpecs.\n\n`ValidateSpecs` MUST NOT invent stamp algorithms or drive soft-hit stamp refresh.\nPassing repository-projected list Meta into `lookup` is allowed and required for\nmulti-spec discovery; it is not \"precomputing\" stamps outside the repository.\n\nSingle-spec validation MAY obtain stamps via `get()` or the Meta methods when list\nMeta is unavailable.\n\nBucket fingerprint inputs (supplied by ValidateSpecs / composition):\n\n- `schemaFingerprint` MUST cover schema identity plus all `scope: 'spec'` artifact\n  validations and `scope: 'spec'` cross-artifact rules, and MUST reflect whether\n  `metadataExtraction.dependsOn` is declared.\n- `engineVersion` MUST change when evaluation logic changes independently of schema\n  YAML.\n\nHost opacity: `ValidateSpecs` MUST remain the only consumer of the cache port for this\nbehaviour. Delivery hosts MUST observe identical public inputs and outputs whether a\ncache hit or miss occurred.\n\nComposition vs direct construction:\n\n- When `validationResultCaches` has no entry for the target workspace, `ValidateSpecs`\n  MUST skip cache lookup and upsert and perform full validation. This degraded path\n  exists for unit tests and direct `new ValidateSpecs(...)` construction only.\n- `resolveValidateSpecsDeps` and config-based `createValidateSpecs` MUST register one\n  `ValidationResultCache` per configured workspace so CLI and kernel paths never hit\n  the skip path during normal operation."
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveValidateSpecsDeps",
      "rules": [
        "The config-based `createValidateSpecs(config, options?)` form MUST derive\n`ValidateSpecsDeps` through `resolveValidateSpecsDeps(resolver)` and then\ndelegate to canonical `createValidateSpecs(deps)`.\n\n`resolveValidateSpecsDeps(resolver)` MUST resolve:\n\n- `specs: ReadonlyMap<string, SpecRepository>`\n- `schemaProvider: SchemaProvider`\n- `parsers: ArtifactParserRegistry`\n- `contentHasher: ContentHasher`\n- `extractorTransforms: ExtractorTransformRegistry`\n- `workspaceRoutes: readonly SpecWorkspaceRoute[]`\n- `validationResultCaches: ReadonlyMap<string, ValidationResultCache>` (one port\n  instance per workspace, wired with that workspace's `SpecRepository`)\n- `getMetadata: ReadonlyMap<string, GetSpecMetadata>` (one\n  instance per workspace, used to self-heal metadata before normalized-content\n  validation)\n\nThe helper is the only use-case-specific composition entry for config-based\nbootstrap. The factory MUST NOT reconstruct fs-shaped wiring inline, including\nfilesystem paths for the validation result cache."
      ]
    }
  ],
  "constraints": [
    "The use case receives a ReadonlyMap<string, SpecRepository> — it MUST NOT\nmodify the map or the repositories.",
    "input.specPath takes precedence: when provided, input.workspace is\nignored.",
    "Validation rules come exclusively from the resolved schema — the use case\ndoes not define its own rules.",
    "The ValidationFailure and ValidationWarning types are shared with the\nValidateArtifacts use case.",
    "ValidateSpecs MUST NOT implement its own metadata freshness comparison,\ngeneration, or persistence — it delegates entirely to\nGetSpecMetadata and only interprets that use case's result and\nwarnings."
  ],
  "scenarios": [
    {
      "requirement": "Resolve the active schema",
      "name": "Schema resolution failure propagates",
      "when": ["SchemaProvider.get() throws SchemaNotFoundError"],
      "then": ["the error propagates — the use case does not catch it"]
    },
    {
      "requirement": "Filter to spec-scoped artifact types",
      "name": "Change-scoped artifacts excluded",
      "given": ["a schema with both scope: 'spec' and scope: 'change' artifact types"],
      "when": ["validation runs"],
      "then": ["only scope: 'spec' artifact types are checked"]
    },
    {
      "requirement": "Single spec validation mode",
      "name": "Validate a single spec by qualified path",
      "given": ["a spec at default:auth/login with a required spec.md present"],
      "when": ["execute({ specPath: 'default:auth/login' }) is called"],
      "then": ["the result contains exactly one entry for 'default:auth/login'"]
    },
    {
      "requirement": "Single spec validation mode",
      "name": "Unknown workspace in specPath throws WorkspaceNotFoundError",
      "when": ["execute({ specPath: 'nonexistent:auth/login' }) is called"],
      "then": ["a WorkspaceNotFoundError is thrown"]
    },
    {
      "requirement": "Single spec validation mode",
      "name": "Spec not found throws SpecNotFoundError",
      "given": ["workspace 'default' exists but has no spec at auth/login"],
      "when": ["execute({ specPath: 'default:auth/login' }) is called"],
      "then": ["a SpecNotFoundError is thrown"]
    },
    {
      "requirement": "Workspace validation mode",
      "name": "All specs in workspace validated",
      "given": ["workspace 'default' contains three specs"],
      "when": ["execute({ workspace: 'default' }) is called"],
      "then": ["the result contains three entries"]
    },
    {
      "requirement": "Workspace validation mode",
      "name": "Unknown workspace throws WorkspaceNotFoundError",
      "when": ["execute({ workspace: 'nonexistent' }) is called"],
      "then": ["a WorkspaceNotFoundError is thrown"]
    },
    {
      "requirement": "All-workspaces validation mode",
      "name": "All specs across all workspaces validated",
      "given": ["two workspaces with two specs each"],
      "when": ["execute({}) is called"],
      "then": ["the result contains four entries"]
    },
    {
      "requirement": "Per-spec artifact validation",
      "name": "Missing required artifact recorded as failure",
      "given": [
        "a spec-scoped artifact type spec.md that is not optional, and the spec has no spec.md"
      ],
      "when": ["validation runs for that spec"],
      "then": [
        "a ValidationFailure is recorded with artifactId matching the artifact type and a description indicating the artifact is missing"
      ]
    },
    {
      "requirement": "Per-spec artifact validation",
      "name": "Missing optional artifact skipped silently",
      "given": ["an optional spec-scoped artifact type, and the spec does not have that file"],
      "when": ["validation runs for that spec"],
      "then": ["no failure or warning is recorded for that artifact"]
    },
    {
      "requirement": "Per-spec artifact validation",
      "name": "Artifact with no validation rules skipped",
      "given": ["a spec-scoped artifact type with an empty validations array, and the file exists"],
      "when": ["validation runs for that spec"],
      "then": ["no failure or warning is recorded for that artifact"]
    },
    {
      "requirement": "Per-spec artifact validation",
      "name": "Artifact with no local rules is still parsed for cross-artifact validation",
      "given": [
        "a spec-scoped artifact file has no local validations",
        "AND a schema crossArtifactValidations rule references that artifact"
      ],
      "when": ["ValidateSpecs validates the spec"],
      "then": ["the artifact is parsed so it can participate in the cross-artifact rule"]
    },
    {
      "requirement": "Per-spec artifact validation",
      "name": "Validation rules evaluated against parsed AST",
      "given": [
        "a spec-scoped artifact type with validation rules, and the file exists and parses successfully"
      ],
      "when": ["validation runs"],
      "then": ["evaluateRules results are included in the entry's failures and warnings"]
    },
    {
      "requirement": "Per-spec cross-artifact validation",
      "name": "Cross-artifact mismatch becomes a spec failure",
      "given": [
        "spec.md and verify.md for one spec both parse successfully",
        "AND a schema crossArtifactValidations rule requires all-equal requirement IDs between them"
      ],
      "when": ["ValidateSpecs evaluates that spec"],
      "then": ["any key mismatch is recorded as a ValidationFailure on that spec entry"]
    },
    {
      "requirement": "Per-spec cross-artifact validation",
      "name": "Deferred rule is surfaced when a participant is not locally valid",
      "given": ["one participant artifact for the spec failed local validation"],
      "when": ["ValidateSpecs evaluates a cross-artifact rule that needs that participant"],
      "then": [
        "the rule is deferred",
        "AND the spec entry includes a non-failing warning explaining the deferral"
      ]
    },
    {
      "requirement": "Per-spec cross-artifact validation",
      "name": "ValidateSpecs reuses ValidateArtifacts cross-artifact semantics",
      "given": ["a cross-artifact rule using keySelector, subset, and options.ordering: strict"],
      "when": ["the same spec content is evaluated through ValidateArtifacts and ValidateSpecs"],
      "then": ["both use cases apply the same participant key extraction and relation semantics"]
    },
    {
      "requirement": "Aggregated result",
      "name": "Counts reflect validation outcomes",
      "given": ["three specs validated where two pass and one fails"],
      "when": ["the result is returned"],
      "then": ["totalSpecs is 3, passed is 2, failed is 1"]
    },
    {
      "requirement": "Aggregated result",
      "name": "Cross-artifact failures and deferred warnings are aggregated per spec",
      "given": [
        "one spec has a cross-artifact mismatch",
        "AND another spec has a deferred cross-artifact rule because one participant is invalid"
      ],
      "when": ["ValidateSpecs returns its result"],
      "then": [
        "the first spec entry includes the relational failure",
        "AND the second spec entry includes the deferred warning"
      ]
    },
    {
      "requirement": "Format inference and parser resolution",
      "name": "Format inferred from filename when not explicit",
      "given": ["an artifact type with no explicit format and filename spec.md"],
      "when": ["validation runs"],
      "then": ["the format is inferred as markdown and the corresponding parser is used"]
    },
    {
      "requirement": "Format inference and parser resolution",
      "name": "No parser available skips artifact silently",
      "given": ["an artifact type whose inferred format has no registered parser"],
      "when": ["validation runs"],
      "then": ["no failure or warning is recorded for that artifact"]
    },
    {
      "requirement": "Metadata materialization and validation",
      "name": "Stale persisted metadata is self-healed, not a failure",
      "given": [
        "a spec has metadata.json whose contentHashes no longer match the current required artifacts"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": [
        "GetSpecMetadata self-heals a fresh in-memory projection (delegating with policy: 'if-needed')",
        "AND no ValidationFailure is recorded solely because the persisted cache was stale"
      ]
    },
    {
      "requirement": "Metadata materialization and validation",
      "name": "Materialization failure becomes a validation failure",
      "given": [
        "GetSpecMetadata cannot produce a valid metadata projection for a spec (for example, a source read failure or a projection that fails structural validation even after regeneration)"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": [
        "the spec entry includes a ValidationFailure indicating that metadata could not be materialized"
      ]
    },
    {
      "requirement": "Metadata materialization and validation",
      "name": "Post-materialization dependsOn mismatch becomes a validation failure",
      "given": [
        "the materialized metadata's normalized dependsOn differs from SpecRepository.readPersistedState(spec).dependsOn (or [] for a lock-less spec)"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": ["the spec entry includes a ValidationFailure describing the internal inconsistency"]
    },
    {
      "requirement": "Metadata materialization and validation",
      "name": "Extracted dependsOn mismatch becomes a validation failure",
      "given": [
        "the schema declares metadataExtraction.dependsOn",
        "AND extraction yields a dependency set different from the persisted dependency state"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": ["the spec entry includes a ValidationFailure"]
    },
    {
      "requirement": "Metadata materialization and validation",
      "name": "Persisted dependency projection remains valid without extraction",
      "given": [
        "the schema omits metadataExtraction.dependsOn",
        "AND metadata.json.dependsOn matches the persisted dependency state"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": ["no dependency-projection failure is recorded for that check"]
    },
    {
      "requirement": "Metadata materialization and validation",
      "name": "metadata-cache-write-failed is a warning, not a failure",
      "given": [
        "materialization returns a metadata-cache-write-failed warning while still producing a valid in-memory projection"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": [
        "the warning is surfaced as a ValidationWarning on the spec's entry",
        "AND it is not recorded as a ValidationFailure"
      ]
    },
    {
      "requirement": "Persisted optimization staleness is an independent failure",
      "name": "Stale optimization field fails validation independently",
      "given": [
        "a spec's persisted optimizedDescription or optimizedContext is stale against its current artifacts or schema identity"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": [
        "the spec entry includes a ValidationFailure identifying the stale field(s) and reason(s)"
      ]
    },
    {
      "requirement": "Persisted optimization staleness is an independent failure",
      "name": "Optimization staleness failure is reported even when metadata materialization succeeds",
      "given": [
        "metadata materialization succeeds and all other checks pass for a spec",
        "AND a persisted optimization field is stale"
      ],
      "when": ["ValidateSpecs validates that spec"],
      "then": ["the spec entry still fails due to the stale optimization field"]
    },
    {
      "requirement": "Persisted optimization staleness is an independent failure",
      "name": "No optimizations or only fresh optimizations does not fail this check",
      "given": ["a spec has no persisted optimizations, or only fresh optimization fields"],
      "when": ["ValidateSpecs validates that spec"],
      "then": ["this check does not fail for that spec"]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Hard hit skips full validation work",
      "given": ["a valid cache row for a spec whose stamps still match"],
      "when": ["ValidateSpecs validates that spec via"],
      "then": [
        "the cached SpecValidationEntry is returned for that spec",
        "AND artifact parse, rule evaluation, cross-artifact evaluation, and metadata\nconsistency checks are not re-run for that spec"
      ]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Soft hit is invisible to ValidateSpecs",
      "given": ["a cache row whose stamps changed but whose cacheFingerprint still matches"],
      "when": ["ValidateSpecs validates that spec"],
      "then": [
        "the result is a hit with the cached entry",
        "AND ValidateSpecs does not receive a soft-hit / refreshStamps signal",
        "AND full validation is not re-run"
      ]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Miss runs full validation and upserts including failures",
      "given": ["no usable cache row for a spec (bucket invalid, stamp+fingerprint miss, or"],
      "when": ["ValidateSpecs validates that spec and records failures and warnings"],
      "then": [
        "the full validation path runs",
        "AND upsert({ entry, spec, schemaFingerprint, engineVersion }) is called",
        "AND ValidateSpecs does not pass stamps or cacheFingerprint on upsert"
      ]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Lock content change forces miss via cacheFingerprint",
      "given": ["two otherwise identical specs whose persisted lock sidecar contents differ"],
      "when": ["each is validated after an initial cached pass"],
      "then": ["the second validation cannot hard/soft-hit on the first row's fingerprint"]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Hosts observe identical public behaviour on hit or miss",
      "when": ["a host invokes validation with the same public inputs"],
      "then": [
        "returned ValidateSpecsResult shape and fields are unchanged by whether a",
        "AND the host is not required to pass cache-specific options"
      ]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Workspace discovery lists with includeMeta",
      "given": ["workspace validation mode"],
      "when": ["specs are discovered for validation"],
      "then": ["the repository is listed with { includeMeta: true }"]
    },
    {
      "requirement": "Transparent validation result cache",
      "name": "Warm hard hit does not N×get solely for stamps",
      "given": [
        "workspace or all-workspaces validation with list Meta available",
        "AND the validation result cache would hard-hit from those stamps"
      ],
      "when": ["ValidateSpecs looks up the cache for each listed spec"],
      "then": [
        "it passes the list Meta stamps into lookup",
        "AND it does not call repo.get() solely to obtain stamps for those hard hits"
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveValidateSpecsDeps",
      "name": "createValidateSpecs config form derives ValidateSpecsDeps through resolveValidateSpecsDeps",
      "when": ["createValidateSpecs(config, options?) is invoked"],
      "then": [
        "it creates a composition resolver for that composition session",
        "AND it derives ValidateSpecsDeps through resolveValidateSpecsDeps(resolver)",
        "AND resolveValidateSpecsDeps(resolver) resolves:",
        "AND the factory delegates to canonical createValidateSpecs(deps)"
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveValidateSpecsDeps",
      "name": "resolveValidateSpecsDeps wires one GetSpecMetadata instance per workspace",
      "when": ["resolveValidateSpecsDeps(resolver) runs"],
      "then": [
        "the resolved deps include getMetadata: ReadonlyMap<string, GetSpecMetadata> with one instance per configured workspace",
        "AND it is used to self-heal metadata before normalized-content validation"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "core:validate-specs",
        "file": "core:src/application/use-cases/validate-specs.ts",
        "symbol": "_validateMetadataConsistency"
      },
      {
        "specId": "core:validate-specs",
        "file": "core:src/application/use-cases/validate-specs.ts",
        "symbol": "ValidateSpecs"
      },
      {
        "specId": "core:validate-specs",
        "file": "core:src/application/use-cases/_shared/validation-source-stamps.ts",
        "symbol": "stampsFromListEntry"
      },
      {
        "specId": "core:validate-specs",
        "file": "core:src/application/use-cases/_shared/validation-source-stamps.ts",
        "symbol": "specFromListEntry"
      },
      {
        "specId": "core:validate-specs",
        "file": "core:src/application/use-cases/_shared/validation-source-stamps.ts",
        "symbol": "listEntryHasMeta"
      }
    ]
  },
  "optimizedDescription": "ValidateSpecs checks spec-scoped artifacts vs schema; three scopes; cache + GetSpecMetadata self-heal.",
  "optimizedContext": "# ValidateSpecs\n## Rules\n### Schema / filter\n- `SchemaProvider.get()` (propagate SchemaNotFound/Validation). Keep only `scope === 'spec'` artifacts.\n### Modes\n- `specPath` (`workspace:capabilityPath`): parse → repo → `get` → validate one. Precedence over workspace.\n- `workspace` only: `list(undefined, { includeMeta: true })` then each.\n- Neither: all workspaces, same list Meta stamps.\n### Per-spec local + cross-artifact\n- Missing required → failure; missing optional → skip. Parse+evaluateRules when rules present. Reuse ValidateArtifacts cross-artifact engine for `scope:spec` rules; defer non-ready participants as non-failing notices.\n### Result\n- `ValidateSpecsResult`: `entries`, `totalSpecs`, `passed`, `failed`. Entry: `spec`, `passed`, `failures`, `warnings`.\n### Format inference\n- Infer via `inferFormat` if no format; no parser → skip silently.\n### Metadata via GetSpecMetadata\n- After artifact validation: `GetSpecMetadata.execute({ specId })` — NOT direct `readMetadataSnapshot` for pass/fail.\n- Missing/stale cache OK if materialization succeeds. Materialization impossible → failure. Validate materialized `dependsOn` vs `readPersistedState().dependsOn` (or `[]`). Extraction mismatch → failure. `metadata-cache-write-failed` → warning.\n### Optimization staleness\n- Independent: stale `optimizedDescription`/`optimizedContext` → ValidationFailure even if other checks pass. No opts / all fresh → OK.\n### ValidationResultCache\n- lookup before full validate; upsert on miss. Hard hit skips revalidation. Pass list Meta stamps. Supply `schemaFingerprint` (spec validations + cross + dependsOn extraction presence) + `engineVersion`.\n- Missing cache map entry → skip cache (tests/direct ctor only). Config factory MUST register per-workspace caches + GetSpecMetadata maps via `resolveValidateSpecsDeps`.\n## Constraints\n- ReadonlyMap of SpecRepositories — no mutation.\n- Rules only from schema. Shared ValidationFailure/Warning with ValidateArtifacts.\n- No own metadata freshness/gen/persist — only GetSpecMetadata.",
  "contentHashes": {
    "spec.md": "sha256:880fc0501a0fac11258c12b46fe7a4c74051d9097d458b2566c6724383e8fe93",
    "verify.md": "sha256:8f38971aacdc01c9abe8b72fedd7ca0dba85da675149f058ace5eb8d7b619729"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:880fc0501a0fac11258c12b46fe7a4c74051d9097d458b2566c6724383e8fe93",
        "lastModified": "2026-07-25T11:53:31.181Z"
      },
      "verify.md": {
        "hash": "sha256:8f38971aacdc01c9abe8b72fedd7ca0dba85da675149f058ace5eb8d7b619729",
        "lastModified": "2026-07-25T11:53:31.182Z"
      }
    },
    "persistedStateHash": "sha256:6b91aeeb0312d84ae8f580b77cbbaaa81006a184d715233b17301d09870f57bd",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
