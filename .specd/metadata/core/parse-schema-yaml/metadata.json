{
  "title": "Parse Schema YAML",
  "description": "Multiple `SchemaRegistry` adapters (filesystem, in-memory, remote) need to parse and structurally validate schema YAML in exactly the same way, and embedding that logic in a single adapter forces duplication. `parseSchemaYaml` extracts YAML parsing and Zod validation into a reusable, adapter-independent infrastructure function that receives raw YAML content and a schema reference string, validates against the `SchemaYaml` Zod schema, and returns a typed `SchemaYamlData` intermediate. It performs structural validation only — domain entity construction and semantic checks (cycle detection, ID format) remain the caller's responsibility.",
  "dependsOn": ["default:_global/architecture", "core:core/schema-format"],
  "context": [
    "Multiple `SchemaRegistry` adapters (filesystem, in-memory, remote) need to parse and structurally validate schema YAML in exactly the same way, and embedding that logic in a single adapter forces duplication. `parseSchemaYaml` extracts YAML parsing and Zod validation into a reusable, adapter-independent infrastructure function that receives raw YAML content and a schema reference string, validates against the `SchemaYaml` Zod schema, and returns a typed `SchemaYamlData` intermediate. It performs structural validation only — domain entity construction and semantic checks (cycle detection, ID format) remain the caller's responsibility."
  ],
  "rules": [
    {
      "requirement": "Module location and layer",
      "rules": [
        "The module SHALL reside at `infrastructure/schema-yaml-parser.ts` within `@specd/core`. It is infrastructure code — it depends on the `yaml` and `zod` libraries — but it is NOT adapter-specific. Any `SchemaRegistry` adapter (filesystem, in-memory, remote) MAY import it to share the same parsing and structural validation logic."
      ]
    },
    {
      "requirement": "Function signature",
      "rules": [
        "The module SHALL export a function `parseSchemaYaml(ref: string, yamlContent: string): SchemaYamlData`.\n\n- `ref` — the schema reference string (e.g. `#my-schema`, `@specd/schema-std`). Used exclusively in error messages to identify which schema failed validation.\n- `yamlContent` — the raw YAML content string to parse and validate.\n- Returns a `SchemaYamlData` object on success."
      ]
    },
    {
      "requirement": "Output type",
      "rules": [
        "The module SHALL export a `SchemaYamlData` type representing the validated intermediate structure. This type uses Zod-inferred shapes — it is NOT a domain `Schema` entity. The type SHALL contain:\n\n- `name: string` — the schema's machine identifier\n- `version: number` — integer schema version\n- `description?: string` — optional human-readable summary\n- `kind: 'schema' | 'schema-plugin'` — schema type discriminator\n- `extends?: string` — optional parent schema reference\n- `artifacts: ArtifactYaml[]` — array of raw artifact definitions as validated by Zod (NOT domain `ArtifactType` instances)\n- `workflow?: WorkflowStepRaw[]` — optional array of raw workflow step definitions before domain transform\n- `metadataExtraction?` — optional metadata extraction configuration as validated by Zod\n\nThe `ArtifactYaml` and `WorkflowStepRaw` types SHALL also be exported for downstream use. These types represent the Zod-inferred output shapes of their respective schemas, preserving `| undefined` on optional fields."
      ]
    },
    {
      "requirement": "YAML parsing",
      "rules": [
        "The function SHALL parse the `yamlContent` string using the `yaml` library. If the parsed result is `null`, not an object, or is an array, the function SHALL throw a `SchemaValidationError` with the provided `ref` and a message indicating that the schema file must be a YAML mapping."
      ]
    },
    {
      "requirement": "Zod structural validation",
      "rules": [
        "After successful YAML parsing, the function SHALL validate the parsed object against the `SchemaYaml` Zod schema. The Zod schema SHALL enforce:\n\n- `name` is a required string\n- `version` is a required integer\n- `kind` is a required enum (`schema` | `schema-plugin`)\n- `extends` is an optional string, only valid when `kind: schema`\n- `artifacts` is a required array of artifact objects, each validated for required fields (`id`, `scope`, `output`) and optional fields with correct types\n- Hook entries require an `id` field alongside `instruction` or `run`\n- Validation/deltaValidation rule entries require an `id` field\n- `preHashCleanup` entries require an `id` field\n- `rules` on artifacts is an optional object with `pre` and `post` arrays of `{ id, text }` `rules` on artifacts is an optional object with `pre` and `post` arrays of `{ id, instruction }`\n- `workflow` is an optional array of workflow step objects\n- `metadataExtraction` is an optional object matching the metadata extraction schema; array entries require an `id` field\n- Unknown top-level fields SHALL be ignored (forward compatibility via Zod's default `strip` mode)\n\nThe Zod schemas for selector fields within validation rules, metadata extractors, and related structures SHALL be imported from the existing `infrastructure/zod/selector-schema.ts` module to avoid duplication."
      ]
    },
    {
      "requirement": "Zod refinement rules",
      "rules": [
        "The Zod schema SHALL include refinement rules that enforce structural constraints at the artifact level:\n\n- `deltaValidations` is only valid when `delta` is `true`\n- `delta: true` is not valid when `scope` is `change`\n- `kind: schema-plugin` must not declare `artifacts`, `workflow`, `metadataExtraction`, or `extends`\n\nThese refinements SHALL produce descriptive error messages."
      ]
    },
    {
      "requirement": "Error handling",
      "rules": [
        "On any validation failure, the function SHALL throw a `SchemaValidationError` (a domain error extending `SpecdError`) with:\n\n- The `ref` string identifying which schema failed\n- A descriptive message that includes the Zod issue path when available, formatted as a dot-bracket string (e.g. `artifacts[0].scope: invalid enum value`)\n\nThe function SHALL report only the first Zod issue in the error message."
      ]
    },
    {
      "requirement": "No semantic validation",
      "rules": [
        "The function SHALL NOT perform semantic validation. The following checks are explicitly out of scope and remain the responsibility of the caller (e.g. `SchemaRegistry.resolve()`):\n\n- Duplicate `artifact.id` detection\n- Duplicate `workflow[].step` detection\n- `artifact.id` format validation (`/^[a-z][a-z0-9-]*$/`)\n- Unknown artifact ID in `artifact.requires`\n- Circular dependency detection in `artifact.requires`\n- Non-optional artifact depending on optional artifact"
      ]
    },
    {
      "requirement": "No domain object construction",
      "rules": [
        "The function SHALL NOT construct domain entities or value objects (`Schema`, `ArtifactType`, `WorkflowStep`, `Selector`, etc.). It returns the Zod-validated intermediate representation only. Domain object construction remains the responsibility of the adapter or caller."
      ]
    },
    {
      "requirement": "formatZodPath utility",
      "rules": [
        "The module SHALL export a `formatZodPath` utility function that converts a Zod issue path (array of strings and numbers) into a human-readable dot-bracket string (e.g. `artifacts[0].scope`). This function is extracted from the existing `FsSchemaRegistry` implementation to be shared across consumers."
      ]
    }
  ],
  "constraints": [
    "The module resides in infrastructure/ but NOT inside infrastructure/fs/ — it has no filesystem dependency",
    "The module depends on yaml (npm) and zod (npm) — both are infrastructure-level dependencies",
    "The module imports SelectorZodSchema and SelectorRaw from infrastructure/zod/selector-schema.ts — selector Zod schemas MUST NOT be duplicated",
    "The module imports SchemaValidationError from domain/errors/ — this is the only domain import permitted",
    "The output types (SchemaYamlData, ArtifactYaml, WorkflowStepRaw) use Zod-inferred shapes with | undefined on optional fields — they are NOT domain types",
    "The Zod schema uses strip mode (default) for unknown fields — no passthrough or strict",
    "The function is synchronous — YAML parsing and Zod validation require no I/O"
  ],
  "scenarios": [
    {
      "requirement": "Function signature",
      "name": "Valid minimal schema YAML",
      "when": [
        "parseSchemaYaml is called with a ref #test and YAML content containing name, version, and a non-empty artifacts array"
      ],
      "then": ["it returns a SchemaYamlData object with the parsed name, version, and artifacts"]
    },
    {
      "requirement": "Output type",
      "name": "Optional fields are present when declared",
      "given": ["YAML content includes workflow, metadataExtraction, and description"],
      "when": ["parseSchemaYaml is called"],
      "then": [
        "the returned SchemaYamlData contains workflow, metadataExtraction, and description with their parsed values"
      ]
    },
    {
      "requirement": "Output type",
      "name": "Optional fields are undefined when omitted",
      "when": [
        "parseSchemaYaml is called with YAML content that omits workflow, metadataExtraction, and description"
      ],
      "then": [
        "the returned SchemaYamlData has workflow, metadataExtraction, and description as undefined"
      ]
    },
    {
      "requirement": "YAML parsing",
      "name": "Null YAML document",
      "when": [
        "parseSchemaYaml is called with YAML content that parses to null (e.g. an empty string or ~)"
      ],
      "then": [
        "it throws a SchemaValidationError with the ref and message schema file must be a YAML mapping"
      ]
    },
    {
      "requirement": "YAML parsing",
      "name": "YAML parses to an array",
      "when": [
        "parseSchemaYaml is called with YAML content that parses to an array (e.g. - item1\\n- item2)"
      ],
      "then": [
        "it throws a SchemaValidationError with the ref and message schema file must be a YAML mapping"
      ]
    },
    {
      "requirement": "YAML parsing",
      "name": "YAML parses to a scalar",
      "when": [
        "parseSchemaYaml is called with YAML content that parses to a scalar (e.g. just a string)"
      ],
      "then": [
        "it throws a SchemaValidationError with the ref and message schema file must be a YAML mapping"
      ]
    },
    {
      "requirement": "Zod structural validation",
      "name": "Missing required field name",
      "when": ["parseSchemaYaml is called with YAML containing version and artifacts but no name"],
      "then": ["it throws a SchemaValidationError whose message includes the path name"]
    },
    {
      "requirement": "Zod structural validation",
      "name": "Missing required field artifacts",
      "when": ["parseSchemaYaml is called with YAML containing name and version but no artifacts"],
      "then": ["it throws a SchemaValidationError whose message includes the path artifacts"]
    },
    {
      "requirement": "Zod structural validation",
      "name": "Wrong type for version",
      "when": ["parseSchemaYaml is called with YAML where version is a string (e.g. \"one\")"],
      "then": ["it throws a SchemaValidationError whose message includes the path version"]
    },
    {
      "requirement": "Zod structural validation",
      "name": "Non-integer version",
      "when": ["parseSchemaYaml is called with YAML where version is 1.5"],
      "then": ["it throws a SchemaValidationError whose message includes the path version"]
    },
    {
      "requirement": "Zod structural validation",
      "name": "Unknown top-level fields are ignored",
      "when": [
        "parseSchemaYaml is called with valid YAML that also contains an unknown field futureField: true"
      ],
      "then": [
        "it returns a SchemaYamlData successfully without error",
        "AND the returned object does not contain futureField"
      ]
    },
    {
      "requirement": "Zod refinement rules",
      "name": "deltaValidations on non-delta artifact",
      "when": [
        "parseSchemaYaml is called with an artifact that has deltaValidations but delta is false or omitted"
      ],
      "then": [
        "it throws a SchemaValidationError whose message indicates deltaValidations is only valid when delta is true"
      ]
    },
    {
      "requirement": "Zod refinement rules",
      "name": "delta true with scope change",
      "when": ["parseSchemaYaml is called with an artifact that has delta: true and scope: change"],
      "then": [
        "it throws a SchemaValidationError whose message indicates delta is not valid when scope is change"
      ]
    },
    {
      "requirement": "Error handling",
      "name": "Error message includes Zod path",
      "when": ["parseSchemaYaml is called with YAML where artifacts[0].scope has an invalid value"],
      "then": ["the thrown SchemaValidationError message includes artifacts[0].scope"]
    },
    {
      "requirement": "Error handling",
      "name": "Error includes the ref",
      "when": ["parseSchemaYaml is called with ref @specd/schema-broken and invalid YAML content"],
      "then": ["the thrown SchemaValidationError contains @specd/schema-broken as the ref"]
    },
    {
      "requirement": "Error handling",
      "name": "Only first Zod issue is reported",
      "when": [
        "parseSchemaYaml is called with YAML that has multiple validation errors (e.g. missing name and missing artifacts)"
      ],
      "then": ["the thrown SchemaValidationError message reports only one issue"]
    },
    {
      "requirement": "No semantic validation",
      "name": "Duplicate artifact IDs pass structural validation",
      "when": ["parseSchemaYaml is called with YAML containing two artifacts with the same id"],
      "then": [
        "it returns a SchemaYamlData without error (duplicate detection is the caller's responsibility)"
      ]
    },
    {
      "requirement": "No semantic validation",
      "name": "Unknown artifact ID in requires passes structural validation",
      "when": [
        "parseSchemaYaml is called with an artifact whose requires references a non-existent artifact ID"
      ],
      "then": ["it returns a SchemaYamlData without error"]
    },
    {
      "requirement": "No semantic validation",
      "name": "Circular requires passes structural validation",
      "when": ["parseSchemaYaml is called with artifacts that have circular requires references"],
      "then": ["it returns a SchemaYamlData without error"]
    },
    {
      "requirement": "No domain object construction",
      "name": "Artifacts are raw Zod-inferred objects",
      "when": ["parseSchemaYaml returns successfully"],
      "then": [
        "the artifacts array contains plain objects with Zod-inferred shapes, NOT ArtifactType domain instances"
      ]
    },
    {
      "requirement": "formatZodPath utility",
      "name": "Numeric path segments formatted as brackets",
      "when": ["formatZodPath is called with ['artifacts', 0, 'scope']"],
      "then": ["it returns artifacts[0].scope"]
    },
    {
      "requirement": "formatZodPath utility",
      "name": "Single string segment",
      "when": ["formatZodPath is called with ['name']"],
      "then": ["it returns name"]
    },
    {
      "requirement": "formatZodPath utility",
      "name": "Empty path",
      "when": ["formatZodPath is called with []"],
      "then": ["it returns an empty string"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:68facb09347b70b4cf5d00146ef97efa0827ecf909ef34712a0efca4b93cc30e",
    "verify.md": "sha256:a7f2fb7802466f2cb2c5cb6a0bb5d8d895aeae8e5f96df2554d40aca5c8d1234"
  },
  "generatedBy": "core"
}
