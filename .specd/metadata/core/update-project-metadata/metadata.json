{
  "title": "core:update-project-metadata",
  "description": "To maintain an optimized project context, we need a way for agents to save their optimizations while the system ensures the cache remains valid. The `UpdateProjectMetadata` use case accepts an optimized payload, computes the current hashes for all project context inputs, and persists the full `project-metadata.json` structure.",
  "dependsOn": ["core:project-metadata", "core:composition-resolver", "core:get-spec-metadata"],
  "context": [
    "To maintain an optimized project context, we need a way for agents to save their optimizations while the system ensures the cache remains valid. The `UpdateProjectMetadata` use case accepts an optimized payload, computes the current hashes for all project context inputs, and persists the full `project-metadata.json` structure."
  ],
  "rules": [
    {
      "requirement": "Hash computation",
      "rules": [
        "The use case SHALL compute current SHA-256 hashes for:\n\n- The project's `specd.yaml`\n- All `contextFiles` defined in the configuration\n\nFor every spec currently included in the project context, the use case SHALL call `GetSpecMetadata.execute({ specId })` (default `'if-needed'` policy) and record the returned `metadataFingerprint` in `freshness`, rather than hashing a raw metadata-cache-file snapshot or a repository storage revision. This keeps freshness comparisons storage-adapter-independent and self-healing: a spec whose metadata was missing or stale is regenerated as part of this computation."
      ]
    },
    {
      "requirement": "Atomicity",
      "rules": ["The update to `project-metadata.json` SHALL be atomic to prevent corruption."]
    },
    {
      "requirement": "Payload separation",
      "rules": [
        "The use case SHALL only accept the `optimizedContext` string (wrapped in an input object) from the caller. It SHALL NOT allow the caller to provide its own freshness hashes or versioning information. This data is mapped to the internal `optimized.context` field in the persisted file."
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveUpdateProjectMetadataDeps",
      "rules": [
        "The config-based `createUpdateProjectMetadata(config, options?)` form MUST derive `UpdateProjectMetadataDeps` through `resolveUpdateProjectMetadataDeps(resolver)` and then delegate to canonical `createUpdateProjectMetadata(deps)`.\n\n`resolveUpdateProjectMetadataDeps(resolver)` MUST resolve:\n\n- `config: SpecdConfig`\n- `listWorkspaces: ListWorkspaces`\n- `specRepos: ReadonlyMap<string, SpecRepository>`\n- `getMetadata: GetSpecMetadata`\n- `files: FileReader`\n- `fileWriter: FileWriter`\n- `hasher: ContentHasher`\n\nThe helper is the only use-case-specific composition entry for config-based bootstrap. The factory MUST NOT reconstruct fs-shaped wiring inline."
      ]
    }
  ],
  "scenarios": [
    {
      "requirement": "Hash computation",
      "name": "Computes all hashes",
      "when": ["UpdateProjectMetadata is executed"],
      "then": [
        "it uses the ContentHasher to get fresh hashes for specd.yaml and context files",
        "AND it calls GetSpecMetadata.execute({ specId }) for each spec included in the project context and records the returned metadataFingerprint"
      ]
    },
    {
      "requirement": "Hash computation",
      "name": "Self-healing fingerprint computation regenerates missing or stale spec metadata",
      "given": ["an included spec's persisted metadata is missing or stale"],
      "when": ["UpdateProjectMetadata computes freshness hashes"],
      "then": [
        "GetSpecMetadata regenerates that spec's metadata as part of computing its metadataFingerprint",
        "AND the resulting fingerprint reflects the regenerated projection"
      ]
    },
    {
      "requirement": "Atomicity",
      "name": "Atomic write",
      "when": ["saving project-metadata.json"],
      "then": ["the write operation is atomic (e.g., write to temp then rename)"]
    },
    {
      "requirement": "Payload separation",
      "name": "Caller cannot override hashes",
      "when": [
        "UpdateProjectMetadata is called with a payload containing internal fields like freshness or version"
      ],
      "then": [
        "the use case ignores those fields and computes them itself, only using the provided optimizedContext"
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveUpdateProjectMetadataDeps",
      "name": "createUpdateProjectMetadata config form derives UpdateProjectMetadataDeps through resolveUpdateProjectMetadataDeps",
      "when": ["createUpdateProjectMetadata(config, options?) is invoked"],
      "then": [
        "it creates a composition resolver for that composition session",
        "AND it derives UpdateProjectMetadataDeps through resolveUpdateProjectMetadataDeps(resolver)",
        "AND resolveUpdateProjectMetadataDeps(resolver) resolves:",
        "AND the factory delegates to canonical createUpdateProjectMetadata(deps)"
      ]
    },
    {
      "requirement": "Config-based factory delegates through resolveUpdateProjectMetadataDeps",
      "name": "resolveUpdateProjectMetadataDeps wires GetSpecMetadata for fingerprint computation",
      "when": ["resolveUpdateProjectMetadataDeps(resolver) runs"],
      "then": [
        "the resolved deps include getMetadata: GetSpecMetadata",
        "AND it is used to compute per-spec metadataFingerprint values instead of hashing raw metadata content"
      ]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:446c7abffb9e396c9dc00596d5bab197ad072c25fb0406bb2d4e3c3b07edad44",
    "verify.md": "sha256:3e49d15ee5bc67c39e73d04aaa4d626bc26100358c307c6e73db5d6fb0ac423c"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:446c7abffb9e396c9dc00596d5bab197ad072c25fb0406bb2d4e3c3b07edad44",
        "lastModified": "2026-07-25T11:53:31.733Z"
      },
      "verify.md": {
        "hash": "sha256:3e49d15ee5bc67c39e73d04aaa4d626bc26100358c307c6e73db5d6fb0ac423c",
        "lastModified": "2026-07-25T11:53:31.733Z"
      }
    },
    "persistedStateHash": "sha256:11b04d98074f1acf57e6654eecc00a72e157c5eb3682657e182dcb856db0bce6",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
