{
  "title": "plugin-agent-claude:plugin-agent",
  "description": "Claude agent plugin implementation. Exports `create(): AgentPlugin` that provides skill installation by declaring Claude-supported capabilities and frontmatter source values to `@specd/skills`, which renders the final installed markdown for Claude Code.",
  "dependsOn": [
    "core:config",
    "plugin-manager:agent-plugin-type",
    "skills:skill-bundle",
    "skills:skill-repository",
    "skills:resolve-bundle",
    "skills:agents"
  ],
  "context": [
    "Claude agent plugin implementation. Exports `create(): AgentPlugin` that provides skill installation by declaring Claude-supported capabilities and frontmatter source values to `@specd/skills`, which renders the final installed markdown for Claude Code."
  ],
  "rules": [
    {
      "requirement": "Factory export",
      "rules": [
        "The package MUST export `create(options: PluginLoaderOptions): AgentPlugin` as default or named export.\n\nThe factory function MUST read `specd-plugin.json` at the infrastructure boundary to obtain `name` and `version`. It MUST search for the manifest in its own directory first, then the parent directory as fallback. The `type` field MUST remain hardcoded as `'agent'`."
      ]
    },
    {
      "requirement": "Domain layer",
      "rules": [
        "The domain layer MUST contain:\n\n- `claude-plugin.ts` — implementation of `AgentPlugin` interface, receiving `name` and `version` via constructor (not hardcoded)\n- `frontmatter.ts` — Frontmatter type definitions\n- `frontmatter/` — skill metadata map (`skillFrontmatter: Record<string, Frontmatter>`)\n\nThe plugin class constructor MUST accept `name` and `version` as parameters. The `type` getter MUST return `'agent'` (hardcoded)."
      ]
    },
    {
      "requirement": "Frontmatter type",
      "rules": [
        "The Claude plugin MUST define a structured value model that can represent the Claude-supported metadata set:\n\n- `name`\n- `description`\n- `when_to_use`\n- `argument_hint`\n- `disable_model_invocation`\n- `user_invocable`\n- `allowed_tools`\n- `model`\n- `effort`\n- `context`\n- `agent`\n- `hooks`\n- `paths`\n- `shell`"
      ]
    },
    {
      "requirement": "Application layer",
      "rules": [
        "The application layer MUST have an `InstallSkills` use case that orchestrates:\n\n1. Get skills via `@specd/skills`\n2. Resolve Claude frontmatter source values for each skill\n3. Declare only the Claude-supported capability identifiers for each install (`['mcp', 'agents', 'frontmatter']`)\n4. Resolve bundles through `ResolveBundle` so built-in render defaults are supplied by `@specd/skills`\n5. Pass `variables.sharedFolder` only when overriding the default shared path contract\n6. Install files not marked as shared to categorized directories relative to `projectRoot`:\n   - Skills to `.claude/skills/<skill-name>/`\n   - Agents to `.claude/agents/<agent-name>/` (only when `agents` capability is supported)\n7. Install files marked as shared to the rendered `sharedFolder` location under the project root\n8. Fallback: If `agents` capability is missing, install agents into the same directory as the shared context file.\n9. Sub-agent Mapping: When installing an agent, the plugin MUST:\n   - Map `allowedTools` list into a single comma-separated string for the `tools` YAML key.\n   - Map the preferred model from `specd-agent.meta.json` to the `model` YAML key if present.\n   - Prepend the rendered system prompt with the generated YAML frontmatter.\n\nThe plugin MUST NOT assemble the final YAML frontmatter document itself for standard skills after bundle resolution.\nThe plugin MUST NOT normalize capability objects on behalf of `@specd/skills`.\nThe plugin MUST NOT call `SkillRepository.getBundle(...)` directly from the install flow when `ResolveBundle` is available.\nThe plugin MUST NOT use a `buildCapabilities` helper; capability arrays MUST be passed as literals or constants."
      ]
    },
    {
      "requirement": "Frontmatter injection",
      "rules": [
        "During install, the plugin MUST provide Claude frontmatter values to `@specd/skills` so that the rendered skill-local markdown output includes Claude-compatible YAML frontmatter.\n\nThe plugin MUST NOT prepend YAML frontmatter itself after bundle resolution.\n\nFiles marked as shared MUST continue to be written without Claude skill frontmatter."
      ]
    },
    {
      "requirement": "Install location",
      "rules": [
        "Skills MUST be installed to `.claude/skills/` in the project root.\nAgents MUST be installed to `.claude/agents/` in the project root (when `agents` capability is supported).\n\nFor each skill or agent, files not marked as shared MUST be installed under its respective categorized directory.\n\nFiles marked as shared MUST be installed under the rendered `sharedFolder` location within the project root. When no override is provided, that location MUST default to a relative path derived from the runtime config directory.\n\nThe resolved shared location MUST NOT contain a `SKILL.md` file."
      ]
    },
    {
      "requirement": "Uninstall behavior",
      "rules": [
        "`uninstall(config: SpecdConfig, options?: AgentInstallOptions)` MUST remove installed skill directories from `.claude/skills/` and agent files from `.claude/agents/` relative to `config.projectRoot`.\n\nWhen `options.skills` is provided, uninstall MUST remove only the selected specd-managed skill directories. The shared resource directory at the resolved `sharedFolder` location MUST remain in place because other installed skills may still reference it.\n\nWhen `options.agents` is provided, uninstall MUST remove only the selected specd-managed agent files from `.claude/agents/`.\n\nWhen no filters are provided, uninstall MUST remove all specd-managed skill directories, all specd-managed agent files under `.claude/agents/`, and the resolved sharedFolder location.\n\nUninstall MUST NOT remove unrelated directories or files under `.claude/skills/` or `.claude/agents/` that are not part of the specd-managed skill or agent set."
      ]
    }
  ],
  "constraints": [
    "This plugin depends on @specd/skills for skill operations.",
    "name and version MUST be read from specd-plugin.json by the factory and passed to the plugin constructor — never hardcoded in the class.",
    "type MUST remain hardcoded as 'agent' for type safety."
  ],
  "scenarios": [
    {
      "requirement": "Factory export",
      "name": "Exports create function",
      "when": ["the package is imported"],
      "then": ["it exports create(): AgentPlugin"]
    },
    {
      "requirement": "Factory export",
      "name": "Factory reads manifest for name and version",
      "given": [
        "specd-plugin.json contains name: \"@specd/plugin-agent-claude\" and version: \"1.0.0\""
      ],
      "when": ["create() is called"],
      "then": [
        "the returned plugin has name === \"@specd/plugin-agent-claude\" and version === \"1.0.0\""
      ]
    },
    {
      "requirement": "Factory export",
      "name": "Type is hardcoded",
      "when": ["create() is called"],
      "then": ["the returned plugin has type === 'agent'"]
    },
    {
      "requirement": "Frontmatter injection",
      "name": "Install emits Claude-compatible frontmatter through skills rendering",
      "when": ["install() is called"],
      "then": [
        "skill-local markdown files are written with Claude-compatible frontmatter already present in the rendered content"
      ]
    },
    {
      "requirement": "Frontmatter injection",
      "name": "Shared files do not receive Claude frontmatter",
      "given": ["a resolved bundle includes files marked as shared"],
      "when": ["install() writes those files"],
      "then": ["shared files are written without Claude skill frontmatter"]
    },
    {
      "requirement": "Install location",
      "name": "Installs to .claude/skills/ and sharedFolder default location",
      "given": ["a SpecdConfig with projectRoot"],
      "when": ["install is called with that configuration"],
      "then": [
        "non-shared files are written to {projectRoot}/.claude/skills/<skill-name>/",
        "AND shared files are written to the resolved sharedFolder location under the project root"
      ]
    },
    {
      "requirement": "Install location",
      "name": "Shared directory is not discovered as a skill",
      "when": ["install writes shared files to the resolved shared location"],
      "then": ["that location does not contain a SKILL.md file"]
    },
    {
      "requirement": "Install location",
      "name": "Installs agents to categorized directory",
      "given": ["a request to install an agent"],
      "when": ["install(config, options) runs"],
      "then": [
        "it installs the agent files to the platform-specific agents/ directory",
        "AND the YAML frontmatter contains tools as a comma-separated string",
        "AND the YAML frontmatter contains model if specified in metadata"
      ]
    },
    {
      "requirement": "Install location",
      "name": "Fallback to shared location when agents capability is missing",
      "given": ["a runtime that does not support agents"],
      "when": ["an agent is installed"],
      "then": ["it is copied to the same directory as shared.md"]
    },
    {
      "requirement": "Uninstall behavior",
      "name": "Uninstall removes selected skill directories and keeps shared resources",
      "given": ["multiple skills are installed and share the resolved sharedFolder location"],
      "when": ["uninstall(config, { skills: ['specd'] }) is executed"],
      "then": ["only .claude/skills/specd/ is removed", "AND the resolved shared location remains"]
    },
    {
      "requirement": "Uninstall behavior",
      "name": "Uninstall without filter removes only specd-managed skills and shared resources",
      "given": [
        "specd-managed skills and unrelated user skills are installed under .claude/skills/"
      ],
      "when": ["uninstall(config) is executed without skills"],
      "then": [
        "all specd-managed skill directories are removed",
        "AND the resolved sharedFolder location is removed",
        "AND unrelated user skill directories remain"
      ]
    },
    {
      "requirement": "Uninstall behavior",
      "name": "Uninstall removes selected agent files",
      "given": ["multiple agents are installed"],
      "when": ["uninstall(config, { agents: ['specd-project-context-optimizer'] }) is executed"],
      "then": ["only .claude/agents/specd-project-context-optimizer.md is removed"]
    },
    {
      "requirement": "Uninstall behavior",
      "name": "Uninstall without filter removes all specd-managed agents",
      "given": [
        "specd-managed agents and unrelated user agents are installed under .claude/agents/"
      ],
      "when": ["uninstall(config) is executed without filters"],
      "then": ["all specd-managed agent files are removed", "AND unrelated user agent files remain"]
    },
    {
      "requirement": "Domain layer",
      "name": "Domain layer contains claude-plugin.ts",
      "when": ["the domain layer is inspected"],
      "then": ["claude-plugin.ts exists implementing the AgentPlugin interface"]
    },
    {
      "requirement": "Domain layer",
      "name": "Domain layer contains frontmatter.ts",
      "when": ["the domain layer is inspected"],
      "then": ["frontmatter.ts exists with Frontmatter type definitions"]
    },
    {
      "requirement": "Frontmatter type",
      "name": "Frontmatter values cover Claude-supported metadata",
      "when": ["Claude frontmatter values are prepared for install"],
      "then": [
        "they can represent the Claude-supported metadata fields",
        "AND they are stored as structured values rather than a prebuilt YAML document"
      ]
    },
    {
      "requirement": "Frontmatter type",
      "name": "Unsupported Claude metadata stays out of the value collection",
      "when": ["a non-Claude metadata key is proposed"],
      "then": ["it is excluded from the Claude frontmatter value collection"]
    },
    {
      "requirement": "Application layer",
      "name": "InstallSkills passes Claude values into skills rendering",
      "given": ["skills are available from @specd/skills"],
      "when": ["InstallSkills runs"],
      "then": [
        "it resolves Claude frontmatter source values per skill",
        "AND it passes only Claude capability identifiers into @specd/skills",
        "AND it resolves install bundles through ResolveBundle"
      ]
    },
    {
      "requirement": "Application layer",
      "name": "Shared files are written to the rendered sharedFolder location",
      "given": ["a resolved bundle contains files marked as shared"],
      "when": ["InstallSkills writes those files"],
      "then": ["they are written to the resolved sharedFolder location under the project root"]
    },
    {
      "requirement": "Application layer",
      "name": "Claude install does not call repository bundle resolution directly",
      "when": ["the Claude install flow is reviewed"],
      "then": [
        "bundle resolution goes through ResolveBundle",
        "AND the plugin does not call SkillRepository.getBundle(...) directly from InstallSkills"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "plugin-agent-claude:plugin-agent",
        "file": "plugin-agent-claude:test/install-skills.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "plugin-agent-claude:plugin-agent",
        "file": "plugin-agent-claude:src/application/use-cases/install-skills.ts",
        "symbol": "InstallSkills"
      },
      {
        "specId": "plugin-agent-claude:plugin-agent",
        "file": "plugin-agent-claude:src/domain/frontmatter/index.ts",
        "symbol": "agentFrontmatter"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:f3fbbf4a53504aa758d4f74c1eb8589dbe744a4d21df4e1abe2a3c24327a7a1a",
    "verify.md": "sha256:3483fb85ce3ac3f3c4465f266ed9c38c6d0ccc127dab48a75135483c8e0c423a"
  },
  "generatedBy": "core"
}
