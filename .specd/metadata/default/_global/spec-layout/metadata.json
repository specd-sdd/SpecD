{
  "title": "Spec Layout",
  "description": "Without a consistent layout, specs become hard to discover and tooling cannot locate them reliably. specd specs are organised by scope under `specs/` at the monorepo root — global constraints in `specs/_global/`, package-internal specs in `specs/<package>/`. Every spec subdirectory contains two files: `spec.md` (requirements and design) and `verify.md` (WHEN/THEN scenarios).",
  "dependsOn": [
    "core:core/schema-format",
    "core:core/content-extraction",
    "core:core/spec-id-format"
  ],
  "context": [
    "Without a consistent layout, specs become hard to discover and tooling cannot locate them reliably. specd specs are organised by scope under `specs/` at the monorepo root — global constraints in `specs/_global/`, package-internal specs in `specs/<package>/`. Every spec subdirectory contains two files: `spec.md` (requirements and design) and `verify.md` (WHEN/THEN scenarios)."
  ],
  "rules": [
    {
      "requirement": "Global specs for cross-cutting constraints",
      "rules": [
        "Specs that apply to all packages — architecture, coding conventions, commit format, testing rules, storage design — live under `specs/_global/<topic>/`. Any spec in `_global/` is a binding constraint on every package in the monorepo."
      ]
    },
    {
      "requirement": "Package specs for package-internal concerns",
      "rules": [
        "Specs that describe the internals of a specific package live under `specs/<package>/`, where `<package>` is the short package name (e.g. `core`, `cli`, `mcp`, `schema-std`). These specs are not binding on other packages."
      ]
    },
    {
      "requirement": "Spec file naming",
      "rules": [
        "Each spec lives in its own named subdirectory and contains two files: `spec.md` and `verify.md`. The subdirectory name is kebab-case and describes the topic concisely.\n\n- `spec.md` — what the system does: requirements, constraints, examples, rationale\n- `verify.md` — how to confirm it works: WHEN/THEN scenarios\n\nThe two files are always paired. `spec.md` contains no WHEN/THEN scenarios; `verify.md` contains no requirement prose."
      ]
    },
    {
      "requirement": "spec.md structure",
      "rules": [
        "Every `spec.md` must follow this structure. Sections marked *optional* may be omitted if not applicable; all other sections are required.\n\n```markdown\n# <Title>\n\n## Purpose\n\n<!-- 1–3 sentences: start with WHY this spec exists (the problem or need),\n     then briefly describe WHAT it covers to give the reader context. -->\n\n## Requirements\n\n### Requirement: <Name>\n\n<!-- Requirement description using SHALL/MUST for normative statements.\n     No WHEN/THEN scenarios here — those go in verify.md. -->\n\n## Constraints\n\n<!-- Bullet list of hard rules that apply across all requirements.\n     Omit if there are no constraints beyond the requirements. -->\n\n## Examples\n\n<!-- Optional. Concrete usage examples — YAML snippets, CLI invocations, etc.\n     Useful as AI context via metadataExtraction. -->\n\n## Spec Dependencies\n\n<!-- List of other specs this spec depends on or that provide context.\n     If none, write: _none_ or _none — this is a global constraint spec_ -->\n\n## ADRs\n\n<!-- Links to ADRs that motivated or constrain this spec.\n     If none, omit this section. -->\n```\n\n`## Purpose`, `## Requirements`, and `## Spec Dependencies` are mandatory. All other sections are included when they add value."
      ]
    },
    {
      "requirement": "verify.md structure",
      "rules": [
        "Every `verify.md` must follow this structure. Scenarios are grouped under requirement headings using the same `### Requirement: <name>` pattern as `spec.md` — this is required for AST-based delta selectors to locate scenario nodes by heading when changes are applied to `verify.md`.\n\n```markdown\n# Verification: <Title>\n\n## Requirements\n\n### Requirement: <Name>\n\n#### Scenario: <Name>\n\n- **GIVEN** <precondition> <!-- optional; omit when obvious from context -->\n- **AND/OR** <additional precondition> <!-- optional; repeat as needed -->\n- **WHEN** <condition>\n- **AND/OR** <additional condition> <!-- optional; repeat as needed -->\n- **THEN** <expected outcome>\n- **AND/OR** <additional assertion> <!-- optional; repeat as needed -->\n```\n\nOnly scenarios that add information beyond what the requirement prose already states are included. Scenarios that merely restate the obvious happy path are omitted."
      ]
    },
    {
      "requirement": "Spec Dependencies section",
      "rules": [
        "Every `spec.md` must include a `## Spec Dependencies` section listing other specs it depends on or that provide context for it. If there are none, the section must say `_none_`. Global specs with no dependencies use the form `_none — this is a global constraint spec_`.\n\nThe canonical dependency entry format is:\n\n- ``[`<workspace>:<capability-path>`](../relative/path/spec.md)`` when the dependency can be linked through a relative path\n- `` `<workspace>:<capability-path>` `` when the dependency should be listed without a link\n\nThe visible dependency label is the canonical spec ID. When a link is present, the `href` is a relative path to the depended-on spec artifact. Specs MAY continue to include older dependency labels while the repo is migrating, but new and modified specs SHOULD use the canonical spec-ID label format."
      ]
    }
  ],
  "constraints": [
    "specs/_global/ is for cross-cutting constraints only — not for any single package's implementation details",
    "Package spec directories use the short package name: core, cli, mcp, skills, schema-std",
    "Every spec subdirectory contains exactly two authored files: spec.md and verify.md",
    "Delta files (spec.md.delta.yaml, verify.md.delta.yaml, etc.) are change artifacts — they live in the change directory alongside the artifact and are never synced to the permanent specs/ directories",
    "spec.md contains no WHEN/THEN scenarios; verify.md contains no requirement prose",
    "Subdirectory names are kebab-case",
    "A spec in specs/<package>/ is not binding on any other package"
  ],
  "scenarios": [
    {
      "requirement": "Global specs for cross-cutting constraints",
      "name": "Global constraint applies to all packages",
      "when": ["a requirement is defined in specs/_global/"],
      "then": ["it binds all packages in the monorepo without exception"]
    },
    {
      "requirement": "Global specs for cross-cutting constraints",
      "name": "Package-specific concern is not global",
      "when": ["a requirement only concerns a single package's internals"],
      "then": ["it must not be placed in specs/_global/"]
    },
    {
      "requirement": "Package specs for package-internal concerns",
      "name": "Core use case spec",
      "when": ["a spec describes a use case implemented in @specd/core"],
      "then": ["it lives at specs/core/<topic>/spec.md and specs/core/<topic>/verify.md"]
    },
    {
      "requirement": "Package specs for package-internal concerns",
      "name": "CLI command spec",
      "when": ["a spec describes a command implemented in @specd/cli"],
      "then": ["it lives at specs/cli/<topic>/spec.md and specs/cli/<topic>/verify.md"]
    },
    {
      "requirement": "Spec file naming",
      "name": "Correct spec path",
      "when": ["a spec covers snapshot hashing in @specd/core"],
      "then": [
        "the files are at specs/core/snapshot-hasher/spec.md and specs/core/snapshot-hasher/verify.md"
      ]
    },
    {
      "requirement": "Spec file naming",
      "name": "Incorrect co-location",
      "when": ["a spec file is placed directly in specs/core/spec.md with no subdirectory"],
      "then": ["it does not conform to this layout and must be moved"]
    },
    {
      "requirement": "Spec file naming",
      "name": "Scenarios found in spec.md",
      "when": ["a spec.md file contains a #### Scenario: heading"],
      "then": ["it does not conform to this layout — scenarios must be moved to verify.md"]
    },
    {
      "requirement": "verify.md structure",
      "name": "Requirement heading missing in verify.md",
      "when": ["a verify.md file contains scenarios not grouped under a ### Requirement: heading"],
      "then": [
        "it does not conform to this layout and AST-based delta selectors will fail to locate those scenarios by heading"
      ]
    },
    {
      "requirement": "Spec Dependencies section",
      "name": "Global spec with no dependencies",
      "when": ["a spec in specs/_global/ has no dependencies on other specs"],
      "then": ["its ## Spec Dependencies section reads _none — this is a global constraint spec_"]
    },
    {
      "requirement": "Spec Dependencies section",
      "name": "Package spec uses canonical dependency label with relative link",
      "when": ["a package spec depends on core:core/config"],
      "then": [
        "it may list the dependency as [`core:core/config`](../config/spec.md)",
        "AND the visible label is the canonical spec ID while the href is the relative path to spec.md"
      ]
    },
    {
      "requirement": "Spec Dependencies section",
      "name": "Package spec may list a canonical dependency without a link",
      "when": ["a package spec needs to declare a dependency but no link is present"],
      "then": ["it may list the dependency as `core:core/config`"]
    },
    {
      "requirement": "Spec Dependencies section",
      "name": "Package spec referencing global constraints uses canonical global label",
      "when": ["a spec in specs/core/ is constrained by global architecture rules"],
      "then": [
        "it lists [`default:_global/architecture`](../../_global/architecture/spec.md) in its ## Spec Dependencies section"
      ]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:dc536a9d1c39e10b307978144bfbbd4143f5f0d2b58d1858bb9938af4fe05d05",
    "verify.md": "sha256:4e3f613bfc55584afcb370a13de35d0a14d85a54ba261aee879db4745a21d30d"
  },
  "generatedBy": "core"
}
