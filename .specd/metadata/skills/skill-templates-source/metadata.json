{
  "title": "skills:skill-templates-source",
  "description": "Defines how skill templates are sourced, stored, rendered, and linked to shared template content across supported agent runtimes. Templates live without static frontmatter in the skills package, while each skill directory declares its own metadata contract and `@specd/skills` renders the final installed markdown for each runtime.",
  "dependsOn": ["skills:skill", "cli:spec-optimizations"],
  "context": [
    "Defines how skill templates are sourced, stored, rendered, and linked to shared template content across supported agent runtimes. Templates live without static frontmatter in the skills package, while each skill directory declares its own metadata contract and `@specd/skills` renders the final installed markdown for each runtime."
  ],
  "rules": [
    {
      "requirement": "Template source location",
      "rules": [
        "Template files MUST live in categorized subdirectories under `packages/skills/templates/`:\n\n- `packages/skills/templates/skills/<skill-name>/` using the `SKILL.md.tpl` convention for standard skills.\n- `packages/skills/templates/agents/<agent-name>/` using the `SPECD-AGENT.md.tpl` convention for specialized agents.\n\nAll template source files MUST use the `.md.tpl` extension.\n\nEach skill or agent template directory MUST also contain a metadata file:\n\n- `skill.meta.json` for skills.\n- `specd-agent.meta.json` for agents.\n\nMetadata files (`skill.meta.json` and `specd-agent.meta.json`) share the same schema and serve the same purpose: declaring required capabilities and shared template dependencies. The different filenames are used strictly as discriminators for internal discovery and categorization.\n\nInstalled files rendered from those templates MUST be emitted as `.md` files after removing the trailing `.tpl` suffix."
      ]
    },
    {
      "requirement": "Template migration",
      "rules": [
        "The template directory structure SHALL be:\n\n- `templates/skills/` — contains `specd/`, `specd-archive/`, `specd-design/`, `specd-implement/`, `specd-new/`, `specd-compliance/`, and `specd-verify/`.\n- `templates/agents/` — contains specialized agents such as `specd-project-context-optimizer` and `specd-spec-context-optimizer`.\n- `templates/shared/` — contains shared template source files like `shared.md.tpl`.\n\nThe `templates/skills/` directory SHALL NOT contain a `specd-metadata/` template. Metadata optimization SHALL be exposed through the specialized agent templates.\n\nEach skill or agent directory contains its respective template file (`SKILL.md.tpl` or `SPECD-AGENT.md.tpl`) plus its metadata contract file.\n\nThe current inverse consumer index model in `shared.meta.json` MUST NOT remain the source of truth for which skills consume shared templates."
      ]
    },
    {
      "requirement": "Template metadata contract (skills and agents)",
      "rules": [
        "Each skill or agent template directory MUST declare a metadata file (`skill.meta.json` or `specd-agent.meta.json`) with this shape:\n\n```json\n{\n  \"kind\": \"skill\" | \"agent\",\n  \"supportedCapabilities\": [\"mcp\", \"agents\", \"frontmatter\"],\n  \"requiredCapabilities\": [],\n  \"requiredSharedTemplates\": [\"shared.md\"]\n}\n```\n\n`kind` (required) MUST categorize the template as a standard `skill` or a specialized `agent`. This value MUST match the parent directory name (`skills/` or `agents/`).\n\n`supportedCapabilities` declares the capability identifiers that templates in that directory are allowed to reference.\n\n`requiredCapabilities` declares the capability identifiers that MUST be present for the templates to be installable.\n\n`requiredSharedTemplates` declares the shared template filenames required by the templates."
      ]
    },
    {
      "requirement": "Capability-aware install-time rendering",
      "rules": [
        "Skill templates MUST support capability-aware install-time rendering.\n\nTemplates MAY use `Handlebars` conditionals and iteration over structured render context provided at install time.\n\nThe rendering model MUST remain deterministic and single-pass at install time. Templates MUST NOT execute arbitrary code or runtime-specific scripting.\n\nThe initial required capability identifiers MUST behave as follows:\n\n- `mcp`: enables template branches and content intended for runtimes that support MCP tools or MCP-connected workflows\n- `agents`: enables template branches and content intended for runtimes that support delegated agent or subagent workflows\n- `frontmatter`: enables final frontmatter composition and insertion from `variables.frontmatter`\n\nShared template references inside installed markdown MUST use the form `@{{sharedFolder}}/shared.md`.\n\n`sharedFolder` MUST be treated as a normal template variable and MUST remain relative to the project root in rendered output."
      ]
    },
    {
      "requirement": "Graph impact terminology in workflow templates",
      "rules": [
        "Workflow skill templates that instruct agents to run `specd graph impact` SHALL use clear user-facing terminology for impact direction and selector semantics:\n\n- **dependents** — symbols and files that depend on the target; implemented by `--direction dependents`, with `--direction upstream` as a compatibility value\n- **dependencies** — symbols and files the target depends on; implemented by `--direction dependencies`, with `--direction downstream` as a compatibility value\n- **both** — combined dependents and dependencies analysis; implemented by `--direction both`\n- **file selectors** — blast-radius queries over files use `--file`, including multiple file inputs when needed; templates MUST NOT instruct agents to use `--changes`\n\nTemplates MUST NOT ask for \"downstream dependents\" or otherwise describe `downstream` as dependents. When a skill needs the blast radius of changing a symbol or file, it SHALL use `--direction dependents` or describe the query as dependents. When a workflow needs the blast radius of several files, it SHALL use `specd graph impact --file <path1> <path2> ...` rather than a separate change-detection selector."
      ]
    },
    {
      "requirement": "Graph search snippet guidance in workflow templates",
      "rules": [
        "Workflow skill templates that instruct agents to run `specd graph search` SHALL describe snippet previews as opt-in output.\n\nSpecifically:\n\n- templates MUST NOT imply that `specd graph search` always returns visible snippet content by default\n- when a workflow example or instruction depends on reading preview text, the template SHALL include `--snippet`\n- when a workflow only needs identifiers, locations, or structured metadata, templates SHOULD omit `--snippet`\n- workflow guidance that mentions `json` or `toon` output SHALL describe the `snippet` field as omitted unless `--snippet` is passed"
      ]
    },
    {
      "requirement": "Frontmatter source",
      "rules": [
        "Frontmatter definitions MUST come from canonical skill metadata and vendor documentation for each target agent runtime. Plugin-specific frontmatter types and value collections MUST reflect those documented contracts exactly.\n\nAgent plugins MUST provide structured frontmatter data under `variables.frontmatter`; they MUST NOT pass prebuilt YAML frontmatter documents for direct insertion."
      ]
    },
    {
      "requirement": "Frontmatter injection",
      "rules": [
        "Agent plugins MUST provide their capability list when installing a skill.\n\n`@specd/skills` MUST inject the final frontmatter block when rendering skill-local markdown templates, using `variables.frontmatter` as input.\n\nFrontmatter insertion MUST occur only when the `frontmatter` capability is present. If `variables.frontmatter` is present while the `frontmatter` capability is absent, the frontmatter block MUST NOT be emitted.\n\nInjection MUST remain runtime-specific: the rendered output for each plugin emits only fields recognized by its target runtime and excludes unsupported fields.\n\nFiles marked as shared MUST NOT receive runtime skill frontmatter."
      ]
    },
    {
      "requirement": "Agent frontmatter matrix",
      "rules": [
        "The plugin frontmatter models MUST cover the complete known field set per runtime:\n\n- **Codex**: `name`, `description`\n- **Copilot**: `name`, `description`, `license`, `allowed-tools`, `user-invocable`, `disable-model-invocation`\n- **Open Code**: `name`, `description`, `license`, `compatibility`, `metadata`\n\nRuntime defaults MAY emit a smaller subset, but model/type coverage MUST include each runtime's full supported set."
      ]
    },
    {
      "requirement": "Why no frontmatter in skills package",
      "rules": [
        "The skills package does not include static frontmatter blocks because each agent environment has different metadata fields and compatibility rules. Agent plugins know their target environment and provide the runtime-specific values, while `@specd/skills` composes and injects the final frontmatter block during template rendering."
      ]
    },
    {
      "requirement": "Implementation tracking instructions in templates",
      "rules": [
        "Workflow skill templates MUST include implementation-tracking guidance for active changes.\n\nAt minimum:\n\n- implementation-oriented workflows MUST mention `specd changes implementation add` when code work is being linked back to specs\n- archive-oriented workflows MUST mention resolving tracked implementation files and reviewing implementation integrity before archive\n- shared workflow guidance MUST describe tracked implementation files and confirmed implementation links using the same terminology as the change artifacts"
      ]
    },
    {
      "requirement": "Metadata self-healing guidance in workflow templates",
      "rules": [
        "Archive-oriented, commit-oriented, and other metadata-oriented workflow skill templates MUST NOT instruct agents to scan for metadata-status values (`stale`, `missing`, `invalid`) or to run routine manual metadata regeneration as a normal workflow step. Templates MUST describe metadata as a self-healing materialized cache: normal consumers obtain a usable, current projection automatically, and `specd spec generate-metadata` MUST be presented only as an explicit forced-rebuild, cache-warming, repair, or diagnostic tool — never as a required step after a routine spec or lock change."
      ]
    },
    {
      "requirement": "Optimizer agent gating declared in templates",
      "rules": [
        "The `specd-project-context-optimizer` and `specd-spec-context-optimizer` agent templates MUST declare that they perform no optimization work — no generation, no persistence — unless the effective `llmOptimizedContext` project setting is `true`. Both templates MUST direct persistence through the lock-owned persisted-optimization commands (`cli:spec-optimizations`), and neither template MUST instruct invoking spec metadata generation after persisting an optimization."
      ]
    }
  ],
  "constraints": [
    "Templates in the skills package MUST NOT contain static frontmatter YAML blocks.",
    "Template source files in the skills package MUST use the .md.tpl extension.",
    "Each skill directory MUST contain a skill.meta.json file.",
    "shared.meta.json MUST NOT remain the canonical source for determining which skills require shared templates.",
    "Templates MAY contain a frontmatter insertion point that is resolved by @specd/skills at install time.",
    "Agent plugins are responsible for declaring supported runtime capabilities and providing structured variables.frontmatter data.",
    "Agent plugins MUST NOT emit fields unsupported by their target runtime.",
    "@specd/skills MUST perform the final frontmatter insertion for skill-local markdown files.",
    "Installed markdown MUST NOT render absolute filesystem paths.",
    "Workflow templates MUST use dependents/dependencies wording for graph impact guidance and MUST prefer --direction dependents / --direction dependencies; upstream / downstream may appear only as compatibility values.",
    "Workflow templates MUST use specd graph impact --file for file-based blast-radius checks and MUST NOT reference specd graph impact --changes."
  ],
  "scenarios": [
    {
      "requirement": "Template source location",
      "name": "Templates use markdown template extension",
      "when": ["files under packages/skills/templates/<skill-name>/ are inspected"],
      "then": [
        "template source files end with .md.tpl",
        "AND they do not use plain .md as the source template extension"
      ]
    },
    {
      "requirement": "Template source location",
      "name": "Skill directories declare metadata",
      "when": ["a skill template directory is inspected"],
      "then": ["it contains skill.meta.json"]
    },
    {
      "requirement": "Template source location",
      "name": "Rendered install files drop the template suffix",
      "given": ["a skill template source file named SKILL.md.tpl"],
      "when": ["@specd/skills resolves the install bundle"],
      "then": ["the emitted bundle filename is SKILL.md"]
    },
    {
      "requirement": "Template migration",
      "name": "Migrated template tree is complete",
      "when": ["the template source directory is validated"],
      "then": [
        "it contains skills/ (using SKILL.md.tpl and skill.meta.json)",
        "AND it contains agents/ (using SPECD-AGENT.md.tpl and specd-agent.meta.json)",
        "AND it contains shared/ for shared template source files"
      ]
    },
    {
      "requirement": "Template migration",
      "name": "Obsolete metadata skill is absent",
      "when": ["standard skill templates are discovered"],
      "then": [
        "no specd-metadata skill template is returned",
        "AND metadata optimization remains available through specialized agent templates"
      ]
    },
    {
      "requirement": "Template migration",
      "name": "Shared consumer index is no longer authoritative",
      "when": ["shared template ownership is reviewed"],
      "then": [
        "shared.meta.json is not the source of truth for which skills require shared templates"
      ]
    },
    {
      "requirement": "Template metadata contract (skills and agents)",
      "name": "Metadata files declare kind and requirements",
      "when": ["a skill.meta.json or specd-agent.meta.json file is inspected"],
      "then": [
        "it declares kind (skill or agent)",
        "AND it declares supportedCapabilities, requiredCapabilities, and requiredSharedTemplates"
      ]
    },
    {
      "requirement": "Template metadata contract (skills and agents)",
      "name": "Initial capability catalogue is declared by the contract",
      "when": ["the template metadata contract is reviewed"],
      "then": ["the initial required capability identifiers are mcp, agents, and frontmatter"]
    },
    {
      "requirement": "Capability-aware install-time rendering",
      "name": "Templates branch on provided capability identifiers",
      "given": ["install-time render context includes capability identifiers for a target runtime"],
      "when": ["a skill template uses a conditional block based on those capabilities"],
      "then": ["@specd/skills renders the branch that matches the provided capability identifiers"]
    },
    {
      "requirement": "Capability-aware install-time rendering",
      "name": "Shared references use sharedFolder variable syntax",
      "when": ["a template references a shared template path"],
      "then": ["it uses the form @{{sharedFolder}}/shared.md"]
    },
    {
      "requirement": "Capability-aware install-time rendering",
      "name": "Templates do not render absolute shared paths",
      "when": ["installed markdown is reviewed"],
      "then": [
        "shared template references remain relative to the project root",
        "AND they do not contain absolute filesystem paths"
      ]
    },
    {
      "requirement": "Capability-aware install-time rendering",
      "name": "Frontmatter capability controls frontmatter insertion",
      "given": [
        "a template contains a frontmatter insertion point",
        "AND variables.frontmatter is present"
      ],
      "when": ["install-time rendering runs with frontmatter enabled"],
      "then": ["the final frontmatter block is inserted"]
    },
    {
      "requirement": "Graph impact terminology in workflow templates",
      "name": "Workflow templates use dependents for blast-radius queries",
      "when": [
        "workflow skill templates instruct an agent to find the blast radius of changing a symbol or file"
      ],
      "then": [
        "they describe the query as dependents or use --direction dependents",
        "AND they do not call this query downstream impact or downstream dependents"
      ]
    },
    {
      "requirement": "Graph impact terminology in workflow templates",
      "name": "Workflow templates reserve downstream for dependencies",
      "when": ["workflow skill templates mention --direction downstream"],
      "then": [
        "they describe it as dependencies: symbols and files the target depends on",
        "AND combined analysis is described as both dependents and dependencies"
      ]
    },
    {
      "requirement": "Graph impact terminology in workflow templates",
      "name": "Workflow templates use --file instead of --changes",
      "when": ["workflow skill templates include a concrete file-impact command example"],
      "then": [
        "they use specd graph impact --file ...",
        "AND they do not reference specd graph impact --changes"
      ]
    },
    {
      "requirement": "Graph impact terminology in workflow templates",
      "name": "Workflow templates prefer workspace-aware file selectors",
      "when": ["workflow skill templates show file selector examples for configured projects"],
      "then": [
        "they prefer canonical workspace-prefixed paths or config-relative paths",
        "AND they do not rely on implicit default: resolution"
      ]
    },
    {
      "requirement": "Graph search snippet guidance in workflow templates",
      "name": "Workflow templates do not assume snippet-by-default text output",
      "when": [
        "workflow skill templates show a specd graph search example for ordinary lookup or metadata gathering"
      ],
      "then": ["the example does not imply that a visible snippet block is returned by default"]
    },
    {
      "requirement": "Graph search snippet guidance in workflow templates",
      "name": "Workflow templates add --snippet when preview text is required",
      "when": [
        "a workflow skill template instructs an agent to inspect preview text from a graph-search result"
      ],
      "then": ["the command example includes --snippet"]
    },
    {
      "requirement": "Graph search snippet guidance in workflow templates",
      "name": "Workflow templates describe structured snippet output as opt-in",
      "when": ["a workflow skill template references json or toon graph-search output"],
      "then": ["it describes the snippet field as omitted unless --snippet is passed"]
    },
    {
      "requirement": "Frontmatter source",
      "name": "Runtime metadata is sourced from canonical contracts",
      "when": ["plugin frontmatter models are reviewed"],
      "then": [
        "each model is derived from canonical skill metadata and the corresponding vendor documentation",
        "AND field definitions match the documented runtime contracts"
      ]
    },
    {
      "requirement": "Frontmatter source",
      "name": "Plugins provide structured values instead of prebuilt YAML",
      "when": ["an agent plugin prepares install-time frontmatter input for @specd/skills"],
      "then": [
        "it passes a structured value collection",
        "AND it does not pass a prebuilt YAML frontmatter document"
      ]
    },
    {
      "requirement": "Frontmatter injection",
      "name": "Injection filters by target runtime support",
      "when": ["an agent plugin installs skills"],
      "then": [
        "the final rendered markdown includes only fields supported by that runtime",
        "AND unsupported fields are excluded from emitted markdown files"
      ]
    },
    {
      "requirement": "Frontmatter injection",
      "name": "Shared files do not receive runtime frontmatter",
      "given": ["a resolved bundle includes files marked as shared"],
      "when": ["@specd/skills renders the install output"],
      "then": ["shared files are emitted without runtime skill frontmatter"]
    },
    {
      "requirement": "Agent frontmatter matrix",
      "name": "Matrix coverage includes all known runtime fields",
      "when": ["frontmatter models are validated across runtimes"],
      "then": [
        "Codex coverage includes name and description",
        "AND Copilot coverage includes name, description, license, allowed-tools, user-invocable, and disable-model-invocation",
        "AND Open Code coverage includes name, description, license, compatibility, and metadata"
      ]
    },
    {
      "requirement": "Why no frontmatter in skills package",
      "name": "Runtime-specific metadata stays value-driven",
      "when": ["shared skill templates and installed skill outputs are compared"],
      "then": [
        "templates remain free of static runtime-specific frontmatter blocks",
        "AND runtime-specific values come from the target agent plugin",
        "AND the final frontmatter block is composed during skills rendering"
      ]
    },
    {
      "requirement": "Implementation tracking instructions in templates",
      "name": "Templates mention add plus review-state resolution before archive",
      "when": ["implementation-oriented and archive-oriented workflow templates are inspected"],
      "then": [
        "they mention specd changes implementation add",
        "AND they mention resolving or ignoring tracked implementation files before archive"
      ]
    },
    {
      "requirement": "Metadata self-healing guidance in workflow templates",
      "name": "Templates do not instruct scanning for metadata-status values",
      "when": [
        "archive-oriented, commit-oriented, or other metadata-oriented workflow skill templates are inspected"
      ],
      "then": [
        "they do not instruct agents to scan for metadata-status values (stale, missing, invalid) as a normal workflow step"
      ]
    },
    {
      "requirement": "Metadata self-healing guidance in workflow templates",
      "name": "generate-metadata is presented only as an explicit forced-rebuild tool",
      "when": ["a workflow skill template mentions specd spec generate-metadata"],
      "then": [
        "it presents the command as an explicit forced-rebuild, cache-warming, repair, or diagnostic tool",
        "AND it does not present the command as a required step after a routine spec or lock change"
      ]
    },
    {
      "requirement": "Optimizer agent gating declared in templates",
      "name": "Optimizer agent templates declare the llmOptimizedContext gate",
      "when": [
        "the specd-project-context-optimizer and specd-spec-context-optimizer agent templates are inspected"
      ],
      "then": [
        "each declares that it performs no generation or persistence unless the effective llmOptimizedContext project setting is true"
      ]
    },
    {
      "requirement": "Optimizer agent gating declared in templates",
      "name": "Optimizer agent templates direct persistence through lock-owned commands",
      "when": ["the optimizer agent templates describe how to persist generated content"],
      "then": [
        "they direct persistence through the lock-owned persisted-optimization commands (cli:spec-optimizations)",
        "AND neither template instructs invoking spec metadata generation after persisting an optimization"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "skills:skill-templates-source",
        "file": "skills:templates/skills/specd-archive/SKILL.md.tpl"
      }
    ]
  },
  "optimizedDescription": "Defines sourcing, metadata contracts, and capability-aware rendering for skill templates without static frontmatter across agent runtimes.",
  "optimizedContext": "# skills:skill-templates-source\n\n## Rules\n\n### Template source location\n- Source templates live in `packages/skills/templates/`:\n  - `packages/skills/templates/skills/<skill-name>/SKILL.md.tpl` (standard skills)\n  - `packages/skills/templates/agents/<agent-name>/SPECD-AGENT.md.tpl` (specialized agents)\n- All template sources MUST use `.md.tpl` extension; installed outputs emit as `.md`.\n- Directories MUST include metadata contract file (`skill.meta.json` or `specd-agent.meta.json`).\n\n### Template migration\n- Directory layout: `templates/skills/`, `templates/agents/`, `templates/shared/`.\n- `templates/skills/` MUST NOT contain `specd-metadata/` template; metadata optimization handled via agent templates.\n- `shared.meta.json` MUST NOT be canonical source of truth for shared template consumers.\n\n### Template metadata contract\n- Metadata files (`skill.meta.json` / `specd-agent.meta.json`) schema:\n```json\n{\n  \"kind\": \"skill\" | \"agent\",\n  \"supportedCapabilities\": [\"mcp\", \"agents\", \"frontmatter\"],\n  \"requiredCapabilities\": [],\n  \"requiredSharedTemplates\": [\"shared.md\"]\n}\n```\n- `kind` MUST match parent directory (`skills/` or `agents/`).\n\n### Capability-aware install-time rendering\n- Render model MUST be deterministic, single-pass Handlebars without arbitrary code execution.\n- Capabilities:\n  - `mcp`: enables MCP tool/workflow branches.\n  - `agents`: enables delegated agent/subagent branches.\n  - `frontmatter`: enables frontmatter insertion from `variables.frontmatter`.\n- Shared references MUST use `@{{sharedFolder}}/shared.md`; `sharedFolder` remains relative to project root.\n\n### Graph impact terminology in workflow templates\n- Impact terminology and flags:\n  - `dependents`: symbols/files depending on target (`--direction dependents`, `--direction upstream` compatibility).\n  - `dependencies`: symbols/files target depends on (`--direction dependencies`, `--direction downstream` compatibility).\n  - `both`: combined (`--direction both`).\n  - File selectors MUST use `--file <path1> <path2> ...`, NOT `--changes`.\n- Templates MUST NOT refer to `downstream` as dependents.\n\n### Graph search snippet guidance in workflow templates\n- `specd graph search` preview content MUST be opt-in via `--snippet`.\n- Omit `--snippet` when requesting identifiers/locations; `snippet` field omitted in `json`/`toon` without `--snippet`.\n\n### Frontmatter source & injection\n- Frontmatter derived from canonical skill metadata & vendor runtime contracts.\n- Plugins MUST pass structured data via `variables.frontmatter`, NEVER prebuilt YAML documents.\n- `@specd/skills` injects frontmatter ONLY if `frontmatter` capability present. Shared files NEVER receive runtime frontmatter.\n- Runtime model coverage:\n  - Codex: `name`, `description`\n  - Copilot: `name`, `description`, `license`, `allowed-tools`, `user-invocable`, `disable-model-invocation`\n  - Open Code: `name`, `description`, `license`, `compatibility`, `metadata`\n\n### Implementation tracking instructions in templates\n- Workflows linking code to specs MUST mention `specd changes implementation add`.\n- Archive workflows MUST resolve/review tracked implementation files before archive.\n\n### Metadata self-healing guidance in workflow templates\n- Workflow templates MUST NOT scan for status values (`stale`, `missing`, `invalid`).\n- `specd spec generate-metadata` MUST be presented strictly as explicit forced-rebuild/repair tool, never routine step.\n\n### Optimizer agent gating declared in templates\n- Agent templates (`specd-project-context-optimizer`, `specd-spec-context-optimizer`) MUST gate work on `llmOptimizedContext` == true.\n- Direct persistence strictly through `cli:spec-optimizations` commands; do NOT invoke metadata generation after persistence.\n\n## Constraints\n\n- Templates MUST NOT contain static frontmatter YAML blocks.\n- Source files MUST use `.md.tpl` extension.\n- Each skill dir MUST contain `skill.meta.json`.\n- `shared.meta.json` MUST NOT be canonical source for shared template requirements.\n- Installed markdown MUST NOT contain absolute filesystem paths.\n- Workflow templates MUST prefer `--direction dependents` / `--direction dependencies` and `--file`, NEVER `--changes`.\n",
  "contentHashes": {
    "spec.md": "sha256:6cd92df56307c64382acec8e5a57053fbbdc9b9a819a5729e911c7088ea30f4a",
    "verify.md": "sha256:dcb2f009f0a966c84ef20588435470a99ee7a4396b6804fe15ab71e05914b1b8"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:6cd92df56307c64382acec8e5a57053fbbdc9b9a819a5729e911c7088ea30f4a",
        "lastModified": "2026-07-27T07:22:53.603Z"
      },
      "verify.md": {
        "hash": "sha256:dcb2f009f0a966c84ef20588435470a99ee7a4396b6804fe15ab71e05914b1b8",
        "lastModified": "2026-07-27T07:22:53.605Z"
      }
    },
    "persistedStateHash": "sha256:502c1c87f6df97ac21fb72c43ae28080923e5a83e7b41b64250cbdacbdf335b7",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
