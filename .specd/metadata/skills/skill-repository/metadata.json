{
  "title": "skills:skill-repository",
  "description": "Defines the `SkillRepository` interface that provides access to skills and their bundles. This is the concrete implementation combining skill retrieval with bundle resolution.",
  "dependsOn": ["skills:skill", "skills:skill-bundle"],
  "context": [
    "Defines the `SkillRepository` interface that provides access to skills and their bundles. This is the concrete implementation combining skill retrieval with bundle resolution."
  ],
  "rules": [
    {
      "requirement": "list() method",
      "rules": [
        "The `list()` method MUST:\n\n- Return an array of all `Skill` objects (metadata only, no template content)\n- Return `readonly Skill[]`"
      ]
    },
    {
      "requirement": "get() method",
      "rules": [
        "The `get()` method MUST:\n\n- Accept a `name: string` parameter\n- Return `Skill | undefined` — the skill with that name, or undefined if not found"
      ]
    },
    {
      "requirement": "getBundle() method",
      "rules": [
        "The `getBundle()` method MUST:\n\n- Accept `name: string` and optional `variables: Record<string, string>` (defaults to `{}`)\n- Return `SkillBundle` with all template files resolved\n- Replace `{{key}}` placeholders in template content with values from the variables map\n- Variables are passed at invocation time — the repository does not define predefined variables"
      ]
    },
    {
      "requirement": "listSharedFiles() method",
      "rules": [
        "The `listSharedFiles()` method MUST:\n\n- Scan the `templates/shared/` directory for `.meta.json` files\n- Return `SharedFile[]` where each `SharedFile` has:\n  - `filename: string`\n  - `content: string`\n  - `skills: string[]` — which skills use this shared file"
      ]
    }
  ],
  "constraints": ["No I/O in the interface definition — implementations handle I/O."],
  "scenarios": [
    {
      "requirement": "list() method",
      "name": "List returns skills",
      "when": ["repository.list() is called"],
      "then": ["an array of Skill objects is returned"]
    },
    {
      "requirement": "get() method",
      "name": "Get existing skill",
      "when": ["repository.get('skill-name') is called for an existing skill"],
      "then": ["the Skill object is returned"]
    },
    {
      "requirement": "get() method",
      "name": "Get non-existing skill",
      "when": ["repository.get('nonexistent') is called"],
      "then": ["undefined is returned"]
    },
    {
      "requirement": "getBundle() method",
      "name": "Get bundle with variables",
      "when": ["repository.getBundle('skill-name', { key: 'value' }) is called"],
      "then": ["placeholders {{key}} are replaced with 'value'"]
    },
    {
      "requirement": "listSharedFiles() method",
      "name": "Lists shared files from templates/shared/",
      "when": ["repository.listSharedFiles() is called"],
      "then": [
        "it scans templates/shared/ for .meta.json files",
        "AND returns SharedFile[] with filename, content, and skills properties"
      ]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:e214cba5b4b441c00661737fb628d7432b62c04f5bf9f6739b17a34df4d53baf",
    "verify.md": "sha256:32b7fdc5a9d492b97102b21fad02186d101eba6f02f7cd182c2c726a4daaaf13"
  },
  "generatedBy": "core"
}
