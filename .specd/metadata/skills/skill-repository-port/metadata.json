{
  "title": "skills:skill-repository-port",
  "description": "Abstract interface for skill storage. This port defines the contract that infrastructure implementations must fulfill.",
  "dependsOn": ["core:core/config", "skills:skill", "skills:skill-bundle"],
  "context": [
    "Abstract interface for skill storage. This port defines the contract that infrastructure implementations must fulfill."
  ],
  "rules": [
    {
      "requirement": "SkillRepositoryPort interface",
      "rules": [
        "`SkillRepositoryPort` MUST define the same interface as `SkillRepository`:\n\n- `list(): Skill[]`\n- `get(name: string): Skill | undefined`\n- `getBundle(name: string, variables?: Record<string, string>, config?: SpecdConfig): SkillBundle`\n- `listSharedFiles(): SharedFile[]`\n\nThe `getBundle` method SHALL support receiving a `SpecdConfig` to enable the injection of built-in variables during template resolution."
      ]
    },
    {
      "requirement": "Port abstraction",
      "rules": [
        "The port is an abstract interface. Concrete implementations (e.g., using node:fs) live in the infrastructure layer."
      ]
    }
  ],
  "constraints": ["The port interface MUST NOT contain any I/O code."],
  "scenarios": [
    {
      "requirement": "Port interface",
      "name": "Has required methods",
      "when": ["SkillRepositoryPort is inspected"],
      "then": ["it has list(), get(), getBundle(), and listSharedFiles() methods"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:2f4d33d766295ee8e71815ceb423d4ae6da6908bf498dad78b9394162ee6010c",
    "verify.md": "sha256:eb53e6e475d8132548a430491907a66833446a18cb831b2763fcdc0a3fff3209"
  },
  "generatedBy": "core"
}
