{
  "title": "IndexProjectGraph",
  "description": "Hosts (`graph index`, SDK `runIndexProjectGraph`) currently assemble workspace targets, merge graph configuration, and invoke `provider.index()` inline. `IndexProjectGraph` extracts that orchestration so adapters handle only I/O formatting, locks, and subprocess isolation while the code-graph package owns index execution semantics.",
  "dependsOn": [
    "code-graph:composition",
    "code-graph:indexer",
    "code-graph:graph-store",
    "core:config"
  ],
  "context": [
    "Hosts (`graph index`, SDK `runIndexProjectGraph`) currently assemble workspace targets, merge graph configuration, and invoke `provider.index()` inline. `IndexProjectGraph` extracts that orchestration so adapters handle only I/O formatting, locks, and subprocess isolation while the code-graph package owns index execution semantics."
  ],
  "rules": [
    {
      "requirement": "Executes project indexing",
      "rules": [
        "`IndexProjectGraph.execute(input)` MUST call `provider.index()` with an `IndexOptions` object built from:\n\n- `projectRoot`\n- `workspaces`\n- `graphConfig`\n- `codeGraphVersion`\n- `vcsRoot`\n- optional `vcsRef`\n- optional `onProgress`\n\nand MUST return the resulting `IndexResult` unchanged."
      ]
    },
    {
      "requirement": "Supports force recreate",
      "rules": [
        "When `input.force` is true, `IndexProjectGraph` MUST pass that intent through the `IndexOptions` forwarded to `provider.index(...)`.\n\n`IndexProjectGraph` MUST NOT call `provider.recreate()` directly. The provider owns any destructive reset, storage-generation rotation, and lock policy required to honor force reindex semantics."
      ]
    },
    {
      "requirement": "Accepts open provider and prepared inputs",
      "rules": [
        "`IndexProjectGraphInput` MUST include:\n\n- `provider: CodeGraphProvider` (already opened)\n- `projectRoot: string`\n- `workspaces: WorkspaceIndexTarget[]`\n- `graphConfig: ProjectGraphConfig`\n- `codeGraphVersion: string`\n- `vcsRoot: string | null`\n- optional `vcsRef: string`\n- optional `force: boolean`\n- optional `onProgress: IndexProgressCallback`\n\n`vcsRoot` MUST be forwarded unchanged to the `IndexOptions` passed to `provider.index(...)`.\n\nThe use case MUST NOT resolve workspaces from `specd.yaml`, acquire locks, spawn subprocesses, or perform direct destructive reset calls against the provider or store."
      ]
    },
    {
      "requirement": "Factory wires dependencies",
      "rules": [
        "`createIndexProjectGraph()` in composition MUST return a stateless `IndexProjectGraph` instance."
      ]
    }
  ],
  "constraints": [
    "MUST NOT open or close the provider.",
    "MUST NOT acquire or release the graph indexing lock.",
    "Per-file index errors are reported in IndexResult.errors; the use case MUST NOT throw for parse failures."
  ],
  "scenarios": [
    {
      "requirement": "Executes project indexing",
      "name": "Incremental index",
      "given": ["an opened provider and prepared IndexProjectGraphInput"],
      "when": ["IndexProjectGraph.execute() runs with force: false"],
      "then": [
        "provider.index() is called once with merged options",
        "AND the returned IndexResult is passed through unchanged"
      ]
    },
    {
      "requirement": "Executes project indexing",
      "name": "Progress callback forwarded",
      "given": ["an onProgress callback in input"],
      "when": ["indexing runs"],
      "then": ["the callback is passed to provider.index()"]
    },
    {
      "requirement": "Supports force recreate",
      "name": "Force rebuild is forwarded to the provider",
      "given": ["force: true"],
      "when": ["IndexProjectGraph.execute() runs"],
      "then": [
        "provider.index() receives force: true",
        "AND IndexProjectGraph does not call provider.recreate() directly"
      ]
    },
    {
      "requirement": "Supports force recreate",
      "name": "Non-forced indexing does not request recreation",
      "given": ["force is omitted or false"],
      "when": ["IndexProjectGraph.execute() runs"],
      "then": [
        "provider.index() receives the non-forced options",
        "AND IndexProjectGraph does not call provider.recreate() directly"
      ]
    },
    {
      "requirement": "Accepts open provider and prepared inputs",
      "name": "Does not resolve workspaces or acquire lock",
      "given": ["a mock provider"],
      "when": ["IndexProjectGraph.execute() runs"],
      "then": ["it does not call lock helpers or read specd.yaml"]
    },
    {
      "requirement": "Accepts open provider and prepared inputs",
      "name": "VCS root is forwarded to provider indexing",
      "given": ["an IndexProjectGraphInput containing vcsRoot"],
      "when": ["IndexProjectGraph.execute() runs"],
      "then": ["provider.index() receives the same vcsRoot value"]
    },
    {
      "requirement": "Factory wires dependencies",
      "name": "Factory returns stateless instance",
      "when": ["createIndexProjectGraph() is called"],
      "then": ["it returns an IndexProjectGraph with no captured config"]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "code-graph:index-project-graph",
        "file": "code-graph:test/application/use-cases/index-project-graph.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "code-graph:index-project-graph",
        "file": "code-graph:src/application/use-cases/index-project-graph.ts",
        "symbol": "IndexProjectGraph"
      },
      {
        "specId": "code-graph:index-project-graph",
        "file": "code-graph:src/application/use-cases/index-project-graph.ts",
        "symbol": "IndexProjectGraphInput"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:eae0aaa60890c772fa2726aba22d015d5d73709c7ed67c76ed0b5f95d2739c18",
    "verify.md": "sha256:837011840b49247e061ac806d45691266056cd45313ac5a48762e0840db8db9c"
  },
  "generatedBy": "core"
}
