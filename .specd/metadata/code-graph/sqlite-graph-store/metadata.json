{
  "title": "SQLite Graph Store",
  "description": "`GraphStore` is the storage contract for the code graph, but SQLite has backend-specific\nbehavior that should not leak into the abstract port spec. This spec defines the\nrequirements that are specific to `SQLiteGraphStore`: its physical schema, persistence\nlayout, full-text search behavior, transaction model, and operational role as the\nbuilt-in default graph-store backend once it satisfies the full Ladybug feature set.",
  "dependsOn": [
    "code-graph:graph-store",
    "core:config",
    "code-graph:symbol-model",
    "code-graph:workspace-integration"
  ],
  "context": [
    "`GraphStore` is the storage contract for the code graph, but SQLite has backend-specific\nbehavior that should not leak into the abstract port spec. This spec defines the\nrequirements that are specific to `SQLiteGraphStore`: its physical schema, persistence\nlayout, full-text search behavior, transaction model, and operational role as the\nbuilt-in default graph-store backend once it satisfies the full Ladybug feature set."
  ],
  "rules": [
    {
      "requirement": "SQLite-backed implementation",
      "rules": [
        "`SQLiteGraphStore` SHALL implement the `GraphStore` contract using SQLite as the\nstorage engine. It is an infrastructure adapter and owns all backend-specific concerns\nthat are not part of the abstract `GraphStore` port.\n\nWithin a composition that supports multiple registered graph-store backends, the\nSQLite adapter is identified by the backend id `sqlite`.\n\nThe adapter MUST:\n\n- open and close a SQLite database connection safely\n- execute the SQLite-specific schema DDL on first use\n- keep backend-specific schema version state\n- translate the abstract graph-store operations into SQLite queries, transactions, and\n  full-text search operations"
      ]
    },
    {
      "requirement": "Config-derived persistence layout",
      "rules": [
        "`SQLiteGraphStore` SHALL use the project-level `configPath` as the root for its\npersisted and temporary filesystem artifacts.\n\nThe adapter MUST derive:\n\n- a graph persistence directory at `{configPath}/graph`\n- a temporary scratch directory at `{configPath}/tmp`\n\nThe concrete SQLite database file and any backend-owned companion artifacts such as\nWAL or shared-memory files SHALL live only under those derived directories. The adapter\nMUST create the required directories on demand."
      ]
    },
    {
      "requirement": "Default backend role",
      "rules": [
        "The built-in code-graph composition SHALL treat `sqlite` as the default backend id\nwhen no explicit `graphStoreId` is selected.\n\nThe SQLite backend therefore MUST preserve the full set of currently supported graph\nbehaviors that Ladybug previously backed, including:\n\n- durable persistence of file, symbol, spec, relation, and metadata state\n- atomic mutation semantics required by the abstract `GraphStore` contract\n- full-text search for symbols and specs\n- bulk indexing and full re-index operations\n- query support for traversal-, impact-, hotspot-, search-, and stats-facing flows"
      ]
    },
    {
      "requirement": "Destructive recreation",
      "rules": [
        "`SQLiteGraphStore` SHALL implement the abstract `GraphStore.recreate()` capability\nusing SQLite-specific persistence cleanup under the configured graph root.\n\nWhen `recreate()` is invoked, the adapter MUST ensure that the next indexing run starts\nfrom an empty SQLite backend without requiring callers to know any SQLite-specific\nfilenames. The implementation MAY close and reopen connections as needed, but the\nobservable effect MUST be that:\n\n- all persisted SQLite graph data under `{configPath}/graph` is discarded\n- any backend-owned companion artifacts such as WAL or shared-memory files are also\n  discarded when they belong to the same graph persistence root\n- the backend is ready to be opened again and rebuilt from scratch"
      ]
    },
    {
      "requirement": "SQLite schema ownership",
      "rules": [
        "`SQLiteGraphStore` SHALL own the SQLite-specific persisted schema for files, symbols,\nspecs, relations, full-text indexes, and store metadata.\n\nThat schema MUST define:\n\n- persisted records for `File`, `Symbol`, `Spec`, and `Meta`\n- persisted relation storage matching the `RelationType` values used by the code graph,\n  including `EXTENDS`, `IMPLEMENTS`, and `OVERRIDES`\n- any derived or backend-specific storage columns required by the implementation, such\n  as normalized search text\n\nThe physical schema is an implementation concern of the SQLite adapter. Storage-agnostic\nconsumers MUST depend on `code-graph:graph-store` instead of this spec."
      ]
    },
    {
      "requirement": "Persisted node storage",
      "rules": [
        "The SQLite schema SHALL persist the logical node kinds required by the abstract graph model:\n\n- `File` nodes for indexed source files, including persisted source content used for symbol snippet extraction\n- `Symbol` nodes for extracted code symbols\n- `Spec` nodes for indexed specification documents\n- **`Document`** nodes for textual non-code resources\n- `Meta` records for store-level metadata\n\nThe `File` table SHALL include the source content needed to derive symbol snippets from file-backed context.\n\nThe `Document` table SHALL include columns for `path` (PK), `configRelativePath`, `contentHash`, `content`, and `workspace`."
      ]
    },
    {
      "requirement": "Persisted relation storage",
      "rules": [
        "The SQLite schema SHALL persist the relation families required by the abstract graph model:\n\n- `IMPORTS`\n- `DEFINES`\n- `CALLS`\n- `EXPORTS`\n- `DEPENDS_ON`\n- `COVERS_FILE`\n- `COVERS_SYMBOL`\n- `EXTENDS`\n- `IMPLEMENTS`\n- `OVERRIDES`\n\nThe adapter MAY represent relation storage with a single relation table, per-type tables, or another SQLite-appropriate layout, provided the observable `GraphStore` query semantics remain preserved.\n\n`COVERS_SYMBOL` entries MUST preserve relation metadata so symbol-level implementation links can surface `stale` state consistently after reload."
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "rules": [
        "`SQLiteGraphStore` SHALL implement symbol, spec, and document search using SQLite full-text search (FTS5).\n\nThe adapter MUST:\n\n- provide full-text search over `Document` content and paths\n- keep SQLite FTS candidate generation in place for `searchSymbols()`, `searchSpecs()`, and `searchDocuments()` using the existing `MATCH` query path\n- sanitize and join multi-token queries using `OR` logic for broad discovery\n- expand raw query tokens with the shared specd/code-aware lexical policy before applying identity-aware ranking\n- supplement the FTS candidate set with identity-derived candidates when FTS tokenization alone would miss a strong identity hit required by the abstract contract\n- compute identity-aware ranking in SQL with explicit ordering columns rather than relying on BM25 weights alone\n- prioritize **exact canonical identity matches** (Spec ID, Symbol Name/ID, Document Path) by boosting results where the query matches the primary identity column exactly\n- prioritize **strong non-exact identity matches** ahead of generic content-only matches, including:\n  - symbol declared-name equality when comment/body-only hits would otherwise rank higher\n  - spec-id prefix, suffix, substring, and real component matches\n  - document canonical-path or config-relative-path prefix, suffix, substring, and real component matches\n- count how many expanded query tokens match the selected identity fields and use that token coverage to rank candidates that satisfy more of the query intent above candidates satisfying fewer identity tokens\n- use BM25 ranking for remaining textual matches across searchable columns after identity preference is applied\n- sanitize and join multi-token queries using `OR` logic for broad discovery\n- derive match-aware snippets and the corresponding 1-based line range from persisted file source content or FTS matches\n\nObservable SQLite ordering semantics MUST hold:\n\n- exact canonical identity matches rank first\n- identity-oriented non-exact hits rank ahead of body-only/comment-only/content-only hits\n- exact token identity matches outrank prefix token matches\n- prefix token matches outrank suffix token matches\n- suffix token matches outrank arbitrary substring token matches\n- real identity-component matches outrank arbitrary substring-only hits on the same identity field\n- candidates matching more expanded identity tokens outrank candidates matching fewer expanded identity tokens when generic text relevance is otherwise competing\n- generic term frequency in spec/document body content MUST NOT outrank a stronger spec-id, symbol-name, or document-path match for the same query intent\n\nThe SQLite FTS schema MUST include:\n\n- **`symbol_fts`** virtual table covering `Symbol.name` and `Symbol.comment`\n- **`spec_fts`** virtual table covering `Spec.title`, `Spec.description`, and `Spec.content`\n- **`document_fts`** virtual table covering `Document.path` and `Document.content`\n\nThe implementation MAY use stemming, weighted BM25, identity boost unions, or other SQLite-supported ranking/indexing options, provided the abstract graph-store contract remains satisfied.\n\nSQLite identity-aware ranking MUST be expressed through explicit SQL ordering logic over the discovered candidate set, such as computed rank columns or `CASE`-based ordering. Candidate discovery MAY combine FTS `MATCH` results with identity-derived candidates, but it MUST NOT replace FTS retrieval with plain whole-query `LIKE` filtering only.\n\nPersisted `File` content used for snippet extraction SHALL NOT, by itself, become a separate full-text searchable file category in this change."
      ]
    },
    {
      "requirement": "Transactional mutation model",
      "rules": [
        "`SQLiteGraphStore` SHALL use SQLite transactions to preserve the atomic mutation\nsemantics required by the abstract `GraphStore` contract.\n\nAt minimum:\n\n- `upsertFile()` MUST replace file-local graph state atomically\n- `removeFile()` MUST remove file-local graph state atomically\n- `upsertSpec()` MUST replace spec-local graph state atomically\n- `removeSpec()` MUST remove spec-local graph state atomically\n- bulk indexing operations MUST commit all-or-nothing backend state for the batch they\n  claim to have persisted\n\nIf a transaction fails, the backend MUST leave previously committed graph state intact."
      ]
    },
    {
      "requirement": "Bulk indexing support",
      "rules": [
        "`SQLiteGraphStore` SHALL support efficient bulk indexing for large repositories.\n\nThe adapter MAY use batching, prepared statements, temporary tables, or other\nSQLite-appropriate techniques to keep large indexing runs stable, provided the\nobservable `GraphStore` contract is preserved.\n\nIf the implementation materializes backend-owned scratch artifacts outside the primary\ndatabase file, those artifacts MUST be scoped to `{configPath}/tmp` and cleaned up\nafter successful completion."
      ]
    },
    {
      "requirement": "Schema versioning",
      "rules": [
        "`SQLiteGraphStore` SHALL track a backend-specific schema version for its persisted\nSQLite schema.\n\nWhen the adapter opens a database:\n\n1. it executes the current SQLite schema definition\n2. it verifies or records the current schema version in persisted metadata\n3. it prepares any required FTS structures\n\nIf the persisted SQLite schema version is incompatible with the expected version, the\nimplementation MAY require a destructive rebuild rather than attempting incremental\nmigration."
      ]
    },
    {
      "requirement": "Backend-specific companion files",
      "rules": [
        "The SQLite-backed graph persistence under `{configPath}/graph` MAY create backend-specific\ncompanion files such as WAL or shared-memory files next to the primary database file.\n\nThose files are part of the SQLite implementation detail and are not part of the\nabstract `GraphStore` contract."
      ]
    }
  ],
  "constraints": [
    "SQLiteGraphStore is an infrastructure adapter, not part of the abstract\ngraph-store contract",
    "sqlite is the stable backend id used to select this adapter from a multi-backend\ngraph-store registry",
    "SQLite-specific file layout, FTS behavior, schema shape, and schema-version handling\nare defined here, not in code-graph:graph-store",
    "All SQLite scratch files and persisted database artifacts are rooted under\nconfigPath",
    "Storage-agnostic use cases and CLI commands MUST NOT depend on this spec unless they\ntruly require SQLite-specific behavior"
  ],
  "scenarios": [
    {
      "requirement": "SQLite-backed implementation",
      "name": "Backend initialization stays inside the adapter",
      "when": ["SQLiteGraphStore.open() is called through the abstract GraphStore"],
      "then": [
        "the adapter initializes its SQLite-specific connection and schema state",
        "AND storage-agnostic callers do not need to know any SQLite DDL or query details"
      ]
    },
    {
      "requirement": "Config-derived persistence layout",
      "name": "Graph and tmp directories are derived from configPath",
      "given": ["project config resolves configPath to /repo/.specd/config"],
      "when": ["SQLiteGraphStore.open() or a backend-owned indexing operation needs"],
      "then": [
        "persistent SQLite files are created only under /repo/.specd/config/graph",
        "AND scratch artifacts are created only under /repo/.specd/config/tmp"
      ]
    },
    {
      "requirement": "Default backend role",
      "name": "SQLite is the built-in default backend id",
      "given": ["the built-in graph-store registry contains both sqlite and ladybug"],
      "when": ["no explicit graphStoreId is supplied during provider or kernel"],
      "then": ["the backend id sqlite is selected"]
    },
    {
      "requirement": "Default backend role",
      "name": "Default backend preserves Ladybug-era capabilities",
      "when": ["graph indexing, search, stats, traversal, impact, and hotspot flows run"],
      "then": ["they succeed through SQLiteGraphStore without requiring a fallback to"]
    },
    {
      "requirement": "Destructive recreation",
      "name": "Recreate discards SQLite-owned graph files under the graph root",
      "given": ["SQLite persistence already exists under {configPath}/graph"],
      "when": ["SQLiteGraphStore.recreate() is invoked through the abstract force-reset"],
      "then": [
        "the previously persisted SQLite graph state is discarded",
        "AND any SQLite-owned companion artifacts in the same graph root are discarded\nwith it",
        "AND callers do not target SQLite filenames directly"
      ]
    },
    {
      "requirement": "SQLite schema ownership",
      "name": "Physical schema remains backend-specific",
      "when": ["storage-agnostic use cases depend on GraphStore"],
      "then": [
        "they rely only on abstract node and relation semantics",
        "AND SQLite table names, virtual tables, indexes, and storage columns remain\ninternal to SQLiteGraphStore"
      ]
    },
    {
      "requirement": "Persisted node storage",
      "name": "Logical node kinds survive backend-specific layout choices",
      "when": ["files, symbols, specs, and metadata are persisted by SQLiteGraphStore"],
      "then": [
        "the abstract graph-store queries can retrieve the expected logical node kinds",
        "AND callers do not need to know whether SQLite uses one table per kind or another internal layout"
      ]
    },
    {
      "requirement": "Persisted node storage",
      "name": "File node persistence includes source content for symbol snippets",
      "when": ["a source file is persisted by SQLiteGraphStore"],
      "then": [
        "the persisted file record includes source content sufficient to derive symbol snippets from file-backed context",
        "AND symbol preview extraction does not require a separate persisted snippet field per symbol"
      ]
    },
    {
      "requirement": "Persisted relation storage",
      "name": "All required relation families are stored",
      "when": ["the SQLite schema is initialized for a fresh graph database"],
      "then": ["persisted storage exists for IMPORTS, DEFINES, CALLS, EXPORTS,"]
    },
    {
      "requirement": "Persisted relation storage",
      "name": "COVERS_SYMBOL metadata survives SQLite persistence",
      "given": ["a persisted COVERS_SYMBOL relation with metadata { \"stale\": true }"],
      "when": ["the relation is loaded through abstract graph-store queries"],
      "then": ["the metadata still marks the relation as stale"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Symbol and spec search use SQLite full-text search",
      "given": ["symbols and specs have been indexed into SQLite"],
      "when": ["abstract search methods are called"],
      "then": [
        "results come back in descending relevance order from SQLite-backed full-text search structures"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Multi-token search uses OR logic for discovery",
      "given": ["symbols \"effectiveStatus\" and \"findBlockingParent\" exist in different files"],
      "when": ["searchSymbols({ query: 'effectiveStatus findBlockingParent' }) is called"],
      "then": [
        "both symbols are returned in the results",
        "AND the FTS5 MATCH clause uses the OR operator between tokens"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "BM25 ranking prioritizes multiple matches for precision",
      "given": ["symbol A contains \"status\", symbol B contains \"effective status\""],
      "when": ["searchSymbols({ query: 'effective status' }) is called"],
      "then": [
        "symbol B has a higher relevance score than symbol A",
        "AND symbol B appears first in the results"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Symbol result derives snippet from file content even for comment-driven hit",
      "given": ["a symbol search hit is returned because of matched comment text"],
      "when": ["searchSymbols(...) returns the symbol"],
      "then": [
        "the result snippet is derived from persisted file source content at the symbol location"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "FTS structures can be refreshed after bulk writes",
      "given": ["the backend has inserted symbols and specs through bulk-loading operations"],
      "when": ["rebuildFtsIndexes() is invoked"],
      "then": ["subsequent abstract search queries see the newly indexed data"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Queries with hyphens do not crash FTS5",
      "given": ["symbols and specs have been indexed into SQLite"],
      "when": ["searchSymbols({ query: 'pending-parent-artifact-review' }) is called"],
      "then": [
        "no SqliteError is thrown",
        "AND results matching the literal search term are returned"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Queries with FTS5 operators are treated as literal text",
      "given": ["symbols and specs have been indexed into SQLite"],
      "when": ["searchSpecs({ query: 'AND OR NOT' }) is called"],
      "then": [
        "the query does not perform boolean logic",
        "AND results matching the literal terms are returned"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Exact identity matches boosted in SQLite FTS",
      "given": ["a spec with ID core:change"],
      "when": ["searching for core:change in the SQLite backend"],
      "then": ["that spec is the first result returned"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Persisted file content does not create SQLite file search category",
      "given": ["SQLite persists file source content for snippet extraction"],
      "when": ["search APIs are used through the current graph-store contract"],
      "then": [
        "there is still no separate file full-text result category introduced by this change"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Spec-id segment outranks content-only hit in SQLite",
      "given": [
        "a spec with ID default:_global/architecture",
        "AND another spec contains architecture more times only in its body content"
      ],
      "when": ["searchSpecs({ query: 'architecture' }) is called on the SQLite backend"],
      "then": ["default:_global/architecture is ranked ahead of the body-only hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Symbol declared name outranks comment-only hit in SQLite",
      "given": [
        "one symbol is named SearchSpecs",
        "AND another symbol contains search specs only in comment text"
      ],
      "when": ["searchSymbols({ query: 'SearchSpecs' }) is called on the SQLite backend"],
      "then": ["the declared-name hit is ranked ahead of the comment-only hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "Config-relative document path participates in identity ranking",
      "given": [
        "a document whose canonical path or configRelativePath contains docs/cli/graph-search.md",
        "AND another document mentions graph search only in body text"
      ],
      "when": ["searchDocuments({ query: 'graph-search' }) is called on the SQLite backend"],
      "then": ["the document-path hit is ranked ahead of the body-only hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite expands specd-shaped tokens before identity ranking",
      "given": [
        "a spec with ID core:change",
        "AND another spec mentions core change only in body content"
      ],
      "when": ["searchSpecs({ query: 'core:change' }) is called on the SQLite backend"],
      "then": [
        "the backend treats core:change, core, and change as usable identity-ranking tokens",
        "AND the spec-id hit is ranked ahead of the body-only hit"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite expands CamelCase tokens before identity ranking",
      "given": [
        "a symbol named ArchiveChange",
        "AND another symbol contains archive change only in comment text"
      ],
      "when": ["searchSymbols({ query: 'ArchiveChange' }) is called on the SQLite backend"],
      "then": [
        "the backend treats archivechange, archive, and change as usable identity-ranking tokens",
        "AND the declared-name hit is ranked ahead of the comment-only hit"
      ]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite exact token match outranks prefix token match",
      "given": [
        "one candidate identity matches token change exactly",
        "AND another candidate identity matches change only by prefix"
      ],
      "when": ["searchSymbols({ query: 'change' }) is called on the SQLite backend"],
      "then": ["the exact-token hit is ranked ahead of the prefix-only hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite prefix token match outranks suffix token match",
      "given": [
        "one candidate identity matches token repo by prefix",
        "AND another candidate identity matches repo only by suffix"
      ],
      "when": ["searchSymbols({ query: 'repo' }) is called on the SQLite backend"],
      "then": ["the prefix-token hit is ranked ahead of the suffix-only hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite suffix token match outranks arbitrary substring token match",
      "given": [
        "one candidate identity matches token repository by suffix",
        "AND another candidate identity matches repository only as an arbitrary substring"
      ],
      "when": ["searchDocuments({ query: 'repository' }) is called on the SQLite backend"],
      "then": ["the suffix-token hit is ranked ahead of the arbitrary-substring hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite real component match outranks arbitrary substring match",
      "given": [
        "one spec ID is core:change",
        "AND another spec ID contains substring core only inside a larger token such as score"
      ],
      "when": ["searchSpecs({ query: 'core' }) is called on the SQLite backend"],
      "then": ["the real component match is ranked ahead of the arbitrary-substring hit"]
    },
    {
      "requirement": "SQLite full-text search",
      "name": "SQLite supplements FTS discovery for a strong suffix identity hit",
      "given": [
        "one symbol identity matches token change by suffix",
        "AND SQLite FTS tokenization alone would not surface that symbol through the default MATCH candidate set"
      ],
      "when": ["searchSymbols({ query: 'change' }) is called on the SQLite backend"],
      "then": [
        "the backend still returns that symbol as a candidate",
        "AND it is ordered by the same identity-strength ladder instead of being omitted for lack of FTS coverage"
      ]
    },
    {
      "requirement": "Transactional mutation model",
      "name": "File upsert is all-or-nothing",
      "given": ["a file already has persisted graph state in SQLite"],
      "when": ["upsertFile() fails during its transaction"],
      "then": ["the previous committed state for that file remains intact"]
    },
    {
      "requirement": "Transactional mutation model",
      "name": "Bulk indexing batch is all-or-nothing",
      "given": ["a bulk indexing operation is persisting a batch of graph data"],
      "when": ["the SQLite transaction for that batch fails"],
      "then": ["the backend does not expose a partially committed batch"]
    },
    {
      "requirement": "Bulk indexing support",
      "name": "Large indexing runs use backend-specific batching safely",
      "when": ["a large repository is indexed through SQLiteGraphStore"],
      "then": [
        "the backend may batch writes, use prepared statements, or use temporary",
        "AND the observable GraphStore contract remains unchanged"
      ]
    },
    {
      "requirement": "Schema versioning",
      "name": "Incompatible schema version permits rebuild strategy",
      "given": ["the persisted SQLite metadata records a schema version older than the"],
      "when": ["the adapter opens the database and determines it cannot migrate safely"],
      "then": ["it may require a destructive rebuild instead of applying an incremental"]
    },
    {
      "requirement": "Backend-specific companion files",
      "name": "SQLite companion files stay under graph persistence root",
      "when": ["SQLite creates a primary database file together with WAL or shared-memory"],
      "then": [
        "all of those files live under {configPath}/graph",
        "AND callers do not configure or address those files individually through the\nabstract port"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "code-graph:sqlite-graph-store",
        "file": "code-graph:test/infrastructure/sqlite/sqlite-graph-store.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "code-graph:sqlite-graph-store",
        "file": "code-graph:src/infrastructure/sqlite/sqlite-graph-store.ts",
        "symbol": "SQLiteGraphStore"
      },
      {
        "specId": "code-graph:sqlite-graph-store",
        "file": "code-graph:src/infrastructure/sqlite/sqlite-graph-store.ts",
        "symbol": "searchSymbols"
      },
      {
        "specId": "code-graph:sqlite-graph-store",
        "file": "code-graph:src/infrastructure/sqlite/sqlite-graph-store.ts",
        "symbol": "searchSpecs"
      },
      {
        "specId": "code-graph:sqlite-graph-store",
        "file": "code-graph:src/infrastructure/sqlite/sqlite-graph-store.ts",
        "symbol": "searchDocuments"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:3853b4bf3199b0c5863bafcf63901af9c93db158eaf20cd498d4d8f53561a53a",
    "verify.md": "sha256:bd15466914767f616661f101fe2303a3fedb8faa79b410b885bc3348d53e35bb"
  },
  "generatedBy": "core"
}
