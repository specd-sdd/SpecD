{
  "title": "SDK Build Project Status Snapshot",
  "description": "`project status --graph` and future API hosts need a single orchestration that combines core project summary data with code-graph health in one call. Today the CLI duplicates this wiring. `buildProjectStatusSnapshot` centralises the cross-package read path in `@specd/sdk`.",
  "dependsOn": ["sdk:host-context", "core:get-project-summary", "code-graph:get-graph-health"],
  "context": [
    "`project status --graph` and future API hosts need a single orchestration that combines core project summary data with code-graph health in one call. Today the CLI duplicates this wiring. `buildProjectStatusSnapshot` centralises the cross-package read path in `@specd/sdk`."
  ],
  "rules": [
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "rules": [
        "`buildProjectStatusSnapshot(ctx: SdkHostContext, options?: BuildProjectStatusSnapshotOptions): Promise<BuildProjectStatusSnapshotResult>` SHALL:\n\n1. Call `ctx.kernel.project.getProjectSummary.execute(summaryInput)` for workspace counts, change/draft/discarded/archive totals, and spec counts per workspace, where `summaryInput` is derived from snapshot options:\n   - `includeChanges` — forwarded when `options.includeChanges` is `true`\n   - `includeSpecsHealth` — forwarded when `options.includeSpecsHealth` is `true`\n   - When both are omitted/false, call `execute()` / `execute({})` so the summary stays count-only\n2. When `options.includeGraph` is `true` (default `false`), call `withOpenGraphProvider(ctx, async (provider) => { ... })`, construct `createGetGraphHealth()` from `@specd/code-graph`, and run `getGraphHealth.execute({ config, provider, codeGraphVersion, workspaces, assertUnlocked: false })` where `config` comes from `ctx.kernel.project.getConfig.execute()` and `workspaces` from `ctx.kernel.project.listWorkspaces.execute()`\n3. Return a merged result object containing summary fields plus optional `graphHealth` and `hotspots` when graph data was requested\n\nWhen graph loading fails (provider open, health query, or hotspots), the function MUST return `graphHealth: null` (and `hotspots: null` when requested) without throwing — hosts treat null as unavailable graph data.\n\nWhen `includeGraph` is `false`, the function MUST NOT open a graph provider."
      ]
    },
    {
      "requirement": "Result shape stability",
      "rules": [
        "`BuildProjectStatusSnapshotOptions` MUST accept:\n\n- `includeGraph?: boolean` (default `false`)\n- `includeHotspots?: boolean`\n- `includeChanges?: boolean` (default `false`) — forwarded to `GetProjectSummary`\n- `includeSpecsHealth?: boolean` (default `false`) — forwarded to `GetProjectSummary`\n\n`BuildProjectStatusSnapshotResult` MUST expose:\n\n- `summary` — the `GetProjectSummaryResult` from core (including optional `active` / `drafts` / `specsHealth` when requested)\n- `graphHealth` — `GetGraphHealthResult | null` (null when graph not requested or unavailable)\n- `approvals` — `{ specEnabled: boolean; signoffEnabled: boolean }` derived from `ctx.kernel.project.getConfig.execute()`\n- `llmOptimizedContext` — boolean from config\n\nHotspots MAY be included when `options.includeHotspots` is true and graph is loaded; otherwise omitted or null.\n\nThe snapshot MUST NOT duplicate enriched listing/health fields outside `summary`; hosts read them from `summary`."
      ]
    },
    {
      "requirement": "No presenter formatting",
      "rules": [
        "The function MUST return structured data only. Text/JSON/toon formatting remains in CLI presenters (change 12)."
      ]
    },
    {
      "requirement": "No direct repository bootstrap in snapshot orchestration",
      "rules": [
        "`buildProjectStatusSnapshot` MUST obtain project summary and workspace information exclusively through project queries exposed by `SdkHostContext`.\n\nThe orchestration MUST NOT construct `ChangeRepository` or `SpecRepository` instances directly, and it MUST NOT recreate a parallel or partial repository bootstrap path outside the canonical composition-backed read flow."
      ]
    }
  ],
  "scenarios": [
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "name": "Summary only without graph",
      "when": ["buildProjectStatusSnapshot(ctx, { includeGraph: false }) is called"],
      "then": [
        "kernel.project.getProjectSummary.execute() is invoked without requiring enrichment flags",
        "AND no graph provider is opened",
        "AND graphHealth is null"
      ]
    },
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "name": "Enrichment options forwarded to getProjectSummary",
      "when": [
        "buildProjectStatusSnapshot(ctx, { includeChanges: true, includeSpecsHealth: true }) is called"
      ],
      "then": [
        "getProjectSummary.execute is invoked with includeChanges: true and includeSpecsHealth: true"
      ]
    },
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "name": "Graph health included when requested",
      "when": ["buildProjectStatusSnapshot(ctx, { includeGraph: true }) is called"],
      "then": [
        "withOpenGraphProvider wraps a GetGraphHealth execution",
        "AND workspaces passed to graph health come from listWorkspaces"
      ]
    },
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "name": "Graph unavailable returns null without throwing",
      "when": [
        "buildProjectStatusSnapshot(ctx, { includeGraph: true }) is called and graph loading fails"
      ],
      "then": ["graphHealth is null", "AND the function resolves without throwing"]
    },
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "name": "Hotspots failure clears the whole graph snapshot",
      "when": [
        "buildProjectStatusSnapshot(ctx, { includeGraph: true, includeHotspots: true }) is called",
        "AND graph health loads successfully",
        "AND hotspot loading fails afterwards"
      ],
      "then": [
        "graphHealth is null",
        "AND hotspots is null",
        "AND the function resolves without throwing"
      ]
    },
    {
      "requirement": "buildProjectStatusSnapshot orchestration",
      "name": "Hotspots included when requested",
      "when": [
        "buildProjectStatusSnapshot(ctx, { includeGraph: true, includeHotspots: true }) is called"
      ],
      "then": ["hotspots is populated from provider.getHotspots() when available"]
    },
    {
      "requirement": "Result shape stability",
      "name": "Approvals derived from getConfig",
      "given": ["config has approvals.spec: true"],
      "when": ["snapshot is built"],
      "then": ["approvals.specEnabled is true"]
    },
    {
      "requirement": "Result shape stability",
      "name": "llmOptimizedContext forwarded",
      "given": ["config has llmOptimizedContext: true"],
      "when": ["snapshot is built"],
      "then": ["result llmOptimizedContext is true"]
    },
    {
      "requirement": "Result shape stability",
      "name": "Enriched fields live only under summary",
      "when": ["snapshot is built with includeChanges and includeSpecsHealth"],
      "then": [
        "summary.active, summary.drafts, and summary.specsHealth are present when the use case returns them",
        "AND the snapshot root does not duplicate those fields outside summary"
      ]
    },
    {
      "requirement": "No presenter formatting",
      "name": "Returns structured object",
      "when": ["snapshot completes"],
      "then": [
        "the result is a plain object with summary, graphHealth, approvals, and llmOptimizedContext fields",
        "AND no formatted text output is produced"
      ]
    },
    {
      "requirement": "No direct repository bootstrap in snapshot orchestration",
      "name": "Snapshot orchestration relies only on SDK host context project queries",
      "when": ["buildProjectStatusSnapshot assembles summary and workspace information"],
      "then": [
        "it obtains them exclusively through SdkHostContext project queries",
        "AND it does not construct ChangeRepository or SpecRepository instances directly",
        "AND it does not recreate a parallel or partial repository bootstrap path outside the canonical composition-backed read flow"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "sdk:build-project-status-snapshot",
        "file": "sdk:src/orchestration/build-project-status-snapshot.ts",
        "symbol": "BuildProjectStatusSnapshotOptions"
      },
      {
        "specId": "sdk:build-project-status-snapshot",
        "file": "sdk:src/orchestration/build-project-status-snapshot.ts",
        "symbol": "buildProjectStatusSnapshot"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:85d1392c2a2f599ed6443122ad4017cd203ef15f1b4a78ccfdfeb63a53ab074c",
    "verify.md": "sha256:73ab2f77ff20c6eaf798f87a91adea7febb6ef441b1f8c120259a8b7ead141aa"
  },
  "provenance": {
    "artifacts": {
      "spec.md": {
        "hash": "sha256:85d1392c2a2f599ed6443122ad4017cd203ef15f1b4a78ccfdfeb63a53ab074c",
        "lastModified": "2026-07-26T11:52:20.607Z"
      },
      "verify.md": {
        "hash": "sha256:73ab2f77ff20c6eaf798f87a91adea7febb6ef441b1f8c120259a8b7ead141aa",
        "lastModified": "2026-07-26T11:52:20.607Z"
      }
    },
    "persistedStateHash": "sha256:f613e131f77f688f5239e7469729f226bb0d1ae15fbc5a01b3e59ee2b88380c1",
    "schema": {
      "name": "schema-std",
      "version": 1
    },
    "projectionVersion": 1,
    "projectionFingerprint": "sha256:e4a28a2db6a35ed39b8308518337235b1490c2948cd839db25866ad79b22404b"
  }
}
