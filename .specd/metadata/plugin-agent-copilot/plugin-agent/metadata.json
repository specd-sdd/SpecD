{
  "title": "plugin-agent-copilot:plugin-agent",
  "description": "This spec defines the Copilot plugin contract for concrete install/uninstall behavior, project install location, and exact frontmatter support so agent skills are usable directly in Copilot workflows.",
  "dependsOn": ["plugin-manager:agent-plugin-type", "skills:skill-templates-source"],
  "context": [
    "This spec defines the Copilot plugin contract for concrete install/uninstall behavior, project install location, and exact frontmatter support so agent skills are usable directly in Copilot workflows."
  ],
  "rules": [
    {
      "requirement": "Factory export",
      "rules": ["The package MUST export `create(): AgentPlugin` as default or named export."]
    },
    {
      "requirement": "Plugin runtime contract",
      "rules": [
        "The implementation MUST return a valid `AgentPlugin` that:\n\n- has `type: 'agent'`\n- has a concrete package name and version\n- implements `install(projectRoot, options)` and `uninstall(projectRoot, options)` with real filesystem behavior\n- uses a stable frontmatter model for generated skill files"
      ]
    },
    {
      "requirement": "Skill installation and frontmatter injection",
      "rules": [
        "The plugin MUST install skills from `@specd/skills` and inject Copilot-compatible frontmatter into markdown skill files during install.\n\nInstallation flow MUST:\n\n1. load requested skills (or all skills when no filter is provided)\n2. resolve frontmatter for each skill\n3. prepend frontmatter to markdown files in the installed skill directories\n4. return an `InstallResult` with installed and skipped entries"
      ]
    },
    {
      "requirement": "Frontmatter field contract",
      "rules": [
        "The Copilot frontmatter contract MUST model and emit this exact supported set:\n\n- required: `name`, `description`\n- optional core field: `license`\n- optional CLI-supported fields: `allowed-tools`, `user-invocable`, `disable-model-invocation`\n\nFields outside this set MUST NOT be emitted by default."
      ]
    },
    {
      "requirement": "Install location",
      "rules": [
        "Project-level skill installation MUST target `.github/skills/` under the provided project root."
      ]
    }
  ],
  "constraints": [
    "The plugin MUST depend on @specd/skills for skill operations.",
    "The plugin MUST implement concrete install and uninstall behavior."
  ],
  "scenarios": [
    {
      "requirement": "Factory export",
      "name": "Exposes plugin factory",
      "when": ["plugin package exports are loaded"],
      "then": ["create() is available and returns an AgentPlugin"]
    },
    {
      "requirement": "Plugin runtime contract",
      "name": "Created plugin satisfies runtime contract",
      "when": ["create() is called"],
      "then": [
        "the returned plugin has type: 'agent', package name, and package version",
        "AND the returned plugin exposes install(projectRoot, options) and uninstall(projectRoot, options) functions"
      ]
    },
    {
      "requirement": "Skill installation and frontmatter injection",
      "name": "Install injects frontmatter and reports outcome",
      "given": ["skills are available from @specd/skills"],
      "when": ["install(projectRoot, options) runs with a selected skill set"],
      "then": [
        "installed markdown skill files include Copilot-compatible frontmatter prepended before content",
        "AND the install result reports installed and skipped entries"
      ]
    },
    {
      "requirement": "Frontmatter field contract",
      "name": "Copilot emitter supports full declared field set",
      "when": ["frontmatter is generated for a skill file"],
      "then": [
        "name and description are always emitted",
        "AND license, allowed-tools, user-invocable, and disable-model-invocation are emitted only when configured",
        "AND unsupported keys are excluded"
      ]
    },
    {
      "requirement": "Install location",
      "name": "Skills are written under GitHub skills directory",
      "when": ["install writes skill files"],
      "then": ["files are created only under .github/skills/ within the target project root"]
    }
  ],
  "contentHashes": {
    "spec.md": "sha256:ce08d74463e90ece79211b0c2285b38a8136e2791cb74e0df3bb90890b61000c",
    "verify.md": "sha256:8f1f75644a88f38f7b648a5e54a8ddb068fe08fe7b98b355ab40b22221e1aef4"
  },
  "generatedBy": "core"
}
