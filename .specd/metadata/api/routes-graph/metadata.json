{
  "title": "Routes Graph",
  "description": "Authoritative HTTP contract (methods, paths, query, bodies, status codes) for **Routes Graph** under `/v1`. Handlers and OpenAPI MUST match this spec exactly so CLI, agents, and Studio stay aligned. HTTP contract for code-graph operations via the SDK host graph factory (`apiContext.createGraphProvider`, `runIndexProjectGraph`) — not CLI/MCP.",
  "dependsOn": ["default:_global/architecture", "default:_global/conventions"],
  "context": [
    "Authoritative HTTP contract (methods, paths, query, bodies, status codes) for **Routes Graph** under `/v1`. Handlers and OpenAPI MUST match this spec exactly so CLI, agents, and Studio stay aligned. HTTP contract for code-graph operations via the SDK host graph factory (`apiContext.createGraphProvider`, `runIndexProjectGraph`) — not CLI/MCP."
  ],
  "rules": [
    {
      "requirement": "GET graph status exposes freshness and stale flag",
      "rules": [
        "`GET /v1/graph/status` MUST return fingerprint, stale/freshness flags, and counts sufficient for sidebar warnings."
      ]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "rules": [
        "`POST /v1/graph/index` MUST trigger reindex via `runIndexProjectGraph` from `@specd/sdk` (using the process-scoped SDK host context). v1 MAY block until completion; async job ids are deferred.\n\nIndexing MUST rebuild the full project graph, not a workspace-scoped subset, so cross-workspace spec-to-symbol and symbol-to-symbol links remain globally consistent.\n\nWhen the request body includes `force: true`, the handler MUST recreate persistent graph storage before indexing so the run starts from an empty graph. The response MUST return the graph provider's indexing summary DTO, including per-workspace breakdown.\n\n`POST /v1/graph/index` MUST accept only the documented body shape `{ force?: boolean }`. Unknown properties such as `workspaces` MUST be rejected with HTTP 400 `application/problem+json` and code `INVALID_REQUEST`."
      ]
    },
    {
      "requirement": "graph index preparation mirrors the CLI assembly flow",
      "rules": [
        "Before invoking the provider index operation, the API MUST assemble index input through `runIndexProjectGraph` from `@specd/sdk`, which mirrors the CLI assembly flow:\n\n- obtain orchestrated workspaces from `kernel.project.listWorkspaces.execute()`\n- derive effective graph config from project `SpecdConfig`\n- pass that assembled project-level input into the code-graph provider\n\nThe API MUST NOT maintain a separate legacy workspace-target bootstrap path that can drift from CLI behavior."
      ]
    },
    {
      "requirement": "search impact and hotspots mirror CLI graph commands",
      "rules": [
        "`GET /v1/graph/search`, `/graph/impact`, and `/graph/hotspots` MUST accept the same query parameters as the CLI graph commands and return presenter-mapped DTOs.\n\nFor graph search, the API MUST support at least:\n\n- `q`\n- `workspace`\n- `kinds`\n- `filePattern`\n- `excludePaths`\n- `excludeWorkspaces`\n- `symbols`\n- `specs`\n- `documents`\n- `limit`\n\nFor graph impact, the API MUST support `symbol`, `file`, and `spec` selectors, matching the CLI capability surface.\n\nFor graph impact, the API MUST surface aggregate blast-radius metrics, affected spec ids, affected files, and per-symbol traversal `depth` from the underlying graph result."
      ]
    },
    {
      "requirement": "graph-route inputs are schema-validated",
      "rules": [
        "Every `params`, `query`, and `body` shape accepted by this route group MUST be declared in Fastify route schema and validated before handler logic runs.\n\nSearch queries MUST require `q`. Impact queries MUST require exactly one of `symbol`, `file`, or `spec`, and `direction` / `depth` MUST reject values outside the documented contract. Invalid input MUST return HTTP 400 with `application/problem+json` and code `INVALID_REQUEST`."
      ]
    },
    {
      "requirement": "spec and change linkage endpoints compose graph with spec scope",
      "rules": [
        "`GET /v1/graph/specs/{workspace}/{path}` and `/graph/changes/{name}` MUST return linkage views composed from graph provider plus spec/change scope."
      ]
    }
  ],
  "constraints": [
    "@specd/api delivery and composition code MUST import host bootstrap and kernel types from @specd/sdk, not @specd/core or @specd/code-graph directly.",
    "HTTP handlers MUST NOT import @specd/core from @specd/ui or @specd/client.",
    "v1 server auth: api.auth.type from specd.yaml (never studio.*); registry registers only disabled; no server-side Bearer enforcement on loopback or specd ui serve.",
    "Artifact save/load MUST use core:save-change-artifact and core:get-change-artifact — not raw ChangeRepository.saveArtifact from HTTP handlers.",
    "There is no GET /changes/{name}/validation resource; use GET .../status and POST .../validate.",
    "Canonical workspace spec artifacts are read-only in Studio v1."
  ],
  "scenarios": [
    {
      "requirement": "GET graph status exposes freshness and stale flag",
      "name": "Versioned API path",
      "when": ["client calls the documented route with /v1 prefix"],
      "then": ["route handler is reached", "AND unprefixed legacy path is not registered"]
    },
    {
      "requirement": "GET graph status exposes freshness and stale flag",
      "name": "Graph status exposes freshness",
      "when": ["GET /v1/graph/status is called"],
      "then": [
        "response includes fingerprint and stale flags",
        "AND sidebar can warn when graph is stale"
      ]
    },
    {
      "requirement": "GET graph status exposes freshness and stale flag",
      "name": "Undocumented path returns 404",
      "when": ["client requests a URL outside this routes contract"],
      "then": ["HTTP 404 is returned", "AND body is application/problem+json"]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "name": "POST /v1/graph/index kicks off index job",
      "when": ["client posts to index endpoint"],
      "then": ["kernel/code-graph index runs", "AND HTTP 200 returns indexing summary DTO"]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "name": "Index always rebuilds the full project graph",
      "when": ["POST /v1/graph/index runs"],
      "then": [
        "the index request covers all configured workspaces",
        "AND cross-workspace graph links remain in scope"
      ]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "name": "Force reindex recreates graph storage first",
      "when": ["client posts { \"force\": true } to /v1/graph/index"],
      "then": [
        "persistent graph storage is recreated before indexing",
        "AND the response still returns the indexing summary DTO"
      ]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "name": "Index rejects unsupported body properties",
      "when": ["client posts { \"workspaces\": [\"api\", \"client\"] } to /v1/graph/index"],
      "then": [
        "HTTP 400 is returned",
        "AND body is application/problem+json",
        "AND code is INVALID_REQUEST"
      ]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "name": "Index completion updates freshness",
      "given": ["index was stale"],
      "when": ["index POST completes"],
      "then": ["subsequent GET status shows fresh", "AND indexedAt updated"]
    },
    {
      "requirement": "POST graph index rebuilds the code graph index",
      "name": "Concurrent index requests are serialized",
      "given": ["index already running"],
      "when": ["second POST /v1/graph/index arrives"],
      "then": ["request rejected or queued", "AND corrupt index not written"]
    },
    {
      "requirement": "graph index preparation mirrors the CLI assembly flow",
      "name": "Index preparation uses orchestrated workspaces and project graph config",
      "when": ["POST /v1/graph/index prepares provider input"],
      "then": [
        "workspaces come from ListWorkspaces",
        "AND effective graph config is assembled before indexing"
      ]
    },
    {
      "requirement": "search impact and hotspots mirror CLI graph commands",
      "name": "Search endpoint accepts CLI-equivalent query",
      "when": ["GET /v1/graph/search with symbol query and graph filters"],
      "then": ["results match specd graph search shape", "AND BM25 ordering preserved"]
    },
    {
      "requirement": "search impact and hotspots mirror CLI graph commands",
      "name": "Impact dependents matches CLI direction",
      "when": ["impact endpoint called with dependents direction"],
      "then": ["dependent symbols returned", "AND same filters as CLI"]
    },
    {
      "requirement": "search impact and hotspots mirror CLI graph commands",
      "name": "Search result includes snippet and line range",
      "when": ["graph search returns hits"],
      "then": [
        "each hit includes preview snippet",
        "AND startLine and endLine identify the matched range"
      ]
    },
    {
      "requirement": "search impact and hotspots mirror CLI graph commands",
      "name": "Impact result includes traversal depth and aggregate metrics",
      "when": ["graph impact returns affected symbols"],
      "then": [
        "each symbol includes depth",
        "AND the response includes risk level, dependency counts, affected specs, and affected files"
      ]
    },
    {
      "requirement": "search impact and hotspots mirror CLI graph commands",
      "name": "Hotspots endpoint exposes risk tiers",
      "when": ["GET /v1/graph/hotspots"],
      "then": ["risk labels align with CLI", "AND min-risk filter honored"]
    },
    {
      "requirement": "graph-route inputs are schema-validated",
      "name": "Impact rejects missing selector",
      "when": ["client calls GET /v1/graph/impact without symbol, file, or spec"],
      "then": [
        "HTTP 400 is returned",
        "AND body is application/problem+json",
        "AND code is INVALID_REQUEST"
      ]
    },
    {
      "requirement": "graph-route inputs are schema-validated",
      "name": "Spec impact selector is accepted",
      "when": ["client calls GET /v1/graph/impact?spec=core:change&direction=dependents"],
      "then": [
        "the route resolves spec impact",
        "AND the response uses the shared graph impact DTO"
      ]
    },
    {
      "requirement": "graph-route inputs are schema-validated",
      "name": "Impact rejects unsupported direction",
      "when": ["client calls GET /v1/graph/impact?symbol=core:test&direction=sideways"],
      "then": ["HTTP 400 is returned", "AND body is application/problem+json"]
    },
    {
      "requirement": "spec and change linkage endpoints compose graph with spec scope",
      "name": "Spec linkage returns symbols for spec id",
      "when": ["GET /v1/graph/specs/core:foo"],
      "then": ["coversSymbol links included", "AND scoped to requested spec"]
    },
    {
      "requirement": "spec and change linkage endpoints compose graph with spec scope",
      "name": "Change linkage limits to change spec deps",
      "given": ["change depends on two specs"],
      "when": ["change linkage endpoint runs"],
      "then": ["graph query restricted to those specs", "AND unrelated workspaces omitted"]
    },
    {
      "requirement": "spec and change linkage endpoints compose graph with spec scope",
      "name": "Unknown spec id returns 404 problem+json",
      "when": ["linkage called with missing spec"],
      "then": ["HTTP 404", "AND problem body explains missing spec"]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "api:routes-graph",
        "file": "api:src/delivery/http/handlers/handler-graph.ts",
        "symbol": "registerGraphRoutes"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:5d2002d9497af4ffc763ca87b2914f76d6109873552e3e411a69837f42f05f5a",
    "verify.md": "sha256:b971d3e13baa1ceaffc9a174ba85981f80a412394d2564cef9e9ca293ef0c1d3"
  },
  "generatedBy": "core"
}
