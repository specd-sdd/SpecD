{
  "title": "Presenter Graph",
  "description": "Maps kernel or graph results into the matching `dto-*` response for **Presenter Graph**. Presenters are pure formatting at the HTTP boundary so handlers do not embed lifecycle or validation logic.",
  "dependsOn": ["default:_global/architecture", "default:_global/conventions", "api:dto-graph"],
  "context": [
    "Maps kernel or graph results into the matching `dto-*` response for **Presenter Graph**. Presenters are pure formatting at the HTTP boundary so handlers do not embed lifecycle or validation logic."
  ],
  "rules": [
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "rules": [
        "Given the same kernel or graph result, the presenter MUST produce the same DTO JSON shape, omitting optional properties when values are absent. **`toGraphSearchResultDto`** MUST support mapping `SymbolNode`, `SpecNode`, and `DocumentNode` search results into the consolidated search DTO."
      ]
    },
    {
      "requirement": "presenter does not encode business rules",
      "rules": [
        "The presenter MUST NOT decide lifecycle transitions, validation outcomes, or approval state — those belong in core use cases invoked via the SDK kernel surface."
      ]
    },
    {
      "requirement": "graph status presenter derives warnings from health result",
      "rules": [
        "`toGraphStatusDto` MUST accept `GetGraphHealthResult` (or equivalent fields) and populate [`api:dto-graph-status`](../dto-graph-status/spec.md) diagnostic properties.\n\nThe presenter MUST derive `warnings[]` deterministically:\n\n- When `stale === true` and both `lastIndexedRef` and `currentRef` are non-null, append `{ type: 'graph-stale', message: ... }` using CLI-equivalent copy (short ref prefixes allowed).\n- When `fingerprintMismatch === true`, append `{ type: 'graph-fingerprint-mismatch', message: ... }` using CLI-equivalent copy.\n\nThe presenter MUST NOT recompute staleness or fingerprint mismatch — it formats values already computed by `GetGraphHealth`."
      ]
    }
  ],
  "constraints": [
    "HTTP handlers MUST NOT import @specd/core from @specd/ui or @specd/client.",
    "v1 server auth: api.auth.type from specd.yaml (never studio.*); registry registers only disabled; no server-side Bearer enforcement on loopback or specd ui serve.",
    "Artifact save/load MUST use core:save-change-artifact and core:get-change-artifact — not raw ChangeRepository.saveArtifact from HTTP handlers.",
    "There is no GET /changes/{name}/validation resource; use GET .../status and POST .../validate.",
    "Canonical workspace spec artifacts are read-only in Studio v1."
  ],
  "scenarios": [
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "name": "Presenter output matches paired DTO",
      "given": ["fixed kernel or graph fixture"],
      "when": ["presenter serializes to JSON"],
      "then": ["shape matches api:dto-* spec", "AND OpenAPI schema validates sample"]
    },
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "name": "Presenter leaves kernel blockers unchanged",
      "given": ["kernel returns known blockers"],
      "when": ["presenter maps response"],
      "then": ["blocker codes equal kernel", "AND no new blockers invented"]
    },
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "name": "Repeated mapping is deterministic",
      "when": ["presenter called twice on same input"],
      "then": ["outputs are identical", "AND no time-dependent fields unless documented"]
    },
    {
      "requirement": "presenter does not encode business rules",
      "name": "Presenter output matches paired DTO",
      "given": ["fixed kernel or graph fixture"],
      "when": ["presenter serializes to JSON"],
      "then": ["shape matches api:dto-* spec", "AND OpenAPI schema validates sample"]
    },
    {
      "requirement": "presenter does not encode business rules",
      "name": "Presenter leaves kernel blockers unchanged",
      "given": ["kernel returns known blockers"],
      "when": ["presenter maps response"],
      "then": ["blocker codes equal kernel", "AND no new blockers invented"]
    },
    {
      "requirement": "presenter does not encode business rules",
      "name": "Repeated mapping is deterministic",
      "when": ["presenter called twice on same input"],
      "then": ["outputs are identical", "AND no time-dependent fields unless documented"]
    },
    {
      "requirement": "graph status presenter derives warnings from health result",
      "name": "Presenter does not recompute staleness",
      "given": ["a GetGraphHealthResult fixture with stale: true"],
      "when": ["toGraphStatusDto runs"],
      "then": ["output stale matches input", "AND warnings are derived only from input fields"]
    },
    {
      "requirement": "graph status presenter derives warnings from health result",
      "name": "CLI-equivalent stale message",
      "given": ["known lastIndexedRef and currentRef"],
      "when": ["stale warning is emitted"],
      "then": ["message mentions indexed vs current ref prefixes"]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "api:presenter-graph",
        "file": "api:src/delivery/http/presenters/presenter-graph.ts",
        "symbol": "toGraphStatusDto"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:0e882fd1646a8e4c836a6dadd2767e0df9ee601991a631f5911dfcf7e84af5da",
    "verify.md": "sha256:8e402660daabeb9684c0d9509c33703f3a94dfa143b70cd76e7f2c3f3001f3c4"
  },
  "generatedBy": "core"
}
