{
  "title": "Presenter Project",
  "description": "Maps kernel or graph results into the matching `dto-*` response for **Presenter Project**. Presenters are pure formatting at the HTTP boundary so handlers do not embed lifecycle or validation logic.",
  "dependsOn": [
    "default:_global/architecture",
    "default:_global/conventions",
    "api:dto-project",
    "client:dto-project-status",
    "api:dto-project-status"
  ],
  "context": [
    "Maps kernel or graph results into the matching `dto-*` response for **Presenter Project**. Presenters are pure formatting at the HTTP boundary so handlers do not embed lifecycle or validation logic."
  ],
  "rules": [
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "rules": [
        "Given the same kernel or graph result, the presenter MUST produce the same DTO JSON shape, omitting optional properties when values are absent."
      ]
    },
    {
      "requirement": "presenter does not encode business rules",
      "rules": [
        "The presenter MUST NOT decide lifecycle transitions, validation outcomes, or approval state — those belong in core use cases invoked via the SDK kernel surface."
      ]
    },
    {
      "requirement": "project status presenter maps graph health diagnostics",
      "rules": [
        "When mapping `buildProjectStatusSnapshot.graphHealth`, the project presenter MUST populate the `graph` slice on [`api:dto-project-status`](../dto-project-status/spec.md) with the same diagnostic fields and `warnings[]` derivation rules as `toGraphStatusDto` in [`api:presenter-graph`](../presenter-graph/spec.md).\n\nCount fields (`symbolCount`, `specCount`, `indexed`) MUST continue to reflect graph statistics when graph health is included."
      ]
    },
    {
      "requirement": "project status presentation uses the canonical client mapper",
      "rules": [
        "The API project-status presenter MUST delegate final DTO construction to the pure mapper exported by `@specd/client`.\n\nAPI-specific code MAY assemble structural inputs from SDK results and server auth state, but MUST NOT maintain a divergent project-status field mapping."
      ]
    }
  ],
  "constraints": [
    "HTTP handlers MUST NOT import @specd/core from @specd/ui or @specd/client.",
    "v1 server auth: api.auth.type from specd.yaml (never studio.*); registry registers only disabled; no server-side Bearer enforcement on loopback or specd ui serve.",
    "Artifact save/load MUST use core:save-change-artifact and core:get-change-artifact — not raw ChangeRepository.saveArtifact from HTTP handlers.",
    "There is no GET /changes/{name}/validation resource; use GET .../status and POST .../validate.",
    "Canonical workspace spec artifacts are read-only in Studio v1."
  ],
  "scenarios": [
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "name": "Presenter output matches paired DTO",
      "given": ["fixed kernel or graph fixture"],
      "when": ["presenter serializes to JSON"],
      "then": ["shape matches api:dto-* spec", "AND OpenAPI schema validates sample"]
    },
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "name": "Presenter leaves kernel blockers unchanged",
      "given": ["kernel returns known blockers"],
      "when": ["presenter maps response"],
      "then": ["blocker codes equal kernel", "AND no new blockers invented"]
    },
    {
      "requirement": "presenter maps entities to DTO fields deterministically",
      "name": "Repeated mapping is deterministic",
      "when": ["presenter called twice on same input"],
      "then": ["outputs are identical", "AND no time-dependent fields unless documented"]
    },
    {
      "requirement": "presenter does not encode business rules",
      "name": "Presenter output matches paired DTO",
      "given": ["fixed kernel or graph fixture"],
      "when": ["presenter serializes to JSON"],
      "then": ["shape matches api:dto-* spec", "AND OpenAPI schema validates sample"]
    },
    {
      "requirement": "presenter does not encode business rules",
      "name": "Presenter leaves kernel blockers unchanged",
      "given": ["kernel returns known blockers"],
      "when": ["presenter maps response"],
      "then": ["blocker codes equal kernel", "AND no new blockers invented"]
    },
    {
      "requirement": "presenter does not encode business rules",
      "name": "Repeated mapping is deterministic",
      "when": ["presenter called twice on same input"],
      "then": ["outputs are identical", "AND no time-dependent fields unless documented"]
    },
    {
      "requirement": "project status presenter maps graph health diagnostics",
      "name": "Project presenter reuses graph warning derivation",
      "given": ["buildProjectStatusSnapshot with graphHealth fixture"],
      "when": ["project status DTO is built"],
      "then": ["graph.warnings matches toGraphStatusDto output for the same health result"]
    },
    {
      "requirement": "project status presentation uses the canonical client mapper",
      "name": "API delegates final status DTO construction",
      "given": ["a project status snapshot, graph health, approvals, and effective auth type"],
      "when": ["the API presenter builds the status response"],
      "then": [
        "it passes structural input to the @specd/client mapper",
        "AND the returned value is used as the HTTP DTO without a second field mapping"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "api:presenter-project",
        "file": "api:src/delivery/http/presenters/presenter-project.ts",
        "symbol": "toProjectStatusDtoFromSnapshot"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:fb8fe6d6f2bbe097f06fbd3fef5a257483ed6d5f6850fa62928c2ec45d275e75",
    "verify.md": "sha256:b8abbffd9f45c81eebce1ce45c78e0c19c5b608ab4bbeb90c70e7468f19606ec"
  },
  "generatedBy": "core"
}
