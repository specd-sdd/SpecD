{
  "title": "Handler Changes Collection",
  "description": "HTTP handlers for **changes collection** in SpecD Studio. They validate requests, call kernel use cases exposed through `@specd/sdk` as listed in the paired `routes-*` spec, and map results through presenters. Business rules live in core use cases invoked via `apiContext.kernel` — not in this delivery module.",
  "dependsOn": [
    "default:_global/architecture",
    "default:_global/conventions",
    "sdk:composition",
    "api:routes-changes-collection"
  ],
  "context": [
    "HTTP handlers for **changes collection** in SpecD Studio. They validate requests, call kernel use cases exposed through `@specd/sdk` as listed in the paired `routes-*` spec, and map results through presenters. Business rules live in core use cases invoked via `apiContext.kernel` — not in this delivery module."
  ],
  "rules": [
    {
      "requirement": "handler implements the routes contract under /v1",
      "rules": [
        "The module MUST implement every method, path, query, and body declared in [`api:routes-changes-collection`](../routes-changes-collection/spec.md) under the `/v1` prefix. It MUST NOT expose undocumented routes."
      ]
    },
    {
      "requirement": "handler delegates to kernel without duplicating domain rules",
      "rules": [
        "Business rules for lifecycle, validation, approvals, and conflicts MUST live in core use cases. This handler MUST invoke them only through `apiContext.kernel`, including:\n\n- `CreateChange`\n- `kernel.changes.list`\n- `kernel.changes.listDrafts`\n- `kernel.changes.listDiscarded`\n- `kernel.changes.listArchived`\n- `DetectOverlap`"
      ]
    },
    {
      "requirement": "successful responses use presenters and DTO wire shapes",
      "rules": [
        "Successful responses MUST be produced by the matching `api:presenter-*` module and conform to the corresponding `api:dto-*` spec."
      ]
    },
    {
      "requirement": "failures map to RFC 7807 problem+json",
      "rules": [
        "Thrown kernel errors and validation failures MUST be converted through `api:problem-json` to `application/problem+json` responses with appropriate HTTP status codes."
      ]
    },
    {
      "requirement": "archived list serializes list-result structure instead of legacy arrays",
      "rules": [
        "When mapping `kernel.changes.listArchived`, the handler MUST preserve the split `items/meta` shape introduced by the merged archive index contract. It MUST NOT flatten archive rows into a bare array that loses pagination metadata."
      ]
    },
    {
      "requirement": "SDK delivery imports",
      "rules": [
        "Handler modules MUST import kernel types, errors, and use-case entry points from `@specd/sdk`. They MUST NOT import `@specd/core` directly."
      ]
    }
  ],
  "constraints": [
    "Handler modules MUST import kernel types and use-case entry points from @specd/sdk, not @specd/core directly.",
    "HTTP handlers MUST NOT import @specd/core from @specd/ui or @specd/client.",
    "v1 server auth: api.auth.type from specd.yaml (never studio.*); registry registers only disabled; no server-side Bearer enforcement on loopback or specd ui serve.",
    "Artifact save/load MUST use core:save-change-artifact and core:get-change-artifact — not raw ChangeRepository.saveArtifact from HTTP handlers.",
    "There is no GET /changes/{name}/validation resource; use GET .../status and POST .../validate.",
    "Canonical workspace spec artifacts are read-only in Studio v1."
  ],
  "scenarios": [
    {
      "requirement": "handler implements the routes contract under /v1",
      "name": "Only documented /v1 routes are registered",
      "when": ["HTTP server route table is inspected after bootstrap"],
      "then": [
        "every handler path matches api:routes-changes-collection under /v1",
        "AND no undocumented shadow routes exist"
      ]
    },
    {
      "requirement": "handler implements the routes contract under /v1",
      "name": "Declared GET route returns presenter-shaped JSON",
      "when": ["client calls a documented GET route with valid fixtures"],
      "then": ["HTTP status is 2xx", "AND body matches the paired api:dto-* spec"]
    },
    {
      "requirement": "handler implements the routes contract under /v1",
      "name": "Undeclared verb on a path returns 405",
      "when": ["client calls a route with a verb not declared in the routes spec"],
      "then": ["HTTP 405 is returned", "AND response uses application/problem+json when mapped"]
    },
    {
      "requirement": "handler delegates to kernel without duplicating domain rules",
      "name": "Handler invokes CreateChange",
      "when": ["a valid request for this handler is processed"],
      "then": [
        "CreateChange runs inside @specd/core",
        "AND handler does not reimplement lifecycle or validation rules"
      ]
    },
    {
      "requirement": "handler delegates to kernel without duplicating domain rules",
      "name": "Handler invokes kernel.changes.list",
      "when": ["a valid request for this handler is processed"],
      "then": [
        "kernel.changes.list runs inside @specd/core",
        "AND handler does not reimplement lifecycle or validation rules"
      ]
    },
    {
      "requirement": "handler delegates to kernel without duplicating domain rules",
      "name": "Handler invokes kernel.changes.listDrafts",
      "when": ["a valid request for this handler is processed"],
      "then": [
        "kernel.changes.listDrafts runs inside @specd/core",
        "AND handler does not reimplement lifecycle or validation rules"
      ]
    },
    {
      "requirement": "archived list serializes list-result structure instead of legacy arrays",
      "name": "Archived list preserves items/meta shape",
      "when": ["kernel.changes.listArchived returns a paginated result"],
      "then": [
        "the handler serializes items and meta",
        "AND it does not flatten archive rows into a bare array"
      ]
    },
    {
      "requirement": "successful responses use presenters and DTO wire shapes",
      "name": "Successful body passes through presenter",
      "given": ["kernel returns a successful result object"],
      "when": ["handler builds the HTTP 200 response"],
      "then": [
        "matching api:presenter-* mapped the result",
        "AND JSON conforms to paired api:dto-*"
      ]
    },
    {
      "requirement": "successful responses use presenters and DTO wire shapes",
      "name": "Presenter output stays stable for fixed fixture",
      "given": ["a fixed kernel fixture for this route"],
      "when": ["the same request is handled twice"],
      "then": ["both JSON bodies are identical", "AND presenter did not mutate kernel state"]
    },
    {
      "requirement": "successful responses use presenters and DTO wire shapes",
      "name": "Invalid create payload fails before presenter",
      "when": ["POST /v1/changes request validation fails"],
      "then": ["HTTP 400 problem+json is returned", "AND presenter is not invoked"]
    },
    {
      "requirement": "failures map to RFC 7807 problem+json",
      "name": "Kernel error maps to problem+json",
      "when": ["kernel throws a domain error for this route"],
      "then": ["Content-Type is application/problem+json", "AND body includes status and title"]
    },
    {
      "requirement": "failures map to RFC 7807 problem+json",
      "name": "Validation failure maps to 4xx problem+json",
      "when": ["ValidateArtifacts or input validation rejects the request"],
      "then": ["HTTP 4xx is returned", "AND problem payload describes the failing constraint"]
    },
    {
      "requirement": "failures map to RFC 7807 problem+json",
      "name": "Unexpected throw does not return plain text",
      "when": ["an unhandled exception escapes the handler stack"],
      "then": [
        "response is not text/plain HTML",
        "AND client receives problem+json or framework 500 mapping"
      ]
    },
    {
      "requirement": "failures map to RFC 7807 problem+json",
      "name": "Create change mutation receives request actor",
      "given": ["createApiContext attached an actor to the request"],
      "when": ["the handler triggers change creation"],
      "then": ["kernel input includes that actor", "AND history by matches the resolved identity"]
    },
    {
      "requirement": "failures map to RFC 7807 problem+json",
      "name": "Read-only routes may omit actor on kernel calls",
      "when": ["handler serves a read-only GET that does not write history"],
      "then": [
        "kernel read use case still succeeds",
        "AND no history event is written for the call"
      ]
    },
    {
      "requirement": "SDK delivery imports",
      "name": "Handler imports kernel surface from SDK",
      "when": ["inspecting handler-changes-collection module imports"],
      "then": [
        "kernel types and errors are imported from @specd/sdk",
        "AND @specd/core is not imported directly"
      ]
    }
  ],
  "implementation": {
    "symbols": [
      {
        "specId": "api:handler-changes-collection",
        "file": "api:src/delivery/http/handlers/handler-changes-collection.ts",
        "symbol": "registerChangesCollectionRoutes"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:aa9c13e9f608c132fe4939e5c011b8c844f2198757dc7620ae1ed9f67f0db48b",
    "verify.md": "sha256:ef36673418f892fddd08aca3149dd24087b8e5acb62426b2931d690f46a7267f"
  },
  "generatedBy": "core"
}
