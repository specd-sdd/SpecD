{
  "title": "Composition Graph Provider",
  "description": "Graph routes must not construct `@specd/code-graph` providers ad hoc with divergent paths or freshness rules. This spec centralizes provider creation from `SpecdConfig` so index, search, impact, and linkage handlers share one implementation and expose stale-state signals to the UI.",
  "dependsOn": [
    "default:_global/architecture",
    "default:_global/conventions",
    "sdk:run-index-project-graph",
    "sdk:host-context",
    "code-graph:composition"
  ],
  "context": [
    "Graph routes must not construct `@specd/code-graph` providers ad hoc with divergent paths or freshness rules. This spec centralizes provider creation from `SpecdConfig` so index, search, impact, and linkage handlers share one implementation and expose stale-state signals to the UI."
  ],
  "rules": [
    {
      "requirement": "provider is created from project configuration",
      "rules": [
        "The API MUST obtain its process-scoped graph provider from the `createGraphProvider`\nfunction on the process-scoped `SdkHostContext` (created by `createSdkContext`).\nThe provider MUST be bound to the resolved `SpecdConfig` (workspaces, code roots) for\nthe served project.\n\nThe server MUST open that provider once under host control, reuse it while healthy,\nreopen or replace it on `GraphProviderStaleError`, and close it on server shutdown —\nmatching the long-lived host contract in `code-graph:composition`."
      ]
    },
    {
      "requirement": "indexing preparation follows the merged project-assembly model",
      "rules": [
        "API graph indexing MUST use SDK orchestration (`runIndexProjectGraph` from `@specd/sdk`) with the same project-level assembly semantics as the CLI:\n\n- orchestrated workspaces from `ListWorkspaces`\n- effective graph config assembled from project config\n- one project-level provider index call\n\nAPI composition MUST NOT reconstruct an older workspace-target contract or duplicate CLI assembly logic outside the SDK helper."
      ]
    },
    {
      "requirement": "stale state is observable",
      "rules": [
        "The provider (or its stats call) MUST expose freshness/stale signals consumed by `GET /v1/graph/status` so the UI can warn when the index is out of date."
      ]
    },
    {
      "requirement": "SDK graph provider factory",
      "rules": [
        "Graph handlers and composition MUST obtain the opened provider through the\nprocess-scoped healthy long-lived accessor (`withGraphProvider` / equivalent host helper).\n`getGraphProvider` MAY return the held opened provider without stale recovery (peek).\nConstruction MUST still go through `SdkHostContext.createGraphProvider`.\nHandlers MUST NOT import `createCodeGraphProvider` from `@specd/code-graph` directly.\nHandlers MUST NOT call `withOpenGraphProvider` for routine graph HTTP routes."
      ]
    },
    {
      "requirement": "long-lived provider stale reopen and index on injected provider",
      "rules": [
        "When indexing, the API host MUST pass its process-scoped long-lived opened provider\ninto `runIndexProjectGraph` as `input.provider`. The host MUST NOT close or replace\nthat provider solely because indexing completed — including when `force: true` —\nbecause force recreate and storage-generation refresh are owned by\n`CodeGraphProvider.index` on that same instance.\n\nWhen a graph operation on the long-lived provider throws `GraphProviderStaleError`,\nthe healthy accessor (`withGraphProvider`) MUST close and reopen (or replace) the\nprovider and MAY retry the operation once.\n\nHost helpers such as `refreshGraphProvider` MAY remain for explicit stale recovery or\nother host-owned replacement, but MUST NOT be required as a post-index step when\nindex used the injected long-lived provider."
      ]
    }
  ],
  "constraints": [
    "@specd/api graph composition MUST import orchestration helpers from @specd/sdk.",
    "HTTP handlers MUST NOT import @specd/core from @specd/ui or @specd/client.",
    "v1 server auth: api.auth.type from specd.yaml (never studio.*); registry registers only disabled; no server-side Bearer enforcement on loopback or specd ui serve.",
    "Artifact save/load MUST use core:save-change-artifact and core:get-change-artifact — not raw ChangeRepository.saveArtifact from HTTP handlers.",
    "There is no GET /changes/{name}/validation resource; use GET .../status and POST .../validate.",
    "Canonical workspace spec artifacts are read-only in Studio v1."
  ],
  "scenarios": [
    {
      "requirement": "provider is created from project configuration",
      "name": "Provider binds to project graph paths",
      "given": ["specd.yaml declares workspaces"],
      "when": ["API server bootstrap creates and opens the long-lived graph provider"],
      "then": ["provider reads project root from config", "AND index paths match workspace layout"]
    },
    {
      "requirement": "provider is created from project configuration",
      "name": "Missing graph index yields empty provider state",
      "given": ["no .specd/graph index on disk"],
      "when": ["provider is constructed"],
      "then": ["provider reports not indexed", "AND handlers can still expose status DTO"]
    },
    {
      "requirement": "provider is created from project configuration",
      "name": "Reconfigured project rebuilds provider",
      "when": ["server restarts after config change"],
      "then": ["new provider instance uses updated paths", "AND old in-memory graph is not reused"]
    },
    {
      "requirement": "indexing preparation follows the merged project-assembly model",
      "name": "API graph indexing assembles project-level input before provider call",
      "when": ["API prepares a graph index request"],
      "then": [
        "it combines ListWorkspaces output with effective graph config",
        "AND the provider receives one project-level index input instead of legacy per-workspace targets"
      ]
    },
    {
      "requirement": "stale state is observable",
      "name": "Stale flag true when index older than sources",
      "given": ["source file newer than graph index mtime"],
      "when": ["GET /v1/graph/status runs"],
      "then": ["stale: true in response", "AND CLI-equivalent freshness message available"]
    },
    {
      "requirement": "stale state is observable",
      "name": "Fresh index reports stale false",
      "given": ["graph index rebuilt after last edit"],
      "when": ["status endpoint is queried"],
      "then": ["stale: false", "AND last indexed timestamp is exposed"]
    },
    {
      "requirement": "stale state is observable",
      "name": "POST index clears stale after success",
      "given": ["status was stale"],
      "when": ["POST /v1/graph/index completes"],
      "then": ["subsequent status shows stale: false", "AND indexedAt advances"]
    },
    {
      "requirement": "SDK graph provider factory",
      "name": "Graph handler uses long-lived withGraphProvider",
      "when": ["a graph handler needs an opened provider"],
      "then": [
        "it calls apiContext.withGraphProvider() (or the process-scoped healthy equivalent)",
        "AND it does not import createCodeGraphProvider from @specd/code-graph",
        "AND it does not open/close a provider per HTTP request"
      ]
    },
    {
      "requirement": "long-lived provider stale reopen and index on injected provider",
      "name": "Stale provider is reopened",
      "given": ["the long-lived provider throws GraphProviderStaleError"],
      "when": ["the healthy accessor recovers"],
      "then": ["it closes and reopens (or replaces) the process-scoped provider"]
    },
    {
      "requirement": "long-lived provider stale reopen and index on injected provider",
      "name": "Index on injected provider does not require post-index host refresh",
      "when": [
        "runIndexProjectGraph completes with input.provider set to the process long-lived provider"
      ],
      "then": [
        "the API does not replace or reopen that provider solely because indexing completed",
        "AND when force: true, the same long-lived instance remains authoritative for subsequent graph reads"
      ]
    }
  ],
  "implementation": {
    "files": [
      {
        "specId": "api:composition-graph-provider",
        "file": "api:test/long-lived-graph.spec.ts"
      }
    ],
    "symbols": [
      {
        "specId": "api:composition-graph-provider",
        "file": "api:src/composition/long-lived-graph.ts",
        "symbol": "closeLongLivedGraphProvider"
      },
      {
        "specId": "api:composition-graph-provider",
        "file": "api:src/composition/long-lived-graph.ts",
        "symbol": "withHealthyGraphProvider"
      },
      {
        "specId": "api:composition-graph-provider",
        "file": "api:src/composition/long-lived-graph.ts",
        "symbol": "refreshLongLivedGraphProvider"
      }
    ]
  },
  "contentHashes": {
    "spec.md": "sha256:4971edeb24dd34b2db6535f3cb36e10d4fb280eb5e608469e8643f869e8b60dc",
    "verify.md": "sha256:e47c7c1b3a5b7334c59f883bbeea112bb5a925a592136c8cd06e859fd6934968"
  },
  "generatedBy": "core"
}
