# Changelog

## Unreleased

### @specd/core

- **feat:** add domain layer — entities, value objects, errors (`86b9b7b`)
- **feat:** add domain services — delta merger and snapshot hasher (`822d1ce`)
- **feat:** add application ports — repository base and contracts (`8c7a848`)
- **feat:** add remaining application ports and schema domain types (`d8a693b`)
- **feat:** rework schema-registry port and add schema version (`0a2b15a`)
- **feat:** add to-fs-path method for os-native filesystem paths (`cfa3b1f`)
- **feat:** add DeltaConflictError and fix mergeSpecs — RENAMED, conflict detection, deltaOperations, correct apply order (`2925ae4`)
- **feat:** implement phase 4 application use cases (`28e487d`)
- **feat:** add skipped artifact state and file-reader port (`bc82431`)
- **feat:** add artifact parser adapters for all supported formats (`5fcf72e`)
- **feat:** add compile-context use case (`186218d`)
- **feat:** add archive-change use case (`525f3e5`)
- **feat:** add validate-artifacts use case (`c04aadd`)
- **feat:** add fs change repository adapter (`b322d91`)
- **feat:** add fs spec repository adapter (`0bb0e8d`)
- **feat:** add fs archive repository adapter (`25a4051`)
- **feat:** add file reader, git, hook runner, and schema registry (`3adfe96`)
- **feat:** add composition factories for repository adapters (`eb8362d`)
- **feat:** add config-validation-error domain error (`21edbe7`)
- **feat:** add specd-config type and config-loader port (`8279607`)
- **feat:** add fs-config-loader infrastructure adapter (`60dac7e`)
- **feat:** add use-case factories, kernel, and composition wiring (`8ee8919`)
- **feat:** add task completion check for implementing to verifying (`ff9bd8e`)
- **feat:** wire composition factories and extend kernel (`2fa6e52`)
- **feat:** add use cases for listing, editing, and project ops (`dad9028`)
- **feat:** extend repositories and change ports (`7dcdeff`)
- **feat:** extend compile-context with dep-following and sections (`db871c0`)
- **feat:** enrich archive-change with git actor (`30cb519`)
- **feat:** extend config spec and loader (`0db0551`)
- **feat:** add dual signatures to composition factories (`9e829d0`)
- **feat:** add workspace and date template variables to archive pattern (`621a4f6`)
- **feat:** add write-time structural validation for spec metadata (`6429a8f`)
- **feat:** add invalid metadata status to spec list (`0b10aff`)
- **feat:** enforce required fields in strict metadata schema (`12df361`)
- **feat:** add invalidate-spec-metadata use case and cli command (`1a0e298`)
- **feat:** validate context pattern syntax in config-loader (`ebdeb65`)
- **feat:** add schema name mismatch guard to use cases (`a2a6122`)
- **feat:** add multi-vcs actor resolver implementations (`1f69edf`)
- **feat:** add cross-workspace resolution to spec repository (`e64f954`)
- **feat:** auto-extract depends-on from spec content after validation (`f422c0c`)
- **feat!:** add multi-file artifact tracking (`c3225b6`)
- **feat:** add depends-on overwrite protection to metadata saving (`a230713`)
- **feat:** add single-artifact validation via artifact-id filter (`a0cac82`)
- **feat:** apply pre-hash-cleanup rules in status derivation (`6a56cc6`)
- **feat:** add hook execution and artifact instruction use cases (`3b639ad`)
- **feat!:** add hooks, requires, and progress to transitions (`370136f`)
- **feat:** add resolve-path method to config loader port (`fca7709`)
- **feat:** add id uniqueness validation for schema arrays (`56b9db7`)
- **feat:** enforce global uniqueness for hook ids in workflow (`3865736`)
- **feat:** include changepath in change create output (`b68fe7f`)
- **feat:** add no-op delta operation and description field (`88ce023`)
- **feat:** enrich get-status with lifecycle context (`313794a`)
- **feat:** clean up scaffold dirs when removing specs from change (`5226ed6`)
- **feat:** implement schema customisation (extends, plugins, overrides) (`4d5607e`)
- **feat:** add lazy context mode with structured result shape (`25ed196`)
- **feat:** add validate-schema use case and extract extends chain (`c793b1f`)
- **feat!:** move task completion gating to workflow steps (`eb9b9c8`)
- **feat:** enforce readonly workspace ownership (`24102f4`)
- **feat:** add preview-spec use case and delta view (`21236a0`)
- **feat!:** derive task completion checks from schema (`4939649`)
- **feat:** make artifact invalidation directional (`c2d86f3`)
- **feat:** allow transition to designing from any state (`505445e`)
- **fix:** use correct delta file path in archive-change (`409eb9c`)
- **fix:** fix workspace source and step availability in compilecontext (`65305b4`)
- **fix:** correct ownership value in change repository mock helper (`9f0272c`)
- **fix:** fix selector matching in validate-artifacts (`cac9344`)
- **fix:** clear artifact hashes on invalidate and verifying loop (`9dc1bac`)
- **fix:** fix typecheck errors in archive-change and compile-context (`9bb7889`)
- **fix:** add missing verifying state to change lifecycle (`e348b57`)
- **fix:** add explicit type annotations in validate-artifacts (`4ced526`)
- **fix:** correct spec-scoped artifact paths in archive-change (`a76c1a7`)
- **fix:** restore blank lines between blocks in content extraction (`1f14a2b`)
- **fix:** preserve inline formatting in markdown parser round-trip (`cbe514b`)
- **fix:** validate skills yaml and specpath.fromsegments segments (`852e207`)
- **fix:** validate invalidate transition and copy contextspecids (`26f95ae`)
- **fix:** guard json path tokenizer against unclosed bracket (`7c88ea1`)
- **fix:** validate deserialization casts in change repository (`dd51771`)
- **fix:** handle non-enoent errors in archive repository (`89f6eee`)
- **fix:** throw typed error for corrupted change manifest (`59a9fb9`)
- **fix:** strengthen config type guard with zod (`09e0cc9`)
- **fix:** throw on missing created event in change entity (`52bcf38`)
- **fix:** add defensive copies and readonly input types (`429dee8`)
- **fix:** add missing barrel exports and remove duplicate jsdoc (`7e3db89`)
- **fix:** add cycle detection in change effective-status (`4d8c7a8`)
- **fix:** add defensive copies in change and archived-change entities (`9e36919`)
- **fix:** use node: prefix for crypto import (`2256e32`)
- **fix:** use type-only exports for use-case classes in barrel (`d664e21`)
- **fix:** replace bare error throws with typed specd error subclasses (`ad3f9db`)
- **fix:** validate delta yaml at infrastructure boundary (`2e89d2f`)
- **fix:** sanitize hook command variables against shell injection (`5b36274`)
- **fix:** include project node_modules in schema resolution paths (`5bd3858`)
- **fix:** validate parsed metadata yaml with zod schema (`047543f`)
- **fix:** add regex safety check for delta selectors (`e0e995e`)
- **fix:** replace bare error throws with typed specd errors (`33c0722`)
- **fix:** fix invalidate rollback for already-designing state (`e374225`)
- **fix:** parse workspace from specid in context-spec-ids traversal (`4e1cadb`)
- **fix:** revert workspace parsing in context-spec-ids and fix test (`9b3449e`)
- **fix:** validate manifest json at load boundary (`c93f991`)
- **fix:** add path traversal guard in file reader (`a832994`)
- **fix:** add redos guards to user-supplied regex patterns (`c313b93`)
- **fix:** update archived-change test for defensive date copy (`3c6f9c5`)
- **fix:** add random suffix to temp file paths (`42b9bf9`)
- **fix:** remove console.warn from production code (`3674e3d`)
- **fix:** validate manifests with zod in archive reindex (`eeedf01`)
- **fix:** execute project-level hooks in archive-change (`086ee78`)
- **fix:** guard spec-path against change scope (`14d222d`)
- **fix:** validate archive manifest and discriminate errors (`8ebcfea`)
- **fix:** add defensive copies in entity constructors (`11e9cd9`)
- **fix:** remove unused hash-files and config-loader-options exports (`4084171`)
- **fix:** preserve yaml comments in record skill install (`316758c`)
- **fix:** stop config walk at cwd when outside git repo (`c991be0`)
- **fix:** handle windows line endings in plaintext parser (`c9aa657`)
- **fix:** wrap parse errors in specd error subclasses (`2bf5222`)
- **fix:** check optional before accepting skipped status (`a78b39e`)
- **fix:** validate prefix match in spec dir resolution (`862a1c1`)
- **fix:** use replace-all in archive pattern expansion (`ce15194`)
- **fix:** make change props readonly and hide visited param (`aeff17d`)
- **fix:** default required to true in rule evaluator (`320ff85`)
- **fix:** cache spec artifact sections parse result (`a7e8509`)
- **fix:** skip headings inside fenced blocks in section parser (`04f6434`)
- **fix:** throw error instead of process.exit in format parser (`7a8410d`) (note: in cli)
- **fix:** normalize path separators in archive manifest collection (`9f1c12c`)
- **fix:** add missing content parameter to delta parser methods (`930bef0`)
- **fix:** enforce full lowercase-hyphen validation for keywords (`517b267`)
- **fix:** reject requires in project-level workflow entries (`62111c7`)
- **fix:** enforce ast exclusivity and delta+change scope validation (`561e687`)
- **fix:** clean up temp files on rename failure and wrap json.parse (`1ffe8c8`)
- **fix:** validate min-1 workspace and specid in change constructor (`26ab66b`)
- **fix:** replace non-null assertions with typed workspace lookup (`5545b1d`)
- **fix:** replace bare error with typed error in archive repository (`edad340`)
- **fix:** add readonly modifiers to interface properties (`169d67a`)
- **fix:** use lstat to skip symlinks in repository enumeration (`b6e7996`)
- **fix:** validate shell and comspec env vars in hook-runner (`302ff3b`)
- **fix:** use safe regex in pre-hash-cleanup and infer-spec-sections (`bbeb968`)
- **fix:** strengthen resolvedir timestamp prefix matching (`fc05b3c`)
- **fix:** align yaml subtree rendering with serialize round-trip path (`15dd5f0`)
- **fix:** detect stale archive index and auto-rebuild (`48e2367`)
- **fix:** gitignore archive index and auto-rebuild on missing (`18b4767`)
- **fix:** strict dir matching and stat error handling (`ef52ba5`)
- **fix:** remove unused param and skip no-op in edit-change (`98a1bd2`)
- **fix:** use platform-aware shell in hook runner (`06f0f28`)
- **fix:** create line nodes in plaintext parser parse output (`e6b184d`)
- **fix:** schema resolution for unknown workspaces and rel paths (`516a883`)
- **fix:** validate storage paths remain within repo root (`05cdd43`)
- **fix:** add redos protection to safe-regex (`7c11e8e`)
- **fix:** use multiline flag in pre-hash-cleanup regex (`2a4abee`)
- **fix:** add archiving as a lifecycle state for hook validation (`fc782ea`)
- **fix:** narrow `inferFormat` return type to `ArtifactFormat` (`2820ef7`)
- **fix:** support tilde fences in `shiftHeadings` (`fbdcd27`)
- **fix:** scan archive index from end without full load in get() (`337e1f0`)
- **fix:** use faithful content rendering in metadata extraction (`b819d3a`)
- **fix:** add depends-on fallback and manifest deps (`03ac40e`)
- **fix:** resolve 6 issues in create-change flow (`c28493c`)
- **fix:** throw on unknown workspace or spec in use cases (`810caf0`)
- **fix:** preserve markdown formatting during delta merge (`ad26ab5`)
- **fix:** correct cycle detection in effective-status dependency walk (`0863a2d`)
- **fix:** correct operator precedence in build-schema validation (`588e249`)
- **fix:** use safe directory move for draft, discard, and archive (`91da170`)
- **fix:** correct source priority in depends-on traversal (`01f4f0d`)
- **fix:** clean up empty parent directories after unscaffold (`f60dab3`)
- **fix:** remove orphaned spec-depends-on entries on spec id update (`e7faf7d`)
- **fix:** copy primary file when archiving new delta-capable specs (`79ada18`)
- **fix:** run post hooks for source state and add granular skip-hooks (`9a4ada7`)
- **fix:** auto-detect vcs adapters in kernel composition (`76cb5fd`)
- **fix:** sort overlap entry changes by name per contract (`25b7b7c`)
- **fix:** add exhaustive check and defensive copy in domain types (`94ff9d5`)
- **fix:** correct type assertion in detect-spec-overlap test (`b21e059`)
- **fix:** fall back to archive in post-archive hooks (`3f6a020`)
- **fix:** normalize schema override hooks and remove config workflow (`7cd459b`)
- **fix:** clarify modified vs added semantics in delta instructions (`69fd0c1`)
- **refactor:** make delta merger schema-driven — DeltaConfig replaces hardcoded section names (`70f1c2c`)
- **refactor:** redesign domain layer for lazy content loading (`b06fb73`)
- **refactor:** encapsulate segments — expose to-fs-path(sep) instead (`9459ee9`)
- **refactor:** encapsulate domain fields behind getters (`8ce8e06`)
- **refactor:** promote parent, child, and is-ancestor-of to domain-path (`0006d63`)
- **refactor:** extract domain-path base class from spec-path (`4235f55`)
- **refactor:** align domain model with current specs (`162ff80`)
- **refactor:** rewrite change entity with event-sourcing history model (`97aad70`)
- **refactor:** add artifact scope, rename workflow step field (`ce914f6`)
- **refactor:** validate schema yaml with zod in fs-schema-registry (`8c3f6fa`)
- **refactor:** rename scope to workspace across domain and ports (`2fa17d3`)
- **refactor:** extract rule-evaluator and add spec-summary service (`a3ea26f`)
- **refactor:** extract shared infer-format utility (`a66689a`)
- **refactor:** move snapshot-hasher to infrastructure layer (`cdb646d`)
- **refactor:** relocate delta-application-error and shift-headings (`9cd4175`)
- **refactor:** extract shared workspace-context type (`793d8bc`)
- **refactor:** deduplicate enoent check into shared helper (`2f29837`)
- **refactor:** import shift-headings from domain directly (`f115628`)
- **refactor:** extract shared parse-spec-id to domain services (`fa0b81b`)
- **refactor:** extract shared atomic file write utility (`e4d7bb7`)
- **refactor:** move yaml parsing and content hashing to domain services (`598c1b2`)
- **refactor:** extract content hasher port from domain (`ab27dd3`)
- **refactor:** replace archive repo test cast with stub class (`b281ef7`)
- **refactor:** replace remaining inline spec-id parsing (`65f9aa8`)
- **refactor:** adopt colon-only spec-id parsing (`db75a0c`)
- **refactor:** route infra exports through composition layer (`58f26e6`)
- **refactor:** move yaml parsing out of domain into application shared (`307cdc9`)
- **refactor:** reuse check metadata freshness in get-project-context (`9b59fa6`)
- **refactor:** extract shared add-context type (`3fab3ef`)
- **refactor:** add spec context and inference use cases (`3fab3ef`)
- **refactor:** minor code quality fixes (`89a77cf`)
- **refactor:** replace untyped mock casts in tests (`ae7f1e8`)
- **refactor:** replace untyped mocks with typed factories (`1b31447`)
- **refactor:** extract duplicated helpers to shared modules (`2a65d06`)
- **refactor:** remove direct yaml imports from use case files (`07af823`)
- **refactor:** move missing-default-workspace error to domain (`6f1c459`)
- **refactor:** convert artifact-type methods to getters (`8a82007`)
- **refactor:** move context spec id resolution into use case (`c1d7442`)
- **refactor:** resolve depends-on dynamically in compile-context (`1772a6f`)
- **refactor:** share adapter instances across kernel use cases (`589f949`)
- **refactor:** move filesystem i/o from cli to core ports (`d76a0e8`)
- **refactor:** remove domain error re-exports from port files (`4ab291f`)
- **refactor:** move config-derived inputs from execute() to constructors (`1c4d2e8`)
- **refactor!:** replace git adapter with technology-neutral vcs port (`fec3db6`)
- **refactor!:** decouple actor identity from git adapter (`72b6adf`)
- **refactor!:** compute workspaces from spec ids (`192d5cb`)
- **refactor:** separate metadata from spec artifacts in spec repository (`d0993e8`)
- **refactor:** extract schema repository port from schema registry (`0aa877a`)
- **refactor:** extract schema parsing and building from registry (`cab0ea7`)
- **refactor:** propagate schema errors instead of swallowing them (`1a39221`)
- **refactor:** replace schema registry triple with schema provider (`bd3329a`)
- **refactor!:** use colon as workspace:path separator (`b6eddb1`)
- **refactor:** element-wise path comparison in apply-delta (`6f82c3c`)
- **refactor:** remove confusing shift-headings re-export from use-cases (`9b4106f`)
- **refactor:** use spread instead of delete in invalidate-spec-metadata (`16b4bca`)
- **revert:** remove to-fs-path — node:path forbidden in domain layer (`78639fa`)
- **test:** add RENAMED, conflict detection, and deltaOperations tests for mergeSpecs (`93344f9`)
- **test:** add domain tests — entities, value objects, errors (`cc05afc`)
- **test:** group archive-change tests into given/when/then blocks (`11dbad2`)
- **test:** add spec-depends-on round-trip tests for change repository (`c1be781`)
- **test:** add missing use case and infrastructure tests (`80c2768`)
- **test:** add tests for domain services missing coverage (`8546514`)
- **test:** replace conditional assertions with unconditional expects (`4c13399`)
- **test:** complete port mocks and strengthen test assertions (`8ef1ed6`)
- **test:** fix test file naming and directory placement (`3e6b2d4`)
- **test:** protect temp dirs with try-finally in integration tests (`6472917`)
- **test:** consolidate duplicated test factories into shared helpers (`8ec5c7a`)
- **test:** replace as-never casts with typed mock factories (`8134594`) (note: in cli)
- **test:** import kernel type and remove stale compile-context entry (`12c6ee8`)
- **perf:** parallelize archive scanning i/o with promise.all (`3e94bde`)
- **perf:** parallelize directory stat calls (`3456223`)
- **perf:** eliminate n+1 manifest reads in archive list (`52ba695`)
- **build:** add zod dependency (`c6dd780`)
- **chore:** regenerate archive-change spec metadata (`93bdf4b`)
- **chore:** add readonly to manifest infrastructure types (`634e143`)
- **chore:** regenerate schema-registry-port metadata (`3c76208`)
- **chore:** refresh stale .specd-metadata.yaml across core specs (`fc63443`)
- **chore:** update spec metadata files (`8b76391`)
- **chore:** refresh spec metadata snapshots (`60c8192`)

### @specd/cli

- **feat:** add cli infrastructure and shared utilities (`6b08346`)
- **feat:** add all command groups (`102f262`)
- **feat:** add schema fork and schema extend commands (`c575d5f`)
- **feat:** add schema validate command (`3fb4e87`)
- **feat,core:** add --status flag to spec list command (`396437e`)
- **feat:** add --all flag to change validate for batch spec validation (`cce911c`)
- **feat:** add --all and --status flags to spec generate-metadata (`4e55c77`)
- **feat:** unify error handling with structured output for json and toon (`bb825d6`)
- **feat:** add copilot and codex to known agents (`3120ce8`)
- **feat:** overhaul entrypoint with global config, dashboard, and banner (`9b7890e`)
- **feat:** add progress bar to graph index command (`202e502`)
- **feat:** add graph commands for code intelligence (`aaaf7a1`)
- **feat:** enhance graph commands with impact modes and lifecycle (`b9ebf61`)
- **feat:** add --no-hooks flag and progress rendering to transitions (`99099dc`)
- **feat:** add change check-overlap command and inline overlap warnings (`2214478`)
- **feat:** reject readonly specs in change create and edit (`b8c0728`)
- **feat:** add change spec-preview command (`a29ebf1`)
- **feat:** add [ref] positional arg to schema show and validate (`621f384`)
- **fix:** use kernel registry for schema fork/extend (`698b4e7`)
- **fix:** add null separator between spec artifacts in content hash (`28248a9`)
- **fix:** validate --direction option in graph impact command (`fa5ef1e`)
- **fix:** clamp progress percentage in graph index tty bar (`d391f3d`)
- **fix:** enforce valid choices for --kind and --min-risk options (`92d0302`)
- **fix:** exit graph commands after native addon cleanup (`4e52750`)
- **fix:** align change-artifacts tests with cli-context mock pattern (`94e0cbe`)
- **fix:** serialize full config in config show (`95af87c`)
- **fix:** verify change is discarded in `discarded show` (`f94bd8d`)
- **fix:** replace double casts with structural mocks in tests (`6c077d5`)
- **fix:** throw error instead of process.exit in format parser (`7a8410d`)
- **fix:** remove duplicate banner render in project overview command (`5994e37`)
- **fix:** resolve context spec ids from metadata on transition (`ea86928`)
- **fix:** handle delta-application-error in error handler (`e419f37`)
- **fix:** extend handle-error with missing typed errors (`dbf47eb`)
- **fix:** narrow union type in spec resolve-path command (`e51d07f`)
- **fix:** read schema version from loaded schema (`7bbd97b`)
- **fix:** import fs/promises instead of sync fs (`1002134`)
- **fix:** use cli_version instead of hardcoded version (`756979e`)
- **fix:** fix workspace hardcoding in change archive (`1a801f2`)
- **fix:** use type import for command in change approve (`869290a`)
- **fix:** validate change state input at cli boundary (`fcc5ca1`)
- **fix:** reject excess positional arguments on all leaf commands (`0800ee1`)
- **fix:** add superseded-by column to discarded list text output (`48b9edf`)
- **fix:** add workspace and actor columns to archive list text output (`f7c10b8`)
- **fix:** include keywords in spec metadata json output (`4596acc`)
- **fix:** harden file path parsing in graph search output (`5e514e5`)
- **fix:** harden hotspots file path parsing and index empty guard (`a1c34da`)
- **fix:** align spec metadata help schema with core types (`6d5e8be`)
- **fix:** address pr review feedback for cli commands (`f8a368f`)
- **fix:** enforce exactly one target selector in graph impact (`b90dd08`)
- **fix:** correct command descriptions and error handling consistency (`7072c45`)
- **fix:** exclude self from overlap warning in change archive (`fa6d3c6`)
- **refactor:** rename --status to --metadata-status in spec list (`46db9c7`)
- **refactor:** consolidate config+kernel bootstrap into shared helper (`1f96925`)
- **refactor:** move config-derived inputs from execute() to constructors (`1c4d2e8`)
- **refactor:** extract shared cli utilities and remove duplication (`94d645e`)
- **refactor:** extract shared cli utilities (`5c50ad7`)
- **refactor:** use more specific name for metadata status filter set (`c191966`)
- **refactor:** replace inline yaml parsing with core domain service (`7237b74`)
- **refactor:** delegate business logic to core use cases (`fb190df`)
- **refactor:** use core's parse metadata and content hasher (`02a6255`)
- **refactor:** deduplicate index-of call in hotspots json output (`29cef4e`)
- **refactor:** restrict banner to project init and overview commands (`0435475`)
- **refactor:** rewrite command descriptions for clarity (`7abeeab`)
- **refactor:** clarify command descriptions for accuracy (`167ce0c`)
- **refactor:** expose config file path from cli context (`18b7b49`)
- **refactor!:** use colon as workspace:path separator (`b6eddb1`)
- **test:** add spec-derived command tests from verify.md scenarios (`8f989e0`)
- **test:** fix dashboard missing-config test to assert exit code 1 (`8ce19e6`)
- **test:** throw sentinel error in process.exit mock (`975a1fe`)
- **test:** use typed errors in cli command test mocks (`040759b`)
- **test:** replace as-never casts with typed mock factories (`8134594`)
- **test:** import kernel type and remove stale compile-context entry (`12c6ee8`)
- **perf:** parallelize multi-symbol impact analysis in graph impact (`393a244`)
- **chore:** remove unused eslint-disable directives (`5bd01ae`)
- **chore:** add .specd-metadata.yaml for spec-list (`acb42de`)
- **chore:** update spec metadata files (`2842075`)

### @specd/code-graph

- **feat:** implement code-graph package (`2a4bf4b`)
- **feat:** add python language adapter (`42a57a7`)
- **feat:** add go language adapter (`4011d55`)
- **feat:** add php language adapter (`ff32111`)
- **feat:** add calls resolution and import parsing (`f21efdd`)
- **feat!:** integrate with specd workspace system (`ba2a979`)
- **feat!:** make indexer language-agnostic (`1794f37`)
- **feat!:** add full-text search, replace graph find (`b5cafa3`)
- **feat:** add hotspot scoring to rank symbols by impact (`2a98beb`)
- **feat:** tokenize symbol names for full-text search (`f3528f1`)
- **feat!:** add exclude filters, push search filters to store (`1c221c8`)
- **feat:** remove all defaults when any filter flag is provided (`2fc9e30`)
- **feat:** add affected symbols to impact results (`6cd47a6`)
- **feat:** resolve affected symbols in import-linked files (`c722dd8`)
- **feat:** handle default and namespace imports in ts adapter (`9761845`)
- **feat:** extract php class properties as variable symbols (`97c79e7`)
- **feat:** support index.ts fallback for directory imports (`9fdde17`)
- **feat:** add graph staleness detection via vcs ref comparison (`46b5f85`)
- **feat:** use spec repository metadata port for spec resolution (`e353e6d`)
- **fix:** correct escape order in cypher query builder (`93eb337`)
- **fix:** handle jsx/tsx extensions in ts import path resolution (`7c5b20f`)
- **fix:** recognize function expressions as enclosing scope (`59fda1c`)
- **fix:** recurse into block-style php namespaces (`9a539e3`)
- **fix:** wrap upsert file in transaction for atomicity (`8aea356`)
- **fix:** strip all "../" prefixes in spec dep paths (`5071bf9`)
- **fix:** include column in symbol id, harden adapters (`3339bde`)
- **fix:** fix direction bug, glob escaping, and guards (`7cba7fe`)
- **fix:** address pr review feedback for domain and infra (`1ac87c4`)
- **fix:** scope nested .gitignore rules to their directory (`49494ef`)
- **fix:** disambiguate python call edges by enclosing scope (`ffc6e1b`)
- **fix:** separate file and symbol dependent counts in impact (`e232476`)
- **fix:** defensively copy embedding in file node factory (`3eef230`)
- **fix:** anchor glob regex to prevent substring matches (`65b9b23`)
- **fix:** drop restrictive hotspot defaults on scope filter (`223b8ec`)
- **fix:** always run spec deletion pass even when none discovered (`79f4b42`)
- **fix:** fallback to index.ts for directory imports (`96f4d46`)
- **fix:** include column in symbol dedup key across all adapters (`54ce4c7`)
- **fix:** let bulk-load failures abort the indexing run (`9454087`)
- **fix:** wrap bulk-load in transaction for atomicity (`cf0e088`)
- **fix:** align test store glob and workspace matching (`4650db1`)
- **fix:** load unchanged file symbols into index for pass 2 (`a8f8f5a`)
- **fix:** resolve depends-on relations across workspaces (`e084043`)
- **fix:** count files-removed after successful deletion only (`9de6b21`)
- **fix:** set last-indexed-at after commit in bulk-load (`c467127`)
- **fix:** harden graph store escape and commit ordering (`aad6175`)
- **fix:** wrap upsert-spec in transaction for atomicity (`98e35a5`)
- **fix:** support gitignore negations from child directories (`4f7c6f0`)
- **fix:** collect all package identities per workspace (`018012c`)
- **fix:** include threshold options in hotspot default removal (`08486e6`)
- **fix:** use lstat to detect symbolic links in file discovery (`56913be`)
- **fix:** remove spec.md fallback, use metadata-only for title (`13597ee`)
- **fix:** pre-prune deleted specs and detect identity collisions (`ae6e08d`)
- **fix:** separate workspace prefix in relative path resolution (`301eb81`)
- **fix:** harden store open and close lifecycle (`2af573d`)
- **fix:** exclude methods from unqualified python call resolution (`6b7bdc7`)
- **fix:** restructure hotspots verify.md for schema (`15d798d`)
- **fix:** work around tinypool channel closed in test runner (`85348ba`)
- **refactor:** remove discover-specs and filesystem fallback (`26a3256`)
- **refactor:** use core's parse metadata and content hasher (`9a980f1`)
- **refactor:** deduplicate risk order and max risk helper (`88c09bd`)
- **perf:** optimize indexing with bulk csv loading (`af264e6`)
- **perf:** use set for deleted files lookup during cleanup (`d41241b`)
- **perf:** scope hotspot symbol scan by workspace and kind (`394b758`)
- **perf:** hoist spec query outside workspace loop (`02afcb7`)
- **test:** improve test hygiene and lifecycle checks (`3de1c34`)
- **test:** fix tests per second review pass (`32e5bdb`)
- **test:** improve test coverage per pr review feedback (`f7a0566`)
- **chore:** add tree-sitter language grammar dependencies (`bfea34e`)
- **chore:** regenerate metadata for all specs (`458ab85`)
- **chore:** add deprecation tag and clarifying comments (`3bce115`)
- **chore:** regenerate language-adapter spec metadata (`68775e3`)

### @specd/skills

- **feat:** add list and get skill functions (`9776239`)
- **feat:** add scope detection to specd lifecycle skill (`bdb7e83`)
- **feat:** add scoped audit modes to specs-compliance skill (`a2474ca`)
- **chore:** add missing name frontmatter to skills (`76803a4`)
- **chore:** rename specd-design skill to specd (`1ce12b9`)
- **chore:** update skill definitions for v2 and v3 (`01ab63d`)
- **chore:** add state routing and session tasks to v3 (`a7dd07f`)
- **chore:** move shared.md reference to .specd/skills/ (`0d7d07d`)
- **chore:** add welcome block and spec registration guidance to v3 (`26c909d`)
- **chore:** add hooks for all lifecycle states (`3fb6ba5`)
- **chore:** update specd-spec-metadata skill for json format (`d506963`)
- **chore:** always use --summary with spec list in skills (`0448e31`)
- **chore:** add mental model and no-op delta guidance to skills (`f2a0774`)
- **chore:** improve agent ux for specd lifecycle skills (`6ad9a51`)
- **chore:** add exploration context, spec deps, and workspace guards (`953fc0e`)

### @specd/schema-std

- **feat:** implement standard specd schema with templates (`433573e`)
- **feat:** add kind, id, and separate instructions (`810a65f`)
- **feat:** add spec dependency tracking to proposal artifact (`8d34544`)
- **feat:** add ready step with requires gating to workflow (`a664d39`)
- **feat:** add cross-verification post-rules to artifact dag (`63c0d9b`)
- **feat:** enrich schema show and artifact-instruction (`eb94925`)
- **fix:** clarify artifact paths and verifying requires (`c5ab1c1`)
- **fix:** clarify modified vs added in delta instructions (`1c22d0a`)
- **fix:** add verify to ready step requires array (`c2c3561`)
- **fix:** add ready-guidance pre-hook to workflow (`4a814c5`)
- **fix:** preserve indentation in checkbox cleanup regex (`5338f42`)
- **fix:** align ready-guidance with required design artifact (`3d8c4a1`)
- **refactor:** move design instructions to template comments (`b31ebf4`)
- **refactor:** remove tool-specific run hooks (`f6625dd`) (note: `f6c84f3`)
- **chore:** add spec deps instruction and update templates (`e4694e4`)

### Root / All Packages

- **feat(root):** add composition layer and eslint boundary enforcement (`96c27ad`)
- **feat(all):** add --next transition flow and approval-boundary errors (`5b56e4a`) (note: `5b3e56a`)
- **feat(all):** add configurable traversal depth and depth tracking (`4102b4f`)
- **feat(all)!:** add deterministic metadata extraction via extractors (`d04ca6a`)
- **fix(root):** rename artifact rule text to instruction (`31fefb5`)
- **fix(root):** correct schema key name in specd.yaml (`35554df`)
- **fix(all):** align --next transition handling and fail-fast docs (`423a1d7`)
- **fix(all):** add line to affected symbols, update impact spec and format (`f6625dd`)
- **refactor(all)!:** replace inline import types and add missing jsdoc (`bb8d137`)
- **refactor(root):** rename artifact generates field to output (`003bafd`)
- **refactor(root):** extract change manifest format to change-manifest spec (`3b03556`)
- **refactor(root):** move config and schema-format specs from \_global to core (`30d120e`)
- **refactor(core,cli):** minor code quality fixes (`89a77cf`)
- **refactor(core,cli):** replace untyped mock casts in tests (`ae7f1e8`)
- **refactor(core,cli):** replace untyped mocks with typed factories (`1b31447`)
- **docs(root):** add storage ADRs and spec (`3c1d20a`)
- **docs(root):** add schema format spec and ADR-0010 (`7436079`)
- **docs(root):** refine schema format spec — validation, duplicates, generates semantics (`8ecfb69`)
- **docs(root):** add spec layout spec and ADR-0011, update plan and proposal (`2fb0593`)
- **docs(root):** add commit granularity requirement to commits spec (`5417fca`)
- **docs(root):** add commit body format requirement to commits spec (`5168fae`)
- **docs(root):** generalize architecture spec to all packages (`9b16138`)
- **docs(root):** add scenarios to all global specs, fix testing spec (`cfd0446`)
- **docs(root):** move storage spec from \_global to specs/core (`c8f3922`)
- **docs(root):** add contributing guide (`935cc40`)
- **docs(root):** rename project to specd in public documentation (`164dee3`)
- **docs(root):** add change layout spec defining change directory structure (`670cd3c`)
- **docs(root):** add jsdoc to all domain layer source files (`0d80a8b`)
- **docs(root):** add eslint, jsdoc, and commitlint specs (`fb2d0d8`)
- **docs(root):** add adrs 0012-0014 for config, workspaces, storage (`e205028`)
- **docs(root):** add lazy loading convention to coding conventions spec (`706166a`)
- **docs(root):** refine schema reference and add schema version tracking (`f1bb0cf`)
- **docs(root):** --config bypasses local override, no specd.local.yaml lookup (`638abe8`)
- **docs(root):** expand schema requirements in config spec (`d83a4c9`)
- **docs(root):** simplify local config — full replacement, no merging (`267512b`)
- **docs(root):** add specd.local.yaml override spec with merge semantics (`74fef5e`)
- **docs(root):** refine config discovery — bound to git root, add --config (`5f39d0f`)
- **docs(root):** add config spec — specd.yaml structure and validation rules (`3ea5eaa`)
- **docs(root):** update adr format spec to use yaml frontmatter (`f661c76`)
- **docs(root):** convert adr metadata from table to yaml frontmatter (`4b7f9f7`)
- **docs(root):** fix pattern table and examples in config spec (`c79468a`)
- **docs(root):** clarify code root is required for non-default workspaces (`c4494f1`)
- **docs(root):** set workspace-level context include default to all specs (`14a7b3d`)
- **docs(root):** add direct spec path scenarios to context selection verify (`db19bf0`)
- **docs(root):** restructure startup validation in config spec (`b365684`)
- **docs(root):** add two-level context spec selection to config (`e4496ad`)
- **docs(root):** update config examples with context spec selection (`a355312`)
- **docs(root):** include order determines context priority in spec selection (`18efcfb`)
- **docs(root):** add context spec selection to config spec (`9c04bb1`)
- **docs(root):** align adrs and adr spec to canonical madr format (`f1e0734`)
- **docs(root):** migrate all adrs to madr format, update adr format spec (`f097bfe`)
- **docs(root):** update agents.md with full spec list and verify.md note (`f834226`)
- **docs(root):** migrate all specs to spec+verify two-file format (`0a1d71d`)
- **docs(root):** rework config spec — adapter pattern, workspaces, templates (`24d86d2`)
- **docs(root):** rework schema-format spec with verify artifact (`0939bde`)
- **docs(root):** define spec+verify two-file format in spec-layout (`336d173`)
- **docs(root):** active workspace derives from change metadata, not fs scan (`6dde009`)
- **docs(root):** define active workspace and harden unknown qualifier check (`45ef2f1`)
- **docs(root):** clarify workspace config independence and manual sync (`e066f47`)
- **docs(root):** two minor hardening tweaks to context spec selection (`fe06e81`)
- **docs(root):** add change domain spec (`08369f3`)
- **docs(root):** add creation timestamp to change identity (`469120f`)
- **docs(root):** add configurable approval gates to change lifecycle (`a3a962e`)
- **docs(root):** redesign approval gates as spec and implementation (`3db58c1`)
- **docs(root):** rename implementation approval gate to signoff (`8ad7ffb`)
- **docs(root):** fix coherence issues in change spec after signoff rename (`c23c952`)
- **docs(root):** rename pre-implementation scenario to spec approval (`175c9eb`)
- **docs(root):** revise change domain with approval history and storage (`3cca645`)
- **docs(root):** add discard record with identity and superseded-by to change (`055ffb6`)
- **docs(root):** add draft record with identity to change (`ee40446`)
- **docs(root):** redesign change spec with event-sourcing history model (`fa2d027`)
- **docs(root):** add manifest format and context spec ids to storage spec (`d863259`)
- **docs(root):** add spec-metadata spec for .specd-metadata.yaml (`b34b25f`)
- **docs(root):** add spec-metadata agent working document (`0736cc5`)
- **docs(root):** note write permission requirement in spec-metadata agent (`c1bf46e`)
- **docs(root):** add .specd-metadata.yaml to all 15 spec directories (`fd9fe9b`)
- **docs(root):** rename workflow skill to step in specs (`3233324`)
- **docs(root):** remove delta-merger and document parsedelta in delta-format (`ea9a3d3`)
- **docs(root):** allow layer-level barrel files in conventions spec (`50916f6`)
- **docs(root):** add yaml boundary validation requirement to specs (`de01933`)
- **docs(root):** add adr-0015 and composition layer spec (`1868be3`)
- **docs(root):** update core docs to reflect composition layer factories (`7e2c2c1`)
- **docs(root):** update port example with real fs adapter constructors (`a4863d8`)
- **docs(root):** clarify adr vs spec dependency model (`57915c2`)
- **docs(root):** fix testing spec — move esm reference to spec dependencies (`b4571c8`)
- **docs(root):** document underscore prefix convention for backing fields (`ee0d464`)
- **docs(root):** add encapsulation requirement to architecture spec (`ebec68a`)
- **docs(root):** rename port specs from port-x to x-port convention (`c35620a`)
- **docs(root):** add foundational specs for workflow, hooks, and templates (`1600662`)
- **docs(root):** add specs for hook execution and artifact instruction (`d911087`)
- **docs(root):** update specs for hook execution in transitions and archives (`ed0201c`)
- **docs(root):** update specs for multi-file artifact tracking (`41c5459`)
- **docs(root):** add workspace spec with verification scenarios (`9a84aff`)
- **docs(root):** update specs for configurable depth and selector validation (`7d2397b`)
- **docs(root):** extract selector model into standalone spec (`b83046f`)
- **docs(root):** add compliance gates and multi-workspace sections to readme (`2182ef8`)
- **docs(root):** update readme with current implementation status (`669828c`)
- **docs(root):** rename overview section to purpose in all specs (`30d4c89`)
- **docs(root):** add adrs 0016, 0017, and 0018 (`71df382`)
- **docs(root):** add quality gate to spec-metadata skill (`78d33c1`)
- **docs(root):** reformat core docs tables with prettier (`1f8c475`)
- **docs(root):** regenerate specd-metadata for 8 specs (`49e4806`)
- **docs(root):** add change-layout spec defining change directory structure (`670cd3c`)
- **docs(root):** update specd-spec-metadata skill definition (`9956e3d`)
- **docs(root):** split specd lifecycle into focused skills (`505abf5`)
- **docs(root):** clarify write-read sequencing in specd shared skill (`1b1e19b`)
- **docs(root):** update specs for readonly workspace ownership enforcement (`34c5e78`)
- **docs(root):** add spec overlap handling to skill definitions (`7f857e6`)
- **docs(root):** fix specs, docs, and metadata accuracy (`79e04ed`)
- **docs(root):** fix typos and formatting in spec files (`c71cf0f`)
- **docs(root):** update schema fork/extend docs for new signature (`2e206aa`)
- **docs(all):** align specs with constructor-injection refactor (`5b85083`)
- **docs(all):** update specs and metadata for workspace:path format (`54690d7`)
- **docs(all):** fix code fences, versions, and spec inaccuracies (`c6abf5d`)
- **docs(all):** fix spec examples and align metadata with implementation (`48a998b`)
- **docs(all):** update specs for configurable depth and selector validation (`7d2397b`)
- **docs:** rewrite and expand project documentation (`e7e6f23`)
- **docs(core):** add specs for schema customisation (`1b9fee2`)
- **docs(core):** add specs for parse-schema-yaml and build-schema (`98c3635`)
- **docs(core):** add specs for all use cases, ports, and kernel contract (`b58a9a3`)
- **docs(core):** add generate-metadata specs for core and cli (`e2fe487`)
- **docs(core):** add schema-format to delta-format spec dependencies (`4596acc`) (note: separate from cli fix)
- **docs(core):** add archive-change, compile-context and validate-artifacts (`dae3eb8`)
- **docs(core):** add spec dependencies delta to schema-format (`172f11e`)
- **docs(core):** add artifact-ast and delta-format specs (`817ef0b`)
- **docs(core):** update validate-artifacts rule evaluation to selector model (`ce63c39`)
- **docs(core):** migrate validations and contextsections to selector model (`08d1746`)
- **docs(core):** add rendersubtree to artifactparser; update archive-change (`48a2df3`)
- **docs(core):** clarify selector and content semantics in delta-format (`9dff84e`)
- **docs(core):** add llmoptimizedcontext field to config spec (`21fe45d`)
- **docs(core):** add skipped artifact state with sentinel hash (`45c8086`)
- **docs(core):** emit warning on missing file in config context (`04d102f`)
- **docs(core):** fix validate-artifacts reference in constraint (`0715ec0`)
- **docs(core):** add rules, constraints, scenarios to spec-metadata spec (`f2aba0c`, `70fd66e`)
- **docs(core):** add snapshot-hasher spec (`07d012e`)
- **docs(core):** correct effectivestatus jsdoc to include skipped state (`6ee500e`)
- **docs(core):** correct archive repository contract in archive-change spec (`02c104f`)
- **docs(core):** update port wiring example to use kernel and config loader (`2e5f240`)
- **docs(core):** fill empty jsdoc blocks (`856a509`)
- **docs(core):** document dummy workspace fields on change-repository (`52de7e8`)
- **docs(core):** align specs with canonical spec-id format (`4dd13bd`)
- **docs(core):** refresh spec metadata (`3026a90`)
- **docs(cli):** add specs for all cli commands (`54d8e64`)
- **docs(cli):** remove hook execution from change-transition (`a8a272b`)
- **docs(cli):** add output schema to all command help texts (`716b102`)
- **docs(cli):** fix command descriptions and jsdoc in cli commands (`1c56a18`)
- **docs(cli):** make `--spec` flag optional in change-create spec (`68b3a55`)
- **docs(cli):** fix spec-list json field name to metadatastatus (`9a6212b`)
- **docs(cli):** reformat spec-validate verify to standard structure (`1a92460`)
- **docs(cli):** improve root program description with skill workflow context (`c75e0ad`)
- **docs(schema-std):** sync verify delta when requirement removed from spec (`1f2da32`)
- **docs(schema-std):** require scenarios for new requirements in verify delta (`76a8507`)
- **docs(schema-std):** clarify workspace prefix in artifact path instructions (`198f125`)
- **docs(schema-std):** update schema instructions and templates (`ed90cb8`)
- **docs(code-graph):** add specs for @specd/code-graph package (`0dfaa1c`)
- **docs(code-graph):** update specs for current implementation (`804ee2b`)
- **docs(code-graph):** fix indexer spec heading to say two-pass extraction (`df180f7`)
- **docs(code-graph):** align workspace-integration spec with implementation (`76acb3a`)
- **docs(code-graph):** clarify core is a runtime dependency, not type-only (`374c152`)
- **docs(code-graph):** align indexer spec with actual spec resolution logic (`c953f13`)
- **docs(code-graph):** fix workspace name in hotspots verify scenario (`255bc78`)
- **docs(code-graph):** add todo for process count in risk computation (`86f9bc4`)
- **docs(code-graph):** document python symbol name shadowing limitation (`54f1224`)
- **docs(code-graph):** add comment field to symbol model specs (`ec6cca1`)
- **docs(code-graph):** add get-language-for-file to adapter registry spec (`143d0d4`)
- **docs(code-graph):** fix is-relative definition in language-adapter spec (`15c7a50`)
- **docs(code-graph):** document import map value format in language-adapter (`9107774`)
- **docs(code-graph):** clarify stateless provider in composition spec (`8b1c928`)
- **docs(code-graph):** fix spec-hash and fallback scenarios in indexer verify (`6cbe7d8`)
- **docs(code-graph):** remove spec.md fallback from symbol-model spec (`9094484`)
- **docs(code-graph):** clarify core error propagation in symbol-model (`5ca91bc`)
- **docs(skills):** add specd-design lifecycle skill to tracked skills (`c908284`)
- **docs(skills):** update spec-metadata skill for depends-on preservation (`0ce5d84`)
- **chore(root):** initial project scaffold (`66dd43a`)
- **chore(root):** fix pnpm workspace esbuild config (`8f40d01`)
- **chore(root):** format all files with prettier (`c1561fe`)
- **chore(root):** add eslint, prettier, commitlint, and husky tooling (`cf47918`)
- **chore(root):** reformat eslint config with prettier (`b438219`)
- **chore(root):** exclude .history from eslint and gitignore (`74cb16b`)
- **chore(root):** add typecheck to lint script and lint-staged (`ba01cb6`)
- **chore(root):** widen jsdoc and filename-case lint rules to all src/ (`508e0be`)
- **chore(root):** add mit license (`432f1c5`)
- **chore(root):** update agent docs and refresh spec metadata files (`706f13d`)
- **chore(root):** add code-graph workspace dependencies (`e909e37`)
- **chore(root):** add code-graph to commitlint scope-enum (`69010c5`)
- **chore(root):** include code-graph in test filter (`6516dbc`)
- **chore(root):** add .codereview/ to gitignore (`40b2b3d`)
- **chore(root):** add .codex to gitignore (`e0ba53e`)
- **chore(root):** update specd-metadata files and add .codex to gitignore (`e0ba53e`)
- **chore(root):** remove test-broken-spec directory (`d188428`)
- **chore(root):** update specd-spec-metadata agent to use --metadata-status (`212c317`)
- **chore(root):** reorganize ai-agent skills and update gitignore (`da50095`)
- **chore(root):** add coderabbit config to ignore specd metadata (`df2fa7e`)
- **chore(root):** exclude specd metadata directory from coderabbit reviews (`231ecd2`)
- **chore(root):** add specd project configuration (`5d4d4e4`)
- **chore(root):** track spec metadata in .specd/metadata/ (`29806f6`)
- **chore(root):** regenerate all spec metadata (`718f1bb`)
- **chore(root):** regenerate all spec metadata with workspace-qualified deps (`8da0cf3`)
- **chore(root):** skip lint and test for inactive packages (`190bc77`)
- **chore(root):** regenerate spec metadata for 24 stale specs (`e9ec20c`)
- **chore(root):** regenerate spec metadata for 18 stale specs (`360e359`)
- **chore(root):** migrate metadata files from yaml to json (`d58fb5b`)
- **chore(root):** regenerate metadata for stale and missing specs (`5be909a`)
- **chore(root):** add specd lifecycle skill definitions (`068dc93`)
- **chore(root):** restructure specd.yaml context and schema overrides (`f7fe80d`)
- **chore(root):** add jsdoc reminder to design artifact rules (`3a237af`)
- **chore(root):** update specd-implement skill definition (`9e0b282`)
- **chore(root):** remove stale change-overlap spec from bad archive (`044bfe0`)
- **chore(root):** re-archive spec-contention-detection change (`446c9f1`)
- **chore(root):** fix typo in specd.yaml schema override (`6abe3d3`)
- **chore(root):** update specd skills with skip-hooks support (`ceb77aa`)
- **chore(root):** add specd.local.yaml to gitignore (`1db7ca3`)
- **chore(root):** regenerate stale workspace spec metadata (`6eea7f2`, `9d4346c`)
- **chore(root):** update shared skill definitions (`0ae8030`)
- **chore(root):** move shared skill notes to top-level path (`71f0f5e`)
- **chore(all):** regenerate stale spec metadata (`ec0f741`, `b85cd5e`, `75e2ace`)
- **chore(all):** regenerate all spec metadata (`60ae492`)
- **chore:** add .opencode/ to gitignore (`ea128a9`)
