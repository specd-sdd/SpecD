title: Storage
description: "specd's storage layer is defined by port interfaces in `@specd/core`. All storage behavior is adapter-specific; the domain and application layers have no knowledge of where data lives. The v1 implementation ships one adapter for all storage ports: `fs` (local filesystem)."
dependsOn:
  - default:_global/architecture
  - core:core/change
  - core:core/change-manifest
context:
  - "specd's storage layer is defined by port interfaces in `@specd/core`. All storage behavior is adapter-specific; the domain and application layers have no knowledge of where data lives. The v1 implementation ships one adapter for all storage ports: `fs` (local filesystem)."
rules:
  - requirement: Change directory naming
    rules:
      - Active change directories must use the format `YYYYMMDD-HHmmss-<name>`, where the timestamp is the moment the change was created. The prefix must be a filesystem convention only — it must not appear in the domain model, the change manifest, or any CLI argument. `FsChangeRepository` must resolve a change by name using a glob pattern `*-<name>`.
  - requirement: Change directory listing order
    rules:
      - '`ChangeRepository.list()` must return changes in creation order (oldest first). With the `fs` adapter, this is achieved by sorting directory entries by name, which is chronological given the timestamp prefix.'
  - requirement: Artifact status derivation
    rules:
      - |-
        Artifact status (`missing`, `in-progress`, `complete`, `skipped`) must be derived at load time — it must not be stored directly in the manifest. The manifest stores only `validatedHash` per artifact. `FsChangeRepository` must compute status using this precedence:

        1. `validatedHash === "__skipped__"` → `skipped` (only valid for `optional: true` artifacts)
        2. File absent (and no sentinel) → `missing`
        3. File present and cleaned hash matches `validatedHash` → `complete`
        4. File present but hash differs or `validatedHash` is `null` → `in-progress`
  - requirement: Artifact dependency cascade
    rules:
      - '`Change.effectiveStatus(type)` must cascade through the artifact dependency graph. An artifact whose own hash matches its `validatedHash` must still be reported as `in-progress` if any artifact in its `requires` chain is neither `complete` nor `skipped`. A `skipped` optional artifact satisfies the dependency — it does not block downstream artifacts.'
  - requirement: ValidateArtifacts is the sole path to complete
    rules:
      - '`Artifact.markComplete(hash)` must only be called by the `ValidateArtifacts` use case. `Artifact.markSkipped()` must only be called by the skip use case (sets `validatedHash` to `"__skipped__"`). No other code path may set these values.'
  - requirement: Archive pattern configuration
    rules:
      - 'The `fs` archive adapter must support a configurable `pattern` field in `specd.yaml` under `storage.archive.pattern`. The pattern controls the directory structure within the archive root. Supported variables: `{{year}}`, `{{month}}`, `{{day}}`, `{{change.name}}`, `{{change.archivedName}}`. The default pattern must be `{{change.archivedName}}`.'
  - requirement: Scope excluded from archive pattern
    rules:
      - '`{{change.scope}}` must not be a supported archive pattern variable. Scope paths use `/` as a segment separator, which produces ambiguous slugs when normalized for use in directory names.'
  - requirement: Archive index
    rules:
      - '`FsArchiveRepository` must maintain an `index.jsonl` at the archive root. Each line must be a JSON object with `name` and `path` fields. The file must be kept in chronological order (oldest first, newest last) so that git diffs only show lines added at the bottom or lines removed — never reorderings. `archive(change)` must append one line at the end (O(1)). `get(name)` must scan the file from the end without loading it fully into memory; if not found, it must fall back to a recursive glob `**/*-<name>` and append the recovered entry. `reindex()` must be declared on the `ArchiveRepository` port. The `fs` adapter implements it by globbing all `manifest.json` files under the archive root, sorting entries by `archivedAt`, and writing a clean `index.jsonl` in chronological order. Other adapters implement it according to their storage mechanism. `specd storage reindex` calls the port method — it has no knowledge of the underlying implementation.'
  - requirement: Archive pattern date variables are zero-padded
    rules:
      - '`{{month}}` and `{{day}}` must be zero-padded to two digits. `{{year}}` is four digits. This ensures lexicographic sort produces chronological order.'
  - requirement: Change manifest format
    rules:
      - The format of `manifest.json` — its fields, event shapes, and schema version behavior — is defined in [`specs/core/change-manifest/spec.md`](../change-manifest/spec.md). `FsChangeRepository` reads and writes the manifest according to that format and must write it atomically (temp file + rename) to prevent partial reads.
constraints:
  - Manifest files must be written atomically (write to temp file, then rename) to prevent partial reads
  - Archive index entries must use forward slashes as path separators regardless of host OS
  - The timestamp in a change directory name must be derived from change.createdAt, not from the system clock at write time
scenarios:
  - requirement: Change directory naming
    name: Change created
    when:
      - a new change named add-auth is created at 2024-03-15T10:30:00
    then:
      - its directory is named 20240315-103000-add-auth
  - requirement: Change directory naming
    name: Change resolved by name
    when:
      - FsChangeRepository.get('add-auth') is called
    then:
      - it resolves the directory by globbing *-add-auth, not by storing the full path
  - requirement: Change directory listing order
    name: Multiple changes listed
    when:
      - list() is called with two changes created at different times
    then:
      - the older change appears first in the result
  - requirement: Artifact status derivation
    name: Artifact edited after validation
    when:
      - an artifact file is modified after ValidateArtifacts ran and stored its hash
    then:
      - FsChangeRepository recomputes the hash on next load and returns in-progress
  - requirement: Artifact status derivation
    name: Artifact file missing
    when:
      - an artifact file does not exist on disk and validatedHash is null
    then:
      - its status is missing
  - requirement: Artifact status derivation
    name: Artifact skipped — sentinel in manifest
    given:
      - 'an optional: true artifact with validatedHash: "__skipped__" in the manifest and no file on disk'
    when:
      - FsChangeRepository loads the change
    then:
      - its status is skipped
  - requirement: Artifact status derivation
    name: validatedHash cleared on rollback — skipped becomes missing
    given:
      - 'an artifact with validatedHash: "__skipped__" in the manifest'
    when:
      - an invalidated event is appended and all validatedHash values are cleared
    then:
      - its status becomes missing — the sentinel is gone and there is no file
  - requirement: Artifact status derivation
    name: validatedHash cleared on rollback — complete becomes in-progress
    given:
      - 'an artifact with validatedHash: "sha256:abc" and its file still present'
    when:
      - all validatedHash values are cleared
    then:
      - its status becomes in-progress — file present but no valid hash
  - requirement: Artifact dependency cascade
    name: Upstream artifact edited
    when:
      - artifact A is complete but its upstream dependency B is edited (becomes in-progress)
    then:
      - Change.effectiveStatus('a') returns in-progress
  - requirement: Artifact dependency cascade
    name: Upstream artifact skipped — downstream unblocked
    given:
      - 'artifact A requires optional artifact B, and B has validatedHash: "__skipped__"'
    when:
      - Change.effectiveStatus('a') is called
    then:
      - it returns A's own derived status — B's skipped state does not block A
  - requirement: ValidateArtifacts is the sole path to complete
    name: Attempt to mark complete outside ValidateArtifacts
    when:
      - any code other than ValidateArtifacts calls artifact.markComplete(hash)
    then:
      - it violates this requirement — the call must be removed
  - requirement: ValidateArtifacts is the sole path to complete
    name: Attempt to mark skipped outside skip use case
    when:
      - any code other than the skip use case sets validatedHash to "__skipped__"
    then:
      - it violates this requirement — the call must be removed
  - requirement: Archive pattern configuration
    name: Custom archive pattern
    when:
      - 'specd.yaml sets storage.archive.pattern: "{{year}}/{{change.archivedName}}"'
    then:
      - archived changes are placed under <archive-root>/2024/<archivedName>/
  - requirement: Archive pattern configuration
    name: Default archive pattern
    when:
      - no pattern is configured in specd.yaml
    then:
      - archives use {{change.archivedName}} as the directory name
  - requirement: Scope excluded from archive pattern
    name: Pattern uses scope variable
    when:
      - storage.archive.pattern contains {{change.scope}}
    then:
      - FsArchiveRepository must reject it as an unsupported variable
  - requirement: Archive index
    name: Change archived
    when:
      - archive(change) is called
    then:
      - one line is appended to index.jsonl — existing lines are not modified
  - requirement: Archive index
    name: Change not in index
    when:
      - get(name) scans index.jsonl and finds no match
    then:
      - it falls back to globbing **/*-<name> and appends the recovered entry to the index
  - requirement: Archive index
    name: reindex called
    when:
      - specd storage reindex is run after manual filesystem changes
    then:
      - index.jsonl is rewritten in chronological order based on each manifest's archivedAt
  - requirement: Archive pattern date variables are zero-padded
    name: January archive
    when:
      - a change is archived on January 5th
    then:
      - '{{month}} resolves to "01" and {{day}} resolves to "05"'
  - requirement: Change manifest format
    name: Manifest structure matches specification
    given:
      - an active change with history events
    when:
      - the change is persisted to disk
    then:
      - manifest.json contains the fields and structure defined in the change-manifest spec
contentHashes:
  spec.md: sha256:3cca70d83c24ff3837cc1d6758ddd50e7b05c24d57b7c3097efe776985f94e75
  verify.md: sha256:785e2517a5cdbfa31327baa503c34eaf87da3d7bc28ece40ee8c70ad639d05f6
generatedBy: core
