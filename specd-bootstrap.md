# specd Bootstrap Overview

> ⚠️ DRAFT — Generated by specd bootstrap on 2026-03-29. Review and refine before treating as binding.

---

## Project Overview

**specd** (Spec-Driven Development) is a platform for AI-native teams that enforces spec-driven workflows. It compiles the specs an AI agent needs at every lifecycle step — so context is delivered, not discovered, and compliance is structural, not hoped for.

The project is in **active development** (March 2026) and fully usable from source in the monorepo. Core packages (`@specd/core`, `@specd/cli`, `@specd/code-graph`, `@specd/schema-std`) are functional. The MCP server, plugin packages, and `@specd/skills` are scaffolded or in redesign.

---

## Tech Stack

| Concern            | Technology                                                                                    |
| ------------------ | --------------------------------------------------------------------------------------------- |
| Language           | TypeScript 5.7+                                                                               |
| Runtime            | Node.js >= 20                                                                                 |
| Module system      | ESM only (`"type": "module"`, `NodeNext` resolution)                                          |
| Package manager    | pnpm 10.6.5                                                                                   |
| Build tool         | Turbo + tsup                                                                                  |
| Test framework     | Vitest 3 (node environment)                                                                   |
| Linting            | ESLint 9 (typescript-eslint, eslint-plugin-unicorn, eslint-plugin-jsdoc)                      |
| Formatting         | Prettier 3                                                                                    |
| Commit hooks       | Husky + lint-staged                                                                           |
| Commit conventions | Conventional Commits (commitlint)                                                             |
| TypeScript config  | `strict: true`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitReturns` |

---

## Architecture Pattern

**Hexagonal Architecture (Ports & Adapters)** with DDD tactical patterns.

- `domain/` — pure TypeScript, zero I/O. Entities, value objects, domain services, errors.
- `application/` — use cases + port interfaces. No I/O; all external concerns go through ports.
- `infrastructure/` — concrete port implementations (fs, git, hg, svn, node, null).
- `composition/` — factory functions for DI wiring. Only entry points call these.
- Delivery mechanisms (`@specd/cli`, `@specd/mcp`, plugins) are independent adapters.

**Key enforced invariants (via ESLint `no-restricted-imports`):**

- `domain/` must not import from `application/`, `infrastructure/`, or `composition/`
- `application/` must not import from `infrastructure/` or `composition/`
- `infrastructure/` must not import from `composition/`

---

## Repository Structure

```
specd/                          ← monorepo root
├── packages/
│   ├── core/                   ← @specd/core — domain, application, infrastructure, composition
│   │   ├── src/domain/         ← entities, value-objects, domain services, errors
│   │   ├── src/application/    ← use cases, port interfaces, config types
│   │   ├── src/infrastructure/ ← fs, git, hg, svn, null, node, artifact-parser adapters
│   │   ├── src/composition/    ← factory functions, kernel, lazy wiring
│   │   └── test/               ← domain/, application/, infrastructure/, composition/ tests
│   ├── cli/                    ← @specd/cli — CLI adapter
│   │   ├── src/commands/       ← change/, spec/, graph/, schema/, skills/, project/, archive/, ...
│   │   ├── src/helpers/        ← cli-context, formatter, handle-error, spec-path, table
│   │   └── test/commands/      ← one spec file per command group
│   ├── code-graph/             ← @specd/code-graph — code indexing, impact analysis
│   │   ├── src/domain/         ← symbol model, ports, services (hotspots, traversal, impact)
│   │   ├── src/application/    ← IndexCodeGraph use case
│   │   ├── src/infrastructure/ ← ladybug (graph DB), tree-sitter (language adapters)
│   │   └── src/composition/    ← CodeGraphProvider factory
│   ├── schema-std/             ← @specd/schema-std — default schema.yaml + templates
│   ├── mcp/                    ← @specd/mcp — MCP server (scaffolded)
│   ├── skills/                 ← @specd/skills — skill registry (redesigning)
│   ├── schema-openspec/        ← @specd/schema-openspec — OpenSpec schema (scaffolded)
│   └── plugins/                ← @specd/plugin-{claude,copilot,codex} — scaffolded
├── specs/                      ← project specs (the authoritative requirements)
│   ├── _global/                ← 7 global constraint specs (architecture, conventions, etc.)
│   ├── core/                   ← 77 specs for @specd/core
│   ├── cli/                    ← 55 specs for @specd/cli
│   └── code-graph/             ← 10 specs for @specd/code-graph
├── docs/
│   ├── adr/                    ← 19 Architecture Decision Records
│   ├── guide/                  ← getting-started, workflow, schemas, workspaces, config, selectors
│   ├── cli/                    ← CLI command reference
│   ├── core/                   ← domain-model, ports, services, use-cases, errors
│   ├── config/                 ← specd.yaml reference + examples
│   └── schemas/                ← schema format reference + examples
├── .specd/
│   ├── changes/                ← active changes
│   ├── drafts/                 ← shelved changes
│   ├── discarded/              ← abandoned changes
│   ├── archive/                ← archived changes (by year/month)
│   ├── metadata/               ← spec metadata files (177 files)
│   └── code-graph.lbug         ← persisted code graph index (LadybugDB)
├── specd.yaml                  ← project config (4 workspaces: default, core, cli, code-graph)
├── eslint.config.js            ← monorepo-wide ESLint config with layer boundary enforcement
├── tsconfig.base.json          ← shared TypeScript base config
└── turbo.json                  ← Turbo task pipeline config
```

---

## Domain Map

| Domain               | Package             | Layer        | Source Path                                                                     | Description                                                                       |
| -------------------- | ------------------- | ------------ | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Change Lifecycle     | `@specd/core`       | Domain + App | `core/src/domain/entities/change.ts`, `application/use-cases/`                  | Creating, transitioning, approving, drafting, discarding, archiving spec changes  |
| Spec Management      | `@specd/core`       | Domain + App | `core/src/domain/entities/spec.ts`, `application/use-cases/list-specs.ts`, etc. | Reading, writing, validating, and metadata management for spec files              |
| Schema Management    | `@specd/core`       | Domain + App | `core/src/domain/value-objects/schema.ts`, `domain/services/build-schema.ts`    | Loading, resolving, extending, and validating schemas                             |
| Context Compilation  | `@specd/core`       | Application  | `application/use-cases/compile-context.ts`                                      | Assembling AI context blocks from specs, schema instructions, and lifecycle hooks |
| Configuration        | `@specd/core`       | Application  | `application/specd-config.ts`, `infrastructure/fs/config-loader.ts`             | Loading and validating `specd.yaml` project configuration                         |
| Composition / Kernel | `@specd/core`       | Composition  | `composition/kernel.ts`, `composition/*.ts`                                     | Dependency injection wiring — assembling use cases from config                    |
| CLI Commands         | `@specd/cli`        | Delivery     | `cli/src/commands/`                                                             | Thin CLI adapter; delegates all logic to core                                     |
| Code Graph           | `@specd/code-graph` | All layers   | `code-graph/src/`                                                               | Multi-language code indexing, impact analysis, hotspot detection, traversal       |

---

## Cross-Cutting Concerns

### Error Handling

- All domain and application errors extend `SpecdError` (abstract base with `code: string`)
- 21 concrete error classes in `core/src/domain/errors/`
- CLI maps errors to exit codes: `1` = domain/user error, `2` = hook failure, `3` = system/schema/unexpected
- Structured JSON errors on stdout for `--format json|toon`; plain text to stderr always

### Output and Formatting

- Three output formats: `text` (human-readable), `json` (machine-readable), `toon` (LLM-friendly Token-Oriented Object Notation)
- All output routed through `output()` in `cli/src/formatter.ts` (score: 221 — the most-called symbol in the CLI)
- Format parsed and validated via `parseFormat()` before every command

### Testing

- Domain and application layers: unit tests with mocked ports (no real filesystem)
- Infrastructure adapters: integration tests against temporary real filesystems
- Test files live in `packages/*/test/`, mirroring `src/` structure
- Pattern: `*.spec.ts` files using Vitest
- No CI pipeline found — tests run via `pnpm test` + lint-staged pre-commit

### Conventions (from ESLint config + specs)

- No default exports — named exports only (`ExportDefaultDeclaration` is forbidden)
- No public class fields — use methods or getters (`PropertyDefinition` without `private|protected` is forbidden)
- No `any` — `@typescript-eslint/no-explicit-any: error`
- Explicit module boundary types on all exports
- Kebab-case filenames for all source files
- JSDoc required on all functions, classes, methods, type aliases, and interfaces in `src/`
- All `@param` and `@returns` tags must have descriptions; `@throws` required where applicable

### Spec-Driven Workflow

- The project uses specd to govern its own development
- `specd.yaml` configures 4 workspaces: `default` (\_global specs), `core`, `cli`, `code-graph`
- Global specs (`default:_global/*`) are always included in context for every change
- Commit hooks run `pnpm test` and `pnpm lint` on transition to implementing and at archiving

---

## Security Model

- No authentication or authorisation mechanisms (specd is a local developer tool, not a server)
- `SpecPath` validates and rejects path traversal attacks at construction time (`..`, reserved chars)
- `PathTraversalError` is a dedicated domain error for traversal attempts
- Actor identity is resolved from VCS config (git config name/email) — no credentials stored by specd
- `read-only` workspace ownership enforces write prohibition at the use-case level

---

## Performance Constraints

- Schema loading: three-level lookup (project-local → user override → npm package); cached via `LazySchemaProvider`
- Spec context compilation: uses pattern matching + `dependsOn` traversal; lazy mode (default) limits full rendering to tier-1 specs only
- Code graph: LadybugDB embedded graph database; incremental indexing via content hashing; last indexed at `82a96a6`
- File writes: `writeFileAtomic()` for all critical writes (manifest, metadata) to prevent corruption

---

## Most Active Domains (from git history, last 50 commits)

Based on commit scope prefixes:

| Scope               | Commit Count | Recent Focus                                                                                        |
| ------------------- | ------------ | --------------------------------------------------------------------------------------------------- |
| `cli`               | 18           | Schema show flags, overlap check, spec-preview, readonly workspace enforcement, entrypoint overhaul |
| `core`              | 9            | Preview-spec, readonly ownership, spec overlap detection, resolve-path, approval boundaries         |
| `root` (docs/chore) | 15           | Skill definitions, spec metadata, global spec updates, changelog                                    |
| `schema-std`        | 2            | Design instructions, spec dependency tracking                                                       |
| `code-graph`        | 2            | Long-running feature branch merged                                                                  |

Most active files by recent changes: CLI command handlers, `specd.yaml`, skill definition files in `.specd/skills/`.

---

## Graph-Derived Spec Priorities

Derived from hotspot analysis (see `.specd/specd-bootstrap-analisys.md` for full details):

**Priority 1 (CRITICAL hotspots, broad impact):**

1. `cli:cli/output-helpers` — `output()`, `parseFormat()` (score 221/212)
2. `cli:cli/error-handling` — `handleError()`, `cliError()`, exit code contract (score 203/149)
3. `cli:cli/context-wiring` — `resolveCliContext()`, `CliContext` (score 220)
4. `core:core/config` — `SpecdConfig` shape, `isSpecdConfig()` type guard (score 118)
5. `core:core/change-lifecycle-transitions` — full transition table spec
6. `core:core/compile-context` — context compilation algorithm, pattern matching, dep traversal
7. `core:core/spec-overlap` — detection, blocking, and reporting
8. `core:core/artifact-parser` — format adapters, delta application

**Priority 2 (HIGH risk, important contracts):**

- Composition factories, VCS adapter auto-detection, repository port contracts, schema loading priority

**Priority 3 (Lower risk, infrastructure internals):**

- FS adapter utilities, metadata extraction, hook runner, skills manifest

---

## Conventions Detected

| Convention             | Source                              | Value                                                                     |
| ---------------------- | ----------------------------------- | ------------------------------------------------------------------------- |
| Module system          | package.json, tsconfig              | ESM only; `"type": "module"`, `NodeNext` resolution                       |
| No default exports     | eslint.config.js                    | `ExportDefaultDeclaration` forbidden                                      |
| No public class fields | eslint.config.js                    | `PropertyDefinition` (non-private) forbidden                              |
| No `any`               | eslint.config.js                    | `@typescript-eslint/no-explicit-any: error`                               |
| Kebab-case filenames   | eslint.config.js                    | `unicorn/filename-case: kebabCase`                                        |
| JSDoc on everything    | eslint.config.js                    | Required on functions, classes, methods, types, interfaces in `src/`      |
| Named exports only     | eslint.config.js + conventions spec | No default exports                                                        |
| Conventional Commits   | commitlint.config.mjs               | `type(scope): message` format enforced                                    |
| Layer boundaries       | eslint.config.js                    | `no-restricted-imports` for domain/application/infrastructure/composition |
| Test file location     | Convention                          | `packages/*/test/` mirroring `src/` structure                             |
| Test file naming       | Convention                          | `*.spec.ts`                                                               |
| Change name format     | domain/entities/change.ts           | Kebab-case: `/^[a-z0-9]+(?:-[a-z0-9]+)*$/`                                |
| Spec ID format         | domain                              | `workspace:capability-path` (e.g. `default:_global/architecture`)         |

---

## Generated Files

| File                                                                        | Description                                                                        |
| --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `/Users/monki/Documents/Proyectos/specd/specd-bootstrap.md`                 | This file — project overview and bootstrap summary                                 |
| `/Users/monki/Documents/Proyectos/specd/.specd/specd-bootstrap-analisys.md` | Graph analysis report — hotspots, impact, spec catalogue, prioritised spec targets |
