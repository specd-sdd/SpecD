<!-- AI guidance: define WHAT the system should do. Use SHALL / MUST for normative
     statements. Do not include WHEN/THEN scenarios — those go in verify.md. -->

# {{spec.name}}

## Purpose

<!-- 2–4 sentences: start with WHY this spec exists (the problem or need),
     then briefly describe WHAT it covers to give the reader context. -->

## Requirements

### Requirement: {{first requirement}}

<!-- Describe the requirement using SHALL / MUST language.
     Each requirement should be independently testable. -->

## Constraints

<!-- Hard limits, invariants, or non-negotiable restrictions.
     Delete this section if there are none. -->

## Spec Dependencies

<!-- This section is mandatory.
     List other specs that this spec depends on.
     Preferred format:
     - [`<workspace>:<capability-path>`](../relative/path/spec.md) — brief reason
     Compatible fallback when no link is available:
     - `<workspace>:<capability-path>` — brief reason
     If there are no dependencies, write `_none_`.
     Global constraint specs with no dependencies use `_none — this is a global constraint spec_`. -->
