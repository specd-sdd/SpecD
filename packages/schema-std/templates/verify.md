<!-- AI guidance: write GIVEN/WHEN/THEN scenarios that verify the spec requirements.
     Group under ### Requirement: headings matching spec.md exactly.
     GIVEN is optional — omit when the precondition is obvious from context.
     Use AND/OR after any clause for additional conditions or assertions.
     Only include scenarios that add information beyond the requirement prose. -->

# Verification: {{spec.name}}

## Requirements

### Requirement: {{first requirement}}

#### Scenario: {{scenario name}}

- **GIVEN** {{precondition}} <!-- optional; omit when obvious from context -->
- **AND/OR** {{additional precondition}} <!-- optional; repeat as needed -->
- **WHEN** {{condition}}
- **AND/OR** {{additional condition}} <!-- optional; repeat as needed -->
- **THEN** {{expected outcome}}
- **AND/OR** {{additional assertion}} <!-- optional; repeat as needed -->
