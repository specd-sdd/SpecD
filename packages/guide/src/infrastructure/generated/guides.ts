// GENERATED CODE - DO NOT EDIT MANUALLY
// Generated by packages/guide/scripts/bundle-guides.ts

import type { GuideTopic } from '../../domain/models/index.js'

export const GUIDES_CATALOG: readonly GuideTopic[] = [
  {
    "topic": "index",
    "title": "Guides Overview",
    "description": "Comprehensive guides, tutorials, and deep dives for SpecD.",
    "order": 0,
    "content": "# SpecD Documentation Guides\n\nWelcome to the SpecD User Guides! SpecD is a spec-driven development platform and codebase intelligence system designed to keep humans and AI agents perfectly aligned through structured specifications, explicit change lifecycles, and deterministic context compilation.\n\nExplore the topics below to understand SpecD from foundational principles to advanced customization.\n\n---\n\n## 1. Foundations & Philosophy\n\nUnderstand why spec-driven development matters and how projects are structured:\n\n- **[Philosophy](./philosophy.md)** — The core problem with traditional software development, why specs must come before code, and the contract between human intent and AI execution.\n- **[Installation & Initialization](./installation.md)** — Installing the SpecD CLI globally or locally, system requirements, initializing projects (`specd project init`), and installing agent plugins.\n- **[Getting Started Quickstart](./getting-started.md)** — A 10-minute walkthrough taking you from zero to your first verified and archived change with slash skills.\n- **[Project Structure](./project-structure.md)** — The directory layout of a SpecD repository, including `specs/`, `.specd/`, workspaces, and application code.\n\n---\n\n## 2. Core Concepts & Lifecycle\n\nLearn the fundamental building blocks of SpecD and how changes evolve:\n\n- **[Specs](./specs.md)** — What a spec is, the role of `spec.md` and `verify.md`, metadata extraction, and how living specs evolve over time.\n- **[Changes](./changes.md)** — The unit of work in SpecD, change manifest tracking, artifact dependency DAG, and change operations.\n- **[Workflow & Lifecycle](./workflow.md)** — The complete 7-step change lifecycle (`drafting` → `designing` → `ready` → `implementing` → `verifying` → `done` → `archivable`), approval gates, and rollback states.\n- **[Skills](./skills.md)** — Slash commands that guide you and the AI agent through the workflow. Covers all built-in skills (`/specd`, `/specd-new`, `/specd-design`, etc.), how they interact with the CLI, and when to use each one.\n- **[Context Compilation](./context-compilation.md)** — How SpecD dynamically compiles token-optimized context blocks for AI agents using graph traversal and include/exclude patterns.\n\n---\n\n## 3. Workspaces & Intelligence\n\nScale SpecD across large codebases and leverage graph intelligence:\n\n- **[Workspaces](./workspaces.md)** — Configuring monorepos, multi-repo coordinator setups, owned vs. read-only workspaces, and spec namespacing.\n- **[Code Graph](./code-graph.md)** — Codebase intelligence, symbol indexing, dependency blast-radius analysis, and hotspot detection.\n\n---\n\n## 4. Schemas & Extensibility\n\nCustomize and extend artifact workflows, validation rules, and templates:\n\n- **[Schemas Overview](./schemas.md)** — The architecture of SpecD schemas: declaring artifacts, transitions, and AI instructions.\n- **[Standard Schema](./standard-schema.md)** — A deep dive into `@specd/schema-std`, the default proposal-spec-design-tasks workflow.\n- **[Custom Schemas](./custom-schemas.md)** — Creating custom schemas or extending existing schemas for domain-specific workflows.\n- **[Selectors](./selectors.md)** — Querying and validating markdown ASTs and structured metadata using SpecD selectors.\n- **[Artifacts & Templates](./artifacts.md)** — What artifacts are, the dependency DAG, drift tracking, and template authoring.\n- **[Deltas](./deltas.md)** — Non-destructive specification patching, three-way merging, and conflict resolution during archive.\n\n---\n\n## 5. Tooling & Reference\n\nCommand-line usage and configuration settings:\n\n- **[CLI Reference](./cli.md)** — Command catalog for `specd guide`, `specd changes`, `specd specs`, `specd graph`, and more.\n- **[Configuration Reference](./configuration.md)** — Comprehensive reference for all options available in `specd.yaml`.\n- **[Configuration Examples](./configuration-examples.md)** — Practical, copy-pasteable configuration templates from single-package apps to enterprise monorepos.\n\n---\n\n## Need Quick Help in the CLI?\n\nAll of these guides are bundled directly into the SpecD CLI for instant, offline terminal access:\n\n```bash\n# List all available guides\nspecd guide\n\n# Read a guide\nspecd guide specs\nspecd guide changes\n\n# Search guides with BM25 full-text search\nspecd guide search \"approval gates\"\n```\n",
    "lineCount": 79,
    "byteLength": 4390,
    "outline": [
      {
        "index": 1,
        "heading": "SpecD Documentation Guides",
        "level": 1,
        "startLine": 1,
        "endLine": 79,
        "lines": 79,
        "content": "# SpecD Documentation Guides\n\nWelcome to the SpecD User Guides! SpecD is a spec-driven development platform and codebase intelligence system designed to keep humans and AI agents perfectly aligned through structured specifications, explicit change lifecycles, and deterministic context compilation.\n\nExplore the topics below to understand SpecD from foundational principles to advanced customization.\n\n---\n\n## 1. Foundations & Philosophy\n\nUnderstand why spec-driven development matters and how projects are structured:\n\n- **[Philosophy](./philosophy.md)** — The core problem with traditional software development, why specs must come before code, and the contract between human intent and AI execution.\n- **[Installation & Initialization](./installation.md)** — Installing the SpecD CLI globally or locally, system requirements, initializing projects (`specd project init`), and installing agent plugins.\n- **[Getting Started Quickstart](./getting-started.md)** — A 10-minute walkthrough taking you from zero to your first verified and archived change with slash skills.\n- **[Project Structure](./project-structure.md)** — The directory layout of a SpecD repository, including `specs/`, `.specd/`, workspaces, and application code.\n\n---\n\n## 2. Core Concepts & Lifecycle\n\nLearn the fundamental building blocks of SpecD and how changes evolve:\n\n- **[Specs](./specs.md)** — What a spec is, the role of `spec.md` and `verify.md`, metadata extraction, and how living specs evolve over time.\n- **[Changes](./changes.md)** — The unit of work in SpecD, change manifest tracking, artifact dependency DAG, and change operations.\n- **[Workflow & Lifecycle](./workflow.md)** — The complete 7-step change lifecycle (`drafting` → `designing` → `ready` → `implementing` → `verifying` → `done` → `archivable`), approval gates, and rollback states.\n- **[Skills](./skills.md)** — Slash commands that guide you and the AI agent through the workflow. Covers all built-in skills (`/specd`, `/specd-new`, `/specd-design`, etc.), how they interact with the CLI, and when to use each one.\n- **[Context Compilation](./context-compilation.md)** — How SpecD dynamically compiles token-optimized context blocks for AI agents using graph traversal and include/exclude patterns.\n\n---\n\n## 3. Workspaces & Intelligence\n\nScale SpecD across large codebases and leverage graph intelligence:\n\n- **[Workspaces](./workspaces.md)** — Configuring monorepos, multi-repo coordinator setups, owned vs. read-only workspaces, and spec namespacing.\n- **[Code Graph](./code-graph.md)** — Codebase intelligence, symbol indexing, dependency blast-radius analysis, and hotspot detection.\n\n---\n\n## 4. Schemas & Extensibility\n\nCustomize and extend artifact workflows, validation rules, and templates:\n\n- **[Schemas Overview](./schemas.md)** — The architecture of SpecD schemas: declaring artifacts, transitions, and AI instructions.\n- **[Standard Schema](./standard-schema.md)** — A deep dive into `@specd/schema-std`, the default proposal-spec-design-tasks workflow.\n- **[Custom Schemas](./custom-schemas.md)** — Creating custom schemas or extending existing schemas for domain-specific workflows.\n- **[Selectors](./selectors.md)** — Querying and validating markdown ASTs and structured metadata using SpecD selectors.\n- **[Artifacts & Templates](./artifacts.md)** — What artifacts are, the dependency DAG, drift tracking, and template authoring.\n- **[Deltas](./deltas.md)** — Non-destructive specification patching, three-way merging, and conflict resolution during archive.\n\n---\n\n## 5. Tooling & Reference\n\nCommand-line usage and configuration settings:\n\n- **[CLI Reference](./cli.md)** — Command catalog for `specd guide`, `specd changes`, `specd specs`, `specd graph`, and more.\n- **[Configuration Reference](./configuration.md)** — Comprehensive reference for all options available in `specd.yaml`.\n- **[Configuration Examples](./configuration-examples.md)** — Practical, copy-pasteable configuration templates from single-package apps to enterprise monorepos.\n\n---\n\n## Need Quick Help in the CLI?\n\nAll of these guides are bundled directly into the SpecD CLI for instant, offline terminal access:\n\n```bash\n# List all available guides\nspecd guide\n\n# Read a guide\nspecd guide specs\nspecd guide changes\n\n# Search guides with BM25 full-text search\nspecd guide search \"approval gates\"\n```\n"
      },
      {
        "index": 2,
        "heading": "1. Foundations & Philosophy",
        "level": 2,
        "startLine": 9,
        "endLine": 19,
        "lines": 11,
        "content": "## 1. Foundations & Philosophy\n\nUnderstand why spec-driven development matters and how projects are structured:\n\n- **[Philosophy](./philosophy.md)** — The core problem with traditional software development, why specs must come before code, and the contract between human intent and AI execution.\n- **[Installation & Initialization](./installation.md)** — Installing the SpecD CLI globally or locally, system requirements, initializing projects (`specd project init`), and installing agent plugins.\n- **[Getting Started Quickstart](./getting-started.md)** — A 10-minute walkthrough taking you from zero to your first verified and archived change with slash skills.\n- **[Project Structure](./project-structure.md)** — The directory layout of a SpecD repository, including `specs/`, `.specd/`, workspaces, and application code.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "2. Core Concepts & Lifecycle",
        "level": 2,
        "startLine": 20,
        "endLine": 31,
        "lines": 12,
        "content": "## 2. Core Concepts & Lifecycle\n\nLearn the fundamental building blocks of SpecD and how changes evolve:\n\n- **[Specs](./specs.md)** — What a spec is, the role of `spec.md` and `verify.md`, metadata extraction, and how living specs evolve over time.\n- **[Changes](./changes.md)** — The unit of work in SpecD, change manifest tracking, artifact dependency DAG, and change operations.\n- **[Workflow & Lifecycle](./workflow.md)** — The complete 7-step change lifecycle (`drafting` → `designing` → `ready` → `implementing` → `verifying` → `done` → `archivable`), approval gates, and rollback states.\n- **[Skills](./skills.md)** — Slash commands that guide you and the AI agent through the workflow. Covers all built-in skills (`/specd`, `/specd-new`, `/specd-design`, etc.), how they interact with the CLI, and when to use each one.\n- **[Context Compilation](./context-compilation.md)** — How SpecD dynamically compiles token-optimized context blocks for AI agents using graph traversal and include/exclude patterns.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "3. Workspaces & Intelligence",
        "level": 2,
        "startLine": 32,
        "endLine": 40,
        "lines": 9,
        "content": "## 3. Workspaces & Intelligence\n\nScale SpecD across large codebases and leverage graph intelligence:\n\n- **[Workspaces](./workspaces.md)** — Configuring monorepos, multi-repo coordinator setups, owned vs. read-only workspaces, and spec namespacing.\n- **[Code Graph](./code-graph.md)** — Codebase intelligence, symbol indexing, dependency blast-radius analysis, and hotspot detection.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "4. Schemas & Extensibility",
        "level": 2,
        "startLine": 41,
        "endLine": 53,
        "lines": 13,
        "content": "## 4. Schemas & Extensibility\n\nCustomize and extend artifact workflows, validation rules, and templates:\n\n- **[Schemas Overview](./schemas.md)** — The architecture of SpecD schemas: declaring artifacts, transitions, and AI instructions.\n- **[Standard Schema](./standard-schema.md)** — A deep dive into `@specd/schema-std`, the default proposal-spec-design-tasks workflow.\n- **[Custom Schemas](./custom-schemas.md)** — Creating custom schemas or extending existing schemas for domain-specific workflows.\n- **[Selectors](./selectors.md)** — Querying and validating markdown ASTs and structured metadata using SpecD selectors.\n- **[Artifacts & Templates](./artifacts.md)** — What artifacts are, the dependency DAG, drift tracking, and template authoring.\n- **[Deltas](./deltas.md)** — Non-destructive specification patching, three-way merging, and conflict resolution during archive.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "5. Tooling & Reference",
        "level": 2,
        "startLine": 54,
        "endLine": 63,
        "lines": 10,
        "content": "## 5. Tooling & Reference\n\nCommand-line usage and configuration settings:\n\n- **[CLI Reference](./cli.md)** — Command catalog for `specd guide`, `specd changes`, `specd specs`, `specd graph`, and more.\n- **[Configuration Reference](./configuration.md)** — Comprehensive reference for all options available in `specd.yaml`.\n- **[Configuration Examples](./configuration-examples.md)** — Practical, copy-pasteable configuration templates from single-package apps to enterprise monorepos.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "Need Quick Help in the CLI?",
        "level": 2,
        "startLine": 64,
        "endLine": 79,
        "lines": 16,
        "content": "## Need Quick Help in the CLI?\n\nAll of these guides are bundled directly into the SpecD CLI for instant, offline terminal access:\n\n```bash\n# List all available guides\nspecd guide\n\n# Read a guide\nspecd guide specs\nspecd guide changes\n\n# Search guides with BM25 full-text search\nspecd guide search \"approval gates\"\n```\n"
      }
    ]
  },
  {
    "topic": "getting-started",
    "title": "Getting Started with SpecD",
    "description": "Quickstart tutorial using SpecD skills to navigate and execute the spec-driven development lifecycle.",
    "order": 1,
    "content": "# Getting Started with SpecD\n\nWelcome to SpecD! This quickstart shows you how to develop software using SpecD and your AI coding assistant (Claude Code, GitHub Copilot, OpenAI Codex, OpenCode, etc.).\n\nInstead of having to memorize CLI commands, artifact schemas, or internal state machines, SpecD equips your assistant with **slash skills**.\n\nAt the center is **/specd** — your starting point, router, and orientation hub.\n\n> Need to install the CLI or initialize a repository first? Follow the [Installation & Initialization Guide](./installation.md).\n\n---\n\n## 1. How Skills Work: The Router vs. Direct Invocation\n\nSpecD skills are designed to give you complete flexibility:\n\n- **When you start a session or aren't sure where you left off:** Run `/specd`. It inspects project health, discovers any active changes or drafts, tells you exactly what state the project is in, and points you to the right next skill.\n- **When you already know what you want to do:** Go straight to the relevant skill! You do **not** need to call `/specd` before every action. If you are starting a new feature, run `/specd-new <intent>`. If you just finished writing tests and want to verify, run `/specd-verify`.\n\n```text\n               ┌──────────────────────────────────────────────┐\n               │                   /specd                     │\n               │   (Orientation hub: check status & route)    │\n               └───────────────────────┬──────────────────────┘\n                                       │ advises\n                                       ▼\n  ┌────────────┐       ┌───────────────┐       ┌──────────────────┐       ┌───────────────┐       ┌─────────────────┐\n  │ /specd-new │  ──►  │ /specd-design │  ──►  │ /specd-implement │  ──►  │ /specd-verify │  ──►  │ /specd-archive  │\n  │ (Creation) │       │   (Design)    │       │ (Implementation) │       │ (Validation)  │       │ (Consolidation) │\n  └────────────┘       └───────────────┘       └──────────────────┘       └───────────────┘       └─────────────────┘\n         ▲                     ▲                       ▲                          ▲                       ▲\n         └─────────────────────┴────── You can invoke ─────────┴──────────────────────────┴───────────────────────┘\n                                      any skill directly!\n```\n\n---\n\n## 2. Walkthrough: A Complete Feature Lifecycle\n\nHere is how a typical change progresses through the skills:\n\n### Step 1: Start a New Change with `/specd-new`\n\nWhen you want to build a new capability or modify an existing one, invoke `/specd-new` directly:\n\n```text\n/specd-new I want to add an in-memory token bucket rate limiter for API requests\n```\n\n_(Tip: If you're not sure if a change is already in progress, run `/specd` first to check.)_\n\n**What happens:**\n\n1. The agent analyzes existing code and specs using the code graph to identify impacted modules.\n2. The agent asks clarifying questions to pin down requirements and scope.\n3. SpecD scaffolds `.specd/changes/<timestamp>-<name>/` containing `manifest.yaml` and `proposal.md`.\n\n---\n\n### Step 2: Author Requirements & Design with `/specd-design`\n\nOnce the change is open, move into specification and architecture design:\n\n```text\n/specd-design\n```\n\n**What happens:**\nThe agent authors the artifacts along the dependency DAG:\n\n1. `specs/rate-limiter/spec.md` — normative requirements (`SHALL return 429 when quota exceeded`).\n2. `specs/rate-limiter/verify.md` — acceptance test scenarios (`WHEN request count > limit, THEN reject with 429`).\n3. `design.md` — technical architecture, classes, interfaces, and blast-radius considerations.\n4. `tasks.md` — granular implementation checklist (`- [ ] Implement TokenBucket class`, `- [ ] Add unit tests`).\n\nOnce all artifacts are validated against the schema, the change state advances to `ready`.\n\n---\n\n### Step 3: Implement Code with `/specd-implement`\n\nWith design and tasks agreed upon, begin writing code:\n\n```text\n/specd-implement\n```\n\n**What happens:**\n\n1. The agent executes tasks defined in `tasks.md` sequentially.\n2. For each task, the agent writes or refactors source files, runs tests, and marks the task as complete (`- [x]`).\n3. When all tasks are checked off, the change transitions to `verifying`.\n\n---\n\n### Step 4: Verify Against Scenarios with `/specd-verify`\n\nConfirm that the implementation satisfies the contract defined in `verify.md`:\n\n```text\n/specd-verify\n```\n\n**What happens:**\n\n1. The agent executes the test suite against every scenario declared in `verify.md`.\n2. If any scenario fails, the agent reports the discrepancy and continues implementation to fix the defect.\n3. If all scenarios pass, the change transitions to `done`.\n\n---\n\n### Step 5: Archive the Change with `/specd-archive`\n\nConsolidate the validated work:\n\n```text\n/specd-archive\n```\n\n**What happens:**\n\n1. Preflight checks ensure the working tree is clean and verifications pass.\n2. Proposed spec deltas are cleanly merged into the living specification library in `specs/`.\n3. The change directory is permanently moved to `.specd/archive/` as an immutable audit record.\n4. The codebase dependency graph is re-indexed.\n\n---\n\n### Need Guidance? Use `/specd` Anytime\n\nWhenever you step away from your keyboard, switch Git branches, or return to a project after days away:\n\n```text\n/specd\n```\n\nSpecD inspects everything and tells you:\n\n- Current active change name and lifecycle state (`designing`, `ready`, `implementing`, `verifying`, or `done`).\n- Any paused drafts in `.specd/drafts/`.\n- Unfinished tasks in `tasks.md`.\n- Which skill you should invoke to proceed.\n\n---\n\n## 3. Skills Catalog at a Glance\n\n| Skill                   | Lifecycle Stage     | When to Use                                                                   |\n| ----------------------- | ------------------- | ----------------------------------------------------------------------------- |\n| **`/specd`**            | **Router & Hub**    | Whenever you start a session or want to inspect current status and get routed |\n| **`/specd-new`**        | Creation            | When starting a new capability, feature, or refactor                          |\n| **`/specd-design`**     | Planning            | When authoring or revising specs, test scenarios, design docs, or tasks       |\n| **`/specd-implement`**  | Execution           | When implementing code against the agreed `tasks.md` checklist                |\n| **`/specd-verify`**     | Validation          | When testing the code against the `verify.md` acceptance scenarios            |\n| **`/specd-archive`**    | Consolidation       | When merging validated specs into `specs/` and archiving the change           |\n| **`/specd-compliance`** | Audit               | When reviewing compliance between implementation and specs                    |\n| **`/specd-fasttrack`**  | Code-First / Hotfix | For quick spikes, urgent bugfixes, or code-first prototyping                  |\n\n> For technical details on stop-points, CLI interactions, and customizing prompt instructions, see the **[Official Skills Guide](./skills.md)**.\n\n---\n\n## Next Steps\n\n- Review the [Installation & Initialization Guide](./installation.md) to set up plugins for your editor or agent.\n- Read [The Philosophy of Spec-Driven Development](./philosophy.md) for the core principles of spec-first engineering.\n- Learn about [Workflow & Lifecycle](./workflow.md) for approval gates and state transitions.\n- Explore [Workspaces](./workspaces.md) if you are working in a monorepo.\n",
    "lineCount": 169,
    "byteLength": 8300,
    "outline": [
      {
        "index": 1,
        "heading": "Getting Started with SpecD",
        "level": 1,
        "startLine": 1,
        "endLine": 169,
        "lines": 169,
        "content": "# Getting Started with SpecD\n\nWelcome to SpecD! This quickstart shows you how to develop software using SpecD and your AI coding assistant (Claude Code, GitHub Copilot, OpenAI Codex, OpenCode, etc.).\n\nInstead of having to memorize CLI commands, artifact schemas, or internal state machines, SpecD equips your assistant with **slash skills**.\n\nAt the center is **/specd** — your starting point, router, and orientation hub.\n\n> Need to install the CLI or initialize a repository first? Follow the [Installation & Initialization Guide](./installation.md).\n\n---\n\n## 1. How Skills Work: The Router vs. Direct Invocation\n\nSpecD skills are designed to give you complete flexibility:\n\n- **When you start a session or aren't sure where you left off:** Run `/specd`. It inspects project health, discovers any active changes or drafts, tells you exactly what state the project is in, and points you to the right next skill.\n- **When you already know what you want to do:** Go straight to the relevant skill! You do **not** need to call `/specd` before every action. If you are starting a new feature, run `/specd-new <intent>`. If you just finished writing tests and want to verify, run `/specd-verify`.\n\n```text\n               ┌──────────────────────────────────────────────┐\n               │                   /specd                     │\n               │   (Orientation hub: check status & route)    │\n               └───────────────────────┬──────────────────────┘\n                                       │ advises\n                                       ▼\n  ┌────────────┐       ┌───────────────┐       ┌──────────────────┐       ┌───────────────┐       ┌─────────────────┐\n  │ /specd-new │  ──►  │ /specd-design │  ──►  │ /specd-implement │  ──►  │ /specd-verify │  ──►  │ /specd-archive  │\n  │ (Creation) │       │   (Design)    │       │ (Implementation) │       │ (Validation)  │       │ (Consolidation) │\n  └────────────┘       └───────────────┘       └──────────────────┘       └───────────────┘       └─────────────────┘\n         ▲                     ▲                       ▲                          ▲                       ▲\n         └─────────────────────┴────── You can invoke ─────────┴──────────────────────────┴───────────────────────┘\n                                      any skill directly!\n```\n\n---\n\n## 2. Walkthrough: A Complete Feature Lifecycle\n\nHere is how a typical change progresses through the skills:\n\n### Step 1: Start a New Change with `/specd-new`\n\nWhen you want to build a new capability or modify an existing one, invoke `/specd-new` directly:\n\n```text\n/specd-new I want to add an in-memory token bucket rate limiter for API requests\n```\n\n_(Tip: If you're not sure if a change is already in progress, run `/specd` first to check.)_\n\n**What happens:**\n\n1. The agent analyzes existing code and specs using the code graph to identify impacted modules.\n2. The agent asks clarifying questions to pin down requirements and scope.\n3. SpecD scaffolds `.specd/changes/<timestamp>-<name>/` containing `manifest.yaml` and `proposal.md`.\n\n---\n\n### Step 2: Author Requirements & Design with `/specd-design`\n\nOnce the change is open, move into specification and architecture design:\n\n```text\n/specd-design\n```\n\n**What happens:**\nThe agent authors the artifacts along the dependency DAG:\n\n1. `specs/rate-limiter/spec.md` — normative requirements (`SHALL return 429 when quota exceeded`).\n2. `specs/rate-limiter/verify.md` — acceptance test scenarios (`WHEN request count > limit, THEN reject with 429`).\n3. `design.md` — technical architecture, classes, interfaces, and blast-radius considerations.\n4. `tasks.md` — granular implementation checklist (`- [ ] Implement TokenBucket class`, `- [ ] Add unit tests`).\n\nOnce all artifacts are validated against the schema, the change state advances to `ready`.\n\n---\n\n### Step 3: Implement Code with `/specd-implement`\n\nWith design and tasks agreed upon, begin writing code:\n\n```text\n/specd-implement\n```\n\n**What happens:**\n\n1. The agent executes tasks defined in `tasks.md` sequentially.\n2. For each task, the agent writes or refactors source files, runs tests, and marks the task as complete (`- [x]`).\n3. When all tasks are checked off, the change transitions to `verifying`.\n\n---\n\n### Step 4: Verify Against Scenarios with `/specd-verify`\n\nConfirm that the implementation satisfies the contract defined in `verify.md`:\n\n```text\n/specd-verify\n```\n\n**What happens:**\n\n1. The agent executes the test suite against every scenario declared in `verify.md`.\n2. If any scenario fails, the agent reports the discrepancy and continues implementation to fix the defect.\n3. If all scenarios pass, the change transitions to `done`.\n\n---\n\n### Step 5: Archive the Change with `/specd-archive`\n\nConsolidate the validated work:\n\n```text\n/specd-archive\n```\n\n**What happens:**\n\n1. Preflight checks ensure the working tree is clean and verifications pass.\n2. Proposed spec deltas are cleanly merged into the living specification library in `specs/`.\n3. The change directory is permanently moved to `.specd/archive/` as an immutable audit record.\n4. The codebase dependency graph is re-indexed.\n\n---\n\n### Need Guidance? Use `/specd` Anytime\n\nWhenever you step away from your keyboard, switch Git branches, or return to a project after days away:\n\n```text\n/specd\n```\n\nSpecD inspects everything and tells you:\n\n- Current active change name and lifecycle state (`designing`, `ready`, `implementing`, `verifying`, or `done`).\n- Any paused drafts in `.specd/drafts/`.\n- Unfinished tasks in `tasks.md`.\n- Which skill you should invoke to proceed.\n\n---\n\n## 3. Skills Catalog at a Glance\n\n| Skill                   | Lifecycle Stage     | When to Use                                                                   |\n| ----------------------- | ------------------- | ----------------------------------------------------------------------------- |\n| **`/specd`**            | **Router & Hub**    | Whenever you start a session or want to inspect current status and get routed |\n| **`/specd-new`**        | Creation            | When starting a new capability, feature, or refactor                          |\n| **`/specd-design`**     | Planning            | When authoring or revising specs, test scenarios, design docs, or tasks       |\n| **`/specd-implement`**  | Execution           | When implementing code against the agreed `tasks.md` checklist                |\n| **`/specd-verify`**     | Validation          | When testing the code against the `verify.md` acceptance scenarios            |\n| **`/specd-archive`**    | Consolidation       | When merging validated specs into `specs/` and archiving the change           |\n| **`/specd-compliance`** | Audit               | When reviewing compliance between implementation and specs                    |\n| **`/specd-fasttrack`**  | Code-First / Hotfix | For quick spikes, urgent bugfixes, or code-first prototyping                  |\n\n> For technical details on stop-points, CLI interactions, and customizing prompt instructions, see the **[Official Skills Guide](./skills.md)**.\n\n---\n\n## Next Steps\n\n- Review the [Installation & Initialization Guide](./installation.md) to set up plugins for your editor or agent.\n- Read [The Philosophy of Spec-Driven Development](./philosophy.md) for the core principles of spec-first engineering.\n- Learn about [Workflow & Lifecycle](./workflow.md) for approval gates and state transitions.\n- Explore [Workspaces](./workspaces.md) if you are working in a monorepo.\n"
      },
      {
        "index": 2,
        "heading": "1. How Skills Work: The Router vs. Direct Invocation",
        "level": 2,
        "startLine": 13,
        "endLine": 37,
        "lines": 25,
        "content": "## 1. How Skills Work: The Router vs. Direct Invocation\n\nSpecD skills are designed to give you complete flexibility:\n\n- **When you start a session or aren't sure where you left off:** Run `/specd`. It inspects project health, discovers any active changes or drafts, tells you exactly what state the project is in, and points you to the right next skill.\n- **When you already know what you want to do:** Go straight to the relevant skill! You do **not** need to call `/specd` before every action. If you are starting a new feature, run `/specd-new <intent>`. If you just finished writing tests and want to verify, run `/specd-verify`.\n\n```text\n               ┌──────────────────────────────────────────────┐\n               │                   /specd                     │\n               │   (Orientation hub: check status & route)    │\n               └───────────────────────┬──────────────────────┘\n                                       │ advises\n                                       ▼\n  ┌────────────┐       ┌───────────────┐       ┌──────────────────┐       ┌───────────────┐       ┌─────────────────┐\n  │ /specd-new │  ──►  │ /specd-design │  ──►  │ /specd-implement │  ──►  │ /specd-verify │  ──►  │ /specd-archive  │\n  │ (Creation) │       │   (Design)    │       │ (Implementation) │       │ (Validation)  │       │ (Consolidation) │\n  └────────────┘       └───────────────┘       └──────────────────┘       └───────────────┘       └─────────────────┘\n         ▲                     ▲                       ▲                          ▲                       ▲\n         └─────────────────────┴────── You can invoke ─────────┴──────────────────────────┴───────────────────────┘\n                                      any skill directly!\n```\n\n---\n"
      },
      {
        "index": 3,
        "heading": "2. Walkthrough: A Complete Feature Lifecycle",
        "level": 2,
        "startLine": 38,
        "endLine": 145,
        "lines": 108,
        "content": "## 2. Walkthrough: A Complete Feature Lifecycle\n\nHere is how a typical change progresses through the skills:\n\n### Step 1: Start a New Change with `/specd-new`\n\nWhen you want to build a new capability or modify an existing one, invoke `/specd-new` directly:\n\n```text\n/specd-new I want to add an in-memory token bucket rate limiter for API requests\n```\n\n_(Tip: If you're not sure if a change is already in progress, run `/specd` first to check.)_\n\n**What happens:**\n\n1. The agent analyzes existing code and specs using the code graph to identify impacted modules.\n2. The agent asks clarifying questions to pin down requirements and scope.\n3. SpecD scaffolds `.specd/changes/<timestamp>-<name>/` containing `manifest.yaml` and `proposal.md`.\n\n---\n\n### Step 2: Author Requirements & Design with `/specd-design`\n\nOnce the change is open, move into specification and architecture design:\n\n```text\n/specd-design\n```\n\n**What happens:**\nThe agent authors the artifacts along the dependency DAG:\n\n1. `specs/rate-limiter/spec.md` — normative requirements (`SHALL return 429 when quota exceeded`).\n2. `specs/rate-limiter/verify.md` — acceptance test scenarios (`WHEN request count > limit, THEN reject with 429`).\n3. `design.md` — technical architecture, classes, interfaces, and blast-radius considerations.\n4. `tasks.md` — granular implementation checklist (`- [ ] Implement TokenBucket class`, `- [ ] Add unit tests`).\n\nOnce all artifacts are validated against the schema, the change state advances to `ready`.\n\n---\n\n### Step 3: Implement Code with `/specd-implement`\n\nWith design and tasks agreed upon, begin writing code:\n\n```text\n/specd-implement\n```\n\n**What happens:**\n\n1. The agent executes tasks defined in `tasks.md` sequentially.\n2. For each task, the agent writes or refactors source files, runs tests, and marks the task as complete (`- [x]`).\n3. When all tasks are checked off, the change transitions to `verifying`.\n\n---\n\n### Step 4: Verify Against Scenarios with `/specd-verify`\n\nConfirm that the implementation satisfies the contract defined in `verify.md`:\n\n```text\n/specd-verify\n```\n\n**What happens:**\n\n1. The agent executes the test suite against every scenario declared in `verify.md`.\n2. If any scenario fails, the agent reports the discrepancy and continues implementation to fix the defect.\n3. If all scenarios pass, the change transitions to `done`.\n\n---\n\n### Step 5: Archive the Change with `/specd-archive`\n\nConsolidate the validated work:\n\n```text\n/specd-archive\n```\n\n**What happens:**\n\n1. Preflight checks ensure the working tree is clean and verifications pass.\n2. Proposed spec deltas are cleanly merged into the living specification library in `specs/`.\n3. The change directory is permanently moved to `.specd/archive/` as an immutable audit record.\n4. The codebase dependency graph is re-indexed.\n\n---\n\n### Need Guidance? Use `/specd` Anytime\n\nWhenever you step away from your keyboard, switch Git branches, or return to a project after days away:\n\n```text\n/specd\n```\n\nSpecD inspects everything and tells you:\n\n- Current active change name and lifecycle state (`designing`, `ready`, `implementing`, `verifying`, or `done`).\n- Any paused drafts in `.specd/drafts/`.\n- Unfinished tasks in `tasks.md`.\n- Which skill you should invoke to proceed.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "Step 1: Start a New Change with `/specd-new`",
        "level": 3,
        "startLine": 42,
        "endLine": 59,
        "lines": 18,
        "content": "### Step 1: Start a New Change with `/specd-new`\n\nWhen you want to build a new capability or modify an existing one, invoke `/specd-new` directly:\n\n```text\n/specd-new I want to add an in-memory token bucket rate limiter for API requests\n```\n\n_(Tip: If you're not sure if a change is already in progress, run `/specd` first to check.)_\n\n**What happens:**\n\n1. The agent analyzes existing code and specs using the code graph to identify impacted modules.\n2. The agent asks clarifying questions to pin down requirements and scope.\n3. SpecD scaffolds `.specd/changes/<timestamp>-<name>/` containing `manifest.yaml` and `proposal.md`.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Step 2: Author Requirements & Design with `/specd-design`",
        "level": 3,
        "startLine": 60,
        "endLine": 79,
        "lines": 20,
        "content": "### Step 2: Author Requirements & Design with `/specd-design`\n\nOnce the change is open, move into specification and architecture design:\n\n```text\n/specd-design\n```\n\n**What happens:**\nThe agent authors the artifacts along the dependency DAG:\n\n1. `specs/rate-limiter/spec.md` — normative requirements (`SHALL return 429 when quota exceeded`).\n2. `specs/rate-limiter/verify.md` — acceptance test scenarios (`WHEN request count > limit, THEN reject with 429`).\n3. `design.md` — technical architecture, classes, interfaces, and blast-radius considerations.\n4. `tasks.md` — granular implementation checklist (`- [ ] Implement TokenBucket class`, `- [ ] Add unit tests`).\n\nOnce all artifacts are validated against the schema, the change state advances to `ready`.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "Step 3: Implement Code with `/specd-implement`",
        "level": 3,
        "startLine": 80,
        "endLine": 95,
        "lines": 16,
        "content": "### Step 3: Implement Code with `/specd-implement`\n\nWith design and tasks agreed upon, begin writing code:\n\n```text\n/specd-implement\n```\n\n**What happens:**\n\n1. The agent executes tasks defined in `tasks.md` sequentially.\n2. For each task, the agent writes or refactors source files, runs tests, and marks the task as complete (`- [x]`).\n3. When all tasks are checked off, the change transitions to `verifying`.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "Step 4: Verify Against Scenarios with `/specd-verify`",
        "level": 3,
        "startLine": 96,
        "endLine": 111,
        "lines": 16,
        "content": "### Step 4: Verify Against Scenarios with `/specd-verify`\n\nConfirm that the implementation satisfies the contract defined in `verify.md`:\n\n```text\n/specd-verify\n```\n\n**What happens:**\n\n1. The agent executes the test suite against every scenario declared in `verify.md`.\n2. If any scenario fails, the agent reports the discrepancy and continues implementation to fix the defect.\n3. If all scenarios pass, the change transitions to `done`.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Step 5: Archive the Change with `/specd-archive`",
        "level": 3,
        "startLine": 112,
        "endLine": 128,
        "lines": 17,
        "content": "### Step 5: Archive the Change with `/specd-archive`\n\nConsolidate the validated work:\n\n```text\n/specd-archive\n```\n\n**What happens:**\n\n1. Preflight checks ensure the working tree is clean and verifications pass.\n2. Proposed spec deltas are cleanly merged into the living specification library in `specs/`.\n3. The change directory is permanently moved to `.specd/archive/` as an immutable audit record.\n4. The codebase dependency graph is re-indexed.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "Need Guidance? Use `/specd` Anytime",
        "level": 3,
        "startLine": 129,
        "endLine": 145,
        "lines": 17,
        "content": "### Need Guidance? Use `/specd` Anytime\n\nWhenever you step away from your keyboard, switch Git branches, or return to a project after days away:\n\n```text\n/specd\n```\n\nSpecD inspects everything and tells you:\n\n- Current active change name and lifecycle state (`designing`, `ready`, `implementing`, `verifying`, or `done`).\n- Any paused drafts in `.specd/drafts/`.\n- Unfinished tasks in `tasks.md`.\n- Which skill you should invoke to proceed.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "3. Skills Catalog at a Glance",
        "level": 2,
        "startLine": 146,
        "endLine": 162,
        "lines": 17,
        "content": "## 3. Skills Catalog at a Glance\n\n| Skill                   | Lifecycle Stage     | When to Use                                                                   |\n| ----------------------- | ------------------- | ----------------------------------------------------------------------------- |\n| **`/specd`**            | **Router & Hub**    | Whenever you start a session or want to inspect current status and get routed |\n| **`/specd-new`**        | Creation            | When starting a new capability, feature, or refactor                          |\n| **`/specd-design`**     | Planning            | When authoring or revising specs, test scenarios, design docs, or tasks       |\n| **`/specd-implement`**  | Execution           | When implementing code against the agreed `tasks.md` checklist                |\n| **`/specd-verify`**     | Validation          | When testing the code against the `verify.md` acceptance scenarios            |\n| **`/specd-archive`**    | Consolidation       | When merging validated specs into `specs/` and archiving the change           |\n| **`/specd-compliance`** | Audit               | When reviewing compliance between implementation and specs                    |\n| **`/specd-fasttrack`**  | Code-First / Hotfix | For quick spikes, urgent bugfixes, or code-first prototyping                  |\n\n> For technical details on stop-points, CLI interactions, and customizing prompt instructions, see the **[Official Skills Guide](./skills.md)**.\n\n---\n"
      },
      {
        "index": 11,
        "heading": "Next Steps",
        "level": 2,
        "startLine": 163,
        "endLine": 169,
        "lines": 7,
        "content": "## Next Steps\n\n- Review the [Installation & Initialization Guide](./installation.md) to set up plugins for your editor or agent.\n- Read [The Philosophy of Spec-Driven Development](./philosophy.md) for the core principles of spec-first engineering.\n- Learn about [Workflow & Lifecycle](./workflow.md) for approval gates and state transitions.\n- Explore [Workspaces](./workspaces.md) if you are working in a monorepo.\n"
      }
    ]
  },
  {
    "topic": "installation",
    "title": "Installation & Initialization",
    "description": "Step-by-step guide to installing the SpecD CLI, system prerequisites, project initialization, and agent plugin setup.",
    "order": 2,
    "content": "# Installation & Initialization\n\nThis guide walks you through installing the SpecD CLI on your machine and initializing SpecD in a new or existing repository.\n\n---\n\n## 1. Prerequisites & System Requirements\n\nBefore installing SpecD, ensure your environment meets the following requirements:\n\n| Component            | Minimum Version                                | Notes                                                                         |\n| -------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------- |\n| **Node.js**          | `>= 20.18.0` (LTS recommended)                 | SpecD is distributed as ESM JavaScript. Check with `node -v`.                 |\n| **Package Manager**  | `npm` (>= 10), `pnpm` (>= 9), `yarn`, or `bun` | SpecD works with any standard package manager.                                |\n| **VCS**              | Git `>= 2.30` or Mercurial (`hg`)              | SpecD detects repository root, current branch, and clean state automatically. |\n| **Operating System** | macOS, Linux, or Windows (WSL recommended)     | Fully cross-platform.                                                         |\n\n---\n\n## 2. Installing the SpecD CLI\n\nThe primary SpecD CLI is distributed as `@specd/specd` (or `@specd/cli` directly):\n\n### Global Installation (Recommended)\n\nInstalling globally gives you the `specd` executable anywhere in your terminal:\n\n```bash npm2yarn\nnpm install -g @specd/specd\n```\n\nVerify the installation:\n\n```bash\nspecd --version\nwhich specd\n```\n\n### Local Project Dependency\n\nIf you prefer to lock SpecD to a specific repository version:\n\n```bash npm2yarn\nnpm install --save-dev @specd/specd\n```\n\nYou can then run commands via `npx specd` or package scripts:\n\n```json title=\"package.json\"\n{\n  \"scripts\": {\n    \"specd\": \"specd\"\n  }\n}\n```\n\n### Direct Execution with `npx`\n\nYou can also run SpecD commands without installing:\n\n```bash\nnpx @specd/specd --help\n```\n\n---\n\n## 3. Initializing SpecD in a Project\n\nTo set up SpecD in your repository, run the initialization command in the project root:\n\n```bash\nspecd project init\n```\n\n_(You can also use the shorthand `specd init`.)_\n\n### Interactive Mode (TTY)\n\nWhen executed in a terminal without flags, `specd project init` launches an interactive wizard powered by `@clack/prompts`:\n\n1. **Schema reference:** Defaults to `@specd/schema-std` (the standard proposal-spec-design-tasks workflow).\n2. **Default workspace ID:** Defaults to `default`.\n3. **Specs directory path:** Defaults to `specs/`.\n4. **Agent plugins selection:** A multiselect list of agent plugins to install immediately:\n   - `@specd/plugin-agent-claude` (Anthropic Claude Code / `.claude/`)\n   - `@specd/plugin-agent-copilot` (GitHub Copilot / `.github/`)\n   - `@specd/plugin-agent-codex` (OpenAI Codex / `.codex/`)\n   - `@specd/plugin-agent-opencode` (OpenCode / `.opencode/`)\n   - `@specd/plugin-agent-standard` (Standard agent skills / `.agents/`)\n5. **Confirmation:** Confirms file creation or overwriting if `specd.yaml` already exists.\n\n```text\n┌  specd project init\n│\n◆  Schema reference:\n│  @specd/schema-std\n│\n◆  Default workspace ID:\n│  default\n│\n◆  Specs path:\n│  specs/\n│\n◆  Select plugins to install:\n│  ◻ @specd/plugin-agent-claude\n│  ◻ @specd/plugin-agent-copilot\n│  ◻ @specd/plugin-agent-codex\n│  ◻ @specd/plugin-agent-opencode\n│  ◼ @specd/plugin-agent-standard\n│\n└  initialized specd in /path/to/project\n```\n\n### Non-Interactive / CI Mode\n\nFor scripts, Docker builds, or CI environments, pass the desired options as command-line flags:\n\n```bash\nspecd project init \\\n  --schema @specd/schema-std \\\n  --workspace default \\\n  --workspace-path specs/ \\\n  --plugin @specd/plugin-agent-claude \\\n  --plugin @specd/plugin-agent-standard \\\n  --force \\\n  --format json\n```\n\n#### Command Options\n\n| Option                        | Description                                       | Default             |\n| ----------------------------- | ------------------------------------------------- | ------------------- |\n| `--schema <ref>`              | Schema package reference or local path            | `@specd/schema-std` |\n| `--workspace <id>`            | Identifier for the default workspace              | `default`           |\n| `--workspace-path <path>`     | Path to the directory where specs live            | `specs/`            |\n| `--plugin <name>`             | Agent plugin to install after init (repeatable)   | `[]`                |\n| `--force`                     | Overwrite existing `specd.yaml` without prompting | `false`             |\n| `--format <text\\|json\\|toon>` | Output format                                     | `text`              |\n\n---\n\n## 4. What Initialization Creates\n\nRunning `specd project init` sets up the foundational directory structure and files:\n\n```\nmy-project/\n├── specd.yaml                  # Core configuration file\n├── specs/                      # Living specifications workspace directory\n├── .specd/                     # SpecD metadata & state storage\n│   ├── changes/                # Active change units\n│   ├── drafts/                 # Paused / shelved changes\n│   ├── discarded/              # Abandoned change audit logs\n│   ├── archive/                # Archived completed changes\n│   └── metadata/               # Local index & graph cache (gitignored)\n└── .gitignore                  # Automatically appended with metadata cache entries\n```\n\n### `specd.yaml`\n\nThe generated configuration file declares your schema and default workspace:\n\n```yaml title=\"specd.yaml\"\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specsPath: specs/\n\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-standard'\n```\n\n### Automatic `.gitignore` Updates\n\nSpecD automatically updates your `.gitignore` to prevent transient caches and local overrides from being committed:\n\n```gitignore title=\".gitignore (appended by SpecD)\"\n# SpecD metadata and local cache\n/.specd/metadata/\nspecd.local.yaml\nspecd.local.*.yaml\n```\n\n---\n\n## 5. Installing and Managing Agent Plugins\n\nAgent plugins configure your coding assistant (Claude Code, GitHub Copilot, OpenAI Codex, OpenCode, or custom agents) with the SpecD slash skills (`/specd`, `/specd-new`, `/specd-design`, `/specd-implement`, `/specd-verify`, `/specd-archive`).\n\n### Adding Plugins Post-Init\n\nIf you did not select a plugin during initialization or want to add another agent:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude\n```\n\nYou can install multiple plugins in a single command:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude @specd/plugin-agent-copilot\n```\n\n### Listing Installed Plugins\n\nCheck runtime status and loaded versions:\n\n```bash\nspecd plugins list\n```\n\nExample output:\n\n```text\nInstalled plugins:\n@specd/plugin-agent-claude    agents  0.2.0  installed\n@specd/plugin-agent-copilot   agents  0.2.0  installed\n```\n\n### Inspecting Plugin Capabilities\n\n```bash\nspecd plugins show @specd/plugin-agent-claude\n```\n\n### Updating or Uninstalling Plugins\n\n```bash\n# Update installed plugins\nspecd plugins update @specd/plugin-agent-claude\n\n# Remove a plugin and remove its skills\nspecd plugins uninstall @specd/plugin-agent-copilot\n```\n\n---\n\n## 6. Verifying Your Setup\n\nOnce initialized, verify that your project configuration and plugins are valid:\n\n```bash\nspecd project status\n```\n\nYou should see output confirming:\n\n- The active schema (`@specd/schema-std`).\n- The configured workspaces (`default`).\n- Zero active changes (clean slate).\n- VCS adapter status (branch and clean working tree).\n\n---\n\n## Next Steps\n\nNow that SpecD is installed and initialized:\n\n- Follow the [Quickstart / Getting Started Tutorial](./getting-started.md) to open your first change.\n- Understand the [Philosophy of Spec-Driven Development](./philosophy.md).\n- Learn about [Skills](./skills.md) and how to interact with your AI coding assistant.\n",
    "lineCount": 264,
    "byteLength": 8008,
    "outline": [
      {
        "index": 1,
        "heading": "Installation & Initialization",
        "level": 1,
        "startLine": 1,
        "endLine": 264,
        "lines": 264,
        "content": "# Installation & Initialization\n\nThis guide walks you through installing the SpecD CLI on your machine and initializing SpecD in a new or existing repository.\n\n---\n\n## 1. Prerequisites & System Requirements\n\nBefore installing SpecD, ensure your environment meets the following requirements:\n\n| Component            | Minimum Version                                | Notes                                                                         |\n| -------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------- |\n| **Node.js**          | `>= 20.18.0` (LTS recommended)                 | SpecD is distributed as ESM JavaScript. Check with `node -v`.                 |\n| **Package Manager**  | `npm` (>= 10), `pnpm` (>= 9), `yarn`, or `bun` | SpecD works with any standard package manager.                                |\n| **VCS**              | Git `>= 2.30` or Mercurial (`hg`)              | SpecD detects repository root, current branch, and clean state automatically. |\n| **Operating System** | macOS, Linux, or Windows (WSL recommended)     | Fully cross-platform.                                                         |\n\n---\n\n## 2. Installing the SpecD CLI\n\nThe primary SpecD CLI is distributed as `@specd/specd` (or `@specd/cli` directly):\n\n### Global Installation (Recommended)\n\nInstalling globally gives you the `specd` executable anywhere in your terminal:\n\n```bash npm2yarn\nnpm install -g @specd/specd\n```\n\nVerify the installation:\n\n```bash\nspecd --version\nwhich specd\n```\n\n### Local Project Dependency\n\nIf you prefer to lock SpecD to a specific repository version:\n\n```bash npm2yarn\nnpm install --save-dev @specd/specd\n```\n\nYou can then run commands via `npx specd` or package scripts:\n\n```json title=\"package.json\"\n{\n  \"scripts\": {\n    \"specd\": \"specd\"\n  }\n}\n```\n\n### Direct Execution with `npx`\n\nYou can also run SpecD commands without installing:\n\n```bash\nnpx @specd/specd --help\n```\n\n---\n\n## 3. Initializing SpecD in a Project\n\nTo set up SpecD in your repository, run the initialization command in the project root:\n\n```bash\nspecd project init\n```\n\n_(You can also use the shorthand `specd init`.)_\n\n### Interactive Mode (TTY)\n\nWhen executed in a terminal without flags, `specd project init` launches an interactive wizard powered by `@clack/prompts`:\n\n1. **Schema reference:** Defaults to `@specd/schema-std` (the standard proposal-spec-design-tasks workflow).\n2. **Default workspace ID:** Defaults to `default`.\n3. **Specs directory path:** Defaults to `specs/`.\n4. **Agent plugins selection:** A multiselect list of agent plugins to install immediately:\n   - `@specd/plugin-agent-claude` (Anthropic Claude Code / `.claude/`)\n   - `@specd/plugin-agent-copilot` (GitHub Copilot / `.github/`)\n   - `@specd/plugin-agent-codex` (OpenAI Codex / `.codex/`)\n   - `@specd/plugin-agent-opencode` (OpenCode / `.opencode/`)\n   - `@specd/plugin-agent-standard` (Standard agent skills / `.agents/`)\n5. **Confirmation:** Confirms file creation or overwriting if `specd.yaml` already exists.\n\n```text\n┌  specd project init\n│\n◆  Schema reference:\n│  @specd/schema-std\n│\n◆  Default workspace ID:\n│  default\n│\n◆  Specs path:\n│  specs/\n│\n◆  Select plugins to install:\n│  ◻ @specd/plugin-agent-claude\n│  ◻ @specd/plugin-agent-copilot\n│  ◻ @specd/plugin-agent-codex\n│  ◻ @specd/plugin-agent-opencode\n│  ◼ @specd/plugin-agent-standard\n│\n└  initialized specd in /path/to/project\n```\n\n### Non-Interactive / CI Mode\n\nFor scripts, Docker builds, or CI environments, pass the desired options as command-line flags:\n\n```bash\nspecd project init \\\n  --schema @specd/schema-std \\\n  --workspace default \\\n  --workspace-path specs/ \\\n  --plugin @specd/plugin-agent-claude \\\n  --plugin @specd/plugin-agent-standard \\\n  --force \\\n  --format json\n```\n\n#### Command Options\n\n| Option                        | Description                                       | Default             |\n| ----------------------------- | ------------------------------------------------- | ------------------- |\n| `--schema <ref>`              | Schema package reference or local path            | `@specd/schema-std` |\n| `--workspace <id>`            | Identifier for the default workspace              | `default`           |\n| `--workspace-path <path>`     | Path to the directory where specs live            | `specs/`            |\n| `--plugin <name>`             | Agent plugin to install after init (repeatable)   | `[]`                |\n| `--force`                     | Overwrite existing `specd.yaml` without prompting | `false`             |\n| `--format <text\\|json\\|toon>` | Output format                                     | `text`              |\n\n---\n\n## 4. What Initialization Creates\n\nRunning `specd project init` sets up the foundational directory structure and files:\n\n```\nmy-project/\n├── specd.yaml                  # Core configuration file\n├── specs/                      # Living specifications workspace directory\n├── .specd/                     # SpecD metadata & state storage\n│   ├── changes/                # Active change units\n│   ├── drafts/                 # Paused / shelved changes\n│   ├── discarded/              # Abandoned change audit logs\n│   ├── archive/                # Archived completed changes\n│   └── metadata/               # Local index & graph cache (gitignored)\n└── .gitignore                  # Automatically appended with metadata cache entries\n```\n\n### `specd.yaml`\n\nThe generated configuration file declares your schema and default workspace:\n\n```yaml title=\"specd.yaml\"\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specsPath: specs/\n\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-standard'\n```\n\n### Automatic `.gitignore` Updates\n\nSpecD automatically updates your `.gitignore` to prevent transient caches and local overrides from being committed:\n\n```gitignore title=\".gitignore (appended by SpecD)\"\n# SpecD metadata and local cache\n/.specd/metadata/\nspecd.local.yaml\nspecd.local.*.yaml\n```\n\n---\n\n## 5. Installing and Managing Agent Plugins\n\nAgent plugins configure your coding assistant (Claude Code, GitHub Copilot, OpenAI Codex, OpenCode, or custom agents) with the SpecD slash skills (`/specd`, `/specd-new`, `/specd-design`, `/specd-implement`, `/specd-verify`, `/specd-archive`).\n\n### Adding Plugins Post-Init\n\nIf you did not select a plugin during initialization or want to add another agent:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude\n```\n\nYou can install multiple plugins in a single command:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude @specd/plugin-agent-copilot\n```\n\n### Listing Installed Plugins\n\nCheck runtime status and loaded versions:\n\n```bash\nspecd plugins list\n```\n\nExample output:\n\n```text\nInstalled plugins:\n@specd/plugin-agent-claude    agents  0.2.0  installed\n@specd/plugin-agent-copilot   agents  0.2.0  installed\n```\n\n### Inspecting Plugin Capabilities\n\n```bash\nspecd plugins show @specd/plugin-agent-claude\n```\n\n### Updating or Uninstalling Plugins\n\n```bash\n# Update installed plugins\nspecd plugins update @specd/plugin-agent-claude\n\n# Remove a plugin and remove its skills\nspecd plugins uninstall @specd/plugin-agent-copilot\n```\n\n---\n\n## 6. Verifying Your Setup\n\nOnce initialized, verify that your project configuration and plugins are valid:\n\n```bash\nspecd project status\n```\n\nYou should see output confirming:\n\n- The active schema (`@specd/schema-std`).\n- The configured workspaces (`default`).\n- Zero active changes (clean slate).\n- VCS adapter status (branch and clean working tree).\n\n---\n\n## Next Steps\n\nNow that SpecD is installed and initialized:\n\n- Follow the [Quickstart / Getting Started Tutorial](./getting-started.md) to open your first change.\n- Understand the [Philosophy of Spec-Driven Development](./philosophy.md).\n- Learn about [Skills](./skills.md) and how to interact with your AI coding assistant.\n"
      },
      {
        "index": 2,
        "heading": "1. Prerequisites & System Requirements",
        "level": 2,
        "startLine": 7,
        "endLine": 19,
        "lines": 13,
        "content": "## 1. Prerequisites & System Requirements\n\nBefore installing SpecD, ensure your environment meets the following requirements:\n\n| Component            | Minimum Version                                | Notes                                                                         |\n| -------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------- |\n| **Node.js**          | `>= 20.18.0` (LTS recommended)                 | SpecD is distributed as ESM JavaScript. Check with `node -v`.                 |\n| **Package Manager**  | `npm` (>= 10), `pnpm` (>= 9), `yarn`, or `bun` | SpecD works with any standard package manager.                                |\n| **VCS**              | Git `>= 2.30` or Mercurial (`hg`)              | SpecD detects repository root, current branch, and clean state automatically. |\n| **Operating System** | macOS, Linux, or Windows (WSL recommended)     | Fully cross-platform.                                                         |\n\n---\n"
      },
      {
        "index": 3,
        "heading": "2. Installing the SpecD CLI",
        "level": 2,
        "startLine": 20,
        "endLine": 66,
        "lines": 47,
        "content": "## 2. Installing the SpecD CLI\n\nThe primary SpecD CLI is distributed as `@specd/specd` (or `@specd/cli` directly):\n\n### Global Installation (Recommended)\n\nInstalling globally gives you the `specd` executable anywhere in your terminal:\n\n```bash npm2yarn\nnpm install -g @specd/specd\n```\n\nVerify the installation:\n\n```bash\nspecd --version\nwhich specd\n```\n\n### Local Project Dependency\n\nIf you prefer to lock SpecD to a specific repository version:\n\n```bash npm2yarn\nnpm install --save-dev @specd/specd\n```\n\nYou can then run commands via `npx specd` or package scripts:\n\n```json title=\"package.json\"\n{\n  \"scripts\": {\n    \"specd\": \"specd\"\n  }\n}\n```\n\n### Direct Execution with `npx`\n\nYou can also run SpecD commands without installing:\n\n```bash\nnpx @specd/specd --help\n```\n\n---\n"
      },
      {
        "index": 4,
        "heading": "Global Installation (Recommended)",
        "level": 3,
        "startLine": 24,
        "endLine": 38,
        "lines": 15,
        "content": "### Global Installation (Recommended)\n\nInstalling globally gives you the `specd` executable anywhere in your terminal:\n\n```bash npm2yarn\nnpm install -g @specd/specd\n```\n\nVerify the installation:\n\n```bash\nspecd --version\nwhich specd\n```\n"
      },
      {
        "index": 5,
        "heading": "Local Project Dependency",
        "level": 3,
        "startLine": 39,
        "endLine": 56,
        "lines": 18,
        "content": "### Local Project Dependency\n\nIf you prefer to lock SpecD to a specific repository version:\n\n```bash npm2yarn\nnpm install --save-dev @specd/specd\n```\n\nYou can then run commands via `npx specd` or package scripts:\n\n```json title=\"package.json\"\n{\n  \"scripts\": {\n    \"specd\": \"specd\"\n  }\n}\n```\n"
      },
      {
        "index": 6,
        "heading": "Direct Execution with `npx`",
        "level": 3,
        "startLine": 57,
        "endLine": 66,
        "lines": 10,
        "content": "### Direct Execution with `npx`\n\nYou can also run SpecD commands without installing:\n\n```bash\nnpx @specd/specd --help\n```\n\n---\n"
      },
      {
        "index": 7,
        "heading": "3. Initializing SpecD in a Project",
        "level": 2,
        "startLine": 67,
        "endLine": 141,
        "lines": 75,
        "content": "## 3. Initializing SpecD in a Project\n\nTo set up SpecD in your repository, run the initialization command in the project root:\n\n```bash\nspecd project init\n```\n\n_(You can also use the shorthand `specd init`.)_\n\n### Interactive Mode (TTY)\n\nWhen executed in a terminal without flags, `specd project init` launches an interactive wizard powered by `@clack/prompts`:\n\n1. **Schema reference:** Defaults to `@specd/schema-std` (the standard proposal-spec-design-tasks workflow).\n2. **Default workspace ID:** Defaults to `default`.\n3. **Specs directory path:** Defaults to `specs/`.\n4. **Agent plugins selection:** A multiselect list of agent plugins to install immediately:\n   - `@specd/plugin-agent-claude` (Anthropic Claude Code / `.claude/`)\n   - `@specd/plugin-agent-copilot` (GitHub Copilot / `.github/`)\n   - `@specd/plugin-agent-codex` (OpenAI Codex / `.codex/`)\n   - `@specd/plugin-agent-opencode` (OpenCode / `.opencode/`)\n   - `@specd/plugin-agent-standard` (Standard agent skills / `.agents/`)\n5. **Confirmation:** Confirms file creation or overwriting if `specd.yaml` already exists.\n\n```text\n┌  specd project init\n│\n◆  Schema reference:\n│  @specd/schema-std\n│\n◆  Default workspace ID:\n│  default\n│\n◆  Specs path:\n│  specs/\n│\n◆  Select plugins to install:\n│  ◻ @specd/plugin-agent-claude\n│  ◻ @specd/plugin-agent-copilot\n│  ◻ @specd/plugin-agent-codex\n│  ◻ @specd/plugin-agent-opencode\n│  ◼ @specd/plugin-agent-standard\n│\n└  initialized specd in /path/to/project\n```\n\n### Non-Interactive / CI Mode\n\nFor scripts, Docker builds, or CI environments, pass the desired options as command-line flags:\n\n```bash\nspecd project init \\\n  --schema @specd/schema-std \\\n  --workspace default \\\n  --workspace-path specs/ \\\n  --plugin @specd/plugin-agent-claude \\\n  --plugin @specd/plugin-agent-standard \\\n  --force \\\n  --format json\n```\n\n#### Command Options\n\n| Option                        | Description                                       | Default             |\n| ----------------------------- | ------------------------------------------------- | ------------------- |\n| `--schema <ref>`              | Schema package reference or local path            | `@specd/schema-std` |\n| `--workspace <id>`            | Identifier for the default workspace              | `default`           |\n| `--workspace-path <path>`     | Path to the directory where specs live            | `specs/`            |\n| `--plugin <name>`             | Agent plugin to install after init (repeatable)   | `[]`                |\n| `--force`                     | Overwrite existing `specd.yaml` without prompting | `false`             |\n| `--format <text\\|json\\|toon>` | Output format                                     | `text`              |\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Interactive Mode (TTY)",
        "level": 3,
        "startLine": 77,
        "endLine": 113,
        "lines": 37,
        "content": "### Interactive Mode (TTY)\n\nWhen executed in a terminal without flags, `specd project init` launches an interactive wizard powered by `@clack/prompts`:\n\n1. **Schema reference:** Defaults to `@specd/schema-std` (the standard proposal-spec-design-tasks workflow).\n2. **Default workspace ID:** Defaults to `default`.\n3. **Specs directory path:** Defaults to `specs/`.\n4. **Agent plugins selection:** A multiselect list of agent plugins to install immediately:\n   - `@specd/plugin-agent-claude` (Anthropic Claude Code / `.claude/`)\n   - `@specd/plugin-agent-copilot` (GitHub Copilot / `.github/`)\n   - `@specd/plugin-agent-codex` (OpenAI Codex / `.codex/`)\n   - `@specd/plugin-agent-opencode` (OpenCode / `.opencode/`)\n   - `@specd/plugin-agent-standard` (Standard agent skills / `.agents/`)\n5. **Confirmation:** Confirms file creation or overwriting if `specd.yaml` already exists.\n\n```text\n┌  specd project init\n│\n◆  Schema reference:\n│  @specd/schema-std\n│\n◆  Default workspace ID:\n│  default\n│\n◆  Specs path:\n│  specs/\n│\n◆  Select plugins to install:\n│  ◻ @specd/plugin-agent-claude\n│  ◻ @specd/plugin-agent-copilot\n│  ◻ @specd/plugin-agent-codex\n│  ◻ @specd/plugin-agent-opencode\n│  ◼ @specd/plugin-agent-standard\n│\n└  initialized specd in /path/to/project\n```\n"
      },
      {
        "index": 9,
        "heading": "Non-Interactive / CI Mode",
        "level": 3,
        "startLine": 114,
        "endLine": 141,
        "lines": 28,
        "content": "### Non-Interactive / CI Mode\n\nFor scripts, Docker builds, or CI environments, pass the desired options as command-line flags:\n\n```bash\nspecd project init \\\n  --schema @specd/schema-std \\\n  --workspace default \\\n  --workspace-path specs/ \\\n  --plugin @specd/plugin-agent-claude \\\n  --plugin @specd/plugin-agent-standard \\\n  --force \\\n  --format json\n```\n\n#### Command Options\n\n| Option                        | Description                                       | Default             |\n| ----------------------------- | ------------------------------------------------- | ------------------- |\n| `--schema <ref>`              | Schema package reference or local path            | `@specd/schema-std` |\n| `--workspace <id>`            | Identifier for the default workspace              | `default`           |\n| `--workspace-path <path>`     | Path to the directory where specs live            | `specs/`            |\n| `--plugin <name>`             | Agent plugin to install after init (repeatable)   | `[]`                |\n| `--force`                     | Overwrite existing `specd.yaml` without prompting | `false`             |\n| `--format <text\\|json\\|toon>` | Output format                                     | `text`              |\n\n---\n"
      },
      {
        "index": 10,
        "heading": "Command Options",
        "level": 4,
        "startLine": 129,
        "endLine": 141,
        "lines": 13,
        "content": "#### Command Options\n\n| Option                        | Description                                       | Default             |\n| ----------------------------- | ------------------------------------------------- | ------------------- |\n| `--schema <ref>`              | Schema package reference or local path            | `@specd/schema-std` |\n| `--workspace <id>`            | Identifier for the default workspace              | `default`           |\n| `--workspace-path <path>`     | Path to the directory where specs live            | `specs/`            |\n| `--plugin <name>`             | Agent plugin to install after init (repeatable)   | `[]`                |\n| `--force`                     | Overwrite existing `specd.yaml` without prompting | `false`             |\n| `--format <text\\|json\\|toon>` | Output format                                     | `text`              |\n\n---\n"
      },
      {
        "index": 11,
        "heading": "4. What Initialization Creates",
        "level": 2,
        "startLine": 142,
        "endLine": 187,
        "lines": 46,
        "content": "## 4. What Initialization Creates\n\nRunning `specd project init` sets up the foundational directory structure and files:\n\n```\nmy-project/\n├── specd.yaml                  # Core configuration file\n├── specs/                      # Living specifications workspace directory\n├── .specd/                     # SpecD metadata & state storage\n│   ├── changes/                # Active change units\n│   ├── drafts/                 # Paused / shelved changes\n│   ├── discarded/              # Abandoned change audit logs\n│   ├── archive/                # Archived completed changes\n│   └── metadata/               # Local index & graph cache (gitignored)\n└── .gitignore                  # Automatically appended with metadata cache entries\n```\n\n### `specd.yaml`\n\nThe generated configuration file declares your schema and default workspace:\n\n```yaml title=\"specd.yaml\"\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specsPath: specs/\n\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-standard'\n```\n\n### Automatic `.gitignore` Updates\n\nSpecD automatically updates your `.gitignore` to prevent transient caches and local overrides from being committed:\n\n```gitignore title=\".gitignore (appended by SpecD)\"\n# SpecD metadata and local cache\n/.specd/metadata/\nspecd.local.yaml\nspecd.local.*.yaml\n```\n\n---\n"
      },
      {
        "index": 12,
        "heading": "`specd.yaml`",
        "level": 3,
        "startLine": 159,
        "endLine": 174,
        "lines": 16,
        "content": "### `specd.yaml`\n\nThe generated configuration file declares your schema and default workspace:\n\n```yaml title=\"specd.yaml\"\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specsPath: specs/\n\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-standard'\n```\n"
      },
      {
        "index": 13,
        "heading": "Automatic `.gitignore` Updates",
        "level": 3,
        "startLine": 175,
        "endLine": 187,
        "lines": 13,
        "content": "### Automatic `.gitignore` Updates\n\nSpecD automatically updates your `.gitignore` to prevent transient caches and local overrides from being committed:\n\n```gitignore title=\".gitignore (appended by SpecD)\"\n# SpecD metadata and local cache\n/.specd/metadata/\nspecd.local.yaml\nspecd.local.*.yaml\n```\n\n---\n"
      },
      {
        "index": 14,
        "heading": "5. Installing and Managing Agent Plugins",
        "level": 2,
        "startLine": 188,
        "endLine": 239,
        "lines": 52,
        "content": "## 5. Installing and Managing Agent Plugins\n\nAgent plugins configure your coding assistant (Claude Code, GitHub Copilot, OpenAI Codex, OpenCode, or custom agents) with the SpecD slash skills (`/specd`, `/specd-new`, `/specd-design`, `/specd-implement`, `/specd-verify`, `/specd-archive`).\n\n### Adding Plugins Post-Init\n\nIf you did not select a plugin during initialization or want to add another agent:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude\n```\n\nYou can install multiple plugins in a single command:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude @specd/plugin-agent-copilot\n```\n\n### Listing Installed Plugins\n\nCheck runtime status and loaded versions:\n\n```bash\nspecd plugins list\n```\n\nExample output:\n\n```text\nInstalled plugins:\n@specd/plugin-agent-claude    agents  0.2.0  installed\n@specd/plugin-agent-copilot   agents  0.2.0  installed\n```\n\n### Inspecting Plugin Capabilities\n\n```bash\nspecd plugins show @specd/plugin-agent-claude\n```\n\n### Updating or Uninstalling Plugins\n\n```bash\n# Update installed plugins\nspecd plugins update @specd/plugin-agent-claude\n\n# Remove a plugin and remove its skills\nspecd plugins uninstall @specd/plugin-agent-copilot\n```\n\n---\n"
      },
      {
        "index": 15,
        "heading": "Adding Plugins Post-Init",
        "level": 3,
        "startLine": 192,
        "endLine": 205,
        "lines": 14,
        "content": "### Adding Plugins Post-Init\n\nIf you did not select a plugin during initialization or want to add another agent:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude\n```\n\nYou can install multiple plugins in a single command:\n\n```bash\nspecd plugins install @specd/plugin-agent-claude @specd/plugin-agent-copilot\n```\n"
      },
      {
        "index": 16,
        "heading": "Listing Installed Plugins",
        "level": 3,
        "startLine": 206,
        "endLine": 221,
        "lines": 16,
        "content": "### Listing Installed Plugins\n\nCheck runtime status and loaded versions:\n\n```bash\nspecd plugins list\n```\n\nExample output:\n\n```text\nInstalled plugins:\n@specd/plugin-agent-claude    agents  0.2.0  installed\n@specd/plugin-agent-copilot   agents  0.2.0  installed\n```\n"
      },
      {
        "index": 17,
        "heading": "Inspecting Plugin Capabilities",
        "level": 3,
        "startLine": 222,
        "endLine": 227,
        "lines": 6,
        "content": "### Inspecting Plugin Capabilities\n\n```bash\nspecd plugins show @specd/plugin-agent-claude\n```\n"
      },
      {
        "index": 18,
        "heading": "Updating or Uninstalling Plugins",
        "level": 3,
        "startLine": 228,
        "endLine": 239,
        "lines": 12,
        "content": "### Updating or Uninstalling Plugins\n\n```bash\n# Update installed plugins\nspecd plugins update @specd/plugin-agent-claude\n\n# Remove a plugin and remove its skills\nspecd plugins uninstall @specd/plugin-agent-copilot\n```\n\n---\n"
      },
      {
        "index": 19,
        "heading": "6. Verifying Your Setup",
        "level": 2,
        "startLine": 240,
        "endLine": 256,
        "lines": 17,
        "content": "## 6. Verifying Your Setup\n\nOnce initialized, verify that your project configuration and plugins are valid:\n\n```bash\nspecd project status\n```\n\nYou should see output confirming:\n\n- The active schema (`@specd/schema-std`).\n- The configured workspaces (`default`).\n- Zero active changes (clean slate).\n- VCS adapter status (branch and clean working tree).\n\n---\n"
      },
      {
        "index": 20,
        "heading": "Next Steps",
        "level": 2,
        "startLine": 257,
        "endLine": 264,
        "lines": 8,
        "content": "## Next Steps\n\nNow that SpecD is installed and initialized:\n\n- Follow the [Quickstart / Getting Started Tutorial](./getting-started.md) to open your first change.\n- Understand the [Philosophy of Spec-Driven Development](./philosophy.md).\n- Learn about [Skills](./skills.md) and how to interact with your AI coding assistant.\n"
      }
    ]
  },
  {
    "topic": "philosophy",
    "title": "The Philosophy of Spec-Driven Development",
    "description": "The core philosophy behind spec-driven development, inverting the order of operations, and human-agent contracts",
    "order": 2,
    "content": "# The Philosophy of Spec-Driven Development\n\nSpec-driven development (SDD) is an architectural discipline designed to restore certainty, durability, and alignment to software engineering — especially in the era of autonomous AI coding agents.\n\n---\n\n## The Problem with Traditional Workflows\n\nMost software development follows a pattern that looks roughly like this:\n\n> idea → rough ticket → code → hope it works → document later (maybe)\n\nRequirements live in a chat message, a half-finished ticket, or someone's memory. A developer — or increasingly, an AI agent — receives a vague prompt and fills the gaps with assumptions. When the output does not match the intent, it is hard to say exactly why, because the intent was never formally stated.\n\nThe result is a familiar cycle: rework, misalignment, and a growing gap between what the system does and what anyone can confidently say it was meant to do. Documentation, if it exists at all, is written after the fact and describes what was built rather than what was intended.\n\n---\n\n## What Spec-Driven Development Does Differently\n\nSpec-driven development inverts the order of operations:\n\n> idea → specify requirements → write verification scenarios → design → implement → verify against spec → archive\n\nThe specification comes first. It describes what the system SHALL do — not how. It is written in plain language, reviewed like code, and treated as a first-class artifact alongside the implementation. The verification scenarios written alongside it describe how to confirm correctness in concrete, unambiguous terms.\n\nThis is not a new idea. Engineering disciplines outside software have worked this way for decades. SpecD makes it practical for day-to-day software development, particularly when AI agents are involved in implementation.\n\n---\n\n## What a Spec Actually Is\n\nA spec is a living requirement document. It answers the question: **what should this capability do?**\n\n- It is written in Markdown, in natural language, with normative language: `SHALL`, `MUST`, `SHOULD`.\n- It is paired with a verification file (`verify.md`) that describes concrete `WHEN`/`THEN` scenarios — not vague acceptance criteria, but specific conditions and expected outcomes.\n- It outlives the change that created it. Once written, a spec is a permanent record of intent. It evolves through structured delta modifications that preserve its history.\n- It is the source of truth for what the system should do, independent of any particular implementation.\n\nA spec does not describe how the code is structured. It does not name functions or modules. It says what the system shall do and what it shall not do. The implementation is a consequence of the spec, not the other way around.\n\n---\n\n## Why Specs Must Come Before Code\n\nForcing clarity before implementation has compounding benefits:\n\n### It makes intent explicit\n\nVague requirements produce vague implementations. Writing a spec forces you to answer questions you would otherwise defer: what are the edge cases? What are the constraints? What does success actually look like?\n\n### It creates alignment between humans and agents\n\nAn AI agent working from a spec is working from a contract. It knows what it is supposed to produce, in what order, and against what criteria. The spec is not a suggestion — it is the acceptance test.\n\n### It provides verifiable acceptance criteria\n\n\"The login should work\" is not a requirement. \"WHEN a user submits valid credentials, THEN they receive an authenticated session token\" is. Verification scenarios make it possible to determine, unambiguously, whether the implementation is correct.\n\n### It enables governance without blocking velocity\n\nApproval gates become natural checkpoints when they are attached to well-defined specs. A reviewer can evaluate whether a design satisfies a spec. A compliance check can verify whether code satisfies verification scenarios. These gates carry meaning because the specs they reference carry meaning.\n\n### It creates an audit trail of why\n\nCode tells you what the system does today. A commit message tells you what changed. A spec tells you why the capability exists and what it was intended to do — even after the code has been refactored, rewritten, or replaced entirely.\n\n### Specs survive refactors; code does not\n\nYou can rewrite the implementation from scratch and still have a complete record of what it was supposed to do. The spec is independent of the technology, the architecture, and the team that wrote the original version.\n\n---\n\n## The Key Insight\n\nThere are four different things that say something about a software system, and they answer different questions:\n\n| Artifact                   | Question it answers                     |\n| -------------------------- | --------------------------------------- |\n| **Code**                   | What does the system do today?          |\n| **Tests**                  | Does the code behave as programmed?     |\n| **Specs**                  | What should the system do, and why?     |\n| **Verification scenarios** | Does the code satisfy the requirements? |\n\nMost teams have code and tests. Few have durable, structured specs. Fewer still have explicit verification scenarios that connect requirements to outcomes. SpecD fills that gap.\n\n---\n\n## The SpecD Approach\n\nSpecD is a platform that makes spec-driven development practical without imposing heavy process:\n\n- **Structure through Schemas:** A schema defines what artifact types exist in your project, what files they produce, and what order they must be produced in. You cannot produce a design until a spec exists. You cannot produce tasks until a design exists. The structure enforces deliberate sequencing without requiring manual enforcement.\n- **Workflow through the Change Lifecycle:** A change tracks a unit of work from initial idea through implementation to completion. Each lifecycle state has a clear meaning. Optional approval gates enforce compliance checks at the transitions that matter most.\n- **Tooling through the CLI and MCP Server:** You can manage the full lifecycle from the command line. AI agents can interact through the MCP interface, which exposes the same workflow as structured tool calls.\n- **Context through Compilation:** At each lifecycle step, SpecD assembles the right set of specs, schema instructions, and lifecycle context into a single block — ready to inject into the agent. The agent gets exactly what it needs for the current step, with full detail on the specs it is directly working with and lighter summaries for broader context.\n- **Human-Agent Collaboration:** Specs are the contract between human intent and agent execution. The agent reads the spec, produces artifacts against it, and is checked against it. The human defines what should be built. The agent works out how.\n",
    "lineCount": 98,
    "byteLength": 6843,
    "outline": [
      {
        "index": 1,
        "heading": "The Philosophy of Spec-Driven Development",
        "level": 1,
        "startLine": 1,
        "endLine": 98,
        "lines": 98,
        "content": "# The Philosophy of Spec-Driven Development\n\nSpec-driven development (SDD) is an architectural discipline designed to restore certainty, durability, and alignment to software engineering — especially in the era of autonomous AI coding agents.\n\n---\n\n## The Problem with Traditional Workflows\n\nMost software development follows a pattern that looks roughly like this:\n\n> idea → rough ticket → code → hope it works → document later (maybe)\n\nRequirements live in a chat message, a half-finished ticket, or someone's memory. A developer — or increasingly, an AI agent — receives a vague prompt and fills the gaps with assumptions. When the output does not match the intent, it is hard to say exactly why, because the intent was never formally stated.\n\nThe result is a familiar cycle: rework, misalignment, and a growing gap between what the system does and what anyone can confidently say it was meant to do. Documentation, if it exists at all, is written after the fact and describes what was built rather than what was intended.\n\n---\n\n## What Spec-Driven Development Does Differently\n\nSpec-driven development inverts the order of operations:\n\n> idea → specify requirements → write verification scenarios → design → implement → verify against spec → archive\n\nThe specification comes first. It describes what the system SHALL do — not how. It is written in plain language, reviewed like code, and treated as a first-class artifact alongside the implementation. The verification scenarios written alongside it describe how to confirm correctness in concrete, unambiguous terms.\n\nThis is not a new idea. Engineering disciplines outside software have worked this way for decades. SpecD makes it practical for day-to-day software development, particularly when AI agents are involved in implementation.\n\n---\n\n## What a Spec Actually Is\n\nA spec is a living requirement document. It answers the question: **what should this capability do?**\n\n- It is written in Markdown, in natural language, with normative language: `SHALL`, `MUST`, `SHOULD`.\n- It is paired with a verification file (`verify.md`) that describes concrete `WHEN`/`THEN` scenarios — not vague acceptance criteria, but specific conditions and expected outcomes.\n- It outlives the change that created it. Once written, a spec is a permanent record of intent. It evolves through structured delta modifications that preserve its history.\n- It is the source of truth for what the system should do, independent of any particular implementation.\n\nA spec does not describe how the code is structured. It does not name functions or modules. It says what the system shall do and what it shall not do. The implementation is a consequence of the spec, not the other way around.\n\n---\n\n## Why Specs Must Come Before Code\n\nForcing clarity before implementation has compounding benefits:\n\n### It makes intent explicit\n\nVague requirements produce vague implementations. Writing a spec forces you to answer questions you would otherwise defer: what are the edge cases? What are the constraints? What does success actually look like?\n\n### It creates alignment between humans and agents\n\nAn AI agent working from a spec is working from a contract. It knows what it is supposed to produce, in what order, and against what criteria. The spec is not a suggestion — it is the acceptance test.\n\n### It provides verifiable acceptance criteria\n\n\"The login should work\" is not a requirement. \"WHEN a user submits valid credentials, THEN they receive an authenticated session token\" is. Verification scenarios make it possible to determine, unambiguously, whether the implementation is correct.\n\n### It enables governance without blocking velocity\n\nApproval gates become natural checkpoints when they are attached to well-defined specs. A reviewer can evaluate whether a design satisfies a spec. A compliance check can verify whether code satisfies verification scenarios. These gates carry meaning because the specs they reference carry meaning.\n\n### It creates an audit trail of why\n\nCode tells you what the system does today. A commit message tells you what changed. A spec tells you why the capability exists and what it was intended to do — even after the code has been refactored, rewritten, or replaced entirely.\n\n### Specs survive refactors; code does not\n\nYou can rewrite the implementation from scratch and still have a complete record of what it was supposed to do. The spec is independent of the technology, the architecture, and the team that wrote the original version.\n\n---\n\n## The Key Insight\n\nThere are four different things that say something about a software system, and they answer different questions:\n\n| Artifact                   | Question it answers                     |\n| -------------------------- | --------------------------------------- |\n| **Code**                   | What does the system do today?          |\n| **Tests**                  | Does the code behave as programmed?     |\n| **Specs**                  | What should the system do, and why?     |\n| **Verification scenarios** | Does the code satisfy the requirements? |\n\nMost teams have code and tests. Few have durable, structured specs. Fewer still have explicit verification scenarios that connect requirements to outcomes. SpecD fills that gap.\n\n---\n\n## The SpecD Approach\n\nSpecD is a platform that makes spec-driven development practical without imposing heavy process:\n\n- **Structure through Schemas:** A schema defines what artifact types exist in your project, what files they produce, and what order they must be produced in. You cannot produce a design until a spec exists. You cannot produce tasks until a design exists. The structure enforces deliberate sequencing without requiring manual enforcement.\n- **Workflow through the Change Lifecycle:** A change tracks a unit of work from initial idea through implementation to completion. Each lifecycle state has a clear meaning. Optional approval gates enforce compliance checks at the transitions that matter most.\n- **Tooling through the CLI and MCP Server:** You can manage the full lifecycle from the command line. AI agents can interact through the MCP interface, which exposes the same workflow as structured tool calls.\n- **Context through Compilation:** At each lifecycle step, SpecD assembles the right set of specs, schema instructions, and lifecycle context into a single block — ready to inject into the agent. The agent gets exactly what it needs for the current step, with full detail on the specs it is directly working with and lighter summaries for broader context.\n- **Human-Agent Collaboration:** Specs are the contract between human intent and agent execution. The agent reads the spec, produces artifacts against it, and is checked against it. The human defines what should be built. The agent works out how.\n"
      },
      {
        "index": 2,
        "heading": "The Problem with Traditional Workflows",
        "level": 2,
        "startLine": 7,
        "endLine": 18,
        "lines": 12,
        "content": "## The Problem with Traditional Workflows\n\nMost software development follows a pattern that looks roughly like this:\n\n> idea → rough ticket → code → hope it works → document later (maybe)\n\nRequirements live in a chat message, a half-finished ticket, or someone's memory. A developer — or increasingly, an AI agent — receives a vague prompt and fills the gaps with assumptions. When the output does not match the intent, it is hard to say exactly why, because the intent was never formally stated.\n\nThe result is a familiar cycle: rework, misalignment, and a growing gap between what the system does and what anyone can confidently say it was meant to do. Documentation, if it exists at all, is written after the fact and describes what was built rather than what was intended.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "What Spec-Driven Development Does Differently",
        "level": 2,
        "startLine": 19,
        "endLine": 30,
        "lines": 12,
        "content": "## What Spec-Driven Development Does Differently\n\nSpec-driven development inverts the order of operations:\n\n> idea → specify requirements → write verification scenarios → design → implement → verify against spec → archive\n\nThe specification comes first. It describes what the system SHALL do — not how. It is written in plain language, reviewed like code, and treated as a first-class artifact alongside the implementation. The verification scenarios written alongside it describe how to confirm correctness in concrete, unambiguous terms.\n\nThis is not a new idea. Engineering disciplines outside software have worked this way for decades. SpecD makes it practical for day-to-day software development, particularly when AI agents are involved in implementation.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "What a Spec Actually Is",
        "level": 2,
        "startLine": 31,
        "endLine": 43,
        "lines": 13,
        "content": "## What a Spec Actually Is\n\nA spec is a living requirement document. It answers the question: **what should this capability do?**\n\n- It is written in Markdown, in natural language, with normative language: `SHALL`, `MUST`, `SHOULD`.\n- It is paired with a verification file (`verify.md`) that describes concrete `WHEN`/`THEN` scenarios — not vague acceptance criteria, but specific conditions and expected outcomes.\n- It outlives the change that created it. Once written, a spec is a permanent record of intent. It evolves through structured delta modifications that preserve its history.\n- It is the source of truth for what the system should do, independent of any particular implementation.\n\nA spec does not describe how the code is structured. It does not name functions or modules. It says what the system shall do and what it shall not do. The implementation is a consequence of the spec, not the other way around.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Why Specs Must Come Before Code",
        "level": 2,
        "startLine": 44,
        "endLine": 73,
        "lines": 30,
        "content": "## Why Specs Must Come Before Code\n\nForcing clarity before implementation has compounding benefits:\n\n### It makes intent explicit\n\nVague requirements produce vague implementations. Writing a spec forces you to answer questions you would otherwise defer: what are the edge cases? What are the constraints? What does success actually look like?\n\n### It creates alignment between humans and agents\n\nAn AI agent working from a spec is working from a contract. It knows what it is supposed to produce, in what order, and against what criteria. The spec is not a suggestion — it is the acceptance test.\n\n### It provides verifiable acceptance criteria\n\n\"The login should work\" is not a requirement. \"WHEN a user submits valid credentials, THEN they receive an authenticated session token\" is. Verification scenarios make it possible to determine, unambiguously, whether the implementation is correct.\n\n### It enables governance without blocking velocity\n\nApproval gates become natural checkpoints when they are attached to well-defined specs. A reviewer can evaluate whether a design satisfies a spec. A compliance check can verify whether code satisfies verification scenarios. These gates carry meaning because the specs they reference carry meaning.\n\n### It creates an audit trail of why\n\nCode tells you what the system does today. A commit message tells you what changed. A spec tells you why the capability exists and what it was intended to do — even after the code has been refactored, rewritten, or replaced entirely.\n\n### Specs survive refactors; code does not\n\nYou can rewrite the implementation from scratch and still have a complete record of what it was supposed to do. The spec is independent of the technology, the architecture, and the team that wrote the original version.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "It makes intent explicit",
        "level": 3,
        "startLine": 48,
        "endLine": 51,
        "lines": 4,
        "content": "### It makes intent explicit\n\nVague requirements produce vague implementations. Writing a spec forces you to answer questions you would otherwise defer: what are the edge cases? What are the constraints? What does success actually look like?\n"
      },
      {
        "index": 7,
        "heading": "It creates alignment between humans and agents",
        "level": 3,
        "startLine": 52,
        "endLine": 55,
        "lines": 4,
        "content": "### It creates alignment between humans and agents\n\nAn AI agent working from a spec is working from a contract. It knows what it is supposed to produce, in what order, and against what criteria. The spec is not a suggestion — it is the acceptance test.\n"
      },
      {
        "index": 8,
        "heading": "It provides verifiable acceptance criteria",
        "level": 3,
        "startLine": 56,
        "endLine": 59,
        "lines": 4,
        "content": "### It provides verifiable acceptance criteria\n\n\"The login should work\" is not a requirement. \"WHEN a user submits valid credentials, THEN they receive an authenticated session token\" is. Verification scenarios make it possible to determine, unambiguously, whether the implementation is correct.\n"
      },
      {
        "index": 9,
        "heading": "It enables governance without blocking velocity",
        "level": 3,
        "startLine": 60,
        "endLine": 63,
        "lines": 4,
        "content": "### It enables governance without blocking velocity\n\nApproval gates become natural checkpoints when they are attached to well-defined specs. A reviewer can evaluate whether a design satisfies a spec. A compliance check can verify whether code satisfies verification scenarios. These gates carry meaning because the specs they reference carry meaning.\n"
      },
      {
        "index": 10,
        "heading": "It creates an audit trail of why",
        "level": 3,
        "startLine": 64,
        "endLine": 67,
        "lines": 4,
        "content": "### It creates an audit trail of why\n\nCode tells you what the system does today. A commit message tells you what changed. A spec tells you why the capability exists and what it was intended to do — even after the code has been refactored, rewritten, or replaced entirely.\n"
      },
      {
        "index": 11,
        "heading": "Specs survive refactors; code does not",
        "level": 3,
        "startLine": 68,
        "endLine": 73,
        "lines": 6,
        "content": "### Specs survive refactors; code does not\n\nYou can rewrite the implementation from scratch and still have a complete record of what it was supposed to do. The spec is independent of the technology, the architecture, and the team that wrote the original version.\n\n---\n"
      },
      {
        "index": 12,
        "heading": "The Key Insight",
        "level": 2,
        "startLine": 74,
        "endLine": 88,
        "lines": 15,
        "content": "## The Key Insight\n\nThere are four different things that say something about a software system, and they answer different questions:\n\n| Artifact                   | Question it answers                     |\n| -------------------------- | --------------------------------------- |\n| **Code**                   | What does the system do today?          |\n| **Tests**                  | Does the code behave as programmed?     |\n| **Specs**                  | What should the system do, and why?     |\n| **Verification scenarios** | Does the code satisfy the requirements? |\n\nMost teams have code and tests. Few have durable, structured specs. Fewer still have explicit verification scenarios that connect requirements to outcomes. SpecD fills that gap.\n\n---\n"
      },
      {
        "index": 13,
        "heading": "The SpecD Approach",
        "level": 2,
        "startLine": 89,
        "endLine": 98,
        "lines": 10,
        "content": "## The SpecD Approach\n\nSpecD is a platform that makes spec-driven development practical without imposing heavy process:\n\n- **Structure through Schemas:** A schema defines what artifact types exist in your project, what files they produce, and what order they must be produced in. You cannot produce a design until a spec exists. You cannot produce tasks until a design exists. The structure enforces deliberate sequencing without requiring manual enforcement.\n- **Workflow through the Change Lifecycle:** A change tracks a unit of work from initial idea through implementation to completion. Each lifecycle state has a clear meaning. Optional approval gates enforce compliance checks at the transitions that matter most.\n- **Tooling through the CLI and MCP Server:** You can manage the full lifecycle from the command line. AI agents can interact through the MCP interface, which exposes the same workflow as structured tool calls.\n- **Context through Compilation:** At each lifecycle step, SpecD assembles the right set of specs, schema instructions, and lifecycle context into a single block — ready to inject into the agent. The agent gets exactly what it needs for the current step, with full detail on the specs it is directly working with and lighter summaries for broader context.\n- **Human-Agent Collaboration:** Specs are the contract between human intent and agent execution. The agent reads the spec, produces artifacts against it, and is checked against it. The human defines what should be built. The agent works out how.\n"
      }
    ]
  },
  {
    "topic": "workflow",
    "title": "Change Lifecycle Guide",
    "description": "Guide to SpecD change lifecycles, states, transitions, approvals, hooks, and verification.",
    "order": 2,
    "content": "# Change Lifecycle Guide\n\nEvery piece of work in specd is a **change**. A change tracks everything related to modifying one or more specs — the proposal, the spec files, the design, the implementation tasks, and the final archive record. This guide explains how changes move through their lifecycle, what each state means, and how approval gates, task completion checks, and hooks affect that progression.\n\n## How the pieces fit together\n\nspecd has three layers that work together to drive a change from idea to completion:\n\n**The CLI** is the engine. It manages changes, transitions between states, validates artifacts, compiles context, and enforces gates. Every operation — creating a change, checking status, transitioning states, archiving — is a CLI command. The CLI is the source of truth for what is happening.\n\n**The workflow** is the rules. It defines which states exist, what transitions are valid, what artifacts must be produced, and what gates must be passed. The workflow comes from two sources: the schema (which defines artifacts and their dependency order) and the project config (which enables approval gates and hooks). Together they determine the path a change must follow.\n\n**The skills** are the interface. They are slash commands installed in your coding assistant (`/specd`, `/specd-new`, `/specd-design`, etc.) that orchestrate the workflow by calling the CLI. Each skill knows how to guide the agent through one phase — loading context, writing artifacts, validating, transitioning. You interact with skills; skills interact with the CLI; the CLI enforces the workflow.\n\n```text\nYou ←→ Skills ←→ CLI ←→ Workflow (schema + config)\n         │                    │\n         │  calls commands    │  enforces rules\n         │  reads context     │  validates artifacts\n         │  writes artifacts  │  manages transitions\n```\n\nIn practice, you type `/specd` and the skill figures out where you are, what to do next, and which CLI commands to run. You can also use the CLI directly — for scripting, CI pipelines, or when you want fine-grained control — but for day-to-day work the skills provide the guided experience.\n\n---\n\n## The lifecycle at a glance\n\n```\n                        ┌──────────────────────────────────────────────────────┐\n                        │    redesign — transition back to designing            │\n                        │    available from any active state (including        │\n                        │    archiving when recovery is needed)               │\n                        └──────────────────────────────────────────────────────┘\n                                              ↑\ndrafting → designing ⇄ ready ─────────────────────────────────── → implementing ⇄ verifying → done ──────────────────────── → archivable → archiving\n                        ↓                                                                       ↓\n               pending-spec-approval                                                    pending-signoff\n                        ↓                                                                       ↓\n                  spec-approved ─────────────────────────────────→ implementing           signed-off\n```\n\nWhen neither approval gate is enabled, the main path is:\n\n```\ndrafting → designing → ready → implementing → verifying → done → archivable → archiving\n```\n\n## States explained\n\n### `drafting`\n\nThe initial state. A newly created change starts here before any work has been done.\n\n- **What it means:** The change exists but has not entered the design phase.\n- **What you do:** Nothing yet. Transition to `designing` when you are ready to start.\n- **Transition out:** `specd changes transition <name> designing`\n- **What can block it:** Nothing — this transition is always allowed.\n\n---\n\n### `designing`\n\nThe active design and specification phase. This is where the bulk of artifact work happens.\n\n- **What it means:** You are writing the proposal, specs, verify files, design, and tasks.\n- **What you do:** Create and refine the five standard artifacts (proposal, specs, verify, design, tasks). Run `specd changes status <name>` to check artifact progress.\n- **Transition out:** `specd changes transition <name> ready` once all required artifacts are complete and validated.\n- **What can block it:** The `ready` step requires all artifacts listed in the schema's `requires` field (by default: proposal, specs, verify, design, tasks) to have `complete` status. The CLI reports which artifacts are still missing or in progress.\n\nThe `designing → designing` self-transition is valid — it records a checkpoint without changing state. This happens automatically when redesign invalidates an already-`designing` change.\n\n---\n\n### `ready`\n\nAll design artifacts are complete. The change is ready to hand off to implementation.\n\n- **What it means:** Every required artifact has been written and validated. The change has been reviewed and is cleared for implementation.\n- **What you do:** If the `approvals.spec` gate is enabled in `specd.yaml`, transition to `pending-spec-approval` to request approval. Otherwise, transition directly to `implementing`.\n- **Transition out:** `implementing` (no gate), or `pending-spec-approval` (with gate), or back to `designing` (redesign).\n- **What can block it:** The spec approval gate (if enabled) must be passed before proceeding to `implementing`.\n\n---\n\n### `pending-spec-approval`\n\nThe spec approval gate is active and a human reviewer has not yet approved the specs.\n\n- **What it means:** specd has been asked to require human sign-off on the spec artifacts before implementation begins. The change is waiting for that approval.\n- **What you do:** A reviewer runs `specd changes approve-spec <name> --reason \"...\"` after inspecting the spec artifacts.\n- **Transition out:** `spec-approved` after approval, or back to `designing` (redesign).\n- **What can block it:** Approval must be recorded by a human. The gate cannot be bypassed.\n\nThis state only appears when `approvals.spec` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n\n### `spec-approved`\n\nA human has reviewed and approved the spec artifacts.\n\n- **What it means:** The specs have been signed off. Implementation may now proceed.\n- **What you do:** Transition to `implementing`.\n- **Transition out:** `implementing`, or back to `designing` (redesign).\n- **What can block it:** If specs were modified after approval, the change is automatically invalidated and returns to `designing` — the approval is cleared.\n\n---\n\n### `implementing`\n\nActive development is in progress.\n\n- **What it means:** The implementation tasks are being worked through.\n- **What you do:** Work through the task list in `tasks.md`, checking off items as you go (`- [x]`). Run `specd changes status <name>` to see task progress.\n- **Transition out:** `verifying` once all tasks are complete, or back to `designing` (redesign). The transition to `verifying` is blocked if any tasks remain incomplete.\n- **What can block it:** The `taskCompletionCheck` defined in the schema. By default, any unchecked `- [ ]` line in `tasks.md` prevents advancing to `verifying`. The CLI reports \"N/M tasks complete\" when this gate is active.\n\n---\n\n### `verifying`\n\nImplementation is complete. Verification scenarios are being run.\n\n- **What it means:** All tasks are done. You are now confirming the implementation satisfies every scenario in `verify.md`.\n- **What you do:** Run through every scenario in `verify.md`. Confirm each one passes by inspecting the code and running the relevant tests.\n- **Transition out:** `done` when all scenarios pass, back to `implementing` if a scenario fails (the schema may clear artifact validations on this backward transition), or back to `designing` (redesign).\n- **What can block it:** Nothing prevents the transition — it is up to you to judge readiness.\n\n---\n\n### `done`\n\nImplementation and verification are complete.\n\n- **What it means:** The change is finished from a development perspective. It can now be archived.\n- **What you do:** If the `approvals.signoff` gate is enabled, transition to `pending-signoff`. Otherwise, transition to `archivable`.\n- **Transition out:** `archivable` (no gate), `pending-signoff` (with gate), or back to `designing` (redesign).\n- **What can block it:** The signoff gate (if enabled) must be passed before proceeding to `archivable`.\n\n---\n\n### `pending-signoff`\n\nThe signoff gate is active and a human reviewer has not yet signed off.\n\n- **What it means:** The change requires final human sign-off before it can be archived. This gate is typically used for compliance or stakeholder review.\n- **What you do:** A reviewer runs `specd changes signoff <name> --reason \"...\"` after inspecting the completed work.\n- **Transition out:** `signed-off` after sign-off, or back to `designing` (redesign).\n- **What can block it:** Sign-off must be recorded by a human.\n\nThis state only appears when `approvals.signoff` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n\n### `signed-off`\n\nA human has reviewed and signed off the completed change.\n\n- **What it means:** The change has received its final human approval and may proceed to archiving.\n- **What you do:** Transition to `archivable`.\n- **Transition out:** `archivable`, or back to `designing` (redesign).\n- **What can block it:** Nothing.\n\n---\n\n### `archivable`\n\nThe change is ready to be archived.\n\n- **What it means:** All gates have been passed. The change can run the archive operation.\n- **What you do:** Run `specd changes archive <name>` to perform the archive.\n- **Transition out:** `archiving` (only after archive preflight succeeds and canonical publication is about to start — see [Archiving](#archiving)), or back to `designing` (redesign).\n- **What can block it:** Archive pre-hooks, overlap/read-only guards, or preflight failures keep the change here (the archive command does not transition to `archiving` until commit preparation has passed).\n\n---\n\n### `archiving`\n\nThe change is in the archive **commit phase** (canonical publication, archive move, and post-archive work), or it completed successfully and was moved to the archive directory.\n\n- **What it means:** While `specd changes archive` is running, the change transitions to `archiving` immediately before the first spec publication, after guards, pre-archive hooks, orphan detection, and batch snapshot. If publication or the archive move fails and batch restore succeeds, the change returns to `archivable` so you can fix the problem and retry. If restore is incomplete, the change stays in `archiving` until you recover manually.\n- **What you do:** If archive failed and the change is back in `archivable`, review restored specs and run `specd changes archive <name>` again. If the change is stuck in `archiving` after a partial restore, use `specd changes transition <name> designing` to revise artifacts, or `specd changes transition <name> archivable` after you have manually restored canonical specs (lifecycle rollback only — no archive hooks).\n- **Transition out:** `archivable` (recovery after a failed commit when canonical storage was restored), `designing` (manual escape to revise specs/deltas), or none after a **successful** archive (the change directory is moved to the archive and is no longer an active change).\n\n---\n\n## Full transition table\n\n| From                    | To                                                                                                          |\n| ----------------------- | ----------------------------------------------------------------------------------------------------------- |\n| `drafting`              | `designing`                                                                                                 |\n| `designing`             | `ready`, `designing`                                                                                        |\n| `ready`                 | `implementing`, `pending-spec-approval`, `designing`                                                        |\n| `pending-spec-approval` | `spec-approved`, `designing`                                                                                |\n| `spec-approved`         | `implementing`, `designing`                                                                                 |\n| `implementing`          | `verifying`, `designing`                                                                                    |\n| `verifying`             | `implementing`, `done`, `designing`                                                                         |\n| `done`                  | `archivable`, `pending-signoff`, `designing`                                                                |\n| `pending-signoff`       | `signed-off`, `designing`                                                                                   |\n| `signed-off`            | `archivable`, `designing`                                                                                   |\n| `archivable`            | `archiving`, `designing`                                                                                    |\n| `archiving`             | `archivable`, `designing` _(active change only; successful archive removes the change from the active set)_ |\n\n---\n\n## Identity and audit trails\n\nspecd records the identity of the person or system performing every significant operation — creating a change, transitioning states, approving specs, and signing off on completed work. These records create a tamper-evident audit trail persisted in the change manifest and history.\n\n### How identity is resolved\n\nSpecD uses a pluggable identity resolution system. By default, it auto-detects the active Version Control System (Git, Mercurial, or Subversion) and retrieves the user's name and email from its configuration.\n\nIn specific environments, you can force a provider or use custom identity plugins. See the [Configuration Guide](configuration.md#identity-resolution) for details.\n\n### Privacy in public repositories\n\nFor projects hosted in public repositories, you can enable **privacy modes** to obfuscate real names and emails before they are persisted in the repository.\n\nSupported modes include:\n\n- **Hashing**: deterministic HMAC-SHA256 obfuscation of emails.\n- **Masking**: partial obfuscation (e.g., `j***z@e***.com`).\n- **Anonymization**: replacement with generic placeholders.\n\nPrivacy settings also automatically filter out sensitive metadata. For more details on configuring privacy, see the [Privacy section](configuration.md#privacy) in the Configuration Guide.\n\n---\n\n## Redesign: going back to designing\n\nAlmost every state can transition back to `designing`. This is the **redesign path** — it is how you handle requirement changes, new information, or mistakes discovered mid-lifecycle.\n\nTo redesign from any active state:\n\n```bash\nspecd changes transition <name> designing\n```\n\nWhen a redesign transition occurs:\n\n1. An `invalidated` event is appended to the change history with `cause: 'redesign'`.\n2. A `transitioned` event rolling back to `designing` is appended.\n3. Any active spec approval (`activeSpecApproval`) and signoff (`activeSignoff`) are cleared — they are no longer valid after specs have been reworked.\n4. Artifact validation hashes are reset, requiring re-validation before the change can advance to `ready` again.\n\nRedesign also happens automatically when spec IDs are updated (`cause: 'spec-change'`) or when artifact content changes after an approval was recorded (`cause: 'artifact-change'`).\n\nRedesign from `archiving` is allowed via `specd changes transition <name> designing` (artifacts are downgraded for review). Use this when batch restore failed partially or you need to change deltas before retrying archive. After a **successful** archive, the change is no longer active — create a new change instead.\n\n---\n\n## Approval gates\n\nspecd supports two optional human-approval gates. Both are disabled by default.\n\n### Spec approval gate\n\nBlocks the transition from `ready` to `implementing` until a human approves the spec artifacts.\n\n**Path when enabled:**\n\n```\nready → pending-spec-approval → spec-approved → implementing\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  spec: true\n```\n\n**Performing the approval:**\n\n```bash\nspecd changes approve-spec add-auth --reason \"Specs reviewed and approved — all requirements are clear\"\n```\n\nThe approver reviews the spec and verify artifacts in the change directory. The approval records a hash of those artifacts at the moment of approval. If any of those artifacts are subsequently modified, an `invalidated` event is appended automatically and the change returns to `designing`, clearing the approval.\n\n### Signoff gate\n\nBlocks the transition from `done` to `archivable` until a human signs off the completed work.\n\n**Path when enabled:**\n\n```\ndone → pending-signoff → signed-off → archivable\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  signoff: true\n```\n\n**Performing the signoff:**\n\n```bash\nspecd changes signoff add-auth --reason \"Implementation reviewed — all scenarios verified\"\n```\n\nBoth gates can be enabled simultaneously:\n\n```yaml\napprovals:\n  spec: true\n  signoff: true\n```\n\n---\n\n## Task completion gating\n\nThe transition from `implementing` to `verifying` is gated on task completion. This is enforced by the `taskCompletionCheck` defined in the schema.\n\nThe default schema (`@specd/schema-std`) checks for unchecked checkbox lines in `tasks.md`:\n\n- **Incomplete pattern:** `^\\s*-\\s+\\[ \\]` — any `- [ ]` line blocks the transition.\n- **Complete pattern:** `^\\s*-\\s+\\[x\\]` — `- [x]` lines count toward completion.\n\nWhen you run `specd changes transition <name> verifying` and tasks remain incomplete, the CLI reports the current progress:\n\n```\n3/5 tasks complete — transition to verifying is blocked\n```\n\nMark tasks complete by changing `- [ ]` to `- [x]` in `tasks.md`. Once all tasks are checked, the transition is allowed.\n\nOne subtlety: `tasks.md` is also validated by artifact hashing. The schema normalises checkboxes before hashing — `- [x]` lines are converted back to `- [ ]` during hash computation so that checking off tasks does not trigger an approval invalidation. Checking off tasks is not a spec change; it is progress tracking.\n\n---\n\n## Hooks\n\nHooks let you attach automated actions or AI guidance to lifecycle transitions. They are defined in the schema's `workflow` section and optionally extended in `specd.yaml` via `schemaOverrides`.\n\n### Pre and post hooks\n\n- **Pre hooks** run before the change enters a step. If a pre hook fails, the transition is blocked.\n- **Post hooks** run after the change enters a step. If a post hook fails, the transition is not rolled back.\n\nWhen shell hooks are long-running, SpecD keeps them observable while they are\nin flight. In human-facing `text` output, completed hooks remain visible,\nactive hooks show recent output plus a running indicator, and quiet hooks emit\nliveness updates. In `json` and `toon`, in-flight hook progress is emitted on\n`stderr` as structured events while the final command result remains on\n`stdout`.\n\n### Hook types\n\n| Type           | Description                                                                              |\n| -------------- | ---------------------------------------------------------------------------------------- |\n| `run:`         | A shell command executed in the project root. Non-zero exit code blocks a pre hook.      |\n| `instruction:` | An AI context block injected into the agent context for the step. Not directly runnable. |\n| `external:`    | An explicit external hook dispatched to a registered external hook runner by `type`.     |\n\nAn `instruction:` hook provides guidance to an AI agent about what to do in that step. It is informational — it does not execute code. A `run:` hook executes a shell command through the internal `HookRunner`. An `external:` hook is routed to a separate `ExternalHookRunner` selected by `external.type`.\n\n```yaml\nworkflow:\n  - step: implementing\n    hooks:\n      pre:\n        - id: run-lint\n          run: pnpm lint\n        - id: docker-test\n          external:\n            type: docker\n            config:\n              image: node:20\n              command: pnpm test\n      post:\n        - id: notify\n          run: echo \"Implementation started for {{change.name}}\"\n```\n\nExternal hooks use the same workflow phase semantics as shell hooks:\n\n- a failing `pre` external hook blocks the transition\n- a failing `post` external hook does not roll the state back\n- an unknown `external.type` is a hard error, not a silent skip\n\n### Template variables\n\nHook `run:` commands support template variable substitution:\n\n| Variable           | Value                                                        |\n| ------------------ | ------------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name (e.g. `add-auth`)                     |\n| `{{change.path}}`  | Absolute path to the change directory                        |\n| `{{project.root}}` | Absolute path to the project root (where `specd.yaml` lives) |\n\n### Hook execution order\n\nWhen a step is entered, schema-level hooks fire first, then any project-level hooks added via `schemaOverrides` in `specd.yaml`. Within each source, hooks run in the order they are declared.\n\n---\n\n## Drafting and discarding\n\nDrafting and discarding are sidebars to the main lifecycle — they do not affect the change's lifecycle state.\n\n### Drafting\n\nDrafting shelves a change temporarily without abandoning it.\n\n```bash\nspecd drafts move add-auth         # move to drafts/\nspecd drafts restore add-auth      # move back to changes/\n```\n\nA drafted change retains its current lifecycle state. When restored, it continues from where it left off. Drafting is useful when you need to context-switch to another change and want to keep the current work safely out of the way.\n\n**Guard:** When a change has previously reached the `implementing` state, drafting is blocked by default — the code and specs may be out of sync, and shelving without archival risks leaving partial implementation behind. If you are certain you want to draft anyway, use `--force`:\n\n```bash\nspecd changes draft add-auth --force\n```\n\n### Discarding\n\nDiscarding permanently abandons a change. A reason is required.\n\n```bash\nspecd discard add-auth --reason \"Approach superseded by add-oauth-flow\"\n```\n\nA discarded change is moved to the discarded directory and is never archived. This is permanent — there is no restore from discard. You can optionally name the change or changes that supersede it:\n\n```bash\nspecd discard add-auth --reason \"Superseded\" --superseded-by add-oauth-flow\n```\n\n**Guard:** When a change has previously reached the `implementing` state, discarding is blocked by default — code that has been implemented should ideally be archived, not silently abandoned. If you are certain you want to discard anyway, use `--force`:\n\n```bash\nspecd changes discard add-auth --reason \"No longer needed\" --force\n```\n\n---\n\n## Archiving\n\nArchiving is the final operation in the lifecycle. The change must be in `archivable`. When you run:\n\n```bash\nspecd changes archive <name>\n```\n\n### Phases and lifecycle state\n\nThe change stays in **`archivable`** through early archive work (schema guard, overlap/read-only checks, pre-archive hooks, and full-batch preflight). It moves to **`archiving`** only immediately before the first canonical spec publication, after orphan backup detection and a per-spec batch snapshot under `.specd-archive-backup/` in each affected spec directory.\n\n| Phase                                | Lifecycle state | On failure                                                                                |\n| ------------------------------------ | --------------- | ----------------------------------------------------------------------------------------- |\n| Guards, pre-archive hooks, preflight | `archivable`    | Stays `archivable`; no backup dirs                                                        |\n| Orphan detection + batch snapshot    | `archivable`    | Stays `archivable` (matching orphan: auto-restore + abort with guidance)                  |\n| Canonical publication + archive move | `archiving`     | Batch restore; `→ archivable` if restore succeeds, stay `archiving` if restore is partial |\n| Metadata + post-archive hooks        | `archiving`     | Archive already committed; failures are reported but do not roll back specs               |\n\nPre-archive hooks use the workflow step name `archiving`; they do **not** require the change to already be in lifecycle state `archiving`.\n\n### Commit steps (after transition to `archiving`)\n\n1. **Canonical publication:** For each spec in the batch, delta artifacts are merged into the base spec in the repository (or full spec files are copied). Publication is staged per spec; created files are recorded for restore.\n2. **Archive move:** The change directory is moved to the archive. An `ArchivedChange` record is created with the change name, timestamp, and spec IDs.\n3. **Backup cleanup:** `.specd-archive-backup/` directories are removed after a successful commit.\n4. **Metadata generation:** Persisted `metadata.json` is written **after** the archive move (in-memory preflight checks still run earlier).\n5. **Post-archive hooks:** Optional hooks configured for the `archiving` workflow step.\n\n### Batch restore and retry\n\nIf publication fails partway through a multi-spec batch, specd restores already-published specs from the batch backup (reverse publication order) and rolls the change back to **`archivable`** when restore completes. Retry archive merges deltas against the restored canonical base — not against partially merged content.\n\nIf restore cannot complete for every spec, the change remains in **`archiving`**. `specd changes status` recommends transitioning to **`designing`** for manual recovery.\n\n### Orphan backups\n\nIf `.specd-archive-backup/` already exists when archive starts:\n\n- **Same change name** in the manifest: canonical specs are auto-restored, the backup is removed, and the current archive attempt aborts (review and retry).\n- **Different change name**: archive aborts with a repair message; live files are not overwritten.\n\n### Recovery transitions from `archiving`\n\n| Transition               | Purpose                                                                                                             |\n| ------------------------ | ------------------------------------------------------------------------------------------------------------------- |\n| `archiving → archivable` | Lifecycle rollback after you fixed canonical specs manually; does not re-run archive hooks                          |\n| `archiving → designing`  | Revise change artifacts (downgrades files to `pending-review`); use after partial restore or when specs need rework |\n\nTransitions to `implementing`, `verifying`, or `done` from `archiving` are not valid.\n\n### After a successful archive\n\nSpecs in the repository reflect the merged requirements. The change history is preserved under `.specd/archive/`. To rework merged specs, open a **new** change — the archived record is not transitioned back.\n\n---\n\n## Practical example: a full lifecycle with `/specd`\n\nHere is a complete lifecycle walkthrough for a change that adds an authentication feature, showing what the agent does at each step. In practice, you drive this through your coding assistant using the `/specd` skill — the agent handles transitions, artifact creation, and validation automatically.\n\n### 1. Starting — `/specd`\n\nYou open your coding assistant and type `/specd`. The skill shows the project status:\n\n```\n# specd\n\n**Schema:** @specd/schema-std\n**Workspaces:** default (8 specs)\n**Active changes:** none\n**Drafts:** none\n\nNo active changes. What would you like to work on?\n```\n\nYou describe what you want: \"I want to add login and logout authentication flows.\"\n\nThe skill suggests creating a change and invokes `/specd-new`.\n\n### 2. Creation — `/specd-new`\n\nThe agent has a discovery conversation with you — understanding the problem, discussing the approach, identifying affected specs, and reaching agreements about scope. Once the picture is clear, it creates the change:\n\n```bash\nspecd changes create add-auth --spec default:auth/login --spec default:auth/logout\n```\n\nBefore handing off, the agent writes a `specd-exploration.md` file to the change directory. This captures everything from the conversation — the problem, the agreed approach, design decisions, rejected alternatives, codebase observations, and open questions. It is the agent's working memory, persisted so that the next phase can pick up where you left off even in a different session. See [Exploration context](#exploration-context-specd-explorationmd) for details.\n\nState: `drafting` → the agent suggests running `/specd-design`.\n\n### 3. Designing — `/specd-design`\n\nThe agent loads the change state, the schema, and the compiled context. It also reads `specd-exploration.md` from the change directory to recover the full discovery context — if time has passed since creation, it verifies that referenced files and spec IDs still exist and flags any drift before proceeding.\n\nThis is the longest phase. The agent works through each artifact in dependency order. The artifacts below follow `@specd/schema-std` — other schemas may define a different set or order:\n\n**Step 3a: Proposal** — The agent writes `proposal.md`:\n\n- Explains the motivation (why the project needs authentication)\n- Describes the current behaviour (no auth exists)\n- Outlines the proposed solution at a high level\n- Lists the specs that will be created: `default:auth/login` and `default:auth/logout`\n- Notes the impact on existing code (new middleware, new routes)\n\nOnce written, the agent validates the proposal and marks it complete.\n\n**Step 3b: Specs** — The agent writes `spec.md` for each spec:\n\n- `specs/default/auth/login/spec.md` — Requirements for login: valid credentials return a session token, invalid credentials return a generic error, sessions expire after 24 hours, rate limiting on the endpoint\n- `specs/default/auth/logout/spec.md` — Requirements for logout: session is invalidated, subsequent requests are rejected\n\nEach spec uses SHALL/MUST language and groups requirements under `### Requirement:` headings.\n\n**Step 3c: Verify** — The agent writes `verify.md` for each spec:\n\n- `specs/default/auth/login/verify.md` — WHEN/THEN scenarios for each login requirement: valid credentials, invalid credentials, expired sessions, rate limit exceeded\n- `specs/default/auth/logout/verify.md` — WHEN/THEN scenarios for logout: successful invalidation, already-expired session\n\nEach scenario mirrors a requirement from `spec.md`.\n\n**Step 3d: Design** — The agent writes `design.md`:\n\n- Reads the proposal, specs, and verify files\n- Analyses the existing codebase to identify affected areas\n- Lists new constructs: `AuthService`, `SessionStore`, login/logout route handlers, auth middleware\n- Documents the approach, key decisions, and trade-offs\n- Maps every requirement and scenario to a concrete implementation path\n\n**Step 3e: Tasks** — The agent writes `tasks.md`:\n\n- Derives discrete tasks from `design.md`\n- Each task is a checkbox with indented context: which file, which symbol, what to change\n- Groups tasks logically (e.g., \"1. Session infrastructure\", \"2. Login flow\", \"3. Logout flow\", \"4. Tests\")\n\nOnce all five artifacts are complete and validated, the agent transitions to `ready`, then to `implementing`.\n\n### 4. Implementing — `/specd-implement`\n\nThe agent reads the compiled context for the `implementing` step — which includes the specs, design, and task list — and works through tasks one by one:\n\n```\n## 1. Session infrastructure\n- [x] 1.1 Create SessionStore interface\n      `src/auth/session-store.ts`: New interface — get, set, delete session\n      Approach: In-memory store with TTL from design.md\n      (Req: Sessions expire after 24 hours)\n\n- [x] 1.2 Implement InMemorySessionStore\n      `src/auth/in-memory-session-store.ts`: Implement SessionStore\n      Approach: Map<string, Session> with setTimeout cleanup\n\n## 2. Login flow\n- [x] 2.1 Create login handler\n      `src/auth/login.ts`: POST /login route handler\n      ...\n- [ ] 2.2 Add rate limiting middleware\n      `src/auth/rate-limit.ts`: ...\n```\n\nThe agent checks off each task as it completes the work. Pre/post hooks run at the step boundaries — for example, `pnpm test` runs after implementation.\n\nOnce all checkboxes are `[x]`, the agent transitions to `verifying`.\n\n### 5. Verifying — `/specd-verify`\n\nVerification is not the same as running tests. Tests check that code behaves as programmed — verification checks that the implementation satisfies the _requirements_. These are different questions:\n\n- **Not every project has tests.** Documentation changes, configuration changes, infrastructure work, early prototypes — many changes have no test suite at all. Verification still applies: the agent inspects the implementation against each scenario and confirms it holds.\n- **Tests can pass while requirements are missed.** A test suite exercises the code paths the developer thought to write. Verification scenarios come from the spec — they represent what was _agreed_ the system should do. A passing test suite does not guarantee that every requirement was addressed.\n- **Verification checks more than code.** The agent inspects the implementation holistically: does the code structure match the design? Were all tasks completed? Do the changes satisfy the GIVEN/WHEN/THEN conditions from `verify.md`? It runs tests where they exist, but also reads code, checks behaviour, and confirms edge cases that tests may not cover.\n\nThe agent reads each scenario from `verify.md` and confirms the implementation satisfies it — by inspecting code, running tests where applicable, and verifying GIVEN/WHEN/THEN conditions:\n\n```\n### Requirement: Valid credentials\n#### Scenario: Successful login\n- WHEN a user submits valid credentials\n- THEN they receive an authenticated session token\n→ ✓ Verified: login handler returns JWT on valid credentials\n\n#### Scenario: Invalid credentials\n- WHEN a user submits invalid credentials\n- THEN they receive a generic error with no field-specific information\n→ ✓ Verified: handler returns 401 with generic message\n```\n\nIf a scenario fails, the agent goes back to fix the issue and then returns to verify again.\n\nOnce all scenarios pass, the agent transitions to `done`.\n\n### 6. Archiving — `/specd-archive`\n\nThe agent runs `specd changes archive add-auth`. specd:\n\n1. Syncs `spec.md` and `verify.md` into `specs/default/auth/login/` and `specs/default/auth/logout/`\n2. Applies any delta files to existing specs\n3. Generates metadata for each spec\n4. Moves the change directory to `.specd/archive/`\n\nThe specs are now permanent. The change is a historical record.\n\n### Redesign mid-lifecycle\n\nIf you discover during implementation that a requirement is wrong:\n\n```\n> The rate limiting should be per-IP, not global. Let's go back and fix the spec.\n```\n\nThe agent transitions back to `designing`. This:\n\n- Records an `invalidated` event with `cause: 'redesign'`\n- Clears any active approvals\n- Resets artifact validation hashes\n\nThe agent updates the spec, verify, design, and tasks, then works forward through the lifecycle again.\n\n---\n\n## Exploration context (`specd-exploration.md`)\n\nWhen an AI agent runs `/specd-new`, it has a discovery conversation with the user — understanding the problem, discussing approaches, identifying affected specs, and reaching agreements about scope and direction. All of this context is captured in a file called `specd-exploration.md`, written to the change directory (`<changePath>/specd-exploration.md`) before the agent hands off to `/specd-design`.\n\n### Why it exists\n\nDesign happens in a different session — often hours or days after creation. The agent that writes artifacts has no memory of the discovery conversation. Without a persistent record, it would need to start from scratch: ask the user to re-explain everything, or worse, guess based on the change name and one-line description alone.\n\n`specd-exploration.md` bridges this gap. It is the agent's working memory, persisted to disk so that any future session can pick up exactly where the conversation left off.\n\n### How it differs from `proposal.md`\n\nThey may look similar at first glance, but they serve fundamentally different purposes:\n\n|                              | `specd-exploration.md`                                                                                        | `proposal.md`                                                                     |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |\n| **What it is**               | Agent working memory — raw conversation context                                                               | Formal schema artifact — structured document                                      |\n| **Who it's for**             | The AI agent in future sessions                                                                               | Humans and agents reviewing the change                                            |\n| **When it's written**        | During `/specd-new`, before any artifacts exist                                                               | During `/specd-design`, as the first artifact                                     |\n| **Part of the schema DAG?**  | No — it's outside the artifact pipeline                                                                       | Yes — other artifacts depend on it                                                |\n| **Validated?**               | No — no schema validation applies                                                                             | Yes — must pass schema validations                                                |\n| **Required by all schemas?** | Yes — every schema needs exploration context                                                                  | No — a schema can omit `proposal` entirely                                        |\n| **Content**                  | Everything discussed: rejected ideas, tangential topics, agreements, codebase observations, conversation flow | Curated: motivation, current behaviour, proposed solution, specs affected, impact |\n\nThe key distinction: **`proposal.md` is a schema artifact that a schema may or may not include.** A custom schema could replace `proposal` with a different first artifact, or skip it entirely and go straight to specs. But even without a proposal, the agent still needs to know what the user wants to build and why. That's what `specd-exploration.md` provides — it is schema-independent context that ensures continuity across sessions regardless of which artifacts the schema defines.\n\n### What it contains\n\nThe file captures everything from the discovery conversation:\n\n- **Problem statement** — what the user wants to achieve and why\n- **Approach / solution outline** — the agreed strategy\n- **Affected areas** — packages, modules, files, specs discussed\n- **Spec IDs** — attached to the change and mentioned as relevant\n- **Design decisions and agreements** — anything confirmed, rejected, or constrained\n- **Steps or plan** — if specific phases or ordering were agreed\n- **Rejected alternatives** — with reasons, so the agent doesn't re-propose them\n- **Open questions** — unresolved items for design to clarify\n- **Codebase observations** — file paths, patterns, current behaviour\n- **Tangential topics** — related issues, future ideas, things mentioned in passing\n- **Conversation flow** — how the discussion evolved\n\nEach item includes concrete anchors (file paths, spec IDs, function names) so the reader can verify whether they still hold, and reasons behind decisions so edge cases can be judged even if details have changed.\n\n### Staleness\n\nTime passes between exploration and design. Code changes, specs get renamed, decisions become outdated. The file includes a `Generated: YYYY-MM-DD` timestamp, and `/specd-design` performs a staleness check before trusting its content — verifying that referenced paths and spec IDs still exist, and flagging any significant drift to the user.\n\n---\n\n## Where to go next\n\n- [Schema format reference](../schemas/schema-format.md) — define custom workflow steps, artifacts, hooks, and task completion checks for your project.\n- [Configuration guide](configuration.md) — enable approval gates, configure workspaces, and add schema overrides.\n- [CLI reference](../cli/cli-reference.md) — all `specd changes`, `specd drafts`, `specd discard`, and `specd archives` commands.\n- [Domain model](../core/domain-model.md) — the `Change`, `ChangeState`, `ChangeEvent`, and `ChangeArtifact` types returned by `@specd/core` use cases.\n",
    "lineCount": 723,
    "byteLength": 41744,
    "outline": [
      {
        "index": 1,
        "heading": "Change Lifecycle Guide",
        "level": 1,
        "startLine": 1,
        "endLine": 723,
        "lines": 723,
        "content": "# Change Lifecycle Guide\n\nEvery piece of work in specd is a **change**. A change tracks everything related to modifying one or more specs — the proposal, the spec files, the design, the implementation tasks, and the final archive record. This guide explains how changes move through their lifecycle, what each state means, and how approval gates, task completion checks, and hooks affect that progression.\n\n## How the pieces fit together\n\nspecd has three layers that work together to drive a change from idea to completion:\n\n**The CLI** is the engine. It manages changes, transitions between states, validates artifacts, compiles context, and enforces gates. Every operation — creating a change, checking status, transitioning states, archiving — is a CLI command. The CLI is the source of truth for what is happening.\n\n**The workflow** is the rules. It defines which states exist, what transitions are valid, what artifacts must be produced, and what gates must be passed. The workflow comes from two sources: the schema (which defines artifacts and their dependency order) and the project config (which enables approval gates and hooks). Together they determine the path a change must follow.\n\n**The skills** are the interface. They are slash commands installed in your coding assistant (`/specd`, `/specd-new`, `/specd-design`, etc.) that orchestrate the workflow by calling the CLI. Each skill knows how to guide the agent through one phase — loading context, writing artifacts, validating, transitioning. You interact with skills; skills interact with the CLI; the CLI enforces the workflow.\n\n```text\nYou ←→ Skills ←→ CLI ←→ Workflow (schema + config)\n         │                    │\n         │  calls commands    │  enforces rules\n         │  reads context     │  validates artifacts\n         │  writes artifacts  │  manages transitions\n```\n\nIn practice, you type `/specd` and the skill figures out where you are, what to do next, and which CLI commands to run. You can also use the CLI directly — for scripting, CI pipelines, or when you want fine-grained control — but for day-to-day work the skills provide the guided experience.\n\n---\n\n## The lifecycle at a glance\n\n```\n                        ┌──────────────────────────────────────────────────────┐\n                        │    redesign — transition back to designing            │\n                        │    available from any active state (including        │\n                        │    archiving when recovery is needed)               │\n                        └──────────────────────────────────────────────────────┘\n                                              ↑\ndrafting → designing ⇄ ready ─────────────────────────────────── → implementing ⇄ verifying → done ──────────────────────── → archivable → archiving\n                        ↓                                                                       ↓\n               pending-spec-approval                                                    pending-signoff\n                        ↓                                                                       ↓\n                  spec-approved ─────────────────────────────────→ implementing           signed-off\n```\n\nWhen neither approval gate is enabled, the main path is:\n\n```\ndrafting → designing → ready → implementing → verifying → done → archivable → archiving\n```\n\n## States explained\n\n### `drafting`\n\nThe initial state. A newly created change starts here before any work has been done.\n\n- **What it means:** The change exists but has not entered the design phase.\n- **What you do:** Nothing yet. Transition to `designing` when you are ready to start.\n- **Transition out:** `specd changes transition <name> designing`\n- **What can block it:** Nothing — this transition is always allowed.\n\n---\n\n### `designing`\n\nThe active design and specification phase. This is where the bulk of artifact work happens.\n\n- **What it means:** You are writing the proposal, specs, verify files, design, and tasks.\n- **What you do:** Create and refine the five standard artifacts (proposal, specs, verify, design, tasks). Run `specd changes status <name>` to check artifact progress.\n- **Transition out:** `specd changes transition <name> ready` once all required artifacts are complete and validated.\n- **What can block it:** The `ready` step requires all artifacts listed in the schema's `requires` field (by default: proposal, specs, verify, design, tasks) to have `complete` status. The CLI reports which artifacts are still missing or in progress.\n\nThe `designing → designing` self-transition is valid — it records a checkpoint without changing state. This happens automatically when redesign invalidates an already-`designing` change.\n\n---\n\n### `ready`\n\nAll design artifacts are complete. The change is ready to hand off to implementation.\n\n- **What it means:** Every required artifact has been written and validated. The change has been reviewed and is cleared for implementation.\n- **What you do:** If the `approvals.spec` gate is enabled in `specd.yaml`, transition to `pending-spec-approval` to request approval. Otherwise, transition directly to `implementing`.\n- **Transition out:** `implementing` (no gate), or `pending-spec-approval` (with gate), or back to `designing` (redesign).\n- **What can block it:** The spec approval gate (if enabled) must be passed before proceeding to `implementing`.\n\n---\n\n### `pending-spec-approval`\n\nThe spec approval gate is active and a human reviewer has not yet approved the specs.\n\n- **What it means:** specd has been asked to require human sign-off on the spec artifacts before implementation begins. The change is waiting for that approval.\n- **What you do:** A reviewer runs `specd changes approve-spec <name> --reason \"...\"` after inspecting the spec artifacts.\n- **Transition out:** `spec-approved` after approval, or back to `designing` (redesign).\n- **What can block it:** Approval must be recorded by a human. The gate cannot be bypassed.\n\nThis state only appears when `approvals.spec` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n\n### `spec-approved`\n\nA human has reviewed and approved the spec artifacts.\n\n- **What it means:** The specs have been signed off. Implementation may now proceed.\n- **What you do:** Transition to `implementing`.\n- **Transition out:** `implementing`, or back to `designing` (redesign).\n- **What can block it:** If specs were modified after approval, the change is automatically invalidated and returns to `designing` — the approval is cleared.\n\n---\n\n### `implementing`\n\nActive development is in progress.\n\n- **What it means:** The implementation tasks are being worked through.\n- **What you do:** Work through the task list in `tasks.md`, checking off items as you go (`- [x]`). Run `specd changes status <name>` to see task progress.\n- **Transition out:** `verifying` once all tasks are complete, or back to `designing` (redesign). The transition to `verifying` is blocked if any tasks remain incomplete.\n- **What can block it:** The `taskCompletionCheck` defined in the schema. By default, any unchecked `- [ ]` line in `tasks.md` prevents advancing to `verifying`. The CLI reports \"N/M tasks complete\" when this gate is active.\n\n---\n\n### `verifying`\n\nImplementation is complete. Verification scenarios are being run.\n\n- **What it means:** All tasks are done. You are now confirming the implementation satisfies every scenario in `verify.md`.\n- **What you do:** Run through every scenario in `verify.md`. Confirm each one passes by inspecting the code and running the relevant tests.\n- **Transition out:** `done` when all scenarios pass, back to `implementing` if a scenario fails (the schema may clear artifact validations on this backward transition), or back to `designing` (redesign).\n- **What can block it:** Nothing prevents the transition — it is up to you to judge readiness.\n\n---\n\n### `done`\n\nImplementation and verification are complete.\n\n- **What it means:** The change is finished from a development perspective. It can now be archived.\n- **What you do:** If the `approvals.signoff` gate is enabled, transition to `pending-signoff`. Otherwise, transition to `archivable`.\n- **Transition out:** `archivable` (no gate), `pending-signoff` (with gate), or back to `designing` (redesign).\n- **What can block it:** The signoff gate (if enabled) must be passed before proceeding to `archivable`.\n\n---\n\n### `pending-signoff`\n\nThe signoff gate is active and a human reviewer has not yet signed off.\n\n- **What it means:** The change requires final human sign-off before it can be archived. This gate is typically used for compliance or stakeholder review.\n- **What you do:** A reviewer runs `specd changes signoff <name> --reason \"...\"` after inspecting the completed work.\n- **Transition out:** `signed-off` after sign-off, or back to `designing` (redesign).\n- **What can block it:** Sign-off must be recorded by a human.\n\nThis state only appears when `approvals.signoff` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n\n### `signed-off`\n\nA human has reviewed and signed off the completed change.\n\n- **What it means:** The change has received its final human approval and may proceed to archiving.\n- **What you do:** Transition to `archivable`.\n- **Transition out:** `archivable`, or back to `designing` (redesign).\n- **What can block it:** Nothing.\n\n---\n\n### `archivable`\n\nThe change is ready to be archived.\n\n- **What it means:** All gates have been passed. The change can run the archive operation.\n- **What you do:** Run `specd changes archive <name>` to perform the archive.\n- **Transition out:** `archiving` (only after archive preflight succeeds and canonical publication is about to start — see [Archiving](#archiving)), or back to `designing` (redesign).\n- **What can block it:** Archive pre-hooks, overlap/read-only guards, or preflight failures keep the change here (the archive command does not transition to `archiving` until commit preparation has passed).\n\n---\n\n### `archiving`\n\nThe change is in the archive **commit phase** (canonical publication, archive move, and post-archive work), or it completed successfully and was moved to the archive directory.\n\n- **What it means:** While `specd changes archive` is running, the change transitions to `archiving` immediately before the first spec publication, after guards, pre-archive hooks, orphan detection, and batch snapshot. If publication or the archive move fails and batch restore succeeds, the change returns to `archivable` so you can fix the problem and retry. If restore is incomplete, the change stays in `archiving` until you recover manually.\n- **What you do:** If archive failed and the change is back in `archivable`, review restored specs and run `specd changes archive <name>` again. If the change is stuck in `archiving` after a partial restore, use `specd changes transition <name> designing` to revise artifacts, or `specd changes transition <name> archivable` after you have manually restored canonical specs (lifecycle rollback only — no archive hooks).\n- **Transition out:** `archivable` (recovery after a failed commit when canonical storage was restored), `designing` (manual escape to revise specs/deltas), or none after a **successful** archive (the change directory is moved to the archive and is no longer an active change).\n\n---\n\n## Full transition table\n\n| From                    | To                                                                                                          |\n| ----------------------- | ----------------------------------------------------------------------------------------------------------- |\n| `drafting`              | `designing`                                                                                                 |\n| `designing`             | `ready`, `designing`                                                                                        |\n| `ready`                 | `implementing`, `pending-spec-approval`, `designing`                                                        |\n| `pending-spec-approval` | `spec-approved`, `designing`                                                                                |\n| `spec-approved`         | `implementing`, `designing`                                                                                 |\n| `implementing`          | `verifying`, `designing`                                                                                    |\n| `verifying`             | `implementing`, `done`, `designing`                                                                         |\n| `done`                  | `archivable`, `pending-signoff`, `designing`                                                                |\n| `pending-signoff`       | `signed-off`, `designing`                                                                                   |\n| `signed-off`            | `archivable`, `designing`                                                                                   |\n| `archivable`            | `archiving`, `designing`                                                                                    |\n| `archiving`             | `archivable`, `designing` _(active change only; successful archive removes the change from the active set)_ |\n\n---\n\n## Identity and audit trails\n\nspecd records the identity of the person or system performing every significant operation — creating a change, transitioning states, approving specs, and signing off on completed work. These records create a tamper-evident audit trail persisted in the change manifest and history.\n\n### How identity is resolved\n\nSpecD uses a pluggable identity resolution system. By default, it auto-detects the active Version Control System (Git, Mercurial, or Subversion) and retrieves the user's name and email from its configuration.\n\nIn specific environments, you can force a provider or use custom identity plugins. See the [Configuration Guide](configuration.md#identity-resolution) for details.\n\n### Privacy in public repositories\n\nFor projects hosted in public repositories, you can enable **privacy modes** to obfuscate real names and emails before they are persisted in the repository.\n\nSupported modes include:\n\n- **Hashing**: deterministic HMAC-SHA256 obfuscation of emails.\n- **Masking**: partial obfuscation (e.g., `j***z@e***.com`).\n- **Anonymization**: replacement with generic placeholders.\n\nPrivacy settings also automatically filter out sensitive metadata. For more details on configuring privacy, see the [Privacy section](configuration.md#privacy) in the Configuration Guide.\n\n---\n\n## Redesign: going back to designing\n\nAlmost every state can transition back to `designing`. This is the **redesign path** — it is how you handle requirement changes, new information, or mistakes discovered mid-lifecycle.\n\nTo redesign from any active state:\n\n```bash\nspecd changes transition <name> designing\n```\n\nWhen a redesign transition occurs:\n\n1. An `invalidated` event is appended to the change history with `cause: 'redesign'`.\n2. A `transitioned` event rolling back to `designing` is appended.\n3. Any active spec approval (`activeSpecApproval`) and signoff (`activeSignoff`) are cleared — they are no longer valid after specs have been reworked.\n4. Artifact validation hashes are reset, requiring re-validation before the change can advance to `ready` again.\n\nRedesign also happens automatically when spec IDs are updated (`cause: 'spec-change'`) or when artifact content changes after an approval was recorded (`cause: 'artifact-change'`).\n\nRedesign from `archiving` is allowed via `specd changes transition <name> designing` (artifacts are downgraded for review). Use this when batch restore failed partially or you need to change deltas before retrying archive. After a **successful** archive, the change is no longer active — create a new change instead.\n\n---\n\n## Approval gates\n\nspecd supports two optional human-approval gates. Both are disabled by default.\n\n### Spec approval gate\n\nBlocks the transition from `ready` to `implementing` until a human approves the spec artifacts.\n\n**Path when enabled:**\n\n```\nready → pending-spec-approval → spec-approved → implementing\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  spec: true\n```\n\n**Performing the approval:**\n\n```bash\nspecd changes approve-spec add-auth --reason \"Specs reviewed and approved — all requirements are clear\"\n```\n\nThe approver reviews the spec and verify artifacts in the change directory. The approval records a hash of those artifacts at the moment of approval. If any of those artifacts are subsequently modified, an `invalidated` event is appended automatically and the change returns to `designing`, clearing the approval.\n\n### Signoff gate\n\nBlocks the transition from `done` to `archivable` until a human signs off the completed work.\n\n**Path when enabled:**\n\n```\ndone → pending-signoff → signed-off → archivable\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  signoff: true\n```\n\n**Performing the signoff:**\n\n```bash\nspecd changes signoff add-auth --reason \"Implementation reviewed — all scenarios verified\"\n```\n\nBoth gates can be enabled simultaneously:\n\n```yaml\napprovals:\n  spec: true\n  signoff: true\n```\n\n---\n\n## Task completion gating\n\nThe transition from `implementing` to `verifying` is gated on task completion. This is enforced by the `taskCompletionCheck` defined in the schema.\n\nThe default schema (`@specd/schema-std`) checks for unchecked checkbox lines in `tasks.md`:\n\n- **Incomplete pattern:** `^\\s*-\\s+\\[ \\]` — any `- [ ]` line blocks the transition.\n- **Complete pattern:** `^\\s*-\\s+\\[x\\]` — `- [x]` lines count toward completion.\n\nWhen you run `specd changes transition <name> verifying` and tasks remain incomplete, the CLI reports the current progress:\n\n```\n3/5 tasks complete — transition to verifying is blocked\n```\n\nMark tasks complete by changing `- [ ]` to `- [x]` in `tasks.md`. Once all tasks are checked, the transition is allowed.\n\nOne subtlety: `tasks.md` is also validated by artifact hashing. The schema normalises checkboxes before hashing — `- [x]` lines are converted back to `- [ ]` during hash computation so that checking off tasks does not trigger an approval invalidation. Checking off tasks is not a spec change; it is progress tracking.\n\n---\n\n## Hooks\n\nHooks let you attach automated actions or AI guidance to lifecycle transitions. They are defined in the schema's `workflow` section and optionally extended in `specd.yaml` via `schemaOverrides`.\n\n### Pre and post hooks\n\n- **Pre hooks** run before the change enters a step. If a pre hook fails, the transition is blocked.\n- **Post hooks** run after the change enters a step. If a post hook fails, the transition is not rolled back.\n\nWhen shell hooks are long-running, SpecD keeps them observable while they are\nin flight. In human-facing `text` output, completed hooks remain visible,\nactive hooks show recent output plus a running indicator, and quiet hooks emit\nliveness updates. In `json` and `toon`, in-flight hook progress is emitted on\n`stderr` as structured events while the final command result remains on\n`stdout`.\n\n### Hook types\n\n| Type           | Description                                                                              |\n| -------------- | ---------------------------------------------------------------------------------------- |\n| `run:`         | A shell command executed in the project root. Non-zero exit code blocks a pre hook.      |\n| `instruction:` | An AI context block injected into the agent context for the step. Not directly runnable. |\n| `external:`    | An explicit external hook dispatched to a registered external hook runner by `type`.     |\n\nAn `instruction:` hook provides guidance to an AI agent about what to do in that step. It is informational — it does not execute code. A `run:` hook executes a shell command through the internal `HookRunner`. An `external:` hook is routed to a separate `ExternalHookRunner` selected by `external.type`.\n\n```yaml\nworkflow:\n  - step: implementing\n    hooks:\n      pre:\n        - id: run-lint\n          run: pnpm lint\n        - id: docker-test\n          external:\n            type: docker\n            config:\n              image: node:20\n              command: pnpm test\n      post:\n        - id: notify\n          run: echo \"Implementation started for {{change.name}}\"\n```\n\nExternal hooks use the same workflow phase semantics as shell hooks:\n\n- a failing `pre` external hook blocks the transition\n- a failing `post` external hook does not roll the state back\n- an unknown `external.type` is a hard error, not a silent skip\n\n### Template variables\n\nHook `run:` commands support template variable substitution:\n\n| Variable           | Value                                                        |\n| ------------------ | ------------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name (e.g. `add-auth`)                     |\n| `{{change.path}}`  | Absolute path to the change directory                        |\n| `{{project.root}}` | Absolute path to the project root (where `specd.yaml` lives) |\n\n### Hook execution order\n\nWhen a step is entered, schema-level hooks fire first, then any project-level hooks added via `schemaOverrides` in `specd.yaml`. Within each source, hooks run in the order they are declared.\n\n---\n\n## Drafting and discarding\n\nDrafting and discarding are sidebars to the main lifecycle — they do not affect the change's lifecycle state.\n\n### Drafting\n\nDrafting shelves a change temporarily without abandoning it.\n\n```bash\nspecd drafts move add-auth         # move to drafts/\nspecd drafts restore add-auth      # move back to changes/\n```\n\nA drafted change retains its current lifecycle state. When restored, it continues from where it left off. Drafting is useful when you need to context-switch to another change and want to keep the current work safely out of the way.\n\n**Guard:** When a change has previously reached the `implementing` state, drafting is blocked by default — the code and specs may be out of sync, and shelving without archival risks leaving partial implementation behind. If you are certain you want to draft anyway, use `--force`:\n\n```bash\nspecd changes draft add-auth --force\n```\n\n### Discarding\n\nDiscarding permanently abandons a change. A reason is required.\n\n```bash\nspecd discard add-auth --reason \"Approach superseded by add-oauth-flow\"\n```\n\nA discarded change is moved to the discarded directory and is never archived. This is permanent — there is no restore from discard. You can optionally name the change or changes that supersede it:\n\n```bash\nspecd discard add-auth --reason \"Superseded\" --superseded-by add-oauth-flow\n```\n\n**Guard:** When a change has previously reached the `implementing` state, discarding is blocked by default — code that has been implemented should ideally be archived, not silently abandoned. If you are certain you want to discard anyway, use `--force`:\n\n```bash\nspecd changes discard add-auth --reason \"No longer needed\" --force\n```\n\n---\n\n## Archiving\n\nArchiving is the final operation in the lifecycle. The change must be in `archivable`. When you run:\n\n```bash\nspecd changes archive <name>\n```\n\n### Phases and lifecycle state\n\nThe change stays in **`archivable`** through early archive work (schema guard, overlap/read-only checks, pre-archive hooks, and full-batch preflight). It moves to **`archiving`** only immediately before the first canonical spec publication, after orphan backup detection and a per-spec batch snapshot under `.specd-archive-backup/` in each affected spec directory.\n\n| Phase                                | Lifecycle state | On failure                                                                                |\n| ------------------------------------ | --------------- | ----------------------------------------------------------------------------------------- |\n| Guards, pre-archive hooks, preflight | `archivable`    | Stays `archivable`; no backup dirs                                                        |\n| Orphan detection + batch snapshot    | `archivable`    | Stays `archivable` (matching orphan: auto-restore + abort with guidance)                  |\n| Canonical publication + archive move | `archiving`     | Batch restore; `→ archivable` if restore succeeds, stay `archiving` if restore is partial |\n| Metadata + post-archive hooks        | `archiving`     | Archive already committed; failures are reported but do not roll back specs               |\n\nPre-archive hooks use the workflow step name `archiving`; they do **not** require the change to already be in lifecycle state `archiving`.\n\n### Commit steps (after transition to `archiving`)\n\n1. **Canonical publication:** For each spec in the batch, delta artifacts are merged into the base spec in the repository (or full spec files are copied). Publication is staged per spec; created files are recorded for restore.\n2. **Archive move:** The change directory is moved to the archive. An `ArchivedChange` record is created with the change name, timestamp, and spec IDs.\n3. **Backup cleanup:** `.specd-archive-backup/` directories are removed after a successful commit.\n4. **Metadata generation:** Persisted `metadata.json` is written **after** the archive move (in-memory preflight checks still run earlier).\n5. **Post-archive hooks:** Optional hooks configured for the `archiving` workflow step.\n\n### Batch restore and retry\n\nIf publication fails partway through a multi-spec batch, specd restores already-published specs from the batch backup (reverse publication order) and rolls the change back to **`archivable`** when restore completes. Retry archive merges deltas against the restored canonical base — not against partially merged content.\n\nIf restore cannot complete for every spec, the change remains in **`archiving`**. `specd changes status` recommends transitioning to **`designing`** for manual recovery.\n\n### Orphan backups\n\nIf `.specd-archive-backup/` already exists when archive starts:\n\n- **Same change name** in the manifest: canonical specs are auto-restored, the backup is removed, and the current archive attempt aborts (review and retry).\n- **Different change name**: archive aborts with a repair message; live files are not overwritten.\n\n### Recovery transitions from `archiving`\n\n| Transition               | Purpose                                                                                                             |\n| ------------------------ | ------------------------------------------------------------------------------------------------------------------- |\n| `archiving → archivable` | Lifecycle rollback after you fixed canonical specs manually; does not re-run archive hooks                          |\n| `archiving → designing`  | Revise change artifacts (downgrades files to `pending-review`); use after partial restore or when specs need rework |\n\nTransitions to `implementing`, `verifying`, or `done` from `archiving` are not valid.\n\n### After a successful archive\n\nSpecs in the repository reflect the merged requirements. The change history is preserved under `.specd/archive/`. To rework merged specs, open a **new** change — the archived record is not transitioned back.\n\n---\n\n## Practical example: a full lifecycle with `/specd`\n\nHere is a complete lifecycle walkthrough for a change that adds an authentication feature, showing what the agent does at each step. In practice, you drive this through your coding assistant using the `/specd` skill — the agent handles transitions, artifact creation, and validation automatically.\n\n### 1. Starting — `/specd`\n\nYou open your coding assistant and type `/specd`. The skill shows the project status:\n\n```\n# specd\n\n**Schema:** @specd/schema-std\n**Workspaces:** default (8 specs)\n**Active changes:** none\n**Drafts:** none\n\nNo active changes. What would you like to work on?\n```\n\nYou describe what you want: \"I want to add login and logout authentication flows.\"\n\nThe skill suggests creating a change and invokes `/specd-new`.\n\n### 2. Creation — `/specd-new`\n\nThe agent has a discovery conversation with you — understanding the problem, discussing the approach, identifying affected specs, and reaching agreements about scope. Once the picture is clear, it creates the change:\n\n```bash\nspecd changes create add-auth --spec default:auth/login --spec default:auth/logout\n```\n\nBefore handing off, the agent writes a `specd-exploration.md` file to the change directory. This captures everything from the conversation — the problem, the agreed approach, design decisions, rejected alternatives, codebase observations, and open questions. It is the agent's working memory, persisted so that the next phase can pick up where you left off even in a different session. See [Exploration context](#exploration-context-specd-explorationmd) for details.\n\nState: `drafting` → the agent suggests running `/specd-design`.\n\n### 3. Designing — `/specd-design`\n\nThe agent loads the change state, the schema, and the compiled context. It also reads `specd-exploration.md` from the change directory to recover the full discovery context — if time has passed since creation, it verifies that referenced files and spec IDs still exist and flags any drift before proceeding.\n\nThis is the longest phase. The agent works through each artifact in dependency order. The artifacts below follow `@specd/schema-std` — other schemas may define a different set or order:\n\n**Step 3a: Proposal** — The agent writes `proposal.md`:\n\n- Explains the motivation (why the project needs authentication)\n- Describes the current behaviour (no auth exists)\n- Outlines the proposed solution at a high level\n- Lists the specs that will be created: `default:auth/login` and `default:auth/logout`\n- Notes the impact on existing code (new middleware, new routes)\n\nOnce written, the agent validates the proposal and marks it complete.\n\n**Step 3b: Specs** — The agent writes `spec.md` for each spec:\n\n- `specs/default/auth/login/spec.md` — Requirements for login: valid credentials return a session token, invalid credentials return a generic error, sessions expire after 24 hours, rate limiting on the endpoint\n- `specs/default/auth/logout/spec.md` — Requirements for logout: session is invalidated, subsequent requests are rejected\n\nEach spec uses SHALL/MUST language and groups requirements under `### Requirement:` headings.\n\n**Step 3c: Verify** — The agent writes `verify.md` for each spec:\n\n- `specs/default/auth/login/verify.md` — WHEN/THEN scenarios for each login requirement: valid credentials, invalid credentials, expired sessions, rate limit exceeded\n- `specs/default/auth/logout/verify.md` — WHEN/THEN scenarios for logout: successful invalidation, already-expired session\n\nEach scenario mirrors a requirement from `spec.md`.\n\n**Step 3d: Design** — The agent writes `design.md`:\n\n- Reads the proposal, specs, and verify files\n- Analyses the existing codebase to identify affected areas\n- Lists new constructs: `AuthService`, `SessionStore`, login/logout route handlers, auth middleware\n- Documents the approach, key decisions, and trade-offs\n- Maps every requirement and scenario to a concrete implementation path\n\n**Step 3e: Tasks** — The agent writes `tasks.md`:\n\n- Derives discrete tasks from `design.md`\n- Each task is a checkbox with indented context: which file, which symbol, what to change\n- Groups tasks logically (e.g., \"1. Session infrastructure\", \"2. Login flow\", \"3. Logout flow\", \"4. Tests\")\n\nOnce all five artifacts are complete and validated, the agent transitions to `ready`, then to `implementing`.\n\n### 4. Implementing — `/specd-implement`\n\nThe agent reads the compiled context for the `implementing` step — which includes the specs, design, and task list — and works through tasks one by one:\n\n```\n## 1. Session infrastructure\n- [x] 1.1 Create SessionStore interface\n      `src/auth/session-store.ts`: New interface — get, set, delete session\n      Approach: In-memory store with TTL from design.md\n      (Req: Sessions expire after 24 hours)\n\n- [x] 1.2 Implement InMemorySessionStore\n      `src/auth/in-memory-session-store.ts`: Implement SessionStore\n      Approach: Map<string, Session> with setTimeout cleanup\n\n## 2. Login flow\n- [x] 2.1 Create login handler\n      `src/auth/login.ts`: POST /login route handler\n      ...\n- [ ] 2.2 Add rate limiting middleware\n      `src/auth/rate-limit.ts`: ...\n```\n\nThe agent checks off each task as it completes the work. Pre/post hooks run at the step boundaries — for example, `pnpm test` runs after implementation.\n\nOnce all checkboxes are `[x]`, the agent transitions to `verifying`.\n\n### 5. Verifying — `/specd-verify`\n\nVerification is not the same as running tests. Tests check that code behaves as programmed — verification checks that the implementation satisfies the _requirements_. These are different questions:\n\n- **Not every project has tests.** Documentation changes, configuration changes, infrastructure work, early prototypes — many changes have no test suite at all. Verification still applies: the agent inspects the implementation against each scenario and confirms it holds.\n- **Tests can pass while requirements are missed.** A test suite exercises the code paths the developer thought to write. Verification scenarios come from the spec — they represent what was _agreed_ the system should do. A passing test suite does not guarantee that every requirement was addressed.\n- **Verification checks more than code.** The agent inspects the implementation holistically: does the code structure match the design? Were all tasks completed? Do the changes satisfy the GIVEN/WHEN/THEN conditions from `verify.md`? It runs tests where they exist, but also reads code, checks behaviour, and confirms edge cases that tests may not cover.\n\nThe agent reads each scenario from `verify.md` and confirms the implementation satisfies it — by inspecting code, running tests where applicable, and verifying GIVEN/WHEN/THEN conditions:\n\n```\n### Requirement: Valid credentials\n#### Scenario: Successful login\n- WHEN a user submits valid credentials\n- THEN they receive an authenticated session token\n→ ✓ Verified: login handler returns JWT on valid credentials\n\n#### Scenario: Invalid credentials\n- WHEN a user submits invalid credentials\n- THEN they receive a generic error with no field-specific information\n→ ✓ Verified: handler returns 401 with generic message\n```\n\nIf a scenario fails, the agent goes back to fix the issue and then returns to verify again.\n\nOnce all scenarios pass, the agent transitions to `done`.\n\n### 6. Archiving — `/specd-archive`\n\nThe agent runs `specd changes archive add-auth`. specd:\n\n1. Syncs `spec.md` and `verify.md` into `specs/default/auth/login/` and `specs/default/auth/logout/`\n2. Applies any delta files to existing specs\n3. Generates metadata for each spec\n4. Moves the change directory to `.specd/archive/`\n\nThe specs are now permanent. The change is a historical record.\n\n### Redesign mid-lifecycle\n\nIf you discover during implementation that a requirement is wrong:\n\n```\n> The rate limiting should be per-IP, not global. Let's go back and fix the spec.\n```\n\nThe agent transitions back to `designing`. This:\n\n- Records an `invalidated` event with `cause: 'redesign'`\n- Clears any active approvals\n- Resets artifact validation hashes\n\nThe agent updates the spec, verify, design, and tasks, then works forward through the lifecycle again.\n\n---\n\n## Exploration context (`specd-exploration.md`)\n\nWhen an AI agent runs `/specd-new`, it has a discovery conversation with the user — understanding the problem, discussing approaches, identifying affected specs, and reaching agreements about scope and direction. All of this context is captured in a file called `specd-exploration.md`, written to the change directory (`<changePath>/specd-exploration.md`) before the agent hands off to `/specd-design`.\n\n### Why it exists\n\nDesign happens in a different session — often hours or days after creation. The agent that writes artifacts has no memory of the discovery conversation. Without a persistent record, it would need to start from scratch: ask the user to re-explain everything, or worse, guess based on the change name and one-line description alone.\n\n`specd-exploration.md` bridges this gap. It is the agent's working memory, persisted to disk so that any future session can pick up exactly where the conversation left off.\n\n### How it differs from `proposal.md`\n\nThey may look similar at first glance, but they serve fundamentally different purposes:\n\n|                              | `specd-exploration.md`                                                                                        | `proposal.md`                                                                     |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |\n| **What it is**               | Agent working memory — raw conversation context                                                               | Formal schema artifact — structured document                                      |\n| **Who it's for**             | The AI agent in future sessions                                                                               | Humans and agents reviewing the change                                            |\n| **When it's written**        | During `/specd-new`, before any artifacts exist                                                               | During `/specd-design`, as the first artifact                                     |\n| **Part of the schema DAG?**  | No — it's outside the artifact pipeline                                                                       | Yes — other artifacts depend on it                                                |\n| **Validated?**               | No — no schema validation applies                                                                             | Yes — must pass schema validations                                                |\n| **Required by all schemas?** | Yes — every schema needs exploration context                                                                  | No — a schema can omit `proposal` entirely                                        |\n| **Content**                  | Everything discussed: rejected ideas, tangential topics, agreements, codebase observations, conversation flow | Curated: motivation, current behaviour, proposed solution, specs affected, impact |\n\nThe key distinction: **`proposal.md` is a schema artifact that a schema may or may not include.** A custom schema could replace `proposal` with a different first artifact, or skip it entirely and go straight to specs. But even without a proposal, the agent still needs to know what the user wants to build and why. That's what `specd-exploration.md` provides — it is schema-independent context that ensures continuity across sessions regardless of which artifacts the schema defines.\n\n### What it contains\n\nThe file captures everything from the discovery conversation:\n\n- **Problem statement** — what the user wants to achieve and why\n- **Approach / solution outline** — the agreed strategy\n- **Affected areas** — packages, modules, files, specs discussed\n- **Spec IDs** — attached to the change and mentioned as relevant\n- **Design decisions and agreements** — anything confirmed, rejected, or constrained\n- **Steps or plan** — if specific phases or ordering were agreed\n- **Rejected alternatives** — with reasons, so the agent doesn't re-propose them\n- **Open questions** — unresolved items for design to clarify\n- **Codebase observations** — file paths, patterns, current behaviour\n- **Tangential topics** — related issues, future ideas, things mentioned in passing\n- **Conversation flow** — how the discussion evolved\n\nEach item includes concrete anchors (file paths, spec IDs, function names) so the reader can verify whether they still hold, and reasons behind decisions so edge cases can be judged even if details have changed.\n\n### Staleness\n\nTime passes between exploration and design. Code changes, specs get renamed, decisions become outdated. The file includes a `Generated: YYYY-MM-DD` timestamp, and `/specd-design` performs a staleness check before trusting its content — verifying that referenced paths and spec IDs still exist, and flagging any significant drift to the user.\n\n---\n\n## Where to go next\n\n- [Schema format reference](../schemas/schema-format.md) — define custom workflow steps, artifacts, hooks, and task completion checks for your project.\n- [Configuration guide](configuration.md) — enable approval gates, configure workspaces, and add schema overrides.\n- [CLI reference](../cli/cli-reference.md) — all `specd changes`, `specd drafts`, `specd discard`, and `specd archives` commands.\n- [Domain model](../core/domain-model.md) — the `Change`, `ChangeState`, `ChangeEvent`, and `ChangeArtifact` types returned by `@specd/core` use cases.\n"
      },
      {
        "index": 2,
        "heading": "How the pieces fit together",
        "level": 2,
        "startLine": 5,
        "endLine": 26,
        "lines": 22,
        "content": "## How the pieces fit together\n\nspecd has three layers that work together to drive a change from idea to completion:\n\n**The CLI** is the engine. It manages changes, transitions between states, validates artifacts, compiles context, and enforces gates. Every operation — creating a change, checking status, transitioning states, archiving — is a CLI command. The CLI is the source of truth for what is happening.\n\n**The workflow** is the rules. It defines which states exist, what transitions are valid, what artifacts must be produced, and what gates must be passed. The workflow comes from two sources: the schema (which defines artifacts and their dependency order) and the project config (which enables approval gates and hooks). Together they determine the path a change must follow.\n\n**The skills** are the interface. They are slash commands installed in your coding assistant (`/specd`, `/specd-new`, `/specd-design`, etc.) that orchestrate the workflow by calling the CLI. Each skill knows how to guide the agent through one phase — loading context, writing artifacts, validating, transitioning. You interact with skills; skills interact with the CLI; the CLI enforces the workflow.\n\n```text\nYou ←→ Skills ←→ CLI ←→ Workflow (schema + config)\n         │                    │\n         │  calls commands    │  enforces rules\n         │  reads context     │  validates artifacts\n         │  writes artifacts  │  manages transitions\n```\n\nIn practice, you type `/specd` and the skill figures out where you are, what to do next, and which CLI commands to run. You can also use the CLI directly — for scripting, CI pipelines, or when you want fine-grained control — but for day-to-day work the skills provide the guided experience.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "The lifecycle at a glance",
        "level": 2,
        "startLine": 27,
        "endLine": 48,
        "lines": 22,
        "content": "## The lifecycle at a glance\n\n```\n                        ┌──────────────────────────────────────────────────────┐\n                        │    redesign — transition back to designing            │\n                        │    available from any active state (including        │\n                        │    archiving when recovery is needed)               │\n                        └──────────────────────────────────────────────────────┘\n                                              ↑\ndrafting → designing ⇄ ready ─────────────────────────────────── → implementing ⇄ verifying → done ──────────────────────── → archivable → archiving\n                        ↓                                                                       ↓\n               pending-spec-approval                                                    pending-signoff\n                        ↓                                                                       ↓\n                  spec-approved ─────────────────────────────────→ implementing           signed-off\n```\n\nWhen neither approval gate is enabled, the main path is:\n\n```\ndrafting → designing → ready → implementing → verifying → done → archivable → archiving\n```\n"
      },
      {
        "index": 4,
        "heading": "States explained",
        "level": 2,
        "startLine": 49,
        "endLine": 187,
        "lines": 139,
        "content": "## States explained\n\n### `drafting`\n\nThe initial state. A newly created change starts here before any work has been done.\n\n- **What it means:** The change exists but has not entered the design phase.\n- **What you do:** Nothing yet. Transition to `designing` when you are ready to start.\n- **Transition out:** `specd changes transition <name> designing`\n- **What can block it:** Nothing — this transition is always allowed.\n\n---\n\n### `designing`\n\nThe active design and specification phase. This is where the bulk of artifact work happens.\n\n- **What it means:** You are writing the proposal, specs, verify files, design, and tasks.\n- **What you do:** Create and refine the five standard artifacts (proposal, specs, verify, design, tasks). Run `specd changes status <name>` to check artifact progress.\n- **Transition out:** `specd changes transition <name> ready` once all required artifacts are complete and validated.\n- **What can block it:** The `ready` step requires all artifacts listed in the schema's `requires` field (by default: proposal, specs, verify, design, tasks) to have `complete` status. The CLI reports which artifacts are still missing or in progress.\n\nThe `designing → designing` self-transition is valid — it records a checkpoint without changing state. This happens automatically when redesign invalidates an already-`designing` change.\n\n---\n\n### `ready`\n\nAll design artifacts are complete. The change is ready to hand off to implementation.\n\n- **What it means:** Every required artifact has been written and validated. The change has been reviewed and is cleared for implementation.\n- **What you do:** If the `approvals.spec` gate is enabled in `specd.yaml`, transition to `pending-spec-approval` to request approval. Otherwise, transition directly to `implementing`.\n- **Transition out:** `implementing` (no gate), or `pending-spec-approval` (with gate), or back to `designing` (redesign).\n- **What can block it:** The spec approval gate (if enabled) must be passed before proceeding to `implementing`.\n\n---\n\n### `pending-spec-approval`\n\nThe spec approval gate is active and a human reviewer has not yet approved the specs.\n\n- **What it means:** specd has been asked to require human sign-off on the spec artifacts before implementation begins. The change is waiting for that approval.\n- **What you do:** A reviewer runs `specd changes approve-spec <name> --reason \"...\"` after inspecting the spec artifacts.\n- **Transition out:** `spec-approved` after approval, or back to `designing` (redesign).\n- **What can block it:** Approval must be recorded by a human. The gate cannot be bypassed.\n\nThis state only appears when `approvals.spec` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n\n### `spec-approved`\n\nA human has reviewed and approved the spec artifacts.\n\n- **What it means:** The specs have been signed off. Implementation may now proceed.\n- **What you do:** Transition to `implementing`.\n- **Transition out:** `implementing`, or back to `designing` (redesign).\n- **What can block it:** If specs were modified after approval, the change is automatically invalidated and returns to `designing` — the approval is cleared.\n\n---\n\n### `implementing`\n\nActive development is in progress.\n\n- **What it means:** The implementation tasks are being worked through.\n- **What you do:** Work through the task list in `tasks.md`, checking off items as you go (`- [x]`). Run `specd changes status <name>` to see task progress.\n- **Transition out:** `verifying` once all tasks are complete, or back to `designing` (redesign). The transition to `verifying` is blocked if any tasks remain incomplete.\n- **What can block it:** The `taskCompletionCheck` defined in the schema. By default, any unchecked `- [ ]` line in `tasks.md` prevents advancing to `verifying`. The CLI reports \"N/M tasks complete\" when this gate is active.\n\n---\n\n### `verifying`\n\nImplementation is complete. Verification scenarios are being run.\n\n- **What it means:** All tasks are done. You are now confirming the implementation satisfies every scenario in `verify.md`.\n- **What you do:** Run through every scenario in `verify.md`. Confirm each one passes by inspecting the code and running the relevant tests.\n- **Transition out:** `done` when all scenarios pass, back to `implementing` if a scenario fails (the schema may clear artifact validations on this backward transition), or back to `designing` (redesign).\n- **What can block it:** Nothing prevents the transition — it is up to you to judge readiness.\n\n---\n\n### `done`\n\nImplementation and verification are complete.\n\n- **What it means:** The change is finished from a development perspective. It can now be archived.\n- **What you do:** If the `approvals.signoff` gate is enabled, transition to `pending-signoff`. Otherwise, transition to `archivable`.\n- **Transition out:** `archivable` (no gate), `pending-signoff` (with gate), or back to `designing` (redesign).\n- **What can block it:** The signoff gate (if enabled) must be passed before proceeding to `archivable`.\n\n---\n\n### `pending-signoff`\n\nThe signoff gate is active and a human reviewer has not yet signed off.\n\n- **What it means:** The change requires final human sign-off before it can be archived. This gate is typically used for compliance or stakeholder review.\n- **What you do:** A reviewer runs `specd changes signoff <name> --reason \"...\"` after inspecting the completed work.\n- **Transition out:** `signed-off` after sign-off, or back to `designing` (redesign).\n- **What can block it:** Sign-off must be recorded by a human.\n\nThis state only appears when `approvals.signoff` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n\n### `signed-off`\n\nA human has reviewed and signed off the completed change.\n\n- **What it means:** The change has received its final human approval and may proceed to archiving.\n- **What you do:** Transition to `archivable`.\n- **Transition out:** `archivable`, or back to `designing` (redesign).\n- **What can block it:** Nothing.\n\n---\n\n### `archivable`\n\nThe change is ready to be archived.\n\n- **What it means:** All gates have been passed. The change can run the archive operation.\n- **What you do:** Run `specd changes archive <name>` to perform the archive.\n- **Transition out:** `archiving` (only after archive preflight succeeds and canonical publication is about to start — see [Archiving](#archiving)), or back to `designing` (redesign).\n- **What can block it:** Archive pre-hooks, overlap/read-only guards, or preflight failures keep the change here (the archive command does not transition to `archiving` until commit preparation has passed).\n\n---\n\n### `archiving`\n\nThe change is in the archive **commit phase** (canonical publication, archive move, and post-archive work), or it completed successfully and was moved to the archive directory.\n\n- **What it means:** While `specd changes archive` is running, the change transitions to `archiving` immediately before the first spec publication, after guards, pre-archive hooks, orphan detection, and batch snapshot. If publication or the archive move fails and batch restore succeeds, the change returns to `archivable` so you can fix the problem and retry. If restore is incomplete, the change stays in `archiving` until you recover manually.\n- **What you do:** If archive failed and the change is back in `archivable`, review restored specs and run `specd changes archive <name>` again. If the change is stuck in `archiving` after a partial restore, use `specd changes transition <name> designing` to revise artifacts, or `specd changes transition <name> archivable` after you have manually restored canonical specs (lifecycle rollback only — no archive hooks).\n- **Transition out:** `archivable` (recovery after a failed commit when canonical storage was restored), `designing` (manual escape to revise specs/deltas), or none after a **successful** archive (the change directory is moved to the archive and is no longer an active change).\n\n---\n"
      },
      {
        "index": 5,
        "heading": "`drafting`",
        "level": 3,
        "startLine": 51,
        "endLine": 61,
        "lines": 11,
        "content": "### `drafting`\n\nThe initial state. A newly created change starts here before any work has been done.\n\n- **What it means:** The change exists but has not entered the design phase.\n- **What you do:** Nothing yet. Transition to `designing` when you are ready to start.\n- **Transition out:** `specd changes transition <name> designing`\n- **What can block it:** Nothing — this transition is always allowed.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "`designing`",
        "level": 3,
        "startLine": 62,
        "endLine": 74,
        "lines": 13,
        "content": "### `designing`\n\nThe active design and specification phase. This is where the bulk of artifact work happens.\n\n- **What it means:** You are writing the proposal, specs, verify files, design, and tasks.\n- **What you do:** Create and refine the five standard artifacts (proposal, specs, verify, design, tasks). Run `specd changes status <name>` to check artifact progress.\n- **Transition out:** `specd changes transition <name> ready` once all required artifacts are complete and validated.\n- **What can block it:** The `ready` step requires all artifacts listed in the schema's `requires` field (by default: proposal, specs, verify, design, tasks) to have `complete` status. The CLI reports which artifacts are still missing or in progress.\n\nThe `designing → designing` self-transition is valid — it records a checkpoint without changing state. This happens automatically when redesign invalidates an already-`designing` change.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "`ready`",
        "level": 3,
        "startLine": 75,
        "endLine": 85,
        "lines": 11,
        "content": "### `ready`\n\nAll design artifacts are complete. The change is ready to hand off to implementation.\n\n- **What it means:** Every required artifact has been written and validated. The change has been reviewed and is cleared for implementation.\n- **What you do:** If the `approvals.spec` gate is enabled in `specd.yaml`, transition to `pending-spec-approval` to request approval. Otherwise, transition directly to `implementing`.\n- **Transition out:** `implementing` (no gate), or `pending-spec-approval` (with gate), or back to `designing` (redesign).\n- **What can block it:** The spec approval gate (if enabled) must be passed before proceeding to `implementing`.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "`pending-spec-approval`",
        "level": 3,
        "startLine": 86,
        "endLine": 98,
        "lines": 13,
        "content": "### `pending-spec-approval`\n\nThe spec approval gate is active and a human reviewer has not yet approved the specs.\n\n- **What it means:** specd has been asked to require human sign-off on the spec artifacts before implementation begins. The change is waiting for that approval.\n- **What you do:** A reviewer runs `specd changes approve-spec <name> --reason \"...\"` after inspecting the spec artifacts.\n- **Transition out:** `spec-approved` after approval, or back to `designing` (redesign).\n- **What can block it:** Approval must be recorded by a human. The gate cannot be bypassed.\n\nThis state only appears when `approvals.spec` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n"
      },
      {
        "index": 9,
        "heading": "`spec-approved`",
        "level": 3,
        "startLine": 99,
        "endLine": 109,
        "lines": 11,
        "content": "### `spec-approved`\n\nA human has reviewed and approved the spec artifacts.\n\n- **What it means:** The specs have been signed off. Implementation may now proceed.\n- **What you do:** Transition to `implementing`.\n- **Transition out:** `implementing`, or back to `designing` (redesign).\n- **What can block it:** If specs were modified after approval, the change is automatically invalidated and returns to `designing` — the approval is cleared.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "`implementing`",
        "level": 3,
        "startLine": 110,
        "endLine": 120,
        "lines": 11,
        "content": "### `implementing`\n\nActive development is in progress.\n\n- **What it means:** The implementation tasks are being worked through.\n- **What you do:** Work through the task list in `tasks.md`, checking off items as you go (`- [x]`). Run `specd changes status <name>` to see task progress.\n- **Transition out:** `verifying` once all tasks are complete, or back to `designing` (redesign). The transition to `verifying` is blocked if any tasks remain incomplete.\n- **What can block it:** The `taskCompletionCheck` defined in the schema. By default, any unchecked `- [ ]` line in `tasks.md` prevents advancing to `verifying`. The CLI reports \"N/M tasks complete\" when this gate is active.\n\n---\n"
      },
      {
        "index": 11,
        "heading": "`verifying`",
        "level": 3,
        "startLine": 121,
        "endLine": 131,
        "lines": 11,
        "content": "### `verifying`\n\nImplementation is complete. Verification scenarios are being run.\n\n- **What it means:** All tasks are done. You are now confirming the implementation satisfies every scenario in `verify.md`.\n- **What you do:** Run through every scenario in `verify.md`. Confirm each one passes by inspecting the code and running the relevant tests.\n- **Transition out:** `done` when all scenarios pass, back to `implementing` if a scenario fails (the schema may clear artifact validations on this backward transition), or back to `designing` (redesign).\n- **What can block it:** Nothing prevents the transition — it is up to you to judge readiness.\n\n---\n"
      },
      {
        "index": 12,
        "heading": "`done`",
        "level": 3,
        "startLine": 132,
        "endLine": 142,
        "lines": 11,
        "content": "### `done`\n\nImplementation and verification are complete.\n\n- **What it means:** The change is finished from a development perspective. It can now be archived.\n- **What you do:** If the `approvals.signoff` gate is enabled, transition to `pending-signoff`. Otherwise, transition to `archivable`.\n- **Transition out:** `archivable` (no gate), `pending-signoff` (with gate), or back to `designing` (redesign).\n- **What can block it:** The signoff gate (if enabled) must be passed before proceeding to `archivable`.\n\n---\n"
      },
      {
        "index": 13,
        "heading": "`pending-signoff`",
        "level": 3,
        "startLine": 143,
        "endLine": 155,
        "lines": 13,
        "content": "### `pending-signoff`\n\nThe signoff gate is active and a human reviewer has not yet signed off.\n\n- **What it means:** The change requires final human sign-off before it can be archived. This gate is typically used for compliance or stakeholder review.\n- **What you do:** A reviewer runs `specd changes signoff <name> --reason \"...\"` after inspecting the completed work.\n- **Transition out:** `signed-off` after sign-off, or back to `designing` (redesign).\n- **What can block it:** Sign-off must be recorded by a human.\n\nThis state only appears when `approvals.signoff` is enabled in `specd.yaml`. See [Approval gates](#approval-gates).\n\n---\n"
      },
      {
        "index": 14,
        "heading": "`signed-off`",
        "level": 3,
        "startLine": 156,
        "endLine": 166,
        "lines": 11,
        "content": "### `signed-off`\n\nA human has reviewed and signed off the completed change.\n\n- **What it means:** The change has received its final human approval and may proceed to archiving.\n- **What you do:** Transition to `archivable`.\n- **Transition out:** `archivable`, or back to `designing` (redesign).\n- **What can block it:** Nothing.\n\n---\n"
      },
      {
        "index": 15,
        "heading": "`archivable`",
        "level": 3,
        "startLine": 167,
        "endLine": 177,
        "lines": 11,
        "content": "### `archivable`\n\nThe change is ready to be archived.\n\n- **What it means:** All gates have been passed. The change can run the archive operation.\n- **What you do:** Run `specd changes archive <name>` to perform the archive.\n- **Transition out:** `archiving` (only after archive preflight succeeds and canonical publication is about to start — see [Archiving](#archiving)), or back to `designing` (redesign).\n- **What can block it:** Archive pre-hooks, overlap/read-only guards, or preflight failures keep the change here (the archive command does not transition to `archiving` until commit preparation has passed).\n\n---\n"
      },
      {
        "index": 16,
        "heading": "`archiving`",
        "level": 3,
        "startLine": 178,
        "endLine": 187,
        "lines": 10,
        "content": "### `archiving`\n\nThe change is in the archive **commit phase** (canonical publication, archive move, and post-archive work), or it completed successfully and was moved to the archive directory.\n\n- **What it means:** While `specd changes archive` is running, the change transitions to `archiving` immediately before the first spec publication, after guards, pre-archive hooks, orphan detection, and batch snapshot. If publication or the archive move fails and batch restore succeeds, the change returns to `archivable` so you can fix the problem and retry. If restore is incomplete, the change stays in `archiving` until you recover manually.\n- **What you do:** If archive failed and the change is back in `archivable`, review restored specs and run `specd changes archive <name>` again. If the change is stuck in `archiving` after a partial restore, use `specd changes transition <name> designing` to revise artifacts, or `specd changes transition <name> archivable` after you have manually restored canonical specs (lifecycle rollback only — no archive hooks).\n- **Transition out:** `archivable` (recovery after a failed commit when canonical storage was restored), `designing` (manual escape to revise specs/deltas), or none after a **successful** archive (the change directory is moved to the archive and is no longer an active change).\n\n---\n"
      },
      {
        "index": 17,
        "heading": "Full transition table",
        "level": 2,
        "startLine": 188,
        "endLine": 206,
        "lines": 19,
        "content": "## Full transition table\n\n| From                    | To                                                                                                          |\n| ----------------------- | ----------------------------------------------------------------------------------------------------------- |\n| `drafting`              | `designing`                                                                                                 |\n| `designing`             | `ready`, `designing`                                                                                        |\n| `ready`                 | `implementing`, `pending-spec-approval`, `designing`                                                        |\n| `pending-spec-approval` | `spec-approved`, `designing`                                                                                |\n| `spec-approved`         | `implementing`, `designing`                                                                                 |\n| `implementing`          | `verifying`, `designing`                                                                                    |\n| `verifying`             | `implementing`, `done`, `designing`                                                                         |\n| `done`                  | `archivable`, `pending-signoff`, `designing`                                                                |\n| `pending-signoff`       | `signed-off`, `designing`                                                                                   |\n| `signed-off`            | `archivable`, `designing`                                                                                   |\n| `archivable`            | `archiving`, `designing`                                                                                    |\n| `archiving`             | `archivable`, `designing` _(active change only; successful archive removes the change from the active set)_ |\n\n---\n"
      },
      {
        "index": 18,
        "heading": "Identity and audit trails",
        "level": 2,
        "startLine": 207,
        "endLine": 230,
        "lines": 24,
        "content": "## Identity and audit trails\n\nspecd records the identity of the person or system performing every significant operation — creating a change, transitioning states, approving specs, and signing off on completed work. These records create a tamper-evident audit trail persisted in the change manifest and history.\n\n### How identity is resolved\n\nSpecD uses a pluggable identity resolution system. By default, it auto-detects the active Version Control System (Git, Mercurial, or Subversion) and retrieves the user's name and email from its configuration.\n\nIn specific environments, you can force a provider or use custom identity plugins. See the [Configuration Guide](configuration.md#identity-resolution) for details.\n\n### Privacy in public repositories\n\nFor projects hosted in public repositories, you can enable **privacy modes** to obfuscate real names and emails before they are persisted in the repository.\n\nSupported modes include:\n\n- **Hashing**: deterministic HMAC-SHA256 obfuscation of emails.\n- **Masking**: partial obfuscation (e.g., `j***z@e***.com`).\n- **Anonymization**: replacement with generic placeholders.\n\nPrivacy settings also automatically filter out sensitive metadata. For more details on configuring privacy, see the [Privacy section](configuration.md#privacy) in the Configuration Guide.\n\n---\n"
      },
      {
        "index": 19,
        "heading": "How identity is resolved",
        "level": 3,
        "startLine": 211,
        "endLine": 216,
        "lines": 6,
        "content": "### How identity is resolved\n\nSpecD uses a pluggable identity resolution system. By default, it auto-detects the active Version Control System (Git, Mercurial, or Subversion) and retrieves the user's name and email from its configuration.\n\nIn specific environments, you can force a provider or use custom identity plugins. See the [Configuration Guide](configuration.md#identity-resolution) for details.\n"
      },
      {
        "index": 20,
        "heading": "Privacy in public repositories",
        "level": 3,
        "startLine": 217,
        "endLine": 230,
        "lines": 14,
        "content": "### Privacy in public repositories\n\nFor projects hosted in public repositories, you can enable **privacy modes** to obfuscate real names and emails before they are persisted in the repository.\n\nSupported modes include:\n\n- **Hashing**: deterministic HMAC-SHA256 obfuscation of emails.\n- **Masking**: partial obfuscation (e.g., `j***z@e***.com`).\n- **Anonymization**: replacement with generic placeholders.\n\nPrivacy settings also automatically filter out sensitive metadata. For more details on configuring privacy, see the [Privacy section](configuration.md#privacy) in the Configuration Guide.\n\n---\n"
      },
      {
        "index": 21,
        "heading": "Redesign: going back to designing",
        "level": 2,
        "startLine": 231,
        "endLine": 253,
        "lines": 23,
        "content": "## Redesign: going back to designing\n\nAlmost every state can transition back to `designing`. This is the **redesign path** — it is how you handle requirement changes, new information, or mistakes discovered mid-lifecycle.\n\nTo redesign from any active state:\n\n```bash\nspecd changes transition <name> designing\n```\n\nWhen a redesign transition occurs:\n\n1. An `invalidated` event is appended to the change history with `cause: 'redesign'`.\n2. A `transitioned` event rolling back to `designing` is appended.\n3. Any active spec approval (`activeSpecApproval`) and signoff (`activeSignoff`) are cleared — they are no longer valid after specs have been reworked.\n4. Artifact validation hashes are reset, requiring re-validation before the change can advance to `ready` again.\n\nRedesign also happens automatically when spec IDs are updated (`cause: 'spec-change'`) or when artifact content changes after an approval was recorded (`cause: 'artifact-change'`).\n\nRedesign from `archiving` is allowed via `specd changes transition <name> designing` (artifacts are downgraded for review). Use this when batch restore failed partially or you need to change deltas before retrying archive. After a **successful** archive, the change is no longer active — create a new change instead.\n\n---\n"
      },
      {
        "index": 22,
        "heading": "Approval gates",
        "level": 2,
        "startLine": 254,
        "endLine": 315,
        "lines": 62,
        "content": "## Approval gates\n\nspecd supports two optional human-approval gates. Both are disabled by default.\n\n### Spec approval gate\n\nBlocks the transition from `ready` to `implementing` until a human approves the spec artifacts.\n\n**Path when enabled:**\n\n```\nready → pending-spec-approval → spec-approved → implementing\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  spec: true\n```\n\n**Performing the approval:**\n\n```bash\nspecd changes approve-spec add-auth --reason \"Specs reviewed and approved — all requirements are clear\"\n```\n\nThe approver reviews the spec and verify artifacts in the change directory. The approval records a hash of those artifacts at the moment of approval. If any of those artifacts are subsequently modified, an `invalidated` event is appended automatically and the change returns to `designing`, clearing the approval.\n\n### Signoff gate\n\nBlocks the transition from `done` to `archivable` until a human signs off the completed work.\n\n**Path when enabled:**\n\n```\ndone → pending-signoff → signed-off → archivable\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  signoff: true\n```\n\n**Performing the signoff:**\n\n```bash\nspecd changes signoff add-auth --reason \"Implementation reviewed — all scenarios verified\"\n```\n\nBoth gates can be enabled simultaneously:\n\n```yaml\napprovals:\n  spec: true\n  signoff: true\n```\n\n---\n"
      },
      {
        "index": 23,
        "heading": "Spec approval gate",
        "level": 3,
        "startLine": 258,
        "endLine": 282,
        "lines": 25,
        "content": "### Spec approval gate\n\nBlocks the transition from `ready` to `implementing` until a human approves the spec artifacts.\n\n**Path when enabled:**\n\n```\nready → pending-spec-approval → spec-approved → implementing\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  spec: true\n```\n\n**Performing the approval:**\n\n```bash\nspecd changes approve-spec add-auth --reason \"Specs reviewed and approved — all requirements are clear\"\n```\n\nThe approver reviews the spec and verify artifacts in the change directory. The approval records a hash of those artifacts at the moment of approval. If any of those artifacts are subsequently modified, an `invalidated` event is appended automatically and the change returns to `designing`, clearing the approval.\n"
      },
      {
        "index": 24,
        "heading": "Signoff gate",
        "level": 3,
        "startLine": 283,
        "endLine": 315,
        "lines": 33,
        "content": "### Signoff gate\n\nBlocks the transition from `done` to `archivable` until a human signs off the completed work.\n\n**Path when enabled:**\n\n```\ndone → pending-signoff → signed-off → archivable\n```\n\n**Enabling in `specd.yaml`:**\n\n```yaml\napprovals:\n  signoff: true\n```\n\n**Performing the signoff:**\n\n```bash\nspecd changes signoff add-auth --reason \"Implementation reviewed — all scenarios verified\"\n```\n\nBoth gates can be enabled simultaneously:\n\n```yaml\napprovals:\n  spec: true\n  signoff: true\n```\n\n---\n"
      },
      {
        "index": 25,
        "heading": "Task completion gating",
        "level": 2,
        "startLine": 316,
        "endLine": 336,
        "lines": 21,
        "content": "## Task completion gating\n\nThe transition from `implementing` to `verifying` is gated on task completion. This is enforced by the `taskCompletionCheck` defined in the schema.\n\nThe default schema (`@specd/schema-std`) checks for unchecked checkbox lines in `tasks.md`:\n\n- **Incomplete pattern:** `^\\s*-\\s+\\[ \\]` — any `- [ ]` line blocks the transition.\n- **Complete pattern:** `^\\s*-\\s+\\[x\\]` — `- [x]` lines count toward completion.\n\nWhen you run `specd changes transition <name> verifying` and tasks remain incomplete, the CLI reports the current progress:\n\n```\n3/5 tasks complete — transition to verifying is blocked\n```\n\nMark tasks complete by changing `- [ ]` to `- [x]` in `tasks.md`. Once all tasks are checked, the transition is allowed.\n\nOne subtlety: `tasks.md` is also validated by artifact hashing. The schema normalises checkboxes before hashing — `- [x]` lines are converted back to `- [ ]` during hash computation so that checking off tasks does not trigger an approval invalidation. Checking off tasks is not a spec change; it is progress tracking.\n\n---\n"
      },
      {
        "index": 26,
        "heading": "Hooks",
        "level": 2,
        "startLine": 337,
        "endLine": 402,
        "lines": 66,
        "content": "## Hooks\n\nHooks let you attach automated actions or AI guidance to lifecycle transitions. They are defined in the schema's `workflow` section and optionally extended in `specd.yaml` via `schemaOverrides`.\n\n### Pre and post hooks\n\n- **Pre hooks** run before the change enters a step. If a pre hook fails, the transition is blocked.\n- **Post hooks** run after the change enters a step. If a post hook fails, the transition is not rolled back.\n\nWhen shell hooks are long-running, SpecD keeps them observable while they are\nin flight. In human-facing `text` output, completed hooks remain visible,\nactive hooks show recent output plus a running indicator, and quiet hooks emit\nliveness updates. In `json` and `toon`, in-flight hook progress is emitted on\n`stderr` as structured events while the final command result remains on\n`stdout`.\n\n### Hook types\n\n| Type           | Description                                                                              |\n| -------------- | ---------------------------------------------------------------------------------------- |\n| `run:`         | A shell command executed in the project root. Non-zero exit code blocks a pre hook.      |\n| `instruction:` | An AI context block injected into the agent context for the step. Not directly runnable. |\n| `external:`    | An explicit external hook dispatched to a registered external hook runner by `type`.     |\n\nAn `instruction:` hook provides guidance to an AI agent about what to do in that step. It is informational — it does not execute code. A `run:` hook executes a shell command through the internal `HookRunner`. An `external:` hook is routed to a separate `ExternalHookRunner` selected by `external.type`.\n\n```yaml\nworkflow:\n  - step: implementing\n    hooks:\n      pre:\n        - id: run-lint\n          run: pnpm lint\n        - id: docker-test\n          external:\n            type: docker\n            config:\n              image: node:20\n              command: pnpm test\n      post:\n        - id: notify\n          run: echo \"Implementation started for {{change.name}}\"\n```\n\nExternal hooks use the same workflow phase semantics as shell hooks:\n\n- a failing `pre` external hook blocks the transition\n- a failing `post` external hook does not roll the state back\n- an unknown `external.type` is a hard error, not a silent skip\n\n### Template variables\n\nHook `run:` commands support template variable substitution:\n\n| Variable           | Value                                                        |\n| ------------------ | ------------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name (e.g. `add-auth`)                     |\n| `{{change.path}}`  | Absolute path to the change directory                        |\n| `{{project.root}}` | Absolute path to the project root (where `specd.yaml` lives) |\n\n### Hook execution order\n\nWhen a step is entered, schema-level hooks fire first, then any project-level hooks added via `schemaOverrides` in `specd.yaml`. Within each source, hooks run in the order they are declared.\n\n---\n"
      },
      {
        "index": 27,
        "heading": "Pre and post hooks",
        "level": 3,
        "startLine": 341,
        "endLine": 352,
        "lines": 12,
        "content": "### Pre and post hooks\n\n- **Pre hooks** run before the change enters a step. If a pre hook fails, the transition is blocked.\n- **Post hooks** run after the change enters a step. If a post hook fails, the transition is not rolled back.\n\nWhen shell hooks are long-running, SpecD keeps them observable while they are\nin flight. In human-facing `text` output, completed hooks remain visible,\nactive hooks show recent output plus a running indicator, and quiet hooks emit\nliveness updates. In `json` and `toon`, in-flight hook progress is emitted on\n`stderr` as structured events while the final command result remains on\n`stdout`.\n"
      },
      {
        "index": 28,
        "heading": "Hook types",
        "level": 3,
        "startLine": 353,
        "endLine": 386,
        "lines": 34,
        "content": "### Hook types\n\n| Type           | Description                                                                              |\n| -------------- | ---------------------------------------------------------------------------------------- |\n| `run:`         | A shell command executed in the project root. Non-zero exit code blocks a pre hook.      |\n| `instruction:` | An AI context block injected into the agent context for the step. Not directly runnable. |\n| `external:`    | An explicit external hook dispatched to a registered external hook runner by `type`.     |\n\nAn `instruction:` hook provides guidance to an AI agent about what to do in that step. It is informational — it does not execute code. A `run:` hook executes a shell command through the internal `HookRunner`. An `external:` hook is routed to a separate `ExternalHookRunner` selected by `external.type`.\n\n```yaml\nworkflow:\n  - step: implementing\n    hooks:\n      pre:\n        - id: run-lint\n          run: pnpm lint\n        - id: docker-test\n          external:\n            type: docker\n            config:\n              image: node:20\n              command: pnpm test\n      post:\n        - id: notify\n          run: echo \"Implementation started for {{change.name}}\"\n```\n\nExternal hooks use the same workflow phase semantics as shell hooks:\n\n- a failing `pre` external hook blocks the transition\n- a failing `post` external hook does not roll the state back\n- an unknown `external.type` is a hard error, not a silent skip\n"
      },
      {
        "index": 29,
        "heading": "Template variables",
        "level": 3,
        "startLine": 387,
        "endLine": 396,
        "lines": 10,
        "content": "### Template variables\n\nHook `run:` commands support template variable substitution:\n\n| Variable           | Value                                                        |\n| ------------------ | ------------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name (e.g. `add-auth`)                     |\n| `{{change.path}}`  | Absolute path to the change directory                        |\n| `{{project.root}}` | Absolute path to the project root (where `specd.yaml` lives) |\n"
      },
      {
        "index": 30,
        "heading": "Hook execution order",
        "level": 3,
        "startLine": 397,
        "endLine": 402,
        "lines": 6,
        "content": "### Hook execution order\n\nWhen a step is entered, schema-level hooks fire first, then any project-level hooks added via `schemaOverrides` in `specd.yaml`. Within each source, hooks run in the order they are declared.\n\n---\n"
      },
      {
        "index": 31,
        "heading": "Drafting and discarding",
        "level": 2,
        "startLine": 403,
        "endLine": 445,
        "lines": 43,
        "content": "## Drafting and discarding\n\nDrafting and discarding are sidebars to the main lifecycle — they do not affect the change's lifecycle state.\n\n### Drafting\n\nDrafting shelves a change temporarily without abandoning it.\n\n```bash\nspecd drafts move add-auth         # move to drafts/\nspecd drafts restore add-auth      # move back to changes/\n```\n\nA drafted change retains its current lifecycle state. When restored, it continues from where it left off. Drafting is useful when you need to context-switch to another change and want to keep the current work safely out of the way.\n\n**Guard:** When a change has previously reached the `implementing` state, drafting is blocked by default — the code and specs may be out of sync, and shelving without archival risks leaving partial implementation behind. If you are certain you want to draft anyway, use `--force`:\n\n```bash\nspecd changes draft add-auth --force\n```\n\n### Discarding\n\nDiscarding permanently abandons a change. A reason is required.\n\n```bash\nspecd discard add-auth --reason \"Approach superseded by add-oauth-flow\"\n```\n\nA discarded change is moved to the discarded directory and is never archived. This is permanent — there is no restore from discard. You can optionally name the change or changes that supersede it:\n\n```bash\nspecd discard add-auth --reason \"Superseded\" --superseded-by add-oauth-flow\n```\n\n**Guard:** When a change has previously reached the `implementing` state, discarding is blocked by default — code that has been implemented should ideally be archived, not silently abandoned. If you are certain you want to discard anyway, use `--force`:\n\n```bash\nspecd changes discard add-auth --reason \"No longer needed\" --force\n```\n\n---\n"
      },
      {
        "index": 32,
        "heading": "Drafting",
        "level": 3,
        "startLine": 407,
        "endLine": 423,
        "lines": 17,
        "content": "### Drafting\n\nDrafting shelves a change temporarily without abandoning it.\n\n```bash\nspecd drafts move add-auth         # move to drafts/\nspecd drafts restore add-auth      # move back to changes/\n```\n\nA drafted change retains its current lifecycle state. When restored, it continues from where it left off. Drafting is useful when you need to context-switch to another change and want to keep the current work safely out of the way.\n\n**Guard:** When a change has previously reached the `implementing` state, drafting is blocked by default — the code and specs may be out of sync, and shelving without archival risks leaving partial implementation behind. If you are certain you want to draft anyway, use `--force`:\n\n```bash\nspecd changes draft add-auth --force\n```\n"
      },
      {
        "index": 33,
        "heading": "Discarding",
        "level": 3,
        "startLine": 424,
        "endLine": 445,
        "lines": 22,
        "content": "### Discarding\n\nDiscarding permanently abandons a change. A reason is required.\n\n```bash\nspecd discard add-auth --reason \"Approach superseded by add-oauth-flow\"\n```\n\nA discarded change is moved to the discarded directory and is never archived. This is permanent — there is no restore from discard. You can optionally name the change or changes that supersede it:\n\n```bash\nspecd discard add-auth --reason \"Superseded\" --superseded-by add-oauth-flow\n```\n\n**Guard:** When a change has previously reached the `implementing` state, discarding is blocked by default — code that has been implemented should ideally be archived, not silently abandoned. If you are certain you want to discard anyway, use `--force`:\n\n```bash\nspecd changes discard add-auth --reason \"No longer needed\" --force\n```\n\n---\n"
      },
      {
        "index": 34,
        "heading": "Archiving",
        "level": 2,
        "startLine": 446,
        "endLine": 502,
        "lines": 57,
        "content": "## Archiving\n\nArchiving is the final operation in the lifecycle. The change must be in `archivable`. When you run:\n\n```bash\nspecd changes archive <name>\n```\n\n### Phases and lifecycle state\n\nThe change stays in **`archivable`** through early archive work (schema guard, overlap/read-only checks, pre-archive hooks, and full-batch preflight). It moves to **`archiving`** only immediately before the first canonical spec publication, after orphan backup detection and a per-spec batch snapshot under `.specd-archive-backup/` in each affected spec directory.\n\n| Phase                                | Lifecycle state | On failure                                                                                |\n| ------------------------------------ | --------------- | ----------------------------------------------------------------------------------------- |\n| Guards, pre-archive hooks, preflight | `archivable`    | Stays `archivable`; no backup dirs                                                        |\n| Orphan detection + batch snapshot    | `archivable`    | Stays `archivable` (matching orphan: auto-restore + abort with guidance)                  |\n| Canonical publication + archive move | `archiving`     | Batch restore; `→ archivable` if restore succeeds, stay `archiving` if restore is partial |\n| Metadata + post-archive hooks        | `archiving`     | Archive already committed; failures are reported but do not roll back specs               |\n\nPre-archive hooks use the workflow step name `archiving`; they do **not** require the change to already be in lifecycle state `archiving`.\n\n### Commit steps (after transition to `archiving`)\n\n1. **Canonical publication:** For each spec in the batch, delta artifacts are merged into the base spec in the repository (or full spec files are copied). Publication is staged per spec; created files are recorded for restore.\n2. **Archive move:** The change directory is moved to the archive. An `ArchivedChange` record is created with the change name, timestamp, and spec IDs.\n3. **Backup cleanup:** `.specd-archive-backup/` directories are removed after a successful commit.\n4. **Metadata generation:** Persisted `metadata.json` is written **after** the archive move (in-memory preflight checks still run earlier).\n5. **Post-archive hooks:** Optional hooks configured for the `archiving` workflow step.\n\n### Batch restore and retry\n\nIf publication fails partway through a multi-spec batch, specd restores already-published specs from the batch backup (reverse publication order) and rolls the change back to **`archivable`** when restore completes. Retry archive merges deltas against the restored canonical base — not against partially merged content.\n\nIf restore cannot complete for every spec, the change remains in **`archiving`**. `specd changes status` recommends transitioning to **`designing`** for manual recovery.\n\n### Orphan backups\n\nIf `.specd-archive-backup/` already exists when archive starts:\n\n- **Same change name** in the manifest: canonical specs are auto-restored, the backup is removed, and the current archive attempt aborts (review and retry).\n- **Different change name**: archive aborts with a repair message; live files are not overwritten.\n\n### Recovery transitions from `archiving`\n\n| Transition               | Purpose                                                                                                             |\n| ------------------------ | ------------------------------------------------------------------------------------------------------------------- |\n| `archiving → archivable` | Lifecycle rollback after you fixed canonical specs manually; does not re-run archive hooks                          |\n| `archiving → designing`  | Revise change artifacts (downgrades files to `pending-review`); use after partial restore or when specs need rework |\n\nTransitions to `implementing`, `verifying`, or `done` from `archiving` are not valid.\n\n### After a successful archive\n\nSpecs in the repository reflect the merged requirements. The change history is preserved under `.specd/archive/`. To rework merged specs, open a **new** change — the archived record is not transitioned back.\n\n---\n"
      },
      {
        "index": 35,
        "heading": "Phases and lifecycle state",
        "level": 3,
        "startLine": 454,
        "endLine": 466,
        "lines": 13,
        "content": "### Phases and lifecycle state\n\nThe change stays in **`archivable`** through early archive work (schema guard, overlap/read-only checks, pre-archive hooks, and full-batch preflight). It moves to **`archiving`** only immediately before the first canonical spec publication, after orphan backup detection and a per-spec batch snapshot under `.specd-archive-backup/` in each affected spec directory.\n\n| Phase                                | Lifecycle state | On failure                                                                                |\n| ------------------------------------ | --------------- | ----------------------------------------------------------------------------------------- |\n| Guards, pre-archive hooks, preflight | `archivable`    | Stays `archivable`; no backup dirs                                                        |\n| Orphan detection + batch snapshot    | `archivable`    | Stays `archivable` (matching orphan: auto-restore + abort with guidance)                  |\n| Canonical publication + archive move | `archiving`     | Batch restore; `→ archivable` if restore succeeds, stay `archiving` if restore is partial |\n| Metadata + post-archive hooks        | `archiving`     | Archive already committed; failures are reported but do not roll back specs               |\n\nPre-archive hooks use the workflow step name `archiving`; they do **not** require the change to already be in lifecycle state `archiving`.\n"
      },
      {
        "index": 36,
        "heading": "Commit steps (after transition to `archiving`)",
        "level": 3,
        "startLine": 467,
        "endLine": 474,
        "lines": 8,
        "content": "### Commit steps (after transition to `archiving`)\n\n1. **Canonical publication:** For each spec in the batch, delta artifacts are merged into the base spec in the repository (or full spec files are copied). Publication is staged per spec; created files are recorded for restore.\n2. **Archive move:** The change directory is moved to the archive. An `ArchivedChange` record is created with the change name, timestamp, and spec IDs.\n3. **Backup cleanup:** `.specd-archive-backup/` directories are removed after a successful commit.\n4. **Metadata generation:** Persisted `metadata.json` is written **after** the archive move (in-memory preflight checks still run earlier).\n5. **Post-archive hooks:** Optional hooks configured for the `archiving` workflow step.\n"
      },
      {
        "index": 37,
        "heading": "Batch restore and retry",
        "level": 3,
        "startLine": 475,
        "endLine": 480,
        "lines": 6,
        "content": "### Batch restore and retry\n\nIf publication fails partway through a multi-spec batch, specd restores already-published specs from the batch backup (reverse publication order) and rolls the change back to **`archivable`** when restore completes. Retry archive merges deltas against the restored canonical base — not against partially merged content.\n\nIf restore cannot complete for every spec, the change remains in **`archiving`**. `specd changes status` recommends transitioning to **`designing`** for manual recovery.\n"
      },
      {
        "index": 38,
        "heading": "Orphan backups",
        "level": 3,
        "startLine": 481,
        "endLine": 487,
        "lines": 7,
        "content": "### Orphan backups\n\nIf `.specd-archive-backup/` already exists when archive starts:\n\n- **Same change name** in the manifest: canonical specs are auto-restored, the backup is removed, and the current archive attempt aborts (review and retry).\n- **Different change name**: archive aborts with a repair message; live files are not overwritten.\n"
      },
      {
        "index": 39,
        "heading": "Recovery transitions from `archiving`",
        "level": 3,
        "startLine": 488,
        "endLine": 496,
        "lines": 9,
        "content": "### Recovery transitions from `archiving`\n\n| Transition               | Purpose                                                                                                             |\n| ------------------------ | ------------------------------------------------------------------------------------------------------------------- |\n| `archiving → archivable` | Lifecycle rollback after you fixed canonical specs manually; does not re-run archive hooks                          |\n| `archiving → designing`  | Revise change artifacts (downgrades files to `pending-review`); use after partial restore or when specs need rework |\n\nTransitions to `implementing`, `verifying`, or `done` from `archiving` are not valid.\n"
      },
      {
        "index": 40,
        "heading": "After a successful archive",
        "level": 3,
        "startLine": 497,
        "endLine": 502,
        "lines": 6,
        "content": "### After a successful archive\n\nSpecs in the repository reflect the merged requirements. The change history is preserved under `.specd/archive/`. To rework merged specs, open a **new** change — the archived record is not transitioned back.\n\n---\n"
      },
      {
        "index": 41,
        "heading": "Practical example: a full lifecycle with `/specd`",
        "level": 2,
        "startLine": 503,
        "endLine": 666,
        "lines": 164,
        "content": "## Practical example: a full lifecycle with `/specd`\n\nHere is a complete lifecycle walkthrough for a change that adds an authentication feature, showing what the agent does at each step. In practice, you drive this through your coding assistant using the `/specd` skill — the agent handles transitions, artifact creation, and validation automatically.\n\n### 1. Starting — `/specd`\n\nYou open your coding assistant and type `/specd`. The skill shows the project status:\n\n```\n# specd\n\n**Schema:** @specd/schema-std\n**Workspaces:** default (8 specs)\n**Active changes:** none\n**Drafts:** none\n\nNo active changes. What would you like to work on?\n```\n\nYou describe what you want: \"I want to add login and logout authentication flows.\"\n\nThe skill suggests creating a change and invokes `/specd-new`.\n\n### 2. Creation — `/specd-new`\n\nThe agent has a discovery conversation with you — understanding the problem, discussing the approach, identifying affected specs, and reaching agreements about scope. Once the picture is clear, it creates the change:\n\n```bash\nspecd changes create add-auth --spec default:auth/login --spec default:auth/logout\n```\n\nBefore handing off, the agent writes a `specd-exploration.md` file to the change directory. This captures everything from the conversation — the problem, the agreed approach, design decisions, rejected alternatives, codebase observations, and open questions. It is the agent's working memory, persisted so that the next phase can pick up where you left off even in a different session. See [Exploration context](#exploration-context-specd-explorationmd) for details.\n\nState: `drafting` → the agent suggests running `/specd-design`.\n\n### 3. Designing — `/specd-design`\n\nThe agent loads the change state, the schema, and the compiled context. It also reads `specd-exploration.md` from the change directory to recover the full discovery context — if time has passed since creation, it verifies that referenced files and spec IDs still exist and flags any drift before proceeding.\n\nThis is the longest phase. The agent works through each artifact in dependency order. The artifacts below follow `@specd/schema-std` — other schemas may define a different set or order:\n\n**Step 3a: Proposal** — The agent writes `proposal.md`:\n\n- Explains the motivation (why the project needs authentication)\n- Describes the current behaviour (no auth exists)\n- Outlines the proposed solution at a high level\n- Lists the specs that will be created: `default:auth/login` and `default:auth/logout`\n- Notes the impact on existing code (new middleware, new routes)\n\nOnce written, the agent validates the proposal and marks it complete.\n\n**Step 3b: Specs** — The agent writes `spec.md` for each spec:\n\n- `specs/default/auth/login/spec.md` — Requirements for login: valid credentials return a session token, invalid credentials return a generic error, sessions expire after 24 hours, rate limiting on the endpoint\n- `specs/default/auth/logout/spec.md` — Requirements for logout: session is invalidated, subsequent requests are rejected\n\nEach spec uses SHALL/MUST language and groups requirements under `### Requirement:` headings.\n\n**Step 3c: Verify** — The agent writes `verify.md` for each spec:\n\n- `specs/default/auth/login/verify.md` — WHEN/THEN scenarios for each login requirement: valid credentials, invalid credentials, expired sessions, rate limit exceeded\n- `specs/default/auth/logout/verify.md` — WHEN/THEN scenarios for logout: successful invalidation, already-expired session\n\nEach scenario mirrors a requirement from `spec.md`.\n\n**Step 3d: Design** — The agent writes `design.md`:\n\n- Reads the proposal, specs, and verify files\n- Analyses the existing codebase to identify affected areas\n- Lists new constructs: `AuthService`, `SessionStore`, login/logout route handlers, auth middleware\n- Documents the approach, key decisions, and trade-offs\n- Maps every requirement and scenario to a concrete implementation path\n\n**Step 3e: Tasks** — The agent writes `tasks.md`:\n\n- Derives discrete tasks from `design.md`\n- Each task is a checkbox with indented context: which file, which symbol, what to change\n- Groups tasks logically (e.g., \"1. Session infrastructure\", \"2. Login flow\", \"3. Logout flow\", \"4. Tests\")\n\nOnce all five artifacts are complete and validated, the agent transitions to `ready`, then to `implementing`.\n\n### 4. Implementing — `/specd-implement`\n\nThe agent reads the compiled context for the `implementing` step — which includes the specs, design, and task list — and works through tasks one by one:\n\n```\n## 1. Session infrastructure\n- [x] 1.1 Create SessionStore interface\n      `src/auth/session-store.ts`: New interface — get, set, delete session\n      Approach: In-memory store with TTL from design.md\n      (Req: Sessions expire after 24 hours)\n\n- [x] 1.2 Implement InMemorySessionStore\n      `src/auth/in-memory-session-store.ts`: Implement SessionStore\n      Approach: Map<string, Session> with setTimeout cleanup\n\n## 2. Login flow\n- [x] 2.1 Create login handler\n      `src/auth/login.ts`: POST /login route handler\n      ...\n- [ ] 2.2 Add rate limiting middleware\n      `src/auth/rate-limit.ts`: ...\n```\n\nThe agent checks off each task as it completes the work. Pre/post hooks run at the step boundaries — for example, `pnpm test` runs after implementation.\n\nOnce all checkboxes are `[x]`, the agent transitions to `verifying`.\n\n### 5. Verifying — `/specd-verify`\n\nVerification is not the same as running tests. Tests check that code behaves as programmed — verification checks that the implementation satisfies the _requirements_. These are different questions:\n\n- **Not every project has tests.** Documentation changes, configuration changes, infrastructure work, early prototypes — many changes have no test suite at all. Verification still applies: the agent inspects the implementation against each scenario and confirms it holds.\n- **Tests can pass while requirements are missed.** A test suite exercises the code paths the developer thought to write. Verification scenarios come from the spec — they represent what was _agreed_ the system should do. A passing test suite does not guarantee that every requirement was addressed.\n- **Verification checks more than code.** The agent inspects the implementation holistically: does the code structure match the design? Were all tasks completed? Do the changes satisfy the GIVEN/WHEN/THEN conditions from `verify.md`? It runs tests where they exist, but also reads code, checks behaviour, and confirms edge cases that tests may not cover.\n\nThe agent reads each scenario from `verify.md` and confirms the implementation satisfies it — by inspecting code, running tests where applicable, and verifying GIVEN/WHEN/THEN conditions:\n\n```\n### Requirement: Valid credentials\n#### Scenario: Successful login\n- WHEN a user submits valid credentials\n- THEN they receive an authenticated session token\n→ ✓ Verified: login handler returns JWT on valid credentials\n\n#### Scenario: Invalid credentials\n- WHEN a user submits invalid credentials\n- THEN they receive a generic error with no field-specific information\n→ ✓ Verified: handler returns 401 with generic message\n```\n\nIf a scenario fails, the agent goes back to fix the issue and then returns to verify again.\n\nOnce all scenarios pass, the agent transitions to `done`.\n\n### 6. Archiving — `/specd-archive`\n\nThe agent runs `specd changes archive add-auth`. specd:\n\n1. Syncs `spec.md` and `verify.md` into `specs/default/auth/login/` and `specs/default/auth/logout/`\n2. Applies any delta files to existing specs\n3. Generates metadata for each spec\n4. Moves the change directory to `.specd/archive/`\n\nThe specs are now permanent. The change is a historical record.\n\n### Redesign mid-lifecycle\n\nIf you discover during implementation that a requirement is wrong:\n\n```\n> The rate limiting should be per-IP, not global. Let's go back and fix the spec.\n```\n\nThe agent transitions back to `designing`. This:\n\n- Records an `invalidated` event with `cause: 'redesign'`\n- Clears any active approvals\n- Resets artifact validation hashes\n\nThe agent updates the spec, verify, design, and tasks, then works forward through the lifecycle again.\n\n---\n"
      },
      {
        "index": 42,
        "heading": "1. Starting — `/specd`",
        "level": 3,
        "startLine": 507,
        "endLine": 525,
        "lines": 19,
        "content": "### 1. Starting — `/specd`\n\nYou open your coding assistant and type `/specd`. The skill shows the project status:\n\n```\n# specd\n\n**Schema:** @specd/schema-std\n**Workspaces:** default (8 specs)\n**Active changes:** none\n**Drafts:** none\n\nNo active changes. What would you like to work on?\n```\n\nYou describe what you want: \"I want to add login and logout authentication flows.\"\n\nThe skill suggests creating a change and invokes `/specd-new`.\n"
      },
      {
        "index": 43,
        "heading": "2. Creation — `/specd-new`",
        "level": 3,
        "startLine": 526,
        "endLine": 537,
        "lines": 12,
        "content": "### 2. Creation — `/specd-new`\n\nThe agent has a discovery conversation with you — understanding the problem, discussing the approach, identifying affected specs, and reaching agreements about scope. Once the picture is clear, it creates the change:\n\n```bash\nspecd changes create add-auth --spec default:auth/login --spec default:auth/logout\n```\n\nBefore handing off, the agent writes a `specd-exploration.md` file to the change directory. This captures everything from the conversation — the problem, the agreed approach, design decisions, rejected alternatives, codebase observations, and open questions. It is the agent's working memory, persisted so that the next phase can pick up where you left off even in a different session. See [Exploration context](#exploration-context-specd-explorationmd) for details.\n\nState: `drafting` → the agent suggests running `/specd-design`.\n"
      },
      {
        "index": 44,
        "heading": "3. Designing — `/specd-design`",
        "level": 3,
        "startLine": 538,
        "endLine": 583,
        "lines": 46,
        "content": "### 3. Designing — `/specd-design`\n\nThe agent loads the change state, the schema, and the compiled context. It also reads `specd-exploration.md` from the change directory to recover the full discovery context — if time has passed since creation, it verifies that referenced files and spec IDs still exist and flags any drift before proceeding.\n\nThis is the longest phase. The agent works through each artifact in dependency order. The artifacts below follow `@specd/schema-std` — other schemas may define a different set or order:\n\n**Step 3a: Proposal** — The agent writes `proposal.md`:\n\n- Explains the motivation (why the project needs authentication)\n- Describes the current behaviour (no auth exists)\n- Outlines the proposed solution at a high level\n- Lists the specs that will be created: `default:auth/login` and `default:auth/logout`\n- Notes the impact on existing code (new middleware, new routes)\n\nOnce written, the agent validates the proposal and marks it complete.\n\n**Step 3b: Specs** — The agent writes `spec.md` for each spec:\n\n- `specs/default/auth/login/spec.md` — Requirements for login: valid credentials return a session token, invalid credentials return a generic error, sessions expire after 24 hours, rate limiting on the endpoint\n- `specs/default/auth/logout/spec.md` — Requirements for logout: session is invalidated, subsequent requests are rejected\n\nEach spec uses SHALL/MUST language and groups requirements under `### Requirement:` headings.\n\n**Step 3c: Verify** — The agent writes `verify.md` for each spec:\n\n- `specs/default/auth/login/verify.md` — WHEN/THEN scenarios for each login requirement: valid credentials, invalid credentials, expired sessions, rate limit exceeded\n- `specs/default/auth/logout/verify.md` — WHEN/THEN scenarios for logout: successful invalidation, already-expired session\n\nEach scenario mirrors a requirement from `spec.md`.\n\n**Step 3d: Design** — The agent writes `design.md`:\n\n- Reads the proposal, specs, and verify files\n- Analyses the existing codebase to identify affected areas\n- Lists new constructs: `AuthService`, `SessionStore`, login/logout route handlers, auth middleware\n- Documents the approach, key decisions, and trade-offs\n- Maps every requirement and scenario to a concrete implementation path\n\n**Step 3e: Tasks** — The agent writes `tasks.md`:\n\n- Derives discrete tasks from `design.md`\n- Each task is a checkbox with indented context: which file, which symbol, what to change\n- Groups tasks logically (e.g., \"1. Session infrastructure\", \"2. Login flow\", \"3. Logout flow\", \"4. Tests\")\n\nOnce all five artifacts are complete and validated, the agent transitions to `ready`, then to `implementing`.\n"
      },
      {
        "index": 45,
        "heading": "4. Implementing — `/specd-implement`",
        "level": 3,
        "startLine": 584,
        "endLine": 610,
        "lines": 27,
        "content": "### 4. Implementing — `/specd-implement`\n\nThe agent reads the compiled context for the `implementing` step — which includes the specs, design, and task list — and works through tasks one by one:\n\n```\n## 1. Session infrastructure\n- [x] 1.1 Create SessionStore interface\n      `src/auth/session-store.ts`: New interface — get, set, delete session\n      Approach: In-memory store with TTL from design.md\n      (Req: Sessions expire after 24 hours)\n\n- [x] 1.2 Implement InMemorySessionStore\n      `src/auth/in-memory-session-store.ts`: Implement SessionStore\n      Approach: Map<string, Session> with setTimeout cleanup\n\n## 2. Login flow\n- [x] 2.1 Create login handler\n      `src/auth/login.ts`: POST /login route handler\n      ...\n- [ ] 2.2 Add rate limiting middleware\n      `src/auth/rate-limit.ts`: ...\n```\n\nThe agent checks off each task as it completes the work. Pre/post hooks run at the step boundaries — for example, `pnpm test` runs after implementation.\n\nOnce all checkboxes are `[x]`, the agent transitions to `verifying`.\n"
      },
      {
        "index": 46,
        "heading": "5. Verifying — `/specd-verify`",
        "level": 3,
        "startLine": 611,
        "endLine": 637,
        "lines": 27,
        "content": "### 5. Verifying — `/specd-verify`\n\nVerification is not the same as running tests. Tests check that code behaves as programmed — verification checks that the implementation satisfies the _requirements_. These are different questions:\n\n- **Not every project has tests.** Documentation changes, configuration changes, infrastructure work, early prototypes — many changes have no test suite at all. Verification still applies: the agent inspects the implementation against each scenario and confirms it holds.\n- **Tests can pass while requirements are missed.** A test suite exercises the code paths the developer thought to write. Verification scenarios come from the spec — they represent what was _agreed_ the system should do. A passing test suite does not guarantee that every requirement was addressed.\n- **Verification checks more than code.** The agent inspects the implementation holistically: does the code structure match the design? Were all tasks completed? Do the changes satisfy the GIVEN/WHEN/THEN conditions from `verify.md`? It runs tests where they exist, but also reads code, checks behaviour, and confirms edge cases that tests may not cover.\n\nThe agent reads each scenario from `verify.md` and confirms the implementation satisfies it — by inspecting code, running tests where applicable, and verifying GIVEN/WHEN/THEN conditions:\n\n```\n### Requirement: Valid credentials\n#### Scenario: Successful login\n- WHEN a user submits valid credentials\n- THEN they receive an authenticated session token\n→ ✓ Verified: login handler returns JWT on valid credentials\n\n#### Scenario: Invalid credentials\n- WHEN a user submits invalid credentials\n- THEN they receive a generic error with no field-specific information\n→ ✓ Verified: handler returns 401 with generic message\n```\n\nIf a scenario fails, the agent goes back to fix the issue and then returns to verify again.\n\nOnce all scenarios pass, the agent transitions to `done`.\n"
      },
      {
        "index": 47,
        "heading": "6. Archiving — `/specd-archive`",
        "level": 3,
        "startLine": 638,
        "endLine": 648,
        "lines": 11,
        "content": "### 6. Archiving — `/specd-archive`\n\nThe agent runs `specd changes archive add-auth`. specd:\n\n1. Syncs `spec.md` and `verify.md` into `specs/default/auth/login/` and `specs/default/auth/logout/`\n2. Applies any delta files to existing specs\n3. Generates metadata for each spec\n4. Moves the change directory to `.specd/archive/`\n\nThe specs are now permanent. The change is a historical record.\n"
      },
      {
        "index": 48,
        "heading": "Redesign mid-lifecycle",
        "level": 3,
        "startLine": 649,
        "endLine": 666,
        "lines": 18,
        "content": "### Redesign mid-lifecycle\n\nIf you discover during implementation that a requirement is wrong:\n\n```\n> The rate limiting should be per-IP, not global. Let's go back and fix the spec.\n```\n\nThe agent transitions back to `designing`. This:\n\n- Records an `invalidated` event with `cause: 'redesign'`\n- Clears any active approvals\n- Resets artifact validation hashes\n\nThe agent updates the spec, verify, design, and tasks, then works forward through the lifecycle again.\n\n---\n"
      },
      {
        "index": 49,
        "heading": "Exploration context (`specd-exploration.md`)",
        "level": 2,
        "startLine": 667,
        "endLine": 716,
        "lines": 50,
        "content": "## Exploration context (`specd-exploration.md`)\n\nWhen an AI agent runs `/specd-new`, it has a discovery conversation with the user — understanding the problem, discussing approaches, identifying affected specs, and reaching agreements about scope and direction. All of this context is captured in a file called `specd-exploration.md`, written to the change directory (`<changePath>/specd-exploration.md`) before the agent hands off to `/specd-design`.\n\n### Why it exists\n\nDesign happens in a different session — often hours or days after creation. The agent that writes artifacts has no memory of the discovery conversation. Without a persistent record, it would need to start from scratch: ask the user to re-explain everything, or worse, guess based on the change name and one-line description alone.\n\n`specd-exploration.md` bridges this gap. It is the agent's working memory, persisted to disk so that any future session can pick up exactly where the conversation left off.\n\n### How it differs from `proposal.md`\n\nThey may look similar at first glance, but they serve fundamentally different purposes:\n\n|                              | `specd-exploration.md`                                                                                        | `proposal.md`                                                                     |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |\n| **What it is**               | Agent working memory — raw conversation context                                                               | Formal schema artifact — structured document                                      |\n| **Who it's for**             | The AI agent in future sessions                                                                               | Humans and agents reviewing the change                                            |\n| **When it's written**        | During `/specd-new`, before any artifacts exist                                                               | During `/specd-design`, as the first artifact                                     |\n| **Part of the schema DAG?**  | No — it's outside the artifact pipeline                                                                       | Yes — other artifacts depend on it                                                |\n| **Validated?**               | No — no schema validation applies                                                                             | Yes — must pass schema validations                                                |\n| **Required by all schemas?** | Yes — every schema needs exploration context                                                                  | No — a schema can omit `proposal` entirely                                        |\n| **Content**                  | Everything discussed: rejected ideas, tangential topics, agreements, codebase observations, conversation flow | Curated: motivation, current behaviour, proposed solution, specs affected, impact |\n\nThe key distinction: **`proposal.md` is a schema artifact that a schema may or may not include.** A custom schema could replace `proposal` with a different first artifact, or skip it entirely and go straight to specs. But even without a proposal, the agent still needs to know what the user wants to build and why. That's what `specd-exploration.md` provides — it is schema-independent context that ensures continuity across sessions regardless of which artifacts the schema defines.\n\n### What it contains\n\nThe file captures everything from the discovery conversation:\n\n- **Problem statement** — what the user wants to achieve and why\n- **Approach / solution outline** — the agreed strategy\n- **Affected areas** — packages, modules, files, specs discussed\n- **Spec IDs** — attached to the change and mentioned as relevant\n- **Design decisions and agreements** — anything confirmed, rejected, or constrained\n- **Steps or plan** — if specific phases or ordering were agreed\n- **Rejected alternatives** — with reasons, so the agent doesn't re-propose them\n- **Open questions** — unresolved items for design to clarify\n- **Codebase observations** — file paths, patterns, current behaviour\n- **Tangential topics** — related issues, future ideas, things mentioned in passing\n- **Conversation flow** — how the discussion evolved\n\nEach item includes concrete anchors (file paths, spec IDs, function names) so the reader can verify whether they still hold, and reasons behind decisions so edge cases can be judged even if details have changed.\n\n### Staleness\n\nTime passes between exploration and design. Code changes, specs get renamed, decisions become outdated. The file includes a `Generated: YYYY-MM-DD` timestamp, and `/specd-design` performs a staleness check before trusting its content — verifying that referenced paths and spec IDs still exist, and flagging any significant drift to the user.\n\n---\n"
      },
      {
        "index": 50,
        "heading": "Why it exists",
        "level": 3,
        "startLine": 671,
        "endLine": 676,
        "lines": 6,
        "content": "### Why it exists\n\nDesign happens in a different session — often hours or days after creation. The agent that writes artifacts has no memory of the discovery conversation. Without a persistent record, it would need to start from scratch: ask the user to re-explain everything, or worse, guess based on the change name and one-line description alone.\n\n`specd-exploration.md` bridges this gap. It is the agent's working memory, persisted to disk so that any future session can pick up exactly where the conversation left off.\n"
      },
      {
        "index": 51,
        "heading": "How it differs from `proposal.md`",
        "level": 3,
        "startLine": 677,
        "endLine": 692,
        "lines": 16,
        "content": "### How it differs from `proposal.md`\n\nThey may look similar at first glance, but they serve fundamentally different purposes:\n\n|                              | `specd-exploration.md`                                                                                        | `proposal.md`                                                                     |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |\n| **What it is**               | Agent working memory — raw conversation context                                                               | Formal schema artifact — structured document                                      |\n| **Who it's for**             | The AI agent in future sessions                                                                               | Humans and agents reviewing the change                                            |\n| **When it's written**        | During `/specd-new`, before any artifacts exist                                                               | During `/specd-design`, as the first artifact                                     |\n| **Part of the schema DAG?**  | No — it's outside the artifact pipeline                                                                       | Yes — other artifacts depend on it                                                |\n| **Validated?**               | No — no schema validation applies                                                                             | Yes — must pass schema validations                                                |\n| **Required by all schemas?** | Yes — every schema needs exploration context                                                                  | No — a schema can omit `proposal` entirely                                        |\n| **Content**                  | Everything discussed: rejected ideas, tangential topics, agreements, codebase observations, conversation flow | Curated: motivation, current behaviour, proposed solution, specs affected, impact |\n\nThe key distinction: **`proposal.md` is a schema artifact that a schema may or may not include.** A custom schema could replace `proposal` with a different first artifact, or skip it entirely and go straight to specs. But even without a proposal, the agent still needs to know what the user wants to build and why. That's what `specd-exploration.md` provides — it is schema-independent context that ensures continuity across sessions regardless of which artifacts the schema defines.\n"
      },
      {
        "index": 52,
        "heading": "What it contains",
        "level": 3,
        "startLine": 693,
        "endLine": 710,
        "lines": 18,
        "content": "### What it contains\n\nThe file captures everything from the discovery conversation:\n\n- **Problem statement** — what the user wants to achieve and why\n- **Approach / solution outline** — the agreed strategy\n- **Affected areas** — packages, modules, files, specs discussed\n- **Spec IDs** — attached to the change and mentioned as relevant\n- **Design decisions and agreements** — anything confirmed, rejected, or constrained\n- **Steps or plan** — if specific phases or ordering were agreed\n- **Rejected alternatives** — with reasons, so the agent doesn't re-propose them\n- **Open questions** — unresolved items for design to clarify\n- **Codebase observations** — file paths, patterns, current behaviour\n- **Tangential topics** — related issues, future ideas, things mentioned in passing\n- **Conversation flow** — how the discussion evolved\n\nEach item includes concrete anchors (file paths, spec IDs, function names) so the reader can verify whether they still hold, and reasons behind decisions so edge cases can be judged even if details have changed.\n"
      },
      {
        "index": 53,
        "heading": "Staleness",
        "level": 3,
        "startLine": 711,
        "endLine": 716,
        "lines": 6,
        "content": "### Staleness\n\nTime passes between exploration and design. Code changes, specs get renamed, decisions become outdated. The file includes a `Generated: YYYY-MM-DD` timestamp, and `/specd-design` performs a staleness check before trusting its content — verifying that referenced paths and spec IDs still exist, and flagging any significant drift to the user.\n\n---\n"
      },
      {
        "index": 54,
        "heading": "Where to go next",
        "level": 2,
        "startLine": 717,
        "endLine": 723,
        "lines": 7,
        "content": "## Where to go next\n\n- [Schema format reference](../schemas/schema-format.md) — define custom workflow steps, artifacts, hooks, and task completion checks for your project.\n- [Configuration guide](configuration.md) — enable approval gates, configure workspaces, and add schema overrides.\n- [CLI reference](../cli/cli-reference.md) — all `specd changes`, `specd drafts`, `specd discard`, and `specd archives` commands.\n- [Domain model](../core/domain-model.md) — the `Change`, `ChangeState`, `ChangeEvent`, and `ChangeArtifact` types returned by `@specd/core` use cases.\n"
      }
    ]
  },
  {
    "topic": "specs",
    "title": "Specifications in SpecD",
    "description": "Comprehensive guide to specs, requirement authoring, verification scenarios, delta evolution, and how specs drive development in SpecD",
    "order": 3,
    "content": "# Specifications in SpecD\n\nIn SpecD, specifications (**specs**) are the absolute source of truth. They define what your software system should do, why it does it, and how to verify that the implementation is correct.\n\nSpecs are not post-hoc documentation, nor are they throwaway sprint tickets. They are living, versioned, machine-readable requirement contracts that govern both human developers and autonomous AI coding agents.\n\n---\n\n## 1. What is a Spec?\n\nA spec is a durable requirement document that captures the agreed-upon behavior of a capability. It answers: **What must the system do, and under what constraints?**\n\nA spec adheres to three core tenets:\n\n1. **Separation of Intent from Implementation:** A spec describes capabilities, user expectations, and behavioral boundaries — never internal classes, function signatures, or private variable names.\n2. **Normative Language:** Requirements use RFC-2119 keywords (`SHALL`, `MUST`, `SHOULD`, `MAY`) to establish clear, unambiguous contractual obligations.\n3. **Paired Verification:** A requirement is only as good as its verification. Every spec file (`spec.md`) is paired with an executable verification scenario file (`verify.md`).\n\n### The Capability Directory Structure\n\nEvery specification lives in a dedicated capability directory under `specs/` (or your workspace's configured `specsPath`):\n\n```\nspecs/\n└── auth/\n    └── login/\n        ├── spec.md         # Requirements, constraints, and normative rules\n        ├── verify.md       # Acceptance criteria and test scenarios\n        └── spec-lock.json  # Authoritative persisted state (dependencies, code links, optimizations)\n```\n\n---\n\n## 2. Anatomy of `spec.md`\n\n`spec.md` defines the functional boundaries of a single cohesive capability.\n\n### Structure\n\n```markdown\n# Capability Title\n\n## Purpose\n\nA concise description explaining why this capability exists, what problem it solves, and the business or architectural motivation.\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\nA clear description of the requirement using normative language.\n\n- Specific rules, behaviors, and invariants.\n- Error conditions and fallback handling.\n\n### Requirement: Another Requirement Name\n\n...\n\n## Constraints\n\nNon-functional constraints, performance ceilings, architectural boundaries, or platform limitations (e.g. \"Zero external runtime dependencies\").\n\n## Spec Dependencies\n\nExplicit cross-references to other specs that this capability depends upon:\n\n- [`auth:jwt-tokens`](../jwt-tokens/spec.md)\n- [`core:storage`](../../core/storage/spec.md)\n```\n\n---\n\n## 3. Anatomy of `verify.md`\n\n`verify.md` defines concrete acceptance criteria for every requirement declared in `spec.md`. It translates abstract requirements into testable conditions.\n\n### Scenario Format\n\nScenarios are organized under their matching requirement heading and structured as `GIVEN` / `WHEN` / `THEN` tuples:\n\n```markdown\n# Verification: Capability Title\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\n#### Scenario: Successful login with valid credentials\n\n- **GIVEN** a registered user with active status in the database\n- **WHEN** POST `/api/v1/login` is called with valid credentials\n- **THEN** status code is 200 OK\n- **AND** response payload contains a signed JWT and refresh token\n\n#### Scenario: Account locked after multiple failed attempts\n\n- **GIVEN** a registered user with 4 prior failed attempts\n- **WHEN** POST `/api/v1/login` is called with invalid password\n- **THEN** response status is 423 Locked\n- **AND** an audit event `AUTH_LOCKOUT` is recorded\n```\n\nVerification scenarios are used during the `verifying` lifecycle state (via `/specd-verify`) to confirm that unit tests, integration tests, or end-to-end assertions prove the code meets the spec.\n\n---\n\n## 4. Metadata Extraction & Indexing\n\nSpecD automatically parses spec files and builds an AST-based metadata index without requiring runtime YAML frontmatter in spec files:\n\n- **Requirements Indexing:** Every `### Requirement: <Name>` is indexed as a discrete requirement node.\n- **Scenarios Indexing:** Every `#### Scenario: <Name>` is linked to its parent requirement.\n- **Dependency Resolution:** Relative links in `## Spec Dependencies` are normalized into canonical spec IDs (e.g. `core:kernel`, `default:auth/login`).\n- **Full-Text BM25 Search:** Requirements and scenarios are indexed for high-speed semantic retrieval via CLI (`specd graph search \"<query>\" --specs`) and agent context compilers.\n\n---\n\n## 5. How Specs Evolve: Spec Deltas\n\nIn traditional development, documentation rots because Pull Requests only show code diffs, leaving documentation disconnected. In SpecD, **specs evolve atomically with code**.\n\nWhen modifying an existing specification, you never edit the canonical `specs/` directory directly. Instead, you create a change (`specd changes create <name> --spec <id>`).\n\nInside the active change, SpecD tracks spec modifications as **Spec Deltas** under `.specd/changes/<name>/deltas/<spec-id>/spec.md.delta.yaml`.\n\n### Delta Operations\n\nA delta patch file uses structured YAML operations to modify requirements:\n\n```yaml\n- op: added\n  description: Add biometric authentication requirement\n  position:\n    parent:\n      type: section\n      matches: Requirements\n    after:\n      type: section\n      matches: 'Requirement: Password Authentication'\n  content: |\n    ### Requirement: Biometric Authentication\n    The system SHALL accept WebAuthn passkeys for passwordless authentication.\n\n- op: modified\n  description: Increase session timeout from 15m to 30m\n  selector:\n    type: section\n    matches: 'Requirement: Session Expiration'\n  content: |\n    ### Requirement: Session Expiration\n    Sessions SHALL expire after 30 minutes of continuous inactivity.\n\n- op: removed\n  description: Deprecate legacy SMS 2FA\n  selector:\n    type: section\n    matches: 'Requirement: SMS Two-Factor'\n\n- op: no-op\n  description: Capability implemented without spec changes\n```\n\n### Previewing and Merging\n\n- **Preview Deltas (`specd changes spec-preview <name>`)**: Shows the unified specification that would result from applying the change's delta patches onto the baseline specs.\n- **Archive Merging (`specd changes archive <name>`)**: When the change completes verification, SpecD applies the deltas to `specs/`, verifies patch integrity, and saves the applied delta records in `.specd/archive/`.\n\n---\n\n## 6. Persisted State (`spec-lock.json`) and Metadata Cache\n\nSpecifications in SpecD are not only human-readable Markdown documents; they also carry durable semantic relationships and fast-retrieval caches.\n\n### The Sidecar File (`spec-lock.json`)\n\nAuthoritative semantic state lives in a `spec-lock.json` sidecar file placed directly beside each spec on disk:\n\n```\nspecs/auth/login/\n├── spec.md\n├── verify.md\n└── spec-lock.json\n```\n\nThe lock file manages four key dimensions of semantic metadata:\n\n1. **`schema`** — The workflow schema identity under which this spec was created or migrated (`specd specs schema`).\n2. **`dependsOn`** — Curated spec dependency links (`specd specs deps`). SpecD uses these links to compute the dependency DAG and automatically pull required upstream specifications into compiled agent context.\n3. **`implementation`** — Explicit source code file and symbol links that implement this capability (`specd specs implementation`).\n4. **`optimizations`** — Baseline summaries and token optimizations for LLM consumption (`specd specs optimizations`).\n\n#### Managing Lock Files\n\n- **Initialize Lock Files (`specd specs init`)**: Creates `spec-lock.json` for any existing specs that lack one:\n  ```bash\n  specd specs init --all\n  ```\n- **Inspect and Update Dependencies**:\n  ```bash\n  specd specs deps default:auth/login\n  specd specs deps default:auth/login --add default:auth/session\n  ```\n- **Track Implementation Links**:\n  ```bash\n  specd specs implementation default:auth/login --file packages/core/src/auth.ts\n  ```\n\nDuring the change archiving process (`specd changes archive`), SpecD automatically validates and synchronizes `spec-lock.json` sidecar entries for all affected specifications.\n\n### The Materialized Metadata Cache (`.specd/metadata/`)\n\nTo deliver sub-millisecond retrieval speeds during agent sessions and CLI queries, SpecD automatically projects and materializes structured metadata into `.specd/metadata/<spec>.json`.\n\n- **Self-Healing**: SpecD computes an SHA-256 fingerprint across `spec.md`, `verify.md`, and `spec-lock.json`. When any command reads metadata (`specd specs metadata`, context compilation, or graph indexing), SpecD automatically detects staleness and rebuilds the projection on the fly.\n- **Gitignored**: The `.specd/metadata/` directory is an ephemeral runtime cache and is automatically gitignored (`/.specd/metadata/` in root `.gitignore`). It should never be committed to version control.\n- **Cache Content**: Materialized JSON includes extracted requirement headers, acceptance scenario blocks, purpose statements, normative constraints, and projected `spec-lock.json` fields.\n\n---\n\n## 7. How Specs Function Inside SpecD\n\nSpecs are deeply woven into the entire SpecD development platform:\n\n```\n  ┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐\n  │  Living Specs   │ ◄───► │   Code Graph    │ ◄───► │  Active Change  │\n  │  (specs/**)     │       │   (AST Store)   │       │  (.specd/**)    │\n  └────────┬────────┘       └────────┬────────┘       └────────┬────────┘\n           │                         │                         │\n           ▼                         ▼                         ▼\n   Human Governance          Blast Radius Impact      Automated Compliance\n   & Audit Records           & Symbol Coverage        & Gate Verification\n```\n\n### 1. The Code Graph & Traceability\n\nSpecD indexes your source code and links it directly to specification files:\n\n- **`COVERS_FILE`**: Identifies which specs govern each source code file.\n- **`COVERS_SYMBOL`**: Links individual functions, classes, and exported APIs directly to the specific requirement they satisfy.\n- **Blast Radius Analysis**: When a spec is modified, `specd graph impact --spec <id> --direction dependents` reveals all downstream files, symbols, and dependent specs that require re-verification.\n\n### 2. Context Compilation for AI Agents\n\nWhen an AI agent runs a SpecD skill (`/specd`, `/specd-implement`, etc.):\n\n- The agent does not read random files across your repository.\n- SpecD compiles the exact target specs, dependency summaries, and verification criteria into a compact context block.\n- The agent implements code against concrete requirement contracts, eliminating hallucinations.\n\n### 3. Approval Gates & Compliance Checking\n\nSpecD provides automated compliance checking:\n\n- **Spec Approval Gate**: Verifies that the proposal and design documents satisfy the requirements before code implementation begins.\n- **Signoff Gate**: Audits the completed code and test evidence against `verify.md` scenarios before archiving.\n\n---\n\n## 8. Authoring Best Practices\n\nTo get the most out of SpecD:\n\n- **Keep specs focused:** One spec directory per capability. If a spec grows beyond 10-12 requirements, split it into sub-capabilities.\n- **Use standard RFC-2119 keywords:** Be explicit about what is mandatory (`MUST`, `SHALL`) versus optional (`MAY`, `SHOULD`).\n- **Never put code in `spec.md`:** Describe data contracts, interfaces, and behaviors conceptually. Use JSON/YAML examples or schemas to describe payload shapes rather than language-specific structs or classes.\n- **Make scenarios testable:** In `verify.md`, avoid subjective words like \"fast\", \"intuitive\", or \"correctly\". Specify measurable criteria: status codes, events, output values, or maximum latencies.\n",
    "lineCount": 269,
    "byteLength": 12081,
    "outline": [
      {
        "index": 1,
        "heading": "Specifications in SpecD",
        "level": 1,
        "startLine": 1,
        "endLine": 269,
        "lines": 269,
        "content": "# Specifications in SpecD\n\nIn SpecD, specifications (**specs**) are the absolute source of truth. They define what your software system should do, why it does it, and how to verify that the implementation is correct.\n\nSpecs are not post-hoc documentation, nor are they throwaway sprint tickets. They are living, versioned, machine-readable requirement contracts that govern both human developers and autonomous AI coding agents.\n\n---\n\n## 1. What is a Spec?\n\nA spec is a durable requirement document that captures the agreed-upon behavior of a capability. It answers: **What must the system do, and under what constraints?**\n\nA spec adheres to three core tenets:\n\n1. **Separation of Intent from Implementation:** A spec describes capabilities, user expectations, and behavioral boundaries — never internal classes, function signatures, or private variable names.\n2. **Normative Language:** Requirements use RFC-2119 keywords (`SHALL`, `MUST`, `SHOULD`, `MAY`) to establish clear, unambiguous contractual obligations.\n3. **Paired Verification:** A requirement is only as good as its verification. Every spec file (`spec.md`) is paired with an executable verification scenario file (`verify.md`).\n\n### The Capability Directory Structure\n\nEvery specification lives in a dedicated capability directory under `specs/` (or your workspace's configured `specsPath`):\n\n```\nspecs/\n└── auth/\n    └── login/\n        ├── spec.md         # Requirements, constraints, and normative rules\n        ├── verify.md       # Acceptance criteria and test scenarios\n        └── spec-lock.json  # Authoritative persisted state (dependencies, code links, optimizations)\n```\n\n---\n\n## 2. Anatomy of `spec.md`\n\n`spec.md` defines the functional boundaries of a single cohesive capability.\n\n### Structure\n\n```markdown\n# Capability Title\n\n## Purpose\n\nA concise description explaining why this capability exists, what problem it solves, and the business or architectural motivation.\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\nA clear description of the requirement using normative language.\n\n- Specific rules, behaviors, and invariants.\n- Error conditions and fallback handling.\n\n### Requirement: Another Requirement Name\n\n...\n\n## Constraints\n\nNon-functional constraints, performance ceilings, architectural boundaries, or platform limitations (e.g. \"Zero external runtime dependencies\").\n\n## Spec Dependencies\n\nExplicit cross-references to other specs that this capability depends upon:\n\n- [`auth:jwt-tokens`](../jwt-tokens/spec.md)\n- [`core:storage`](../../core/storage/spec.md)\n```\n\n---\n\n## 3. Anatomy of `verify.md`\n\n`verify.md` defines concrete acceptance criteria for every requirement declared in `spec.md`. It translates abstract requirements into testable conditions.\n\n### Scenario Format\n\nScenarios are organized under their matching requirement heading and structured as `GIVEN` / `WHEN` / `THEN` tuples:\n\n```markdown\n# Verification: Capability Title\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\n#### Scenario: Successful login with valid credentials\n\n- **GIVEN** a registered user with active status in the database\n- **WHEN** POST `/api/v1/login` is called with valid credentials\n- **THEN** status code is 200 OK\n- **AND** response payload contains a signed JWT and refresh token\n\n#### Scenario: Account locked after multiple failed attempts\n\n- **GIVEN** a registered user with 4 prior failed attempts\n- **WHEN** POST `/api/v1/login` is called with invalid password\n- **THEN** response status is 423 Locked\n- **AND** an audit event `AUTH_LOCKOUT` is recorded\n```\n\nVerification scenarios are used during the `verifying` lifecycle state (via `/specd-verify`) to confirm that unit tests, integration tests, or end-to-end assertions prove the code meets the spec.\n\n---\n\n## 4. Metadata Extraction & Indexing\n\nSpecD automatically parses spec files and builds an AST-based metadata index without requiring runtime YAML frontmatter in spec files:\n\n- **Requirements Indexing:** Every `### Requirement: <Name>` is indexed as a discrete requirement node.\n- **Scenarios Indexing:** Every `#### Scenario: <Name>` is linked to its parent requirement.\n- **Dependency Resolution:** Relative links in `## Spec Dependencies` are normalized into canonical spec IDs (e.g. `core:kernel`, `default:auth/login`).\n- **Full-Text BM25 Search:** Requirements and scenarios are indexed for high-speed semantic retrieval via CLI (`specd graph search \"<query>\" --specs`) and agent context compilers.\n\n---\n\n## 5. How Specs Evolve: Spec Deltas\n\nIn traditional development, documentation rots because Pull Requests only show code diffs, leaving documentation disconnected. In SpecD, **specs evolve atomically with code**.\n\nWhen modifying an existing specification, you never edit the canonical `specs/` directory directly. Instead, you create a change (`specd changes create <name> --spec <id>`).\n\nInside the active change, SpecD tracks spec modifications as **Spec Deltas** under `.specd/changes/<name>/deltas/<spec-id>/spec.md.delta.yaml`.\n\n### Delta Operations\n\nA delta patch file uses structured YAML operations to modify requirements:\n\n```yaml\n- op: added\n  description: Add biometric authentication requirement\n  position:\n    parent:\n      type: section\n      matches: Requirements\n    after:\n      type: section\n      matches: 'Requirement: Password Authentication'\n  content: |\n    ### Requirement: Biometric Authentication\n    The system SHALL accept WebAuthn passkeys for passwordless authentication.\n\n- op: modified\n  description: Increase session timeout from 15m to 30m\n  selector:\n    type: section\n    matches: 'Requirement: Session Expiration'\n  content: |\n    ### Requirement: Session Expiration\n    Sessions SHALL expire after 30 minutes of continuous inactivity.\n\n- op: removed\n  description: Deprecate legacy SMS 2FA\n  selector:\n    type: section\n    matches: 'Requirement: SMS Two-Factor'\n\n- op: no-op\n  description: Capability implemented without spec changes\n```\n\n### Previewing and Merging\n\n- **Preview Deltas (`specd changes spec-preview <name>`)**: Shows the unified specification that would result from applying the change's delta patches onto the baseline specs.\n- **Archive Merging (`specd changes archive <name>`)**: When the change completes verification, SpecD applies the deltas to `specs/`, verifies patch integrity, and saves the applied delta records in `.specd/archive/`.\n\n---\n\n## 6. Persisted State (`spec-lock.json`) and Metadata Cache\n\nSpecifications in SpecD are not only human-readable Markdown documents; they also carry durable semantic relationships and fast-retrieval caches.\n\n### The Sidecar File (`spec-lock.json`)\n\nAuthoritative semantic state lives in a `spec-lock.json` sidecar file placed directly beside each spec on disk:\n\n```\nspecs/auth/login/\n├── spec.md\n├── verify.md\n└── spec-lock.json\n```\n\nThe lock file manages four key dimensions of semantic metadata:\n\n1. **`schema`** — The workflow schema identity under which this spec was created or migrated (`specd specs schema`).\n2. **`dependsOn`** — Curated spec dependency links (`specd specs deps`). SpecD uses these links to compute the dependency DAG and automatically pull required upstream specifications into compiled agent context.\n3. **`implementation`** — Explicit source code file and symbol links that implement this capability (`specd specs implementation`).\n4. **`optimizations`** — Baseline summaries and token optimizations for LLM consumption (`specd specs optimizations`).\n\n#### Managing Lock Files\n\n- **Initialize Lock Files (`specd specs init`)**: Creates `spec-lock.json` for any existing specs that lack one:\n  ```bash\n  specd specs init --all\n  ```\n- **Inspect and Update Dependencies**:\n  ```bash\n  specd specs deps default:auth/login\n  specd specs deps default:auth/login --add default:auth/session\n  ```\n- **Track Implementation Links**:\n  ```bash\n  specd specs implementation default:auth/login --file packages/core/src/auth.ts\n  ```\n\nDuring the change archiving process (`specd changes archive`), SpecD automatically validates and synchronizes `spec-lock.json` sidecar entries for all affected specifications.\n\n### The Materialized Metadata Cache (`.specd/metadata/`)\n\nTo deliver sub-millisecond retrieval speeds during agent sessions and CLI queries, SpecD automatically projects and materializes structured metadata into `.specd/metadata/<spec>.json`.\n\n- **Self-Healing**: SpecD computes an SHA-256 fingerprint across `spec.md`, `verify.md`, and `spec-lock.json`. When any command reads metadata (`specd specs metadata`, context compilation, or graph indexing), SpecD automatically detects staleness and rebuilds the projection on the fly.\n- **Gitignored**: The `.specd/metadata/` directory is an ephemeral runtime cache and is automatically gitignored (`/.specd/metadata/` in root `.gitignore`). It should never be committed to version control.\n- **Cache Content**: Materialized JSON includes extracted requirement headers, acceptance scenario blocks, purpose statements, normative constraints, and projected `spec-lock.json` fields.\n\n---\n\n## 7. How Specs Function Inside SpecD\n\nSpecs are deeply woven into the entire SpecD development platform:\n\n```\n  ┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐\n  │  Living Specs   │ ◄───► │   Code Graph    │ ◄───► │  Active Change  │\n  │  (specs/**)     │       │   (AST Store)   │       │  (.specd/**)    │\n  └────────┬────────┘       └────────┬────────┘       └────────┬────────┘\n           │                         │                         │\n           ▼                         ▼                         ▼\n   Human Governance          Blast Radius Impact      Automated Compliance\n   & Audit Records           & Symbol Coverage        & Gate Verification\n```\n\n### 1. The Code Graph & Traceability\n\nSpecD indexes your source code and links it directly to specification files:\n\n- **`COVERS_FILE`**: Identifies which specs govern each source code file.\n- **`COVERS_SYMBOL`**: Links individual functions, classes, and exported APIs directly to the specific requirement they satisfy.\n- **Blast Radius Analysis**: When a spec is modified, `specd graph impact --spec <id> --direction dependents` reveals all downstream files, symbols, and dependent specs that require re-verification.\n\n### 2. Context Compilation for AI Agents\n\nWhen an AI agent runs a SpecD skill (`/specd`, `/specd-implement`, etc.):\n\n- The agent does not read random files across your repository.\n- SpecD compiles the exact target specs, dependency summaries, and verification criteria into a compact context block.\n- The agent implements code against concrete requirement contracts, eliminating hallucinations.\n\n### 3. Approval Gates & Compliance Checking\n\nSpecD provides automated compliance checking:\n\n- **Spec Approval Gate**: Verifies that the proposal and design documents satisfy the requirements before code implementation begins.\n- **Signoff Gate**: Audits the completed code and test evidence against `verify.md` scenarios before archiving.\n\n---\n\n## 8. Authoring Best Practices\n\nTo get the most out of SpecD:\n\n- **Keep specs focused:** One spec directory per capability. If a spec grows beyond 10-12 requirements, split it into sub-capabilities.\n- **Use standard RFC-2119 keywords:** Be explicit about what is mandatory (`MUST`, `SHALL`) versus optional (`MAY`, `SHOULD`).\n- **Never put code in `spec.md`:** Describe data contracts, interfaces, and behaviors conceptually. Use JSON/YAML examples or schemas to describe payload shapes rather than language-specific structs or classes.\n- **Make scenarios testable:** In `verify.md`, avoid subjective words like \"fast\", \"intuitive\", or \"correctly\". Specify measurable criteria: status codes, events, output values, or maximum latencies.\n"
      },
      {
        "index": 2,
        "heading": "1. What is a Spec?",
        "level": 2,
        "startLine": 9,
        "endLine": 33,
        "lines": 25,
        "content": "## 1. What is a Spec?\n\nA spec is a durable requirement document that captures the agreed-upon behavior of a capability. It answers: **What must the system do, and under what constraints?**\n\nA spec adheres to three core tenets:\n\n1. **Separation of Intent from Implementation:** A spec describes capabilities, user expectations, and behavioral boundaries — never internal classes, function signatures, or private variable names.\n2. **Normative Language:** Requirements use RFC-2119 keywords (`SHALL`, `MUST`, `SHOULD`, `MAY`) to establish clear, unambiguous contractual obligations.\n3. **Paired Verification:** A requirement is only as good as its verification. Every spec file (`spec.md`) is paired with an executable verification scenario file (`verify.md`).\n\n### The Capability Directory Structure\n\nEvery specification lives in a dedicated capability directory under `specs/` (or your workspace's configured `specsPath`):\n\n```\nspecs/\n└── auth/\n    └── login/\n        ├── spec.md         # Requirements, constraints, and normative rules\n        ├── verify.md       # Acceptance criteria and test scenarios\n        └── spec-lock.json  # Authoritative persisted state (dependencies, code links, optimizations)\n```\n\n---\n"
      },
      {
        "index": 3,
        "heading": "The Capability Directory Structure",
        "level": 3,
        "startLine": 19,
        "endLine": 33,
        "lines": 15,
        "content": "### The Capability Directory Structure\n\nEvery specification lives in a dedicated capability directory under `specs/` (or your workspace's configured `specsPath`):\n\n```\nspecs/\n└── auth/\n    └── login/\n        ├── spec.md         # Requirements, constraints, and normative rules\n        ├── verify.md       # Acceptance criteria and test scenarios\n        └── spec-lock.json  # Authoritative persisted state (dependencies, code links, optimizations)\n```\n\n---\n"
      },
      {
        "index": 4,
        "heading": "2. Anatomy of `spec.md`",
        "level": 2,
        "startLine": 34,
        "endLine": 73,
        "lines": 40,
        "content": "## 2. Anatomy of `spec.md`\n\n`spec.md` defines the functional boundaries of a single cohesive capability.\n\n### Structure\n\n```markdown\n# Capability Title\n\n## Purpose\n\nA concise description explaining why this capability exists, what problem it solves, and the business or architectural motivation.\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\nA clear description of the requirement using normative language.\n\n- Specific rules, behaviors, and invariants.\n- Error conditions and fallback handling.\n\n### Requirement: Another Requirement Name\n\n...\n\n## Constraints\n\nNon-functional constraints, performance ceilings, architectural boundaries, or platform limitations (e.g. \"Zero external runtime dependencies\").\n\n## Spec Dependencies\n\nExplicit cross-references to other specs that this capability depends upon:\n\n- [`auth:jwt-tokens`](../jwt-tokens/spec.md)\n- [`core:storage`](../../core/storage/spec.md)\n```\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Structure",
        "level": 3,
        "startLine": 38,
        "endLine": 73,
        "lines": 36,
        "content": "### Structure\n\n```markdown\n# Capability Title\n\n## Purpose\n\nA concise description explaining why this capability exists, what problem it solves, and the business or architectural motivation.\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\nA clear description of the requirement using normative language.\n\n- Specific rules, behaviors, and invariants.\n- Error conditions and fallback handling.\n\n### Requirement: Another Requirement Name\n\n...\n\n## Constraints\n\nNon-functional constraints, performance ceilings, architectural boundaries, or platform limitations (e.g. \"Zero external runtime dependencies\").\n\n## Spec Dependencies\n\nExplicit cross-references to other specs that this capability depends upon:\n\n- [`auth:jwt-tokens`](../jwt-tokens/spec.md)\n- [`core:storage`](../../core/storage/spec.md)\n```\n\n---\n"
      },
      {
        "index": 6,
        "heading": "3. Anatomy of `verify.md`",
        "level": 2,
        "startLine": 74,
        "endLine": 107,
        "lines": 34,
        "content": "## 3. Anatomy of `verify.md`\n\n`verify.md` defines concrete acceptance criteria for every requirement declared in `spec.md`. It translates abstract requirements into testable conditions.\n\n### Scenario Format\n\nScenarios are organized under their matching requirement heading and structured as `GIVEN` / `WHEN` / `THEN` tuples:\n\n```markdown\n# Verification: Capability Title\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\n#### Scenario: Successful login with valid credentials\n\n- **GIVEN** a registered user with active status in the database\n- **WHEN** POST `/api/v1/login` is called with valid credentials\n- **THEN** status code is 200 OK\n- **AND** response payload contains a signed JWT and refresh token\n\n#### Scenario: Account locked after multiple failed attempts\n\n- **GIVEN** a registered user with 4 prior failed attempts\n- **WHEN** POST `/api/v1/login` is called with invalid password\n- **THEN** response status is 423 Locked\n- **AND** an audit event `AUTH_LOCKOUT` is recorded\n```\n\nVerification scenarios are used during the `verifying` lifecycle state (via `/specd-verify`) to confirm that unit tests, integration tests, or end-to-end assertions prove the code meets the spec.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "Scenario Format",
        "level": 3,
        "startLine": 78,
        "endLine": 107,
        "lines": 30,
        "content": "### Scenario Format\n\nScenarios are organized under their matching requirement heading and structured as `GIVEN` / `WHEN` / `THEN` tuples:\n\n```markdown\n# Verification: Capability Title\n\n## Requirements\n\n### Requirement: Unique Requirement Name\n\n#### Scenario: Successful login with valid credentials\n\n- **GIVEN** a registered user with active status in the database\n- **WHEN** POST `/api/v1/login` is called with valid credentials\n- **THEN** status code is 200 OK\n- **AND** response payload contains a signed JWT and refresh token\n\n#### Scenario: Account locked after multiple failed attempts\n\n- **GIVEN** a registered user with 4 prior failed attempts\n- **WHEN** POST `/api/v1/login` is called with invalid password\n- **THEN** response status is 423 Locked\n- **AND** an audit event `AUTH_LOCKOUT` is recorded\n```\n\nVerification scenarios are used during the `verifying` lifecycle state (via `/specd-verify`) to confirm that unit tests, integration tests, or end-to-end assertions prove the code meets the spec.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "4. Metadata Extraction & Indexing",
        "level": 2,
        "startLine": 108,
        "endLine": 118,
        "lines": 11,
        "content": "## 4. Metadata Extraction & Indexing\n\nSpecD automatically parses spec files and builds an AST-based metadata index without requiring runtime YAML frontmatter in spec files:\n\n- **Requirements Indexing:** Every `### Requirement: <Name>` is indexed as a discrete requirement node.\n- **Scenarios Indexing:** Every `#### Scenario: <Name>` is linked to its parent requirement.\n- **Dependency Resolution:** Relative links in `## Spec Dependencies` are normalized into canonical spec IDs (e.g. `core:kernel`, `default:auth/login`).\n- **Full-Text BM25 Search:** Requirements and scenarios are indexed for high-speed semantic retrieval via CLI (`specd graph search \"<query>\" --specs`) and agent context compilers.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "5. How Specs Evolve: Spec Deltas",
        "level": 2,
        "startLine": 119,
        "endLine": 170,
        "lines": 52,
        "content": "## 5. How Specs Evolve: Spec Deltas\n\nIn traditional development, documentation rots because Pull Requests only show code diffs, leaving documentation disconnected. In SpecD, **specs evolve atomically with code**.\n\nWhen modifying an existing specification, you never edit the canonical `specs/` directory directly. Instead, you create a change (`specd changes create <name> --spec <id>`).\n\nInside the active change, SpecD tracks spec modifications as **Spec Deltas** under `.specd/changes/<name>/deltas/<spec-id>/spec.md.delta.yaml`.\n\n### Delta Operations\n\nA delta patch file uses structured YAML operations to modify requirements:\n\n```yaml\n- op: added\n  description: Add biometric authentication requirement\n  position:\n    parent:\n      type: section\n      matches: Requirements\n    after:\n      type: section\n      matches: 'Requirement: Password Authentication'\n  content: |\n    ### Requirement: Biometric Authentication\n    The system SHALL accept WebAuthn passkeys for passwordless authentication.\n\n- op: modified\n  description: Increase session timeout from 15m to 30m\n  selector:\n    type: section\n    matches: 'Requirement: Session Expiration'\n  content: |\n    ### Requirement: Session Expiration\n    Sessions SHALL expire after 30 minutes of continuous inactivity.\n\n- op: removed\n  description: Deprecate legacy SMS 2FA\n  selector:\n    type: section\n    matches: 'Requirement: SMS Two-Factor'\n\n- op: no-op\n  description: Capability implemented without spec changes\n```\n\n### Previewing and Merging\n\n- **Preview Deltas (`specd changes spec-preview <name>`)**: Shows the unified specification that would result from applying the change's delta patches onto the baseline specs.\n- **Archive Merging (`specd changes archive <name>`)**: When the change completes verification, SpecD applies the deltas to `specs/`, verifies patch integrity, and saves the applied delta records in `.specd/archive/`.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "Delta Operations",
        "level": 3,
        "startLine": 127,
        "endLine": 163,
        "lines": 37,
        "content": "### Delta Operations\n\nA delta patch file uses structured YAML operations to modify requirements:\n\n```yaml\n- op: added\n  description: Add biometric authentication requirement\n  position:\n    parent:\n      type: section\n      matches: Requirements\n    after:\n      type: section\n      matches: 'Requirement: Password Authentication'\n  content: |\n    ### Requirement: Biometric Authentication\n    The system SHALL accept WebAuthn passkeys for passwordless authentication.\n\n- op: modified\n  description: Increase session timeout from 15m to 30m\n  selector:\n    type: section\n    matches: 'Requirement: Session Expiration'\n  content: |\n    ### Requirement: Session Expiration\n    Sessions SHALL expire after 30 minutes of continuous inactivity.\n\n- op: removed\n  description: Deprecate legacy SMS 2FA\n  selector:\n    type: section\n    matches: 'Requirement: SMS Two-Factor'\n\n- op: no-op\n  description: Capability implemented without spec changes\n```\n"
      },
      {
        "index": 11,
        "heading": "Previewing and Merging",
        "level": 3,
        "startLine": 164,
        "endLine": 170,
        "lines": 7,
        "content": "### Previewing and Merging\n\n- **Preview Deltas (`specd changes spec-preview <name>`)**: Shows the unified specification that would result from applying the change's delta patches onto the baseline specs.\n- **Archive Merging (`specd changes archive <name>`)**: When the change completes verification, SpecD applies the deltas to `specs/`, verifies patch integrity, and saves the applied delta records in `.specd/archive/`.\n\n---\n"
      },
      {
        "index": 12,
        "heading": "6. Persisted State (`spec-lock.json`) and Metadata Cache",
        "level": 2,
        "startLine": 171,
        "endLine": 220,
        "lines": 50,
        "content": "## 6. Persisted State (`spec-lock.json`) and Metadata Cache\n\nSpecifications in SpecD are not only human-readable Markdown documents; they also carry durable semantic relationships and fast-retrieval caches.\n\n### The Sidecar File (`spec-lock.json`)\n\nAuthoritative semantic state lives in a `spec-lock.json` sidecar file placed directly beside each spec on disk:\n\n```\nspecs/auth/login/\n├── spec.md\n├── verify.md\n└── spec-lock.json\n```\n\nThe lock file manages four key dimensions of semantic metadata:\n\n1. **`schema`** — The workflow schema identity under which this spec was created or migrated (`specd specs schema`).\n2. **`dependsOn`** — Curated spec dependency links (`specd specs deps`). SpecD uses these links to compute the dependency DAG and automatically pull required upstream specifications into compiled agent context.\n3. **`implementation`** — Explicit source code file and symbol links that implement this capability (`specd specs implementation`).\n4. **`optimizations`** — Baseline summaries and token optimizations for LLM consumption (`specd specs optimizations`).\n\n#### Managing Lock Files\n\n- **Initialize Lock Files (`specd specs init`)**: Creates `spec-lock.json` for any existing specs that lack one:\n  ```bash\n  specd specs init --all\n  ```\n- **Inspect and Update Dependencies**:\n  ```bash\n  specd specs deps default:auth/login\n  specd specs deps default:auth/login --add default:auth/session\n  ```\n- **Track Implementation Links**:\n  ```bash\n  specd specs implementation default:auth/login --file packages/core/src/auth.ts\n  ```\n\nDuring the change archiving process (`specd changes archive`), SpecD automatically validates and synchronizes `spec-lock.json` sidecar entries for all affected specifications.\n\n### The Materialized Metadata Cache (`.specd/metadata/`)\n\nTo deliver sub-millisecond retrieval speeds during agent sessions and CLI queries, SpecD automatically projects and materializes structured metadata into `.specd/metadata/<spec>.json`.\n\n- **Self-Healing**: SpecD computes an SHA-256 fingerprint across `spec.md`, `verify.md`, and `spec-lock.json`. When any command reads metadata (`specd specs metadata`, context compilation, or graph indexing), SpecD automatically detects staleness and rebuilds the projection on the fly.\n- **Gitignored**: The `.specd/metadata/` directory is an ephemeral runtime cache and is automatically gitignored (`/.specd/metadata/` in root `.gitignore`). It should never be committed to version control.\n- **Cache Content**: Materialized JSON includes extracted requirement headers, acceptance scenario blocks, purpose statements, normative constraints, and projected `spec-lock.json` fields.\n\n---\n"
      },
      {
        "index": 13,
        "heading": "The Sidecar File (`spec-lock.json`)",
        "level": 3,
        "startLine": 175,
        "endLine": 210,
        "lines": 36,
        "content": "### The Sidecar File (`spec-lock.json`)\n\nAuthoritative semantic state lives in a `spec-lock.json` sidecar file placed directly beside each spec on disk:\n\n```\nspecs/auth/login/\n├── spec.md\n├── verify.md\n└── spec-lock.json\n```\n\nThe lock file manages four key dimensions of semantic metadata:\n\n1. **`schema`** — The workflow schema identity under which this spec was created or migrated (`specd specs schema`).\n2. **`dependsOn`** — Curated spec dependency links (`specd specs deps`). SpecD uses these links to compute the dependency DAG and automatically pull required upstream specifications into compiled agent context.\n3. **`implementation`** — Explicit source code file and symbol links that implement this capability (`specd specs implementation`).\n4. **`optimizations`** — Baseline summaries and token optimizations for LLM consumption (`specd specs optimizations`).\n\n#### Managing Lock Files\n\n- **Initialize Lock Files (`specd specs init`)**: Creates `spec-lock.json` for any existing specs that lack one:\n  ```bash\n  specd specs init --all\n  ```\n- **Inspect and Update Dependencies**:\n  ```bash\n  specd specs deps default:auth/login\n  specd specs deps default:auth/login --add default:auth/session\n  ```\n- **Track Implementation Links**:\n  ```bash\n  specd specs implementation default:auth/login --file packages/core/src/auth.ts\n  ```\n\nDuring the change archiving process (`specd changes archive`), SpecD automatically validates and synchronizes `spec-lock.json` sidecar entries for all affected specifications.\n"
      },
      {
        "index": 14,
        "heading": "Managing Lock Files",
        "level": 4,
        "startLine": 193,
        "endLine": 210,
        "lines": 18,
        "content": "#### Managing Lock Files\n\n- **Initialize Lock Files (`specd specs init`)**: Creates `spec-lock.json` for any existing specs that lack one:\n  ```bash\n  specd specs init --all\n  ```\n- **Inspect and Update Dependencies**:\n  ```bash\n  specd specs deps default:auth/login\n  specd specs deps default:auth/login --add default:auth/session\n  ```\n- **Track Implementation Links**:\n  ```bash\n  specd specs implementation default:auth/login --file packages/core/src/auth.ts\n  ```\n\nDuring the change archiving process (`specd changes archive`), SpecD automatically validates and synchronizes `spec-lock.json` sidecar entries for all affected specifications.\n"
      },
      {
        "index": 15,
        "heading": "The Materialized Metadata Cache (`.specd/metadata/`)",
        "level": 3,
        "startLine": 211,
        "endLine": 220,
        "lines": 10,
        "content": "### The Materialized Metadata Cache (`.specd/metadata/`)\n\nTo deliver sub-millisecond retrieval speeds during agent sessions and CLI queries, SpecD automatically projects and materializes structured metadata into `.specd/metadata/<spec>.json`.\n\n- **Self-Healing**: SpecD computes an SHA-256 fingerprint across `spec.md`, `verify.md`, and `spec-lock.json`. When any command reads metadata (`specd specs metadata`, context compilation, or graph indexing), SpecD automatically detects staleness and rebuilds the projection on the fly.\n- **Gitignored**: The `.specd/metadata/` directory is an ephemeral runtime cache and is automatically gitignored (`/.specd/metadata/` in root `.gitignore`). It should never be committed to version control.\n- **Cache Content**: Materialized JSON includes extracted requirement headers, acceptance scenario blocks, purpose statements, normative constraints, and projected `spec-lock.json` fields.\n\n---\n"
      },
      {
        "index": 16,
        "heading": "7. How Specs Function Inside SpecD",
        "level": 2,
        "startLine": 221,
        "endLine": 260,
        "lines": 40,
        "content": "## 7. How Specs Function Inside SpecD\n\nSpecs are deeply woven into the entire SpecD development platform:\n\n```\n  ┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐\n  │  Living Specs   │ ◄───► │   Code Graph    │ ◄───► │  Active Change  │\n  │  (specs/**)     │       │   (AST Store)   │       │  (.specd/**)    │\n  └────────┬────────┘       └────────┬────────┘       └────────┬────────┘\n           │                         │                         │\n           ▼                         ▼                         ▼\n   Human Governance          Blast Radius Impact      Automated Compliance\n   & Audit Records           & Symbol Coverage        & Gate Verification\n```\n\n### 1. The Code Graph & Traceability\n\nSpecD indexes your source code and links it directly to specification files:\n\n- **`COVERS_FILE`**: Identifies which specs govern each source code file.\n- **`COVERS_SYMBOL`**: Links individual functions, classes, and exported APIs directly to the specific requirement they satisfy.\n- **Blast Radius Analysis**: When a spec is modified, `specd graph impact --spec <id> --direction dependents` reveals all downstream files, symbols, and dependent specs that require re-verification.\n\n### 2. Context Compilation for AI Agents\n\nWhen an AI agent runs a SpecD skill (`/specd`, `/specd-implement`, etc.):\n\n- The agent does not read random files across your repository.\n- SpecD compiles the exact target specs, dependency summaries, and verification criteria into a compact context block.\n- The agent implements code against concrete requirement contracts, eliminating hallucinations.\n\n### 3. Approval Gates & Compliance Checking\n\nSpecD provides automated compliance checking:\n\n- **Spec Approval Gate**: Verifies that the proposal and design documents satisfy the requirements before code implementation begins.\n- **Signoff Gate**: Audits the completed code and test evidence against `verify.md` scenarios before archiving.\n\n---\n"
      },
      {
        "index": 17,
        "heading": "1. The Code Graph & Traceability",
        "level": 3,
        "startLine": 236,
        "endLine": 243,
        "lines": 8,
        "content": "### 1. The Code Graph & Traceability\n\nSpecD indexes your source code and links it directly to specification files:\n\n- **`COVERS_FILE`**: Identifies which specs govern each source code file.\n- **`COVERS_SYMBOL`**: Links individual functions, classes, and exported APIs directly to the specific requirement they satisfy.\n- **Blast Radius Analysis**: When a spec is modified, `specd graph impact --spec <id> --direction dependents` reveals all downstream files, symbols, and dependent specs that require re-verification.\n"
      },
      {
        "index": 18,
        "heading": "2. Context Compilation for AI Agents",
        "level": 3,
        "startLine": 244,
        "endLine": 251,
        "lines": 8,
        "content": "### 2. Context Compilation for AI Agents\n\nWhen an AI agent runs a SpecD skill (`/specd`, `/specd-implement`, etc.):\n\n- The agent does not read random files across your repository.\n- SpecD compiles the exact target specs, dependency summaries, and verification criteria into a compact context block.\n- The agent implements code against concrete requirement contracts, eliminating hallucinations.\n"
      },
      {
        "index": 19,
        "heading": "3. Approval Gates & Compliance Checking",
        "level": 3,
        "startLine": 252,
        "endLine": 260,
        "lines": 9,
        "content": "### 3. Approval Gates & Compliance Checking\n\nSpecD provides automated compliance checking:\n\n- **Spec Approval Gate**: Verifies that the proposal and design documents satisfy the requirements before code implementation begins.\n- **Signoff Gate**: Audits the completed code and test evidence against `verify.md` scenarios before archiving.\n\n---\n"
      },
      {
        "index": 20,
        "heading": "8. Authoring Best Practices",
        "level": 2,
        "startLine": 261,
        "endLine": 269,
        "lines": 9,
        "content": "## 8. Authoring Best Practices\n\nTo get the most out of SpecD:\n\n- **Keep specs focused:** One spec directory per capability. If a spec grows beyond 10-12 requirements, split it into sub-capabilities.\n- **Use standard RFC-2119 keywords:** Be explicit about what is mandatory (`MUST`, `SHALL`) versus optional (`MAY`, `SHOULD`).\n- **Never put code in `spec.md`:** Describe data contracts, interfaces, and behaviors conceptually. Use JSON/YAML examples or schemas to describe payload shapes rather than language-specific structs or classes.\n- **Make scenarios testable:** In `verify.md`, avoid subjective words like \"fast\", \"intuitive\", or \"correctly\". Specify measurable criteria: status codes, events, output values, or maximum latencies.\n"
      }
    ]
  },
  {
    "topic": "changes",
    "title": "Changes in SpecD",
    "description": "Comprehensive guide to changes as the atomic unit of work, directory structure, manifest lifecycle, and artifact DAG in SpecD",
    "order": 4,
    "content": "# Changes in SpecD\n\nIn SpecD, a **change** is the fundamental unit of work. Every modification to your system — whether creating a new capability, refactoring an existing service, or fixing a bug — is organized as an active change.\n\nThere is no \"code-only\" workflow in SpecD. Code never moves forward in isolation; it moves forward in lockstep with the specifications that govern it.\n\n---\n\n## 1. What is a Change?\n\nA change is an isolated, structured workspace that tracks the evolution of one or more specifications from initial intent through technical design, code implementation, and verification, to permanent archiving.\n\nA change answers four essential questions:\n\n1. **What intent is being addressed?** Captured in `proposal.md`.\n2. **Which specifications are being created or updated?** Declared in `specIds` and staged in `specs/` or `deltas/`.\n3. **How will it be built?** Planned in `design.md` and tracked in `tasks.md`.\n4. **Where does the work currently sit?** Tracked in `manifest.yaml` as an explicit lifecycle state.\n\nUnlike ephemeral git branches or informal issue tickets, a change in SpecD is a durable directory on disk containing both human-readable documentation and machine-verifiable metadata.\n\n---\n\n## 2. Anatomy of a Change Directory\n\nActive changes live in `.specd/changes/` under a timestamped directory slug:\n\n```\n.specd/changes/\n└── 20260923-144548-add-oauth-flow/\n    ├── manifest.yaml          # Machine-readable state, scope, and artifact baselines\n    ├── proposal.md            # Problem statement, intent, and user requirements\n    ├── specs/                 # Staged full artifacts for brand-new specifications\n    │   └── auth/\n    │       └── oauth/\n    │           ├── spec.md\n    │           └── verify.md\n    ├── deltas/                # Structured YAML delta patches for existing specs\n    │   └── auth/\n    │       └── login/\n    │           └── spec.md.delta.yaml\n    ├── design.md              # Technical architecture, symbol impact, and trade-offs\n    └── tasks.md               # Granular implementation task checklist\n```\n\n### The Change Manifest (`manifest.yaml`)\n\nThe `manifest.yaml` is the control center of the change. It is managed automatically by the SpecD CLI and records:\n\n- **Identity:** Unique change name and human-readable description.\n- **State:** Current lifecycle step (e.g. `drafting`, `designing`, `implementing`, `verifying`).\n- **Target Specs (`specIds`):** List of canonical spec identifiers affected by this change (e.g. `auth:login`, `core:storage`).\n- **Artifact Tracking:** Content hashes and validation states for every required artifact. If an upstream artifact changes on disk after validation, SpecD flags it as `drifted` and requires review before advancing.\n- **History:** Chronological log of state transitions, actor timestamps, and approval signatures.\n\n---\n\n## 3. The Artifact Dependency DAG\n\nThe schema governing your project (such as `@specd/schema-std`) defines an explicit Directed Acyclic Graph (DAG) of required artifacts:\n\n```\n  proposal (scope: change)\n     │\n     ▼\n   specs (scope: spec)\n     │\n     ▼\n   verify (scope: spec)\n     │\n     ▼\n   design (scope: change)\n     │\n     ▼\n   tasks (scope: change)\n```\n\nEach artifact depends on its predecessor:\n\n- You cannot write a **design** until you have defined requirements in **specs** and acceptance criteria in **verify**.\n- You cannot write implementation **tasks** until you have planned your technical architecture in **design**.\n- You cannot transition to **`implementing`** until all upstream artifacts are validated and mark complete.\n\nThis guarantees that developers and AI agents never skip planning or jump straight to code without clear intent.\n\n---\n\n## 4. The Change Lifecycle\n\nA change transitions through a series of explicit states:\n\n```\n  create\n    │\n    ▼\n  drafting ──► designing ──► ready ──► implementing ──► verifying ──► done ──► archivable\n    │                                                                             │\n    │                                                                          archive\n    ▼\n  [pause: drafts]          [discard: discarded]\n```\n\n| Lifecycle State    | Meaning & Activities                                                                                                                                                        |\n| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| **`drafting`**     | The change is opened. The developer and AI assistant explore the problem and draft the initial `proposal.md`.                                                               |\n| **`designing`**    | Requirements are drafted into `specs/` or `deltas/`, acceptance scenarios are written in `verify.md`, and technical architecture is detailed in `design.md` and `tasks.md`. |\n| **`ready`**        | All design artifacts are complete and validated against the schema. Ready for implementation. (If configured, the Spec Approval Gate triggers here).                        |\n| **`implementing`** | Application code is modified. The agent checks off tasks in `tasks.md` one by one.                                                                                          |\n| **`verifying`**    | Code is complete. Tests and verification scenarios from `verify.md` are executed to prove compliance.                                                                       |\n| **`done`**         | Verification has passed. Ready for final review.                                                                                                                            |\n| **`archivable`**   | All required signoffs and preflight integrity checks have passed.                                                                                                           |\n| **`archived`**     | Spec deltas are merged into the living `specs/` directory; the change is moved to `.specd/archive/`.                                                                        |\n\n---\n\n## 5. Operations on Changes\n\n### Creating a Change\n\n```bash\n# Create a change targeting existing specs\nspecd changes create add-oauth --spec auth/login --description \"Add OAuth2 support\"\n```\n\n### Checking Status and the Artifact DAG\n\nThe `specd changes status` command renders the full visual DAG, blockers, and recommended next actions:\n\n```bash\nspecd changes status add-oauth\n```\n\n### Transitioning Lifecycle States\n\n```bash\n# Explicit transition\nspecd changes transition add-oauth implementing\n\n# Automatically resolve the next logical forward step\nspecd changes transition add-oauth --next\n```\n\n### Shelving & Pausing (`drafts/`)\n\nWhen urgent work interrupts an active change, you don't need to stash git changes or lose context:\n\n```bash\n# Shelve the active change\nspecd changes draft add-oauth --reason \"Pausing to address production hotfix\"\n\n# Restore it later\nspecd drafts restore add-oauth\n```\n\n### Discarding (`discarded/`)\n\nIf an architectural spike or idea is abandoned:\n\n```bash\nspecd changes discard add-oauth\n```\n\nThe change is moved to `.specd/discarded/` for auditability rather than being deleted.\n\n### Detecting Overlapping Changes\n\nWhen multiple developers or AI agents work concurrently in the same repository:\n\n```bash\nspecd changes check-overlap add-oauth\n```\n\nDetects if another active change is modifying the same specifications, preventing merge conflicts before they happen.\n\n---\n\n## 6. Archiving: The Final Step\n\nWhen a change reaches `archivable`, you archive it:\n\n```bash\nspecd changes archive add-oauth\n```\n\nArchiving executes an atomic transaction:\n\n1. **Delta Application:** Staged `.delta.yaml` patches are applied to the canonical `specs/` directory, updating the living specifications.\n2. **Integrity Validation:** SpecD validates that the resulting specs are parseable and satisfy all schema rules.\n3. **Permanent Record:** The entire change folder is moved from `.specd/changes/` to `.specd/archive/<date>-add-oauth/`.\n\nArchived changes remain in your repository as an immutable, permanent record of _why_ your system changed, _what_ was designed, and _how_ it was verified.\n",
    "lineCount": 192,
    "byteLength": 8394,
    "outline": [
      {
        "index": 1,
        "heading": "Changes in SpecD",
        "level": 1,
        "startLine": 1,
        "endLine": 192,
        "lines": 192,
        "content": "# Changes in SpecD\n\nIn SpecD, a **change** is the fundamental unit of work. Every modification to your system — whether creating a new capability, refactoring an existing service, or fixing a bug — is organized as an active change.\n\nThere is no \"code-only\" workflow in SpecD. Code never moves forward in isolation; it moves forward in lockstep with the specifications that govern it.\n\n---\n\n## 1. What is a Change?\n\nA change is an isolated, structured workspace that tracks the evolution of one or more specifications from initial intent through technical design, code implementation, and verification, to permanent archiving.\n\nA change answers four essential questions:\n\n1. **What intent is being addressed?** Captured in `proposal.md`.\n2. **Which specifications are being created or updated?** Declared in `specIds` and staged in `specs/` or `deltas/`.\n3. **How will it be built?** Planned in `design.md` and tracked in `tasks.md`.\n4. **Where does the work currently sit?** Tracked in `manifest.yaml` as an explicit lifecycle state.\n\nUnlike ephemeral git branches or informal issue tickets, a change in SpecD is a durable directory on disk containing both human-readable documentation and machine-verifiable metadata.\n\n---\n\n## 2. Anatomy of a Change Directory\n\nActive changes live in `.specd/changes/` under a timestamped directory slug:\n\n```\n.specd/changes/\n└── 20260923-144548-add-oauth-flow/\n    ├── manifest.yaml          # Machine-readable state, scope, and artifact baselines\n    ├── proposal.md            # Problem statement, intent, and user requirements\n    ├── specs/                 # Staged full artifacts for brand-new specifications\n    │   └── auth/\n    │       └── oauth/\n    │           ├── spec.md\n    │           └── verify.md\n    ├── deltas/                # Structured YAML delta patches for existing specs\n    │   └── auth/\n    │       └── login/\n    │           └── spec.md.delta.yaml\n    ├── design.md              # Technical architecture, symbol impact, and trade-offs\n    └── tasks.md               # Granular implementation task checklist\n```\n\n### The Change Manifest (`manifest.yaml`)\n\nThe `manifest.yaml` is the control center of the change. It is managed automatically by the SpecD CLI and records:\n\n- **Identity:** Unique change name and human-readable description.\n- **State:** Current lifecycle step (e.g. `drafting`, `designing`, `implementing`, `verifying`).\n- **Target Specs (`specIds`):** List of canonical spec identifiers affected by this change (e.g. `auth:login`, `core:storage`).\n- **Artifact Tracking:** Content hashes and validation states for every required artifact. If an upstream artifact changes on disk after validation, SpecD flags it as `drifted` and requires review before advancing.\n- **History:** Chronological log of state transitions, actor timestamps, and approval signatures.\n\n---\n\n## 3. The Artifact Dependency DAG\n\nThe schema governing your project (such as `@specd/schema-std`) defines an explicit Directed Acyclic Graph (DAG) of required artifacts:\n\n```\n  proposal (scope: change)\n     │\n     ▼\n   specs (scope: spec)\n     │\n     ▼\n   verify (scope: spec)\n     │\n     ▼\n   design (scope: change)\n     │\n     ▼\n   tasks (scope: change)\n```\n\nEach artifact depends on its predecessor:\n\n- You cannot write a **design** until you have defined requirements in **specs** and acceptance criteria in **verify**.\n- You cannot write implementation **tasks** until you have planned your technical architecture in **design**.\n- You cannot transition to **`implementing`** until all upstream artifacts are validated and mark complete.\n\nThis guarantees that developers and AI agents never skip planning or jump straight to code without clear intent.\n\n---\n\n## 4. The Change Lifecycle\n\nA change transitions through a series of explicit states:\n\n```\n  create\n    │\n    ▼\n  drafting ──► designing ──► ready ──► implementing ──► verifying ──► done ──► archivable\n    │                                                                             │\n    │                                                                          archive\n    ▼\n  [pause: drafts]          [discard: discarded]\n```\n\n| Lifecycle State    | Meaning & Activities                                                                                                                                                        |\n| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| **`drafting`**     | The change is opened. The developer and AI assistant explore the problem and draft the initial `proposal.md`.                                                               |\n| **`designing`**    | Requirements are drafted into `specs/` or `deltas/`, acceptance scenarios are written in `verify.md`, and technical architecture is detailed in `design.md` and `tasks.md`. |\n| **`ready`**        | All design artifacts are complete and validated against the schema. Ready for implementation. (If configured, the Spec Approval Gate triggers here).                        |\n| **`implementing`** | Application code is modified. The agent checks off tasks in `tasks.md` one by one.                                                                                          |\n| **`verifying`**    | Code is complete. Tests and verification scenarios from `verify.md` are executed to prove compliance.                                                                       |\n| **`done`**         | Verification has passed. Ready for final review.                                                                                                                            |\n| **`archivable`**   | All required signoffs and preflight integrity checks have passed.                                                                                                           |\n| **`archived`**     | Spec deltas are merged into the living `specs/` directory; the change is moved to `.specd/archive/`.                                                                        |\n\n---\n\n## 5. Operations on Changes\n\n### Creating a Change\n\n```bash\n# Create a change targeting existing specs\nspecd changes create add-oauth --spec auth/login --description \"Add OAuth2 support\"\n```\n\n### Checking Status and the Artifact DAG\n\nThe `specd changes status` command renders the full visual DAG, blockers, and recommended next actions:\n\n```bash\nspecd changes status add-oauth\n```\n\n### Transitioning Lifecycle States\n\n```bash\n# Explicit transition\nspecd changes transition add-oauth implementing\n\n# Automatically resolve the next logical forward step\nspecd changes transition add-oauth --next\n```\n\n### Shelving & Pausing (`drafts/`)\n\nWhen urgent work interrupts an active change, you don't need to stash git changes or lose context:\n\n```bash\n# Shelve the active change\nspecd changes draft add-oauth --reason \"Pausing to address production hotfix\"\n\n# Restore it later\nspecd drafts restore add-oauth\n```\n\n### Discarding (`discarded/`)\n\nIf an architectural spike or idea is abandoned:\n\n```bash\nspecd changes discard add-oauth\n```\n\nThe change is moved to `.specd/discarded/` for auditability rather than being deleted.\n\n### Detecting Overlapping Changes\n\nWhen multiple developers or AI agents work concurrently in the same repository:\n\n```bash\nspecd changes check-overlap add-oauth\n```\n\nDetects if another active change is modifying the same specifications, preventing merge conflicts before they happen.\n\n---\n\n## 6. Archiving: The Final Step\n\nWhen a change reaches `archivable`, you archive it:\n\n```bash\nspecd changes archive add-oauth\n```\n\nArchiving executes an atomic transaction:\n\n1. **Delta Application:** Staged `.delta.yaml` patches are applied to the canonical `specs/` directory, updating the living specifications.\n2. **Integrity Validation:** SpecD validates that the resulting specs are parseable and satisfy all schema rules.\n3. **Permanent Record:** The entire change folder is moved from `.specd/changes/` to `.specd/archive/<date>-add-oauth/`.\n\nArchived changes remain in your repository as an immutable, permanent record of _why_ your system changed, _what_ was designed, and _how_ it was verified.\n"
      },
      {
        "index": 2,
        "heading": "1. What is a Change?",
        "level": 2,
        "startLine": 9,
        "endLine": 23,
        "lines": 15,
        "content": "## 1. What is a Change?\n\nA change is an isolated, structured workspace that tracks the evolution of one or more specifications from initial intent through technical design, code implementation, and verification, to permanent archiving.\n\nA change answers four essential questions:\n\n1. **What intent is being addressed?** Captured in `proposal.md`.\n2. **Which specifications are being created or updated?** Declared in `specIds` and staged in `specs/` or `deltas/`.\n3. **How will it be built?** Planned in `design.md` and tracked in `tasks.md`.\n4. **Where does the work currently sit?** Tracked in `manifest.yaml` as an explicit lifecycle state.\n\nUnlike ephemeral git branches or informal issue tickets, a change in SpecD is a durable directory on disk containing both human-readable documentation and machine-verifiable metadata.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "2. Anatomy of a Change Directory",
        "level": 2,
        "startLine": 24,
        "endLine": 57,
        "lines": 34,
        "content": "## 2. Anatomy of a Change Directory\n\nActive changes live in `.specd/changes/` under a timestamped directory slug:\n\n```\n.specd/changes/\n└── 20260923-144548-add-oauth-flow/\n    ├── manifest.yaml          # Machine-readable state, scope, and artifact baselines\n    ├── proposal.md            # Problem statement, intent, and user requirements\n    ├── specs/                 # Staged full artifacts for brand-new specifications\n    │   └── auth/\n    │       └── oauth/\n    │           ├── spec.md\n    │           └── verify.md\n    ├── deltas/                # Structured YAML delta patches for existing specs\n    │   └── auth/\n    │       └── login/\n    │           └── spec.md.delta.yaml\n    ├── design.md              # Technical architecture, symbol impact, and trade-offs\n    └── tasks.md               # Granular implementation task checklist\n```\n\n### The Change Manifest (`manifest.yaml`)\n\nThe `manifest.yaml` is the control center of the change. It is managed automatically by the SpecD CLI and records:\n\n- **Identity:** Unique change name and human-readable description.\n- **State:** Current lifecycle step (e.g. `drafting`, `designing`, `implementing`, `verifying`).\n- **Target Specs (`specIds`):** List of canonical spec identifiers affected by this change (e.g. `auth:login`, `core:storage`).\n- **Artifact Tracking:** Content hashes and validation states for every required artifact. If an upstream artifact changes on disk after validation, SpecD flags it as `drifted` and requires review before advancing.\n- **History:** Chronological log of state transitions, actor timestamps, and approval signatures.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "The Change Manifest (`manifest.yaml`)",
        "level": 3,
        "startLine": 46,
        "endLine": 57,
        "lines": 12,
        "content": "### The Change Manifest (`manifest.yaml`)\n\nThe `manifest.yaml` is the control center of the change. It is managed automatically by the SpecD CLI and records:\n\n- **Identity:** Unique change name and human-readable description.\n- **State:** Current lifecycle step (e.g. `drafting`, `designing`, `implementing`, `verifying`).\n- **Target Specs (`specIds`):** List of canonical spec identifiers affected by this change (e.g. `auth:login`, `core:storage`).\n- **Artifact Tracking:** Content hashes and validation states for every required artifact. If an upstream artifact changes on disk after validation, SpecD flags it as `drifted` and requires review before advancing.\n- **History:** Chronological log of state transitions, actor timestamps, and approval signatures.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "3. The Artifact Dependency DAG",
        "level": 2,
        "startLine": 58,
        "endLine": 87,
        "lines": 30,
        "content": "## 3. The Artifact Dependency DAG\n\nThe schema governing your project (such as `@specd/schema-std`) defines an explicit Directed Acyclic Graph (DAG) of required artifacts:\n\n```\n  proposal (scope: change)\n     │\n     ▼\n   specs (scope: spec)\n     │\n     ▼\n   verify (scope: spec)\n     │\n     ▼\n   design (scope: change)\n     │\n     ▼\n   tasks (scope: change)\n```\n\nEach artifact depends on its predecessor:\n\n- You cannot write a **design** until you have defined requirements in **specs** and acceptance criteria in **verify**.\n- You cannot write implementation **tasks** until you have planned your technical architecture in **design**.\n- You cannot transition to **`implementing`** until all upstream artifacts are validated and mark complete.\n\nThis guarantees that developers and AI agents never skip planning or jump straight to code without clear intent.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "4. The Change Lifecycle",
        "level": 2,
        "startLine": 88,
        "endLine": 115,
        "lines": 28,
        "content": "## 4. The Change Lifecycle\n\nA change transitions through a series of explicit states:\n\n```\n  create\n    │\n    ▼\n  drafting ──► designing ──► ready ──► implementing ──► verifying ──► done ──► archivable\n    │                                                                             │\n    │                                                                          archive\n    ▼\n  [pause: drafts]          [discard: discarded]\n```\n\n| Lifecycle State    | Meaning & Activities                                                                                                                                                        |\n| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| **`drafting`**     | The change is opened. The developer and AI assistant explore the problem and draft the initial `proposal.md`.                                                               |\n| **`designing`**    | Requirements are drafted into `specs/` or `deltas/`, acceptance scenarios are written in `verify.md`, and technical architecture is detailed in `design.md` and `tasks.md`. |\n| **`ready`**        | All design artifacts are complete and validated against the schema. Ready for implementation. (If configured, the Spec Approval Gate triggers here).                        |\n| **`implementing`** | Application code is modified. The agent checks off tasks in `tasks.md` one by one.                                                                                          |\n| **`verifying`**    | Code is complete. Tests and verification scenarios from `verify.md` are executed to prove compliance.                                                                       |\n| **`done`**         | Verification has passed. Ready for final review.                                                                                                                            |\n| **`archivable`**   | All required signoffs and preflight integrity checks have passed.                                                                                                           |\n| **`archived`**     | Spec deltas are merged into the living `specs/` directory; the change is moved to `.specd/archive/`.                                                                        |\n\n---\n"
      },
      {
        "index": 7,
        "heading": "5. Operations on Changes",
        "level": 2,
        "startLine": 116,
        "endLine": 176,
        "lines": 61,
        "content": "## 5. Operations on Changes\n\n### Creating a Change\n\n```bash\n# Create a change targeting existing specs\nspecd changes create add-oauth --spec auth/login --description \"Add OAuth2 support\"\n```\n\n### Checking Status and the Artifact DAG\n\nThe `specd changes status` command renders the full visual DAG, blockers, and recommended next actions:\n\n```bash\nspecd changes status add-oauth\n```\n\n### Transitioning Lifecycle States\n\n```bash\n# Explicit transition\nspecd changes transition add-oauth implementing\n\n# Automatically resolve the next logical forward step\nspecd changes transition add-oauth --next\n```\n\n### Shelving & Pausing (`drafts/`)\n\nWhen urgent work interrupts an active change, you don't need to stash git changes or lose context:\n\n```bash\n# Shelve the active change\nspecd changes draft add-oauth --reason \"Pausing to address production hotfix\"\n\n# Restore it later\nspecd drafts restore add-oauth\n```\n\n### Discarding (`discarded/`)\n\nIf an architectural spike or idea is abandoned:\n\n```bash\nspecd changes discard add-oauth\n```\n\nThe change is moved to `.specd/discarded/` for auditability rather than being deleted.\n\n### Detecting Overlapping Changes\n\nWhen multiple developers or AI agents work concurrently in the same repository:\n\n```bash\nspecd changes check-overlap add-oauth\n```\n\nDetects if another active change is modifying the same specifications, preventing merge conflicts before they happen.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Creating a Change",
        "level": 3,
        "startLine": 118,
        "endLine": 124,
        "lines": 7,
        "content": "### Creating a Change\n\n```bash\n# Create a change targeting existing specs\nspecd changes create add-oauth --spec auth/login --description \"Add OAuth2 support\"\n```\n"
      },
      {
        "index": 9,
        "heading": "Checking Status and the Artifact DAG",
        "level": 3,
        "startLine": 125,
        "endLine": 132,
        "lines": 8,
        "content": "### Checking Status and the Artifact DAG\n\nThe `specd changes status` command renders the full visual DAG, blockers, and recommended next actions:\n\n```bash\nspecd changes status add-oauth\n```\n"
      },
      {
        "index": 10,
        "heading": "Transitioning Lifecycle States",
        "level": 3,
        "startLine": 133,
        "endLine": 142,
        "lines": 10,
        "content": "### Transitioning Lifecycle States\n\n```bash\n# Explicit transition\nspecd changes transition add-oauth implementing\n\n# Automatically resolve the next logical forward step\nspecd changes transition add-oauth --next\n```\n"
      },
      {
        "index": 11,
        "heading": "Shelving & Pausing (`drafts/`)",
        "level": 3,
        "startLine": 143,
        "endLine": 154,
        "lines": 12,
        "content": "### Shelving & Pausing (`drafts/`)\n\nWhen urgent work interrupts an active change, you don't need to stash git changes or lose context:\n\n```bash\n# Shelve the active change\nspecd changes draft add-oauth --reason \"Pausing to address production hotfix\"\n\n# Restore it later\nspecd drafts restore add-oauth\n```\n"
      },
      {
        "index": 12,
        "heading": "Discarding (`discarded/`)",
        "level": 3,
        "startLine": 155,
        "endLine": 164,
        "lines": 10,
        "content": "### Discarding (`discarded/`)\n\nIf an architectural spike or idea is abandoned:\n\n```bash\nspecd changes discard add-oauth\n```\n\nThe change is moved to `.specd/discarded/` for auditability rather than being deleted.\n"
      },
      {
        "index": 13,
        "heading": "Detecting Overlapping Changes",
        "level": 3,
        "startLine": 165,
        "endLine": 176,
        "lines": 12,
        "content": "### Detecting Overlapping Changes\n\nWhen multiple developers or AI agents work concurrently in the same repository:\n\n```bash\nspecd changes check-overlap add-oauth\n```\n\nDetects if another active change is modifying the same specifications, preventing merge conflicts before they happen.\n\n---\n"
      },
      {
        "index": 14,
        "heading": "6. Archiving: The Final Step",
        "level": 2,
        "startLine": 177,
        "endLine": 192,
        "lines": 16,
        "content": "## 6. Archiving: The Final Step\n\nWhen a change reaches `archivable`, you archive it:\n\n```bash\nspecd changes archive add-oauth\n```\n\nArchiving executes an atomic transaction:\n\n1. **Delta Application:** Staged `.delta.yaml` patches are applied to the canonical `specs/` directory, updating the living specifications.\n2. **Integrity Validation:** SpecD validates that the resulting specs are parseable and satisfy all schema rules.\n3. **Permanent Record:** The entire change folder is moved from `.specd/changes/` to `.specd/archive/<date>-add-oauth/`.\n\nArchived changes remain in your repository as an immutable, permanent record of _why_ your system changed, _what_ was designed, and _how_ it was verified.\n"
      }
    ]
  },
  {
    "topic": "configuration",
    "title": "Configuring Your SpecD Project",
    "description": "Practical guide to specd.yaml configuration, workspaces, adapters, and the layered config cascade.",
    "order": 4,
    "content": "# Configuring Your SpecD Project\n\n`specd.yaml` is the single configuration file for a SpecD project. Every tool in the SpecD ecosystem — the CLI, the MCP server, and agent plugins — reads it to understand where your specs live, how changes are stored, and what rules govern your workflow.\n\nThis guide walks through all configuration areas conceptually and provides a complete field-by-field reference. For real-world scenario examples and multi-workspace architectures, see [Configuration Examples](./configuration-examples.md).\n\n---\n\n## Getting a specd.yaml\n\nRun `specd project init` in your project root. SpecD creates a `specd.yaml` with sensible defaults for a single-repo project. You will then edit it to reflect your team's structure and policies.\n\nSpecD discovers its configuration by walking up from the current working directory to the active VCS root, collecting all `specd.yaml`, `specd.*.yaml`, `specd.local.yaml`, and `specd.local.*.yaml` candidate files from the first directory that contains at least one. The candidates are resolved into an active chain, deep-merged, and validated as one config. See [Config cascade and local variants](#config-cascade-and-local-variants) below for details.\n\n---\n\n## The minimal configuration\n\nThis is the smallest valid `specd.yaml`. It covers everything SpecD needs to operate:\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\nEvery other field covered in this guide is optional. Start here and add what you need.\n\n---\n\n## Logging\n\nSpecD supports project-level logging settings in `specd.yaml`:\n\n```yaml\nlogging:\n  level: info # trace | debug | info | warn | error | silent\n```\n\n- The section is optional.\n- If omitted, SpecD defaults to `info`.\n- Logs are written to `{configPath}/log/specd.log` as structured JSON.\n- CLI runtime verbosity can be increased with `-v` / `-vv` for console output without changing file-level defaults.\n\n---\n\n## Environment variables and .env support\n\nSpecD natively supports loading environment variables from `.env` and `.env.local` files in your project root. `.env.local` has higher priority and should be used for secrets that must never be committed (like privacy salts).\n\nThe following variables map directly to root-level configuration settings and **override** values in `specd.yaml` or `specd.local.yaml`:\n\n| Variable               | Configuration Field   |\n| ---------------------- | --------------------- |\n| `SPECD_ACTOR_PROVIDER` | `actorProvider`       |\n| `SPECD_PRIVACY_MODE`   | `privacy.mode`        |\n| `SPECD_PRIVACY_SALT`   | `privacy.salt`        |\n| `SPECD_LOG_LEVEL`      | `logging.level`       |\n| `SPECD_CONTEXT_MODE`   | `contextMode`         |\n| `SPECD_LLM_OPTIMIZED`  | `llmOptimizedContext` |\n| `SPECD_SCHEMA`         | `schemaRef`           |\n\n---\n\n## Identity resolution\n\nSpecD automatically identifies the user performing an operation by probing the environment. By default, it prioritizes Version Control Systems (Git, Mercurial, Subversion) to resolve the actor's name and email.\n\n### Manual provider selection\n\nIf you need to bypass auto-detection and force a specific identity provider, use the `actorProvider` field:\n\n```yaml\nactorProvider: git # forces git even if other systems are detected\n```\n\nThis is useful in environments where multiple systems might be present or when using a custom identity plugin.\n\n---\n\n## Privacy\n\nFor projects in public repositories, you might want to avoid exposing real names or emails in change manifests and archives. SpecD provides built-in privacy modes to obfuscate identity data:\n\n```yaml\nprivacy:\n  mode: mask # hash | mask | anonymous\n  excludeActors: # optional: skip obfuscation for these actors\n    - 'specd'\n    - 'system@getspecd.dev'\n  allowedMetadataKeys: # optional: preserve specific metadata keys\n    - 'department'\n```\n\n- **`mode`**: Obfuscation mode (`hash`, `mask`, or `anonymous`). _Cascade:_ **Replaces**.\n- **`salt`**: Secret salt string required when `mode` is `hash`. _Cascade:_ **Replaces**.\n- **`excludeActors`**: Actor names or emails exempt from obfuscation.\n  - _Default:_ Built-in defaults (`'specd'`, `'system@getspecd.dev'`).\n  - _Engine replacement:_ When specified in configuration, it **replaces** the built-in defaults entirely (it does not append to them). If you specify custom excluded actors, you must explicitly include `'specd'` or `'system@getspecd.dev'` if you still want them exempt.\n  - _Cascade behavior:_ Array — in configuration cascade, an overlay config file (`specd.local.yaml`) **appends** to the base config file's list.\n- **`allowedMetadataKeys`**: Whitelist of metadata keys preserved under active privacy modes.\n  - _Default:_ None (all metadata stripped).\n  - _Cascade behavior:_ Array — an overlay config file **appends** to the base config file's list.\n\n### Privacy modes\n\n| Mode        | Effect                                                                    |\n| ----------- | ------------------------------------------------------------------------- |\n| `hash`      | Obfuscates email using HMAC-SHA256 with a salt. Requires a `salt`.        |\n| `mask`      | Partially masks name and email (e.g., `j***z@e***.com`).                  |\n| `anonymous` | Replaces all identity data with \"Anonymous\" and `anonymous@getspecd.dev`. |\n\nWhen using `hash` mode, you **must** provide a salt, preferably via the `SPECD_PRIVACY_SALT` environment variable.\n\n### Metadata privacy\n\nBy default, any active privacy mode removes the internal `providerId` and all `metadata` fields to prevent accidental PII leakage. Use `allowedMetadataKeys` to whitelist specific non-sensitive keys you wish to preserve.\n\n---\n\n## Runtime and state paths: specdPath and configPath\n\nSpecD distinguishes between workflow artifact storage and runtime operational state:\n\n```yaml\n# Directory for workflow state: changes, drafts, discarded, archive\nspecdPath: .specd # default: .specd\n\n# Directory for runtime-owned state: graph indexes, scratch files, logs\nconfigPath: .specd/config # default: .specd/config\n```\n\n- **`specdPath`**: Sets the root path for SpecD lifecycle state. Storage bindings (`changes/`, `drafts/`, `discarded/`, `archive/`) resolve relative to this directory.\n  - _Default:_ `.specd`\n  - _Cascade behavior:_ Scalar value — an overlay config **replaces** the base value.\n- **`configPath`**: Sets the root path for backend-owned runtime data. Kept separate from workflow artifacts so caches and logs can be cleaned without touching spec or change history.\n  - `{configPath}/graph` for persisted graph backend databases and indexes.\n  - `{configPath}/tmp` for graph staging and temporary scratch files.\n  - `{configPath}/log/specd.log` for structured logging.\n  - _Default:_ `.specd/config`\n  - _Cascade behavior:_ Scalar value — an overlay config **replaces** the base value.\n\n---\n\n## Config cascade and local variants\n\nSpecD supports a layered config cascade. Multiple YAML files in the same directory are discovered, resolved into an active chain, deep-merged, and validated as one.\n\n### File naming and discovery order\n\nWhen SpecD searches for configuration, it looks for files in this order within the directory containing `specd.yaml`:\n\n1. `specd.yaml` — the committed project root (always the first active layer)\n2. `specd.*.yaml` — named shared variants (e.g. `specd.ci.yaml`, `specd.staging.yaml`), sorted lexicographically\n3. `specd.local.yaml` — personal local override\n4. `specd.local.*.yaml` — named local variants (e.g. `specd.local.mono.yaml`), sorted lexicographically\n\nDiscovery walks up from the current working directory to the active VCS root and stops at the first directory containing at least one candidate file. `specd.yaml` must exist for discovery to succeed.\n\n### Cascade resolution\n\nEach variant file may declare an `extends` key:\n\n| `extends` value | Behaviour                                                                                         |\n| --------------- | ------------------------------------------------------------------------------------------------- |\n| absent          | File becomes a **standalone root** — all previous layers are discarded and this file starts fresh |\n| `true`          | File inherits from the previous active layer (or from `specd.yaml` if it is the first overlay)    |\n| `<path>`        | File inherits from the named base file, but **only if that base is already in the active chain**  |\n\nResolution builds an active chain starting from `specd.yaml`. Each subsequent candidate is either attached (extends a layer in the chain) or skipped (its base is not active). A file with no `extends` becomes a new standalone root — everything before it is discarded.\n\n### Forced mode (`--config`)\n\nPassing `--config path/to/file.yaml` loads that file as a self-contained closed chain. If the file declares `extends: true`, it resolves against `specd.yaml` in the same directory. If it declares `extends: <path>`, the chain follows explicit links until no file extends further.\n\n### Merge semantics\n\nLayers in the active chain are deep-merged in order:\n\n- **Scalars** — later values replace earlier ones\n- **Objects** — recursively merged\n- **Arrays** — later arrays **append** to earlier ones (no replacement)\n\n### Removals\n\nAn overlay layer may declare a `remove` block to strip entries inherited from prior layers:\n\n```yaml\nextends: true\nremove:\n  root:\n    - contextExcludeSpecs\n  workspaces:\n    - staging\n  context:\n    - id: ci-only-instruction\n    - file: CI_AGENTS.md\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n| Removal target   | Keyed by                       | Effect                                |\n| ---------------- | ------------------------------ | ------------------------------------- |\n| `root`           | field name                     | Removes the named top-level field     |\n| `workspaces`     | workspace name                 | Removes the named workspace           |\n| `storage`        | storage key                    | Removes a storage binding             |\n| `context`        | `id`, `file`, or `instruction` | Removes matching context entries      |\n| `plugins.agents` | `name`                         | Removes a matching plugin declaration |\n\nRemovals are applied immediately after the layer that declares them is merged. A `remove` block without `extends` is an error.\n\n### Gitignored local files\n\n`specd project init` adds both `specd.local.yaml` and `specd.local.*.yaml` to `.gitignore` automatically. Local variants are for personal experimentation: trying a different schema branch, pointing at a local schema copy, or testing a configuration change without affecting the rest of the team.\n\n### Common patterns\n\n**Personal local override:**\n\n```yaml\n# specd.local.yaml\nextends: true\nschema: './my-local-schema'\n```\n\n**CI-specific variant:**\n\n```yaml\n# specd.ci.yaml\nextends: true\ncontext:\n  - instruction: 'Running in CI. Skip interactive prompts.'\nremove:\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n**Standalone local config (fresh start, no inheritance):**\n\n```yaml\n# specd.local.yaml — no extends key\nschema: '@specd/schema-std'\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n```\n\n### Bootstrap mode exceptions (`--path`)\n\nSome `specd graph` CLI commands support an unconfigured repository bootstrap mode for initial indexing and exploration before a project has a `specd.yaml`:\n\n- Passing `--path <repo-root>` forces bootstrap mode and is mutually exclusive with `--config`.\n- When no `specd.yaml` is discovered in the directory walk up to the VCS root, those graph commands fall back to bootstrap mode automatically.\n\nIn bootstrap mode, SpecD behaves as if the repository were a single synthetic `default` workspace with `codeRoot` set to the VCS root. Any discovered `specd.yaml` is ignored when `--path` is explicitly provided. Bootstrap mode is intended for exploratory graph queries; once a project is initialized with `specd project init`, standard configured execution should always be preferred.\n\n---\n\n## Schema selection\n\nThe `schema` field tells SpecD which workflow schema to use. Schemas define your artifact types, lifecycle steps, and validation rules. There is exactly one active schema per project.\n\nSpecD resolves the `schema` value using a prefix convention:\n\n| Value                            | Where SpecD looks                                                               |\n| -------------------------------- | ------------------------------------------------------------------------------- |\n| `'@specd/schema-std'`            | npm package in `node_modules/@specd/schema-std/schema.yaml`                     |\n| `'my-workflow'`                  | Bare name — `.specd/schemas/my-workflow/schema.yaml` in the default workspace   |\n| `'#my-workflow'`                 | Hash prefix — equivalent to bare name, explicit about resolving from `default`  |\n| `'#billing:my-schema'`           | Workspace-qualified — resolves from the `billing` workspace's schemas directory |\n| `'./schemas/custom/schema.yaml'` | Relative path from the `specd.yaml` directory                                   |\n\nMost projects start with `@specd/schema-std`. If you need a workflow that differs substantially from the standard one, you can author a local schema and reference it by name. Local schemas live inside your repository and evolve with your project.\n\nTo customise the standard schema without forking it, use `schemaOverrides` — covered below.\n\n---\n\n## Workspaces\n\nA workspace tells SpecD where a set of specs lives and what relationship the project has with them.\n\nEvery `specd.yaml` must declare a `default` workspace. This is the local project workspace — the one the current repository owns. Additional workspaces reference external repositories or sub-directories within a monorepo.\n\n### The default workspace\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThis is the minimum. Two useful optional fields:\n\n- **`codeRoot`** — the directory where implementation code lives, relative to `specd.yaml`. Defaults to the project root. Useful if your code lives in a subdirectory.\n- **`ownership`** — defaults to `owned`, meaning this project freely proposes changes to these specs. The default is almost always correct for the `default` workspace.\n\n### Additional workspaces\n\nIn a monorepo or multi-repo setup, you can declare workspaces that point elsewhere:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/payments/specs\n    codeRoot: packages/payments\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\n`codeRoot` is required for any workspace that is not `default` — there is no sensible default.\n\nThe `ownership` field describes the project's relationship with each workspace's specs:\n\n| Value      | Meaning                                                        |\n| ---------- | -------------------------------------------------------------- |\n| `owned`    | This project freely proposes and modifies specs here.          |\n| `shared`   | Co-owned; changes may require coordination with other teams.   |\n| `readOnly` | This project reads specs for context but does not modify them. |\n\n`readOnly` is the default for non-`default` workspaces. Use it for external dependencies whose specs you want in context but do not control.\n\n### Workspace prefixes\n\nSpec IDs always use the form `workspace:capability-path`. A spec in the `payments` workspace at `specs/checkout.md` has the ID `payments:checkout`. If you want every spec in a workspace to live under a leading path segment — for example to mirror a directory structure — you can declare `prefix`:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture`.\n\n`prefix` does not replace the workspace name in the spec ID. It prepends a path segment to the capability-path portion. In other words:\n\n- workspace name: `default`\n- prefix: `_global`\n- resulting spec ID: `default:_global/architecture`\n\nConcrete example:\n\n- `specsPath` is `specs/_global`\n- the spec lives on disk at `specs/_global/architecture`\n- relative to the workspace root, the spec path is just `architecture`\n\nWithout `prefix`, the spec ID would therefore be `default:architecture`. `prefix: _global` exists precisely to add that lost leading path segment back into the capability-path, producing `default:_global/architecture`.\n\n- _Default:_ None (bare capability paths).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base prefix.\n\n### Workspace schemas\n\nWorkspaces can optionally declare their own schema repository using the `schemas` adapter binding:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas\n```\n\n- When declared, schemas in that directory can be referenced via `#workspace:schema-name` (or `#schema-name` for `default`).\n- _Default:_ `null` (no local workspace schemas; schema references targeting workspaces without `schemas` fail validation).\n- _Cascade behavior:_ Object — deep merged with base workspace config.\n\n### Workspace-level context specs\n\nWorkspaces can define targeted spec inclusion and exclusion filters that activate only when that workspace is touched by a change:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/billing/specs\n    contextIncludeSpecs:\n      - 'billing:api/*' # include billing API specs when billing is active\n    contextExcludeSpecs:\n      - 'billing:internal/*' # exclude internal billing specs\n```\n\n- **`contextIncludeSpecs`**: Patterns matching specs in this workspace that must be included when active.\n  - _Default:_ None (only specs explicitly declared in the change and their dependencies are included).\n  - _Cascade behavior:_ Array — **appends** to inherited include patterns.\n- **`contextExcludeSpecs`**: Patterns matching specs in this workspace to exclude from context.\n  - _Default:_ None.\n  - _Cascade behavior:_ Array — **appends** to inherited exclude patterns.\n\n### Workspace code graph settings\n\nEach workspace can fine-tune its code graph discovery inside its `codeRoot`:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    graph:\n      respectGitignore: true # default: true\n      excludePaths:\n        - 'dist/**'\n        - 'coverage/**'\n      allowedPaths:\n        - 'src/**'\n```\n\n- **`respectGitignore`**: Whether `.gitignore` files inside `codeRoot` are respected during symbol and file indexing.\n  - _Default:_ `true`. When set to `false`, files ignored by Git are still discovered unless matched by `excludePaths`.\n  - _Cascade behavior:_ Scalar boolean — **replaces** the base setting.\n- **`excludePaths`**: Additional gitignore-syntax exclusion patterns relative to `codeRoot`.\n  - _Default:_ None (inherits project-level and built-in defaults).\n  - _Cascade behavior:_ Array — **appends** to project-level and workspace-level excludes.\n\n  **Monorepo child workspace isolation pattern:**\n  In monorepos where `workspaces.default.codeRoot` is `.` (the repository root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` pointing to subdirectories (e.g. `packages/core`, `packages/cli`, `apps/web`), the default workspace would physically crawl and index those directories as well. This leads to duplicate symbol definitions and index collisions.\n\n  To prevent this, configure `workspaces.default.graph.excludePaths` to explicitly exclude child workspace directories:\n\n  ```yaml\n  workspaces:\n    default:\n      prefix: _global\n      specs:\n        adapter: fs\n        fs:\n          path: specs/_global\n      codeRoot: .\n      graph:\n        excludePaths:\n          - node_modules/\n          - .git/\n          - .specd/\n          - dist/\n          - build/\n          - coverage/\n          - packages/ # Indexed independently by child workspaces\n          - apps/ # Indexed independently by child workspaces\n          - specs/\n          - specd-sdd/\n\n    core:\n      specs:\n        adapter: fs\n        fs:\n          path: specs/core\n      codeRoot: packages/core\n  ```\n\n- **`allowedPaths`**: Allowlist restricting graph indexing to specific subdirectories within `codeRoot`.\n  - _Default:_ None (entire `codeRoot` is visible to graph).\n  - _Cascade behavior:_ Array — **appends**.\n\n---\n\n## Storage\n\n`storage` declares where SpecD persists changes during their lifecycle. All four sub-keys are required.\n\n```yaml\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\nEach directory holds changes in a different state:\n\n- **`changes`** — active changes currently in progress\n- **`drafts`** — shelved changes that can be restored at any time\n- **`discarded`** — abandoned changes, kept for reference but no longer active\n- **`archive`** — completed changes; the permanent record after archiving\n\nAll paths resolve relative to the `specd.yaml` directory and must stay within the repository root.\n\n### Adapter bindings and kernel extensions\n\nEvery workspace and storage declaration is a named adapter binding:\n\n```yaml\nspecs:\n  adapter: fs\n  fs:\n    path: specs/\n```\n\nThe built-in path is `fs`, but `createKernel(config, options)` and `createKernelBuilder(config)` can register additive storage factories under other adapter names. The config loader preserves the selected adapter name and its opaque config block; the kernel then validates that the named factory exists in the merged registry.\n\nThat split is deliberate:\n\n- `FsConfigLoader` resolves and validates `fs` paths\n- the kernel validates whether non-built-in adapter names are actually registered\n\nUse this when you need a custom storage backend without forking the core workflow model.\n\nBy default, `specd project init` adds `.specd/drafts/` and `.specd/discarded/` to `.gitignore`. Teams who want to commit drafts — for example, to share in-progress work across machines — can remove those entries.\n\n### Organising the archive\n\nBy default, archived changes are stored with the name `{{change.archivedName}}` — a slug prefixed with the archive date, for example `2024-01-15-add-auth-flow`. You can customise this with the `pattern` field:\n\n```yaml\narchive:\n  adapter: fs\n  fs:\n    path: .specd/archive\n    pattern: '{{year}}/{{change.archivedName}}'\n```\n\nThis organises archived changes into yearly subdirectories.\n\n| Variable                  | Value                                    | Example                    |\n| :------------------------ | :--------------------------------------- | :------------------------- |\n| `{{change.archivedName}}` | Full archived directory name (default)   | `2024-01-15-add-auth-flow` |\n| `{{change.name}}`         | Change slug name                         | `add-auth-flow`            |\n| `{{year}}`                | Four-digit calendar year at archive time | `2024`                     |\n| `{{date}}`                | ISO calendar date at archive time        | `2024-01-15`               |\n\n> [!NOTE]\n> `{{change.workspace}}` is not supported because changes in SpecD can touch multiple workspaces simultaneously and have no single primary workspace. Use `{{change.name}}` or `{{change.archivedName}}` instead.\n\n---\n\n## Context configuration\n\nSpecD compiles a context block for the agent at each lifecycle step. The context includes relevant specs, schema instructions, and any additional content you inject here.\n\n### Injecting files and instructions\n\nThe `context` field at the top level injects content before any spec content, for every change in the project:\n\n```yaml\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Always prefer editing existing files over creating new ones.'\n```\n\n- `file` entries are read at compile time and injected verbatim. If the file does not exist, a warning is emitted and the entry is skipped.\n- `instruction` entries are injected as-is.\n\nThis is the right place for project-wide agent guidance — coding conventions, team norms, architectural principles.\n\n### Controlling which specs are included\n\nBy default, only the specs declared by the change and their explicit dependencies are included in context. You can expand or narrow this with `contextIncludeSpecs` and `contextExcludeSpecs`.\n\nThese can be declared at two levels:\n\n- **Project level** — patterns apply to every compiled context, regardless of which change is active. Use this for specs that should always be present: global constraints, cross-cutting architecture specs.\n- **Workspace level** — patterns apply only when that workspace is active in the current change (i.e., at least one of its specs is listed in the change).\n\n```yaml\n# Always include global constraints\ncontextIncludeSpecs:\n  - 'default:_global/*'\n\n# Never include commit message guidelines (not relevant to agents)\ncontextExcludeSpecs:\n  - 'default:commits'\n```\n\nPattern syntax:\n\n| Pattern              | Matches                                                                                       |\n| -------------------- | --------------------------------------------------------------------------------------------- |\n| `*`                  | All specs in all workspaces (at project level) or this workspace (at workspace level)         |\n| `workspace:*`        | All specs in the named workspace                                                              |\n| `prefix/*`           | All specs under `prefix/`                                                                     |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace                                              |\n| `path/name`          | Exact spec path (resolved from `default` at project level, this workspace at workspace level) |\n\n`*` is only valid in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\n### Spec collection order and priority\n\nDuring context compilation, SpecD collects and filters specs in a deterministic 6-step pipeline:\n\n1. **Change specs (`specIds`)**: Specs directly declared on the active change (`change.specIds`) are seeded and protected from exclusion.\n2. **Direct dependencies (`specDependsOn`)**: Direct dependencies declared on change specs are included.\n3. **Project-level includes (`contextIncludeSpecs`)**: Project-wide glob patterns are matched and added.\n4. **Project-level excludes (`contextExcludeSpecs`)**: Project-wide exclude patterns remove matching non-protected specs.\n5. **Workspace-level filters**: For each active workspace in the change:\n   - Workspace `contextIncludeSpecs` are added.\n   - Workspace `contextExcludeSpecs` are removed.\n6. **Transitive dependency traversal**: When `followDeps` is enabled, specs reachable via `dependsOn` traversal are added (and survive exclude rules).\n\n**Deduplication rule:** A spec matching multiple include rules appears only once in compiled context, retaining first-match ordering.\n\n### Context rendering mode\n\n`contextMode` controls how specs are formatted and presented in the compiled context delivered to AI agents:\n\n```yaml\ncontextMode: summary # default: summary (list | summary | full | hybrid)\n```\n\n- **`summary`** _(default)_ — Renders spec catalogue entries (title and description) extracted from frontmatter and overview headings. This delivers the highest signal-to-token ratio and keeps prompt context compact for routine tasks.\n- **`hybrid`** — Renders specs directly touched or modified by the active change with their complete Markdown content, while transitively referenced specs (via `dependsOn`) are rendered as metadata summaries.\n- **`full`** — Renders every in-context spec with its complete, verbatim Markdown content. Useful for complex refactoring where agents require the full text of all dependencies.\n- **`list`** — Renders only spec identifiers with source and mode metadata (minimal tokens).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base mode.\n- _Environment override:_ `SPECD_CONTEXT_MODE`.\n\n---\n\n## Approval gates\n\nBy default, the change lifecycle flows freely. Two optional gates can require explicit human approval before a change advances:\n\n```yaml\napprovals:\n  spec: false # default\n  signoff: false # default\n```\n\nSetting either gate to `true` changes the lifecycle:\n\n| Gate      | Effect when `true`                                                                                                                                     |\n| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `spec`    | `ready → implementing` is blocked until a human runs `specd approve spec`. The change must pass through `pending-spec-approval → spec-approved` first. |\n| `signoff` | `done → archivable` is blocked until a human runs `specd approve signoff`. The change must pass through `pending-signoff → signed-off` first.          |\n\nThe two gates are independent — you can enable either or both.\n\nEnable `spec` approval when your team wants a human review of the spec design before any code is written. Enable `signoff` approval for compliance requirements or regulated environments where a named person must sign off on each completed change before it is committed to the archive.\n\nBoth approval records capture the approver's git identity, a reason, and a hash of the artifacts at approval time — creating a tamper-evident audit trail.\n\n---\n\n## Invalidation policy\n\nWhen a change's validated artifacts drift from their baseline (files edited on disk after validation) or when you manually invalidate a change, SpecD reopens artifacts for review. The `invalidationPolicy` field controls how far that reopening propagates:\n\n```yaml\ninvalidationPolicy: downstream # default\n```\n\n| Policy       | What gets reopened                                                                           |\n| ------------ | -------------------------------------------------------------------------------------------- |\n| `none`       | Nothing. Drift is tracked but artifacts stay `complete`. Status shows `complete-with-drift`. |\n| `surgical`   | Only the specific files that changed.                                                        |\n| `downstream` | Changed files plus all artifacts that depend on them in the DAG. This is the default.        |\n| `global`     | Every artifact in the change, regardless of which file triggered the invalidation.           |\n\nThe project-level default is persisted on each change at creation time. You can change it per-change with:\n\n```bash\nspecd changes edit my-change --invalidation-policy surgical\n```\n\nOr override it for a single manual invalidation:\n\n```bash\nspecd changes invalidate my-change --reason \"API changed\" --target specs --policy surgical\n```\n\nUnder `none`, drift is still visible — `changes status` and `changes artifacts` show `complete-with-drift` and a `[drift]` tag — but the lifecycle is not blocked and artifacts are not reopened. Use `none` when you want informational drift tracking without automatic reopening.\n\n---\n\n## Schema overrides\n\n`schemaOverrides` lets you customise the active schema for your project without forking it or publishing a new package. Changes are applied inline, on top of whatever schema is declared in the `schema` field.\n\nThis is the recommended way to add project-specific automation and rules.\n\n### Operations\n\n| Operation | What it does                                                                |\n| --------- | --------------------------------------------------------------------------- |\n| `append`  | Adds entries to the end of an array (hooks, artifact rules, artifacts)      |\n| `prepend` | Adds entries to the start of an array                                       |\n| `create`  | Adds a new artifact or workflow step that does not exist in the base schema |\n| `set`     | Replaces a scalar field or a whole array entry by identity                  |\n| `remove`  | Removes entries from an array by identity                                   |\n\nEach operation targets one or more schema sections: `artifacts`, `workflow`, or `metadataExtraction`.\n\n### Common uses\n\n**Adding shell hooks to lifecycle steps:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n            - id: run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-tests\n              run: 'pnpm test'\n```\n\nThis runs tests and lint after each implementing step, and runs tests again before archiving. If a `run:` hook exits non-zero, the transition is aborted.\n\n**Adding instruction hooks:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: check-global-compliance\n              instruction: >-\n                Before finishing design, review all artifacts against the global specs.\n                Flag any violation before transitioning to ready.\n```\n\nInstruction hooks inject text into the compiled context at that lifecycle step. Use them to remind the agent of project-specific checks.\n\n**Adding per-artifact rules:**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-adr-references\n              text: 'Architecture decisions must reference an ADR number.'\n```\n\nThis adds a rule injected into the compiled context alongside the schema's own instruction for the `design` artifact. It is additive — it does not replace the schema's instruction.\n\nHook and rule entries in `schemaOverrides` require an `id` field. This is how subsequent `append`, `prepend`, `set`, and `remove` operations identify individual entries within an array.\n\n### Real example: specd's own configuration\n\nThe specd project uses `schemaOverrides` to enforce its own global constraints during development:\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-global-specs-compliance\n              text: >-\n                Cross-reference this design against the global specs:\n                architecture (hexagonal layers, dependency direction, no I/O in domain),\n                conventions (naming, exports, ESM, no default exports, no any),\n                and testing (test structure, coverage). Flag any decision that violates\n                a global constraint before proceeding.\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: designing-check-global-specs\n              instruction: >-\n                Before finishing design, review all artifacts and deltas in this change\n                against the global specs. Check compliance with architecture, conventions,\n                testing, and any other applicable global constraint. Flag violations\n                before transitioning to ready.\n      - step: implementing\n        hooks:\n          post:\n            - id: implementing-run-tests\n              run: 'pnpm test'\n            - id: implementing-run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: archiving-run-tests\n              run: 'pnpm test'\n            - id: archiving-run-lint\n              run: 'pnpm lint'\n```\n\n---\n\n## Schema plugins\n\n`schemaPlugins` lists schema reference strings that are loaded and merged into the active schema before `schemaOverrides` is applied. Each entry uses the same resolution rules as the top-level `schema` field.\n\n```yaml\nschemaPlugins:\n  - '@acme/specd-plugin-compliance'\n  - '#billing:billing-plugin'\n```\n\nPlugins are full schema layers — they can add or modify artifacts, workflow steps, and metadata extraction rules in bulk. They are applied in declaration order. `schemaOverrides` is applied after all plugins, so your inline overrides always take final precedence.\n\nUse plugins when you have a set of schema customisations that is shared across multiple projects and you want to manage it as a versioned package.\n\n---\n\n## LLM-optimised context\n\n```yaml\nllmOptimizedContext: true # default: false\n```\n\nWhen `true`, SpecD prefers LLM-optimized descriptions and context blocks when available. This improves agent performance and reduces token usage by providing a condensed, high-signal representation of specs and project background.\n\nWhen enabled:\n\n- **Agents** populate `optimizedDescription` and `optimizedContext` fields in `metadata.json`.\n- **Compilers** prefer these fields over raw artifact content.\n- **Cache**: Project-level background context is cached in `project-metadata.json` and verified with hashes.\n\nWhen `false` or absent, SpecD uses standard deterministic extraction from Markdown artifacts.\n\nSet this to `true` to unblock advanced agent features and optimize token costs.\n\n- _Default:_ `false`\n- _Cascade behavior:_ Scalar boolean — an overlay config **replaces** the base setting.\n- _Environment override:_ `SPECD_LLM_OPTIMIZED`.\n\n---\n\n## Code Graph configuration\n\nSpecD includes an in-memory and persisted code graph for symbol indexing, dependency blast-radius analysis, and architectural impact assessment. Configure global indexing boundaries with the top-level `graph` section:\n\n```yaml\ngraph:\n  includePaths:\n    - 'apps/**'\n    - 'packages/**'\n  excludePaths:\n    - '**/*.test.ts'\n    - '**/*.spec.ts'\n    - '**/node_modules/**'\n    - '**/dist/**'\n```\n\n- **`includePaths`**: Project-global glob patterns (relative to the project root) included in the code graph, indexed under the reserved `root:` namespace.\n  - _Default:_ None (all discovered files within registered workspace `codeRoot` directories are indexed).\n  - _Cascade behavior:_ Array — an overlay config **appends** to inherited include paths.\n- **`excludePaths`**: Project-global gitignore-syntax patterns excluded during symbol, file, and spec discovery across all workspaces.\n  - _Default:_ Built-in defaults (`node_modules/`, `.git/`, `.hg/`, `.svn/`, `.specd/`, `dist/`, `build/`, `coverage/`, `.next/`, `.nuxt/`).\n  - _Engine replacement:_ When `graph.excludePaths` is specified in your configuration, it **replaces** the built-in defaults entirely (it does not append to them). If you specify custom exclusions, you should include standard directories you still wish to exclude, or rely on `.gitignore` with `respectGitignore: true`.\n  - _Cascade behavior:_ Array — in configuration cascade, an overlay config file (`specd.local.yaml`) **appends** to the base config file's (`specd.yaml`) exclusion patterns. Workspace-level `graph.excludePaths` are also additive on top of this global set.\n\n---\n\n## Plugins\n\nSpecD supports agent plugins that integrate with coding assistants (Claude Code, Copilot, Codex, OpenCode):\n\n```yaml\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-claude'\n    - name: '@specd/plugin-agent-copilot'\n      config:\n        model: 'gpt-4o'\n```\n\n- **`plugins.agents`**: A list of agent plugin declarations.\n  - `name`: Package or module name of the plugin (e.g. `'@specd/plugin-agent-claude'`).\n  - `config`: Optional plugin-specific configuration record passed to the plugin adapter during initialization.\n  - _Default:_ None (no third-party plugins loaded; built-in plugins activate via project defaults).\n  - _Cascade behavior:_ Array — an overlay config **appends** new plugin declarations to the inherited list. To remove an inherited plugin in an overlay config, use `remove.plugins.agents: [{ name: '@specd/plugin-agent-copilot' }]`.\n\n---\n\n## Config cascade and local variants\n\nSpecD projects support layered configuration through candidate files in the same directory:\n\n- `specd.yaml`: Base project configuration committed to version control.\n- `specd.<variant>.yaml`: Shared variant configuration (e.g. `specd.ci.yaml`).\n- `specd.local.yaml`: Developer-local overrides (automatically gitignored).\n- `specd.local.<variant>.yaml`: Variant-specific developer-local overrides.\n\n### Inheritance with `extends`\n\nA config layer can inherit from prior candidates in the cascade:\n\n```yaml\n# specd.local.yaml\nextends: true # Inherit from the preceding configuration candidate\n\nlogging:\n  level: debug # Override just the log level locally\n```\n\nOr inherit explicitly from another config file path:\n\n```yaml\nextends: '../shared/specd.base.yaml'\n```\n\n### Targeted removals with `remove`\n\nWhen extending a configuration, you can explicitly remove inherited elements:\n\n```yaml\nextends: true\nremove:\n  workspaces:\n    - legacy-app # Exclude a workspace in your local environment\n  context:\n    - id: global-notice # Remove a specific context injection entry\n```\n\n---\n\n## Validating your configuration\n\nRun `specd config validate` at any time to check your configuration:\n\n```bash\nspecd config validate\n```\n\nThis runs a stricter check than the startup validator:\n\n- Unknown workspace qualifiers in context patterns are **errors**, not warnings — a typo silently excludes specs from context, which is a dangerous silent failure in team environments.\n- Patterns that match no specs on disk emit warnings — useful for catching typos early.\n\n### Startup validation matrix\n\nSpecD validates `specd.yaml` before executing any command that requires configuration.\n\n#### Errors that abort startup\n\nThe following conditions are hard errors — SpecD exits immediately:\n\n| Condition                                                                        | Error Cause                                                      |\n| :------------------------------------------------------------------------------- | :--------------------------------------------------------------- |\n| `schema` field is missing                                                        | Config is invalid without a schema reference.                    |\n| `workspaces` section is missing or has no `default` workspace                    | Every project must declare a default workspace.                  |\n| `specs` section is missing in any workspace                                      | SpecD cannot locate specs without a specs adapter path.          |\n| `codeRoot` is missing in any non-`default` workspace                             | Required for non-default workspaces; no sensible default exists. |\n| `storage` section is missing, or `changes` or `archive` sub-key is absent        | Both storage directories are mandatory.                          |\n| `adapter` is missing in any `specs`, `schemas`, or `storage` section             | Required in every storage declaration.                           |\n| Required adapter-specific fields are absent (e.g. `config.path` when `type: fs`) | The adapter cannot function without its required options.        |\n| An adapter name has no registered factory at kernel construction time            | The kernel rejects unknown named adapters with a clear error.    |\n| Storage or config path resolves outside the repository root                      | Paths must remain within the VCS repository root boundary.       |\n| Invalid `contextIncludeSpecs` or `contextExcludeSpecs` pattern syntax            | e.g. `*` in a disallowed position.                               |\n| `llmOptimizedContext` is not a boolean                                           | Any other type is rejected.                                      |\n| Legacy `artifactRules` or `skills` fields are present                            | Use `schemaOverrides` or the plugin system instead.              |\n\n#### Warnings that allow startup to proceed\n\n| Condition                                                                     | Warning Behavior                                                               |\n| :---------------------------------------------------------------------------- | :----------------------------------------------------------------------------- |\n| Duplicate workspace names                                                     | YAML parser retains last-wins; the duplicate is flagged as a probable error.   |\n| Unknown workspace qualifier in a context pattern (runtime)                    | A typo silently excludes specs; warned at runtime.                             |\n| Legacy configuration format declared (`adapter: fs` with sibling `fs:` block) | Emits a deprecation warning advising migration to `adapter: { type, config }`. |\n\n#### Commands that skip validation entirely\n\nThe following lightweight or bootstrap commands do not require a valid `specd.yaml`:\n\n- `--help`, `--version`\n- `specd project init`\n- `specd config validate`\n- `specd plugin` subcommands\n- `specd graph` commands run with `--path <repo-root>` (bootstrap mode)\n\n## Complete configuration reference and cascade rules\n\nThe table below catalogs every configuration option supported by `specd.yaml` and its cascade layers, including its level, data type, default value, how it behaves under the layered configuration cascade (whether overlay values **replace** base scalars, **append** to arrays, or **deep-merge** objects), and its operational effect.\n\n| Option                                     | Level         | Type                                                            | Default                                          | Cascade Rule                                  | Effect & Description                                                                                                                                                             |\n| :----------------------------------------- | :------------ | :-------------------------------------------------------------- | :----------------------------------------------- | :-------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `schema`                                   | Project       | `string`                                                        | _(Required)_                                     | **Replaces**                                  | Reference to the active workflow schema (e.g. `'@specd/schema-std'`, `'#workspace:schema'`, `'./path'`). Overridden by `SPECD_SCHEMA`.                                           |\n| `specdPath`                                | Project       | `string`                                                        | `'.specd'`                                       | **Replaces**                                  | Directory for SpecD state and change tracking directories (`changes/`, `drafts/`, `discarded/`, `archive/`).                                                                     |\n| `configPath`                               | Project       | `string`                                                        | `'.specd/config'`                                | **Replaces**                                  | Root directory for backend-owned operational data (graph databases, logs, temporary scratch files).                                                                              |\n| `workspaces`                               | Project       | `Record<string, Workspace>`                                     | `{ default: ... }`                               | **Deep-merges**                               | Map of workspace definitions. Must contain at least `default`. Merged per workspace name; remove with `remove.workspaces`.                                                       |\n| `workspaces.<name>.specs`                  | Workspace     | `AdapterBinding`                                                | _(Required)_                                     | **Deep-merges**                               | Named adapter binding for specs repository (e.g. `adapter: fs`, `fs.path: specs/`).                                                                                              |\n| `workspaces.<name>.prefix`                 | Workspace     | `string`                                                        | `null` (None)                                    | **Replaces**                                  | Logical path prefix prepended to all spec IDs in this workspace (e.g. `_global` -> `default:_global/spec`).                                                                      |\n| `workspaces.<name>.schemas`                | Workspace     | `AdapterBinding`                                                | `null` (None)                                    | **Deep-merges**                               | Optional adapter binding for workspace-local custom schemas.                                                                                                                     |\n| `workspaces.<name>.codeRoot`               | Workspace     | `string`                                                        | `'.'` for `default`, _(Required)_ for others     | **Replaces**                                  | Filesystem directory where implementation source code lives relative to `specd.yaml`.                                                                                            |\n| `workspaces.<name>.ownership`              | Workspace     | `'owned' \\| 'shared' \\| 'readOnly'`                             | `'owned'` for `default`, `'readOnly'` for others | **Replaces**                                  | Governance relationship: `owned` (fully mutable), `shared` (co-owned), or `readOnly` (context-only).                                                                             |\n| `workspaces.<name>.contextIncludeSpecs`    | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Patterns matching specs in this workspace that must be included in context when this workspace is active.                                                                        |\n| `workspaces.<name>.contextExcludeSpecs`    | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Patterns matching specs in this workspace to exclude from context when this workspace is active.                                                                                 |\n| `workspaces.<name>.graph.respectGitignore` | Workspace     | `boolean`                                                       | `true`                                           | **Replaces**                                  | When `true`, Git-ignored files in `codeRoot` are skipped during code graph indexing. When `false`, they are discovered.                                                          |\n| `workspaces.<name>.graph.excludePaths`     | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Gitignore-syntax patterns relative to `codeRoot` excluded from graph indexing. Additive over project excludes.                                                                   |\n| `workspaces.<name>.graph.allowedPaths`     | Workspace     | `string[]`                                                      | `[]` (Entire codeRoot)                           | **Appends**                                   | Inclusions restricting code graph index visibility to specific subdirectories within `codeRoot`.                                                                                 |\n| `actorProvider`                            | Project       | `string`                                                        | Auto-detected                                    | **Replaces**                                  | Forces a specific VCS actor identity provider (e.g. `'git'`, `'hg'`). Overridden by `SPECD_ACTOR_PROVIDER`.                                                                      |\n| `privacy.mode`                             | Project       | `'hash' \\| 'mask' \\| 'anonymous'`                               | `null` (None)                                    | **Replaces**                                  | Identity obfuscation strategy for manifests and archives. Overridden by `SPECD_PRIVACY_MODE`.                                                                                    |\n| `privacy.salt`                             | Project       | `string`                                                        | `null`                                           | **Replaces**                                  | Secret salt string required when `privacy.mode` is `'hash'`. Overridden by `SPECD_PRIVACY_SALT`.                                                                                 |\n| `privacy.excludeActors`                    | Project       | `string[]`                                                      | `['specd', 'system@getspecd.dev']`               | **Appends** (cascade) / **Replaces defaults** | Case-insensitive actors exempt from obfuscation. When specified, replaces built-in defaults (`specd`, `system@getspecd.dev`). Overlay files in cascade append to base config.    |\n| `privacy.allowedMetadataKeys`              | Project       | `string[]`                                                      | `[]` (Strips all)                                | **Appends**                                   | Whitelist of non-PII metadata keys preserved under active privacy modes.                                                                                                         |\n| `storage.changes`                          | Project       | `AdapterBinding`                                                | `.specd/changes`                                 | **Deep-merges**                               | Named adapter binding for in-progress change directories.                                                                                                                        |\n| `storage.drafts`                           | Project       | `AdapterBinding`                                                | `.specd/drafts`                                  | **Deep-merges**                               | Named adapter binding for shelved drafts.                                                                                                                                        |\n| `storage.discarded`                        | Project       | `AdapterBinding`                                                | `.specd/discarded`                               | **Deep-merges**                               | Named adapter binding for abandoned changes.                                                                                                                                     |\n| `storage.archive`                          | Project       | `AdapterBinding`                                                | `.specd/archive`                                 | **Deep-merges**                               | Named adapter binding for permanent change archive records.                                                                                                                      |\n| `storage.archive.pattern`                  | Project       | `string`                                                        | `'{{change.archivedName}}'`                      | **Replaces**                                  | Nunjucks template defining archive subfolder hierarchy (e.g. `'{{year}}/{{change.archivedName}}'`).                                                                              |\n| `approvals.spec`                           | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, blocks `ready → implementing` until human reviewer records `specd changes approve-spec`.                                                                            |\n| `approvals.signoff`                        | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, blocks `done → archivable` until human reviewer records `specd changes signoff`.                                                                                    |\n| `graph.includePaths`                       | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global file globs included in code graph indexing under reserved `root:` namespace.                                                                                              |\n| `graph.excludePaths`                       | Project       | `string[]`                                                      | Built-in defaults                                | **Appends** (cascade) / **Replaces defaults** | Global file globs excluded from discovery. When specified, replaces built-in defaults (`node_modules/`, `.git/`, `dist/`, etc.). Overlay files in cascade append to base config. |\n| `logging.level`                            | Project       | `'trace' \\| 'debug' \\| 'info' \\| 'warn' \\| 'error' \\| 'silent'` | `'info'`                                         | **Replaces**                                  | Minimum threshold for `{configPath}/log/specd.log`. Overridden by `SPECD_LOG_LEVEL`.                                                                                             |\n| `context`                                  | Project       | `ContextEntry[]`                                                | `[]` (None)                                      | **Appends**                                   | Static `{ file: string }` or `{ instruction: string }` blocks injected into every compiled context for AI agents.                                                                |\n| `contextIncludeSpecs`                      | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global spec inclusion patterns applied across all compiled contexts regardless of active workspace.                                                                              |\n| `contextExcludeSpecs`                      | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global spec exclusion patterns applied across all compiled contexts.                                                                                                             |\n| `contextMode`                              | Project       | `'list' \\| 'summary' \\| 'full' \\| 'hybrid'`                     | `'summary'`                                      | **Replaces**                                  | Spec presentation format in compiled context. Overridden by `SPECD_CONTEXT_MODE`.                                                                                                |\n| `llmOptimizedContext`                      | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, prefers condensed descriptions from metadata cache. Overridden by `SPECD_LLM_OPTIMIZED`.                                                                            |\n| `schemaPlugins`                            | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Schema package references merged into active schema prior to inline `schemaOverrides`.                                                                                           |\n| `schemaOverrides`                          | Project       | `SchemaOverrides`                                               | `null` (None)                                    | **Deep-merges**                               | Inline schema modification operations: `create`, `remove`, `set`, `append`, `prepend`.                                                                                           |\n| `invalidationPolicy`                       | Project       | `'none' \\| 'surgical' \\| 'downstream' \\| 'global'`              | `'downstream'`                                   | **Replaces**                                  | Default artifact invalidation propagation policy assigned to newly created changes.                                                                                              |\n| `plugins.agents`                           | Project       | `PluginEntry[]`                                                 | `[]` (None)                                      | **Appends**                                   | Registered agent plugin definitions (`name` and optional `config`). Remove with `remove.plugins.agents`.                                                                         |\n| `extends`                                  | Cascade Layer | `true \\| string`                                                | `null` (Standalone)                              | _Special_                                     | Controls inheritance: `true` inherits from preceding layer; `<path>` inherits from specific active file.                                                                         |\n| `remove.root`                              | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited top-level fields (e.g. `contextExcludeSpecs`, `actorProvider`). Cannot remove `schema`.                                                                          |\n| `remove.workspaces`                        | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited workspace definitions by name.                                                                                                                                   |\n| `remove.storage`                           | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited storage adapter bindings by key (`changes`, `drafts`, `discarded`, `archive`).                                                                                   |\n| `remove.context`                           | Cascade Layer | `RemovalMatcher[]`                                              | `null`                                           | _Removal_                                     | Drops matching inherited context injection entries by `id`, `file`, or `instruction`.                                                                                            |\n| `remove.plugins.agents`                    | Cascade Layer | `{ name: string }[]`                                            | `null`                                           | _Removal_                                     | Drops inherited agent plugin declarations by package name.                                                                                                                       |\n\n---\n\n## Where to go next\n\n| Topic                                                    | Document                                               |\n| :------------------------------------------------------- | :----------------------------------------------------- |\n| Practical and exhaustive scenario examples               | [Configuration Examples](configuration-examples.md)    |\n| Schema format — authoring or customising a schema        | [Schema Format Reference](../schemas/schema-format.md) |\n| Lifecycle states, transitions, hooks, and approval gates | [Workflow Reference](workflow.md)                      |\n| Full CLI command reference                               | [CLI Reference](../cli/cli-reference.md)               |\n",
    "lineCount": 1116,
    "byteLength": 65736,
    "outline": [
      {
        "index": 1,
        "heading": "Configuring Your SpecD Project",
        "level": 1,
        "startLine": 1,
        "endLine": 1116,
        "lines": 1116,
        "content": "# Configuring Your SpecD Project\n\n`specd.yaml` is the single configuration file for a SpecD project. Every tool in the SpecD ecosystem — the CLI, the MCP server, and agent plugins — reads it to understand where your specs live, how changes are stored, and what rules govern your workflow.\n\nThis guide walks through all configuration areas conceptually and provides a complete field-by-field reference. For real-world scenario examples and multi-workspace architectures, see [Configuration Examples](./configuration-examples.md).\n\n---\n\n## Getting a specd.yaml\n\nRun `specd project init` in your project root. SpecD creates a `specd.yaml` with sensible defaults for a single-repo project. You will then edit it to reflect your team's structure and policies.\n\nSpecD discovers its configuration by walking up from the current working directory to the active VCS root, collecting all `specd.yaml`, `specd.*.yaml`, `specd.local.yaml`, and `specd.local.*.yaml` candidate files from the first directory that contains at least one. The candidates are resolved into an active chain, deep-merged, and validated as one config. See [Config cascade and local variants](#config-cascade-and-local-variants) below for details.\n\n---\n\n## The minimal configuration\n\nThis is the smallest valid `specd.yaml`. It covers everything SpecD needs to operate:\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\nEvery other field covered in this guide is optional. Start here and add what you need.\n\n---\n\n## Logging\n\nSpecD supports project-level logging settings in `specd.yaml`:\n\n```yaml\nlogging:\n  level: info # trace | debug | info | warn | error | silent\n```\n\n- The section is optional.\n- If omitted, SpecD defaults to `info`.\n- Logs are written to `{configPath}/log/specd.log` as structured JSON.\n- CLI runtime verbosity can be increased with `-v` / `-vv` for console output without changing file-level defaults.\n\n---\n\n## Environment variables and .env support\n\nSpecD natively supports loading environment variables from `.env` and `.env.local` files in your project root. `.env.local` has higher priority and should be used for secrets that must never be committed (like privacy salts).\n\nThe following variables map directly to root-level configuration settings and **override** values in `specd.yaml` or `specd.local.yaml`:\n\n| Variable               | Configuration Field   |\n| ---------------------- | --------------------- |\n| `SPECD_ACTOR_PROVIDER` | `actorProvider`       |\n| `SPECD_PRIVACY_MODE`   | `privacy.mode`        |\n| `SPECD_PRIVACY_SALT`   | `privacy.salt`        |\n| `SPECD_LOG_LEVEL`      | `logging.level`       |\n| `SPECD_CONTEXT_MODE`   | `contextMode`         |\n| `SPECD_LLM_OPTIMIZED`  | `llmOptimizedContext` |\n| `SPECD_SCHEMA`         | `schemaRef`           |\n\n---\n\n## Identity resolution\n\nSpecD automatically identifies the user performing an operation by probing the environment. By default, it prioritizes Version Control Systems (Git, Mercurial, Subversion) to resolve the actor's name and email.\n\n### Manual provider selection\n\nIf you need to bypass auto-detection and force a specific identity provider, use the `actorProvider` field:\n\n```yaml\nactorProvider: git # forces git even if other systems are detected\n```\n\nThis is useful in environments where multiple systems might be present or when using a custom identity plugin.\n\n---\n\n## Privacy\n\nFor projects in public repositories, you might want to avoid exposing real names or emails in change manifests and archives. SpecD provides built-in privacy modes to obfuscate identity data:\n\n```yaml\nprivacy:\n  mode: mask # hash | mask | anonymous\n  excludeActors: # optional: skip obfuscation for these actors\n    - 'specd'\n    - 'system@getspecd.dev'\n  allowedMetadataKeys: # optional: preserve specific metadata keys\n    - 'department'\n```\n\n- **`mode`**: Obfuscation mode (`hash`, `mask`, or `anonymous`). _Cascade:_ **Replaces**.\n- **`salt`**: Secret salt string required when `mode` is `hash`. _Cascade:_ **Replaces**.\n- **`excludeActors`**: Actor names or emails exempt from obfuscation.\n  - _Default:_ Built-in defaults (`'specd'`, `'system@getspecd.dev'`).\n  - _Engine replacement:_ When specified in configuration, it **replaces** the built-in defaults entirely (it does not append to them). If you specify custom excluded actors, you must explicitly include `'specd'` or `'system@getspecd.dev'` if you still want them exempt.\n  - _Cascade behavior:_ Array — in configuration cascade, an overlay config file (`specd.local.yaml`) **appends** to the base config file's list.\n- **`allowedMetadataKeys`**: Whitelist of metadata keys preserved under active privacy modes.\n  - _Default:_ None (all metadata stripped).\n  - _Cascade behavior:_ Array — an overlay config file **appends** to the base config file's list.\n\n### Privacy modes\n\n| Mode        | Effect                                                                    |\n| ----------- | ------------------------------------------------------------------------- |\n| `hash`      | Obfuscates email using HMAC-SHA256 with a salt. Requires a `salt`.        |\n| `mask`      | Partially masks name and email (e.g., `j***z@e***.com`).                  |\n| `anonymous` | Replaces all identity data with \"Anonymous\" and `anonymous@getspecd.dev`. |\n\nWhen using `hash` mode, you **must** provide a salt, preferably via the `SPECD_PRIVACY_SALT` environment variable.\n\n### Metadata privacy\n\nBy default, any active privacy mode removes the internal `providerId` and all `metadata` fields to prevent accidental PII leakage. Use `allowedMetadataKeys` to whitelist specific non-sensitive keys you wish to preserve.\n\n---\n\n## Runtime and state paths: specdPath and configPath\n\nSpecD distinguishes between workflow artifact storage and runtime operational state:\n\n```yaml\n# Directory for workflow state: changes, drafts, discarded, archive\nspecdPath: .specd # default: .specd\n\n# Directory for runtime-owned state: graph indexes, scratch files, logs\nconfigPath: .specd/config # default: .specd/config\n```\n\n- **`specdPath`**: Sets the root path for SpecD lifecycle state. Storage bindings (`changes/`, `drafts/`, `discarded/`, `archive/`) resolve relative to this directory.\n  - _Default:_ `.specd`\n  - _Cascade behavior:_ Scalar value — an overlay config **replaces** the base value.\n- **`configPath`**: Sets the root path for backend-owned runtime data. Kept separate from workflow artifacts so caches and logs can be cleaned without touching spec or change history.\n  - `{configPath}/graph` for persisted graph backend databases and indexes.\n  - `{configPath}/tmp` for graph staging and temporary scratch files.\n  - `{configPath}/log/specd.log` for structured logging.\n  - _Default:_ `.specd/config`\n  - _Cascade behavior:_ Scalar value — an overlay config **replaces** the base value.\n\n---\n\n## Config cascade and local variants\n\nSpecD supports a layered config cascade. Multiple YAML files in the same directory are discovered, resolved into an active chain, deep-merged, and validated as one.\n\n### File naming and discovery order\n\nWhen SpecD searches for configuration, it looks for files in this order within the directory containing `specd.yaml`:\n\n1. `specd.yaml` — the committed project root (always the first active layer)\n2. `specd.*.yaml` — named shared variants (e.g. `specd.ci.yaml`, `specd.staging.yaml`), sorted lexicographically\n3. `specd.local.yaml` — personal local override\n4. `specd.local.*.yaml` — named local variants (e.g. `specd.local.mono.yaml`), sorted lexicographically\n\nDiscovery walks up from the current working directory to the active VCS root and stops at the first directory containing at least one candidate file. `specd.yaml` must exist for discovery to succeed.\n\n### Cascade resolution\n\nEach variant file may declare an `extends` key:\n\n| `extends` value | Behaviour                                                                                         |\n| --------------- | ------------------------------------------------------------------------------------------------- |\n| absent          | File becomes a **standalone root** — all previous layers are discarded and this file starts fresh |\n| `true`          | File inherits from the previous active layer (or from `specd.yaml` if it is the first overlay)    |\n| `<path>`        | File inherits from the named base file, but **only if that base is already in the active chain**  |\n\nResolution builds an active chain starting from `specd.yaml`. Each subsequent candidate is either attached (extends a layer in the chain) or skipped (its base is not active). A file with no `extends` becomes a new standalone root — everything before it is discarded.\n\n### Forced mode (`--config`)\n\nPassing `--config path/to/file.yaml` loads that file as a self-contained closed chain. If the file declares `extends: true`, it resolves against `specd.yaml` in the same directory. If it declares `extends: <path>`, the chain follows explicit links until no file extends further.\n\n### Merge semantics\n\nLayers in the active chain are deep-merged in order:\n\n- **Scalars** — later values replace earlier ones\n- **Objects** — recursively merged\n- **Arrays** — later arrays **append** to earlier ones (no replacement)\n\n### Removals\n\nAn overlay layer may declare a `remove` block to strip entries inherited from prior layers:\n\n```yaml\nextends: true\nremove:\n  root:\n    - contextExcludeSpecs\n  workspaces:\n    - staging\n  context:\n    - id: ci-only-instruction\n    - file: CI_AGENTS.md\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n| Removal target   | Keyed by                       | Effect                                |\n| ---------------- | ------------------------------ | ------------------------------------- |\n| `root`           | field name                     | Removes the named top-level field     |\n| `workspaces`     | workspace name                 | Removes the named workspace           |\n| `storage`        | storage key                    | Removes a storage binding             |\n| `context`        | `id`, `file`, or `instruction` | Removes matching context entries      |\n| `plugins.agents` | `name`                         | Removes a matching plugin declaration |\n\nRemovals are applied immediately after the layer that declares them is merged. A `remove` block without `extends` is an error.\n\n### Gitignored local files\n\n`specd project init` adds both `specd.local.yaml` and `specd.local.*.yaml` to `.gitignore` automatically. Local variants are for personal experimentation: trying a different schema branch, pointing at a local schema copy, or testing a configuration change without affecting the rest of the team.\n\n### Common patterns\n\n**Personal local override:**\n\n```yaml\n# specd.local.yaml\nextends: true\nschema: './my-local-schema'\n```\n\n**CI-specific variant:**\n\n```yaml\n# specd.ci.yaml\nextends: true\ncontext:\n  - instruction: 'Running in CI. Skip interactive prompts.'\nremove:\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n**Standalone local config (fresh start, no inheritance):**\n\n```yaml\n# specd.local.yaml — no extends key\nschema: '@specd/schema-std'\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n```\n\n### Bootstrap mode exceptions (`--path`)\n\nSome `specd graph` CLI commands support an unconfigured repository bootstrap mode for initial indexing and exploration before a project has a `specd.yaml`:\n\n- Passing `--path <repo-root>` forces bootstrap mode and is mutually exclusive with `--config`.\n- When no `specd.yaml` is discovered in the directory walk up to the VCS root, those graph commands fall back to bootstrap mode automatically.\n\nIn bootstrap mode, SpecD behaves as if the repository were a single synthetic `default` workspace with `codeRoot` set to the VCS root. Any discovered `specd.yaml` is ignored when `--path` is explicitly provided. Bootstrap mode is intended for exploratory graph queries; once a project is initialized with `specd project init`, standard configured execution should always be preferred.\n\n---\n\n## Schema selection\n\nThe `schema` field tells SpecD which workflow schema to use. Schemas define your artifact types, lifecycle steps, and validation rules. There is exactly one active schema per project.\n\nSpecD resolves the `schema` value using a prefix convention:\n\n| Value                            | Where SpecD looks                                                               |\n| -------------------------------- | ------------------------------------------------------------------------------- |\n| `'@specd/schema-std'`            | npm package in `node_modules/@specd/schema-std/schema.yaml`                     |\n| `'my-workflow'`                  | Bare name — `.specd/schemas/my-workflow/schema.yaml` in the default workspace   |\n| `'#my-workflow'`                 | Hash prefix — equivalent to bare name, explicit about resolving from `default`  |\n| `'#billing:my-schema'`           | Workspace-qualified — resolves from the `billing` workspace's schemas directory |\n| `'./schemas/custom/schema.yaml'` | Relative path from the `specd.yaml` directory                                   |\n\nMost projects start with `@specd/schema-std`. If you need a workflow that differs substantially from the standard one, you can author a local schema and reference it by name. Local schemas live inside your repository and evolve with your project.\n\nTo customise the standard schema without forking it, use `schemaOverrides` — covered below.\n\n---\n\n## Workspaces\n\nA workspace tells SpecD where a set of specs lives and what relationship the project has with them.\n\nEvery `specd.yaml` must declare a `default` workspace. This is the local project workspace — the one the current repository owns. Additional workspaces reference external repositories or sub-directories within a monorepo.\n\n### The default workspace\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThis is the minimum. Two useful optional fields:\n\n- **`codeRoot`** — the directory where implementation code lives, relative to `specd.yaml`. Defaults to the project root. Useful if your code lives in a subdirectory.\n- **`ownership`** — defaults to `owned`, meaning this project freely proposes changes to these specs. The default is almost always correct for the `default` workspace.\n\n### Additional workspaces\n\nIn a monorepo or multi-repo setup, you can declare workspaces that point elsewhere:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/payments/specs\n    codeRoot: packages/payments\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\n`codeRoot` is required for any workspace that is not `default` — there is no sensible default.\n\nThe `ownership` field describes the project's relationship with each workspace's specs:\n\n| Value      | Meaning                                                        |\n| ---------- | -------------------------------------------------------------- |\n| `owned`    | This project freely proposes and modifies specs here.          |\n| `shared`   | Co-owned; changes may require coordination with other teams.   |\n| `readOnly` | This project reads specs for context but does not modify them. |\n\n`readOnly` is the default for non-`default` workspaces. Use it for external dependencies whose specs you want in context but do not control.\n\n### Workspace prefixes\n\nSpec IDs always use the form `workspace:capability-path`. A spec in the `payments` workspace at `specs/checkout.md` has the ID `payments:checkout`. If you want every spec in a workspace to live under a leading path segment — for example to mirror a directory structure — you can declare `prefix`:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture`.\n\n`prefix` does not replace the workspace name in the spec ID. It prepends a path segment to the capability-path portion. In other words:\n\n- workspace name: `default`\n- prefix: `_global`\n- resulting spec ID: `default:_global/architecture`\n\nConcrete example:\n\n- `specsPath` is `specs/_global`\n- the spec lives on disk at `specs/_global/architecture`\n- relative to the workspace root, the spec path is just `architecture`\n\nWithout `prefix`, the spec ID would therefore be `default:architecture`. `prefix: _global` exists precisely to add that lost leading path segment back into the capability-path, producing `default:_global/architecture`.\n\n- _Default:_ None (bare capability paths).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base prefix.\n\n### Workspace schemas\n\nWorkspaces can optionally declare their own schema repository using the `schemas` adapter binding:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas\n```\n\n- When declared, schemas in that directory can be referenced via `#workspace:schema-name` (or `#schema-name` for `default`).\n- _Default:_ `null` (no local workspace schemas; schema references targeting workspaces without `schemas` fail validation).\n- _Cascade behavior:_ Object — deep merged with base workspace config.\n\n### Workspace-level context specs\n\nWorkspaces can define targeted spec inclusion and exclusion filters that activate only when that workspace is touched by a change:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/billing/specs\n    contextIncludeSpecs:\n      - 'billing:api/*' # include billing API specs when billing is active\n    contextExcludeSpecs:\n      - 'billing:internal/*' # exclude internal billing specs\n```\n\n- **`contextIncludeSpecs`**: Patterns matching specs in this workspace that must be included when active.\n  - _Default:_ None (only specs explicitly declared in the change and their dependencies are included).\n  - _Cascade behavior:_ Array — **appends** to inherited include patterns.\n- **`contextExcludeSpecs`**: Patterns matching specs in this workspace to exclude from context.\n  - _Default:_ None.\n  - _Cascade behavior:_ Array — **appends** to inherited exclude patterns.\n\n### Workspace code graph settings\n\nEach workspace can fine-tune its code graph discovery inside its `codeRoot`:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    graph:\n      respectGitignore: true # default: true\n      excludePaths:\n        - 'dist/**'\n        - 'coverage/**'\n      allowedPaths:\n        - 'src/**'\n```\n\n- **`respectGitignore`**: Whether `.gitignore` files inside `codeRoot` are respected during symbol and file indexing.\n  - _Default:_ `true`. When set to `false`, files ignored by Git are still discovered unless matched by `excludePaths`.\n  - _Cascade behavior:_ Scalar boolean — **replaces** the base setting.\n- **`excludePaths`**: Additional gitignore-syntax exclusion patterns relative to `codeRoot`.\n  - _Default:_ None (inherits project-level and built-in defaults).\n  - _Cascade behavior:_ Array — **appends** to project-level and workspace-level excludes.\n\n  **Monorepo child workspace isolation pattern:**\n  In monorepos where `workspaces.default.codeRoot` is `.` (the repository root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` pointing to subdirectories (e.g. `packages/core`, `packages/cli`, `apps/web`), the default workspace would physically crawl and index those directories as well. This leads to duplicate symbol definitions and index collisions.\n\n  To prevent this, configure `workspaces.default.graph.excludePaths` to explicitly exclude child workspace directories:\n\n  ```yaml\n  workspaces:\n    default:\n      prefix: _global\n      specs:\n        adapter: fs\n        fs:\n          path: specs/_global\n      codeRoot: .\n      graph:\n        excludePaths:\n          - node_modules/\n          - .git/\n          - .specd/\n          - dist/\n          - build/\n          - coverage/\n          - packages/ # Indexed independently by child workspaces\n          - apps/ # Indexed independently by child workspaces\n          - specs/\n          - specd-sdd/\n\n    core:\n      specs:\n        adapter: fs\n        fs:\n          path: specs/core\n      codeRoot: packages/core\n  ```\n\n- **`allowedPaths`**: Allowlist restricting graph indexing to specific subdirectories within `codeRoot`.\n  - _Default:_ None (entire `codeRoot` is visible to graph).\n  - _Cascade behavior:_ Array — **appends**.\n\n---\n\n## Storage\n\n`storage` declares where SpecD persists changes during their lifecycle. All four sub-keys are required.\n\n```yaml\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\nEach directory holds changes in a different state:\n\n- **`changes`** — active changes currently in progress\n- **`drafts`** — shelved changes that can be restored at any time\n- **`discarded`** — abandoned changes, kept for reference but no longer active\n- **`archive`** — completed changes; the permanent record after archiving\n\nAll paths resolve relative to the `specd.yaml` directory and must stay within the repository root.\n\n### Adapter bindings and kernel extensions\n\nEvery workspace and storage declaration is a named adapter binding:\n\n```yaml\nspecs:\n  adapter: fs\n  fs:\n    path: specs/\n```\n\nThe built-in path is `fs`, but `createKernel(config, options)` and `createKernelBuilder(config)` can register additive storage factories under other adapter names. The config loader preserves the selected adapter name and its opaque config block; the kernel then validates that the named factory exists in the merged registry.\n\nThat split is deliberate:\n\n- `FsConfigLoader` resolves and validates `fs` paths\n- the kernel validates whether non-built-in adapter names are actually registered\n\nUse this when you need a custom storage backend without forking the core workflow model.\n\nBy default, `specd project init` adds `.specd/drafts/` and `.specd/discarded/` to `.gitignore`. Teams who want to commit drafts — for example, to share in-progress work across machines — can remove those entries.\n\n### Organising the archive\n\nBy default, archived changes are stored with the name `{{change.archivedName}}` — a slug prefixed with the archive date, for example `2024-01-15-add-auth-flow`. You can customise this with the `pattern` field:\n\n```yaml\narchive:\n  adapter: fs\n  fs:\n    path: .specd/archive\n    pattern: '{{year}}/{{change.archivedName}}'\n```\n\nThis organises archived changes into yearly subdirectories.\n\n| Variable                  | Value                                    | Example                    |\n| :------------------------ | :--------------------------------------- | :------------------------- |\n| `{{change.archivedName}}` | Full archived directory name (default)   | `2024-01-15-add-auth-flow` |\n| `{{change.name}}`         | Change slug name                         | `add-auth-flow`            |\n| `{{year}}`                | Four-digit calendar year at archive time | `2024`                     |\n| `{{date}}`                | ISO calendar date at archive time        | `2024-01-15`               |\n\n> [!NOTE]\n> `{{change.workspace}}` is not supported because changes in SpecD can touch multiple workspaces simultaneously and have no single primary workspace. Use `{{change.name}}` or `{{change.archivedName}}` instead.\n\n---\n\n## Context configuration\n\nSpecD compiles a context block for the agent at each lifecycle step. The context includes relevant specs, schema instructions, and any additional content you inject here.\n\n### Injecting files and instructions\n\nThe `context` field at the top level injects content before any spec content, for every change in the project:\n\n```yaml\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Always prefer editing existing files over creating new ones.'\n```\n\n- `file` entries are read at compile time and injected verbatim. If the file does not exist, a warning is emitted and the entry is skipped.\n- `instruction` entries are injected as-is.\n\nThis is the right place for project-wide agent guidance — coding conventions, team norms, architectural principles.\n\n### Controlling which specs are included\n\nBy default, only the specs declared by the change and their explicit dependencies are included in context. You can expand or narrow this with `contextIncludeSpecs` and `contextExcludeSpecs`.\n\nThese can be declared at two levels:\n\n- **Project level** — patterns apply to every compiled context, regardless of which change is active. Use this for specs that should always be present: global constraints, cross-cutting architecture specs.\n- **Workspace level** — patterns apply only when that workspace is active in the current change (i.e., at least one of its specs is listed in the change).\n\n```yaml\n# Always include global constraints\ncontextIncludeSpecs:\n  - 'default:_global/*'\n\n# Never include commit message guidelines (not relevant to agents)\ncontextExcludeSpecs:\n  - 'default:commits'\n```\n\nPattern syntax:\n\n| Pattern              | Matches                                                                                       |\n| -------------------- | --------------------------------------------------------------------------------------------- |\n| `*`                  | All specs in all workspaces (at project level) or this workspace (at workspace level)         |\n| `workspace:*`        | All specs in the named workspace                                                              |\n| `prefix/*`           | All specs under `prefix/`                                                                     |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace                                              |\n| `path/name`          | Exact spec path (resolved from `default` at project level, this workspace at workspace level) |\n\n`*` is only valid in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\n### Spec collection order and priority\n\nDuring context compilation, SpecD collects and filters specs in a deterministic 6-step pipeline:\n\n1. **Change specs (`specIds`)**: Specs directly declared on the active change (`change.specIds`) are seeded and protected from exclusion.\n2. **Direct dependencies (`specDependsOn`)**: Direct dependencies declared on change specs are included.\n3. **Project-level includes (`contextIncludeSpecs`)**: Project-wide glob patterns are matched and added.\n4. **Project-level excludes (`contextExcludeSpecs`)**: Project-wide exclude patterns remove matching non-protected specs.\n5. **Workspace-level filters**: For each active workspace in the change:\n   - Workspace `contextIncludeSpecs` are added.\n   - Workspace `contextExcludeSpecs` are removed.\n6. **Transitive dependency traversal**: When `followDeps` is enabled, specs reachable via `dependsOn` traversal are added (and survive exclude rules).\n\n**Deduplication rule:** A spec matching multiple include rules appears only once in compiled context, retaining first-match ordering.\n\n### Context rendering mode\n\n`contextMode` controls how specs are formatted and presented in the compiled context delivered to AI agents:\n\n```yaml\ncontextMode: summary # default: summary (list | summary | full | hybrid)\n```\n\n- **`summary`** _(default)_ — Renders spec catalogue entries (title and description) extracted from frontmatter and overview headings. This delivers the highest signal-to-token ratio and keeps prompt context compact for routine tasks.\n- **`hybrid`** — Renders specs directly touched or modified by the active change with their complete Markdown content, while transitively referenced specs (via `dependsOn`) are rendered as metadata summaries.\n- **`full`** — Renders every in-context spec with its complete, verbatim Markdown content. Useful for complex refactoring where agents require the full text of all dependencies.\n- **`list`** — Renders only spec identifiers with source and mode metadata (minimal tokens).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base mode.\n- _Environment override:_ `SPECD_CONTEXT_MODE`.\n\n---\n\n## Approval gates\n\nBy default, the change lifecycle flows freely. Two optional gates can require explicit human approval before a change advances:\n\n```yaml\napprovals:\n  spec: false # default\n  signoff: false # default\n```\n\nSetting either gate to `true` changes the lifecycle:\n\n| Gate      | Effect when `true`                                                                                                                                     |\n| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `spec`    | `ready → implementing` is blocked until a human runs `specd approve spec`. The change must pass through `pending-spec-approval → spec-approved` first. |\n| `signoff` | `done → archivable` is blocked until a human runs `specd approve signoff`. The change must pass through `pending-signoff → signed-off` first.          |\n\nThe two gates are independent — you can enable either or both.\n\nEnable `spec` approval when your team wants a human review of the spec design before any code is written. Enable `signoff` approval for compliance requirements or regulated environments where a named person must sign off on each completed change before it is committed to the archive.\n\nBoth approval records capture the approver's git identity, a reason, and a hash of the artifacts at approval time — creating a tamper-evident audit trail.\n\n---\n\n## Invalidation policy\n\nWhen a change's validated artifacts drift from their baseline (files edited on disk after validation) or when you manually invalidate a change, SpecD reopens artifacts for review. The `invalidationPolicy` field controls how far that reopening propagates:\n\n```yaml\ninvalidationPolicy: downstream # default\n```\n\n| Policy       | What gets reopened                                                                           |\n| ------------ | -------------------------------------------------------------------------------------------- |\n| `none`       | Nothing. Drift is tracked but artifacts stay `complete`. Status shows `complete-with-drift`. |\n| `surgical`   | Only the specific files that changed.                                                        |\n| `downstream` | Changed files plus all artifacts that depend on them in the DAG. This is the default.        |\n| `global`     | Every artifact in the change, regardless of which file triggered the invalidation.           |\n\nThe project-level default is persisted on each change at creation time. You can change it per-change with:\n\n```bash\nspecd changes edit my-change --invalidation-policy surgical\n```\n\nOr override it for a single manual invalidation:\n\n```bash\nspecd changes invalidate my-change --reason \"API changed\" --target specs --policy surgical\n```\n\nUnder `none`, drift is still visible — `changes status` and `changes artifacts` show `complete-with-drift` and a `[drift]` tag — but the lifecycle is not blocked and artifacts are not reopened. Use `none` when you want informational drift tracking without automatic reopening.\n\n---\n\n## Schema overrides\n\n`schemaOverrides` lets you customise the active schema for your project without forking it or publishing a new package. Changes are applied inline, on top of whatever schema is declared in the `schema` field.\n\nThis is the recommended way to add project-specific automation and rules.\n\n### Operations\n\n| Operation | What it does                                                                |\n| --------- | --------------------------------------------------------------------------- |\n| `append`  | Adds entries to the end of an array (hooks, artifact rules, artifacts)      |\n| `prepend` | Adds entries to the start of an array                                       |\n| `create`  | Adds a new artifact or workflow step that does not exist in the base schema |\n| `set`     | Replaces a scalar field or a whole array entry by identity                  |\n| `remove`  | Removes entries from an array by identity                                   |\n\nEach operation targets one or more schema sections: `artifacts`, `workflow`, or `metadataExtraction`.\n\n### Common uses\n\n**Adding shell hooks to lifecycle steps:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n            - id: run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-tests\n              run: 'pnpm test'\n```\n\nThis runs tests and lint after each implementing step, and runs tests again before archiving. If a `run:` hook exits non-zero, the transition is aborted.\n\n**Adding instruction hooks:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: check-global-compliance\n              instruction: >-\n                Before finishing design, review all artifacts against the global specs.\n                Flag any violation before transitioning to ready.\n```\n\nInstruction hooks inject text into the compiled context at that lifecycle step. Use them to remind the agent of project-specific checks.\n\n**Adding per-artifact rules:**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-adr-references\n              text: 'Architecture decisions must reference an ADR number.'\n```\n\nThis adds a rule injected into the compiled context alongside the schema's own instruction for the `design` artifact. It is additive — it does not replace the schema's instruction.\n\nHook and rule entries in `schemaOverrides` require an `id` field. This is how subsequent `append`, `prepend`, `set`, and `remove` operations identify individual entries within an array.\n\n### Real example: specd's own configuration\n\nThe specd project uses `schemaOverrides` to enforce its own global constraints during development:\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-global-specs-compliance\n              text: >-\n                Cross-reference this design against the global specs:\n                architecture (hexagonal layers, dependency direction, no I/O in domain),\n                conventions (naming, exports, ESM, no default exports, no any),\n                and testing (test structure, coverage). Flag any decision that violates\n                a global constraint before proceeding.\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: designing-check-global-specs\n              instruction: >-\n                Before finishing design, review all artifacts and deltas in this change\n                against the global specs. Check compliance with architecture, conventions,\n                testing, and any other applicable global constraint. Flag violations\n                before transitioning to ready.\n      - step: implementing\n        hooks:\n          post:\n            - id: implementing-run-tests\n              run: 'pnpm test'\n            - id: implementing-run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: archiving-run-tests\n              run: 'pnpm test'\n            - id: archiving-run-lint\n              run: 'pnpm lint'\n```\n\n---\n\n## Schema plugins\n\n`schemaPlugins` lists schema reference strings that are loaded and merged into the active schema before `schemaOverrides` is applied. Each entry uses the same resolution rules as the top-level `schema` field.\n\n```yaml\nschemaPlugins:\n  - '@acme/specd-plugin-compliance'\n  - '#billing:billing-plugin'\n```\n\nPlugins are full schema layers — they can add or modify artifacts, workflow steps, and metadata extraction rules in bulk. They are applied in declaration order. `schemaOverrides` is applied after all plugins, so your inline overrides always take final precedence.\n\nUse plugins when you have a set of schema customisations that is shared across multiple projects and you want to manage it as a versioned package.\n\n---\n\n## LLM-optimised context\n\n```yaml\nllmOptimizedContext: true # default: false\n```\n\nWhen `true`, SpecD prefers LLM-optimized descriptions and context blocks when available. This improves agent performance and reduces token usage by providing a condensed, high-signal representation of specs and project background.\n\nWhen enabled:\n\n- **Agents** populate `optimizedDescription` and `optimizedContext` fields in `metadata.json`.\n- **Compilers** prefer these fields over raw artifact content.\n- **Cache**: Project-level background context is cached in `project-metadata.json` and verified with hashes.\n\nWhen `false` or absent, SpecD uses standard deterministic extraction from Markdown artifacts.\n\nSet this to `true` to unblock advanced agent features and optimize token costs.\n\n- _Default:_ `false`\n- _Cascade behavior:_ Scalar boolean — an overlay config **replaces** the base setting.\n- _Environment override:_ `SPECD_LLM_OPTIMIZED`.\n\n---\n\n## Code Graph configuration\n\nSpecD includes an in-memory and persisted code graph for symbol indexing, dependency blast-radius analysis, and architectural impact assessment. Configure global indexing boundaries with the top-level `graph` section:\n\n```yaml\ngraph:\n  includePaths:\n    - 'apps/**'\n    - 'packages/**'\n  excludePaths:\n    - '**/*.test.ts'\n    - '**/*.spec.ts'\n    - '**/node_modules/**'\n    - '**/dist/**'\n```\n\n- **`includePaths`**: Project-global glob patterns (relative to the project root) included in the code graph, indexed under the reserved `root:` namespace.\n  - _Default:_ None (all discovered files within registered workspace `codeRoot` directories are indexed).\n  - _Cascade behavior:_ Array — an overlay config **appends** to inherited include paths.\n- **`excludePaths`**: Project-global gitignore-syntax patterns excluded during symbol, file, and spec discovery across all workspaces.\n  - _Default:_ Built-in defaults (`node_modules/`, `.git/`, `.hg/`, `.svn/`, `.specd/`, `dist/`, `build/`, `coverage/`, `.next/`, `.nuxt/`).\n  - _Engine replacement:_ When `graph.excludePaths` is specified in your configuration, it **replaces** the built-in defaults entirely (it does not append to them). If you specify custom exclusions, you should include standard directories you still wish to exclude, or rely on `.gitignore` with `respectGitignore: true`.\n  - _Cascade behavior:_ Array — in configuration cascade, an overlay config file (`specd.local.yaml`) **appends** to the base config file's (`specd.yaml`) exclusion patterns. Workspace-level `graph.excludePaths` are also additive on top of this global set.\n\n---\n\n## Plugins\n\nSpecD supports agent plugins that integrate with coding assistants (Claude Code, Copilot, Codex, OpenCode):\n\n```yaml\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-claude'\n    - name: '@specd/plugin-agent-copilot'\n      config:\n        model: 'gpt-4o'\n```\n\n- **`plugins.agents`**: A list of agent plugin declarations.\n  - `name`: Package or module name of the plugin (e.g. `'@specd/plugin-agent-claude'`).\n  - `config`: Optional plugin-specific configuration record passed to the plugin adapter during initialization.\n  - _Default:_ None (no third-party plugins loaded; built-in plugins activate via project defaults).\n  - _Cascade behavior:_ Array — an overlay config **appends** new plugin declarations to the inherited list. To remove an inherited plugin in an overlay config, use `remove.plugins.agents: [{ name: '@specd/plugin-agent-copilot' }]`.\n\n---\n\n## Config cascade and local variants\n\nSpecD projects support layered configuration through candidate files in the same directory:\n\n- `specd.yaml`: Base project configuration committed to version control.\n- `specd.<variant>.yaml`: Shared variant configuration (e.g. `specd.ci.yaml`).\n- `specd.local.yaml`: Developer-local overrides (automatically gitignored).\n- `specd.local.<variant>.yaml`: Variant-specific developer-local overrides.\n\n### Inheritance with `extends`\n\nA config layer can inherit from prior candidates in the cascade:\n\n```yaml\n# specd.local.yaml\nextends: true # Inherit from the preceding configuration candidate\n\nlogging:\n  level: debug # Override just the log level locally\n```\n\nOr inherit explicitly from another config file path:\n\n```yaml\nextends: '../shared/specd.base.yaml'\n```\n\n### Targeted removals with `remove`\n\nWhen extending a configuration, you can explicitly remove inherited elements:\n\n```yaml\nextends: true\nremove:\n  workspaces:\n    - legacy-app # Exclude a workspace in your local environment\n  context:\n    - id: global-notice # Remove a specific context injection entry\n```\n\n---\n\n## Validating your configuration\n\nRun `specd config validate` at any time to check your configuration:\n\n```bash\nspecd config validate\n```\n\nThis runs a stricter check than the startup validator:\n\n- Unknown workspace qualifiers in context patterns are **errors**, not warnings — a typo silently excludes specs from context, which is a dangerous silent failure in team environments.\n- Patterns that match no specs on disk emit warnings — useful for catching typos early.\n\n### Startup validation matrix\n\nSpecD validates `specd.yaml` before executing any command that requires configuration.\n\n#### Errors that abort startup\n\nThe following conditions are hard errors — SpecD exits immediately:\n\n| Condition                                                                        | Error Cause                                                      |\n| :------------------------------------------------------------------------------- | :--------------------------------------------------------------- |\n| `schema` field is missing                                                        | Config is invalid without a schema reference.                    |\n| `workspaces` section is missing or has no `default` workspace                    | Every project must declare a default workspace.                  |\n| `specs` section is missing in any workspace                                      | SpecD cannot locate specs without a specs adapter path.          |\n| `codeRoot` is missing in any non-`default` workspace                             | Required for non-default workspaces; no sensible default exists. |\n| `storage` section is missing, or `changes` or `archive` sub-key is absent        | Both storage directories are mandatory.                          |\n| `adapter` is missing in any `specs`, `schemas`, or `storage` section             | Required in every storage declaration.                           |\n| Required adapter-specific fields are absent (e.g. `config.path` when `type: fs`) | The adapter cannot function without its required options.        |\n| An adapter name has no registered factory at kernel construction time            | The kernel rejects unknown named adapters with a clear error.    |\n| Storage or config path resolves outside the repository root                      | Paths must remain within the VCS repository root boundary.       |\n| Invalid `contextIncludeSpecs` or `contextExcludeSpecs` pattern syntax            | e.g. `*` in a disallowed position.                               |\n| `llmOptimizedContext` is not a boolean                                           | Any other type is rejected.                                      |\n| Legacy `artifactRules` or `skills` fields are present                            | Use `schemaOverrides` or the plugin system instead.              |\n\n#### Warnings that allow startup to proceed\n\n| Condition                                                                     | Warning Behavior                                                               |\n| :---------------------------------------------------------------------------- | :----------------------------------------------------------------------------- |\n| Duplicate workspace names                                                     | YAML parser retains last-wins; the duplicate is flagged as a probable error.   |\n| Unknown workspace qualifier in a context pattern (runtime)                    | A typo silently excludes specs; warned at runtime.                             |\n| Legacy configuration format declared (`adapter: fs` with sibling `fs:` block) | Emits a deprecation warning advising migration to `adapter: { type, config }`. |\n\n#### Commands that skip validation entirely\n\nThe following lightweight or bootstrap commands do not require a valid `specd.yaml`:\n\n- `--help`, `--version`\n- `specd project init`\n- `specd config validate`\n- `specd plugin` subcommands\n- `specd graph` commands run with `--path <repo-root>` (bootstrap mode)\n\n## Complete configuration reference and cascade rules\n\nThe table below catalogs every configuration option supported by `specd.yaml` and its cascade layers, including its level, data type, default value, how it behaves under the layered configuration cascade (whether overlay values **replace** base scalars, **append** to arrays, or **deep-merge** objects), and its operational effect.\n\n| Option                                     | Level         | Type                                                            | Default                                          | Cascade Rule                                  | Effect & Description                                                                                                                                                             |\n| :----------------------------------------- | :------------ | :-------------------------------------------------------------- | :----------------------------------------------- | :-------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `schema`                                   | Project       | `string`                                                        | _(Required)_                                     | **Replaces**                                  | Reference to the active workflow schema (e.g. `'@specd/schema-std'`, `'#workspace:schema'`, `'./path'`). Overridden by `SPECD_SCHEMA`.                                           |\n| `specdPath`                                | Project       | `string`                                                        | `'.specd'`                                       | **Replaces**                                  | Directory for SpecD state and change tracking directories (`changes/`, `drafts/`, `discarded/`, `archive/`).                                                                     |\n| `configPath`                               | Project       | `string`                                                        | `'.specd/config'`                                | **Replaces**                                  | Root directory for backend-owned operational data (graph databases, logs, temporary scratch files).                                                                              |\n| `workspaces`                               | Project       | `Record<string, Workspace>`                                     | `{ default: ... }`                               | **Deep-merges**                               | Map of workspace definitions. Must contain at least `default`. Merged per workspace name; remove with `remove.workspaces`.                                                       |\n| `workspaces.<name>.specs`                  | Workspace     | `AdapterBinding`                                                | _(Required)_                                     | **Deep-merges**                               | Named adapter binding for specs repository (e.g. `adapter: fs`, `fs.path: specs/`).                                                                                              |\n| `workspaces.<name>.prefix`                 | Workspace     | `string`                                                        | `null` (None)                                    | **Replaces**                                  | Logical path prefix prepended to all spec IDs in this workspace (e.g. `_global` -> `default:_global/spec`).                                                                      |\n| `workspaces.<name>.schemas`                | Workspace     | `AdapterBinding`                                                | `null` (None)                                    | **Deep-merges**                               | Optional adapter binding for workspace-local custom schemas.                                                                                                                     |\n| `workspaces.<name>.codeRoot`               | Workspace     | `string`                                                        | `'.'` for `default`, _(Required)_ for others     | **Replaces**                                  | Filesystem directory where implementation source code lives relative to `specd.yaml`.                                                                                            |\n| `workspaces.<name>.ownership`              | Workspace     | `'owned' \\| 'shared' \\| 'readOnly'`                             | `'owned'` for `default`, `'readOnly'` for others | **Replaces**                                  | Governance relationship: `owned` (fully mutable), `shared` (co-owned), or `readOnly` (context-only).                                                                             |\n| `workspaces.<name>.contextIncludeSpecs`    | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Patterns matching specs in this workspace that must be included in context when this workspace is active.                                                                        |\n| `workspaces.<name>.contextExcludeSpecs`    | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Patterns matching specs in this workspace to exclude from context when this workspace is active.                                                                                 |\n| `workspaces.<name>.graph.respectGitignore` | Workspace     | `boolean`                                                       | `true`                                           | **Replaces**                                  | When `true`, Git-ignored files in `codeRoot` are skipped during code graph indexing. When `false`, they are discovered.                                                          |\n| `workspaces.<name>.graph.excludePaths`     | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Gitignore-syntax patterns relative to `codeRoot` excluded from graph indexing. Additive over project excludes.                                                                   |\n| `workspaces.<name>.graph.allowedPaths`     | Workspace     | `string[]`                                                      | `[]` (Entire codeRoot)                           | **Appends**                                   | Inclusions restricting code graph index visibility to specific subdirectories within `codeRoot`.                                                                                 |\n| `actorProvider`                            | Project       | `string`                                                        | Auto-detected                                    | **Replaces**                                  | Forces a specific VCS actor identity provider (e.g. `'git'`, `'hg'`). Overridden by `SPECD_ACTOR_PROVIDER`.                                                                      |\n| `privacy.mode`                             | Project       | `'hash' \\| 'mask' \\| 'anonymous'`                               | `null` (None)                                    | **Replaces**                                  | Identity obfuscation strategy for manifests and archives. Overridden by `SPECD_PRIVACY_MODE`.                                                                                    |\n| `privacy.salt`                             | Project       | `string`                                                        | `null`                                           | **Replaces**                                  | Secret salt string required when `privacy.mode` is `'hash'`. Overridden by `SPECD_PRIVACY_SALT`.                                                                                 |\n| `privacy.excludeActors`                    | Project       | `string[]`                                                      | `['specd', 'system@getspecd.dev']`               | **Appends** (cascade) / **Replaces defaults** | Case-insensitive actors exempt from obfuscation. When specified, replaces built-in defaults (`specd`, `system@getspecd.dev`). Overlay files in cascade append to base config.    |\n| `privacy.allowedMetadataKeys`              | Project       | `string[]`                                                      | `[]` (Strips all)                                | **Appends**                                   | Whitelist of non-PII metadata keys preserved under active privacy modes.                                                                                                         |\n| `storage.changes`                          | Project       | `AdapterBinding`                                                | `.specd/changes`                                 | **Deep-merges**                               | Named adapter binding for in-progress change directories.                                                                                                                        |\n| `storage.drafts`                           | Project       | `AdapterBinding`                                                | `.specd/drafts`                                  | **Deep-merges**                               | Named adapter binding for shelved drafts.                                                                                                                                        |\n| `storage.discarded`                        | Project       | `AdapterBinding`                                                | `.specd/discarded`                               | **Deep-merges**                               | Named adapter binding for abandoned changes.                                                                                                                                     |\n| `storage.archive`                          | Project       | `AdapterBinding`                                                | `.specd/archive`                                 | **Deep-merges**                               | Named adapter binding for permanent change archive records.                                                                                                                      |\n| `storage.archive.pattern`                  | Project       | `string`                                                        | `'{{change.archivedName}}'`                      | **Replaces**                                  | Nunjucks template defining archive subfolder hierarchy (e.g. `'{{year}}/{{change.archivedName}}'`).                                                                              |\n| `approvals.spec`                           | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, blocks `ready → implementing` until human reviewer records `specd changes approve-spec`.                                                                            |\n| `approvals.signoff`                        | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, blocks `done → archivable` until human reviewer records `specd changes signoff`.                                                                                    |\n| `graph.includePaths`                       | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global file globs included in code graph indexing under reserved `root:` namespace.                                                                                              |\n| `graph.excludePaths`                       | Project       | `string[]`                                                      | Built-in defaults                                | **Appends** (cascade) / **Replaces defaults** | Global file globs excluded from discovery. When specified, replaces built-in defaults (`node_modules/`, `.git/`, `dist/`, etc.). Overlay files in cascade append to base config. |\n| `logging.level`                            | Project       | `'trace' \\| 'debug' \\| 'info' \\| 'warn' \\| 'error' \\| 'silent'` | `'info'`                                         | **Replaces**                                  | Minimum threshold for `{configPath}/log/specd.log`. Overridden by `SPECD_LOG_LEVEL`.                                                                                             |\n| `context`                                  | Project       | `ContextEntry[]`                                                | `[]` (None)                                      | **Appends**                                   | Static `{ file: string }` or `{ instruction: string }` blocks injected into every compiled context for AI agents.                                                                |\n| `contextIncludeSpecs`                      | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global spec inclusion patterns applied across all compiled contexts regardless of active workspace.                                                                              |\n| `contextExcludeSpecs`                      | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global spec exclusion patterns applied across all compiled contexts.                                                                                                             |\n| `contextMode`                              | Project       | `'list' \\| 'summary' \\| 'full' \\| 'hybrid'`                     | `'summary'`                                      | **Replaces**                                  | Spec presentation format in compiled context. Overridden by `SPECD_CONTEXT_MODE`.                                                                                                |\n| `llmOptimizedContext`                      | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, prefers condensed descriptions from metadata cache. Overridden by `SPECD_LLM_OPTIMIZED`.                                                                            |\n| `schemaPlugins`                            | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Schema package references merged into active schema prior to inline `schemaOverrides`.                                                                                           |\n| `schemaOverrides`                          | Project       | `SchemaOverrides`                                               | `null` (None)                                    | **Deep-merges**                               | Inline schema modification operations: `create`, `remove`, `set`, `append`, `prepend`.                                                                                           |\n| `invalidationPolicy`                       | Project       | `'none' \\| 'surgical' \\| 'downstream' \\| 'global'`              | `'downstream'`                                   | **Replaces**                                  | Default artifact invalidation propagation policy assigned to newly created changes.                                                                                              |\n| `plugins.agents`                           | Project       | `PluginEntry[]`                                                 | `[]` (None)                                      | **Appends**                                   | Registered agent plugin definitions (`name` and optional `config`). Remove with `remove.plugins.agents`.                                                                         |\n| `extends`                                  | Cascade Layer | `true \\| string`                                                | `null` (Standalone)                              | _Special_                                     | Controls inheritance: `true` inherits from preceding layer; `<path>` inherits from specific active file.                                                                         |\n| `remove.root`                              | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited top-level fields (e.g. `contextExcludeSpecs`, `actorProvider`). Cannot remove `schema`.                                                                          |\n| `remove.workspaces`                        | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited workspace definitions by name.                                                                                                                                   |\n| `remove.storage`                           | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited storage adapter bindings by key (`changes`, `drafts`, `discarded`, `archive`).                                                                                   |\n| `remove.context`                           | Cascade Layer | `RemovalMatcher[]`                                              | `null`                                           | _Removal_                                     | Drops matching inherited context injection entries by `id`, `file`, or `instruction`.                                                                                            |\n| `remove.plugins.agents`                    | Cascade Layer | `{ name: string }[]`                                            | `null`                                           | _Removal_                                     | Drops inherited agent plugin declarations by package name.                                                                                                                       |\n\n---\n\n## Where to go next\n\n| Topic                                                    | Document                                               |\n| :------------------------------------------------------- | :----------------------------------------------------- |\n| Practical and exhaustive scenario examples               | [Configuration Examples](configuration-examples.md)    |\n| Schema format — authoring or customising a schema        | [Schema Format Reference](../schemas/schema-format.md) |\n| Lifecycle states, transitions, hooks, and approval gates | [Workflow Reference](workflow.md)                      |\n| Full CLI command reference                               | [CLI Reference](../cli/cli-reference.md)               |\n"
      },
      {
        "index": 2,
        "heading": "Getting a specd.yaml",
        "level": 2,
        "startLine": 9,
        "endLine": 16,
        "lines": 8,
        "content": "## Getting a specd.yaml\n\nRun `specd project init` in your project root. SpecD creates a `specd.yaml` with sensible defaults for a single-repo project. You will then edit it to reflect your team's structure and policies.\n\nSpecD discovers its configuration by walking up from the current working directory to the active VCS root, collecting all `specd.yaml`, `specd.*.yaml`, `specd.local.yaml`, and `specd.local.*.yaml` candidate files from the first directory that contains at least one. The candidates are resolved into an active chain, deep-merged, and validated as one config. See [Config cascade and local variants](#config-cascade-and-local-variants) below for details.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "The minimal configuration",
        "level": 2,
        "startLine": 17,
        "endLine": 53,
        "lines": 37,
        "content": "## The minimal configuration\n\nThis is the smallest valid `specd.yaml`. It covers everything SpecD needs to operate:\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\nEvery other field covered in this guide is optional. Start here and add what you need.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "Logging",
        "level": 2,
        "startLine": 54,
        "endLine": 69,
        "lines": 16,
        "content": "## Logging\n\nSpecD supports project-level logging settings in `specd.yaml`:\n\n```yaml\nlogging:\n  level: info # trace | debug | info | warn | error | silent\n```\n\n- The section is optional.\n- If omitted, SpecD defaults to `info`.\n- Logs are written to `{configPath}/log/specd.log` as structured JSON.\n- CLI runtime verbosity can be increased with `-v` / `-vv` for console output without changing file-level defaults.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Environment variables and .env support",
        "level": 2,
        "startLine": 70,
        "endLine": 87,
        "lines": 18,
        "content": "## Environment variables and .env support\n\nSpecD natively supports loading environment variables from `.env` and `.env.local` files in your project root. `.env.local` has higher priority and should be used for secrets that must never be committed (like privacy salts).\n\nThe following variables map directly to root-level configuration settings and **override** values in `specd.yaml` or `specd.local.yaml`:\n\n| Variable               | Configuration Field   |\n| ---------------------- | --------------------- |\n| `SPECD_ACTOR_PROVIDER` | `actorProvider`       |\n| `SPECD_PRIVACY_MODE`   | `privacy.mode`        |\n| `SPECD_PRIVACY_SALT`   | `privacy.salt`        |\n| `SPECD_LOG_LEVEL`      | `logging.level`       |\n| `SPECD_CONTEXT_MODE`   | `contextMode`         |\n| `SPECD_LLM_OPTIMIZED`  | `llmOptimizedContext` |\n| `SPECD_SCHEMA`         | `schemaRef`           |\n\n---\n"
      },
      {
        "index": 6,
        "heading": "Identity resolution",
        "level": 2,
        "startLine": 88,
        "endLine": 103,
        "lines": 16,
        "content": "## Identity resolution\n\nSpecD automatically identifies the user performing an operation by probing the environment. By default, it prioritizes Version Control Systems (Git, Mercurial, Subversion) to resolve the actor's name and email.\n\n### Manual provider selection\n\nIf you need to bypass auto-detection and force a specific identity provider, use the `actorProvider` field:\n\n```yaml\nactorProvider: git # forces git even if other systems are detected\n```\n\nThis is useful in environments where multiple systems might be present or when using a custom identity plugin.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "Manual provider selection",
        "level": 3,
        "startLine": 92,
        "endLine": 103,
        "lines": 12,
        "content": "### Manual provider selection\n\nIf you need to bypass auto-detection and force a specific identity provider, use the `actorProvider` field:\n\n```yaml\nactorProvider: git # forces git even if other systems are detected\n```\n\nThis is useful in environments where multiple systems might be present or when using a custom identity plugin.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Privacy",
        "level": 2,
        "startLine": 104,
        "endLine": 143,
        "lines": 40,
        "content": "## Privacy\n\nFor projects in public repositories, you might want to avoid exposing real names or emails in change manifests and archives. SpecD provides built-in privacy modes to obfuscate identity data:\n\n```yaml\nprivacy:\n  mode: mask # hash | mask | anonymous\n  excludeActors: # optional: skip obfuscation for these actors\n    - 'specd'\n    - 'system@getspecd.dev'\n  allowedMetadataKeys: # optional: preserve specific metadata keys\n    - 'department'\n```\n\n- **`mode`**: Obfuscation mode (`hash`, `mask`, or `anonymous`). _Cascade:_ **Replaces**.\n- **`salt`**: Secret salt string required when `mode` is `hash`. _Cascade:_ **Replaces**.\n- **`excludeActors`**: Actor names or emails exempt from obfuscation.\n  - _Default:_ Built-in defaults (`'specd'`, `'system@getspecd.dev'`).\n  - _Engine replacement:_ When specified in configuration, it **replaces** the built-in defaults entirely (it does not append to them). If you specify custom excluded actors, you must explicitly include `'specd'` or `'system@getspecd.dev'` if you still want them exempt.\n  - _Cascade behavior:_ Array — in configuration cascade, an overlay config file (`specd.local.yaml`) **appends** to the base config file's list.\n- **`allowedMetadataKeys`**: Whitelist of metadata keys preserved under active privacy modes.\n  - _Default:_ None (all metadata stripped).\n  - _Cascade behavior:_ Array — an overlay config file **appends** to the base config file's list.\n\n### Privacy modes\n\n| Mode        | Effect                                                                    |\n| ----------- | ------------------------------------------------------------------------- |\n| `hash`      | Obfuscates email using HMAC-SHA256 with a salt. Requires a `salt`.        |\n| `mask`      | Partially masks name and email (e.g., `j***z@e***.com`).                  |\n| `anonymous` | Replaces all identity data with \"Anonymous\" and `anonymous@getspecd.dev`. |\n\nWhen using `hash` mode, you **must** provide a salt, preferably via the `SPECD_PRIVACY_SALT` environment variable.\n\n### Metadata privacy\n\nBy default, any active privacy mode removes the internal `providerId` and all `metadata` fields to prevent accidental PII leakage. Use `allowedMetadataKeys` to whitelist specific non-sensitive keys you wish to preserve.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "Privacy modes",
        "level": 3,
        "startLine": 128,
        "endLine": 137,
        "lines": 10,
        "content": "### Privacy modes\n\n| Mode        | Effect                                                                    |\n| ----------- | ------------------------------------------------------------------------- |\n| `hash`      | Obfuscates email using HMAC-SHA256 with a salt. Requires a `salt`.        |\n| `mask`      | Partially masks name and email (e.g., `j***z@e***.com`).                  |\n| `anonymous` | Replaces all identity data with \"Anonymous\" and `anonymous@getspecd.dev`. |\n\nWhen using `hash` mode, you **must** provide a salt, preferably via the `SPECD_PRIVACY_SALT` environment variable.\n"
      },
      {
        "index": 10,
        "heading": "Metadata privacy",
        "level": 3,
        "startLine": 138,
        "endLine": 143,
        "lines": 6,
        "content": "### Metadata privacy\n\nBy default, any active privacy mode removes the internal `providerId` and all `metadata` fields to prevent accidental PII leakage. Use `allowedMetadataKeys` to whitelist specific non-sensitive keys you wish to preserve.\n\n---\n"
      },
      {
        "index": 11,
        "heading": "Runtime and state paths: specdPath and configPath",
        "level": 2,
        "startLine": 144,
        "endLine": 167,
        "lines": 24,
        "content": "## Runtime and state paths: specdPath and configPath\n\nSpecD distinguishes between workflow artifact storage and runtime operational state:\n\n```yaml\n# Directory for workflow state: changes, drafts, discarded, archive\nspecdPath: .specd # default: .specd\n\n# Directory for runtime-owned state: graph indexes, scratch files, logs\nconfigPath: .specd/config # default: .specd/config\n```\n\n- **`specdPath`**: Sets the root path for SpecD lifecycle state. Storage bindings (`changes/`, `drafts/`, `discarded/`, `archive/`) resolve relative to this directory.\n  - _Default:_ `.specd`\n  - _Cascade behavior:_ Scalar value — an overlay config **replaces** the base value.\n- **`configPath`**: Sets the root path for backend-owned runtime data. Kept separate from workflow artifacts so caches and logs can be cleaned without touching spec or change history.\n  - `{configPath}/graph` for persisted graph backend databases and indexes.\n  - `{configPath}/tmp` for graph staging and temporary scratch files.\n  - `{configPath}/log/specd.log` for structured logging.\n  - _Default:_ `.specd/config`\n  - _Cascade behavior:_ Scalar value — an overlay config **replaces** the base value.\n\n---\n"
      },
      {
        "index": 12,
        "heading": "Config cascade and local variants",
        "level": 2,
        "startLine": 168,
        "endLine": 301,
        "lines": 134,
        "content": "## Config cascade and local variants\n\nSpecD supports a layered config cascade. Multiple YAML files in the same directory are discovered, resolved into an active chain, deep-merged, and validated as one.\n\n### File naming and discovery order\n\nWhen SpecD searches for configuration, it looks for files in this order within the directory containing `specd.yaml`:\n\n1. `specd.yaml` — the committed project root (always the first active layer)\n2. `specd.*.yaml` — named shared variants (e.g. `specd.ci.yaml`, `specd.staging.yaml`), sorted lexicographically\n3. `specd.local.yaml` — personal local override\n4. `specd.local.*.yaml` — named local variants (e.g. `specd.local.mono.yaml`), sorted lexicographically\n\nDiscovery walks up from the current working directory to the active VCS root and stops at the first directory containing at least one candidate file. `specd.yaml` must exist for discovery to succeed.\n\n### Cascade resolution\n\nEach variant file may declare an `extends` key:\n\n| `extends` value | Behaviour                                                                                         |\n| --------------- | ------------------------------------------------------------------------------------------------- |\n| absent          | File becomes a **standalone root** — all previous layers are discarded and this file starts fresh |\n| `true`          | File inherits from the previous active layer (or from `specd.yaml` if it is the first overlay)    |\n| `<path>`        | File inherits from the named base file, but **only if that base is already in the active chain**  |\n\nResolution builds an active chain starting from `specd.yaml`. Each subsequent candidate is either attached (extends a layer in the chain) or skipped (its base is not active). A file with no `extends` becomes a new standalone root — everything before it is discarded.\n\n### Forced mode (`--config`)\n\nPassing `--config path/to/file.yaml` loads that file as a self-contained closed chain. If the file declares `extends: true`, it resolves against `specd.yaml` in the same directory. If it declares `extends: <path>`, the chain follows explicit links until no file extends further.\n\n### Merge semantics\n\nLayers in the active chain are deep-merged in order:\n\n- **Scalars** — later values replace earlier ones\n- **Objects** — recursively merged\n- **Arrays** — later arrays **append** to earlier ones (no replacement)\n\n### Removals\n\nAn overlay layer may declare a `remove` block to strip entries inherited from prior layers:\n\n```yaml\nextends: true\nremove:\n  root:\n    - contextExcludeSpecs\n  workspaces:\n    - staging\n  context:\n    - id: ci-only-instruction\n    - file: CI_AGENTS.md\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n| Removal target   | Keyed by                       | Effect                                |\n| ---------------- | ------------------------------ | ------------------------------------- |\n| `root`           | field name                     | Removes the named top-level field     |\n| `workspaces`     | workspace name                 | Removes the named workspace           |\n| `storage`        | storage key                    | Removes a storage binding             |\n| `context`        | `id`, `file`, or `instruction` | Removes matching context entries      |\n| `plugins.agents` | `name`                         | Removes a matching plugin declaration |\n\nRemovals are applied immediately after the layer that declares them is merged. A `remove` block without `extends` is an error.\n\n### Gitignored local files\n\n`specd project init` adds both `specd.local.yaml` and `specd.local.*.yaml` to `.gitignore` automatically. Local variants are for personal experimentation: trying a different schema branch, pointing at a local schema copy, or testing a configuration change without affecting the rest of the team.\n\n### Common patterns\n\n**Personal local override:**\n\n```yaml\n# specd.local.yaml\nextends: true\nschema: './my-local-schema'\n```\n\n**CI-specific variant:**\n\n```yaml\n# specd.ci.yaml\nextends: true\ncontext:\n  - instruction: 'Running in CI. Skip interactive prompts.'\nremove:\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n**Standalone local config (fresh start, no inheritance):**\n\n```yaml\n# specd.local.yaml — no extends key\nschema: '@specd/schema-std'\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n```\n\n### Bootstrap mode exceptions (`--path`)\n\nSome `specd graph` CLI commands support an unconfigured repository bootstrap mode for initial indexing and exploration before a project has a `specd.yaml`:\n\n- Passing `--path <repo-root>` forces bootstrap mode and is mutually exclusive with `--config`.\n- When no `specd.yaml` is discovered in the directory walk up to the VCS root, those graph commands fall back to bootstrap mode automatically.\n\nIn bootstrap mode, SpecD behaves as if the repository were a single synthetic `default` workspace with `codeRoot` set to the VCS root. Any discovered `specd.yaml` is ignored when `--path` is explicitly provided. Bootstrap mode is intended for exploratory graph queries; once a project is initialized with `specd project init`, standard configured execution should always be preferred.\n\n---\n"
      },
      {
        "index": 13,
        "heading": "File naming and discovery order",
        "level": 3,
        "startLine": 172,
        "endLine": 182,
        "lines": 11,
        "content": "### File naming and discovery order\n\nWhen SpecD searches for configuration, it looks for files in this order within the directory containing `specd.yaml`:\n\n1. `specd.yaml` — the committed project root (always the first active layer)\n2. `specd.*.yaml` — named shared variants (e.g. `specd.ci.yaml`, `specd.staging.yaml`), sorted lexicographically\n3. `specd.local.yaml` — personal local override\n4. `specd.local.*.yaml` — named local variants (e.g. `specd.local.mono.yaml`), sorted lexicographically\n\nDiscovery walks up from the current working directory to the active VCS root and stops at the first directory containing at least one candidate file. `specd.yaml` must exist for discovery to succeed.\n"
      },
      {
        "index": 14,
        "heading": "Cascade resolution",
        "level": 3,
        "startLine": 183,
        "endLine": 194,
        "lines": 12,
        "content": "### Cascade resolution\n\nEach variant file may declare an `extends` key:\n\n| `extends` value | Behaviour                                                                                         |\n| --------------- | ------------------------------------------------------------------------------------------------- |\n| absent          | File becomes a **standalone root** — all previous layers are discarded and this file starts fresh |\n| `true`          | File inherits from the previous active layer (or from `specd.yaml` if it is the first overlay)    |\n| `<path>`        | File inherits from the named base file, but **only if that base is already in the active chain**  |\n\nResolution builds an active chain starting from `specd.yaml`. Each subsequent candidate is either attached (extends a layer in the chain) or skipped (its base is not active). A file with no `extends` becomes a new standalone root — everything before it is discarded.\n"
      },
      {
        "index": 15,
        "heading": "Forced mode (`--config`)",
        "level": 3,
        "startLine": 195,
        "endLine": 198,
        "lines": 4,
        "content": "### Forced mode (`--config`)\n\nPassing `--config path/to/file.yaml` loads that file as a self-contained closed chain. If the file declares `extends: true`, it resolves against `specd.yaml` in the same directory. If it declares `extends: <path>`, the chain follows explicit links until no file extends further.\n"
      },
      {
        "index": 16,
        "heading": "Merge semantics",
        "level": 3,
        "startLine": 199,
        "endLine": 206,
        "lines": 8,
        "content": "### Merge semantics\n\nLayers in the active chain are deep-merged in order:\n\n- **Scalars** — later values replace earlier ones\n- **Objects** — recursively merged\n- **Arrays** — later arrays **append** to earlier ones (no replacement)\n"
      },
      {
        "index": 17,
        "heading": "Removals",
        "level": 3,
        "startLine": 207,
        "endLine": 235,
        "lines": 29,
        "content": "### Removals\n\nAn overlay layer may declare a `remove` block to strip entries inherited from prior layers:\n\n```yaml\nextends: true\nremove:\n  root:\n    - contextExcludeSpecs\n  workspaces:\n    - staging\n  context:\n    - id: ci-only-instruction\n    - file: CI_AGENTS.md\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n| Removal target   | Keyed by                       | Effect                                |\n| ---------------- | ------------------------------ | ------------------------------------- |\n| `root`           | field name                     | Removes the named top-level field     |\n| `workspaces`     | workspace name                 | Removes the named workspace           |\n| `storage`        | storage key                    | Removes a storage binding             |\n| `context`        | `id`, `file`, or `instruction` | Removes matching context entries      |\n| `plugins.agents` | `name`                         | Removes a matching plugin declaration |\n\nRemovals are applied immediately after the layer that declares them is merged. A `remove` block without `extends` is an error.\n"
      },
      {
        "index": 18,
        "heading": "Gitignored local files",
        "level": 3,
        "startLine": 236,
        "endLine": 239,
        "lines": 4,
        "content": "### Gitignored local files\n\n`specd project init` adds both `specd.local.yaml` and `specd.local.*.yaml` to `.gitignore` automatically. Local variants are for personal experimentation: trying a different schema branch, pointing at a local schema copy, or testing a configuration change without affecting the rest of the team.\n"
      },
      {
        "index": 19,
        "heading": "Common patterns",
        "level": 3,
        "startLine": 240,
        "endLine": 290,
        "lines": 51,
        "content": "### Common patterns\n\n**Personal local override:**\n\n```yaml\n# specd.local.yaml\nextends: true\nschema: './my-local-schema'\n```\n\n**CI-specific variant:**\n\n```yaml\n# specd.ci.yaml\nextends: true\ncontext:\n  - instruction: 'Running in CI. Skip interactive prompts.'\nremove:\n  plugins:\n    agents:\n      - name: '@specd/plugin-agent-copilot'\n```\n\n**Standalone local config (fresh start, no inheritance):**\n\n```yaml\n# specd.local.yaml — no extends key\nschema: '@specd/schema-std'\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n```\n"
      },
      {
        "index": 20,
        "heading": "Bootstrap mode exceptions (`--path`)",
        "level": 3,
        "startLine": 291,
        "endLine": 301,
        "lines": 11,
        "content": "### Bootstrap mode exceptions (`--path`)\n\nSome `specd graph` CLI commands support an unconfigured repository bootstrap mode for initial indexing and exploration before a project has a `specd.yaml`:\n\n- Passing `--path <repo-root>` forces bootstrap mode and is mutually exclusive with `--config`.\n- When no `specd.yaml` is discovered in the directory walk up to the VCS root, those graph commands fall back to bootstrap mode automatically.\n\nIn bootstrap mode, SpecD behaves as if the repository were a single synthetic `default` workspace with `codeRoot` set to the VCS root. Any discovered `specd.yaml` is ignored when `--path` is explicitly provided. Bootstrap mode is intended for exploratory graph queries; once a project is initialized with `specd project init`, standard configured execution should always be preferred.\n\n---\n"
      },
      {
        "index": 21,
        "heading": "Schema selection",
        "level": 2,
        "startLine": 302,
        "endLine": 321,
        "lines": 20,
        "content": "## Schema selection\n\nThe `schema` field tells SpecD which workflow schema to use. Schemas define your artifact types, lifecycle steps, and validation rules. There is exactly one active schema per project.\n\nSpecD resolves the `schema` value using a prefix convention:\n\n| Value                            | Where SpecD looks                                                               |\n| -------------------------------- | ------------------------------------------------------------------------------- |\n| `'@specd/schema-std'`            | npm package in `node_modules/@specd/schema-std/schema.yaml`                     |\n| `'my-workflow'`                  | Bare name — `.specd/schemas/my-workflow/schema.yaml` in the default workspace   |\n| `'#my-workflow'`                 | Hash prefix — equivalent to bare name, explicit about resolving from `default`  |\n| `'#billing:my-schema'`           | Workspace-qualified — resolves from the `billing` workspace's schemas directory |\n| `'./schemas/custom/schema.yaml'` | Relative path from the `specd.yaml` directory                                   |\n\nMost projects start with `@specd/schema-std`. If you need a workflow that differs substantially from the standard one, you can author a local schema and reference it by name. Local schemas live inside your repository and evolve with your project.\n\nTo customise the standard schema without forking it, use `schemaOverrides` — covered below.\n\n---\n"
      },
      {
        "index": 22,
        "heading": "Workspaces",
        "level": 2,
        "startLine": 322,
        "endLine": 530,
        "lines": 209,
        "content": "## Workspaces\n\nA workspace tells SpecD where a set of specs lives and what relationship the project has with them.\n\nEvery `specd.yaml` must declare a `default` workspace. This is the local project workspace — the one the current repository owns. Additional workspaces reference external repositories or sub-directories within a monorepo.\n\n### The default workspace\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThis is the minimum. Two useful optional fields:\n\n- **`codeRoot`** — the directory where implementation code lives, relative to `specd.yaml`. Defaults to the project root. Useful if your code lives in a subdirectory.\n- **`ownership`** — defaults to `owned`, meaning this project freely proposes changes to these specs. The default is almost always correct for the `default` workspace.\n\n### Additional workspaces\n\nIn a monorepo or multi-repo setup, you can declare workspaces that point elsewhere:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/payments/specs\n    codeRoot: packages/payments\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\n`codeRoot` is required for any workspace that is not `default` — there is no sensible default.\n\nThe `ownership` field describes the project's relationship with each workspace's specs:\n\n| Value      | Meaning                                                        |\n| ---------- | -------------------------------------------------------------- |\n| `owned`    | This project freely proposes and modifies specs here.          |\n| `shared`   | Co-owned; changes may require coordination with other teams.   |\n| `readOnly` | This project reads specs for context but does not modify them. |\n\n`readOnly` is the default for non-`default` workspaces. Use it for external dependencies whose specs you want in context but do not control.\n\n### Workspace prefixes\n\nSpec IDs always use the form `workspace:capability-path`. A spec in the `payments` workspace at `specs/checkout.md` has the ID `payments:checkout`. If you want every spec in a workspace to live under a leading path segment — for example to mirror a directory structure — you can declare `prefix`:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture`.\n\n`prefix` does not replace the workspace name in the spec ID. It prepends a path segment to the capability-path portion. In other words:\n\n- workspace name: `default`\n- prefix: `_global`\n- resulting spec ID: `default:_global/architecture`\n\nConcrete example:\n\n- `specsPath` is `specs/_global`\n- the spec lives on disk at `specs/_global/architecture`\n- relative to the workspace root, the spec path is just `architecture`\n\nWithout `prefix`, the spec ID would therefore be `default:architecture`. `prefix: _global` exists precisely to add that lost leading path segment back into the capability-path, producing `default:_global/architecture`.\n\n- _Default:_ None (bare capability paths).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base prefix.\n\n### Workspace schemas\n\nWorkspaces can optionally declare their own schema repository using the `schemas` adapter binding:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas\n```\n\n- When declared, schemas in that directory can be referenced via `#workspace:schema-name` (or `#schema-name` for `default`).\n- _Default:_ `null` (no local workspace schemas; schema references targeting workspaces without `schemas` fail validation).\n- _Cascade behavior:_ Object — deep merged with base workspace config.\n\n### Workspace-level context specs\n\nWorkspaces can define targeted spec inclusion and exclusion filters that activate only when that workspace is touched by a change:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/billing/specs\n    contextIncludeSpecs:\n      - 'billing:api/*' # include billing API specs when billing is active\n    contextExcludeSpecs:\n      - 'billing:internal/*' # exclude internal billing specs\n```\n\n- **`contextIncludeSpecs`**: Patterns matching specs in this workspace that must be included when active.\n  - _Default:_ None (only specs explicitly declared in the change and their dependencies are included).\n  - _Cascade behavior:_ Array — **appends** to inherited include patterns.\n- **`contextExcludeSpecs`**: Patterns matching specs in this workspace to exclude from context.\n  - _Default:_ None.\n  - _Cascade behavior:_ Array — **appends** to inherited exclude patterns.\n\n### Workspace code graph settings\n\nEach workspace can fine-tune its code graph discovery inside its `codeRoot`:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    graph:\n      respectGitignore: true # default: true\n      excludePaths:\n        - 'dist/**'\n        - 'coverage/**'\n      allowedPaths:\n        - 'src/**'\n```\n\n- **`respectGitignore`**: Whether `.gitignore` files inside `codeRoot` are respected during symbol and file indexing.\n  - _Default:_ `true`. When set to `false`, files ignored by Git are still discovered unless matched by `excludePaths`.\n  - _Cascade behavior:_ Scalar boolean — **replaces** the base setting.\n- **`excludePaths`**: Additional gitignore-syntax exclusion patterns relative to `codeRoot`.\n  - _Default:_ None (inherits project-level and built-in defaults).\n  - _Cascade behavior:_ Array — **appends** to project-level and workspace-level excludes.\n\n  **Monorepo child workspace isolation pattern:**\n  In monorepos where `workspaces.default.codeRoot` is `.` (the repository root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` pointing to subdirectories (e.g. `packages/core`, `packages/cli`, `apps/web`), the default workspace would physically crawl and index those directories as well. This leads to duplicate symbol definitions and index collisions.\n\n  To prevent this, configure `workspaces.default.graph.excludePaths` to explicitly exclude child workspace directories:\n\n  ```yaml\n  workspaces:\n    default:\n      prefix: _global\n      specs:\n        adapter: fs\n        fs:\n          path: specs/_global\n      codeRoot: .\n      graph:\n        excludePaths:\n          - node_modules/\n          - .git/\n          - .specd/\n          - dist/\n          - build/\n          - coverage/\n          - packages/ # Indexed independently by child workspaces\n          - apps/ # Indexed independently by child workspaces\n          - specs/\n          - specd-sdd/\n\n    core:\n      specs:\n        adapter: fs\n        fs:\n          path: specs/core\n      codeRoot: packages/core\n  ```\n\n- **`allowedPaths`**: Allowlist restricting graph indexing to specific subdirectories within `codeRoot`.\n  - _Default:_ None (entire `codeRoot` is visible to graph).\n  - _Cascade behavior:_ Array — **appends**.\n\n---\n"
      },
      {
        "index": 23,
        "heading": "The default workspace",
        "level": 3,
        "startLine": 328,
        "endLine": 343,
        "lines": 16,
        "content": "### The default workspace\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThis is the minimum. Two useful optional fields:\n\n- **`codeRoot`** — the directory where implementation code lives, relative to `specd.yaml`. Defaults to the project root. Useful if your code lives in a subdirectory.\n- **`ownership`** — defaults to `owned`, meaning this project freely proposes changes to these specs. The default is almost always correct for the `default` workspace.\n"
      },
      {
        "index": 24,
        "heading": "Additional workspaces",
        "level": 3,
        "startLine": 344,
        "endLine": 384,
        "lines": 41,
        "content": "### Additional workspaces\n\nIn a monorepo or multi-repo setup, you can declare workspaces that point elsewhere:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/payments/specs\n    codeRoot: packages/payments\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\n`codeRoot` is required for any workspace that is not `default` — there is no sensible default.\n\nThe `ownership` field describes the project's relationship with each workspace's specs:\n\n| Value      | Meaning                                                        |\n| ---------- | -------------------------------------------------------------- |\n| `owned`    | This project freely proposes and modifies specs here.          |\n| `shared`   | Co-owned; changes may require coordination with other teams.   |\n| `readOnly` | This project reads specs for context but does not modify them. |\n\n`readOnly` is the default for non-`default` workspaces. Use it for external dependencies whose specs you want in context but do not control.\n"
      },
      {
        "index": 25,
        "heading": "Workspace prefixes",
        "level": 3,
        "startLine": 385,
        "endLine": 417,
        "lines": 33,
        "content": "### Workspace prefixes\n\nSpec IDs always use the form `workspace:capability-path`. A spec in the `payments` workspace at `specs/checkout.md` has the ID `payments:checkout`. If you want every spec in a workspace to live under a leading path segment — for example to mirror a directory structure — you can declare `prefix`:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture`.\n\n`prefix` does not replace the workspace name in the spec ID. It prepends a path segment to the capability-path portion. In other words:\n\n- workspace name: `default`\n- prefix: `_global`\n- resulting spec ID: `default:_global/architecture`\n\nConcrete example:\n\n- `specsPath` is `specs/_global`\n- the spec lives on disk at `specs/_global/architecture`\n- relative to the workspace root, the spec path is just `architecture`\n\nWithout `prefix`, the spec ID would therefore be `default:architecture`. `prefix: _global` exists precisely to add that lost leading path segment back into the capability-path, producing `default:_global/architecture`.\n\n- _Default:_ None (bare capability paths).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base prefix.\n"
      },
      {
        "index": 26,
        "heading": "Workspace schemas",
        "level": 3,
        "startLine": 418,
        "endLine": 438,
        "lines": 21,
        "content": "### Workspace schemas\n\nWorkspaces can optionally declare their own schema repository using the `schemas` adapter binding:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas\n```\n\n- When declared, schemas in that directory can be referenced via `#workspace:schema-name` (or `#schema-name` for `default`).\n- _Default:_ `null` (no local workspace schemas; schema references targeting workspaces without `schemas` fail validation).\n- _Cascade behavior:_ Object — deep merged with base workspace config.\n"
      },
      {
        "index": 27,
        "heading": "Workspace-level context specs",
        "level": 3,
        "startLine": 439,
        "endLine": 462,
        "lines": 24,
        "content": "### Workspace-level context specs\n\nWorkspaces can define targeted spec inclusion and exclusion filters that activate only when that workspace is touched by a change:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/billing/specs\n    contextIncludeSpecs:\n      - 'billing:api/*' # include billing API specs when billing is active\n    contextExcludeSpecs:\n      - 'billing:internal/*' # exclude internal billing specs\n```\n\n- **`contextIncludeSpecs`**: Patterns matching specs in this workspace that must be included when active.\n  - _Default:_ None (only specs explicitly declared in the change and their dependencies are included).\n  - _Cascade behavior:_ Array — **appends** to inherited include patterns.\n- **`contextExcludeSpecs`**: Patterns matching specs in this workspace to exclude from context.\n  - _Default:_ None.\n  - _Cascade behavior:_ Array — **appends** to inherited exclude patterns.\n"
      },
      {
        "index": 28,
        "heading": "Workspace code graph settings",
        "level": 3,
        "startLine": 463,
        "endLine": 530,
        "lines": 68,
        "content": "### Workspace code graph settings\n\nEach workspace can fine-tune its code graph discovery inside its `codeRoot`:\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    graph:\n      respectGitignore: true # default: true\n      excludePaths:\n        - 'dist/**'\n        - 'coverage/**'\n      allowedPaths:\n        - 'src/**'\n```\n\n- **`respectGitignore`**: Whether `.gitignore` files inside `codeRoot` are respected during symbol and file indexing.\n  - _Default:_ `true`. When set to `false`, files ignored by Git are still discovered unless matched by `excludePaths`.\n  - _Cascade behavior:_ Scalar boolean — **replaces** the base setting.\n- **`excludePaths`**: Additional gitignore-syntax exclusion patterns relative to `codeRoot`.\n  - _Default:_ None (inherits project-level and built-in defaults).\n  - _Cascade behavior:_ Array — **appends** to project-level and workspace-level excludes.\n\n  **Monorepo child workspace isolation pattern:**\n  In monorepos where `workspaces.default.codeRoot` is `.` (the repository root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` pointing to subdirectories (e.g. `packages/core`, `packages/cli`, `apps/web`), the default workspace would physically crawl and index those directories as well. This leads to duplicate symbol definitions and index collisions.\n\n  To prevent this, configure `workspaces.default.graph.excludePaths` to explicitly exclude child workspace directories:\n\n  ```yaml\n  workspaces:\n    default:\n      prefix: _global\n      specs:\n        adapter: fs\n        fs:\n          path: specs/_global\n      codeRoot: .\n      graph:\n        excludePaths:\n          - node_modules/\n          - .git/\n          - .specd/\n          - dist/\n          - build/\n          - coverage/\n          - packages/ # Indexed independently by child workspaces\n          - apps/ # Indexed independently by child workspaces\n          - specs/\n          - specd-sdd/\n\n    core:\n      specs:\n        adapter: fs\n        fs:\n          path: specs/core\n      codeRoot: packages/core\n  ```\n\n- **`allowedPaths`**: Allowlist restricting graph indexing to specific subdirectories within `codeRoot`.\n  - _Default:_ None (entire `codeRoot` is visible to graph).\n  - _Cascade behavior:_ Array — **appends**.\n\n---\n"
      },
      {
        "index": 29,
        "heading": "Storage",
        "level": 2,
        "startLine": 531,
        "endLine": 611,
        "lines": 81,
        "content": "## Storage\n\n`storage` declares where SpecD persists changes during their lifecycle. All four sub-keys are required.\n\n```yaml\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\nEach directory holds changes in a different state:\n\n- **`changes`** — active changes currently in progress\n- **`drafts`** — shelved changes that can be restored at any time\n- **`discarded`** — abandoned changes, kept for reference but no longer active\n- **`archive`** — completed changes; the permanent record after archiving\n\nAll paths resolve relative to the `specd.yaml` directory and must stay within the repository root.\n\n### Adapter bindings and kernel extensions\n\nEvery workspace and storage declaration is a named adapter binding:\n\n```yaml\nspecs:\n  adapter: fs\n  fs:\n    path: specs/\n```\n\nThe built-in path is `fs`, but `createKernel(config, options)` and `createKernelBuilder(config)` can register additive storage factories under other adapter names. The config loader preserves the selected adapter name and its opaque config block; the kernel then validates that the named factory exists in the merged registry.\n\nThat split is deliberate:\n\n- `FsConfigLoader` resolves and validates `fs` paths\n- the kernel validates whether non-built-in adapter names are actually registered\n\nUse this when you need a custom storage backend without forking the core workflow model.\n\nBy default, `specd project init` adds `.specd/drafts/` and `.specd/discarded/` to `.gitignore`. Teams who want to commit drafts — for example, to share in-progress work across machines — can remove those entries.\n\n### Organising the archive\n\nBy default, archived changes are stored with the name `{{change.archivedName}}` — a slug prefixed with the archive date, for example `2024-01-15-add-auth-flow`. You can customise this with the `pattern` field:\n\n```yaml\narchive:\n  adapter: fs\n  fs:\n    path: .specd/archive\n    pattern: '{{year}}/{{change.archivedName}}'\n```\n\nThis organises archived changes into yearly subdirectories.\n\n| Variable                  | Value                                    | Example                    |\n| :------------------------ | :--------------------------------------- | :------------------------- |\n| `{{change.archivedName}}` | Full archived directory name (default)   | `2024-01-15-add-auth-flow` |\n| `{{change.name}}`         | Change slug name                         | `add-auth-flow`            |\n| `{{year}}`                | Four-digit calendar year at archive time | `2024`                     |\n| `{{date}}`                | ISO calendar date at archive time        | `2024-01-15`               |\n\n> [!NOTE]\n> `{{change.workspace}}` is not supported because changes in SpecD can touch multiple workspaces simultaneously and have no single primary workspace. Use `{{change.name}}` or `{{change.archivedName}}` instead.\n\n---\n"
      },
      {
        "index": 30,
        "heading": "Adapter bindings and kernel extensions",
        "level": 3,
        "startLine": 564,
        "endLine": 585,
        "lines": 22,
        "content": "### Adapter bindings and kernel extensions\n\nEvery workspace and storage declaration is a named adapter binding:\n\n```yaml\nspecs:\n  adapter: fs\n  fs:\n    path: specs/\n```\n\nThe built-in path is `fs`, but `createKernel(config, options)` and `createKernelBuilder(config)` can register additive storage factories under other adapter names. The config loader preserves the selected adapter name and its opaque config block; the kernel then validates that the named factory exists in the merged registry.\n\nThat split is deliberate:\n\n- `FsConfigLoader` resolves and validates `fs` paths\n- the kernel validates whether non-built-in adapter names are actually registered\n\nUse this when you need a custom storage backend without forking the core workflow model.\n\nBy default, `specd project init` adds `.specd/drafts/` and `.specd/discarded/` to `.gitignore`. Teams who want to commit drafts — for example, to share in-progress work across machines — can remove those entries.\n"
      },
      {
        "index": 31,
        "heading": "Organising the archive",
        "level": 3,
        "startLine": 586,
        "endLine": 611,
        "lines": 26,
        "content": "### Organising the archive\n\nBy default, archived changes are stored with the name `{{change.archivedName}}` — a slug prefixed with the archive date, for example `2024-01-15-add-auth-flow`. You can customise this with the `pattern` field:\n\n```yaml\narchive:\n  adapter: fs\n  fs:\n    path: .specd/archive\n    pattern: '{{year}}/{{change.archivedName}}'\n```\n\nThis organises archived changes into yearly subdirectories.\n\n| Variable                  | Value                                    | Example                    |\n| :------------------------ | :--------------------------------------- | :------------------------- |\n| `{{change.archivedName}}` | Full archived directory name (default)   | `2024-01-15-add-auth-flow` |\n| `{{change.name}}`         | Change slug name                         | `add-auth-flow`            |\n| `{{year}}`                | Four-digit calendar year at archive time | `2024`                     |\n| `{{date}}`                | ISO calendar date at archive time        | `2024-01-15`               |\n\n> [!NOTE]\n> `{{change.workspace}}` is not supported because changes in SpecD can touch multiple workspaces simultaneously and have no single primary workspace. Use `{{change.name}}` or `{{change.archivedName}}` instead.\n\n---\n"
      },
      {
        "index": 32,
        "heading": "Context configuration",
        "level": 2,
        "startLine": 612,
        "endLine": 693,
        "lines": 82,
        "content": "## Context configuration\n\nSpecD compiles a context block for the agent at each lifecycle step. The context includes relevant specs, schema instructions, and any additional content you inject here.\n\n### Injecting files and instructions\n\nThe `context` field at the top level injects content before any spec content, for every change in the project:\n\n```yaml\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Always prefer editing existing files over creating new ones.'\n```\n\n- `file` entries are read at compile time and injected verbatim. If the file does not exist, a warning is emitted and the entry is skipped.\n- `instruction` entries are injected as-is.\n\nThis is the right place for project-wide agent guidance — coding conventions, team norms, architectural principles.\n\n### Controlling which specs are included\n\nBy default, only the specs declared by the change and their explicit dependencies are included in context. You can expand or narrow this with `contextIncludeSpecs` and `contextExcludeSpecs`.\n\nThese can be declared at two levels:\n\n- **Project level** — patterns apply to every compiled context, regardless of which change is active. Use this for specs that should always be present: global constraints, cross-cutting architecture specs.\n- **Workspace level** — patterns apply only when that workspace is active in the current change (i.e., at least one of its specs is listed in the change).\n\n```yaml\n# Always include global constraints\ncontextIncludeSpecs:\n  - 'default:_global/*'\n\n# Never include commit message guidelines (not relevant to agents)\ncontextExcludeSpecs:\n  - 'default:commits'\n```\n\nPattern syntax:\n\n| Pattern              | Matches                                                                                       |\n| -------------------- | --------------------------------------------------------------------------------------------- |\n| `*`                  | All specs in all workspaces (at project level) or this workspace (at workspace level)         |\n| `workspace:*`        | All specs in the named workspace                                                              |\n| `prefix/*`           | All specs under `prefix/`                                                                     |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace                                              |\n| `path/name`          | Exact spec path (resolved from `default` at project level, this workspace at workspace level) |\n\n`*` is only valid in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\n### Spec collection order and priority\n\nDuring context compilation, SpecD collects and filters specs in a deterministic 6-step pipeline:\n\n1. **Change specs (`specIds`)**: Specs directly declared on the active change (`change.specIds`) are seeded and protected from exclusion.\n2. **Direct dependencies (`specDependsOn`)**: Direct dependencies declared on change specs are included.\n3. **Project-level includes (`contextIncludeSpecs`)**: Project-wide glob patterns are matched and added.\n4. **Project-level excludes (`contextExcludeSpecs`)**: Project-wide exclude patterns remove matching non-protected specs.\n5. **Workspace-level filters**: For each active workspace in the change:\n   - Workspace `contextIncludeSpecs` are added.\n   - Workspace `contextExcludeSpecs` are removed.\n6. **Transitive dependency traversal**: When `followDeps` is enabled, specs reachable via `dependsOn` traversal are added (and survive exclude rules).\n\n**Deduplication rule:** A spec matching multiple include rules appears only once in compiled context, retaining first-match ordering.\n\n### Context rendering mode\n\n`contextMode` controls how specs are formatted and presented in the compiled context delivered to AI agents:\n\n```yaml\ncontextMode: summary # default: summary (list | summary | full | hybrid)\n```\n\n- **`summary`** _(default)_ — Renders spec catalogue entries (title and description) extracted from frontmatter and overview headings. This delivers the highest signal-to-token ratio and keeps prompt context compact for routine tasks.\n- **`hybrid`** — Renders specs directly touched or modified by the active change with their complete Markdown content, while transitively referenced specs (via `dependsOn`) are rendered as metadata summaries.\n- **`full`** — Renders every in-context spec with its complete, verbatim Markdown content. Useful for complex refactoring where agents require the full text of all dependencies.\n- **`list`** — Renders only spec identifiers with source and mode metadata (minimal tokens).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base mode.\n- _Environment override:_ `SPECD_CONTEXT_MODE`.\n\n---\n"
      },
      {
        "index": 33,
        "heading": "Injecting files and instructions",
        "level": 3,
        "startLine": 616,
        "endLine": 630,
        "lines": 15,
        "content": "### Injecting files and instructions\n\nThe `context` field at the top level injects content before any spec content, for every change in the project:\n\n```yaml\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Always prefer editing existing files over creating new ones.'\n```\n\n- `file` entries are read at compile time and injected verbatim. If the file does not exist, a warning is emitted and the entry is skipped.\n- `instruction` entries are injected as-is.\n\nThis is the right place for project-wide agent guidance — coding conventions, team norms, architectural principles.\n"
      },
      {
        "index": 34,
        "heading": "Controlling which specs are included",
        "level": 3,
        "startLine": 631,
        "endLine": 661,
        "lines": 31,
        "content": "### Controlling which specs are included\n\nBy default, only the specs declared by the change and their explicit dependencies are included in context. You can expand or narrow this with `contextIncludeSpecs` and `contextExcludeSpecs`.\n\nThese can be declared at two levels:\n\n- **Project level** — patterns apply to every compiled context, regardless of which change is active. Use this for specs that should always be present: global constraints, cross-cutting architecture specs.\n- **Workspace level** — patterns apply only when that workspace is active in the current change (i.e., at least one of its specs is listed in the change).\n\n```yaml\n# Always include global constraints\ncontextIncludeSpecs:\n  - 'default:_global/*'\n\n# Never include commit message guidelines (not relevant to agents)\ncontextExcludeSpecs:\n  - 'default:commits'\n```\n\nPattern syntax:\n\n| Pattern              | Matches                                                                                       |\n| -------------------- | --------------------------------------------------------------------------------------------- |\n| `*`                  | All specs in all workspaces (at project level) or this workspace (at workspace level)         |\n| `workspace:*`        | All specs in the named workspace                                                              |\n| `prefix/*`           | All specs under `prefix/`                                                                     |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace                                              |\n| `path/name`          | Exact spec path (resolved from `default` at project level, this workspace at workspace level) |\n\n`*` is only valid in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n"
      },
      {
        "index": 35,
        "heading": "Spec collection order and priority",
        "level": 3,
        "startLine": 662,
        "endLine": 676,
        "lines": 15,
        "content": "### Spec collection order and priority\n\nDuring context compilation, SpecD collects and filters specs in a deterministic 6-step pipeline:\n\n1. **Change specs (`specIds`)**: Specs directly declared on the active change (`change.specIds`) are seeded and protected from exclusion.\n2. **Direct dependencies (`specDependsOn`)**: Direct dependencies declared on change specs are included.\n3. **Project-level includes (`contextIncludeSpecs`)**: Project-wide glob patterns are matched and added.\n4. **Project-level excludes (`contextExcludeSpecs`)**: Project-wide exclude patterns remove matching non-protected specs.\n5. **Workspace-level filters**: For each active workspace in the change:\n   - Workspace `contextIncludeSpecs` are added.\n   - Workspace `contextExcludeSpecs` are removed.\n6. **Transitive dependency traversal**: When `followDeps` is enabled, specs reachable via `dependsOn` traversal are added (and survive exclude rules).\n\n**Deduplication rule:** A spec matching multiple include rules appears only once in compiled context, retaining first-match ordering.\n"
      },
      {
        "index": 36,
        "heading": "Context rendering mode",
        "level": 3,
        "startLine": 677,
        "endLine": 693,
        "lines": 17,
        "content": "### Context rendering mode\n\n`contextMode` controls how specs are formatted and presented in the compiled context delivered to AI agents:\n\n```yaml\ncontextMode: summary # default: summary (list | summary | full | hybrid)\n```\n\n- **`summary`** _(default)_ — Renders spec catalogue entries (title and description) extracted from frontmatter and overview headings. This delivers the highest signal-to-token ratio and keeps prompt context compact for routine tasks.\n- **`hybrid`** — Renders specs directly touched or modified by the active change with their complete Markdown content, while transitively referenced specs (via `dependsOn`) are rendered as metadata summaries.\n- **`full`** — Renders every in-context spec with its complete, verbatim Markdown content. Useful for complex refactoring where agents require the full text of all dependencies.\n- **`list`** — Renders only spec identifiers with source and mode metadata (minimal tokens).\n- _Cascade behavior:_ Scalar value — an overlay config **replaces** the base mode.\n- _Environment override:_ `SPECD_CONTEXT_MODE`.\n\n---\n"
      },
      {
        "index": 37,
        "heading": "Approval gates",
        "level": 2,
        "startLine": 694,
        "endLine": 718,
        "lines": 25,
        "content": "## Approval gates\n\nBy default, the change lifecycle flows freely. Two optional gates can require explicit human approval before a change advances:\n\n```yaml\napprovals:\n  spec: false # default\n  signoff: false # default\n```\n\nSetting either gate to `true` changes the lifecycle:\n\n| Gate      | Effect when `true`                                                                                                                                     |\n| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `spec`    | `ready → implementing` is blocked until a human runs `specd approve spec`. The change must pass through `pending-spec-approval → spec-approved` first. |\n| `signoff` | `done → archivable` is blocked until a human runs `specd approve signoff`. The change must pass through `pending-signoff → signed-off` first.          |\n\nThe two gates are independent — you can enable either or both.\n\nEnable `spec` approval when your team wants a human review of the spec design before any code is written. Enable `signoff` approval for compliance requirements or regulated environments where a named person must sign off on each completed change before it is committed to the archive.\n\nBoth approval records capture the approver's git identity, a reason, and a hash of the artifacts at approval time — creating a tamper-evident audit trail.\n\n---\n"
      },
      {
        "index": 38,
        "heading": "Invalidation policy",
        "level": 2,
        "startLine": 719,
        "endLine": 749,
        "lines": 31,
        "content": "## Invalidation policy\n\nWhen a change's validated artifacts drift from their baseline (files edited on disk after validation) or when you manually invalidate a change, SpecD reopens artifacts for review. The `invalidationPolicy` field controls how far that reopening propagates:\n\n```yaml\ninvalidationPolicy: downstream # default\n```\n\n| Policy       | What gets reopened                                                                           |\n| ------------ | -------------------------------------------------------------------------------------------- |\n| `none`       | Nothing. Drift is tracked but artifacts stay `complete`. Status shows `complete-with-drift`. |\n| `surgical`   | Only the specific files that changed.                                                        |\n| `downstream` | Changed files plus all artifacts that depend on them in the DAG. This is the default.        |\n| `global`     | Every artifact in the change, regardless of which file triggered the invalidation.           |\n\nThe project-level default is persisted on each change at creation time. You can change it per-change with:\n\n```bash\nspecd changes edit my-change --invalidation-policy surgical\n```\n\nOr override it for a single manual invalidation:\n\n```bash\nspecd changes invalidate my-change --reason \"API changed\" --target specs --policy surgical\n```\n\nUnder `none`, drift is still visible — `changes status` and `changes artifacts` show `complete-with-drift` and a `[drift]` tag — but the lifecycle is not blocked and artifacts are not reopened. Use `none` when you want informational drift tracking without automatic reopening.\n\n---\n"
      },
      {
        "index": 39,
        "heading": "Schema overrides",
        "level": 2,
        "startLine": 750,
        "endLine": 871,
        "lines": 122,
        "content": "## Schema overrides\n\n`schemaOverrides` lets you customise the active schema for your project without forking it or publishing a new package. Changes are applied inline, on top of whatever schema is declared in the `schema` field.\n\nThis is the recommended way to add project-specific automation and rules.\n\n### Operations\n\n| Operation | What it does                                                                |\n| --------- | --------------------------------------------------------------------------- |\n| `append`  | Adds entries to the end of an array (hooks, artifact rules, artifacts)      |\n| `prepend` | Adds entries to the start of an array                                       |\n| `create`  | Adds a new artifact or workflow step that does not exist in the base schema |\n| `set`     | Replaces a scalar field or a whole array entry by identity                  |\n| `remove`  | Removes entries from an array by identity                                   |\n\nEach operation targets one or more schema sections: `artifacts`, `workflow`, or `metadataExtraction`.\n\n### Common uses\n\n**Adding shell hooks to lifecycle steps:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n            - id: run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-tests\n              run: 'pnpm test'\n```\n\nThis runs tests and lint after each implementing step, and runs tests again before archiving. If a `run:` hook exits non-zero, the transition is aborted.\n\n**Adding instruction hooks:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: check-global-compliance\n              instruction: >-\n                Before finishing design, review all artifacts against the global specs.\n                Flag any violation before transitioning to ready.\n```\n\nInstruction hooks inject text into the compiled context at that lifecycle step. Use them to remind the agent of project-specific checks.\n\n**Adding per-artifact rules:**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-adr-references\n              text: 'Architecture decisions must reference an ADR number.'\n```\n\nThis adds a rule injected into the compiled context alongside the schema's own instruction for the `design` artifact. It is additive — it does not replace the schema's instruction.\n\nHook and rule entries in `schemaOverrides` require an `id` field. This is how subsequent `append`, `prepend`, `set`, and `remove` operations identify individual entries within an array.\n\n### Real example: specd's own configuration\n\nThe specd project uses `schemaOverrides` to enforce its own global constraints during development:\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-global-specs-compliance\n              text: >-\n                Cross-reference this design against the global specs:\n                architecture (hexagonal layers, dependency direction, no I/O in domain),\n                conventions (naming, exports, ESM, no default exports, no any),\n                and testing (test structure, coverage). Flag any decision that violates\n                a global constraint before proceeding.\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: designing-check-global-specs\n              instruction: >-\n                Before finishing design, review all artifacts and deltas in this change\n                against the global specs. Check compliance with architecture, conventions,\n                testing, and any other applicable global constraint. Flag violations\n                before transitioning to ready.\n      - step: implementing\n        hooks:\n          post:\n            - id: implementing-run-tests\n              run: 'pnpm test'\n            - id: implementing-run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: archiving-run-tests\n              run: 'pnpm test'\n            - id: archiving-run-lint\n              run: 'pnpm lint'\n```\n\n---\n"
      },
      {
        "index": 40,
        "heading": "Operations",
        "level": 3,
        "startLine": 756,
        "endLine": 767,
        "lines": 12,
        "content": "### Operations\n\n| Operation | What it does                                                                |\n| --------- | --------------------------------------------------------------------------- |\n| `append`  | Adds entries to the end of an array (hooks, artifact rules, artifacts)      |\n| `prepend` | Adds entries to the start of an array                                       |\n| `create`  | Adds a new artifact or workflow step that does not exist in the base schema |\n| `set`     | Replaces a scalar field or a whole array entry by identity                  |\n| `remove`  | Removes entries from an array by identity                                   |\n\nEach operation targets one or more schema sections: `artifacts`, `workflow`, or `metadataExtraction`.\n"
      },
      {
        "index": 41,
        "heading": "Common uses",
        "level": 3,
        "startLine": 768,
        "endLine": 825,
        "lines": 58,
        "content": "### Common uses\n\n**Adding shell hooks to lifecycle steps:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n            - id: run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-tests\n              run: 'pnpm test'\n```\n\nThis runs tests and lint after each implementing step, and runs tests again before archiving. If a `run:` hook exits non-zero, the transition is aborted.\n\n**Adding instruction hooks:**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: check-global-compliance\n              instruction: >-\n                Before finishing design, review all artifacts against the global specs.\n                Flag any violation before transitioning to ready.\n```\n\nInstruction hooks inject text into the compiled context at that lifecycle step. Use them to remind the agent of project-specific checks.\n\n**Adding per-artifact rules:**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-adr-references\n              text: 'Architecture decisions must reference an ADR number.'\n```\n\nThis adds a rule injected into the compiled context alongside the schema's own instruction for the `design` artifact. It is additive — it does not replace the schema's instruction.\n\nHook and rule entries in `schemaOverrides` require an `id` field. This is how subsequent `append`, `prepend`, `set`, and `remove` operations identify individual entries within an array.\n"
      },
      {
        "index": 42,
        "heading": "Real example: specd's own configuration",
        "level": 3,
        "startLine": 826,
        "endLine": 871,
        "lines": 46,
        "content": "### Real example: specd's own configuration\n\nThe specd project uses `schemaOverrides` to enforce its own global constraints during development:\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-global-specs-compliance\n              text: >-\n                Cross-reference this design against the global specs:\n                architecture (hexagonal layers, dependency direction, no I/O in domain),\n                conventions (naming, exports, ESM, no default exports, no any),\n                and testing (test structure, coverage). Flag any decision that violates\n                a global constraint before proceeding.\n    workflow:\n      - step: designing\n        hooks:\n          post:\n            - id: designing-check-global-specs\n              instruction: >-\n                Before finishing design, review all artifacts and deltas in this change\n                against the global specs. Check compliance with architecture, conventions,\n                testing, and any other applicable global constraint. Flag violations\n                before transitioning to ready.\n      - step: implementing\n        hooks:\n          post:\n            - id: implementing-run-tests\n              run: 'pnpm test'\n            - id: implementing-run-lint\n              run: 'pnpm lint'\n      - step: archiving\n        hooks:\n          pre:\n            - id: archiving-run-tests\n              run: 'pnpm test'\n            - id: archiving-run-lint\n              run: 'pnpm lint'\n```\n\n---\n"
      },
      {
        "index": 43,
        "heading": "Schema plugins",
        "level": 2,
        "startLine": 872,
        "endLine": 887,
        "lines": 16,
        "content": "## Schema plugins\n\n`schemaPlugins` lists schema reference strings that are loaded and merged into the active schema before `schemaOverrides` is applied. Each entry uses the same resolution rules as the top-level `schema` field.\n\n```yaml\nschemaPlugins:\n  - '@acme/specd-plugin-compliance'\n  - '#billing:billing-plugin'\n```\n\nPlugins are full schema layers — they can add or modify artifacts, workflow steps, and metadata extraction rules in bulk. They are applied in declaration order. `schemaOverrides` is applied after all plugins, so your inline overrides always take final precedence.\n\nUse plugins when you have a set of schema customisations that is shared across multiple projects and you want to manage it as a versioned package.\n\n---\n"
      },
      {
        "index": 44,
        "heading": "LLM-optimised context",
        "level": 2,
        "startLine": 888,
        "endLine": 911,
        "lines": 24,
        "content": "## LLM-optimised context\n\n```yaml\nllmOptimizedContext: true # default: false\n```\n\nWhen `true`, SpecD prefers LLM-optimized descriptions and context blocks when available. This improves agent performance and reduces token usage by providing a condensed, high-signal representation of specs and project background.\n\nWhen enabled:\n\n- **Agents** populate `optimizedDescription` and `optimizedContext` fields in `metadata.json`.\n- **Compilers** prefer these fields over raw artifact content.\n- **Cache**: Project-level background context is cached in `project-metadata.json` and verified with hashes.\n\nWhen `false` or absent, SpecD uses standard deterministic extraction from Markdown artifacts.\n\nSet this to `true` to unblock advanced agent features and optimize token costs.\n\n- _Default:_ `false`\n- _Cascade behavior:_ Scalar boolean — an overlay config **replaces** the base setting.\n- _Environment override:_ `SPECD_LLM_OPTIMIZED`.\n\n---\n"
      },
      {
        "index": 45,
        "heading": "Code Graph configuration",
        "level": 2,
        "startLine": 912,
        "endLine": 937,
        "lines": 26,
        "content": "## Code Graph configuration\n\nSpecD includes an in-memory and persisted code graph for symbol indexing, dependency blast-radius analysis, and architectural impact assessment. Configure global indexing boundaries with the top-level `graph` section:\n\n```yaml\ngraph:\n  includePaths:\n    - 'apps/**'\n    - 'packages/**'\n  excludePaths:\n    - '**/*.test.ts'\n    - '**/*.spec.ts'\n    - '**/node_modules/**'\n    - '**/dist/**'\n```\n\n- **`includePaths`**: Project-global glob patterns (relative to the project root) included in the code graph, indexed under the reserved `root:` namespace.\n  - _Default:_ None (all discovered files within registered workspace `codeRoot` directories are indexed).\n  - _Cascade behavior:_ Array — an overlay config **appends** to inherited include paths.\n- **`excludePaths`**: Project-global gitignore-syntax patterns excluded during symbol, file, and spec discovery across all workspaces.\n  - _Default:_ Built-in defaults (`node_modules/`, `.git/`, `.hg/`, `.svn/`, `.specd/`, `dist/`, `build/`, `coverage/`, `.next/`, `.nuxt/`).\n  - _Engine replacement:_ When `graph.excludePaths` is specified in your configuration, it **replaces** the built-in defaults entirely (it does not append to them). If you specify custom exclusions, you should include standard directories you still wish to exclude, or rely on `.gitignore` with `respectGitignore: true`.\n  - _Cascade behavior:_ Array — in configuration cascade, an overlay config file (`specd.local.yaml`) **appends** to the base config file's (`specd.yaml`) exclusion patterns. Workspace-level `graph.excludePaths` are also additive on top of this global set.\n\n---\n"
      },
      {
        "index": 46,
        "heading": "Plugins",
        "level": 2,
        "startLine": 938,
        "endLine": 958,
        "lines": 21,
        "content": "## Plugins\n\nSpecD supports agent plugins that integrate with coding assistants (Claude Code, Copilot, Codex, OpenCode):\n\n```yaml\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-claude'\n    - name: '@specd/plugin-agent-copilot'\n      config:\n        model: 'gpt-4o'\n```\n\n- **`plugins.agents`**: A list of agent plugin declarations.\n  - `name`: Package or module name of the plugin (e.g. `'@specd/plugin-agent-claude'`).\n  - `config`: Optional plugin-specific configuration record passed to the plugin adapter during initialization.\n  - _Default:_ None (no third-party plugins loaded; built-in plugins activate via project defaults).\n  - _Cascade behavior:_ Array — an overlay config **appends** new plugin declarations to the inherited list. To remove an inherited plugin in an overlay config, use `remove.plugins.agents: [{ name: '@specd/plugin-agent-copilot' }]`.\n\n---\n"
      },
      {
        "index": 47,
        "heading": "Config cascade and local variants",
        "level": 2,
        "startLine": 959,
        "endLine": 1000,
        "lines": 42,
        "content": "## Config cascade and local variants\n\nSpecD projects support layered configuration through candidate files in the same directory:\n\n- `specd.yaml`: Base project configuration committed to version control.\n- `specd.<variant>.yaml`: Shared variant configuration (e.g. `specd.ci.yaml`).\n- `specd.local.yaml`: Developer-local overrides (automatically gitignored).\n- `specd.local.<variant>.yaml`: Variant-specific developer-local overrides.\n\n### Inheritance with `extends`\n\nA config layer can inherit from prior candidates in the cascade:\n\n```yaml\n# specd.local.yaml\nextends: true # Inherit from the preceding configuration candidate\n\nlogging:\n  level: debug # Override just the log level locally\n```\n\nOr inherit explicitly from another config file path:\n\n```yaml\nextends: '../shared/specd.base.yaml'\n```\n\n### Targeted removals with `remove`\n\nWhen extending a configuration, you can explicitly remove inherited elements:\n\n```yaml\nextends: true\nremove:\n  workspaces:\n    - legacy-app # Exclude a workspace in your local environment\n  context:\n    - id: global-notice # Remove a specific context injection entry\n```\n\n---\n"
      },
      {
        "index": 48,
        "heading": "Inheritance with `extends`",
        "level": 3,
        "startLine": 968,
        "endLine": 985,
        "lines": 18,
        "content": "### Inheritance with `extends`\n\nA config layer can inherit from prior candidates in the cascade:\n\n```yaml\n# specd.local.yaml\nextends: true # Inherit from the preceding configuration candidate\n\nlogging:\n  level: debug # Override just the log level locally\n```\n\nOr inherit explicitly from another config file path:\n\n```yaml\nextends: '../shared/specd.base.yaml'\n```\n"
      },
      {
        "index": 49,
        "heading": "Targeted removals with `remove`",
        "level": 3,
        "startLine": 986,
        "endLine": 1000,
        "lines": 15,
        "content": "### Targeted removals with `remove`\n\nWhen extending a configuration, you can explicitly remove inherited elements:\n\n```yaml\nextends: true\nremove:\n  workspaces:\n    - legacy-app # Exclude a workspace in your local environment\n  context:\n    - id: global-notice # Remove a specific context injection entry\n```\n\n---\n"
      },
      {
        "index": 50,
        "heading": "Validating your configuration",
        "level": 2,
        "startLine": 1001,
        "endLine": 1054,
        "lines": 54,
        "content": "## Validating your configuration\n\nRun `specd config validate` at any time to check your configuration:\n\n```bash\nspecd config validate\n```\n\nThis runs a stricter check than the startup validator:\n\n- Unknown workspace qualifiers in context patterns are **errors**, not warnings — a typo silently excludes specs from context, which is a dangerous silent failure in team environments.\n- Patterns that match no specs on disk emit warnings — useful for catching typos early.\n\n### Startup validation matrix\n\nSpecD validates `specd.yaml` before executing any command that requires configuration.\n\n#### Errors that abort startup\n\nThe following conditions are hard errors — SpecD exits immediately:\n\n| Condition                                                                        | Error Cause                                                      |\n| :------------------------------------------------------------------------------- | :--------------------------------------------------------------- |\n| `schema` field is missing                                                        | Config is invalid without a schema reference.                    |\n| `workspaces` section is missing or has no `default` workspace                    | Every project must declare a default workspace.                  |\n| `specs` section is missing in any workspace                                      | SpecD cannot locate specs without a specs adapter path.          |\n| `codeRoot` is missing in any non-`default` workspace                             | Required for non-default workspaces; no sensible default exists. |\n| `storage` section is missing, or `changes` or `archive` sub-key is absent        | Both storage directories are mandatory.                          |\n| `adapter` is missing in any `specs`, `schemas`, or `storage` section             | Required in every storage declaration.                           |\n| Required adapter-specific fields are absent (e.g. `config.path` when `type: fs`) | The adapter cannot function without its required options.        |\n| An adapter name has no registered factory at kernel construction time            | The kernel rejects unknown named adapters with a clear error.    |\n| Storage or config path resolves outside the repository root                      | Paths must remain within the VCS repository root boundary.       |\n| Invalid `contextIncludeSpecs` or `contextExcludeSpecs` pattern syntax            | e.g. `*` in a disallowed position.                               |\n| `llmOptimizedContext` is not a boolean                                           | Any other type is rejected.                                      |\n| Legacy `artifactRules` or `skills` fields are present                            | Use `schemaOverrides` or the plugin system instead.              |\n\n#### Warnings that allow startup to proceed\n\n| Condition                                                                     | Warning Behavior                                                               |\n| :---------------------------------------------------------------------------- | :----------------------------------------------------------------------------- |\n| Duplicate workspace names                                                     | YAML parser retains last-wins; the duplicate is flagged as a probable error.   |\n| Unknown workspace qualifier in a context pattern (runtime)                    | A typo silently excludes specs; warned at runtime.                             |\n| Legacy configuration format declared (`adapter: fs` with sibling `fs:` block) | Emits a deprecation warning advising migration to `adapter: { type, config }`. |\n\n#### Commands that skip validation entirely\n\nThe following lightweight or bootstrap commands do not require a valid `specd.yaml`:\n\n- `--help`, `--version`\n- `specd project init`\n- `specd config validate`\n- `specd plugin` subcommands\n- `specd graph` commands run with `--path <repo-root>` (bootstrap mode)\n"
      },
      {
        "index": 51,
        "heading": "Startup validation matrix",
        "level": 3,
        "startLine": 1014,
        "endLine": 1054,
        "lines": 41,
        "content": "### Startup validation matrix\n\nSpecD validates `specd.yaml` before executing any command that requires configuration.\n\n#### Errors that abort startup\n\nThe following conditions are hard errors — SpecD exits immediately:\n\n| Condition                                                                        | Error Cause                                                      |\n| :------------------------------------------------------------------------------- | :--------------------------------------------------------------- |\n| `schema` field is missing                                                        | Config is invalid without a schema reference.                    |\n| `workspaces` section is missing or has no `default` workspace                    | Every project must declare a default workspace.                  |\n| `specs` section is missing in any workspace                                      | SpecD cannot locate specs without a specs adapter path.          |\n| `codeRoot` is missing in any non-`default` workspace                             | Required for non-default workspaces; no sensible default exists. |\n| `storage` section is missing, or `changes` or `archive` sub-key is absent        | Both storage directories are mandatory.                          |\n| `adapter` is missing in any `specs`, `schemas`, or `storage` section             | Required in every storage declaration.                           |\n| Required adapter-specific fields are absent (e.g. `config.path` when `type: fs`) | The adapter cannot function without its required options.        |\n| An adapter name has no registered factory at kernel construction time            | The kernel rejects unknown named adapters with a clear error.    |\n| Storage or config path resolves outside the repository root                      | Paths must remain within the VCS repository root boundary.       |\n| Invalid `contextIncludeSpecs` or `contextExcludeSpecs` pattern syntax            | e.g. `*` in a disallowed position.                               |\n| `llmOptimizedContext` is not a boolean                                           | Any other type is rejected.                                      |\n| Legacy `artifactRules` or `skills` fields are present                            | Use `schemaOverrides` or the plugin system instead.              |\n\n#### Warnings that allow startup to proceed\n\n| Condition                                                                     | Warning Behavior                                                               |\n| :---------------------------------------------------------------------------- | :----------------------------------------------------------------------------- |\n| Duplicate workspace names                                                     | YAML parser retains last-wins; the duplicate is flagged as a probable error.   |\n| Unknown workspace qualifier in a context pattern (runtime)                    | A typo silently excludes specs; warned at runtime.                             |\n| Legacy configuration format declared (`adapter: fs` with sibling `fs:` block) | Emits a deprecation warning advising migration to `adapter: { type, config }`. |\n\n#### Commands that skip validation entirely\n\nThe following lightweight or bootstrap commands do not require a valid `specd.yaml`:\n\n- `--help`, `--version`\n- `specd project init`\n- `specd config validate`\n- `specd plugin` subcommands\n- `specd graph` commands run with `--path <repo-root>` (bootstrap mode)\n"
      },
      {
        "index": 52,
        "heading": "Errors that abort startup",
        "level": 4,
        "startLine": 1018,
        "endLine": 1036,
        "lines": 19,
        "content": "#### Errors that abort startup\n\nThe following conditions are hard errors — SpecD exits immediately:\n\n| Condition                                                                        | Error Cause                                                      |\n| :------------------------------------------------------------------------------- | :--------------------------------------------------------------- |\n| `schema` field is missing                                                        | Config is invalid without a schema reference.                    |\n| `workspaces` section is missing or has no `default` workspace                    | Every project must declare a default workspace.                  |\n| `specs` section is missing in any workspace                                      | SpecD cannot locate specs without a specs adapter path.          |\n| `codeRoot` is missing in any non-`default` workspace                             | Required for non-default workspaces; no sensible default exists. |\n| `storage` section is missing, or `changes` or `archive` sub-key is absent        | Both storage directories are mandatory.                          |\n| `adapter` is missing in any `specs`, `schemas`, or `storage` section             | Required in every storage declaration.                           |\n| Required adapter-specific fields are absent (e.g. `config.path` when `type: fs`) | The adapter cannot function without its required options.        |\n| An adapter name has no registered factory at kernel construction time            | The kernel rejects unknown named adapters with a clear error.    |\n| Storage or config path resolves outside the repository root                      | Paths must remain within the VCS repository root boundary.       |\n| Invalid `contextIncludeSpecs` or `contextExcludeSpecs` pattern syntax            | e.g. `*` in a disallowed position.                               |\n| `llmOptimizedContext` is not a boolean                                           | Any other type is rejected.                                      |\n| Legacy `artifactRules` or `skills` fields are present                            | Use `schemaOverrides` or the plugin system instead.              |\n"
      },
      {
        "index": 53,
        "heading": "Warnings that allow startup to proceed",
        "level": 4,
        "startLine": 1037,
        "endLine": 1044,
        "lines": 8,
        "content": "#### Warnings that allow startup to proceed\n\n| Condition                                                                     | Warning Behavior                                                               |\n| :---------------------------------------------------------------------------- | :----------------------------------------------------------------------------- |\n| Duplicate workspace names                                                     | YAML parser retains last-wins; the duplicate is flagged as a probable error.   |\n| Unknown workspace qualifier in a context pattern (runtime)                    | A typo silently excludes specs; warned at runtime.                             |\n| Legacy configuration format declared (`adapter: fs` with sibling `fs:` block) | Emits a deprecation warning advising migration to `adapter: { type, config }`. |\n"
      },
      {
        "index": 54,
        "heading": "Commands that skip validation entirely",
        "level": 4,
        "startLine": 1045,
        "endLine": 1054,
        "lines": 10,
        "content": "#### Commands that skip validation entirely\n\nThe following lightweight or bootstrap commands do not require a valid `specd.yaml`:\n\n- `--help`, `--version`\n- `specd project init`\n- `specd config validate`\n- `specd plugin` subcommands\n- `specd graph` commands run with `--path <repo-root>` (bootstrap mode)\n"
      },
      {
        "index": 55,
        "heading": "Complete configuration reference and cascade rules",
        "level": 2,
        "startLine": 1055,
        "endLine": 1107,
        "lines": 53,
        "content": "## Complete configuration reference and cascade rules\n\nThe table below catalogs every configuration option supported by `specd.yaml` and its cascade layers, including its level, data type, default value, how it behaves under the layered configuration cascade (whether overlay values **replace** base scalars, **append** to arrays, or **deep-merge** objects), and its operational effect.\n\n| Option                                     | Level         | Type                                                            | Default                                          | Cascade Rule                                  | Effect & Description                                                                                                                                                             |\n| :----------------------------------------- | :------------ | :-------------------------------------------------------------- | :----------------------------------------------- | :-------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `schema`                                   | Project       | `string`                                                        | _(Required)_                                     | **Replaces**                                  | Reference to the active workflow schema (e.g. `'@specd/schema-std'`, `'#workspace:schema'`, `'./path'`). Overridden by `SPECD_SCHEMA`.                                           |\n| `specdPath`                                | Project       | `string`                                                        | `'.specd'`                                       | **Replaces**                                  | Directory for SpecD state and change tracking directories (`changes/`, `drafts/`, `discarded/`, `archive/`).                                                                     |\n| `configPath`                               | Project       | `string`                                                        | `'.specd/config'`                                | **Replaces**                                  | Root directory for backend-owned operational data (graph databases, logs, temporary scratch files).                                                                              |\n| `workspaces`                               | Project       | `Record<string, Workspace>`                                     | `{ default: ... }`                               | **Deep-merges**                               | Map of workspace definitions. Must contain at least `default`. Merged per workspace name; remove with `remove.workspaces`.                                                       |\n| `workspaces.<name>.specs`                  | Workspace     | `AdapterBinding`                                                | _(Required)_                                     | **Deep-merges**                               | Named adapter binding for specs repository (e.g. `adapter: fs`, `fs.path: specs/`).                                                                                              |\n| `workspaces.<name>.prefix`                 | Workspace     | `string`                                                        | `null` (None)                                    | **Replaces**                                  | Logical path prefix prepended to all spec IDs in this workspace (e.g. `_global` -> `default:_global/spec`).                                                                      |\n| `workspaces.<name>.schemas`                | Workspace     | `AdapterBinding`                                                | `null` (None)                                    | **Deep-merges**                               | Optional adapter binding for workspace-local custom schemas.                                                                                                                     |\n| `workspaces.<name>.codeRoot`               | Workspace     | `string`                                                        | `'.'` for `default`, _(Required)_ for others     | **Replaces**                                  | Filesystem directory where implementation source code lives relative to `specd.yaml`.                                                                                            |\n| `workspaces.<name>.ownership`              | Workspace     | `'owned' \\| 'shared' \\| 'readOnly'`                             | `'owned'` for `default`, `'readOnly'` for others | **Replaces**                                  | Governance relationship: `owned` (fully mutable), `shared` (co-owned), or `readOnly` (context-only).                                                                             |\n| `workspaces.<name>.contextIncludeSpecs`    | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Patterns matching specs in this workspace that must be included in context when this workspace is active.                                                                        |\n| `workspaces.<name>.contextExcludeSpecs`    | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Patterns matching specs in this workspace to exclude from context when this workspace is active.                                                                                 |\n| `workspaces.<name>.graph.respectGitignore` | Workspace     | `boolean`                                                       | `true`                                           | **Replaces**                                  | When `true`, Git-ignored files in `codeRoot` are skipped during code graph indexing. When `false`, they are discovered.                                                          |\n| `workspaces.<name>.graph.excludePaths`     | Workspace     | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Gitignore-syntax patterns relative to `codeRoot` excluded from graph indexing. Additive over project excludes.                                                                   |\n| `workspaces.<name>.graph.allowedPaths`     | Workspace     | `string[]`                                                      | `[]` (Entire codeRoot)                           | **Appends**                                   | Inclusions restricting code graph index visibility to specific subdirectories within `codeRoot`.                                                                                 |\n| `actorProvider`                            | Project       | `string`                                                        | Auto-detected                                    | **Replaces**                                  | Forces a specific VCS actor identity provider (e.g. `'git'`, `'hg'`). Overridden by `SPECD_ACTOR_PROVIDER`.                                                                      |\n| `privacy.mode`                             | Project       | `'hash' \\| 'mask' \\| 'anonymous'`                               | `null` (None)                                    | **Replaces**                                  | Identity obfuscation strategy for manifests and archives. Overridden by `SPECD_PRIVACY_MODE`.                                                                                    |\n| `privacy.salt`                             | Project       | `string`                                                        | `null`                                           | **Replaces**                                  | Secret salt string required when `privacy.mode` is `'hash'`. Overridden by `SPECD_PRIVACY_SALT`.                                                                                 |\n| `privacy.excludeActors`                    | Project       | `string[]`                                                      | `['specd', 'system@getspecd.dev']`               | **Appends** (cascade) / **Replaces defaults** | Case-insensitive actors exempt from obfuscation. When specified, replaces built-in defaults (`specd`, `system@getspecd.dev`). Overlay files in cascade append to base config.    |\n| `privacy.allowedMetadataKeys`              | Project       | `string[]`                                                      | `[]` (Strips all)                                | **Appends**                                   | Whitelist of non-PII metadata keys preserved under active privacy modes.                                                                                                         |\n| `storage.changes`                          | Project       | `AdapterBinding`                                                | `.specd/changes`                                 | **Deep-merges**                               | Named adapter binding for in-progress change directories.                                                                                                                        |\n| `storage.drafts`                           | Project       | `AdapterBinding`                                                | `.specd/drafts`                                  | **Deep-merges**                               | Named adapter binding for shelved drafts.                                                                                                                                        |\n| `storage.discarded`                        | Project       | `AdapterBinding`                                                | `.specd/discarded`                               | **Deep-merges**                               | Named adapter binding for abandoned changes.                                                                                                                                     |\n| `storage.archive`                          | Project       | `AdapterBinding`                                                | `.specd/archive`                                 | **Deep-merges**                               | Named adapter binding for permanent change archive records.                                                                                                                      |\n| `storage.archive.pattern`                  | Project       | `string`                                                        | `'{{change.archivedName}}'`                      | **Replaces**                                  | Nunjucks template defining archive subfolder hierarchy (e.g. `'{{year}}/{{change.archivedName}}'`).                                                                              |\n| `approvals.spec`                           | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, blocks `ready → implementing` until human reviewer records `specd changes approve-spec`.                                                                            |\n| `approvals.signoff`                        | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, blocks `done → archivable` until human reviewer records `specd changes signoff`.                                                                                    |\n| `graph.includePaths`                       | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global file globs included in code graph indexing under reserved `root:` namespace.                                                                                              |\n| `graph.excludePaths`                       | Project       | `string[]`                                                      | Built-in defaults                                | **Appends** (cascade) / **Replaces defaults** | Global file globs excluded from discovery. When specified, replaces built-in defaults (`node_modules/`, `.git/`, `dist/`, etc.). Overlay files in cascade append to base config. |\n| `logging.level`                            | Project       | `'trace' \\| 'debug' \\| 'info' \\| 'warn' \\| 'error' \\| 'silent'` | `'info'`                                         | **Replaces**                                  | Minimum threshold for `{configPath}/log/specd.log`. Overridden by `SPECD_LOG_LEVEL`.                                                                                             |\n| `context`                                  | Project       | `ContextEntry[]`                                                | `[]` (None)                                      | **Appends**                                   | Static `{ file: string }` or `{ instruction: string }` blocks injected into every compiled context for AI agents.                                                                |\n| `contextIncludeSpecs`                      | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global spec inclusion patterns applied across all compiled contexts regardless of active workspace.                                                                              |\n| `contextExcludeSpecs`                      | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Global spec exclusion patterns applied across all compiled contexts.                                                                                                             |\n| `contextMode`                              | Project       | `'list' \\| 'summary' \\| 'full' \\| 'hybrid'`                     | `'summary'`                                      | **Replaces**                                  | Spec presentation format in compiled context. Overridden by `SPECD_CONTEXT_MODE`.                                                                                                |\n| `llmOptimizedContext`                      | Project       | `boolean`                                                       | `false`                                          | **Replaces**                                  | When `true`, prefers condensed descriptions from metadata cache. Overridden by `SPECD_LLM_OPTIMIZED`.                                                                            |\n| `schemaPlugins`                            | Project       | `string[]`                                                      | `[]` (None)                                      | **Appends**                                   | Schema package references merged into active schema prior to inline `schemaOverrides`.                                                                                           |\n| `schemaOverrides`                          | Project       | `SchemaOverrides`                                               | `null` (None)                                    | **Deep-merges**                               | Inline schema modification operations: `create`, `remove`, `set`, `append`, `prepend`.                                                                                           |\n| `invalidationPolicy`                       | Project       | `'none' \\| 'surgical' \\| 'downstream' \\| 'global'`              | `'downstream'`                                   | **Replaces**                                  | Default artifact invalidation propagation policy assigned to newly created changes.                                                                                              |\n| `plugins.agents`                           | Project       | `PluginEntry[]`                                                 | `[]` (None)                                      | **Appends**                                   | Registered agent plugin definitions (`name` and optional `config`). Remove with `remove.plugins.agents`.                                                                         |\n| `extends`                                  | Cascade Layer | `true \\| string`                                                | `null` (Standalone)                              | _Special_                                     | Controls inheritance: `true` inherits from preceding layer; `<path>` inherits from specific active file.                                                                         |\n| `remove.root`                              | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited top-level fields (e.g. `contextExcludeSpecs`, `actorProvider`). Cannot remove `schema`.                                                                          |\n| `remove.workspaces`                        | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited workspace definitions by name.                                                                                                                                   |\n| `remove.storage`                           | Cascade Layer | `string[]`                                                      | `null`                                           | _Removal_                                     | Drops inherited storage adapter bindings by key (`changes`, `drafts`, `discarded`, `archive`).                                                                                   |\n| `remove.context`                           | Cascade Layer | `RemovalMatcher[]`                                              | `null`                                           | _Removal_                                     | Drops matching inherited context injection entries by `id`, `file`, or `instruction`.                                                                                            |\n| `remove.plugins.agents`                    | Cascade Layer | `{ name: string }[]`                                            | `null`                                           | _Removal_                                     | Drops inherited agent plugin declarations by package name.                                                                                                                       |\n\n---\n"
      },
      {
        "index": 56,
        "heading": "Where to go next",
        "level": 2,
        "startLine": 1108,
        "endLine": 1116,
        "lines": 9,
        "content": "## Where to go next\n\n| Topic                                                    | Document                                               |\n| :------------------------------------------------------- | :----------------------------------------------------- |\n| Practical and exhaustive scenario examples               | [Configuration Examples](configuration-examples.md)    |\n| Schema format — authoring or customising a schema        | [Schema Format Reference](../schemas/schema-format.md) |\n| Lifecycle states, transitions, hooks, and approval gates | [Workflow Reference](workflow.md)                      |\n| Full CLI command reference                               | [CLI Reference](../cli/cli-reference.md)               |\n"
      }
    ]
  },
  {
    "topic": "project-structure",
    "title": "Project Structure & Organization",
    "description": "Guide to repository organization, specifications directory, changes directory, and monorepo structure in SpecD",
    "order": 4,
    "content": "# Project Structure & Organization\n\nSpecD establishes clear boundaries between living specifications, active change workflows, and your application source code.\n\n---\n\n## High-Level Repository Layout\n\nA typical repository managed by SpecD looks like this:\n\n```\nmy-project/\n├── specd.yaml                  # Project configuration root\n├── specs/                      # Living specifications (source of truth)\n│   └── auth/\n│       └── login/\n│           ├── spec.md         # Requirements and normative constraints\n│           ├── verify.md       # Acceptance criteria and test scenarios\n│           └── spec-lock.json  # Persisted semantic state (deps, code links, optimizations)\n├── .specd/                     # Managed by SpecD CLI and agent workflows\n│   ├── changes/                # Active changes currently in progress\n│   │   └── 20260923-add-auth/\n│   │       ├── manifest.yaml   # Machine-readable change metadata & hash baselines\n│   │       ├── proposal.md     # Problem statement and intent\n│   │       ├── specs/          # Proposed new specs or deltas\n│   │       ├── design.md       # Technical design and trade-offs\n│   │       └── tasks.md        # Implementation task checklist\n│   ├── drafts/                 # Shelved or paused changes\n│   ├── discarded/              # Abandoned changes (audit history)\n│   ├── archive/                # Completed and verified changes (permanent history)\n│   └── metadata/               # Gitignored local metadata and search cache\n└── src/                        # Your application source code\n```\n\nThe `specs/` directory holds your specifications. The `.specd/` directory is managed by SpecD and tracks all change state. Your application code sits alongside both — SpecD does not impose any structure on your application code.\n\n---\n\n## 1. The `specs/` Directory\n\nThe `specs/` directory contains living specifications representing the current agreed-upon behavior of your system.\n\n- **Organized by Capability:** Each capability lives in its own directory (e.g. `specs/billing/invoices/`, `specs/auth/login/`).\n- **Paired Documents:** Each capability contains `spec.md` (requirements) and `verify.md` (acceptance criteria).\n- **Semantic Sidecar (`spec-lock.json`):** Persists durable machine-readable metadata alongside each spec, including schema identity, curated dependencies (`dependsOn`), code implementation links, and LLM optimization baselines.\n- **Immutable during Active Changes:** You never edit `specs/` directly when implementing a feature or bugfix. You modify specs through active change deltas, which are merged into `specs/` only when the change is archived.\n\n---\n\n## 2. The `.specd/` Directory\n\nThe `.specd/` directory is the operational engine of the SpecD platform. It tracks changes throughout their lifecycle:\n\n### `changes/`\n\nHolds changes currently in progress. Each change directory is named with a timestamped slug (e.g. `.specd/changes/20260923-add-auth/`).\n\nInside an active change:\n\n- `manifest.yaml`: Machine-readable tracking file recording the change name, lifecycle state, targeted spec IDs, artifact hashes, and audit history.\n- `proposal.md`: Explains what is being built and why.\n- `specs/`: Proposed new spec files, or `deltas/` containing structured YAML patches (`*.delta.yaml`) for existing specs.\n- `design.md`: Technical architecture and implementation strategy.\n- `tasks.md`: Implementation task checklist (`- [ ]` to `- [x]`).\n\n### `drafts/`\n\nWhen work on an active change needs to be paused or shelved, running `specd changes draft <name>` moves it to `.specd/drafts/`. Shelved changes are preserved exactly as they were and can be restored at any time via `specd drafts restore <name>`.\n\n### `discarded/`\n\nWhen a proposed change is abandoned, running `specd changes discard <name>` moves it to `.specd/discarded/`. Retaining discarded changes ensures an immutable audit log of ideas and investigations without cluttering the active workspace.\n\n### `archive/`\n\nWhen a change is verified and approved, running `specd changes archive <name>` applies its delta patches to the living `specs/` directory and archives the entire change package under `.specd/archive/`. Archived records serve as a permanent history of system evolution.\n\n### `metadata/`\n\nContains local indices, SQLite code graph caches, and self-healing materialized metadata projections (`.specd/metadata/<spec>.json`). SpecD automatically adds `.specd/metadata/` to `.gitignore`.\n\n---\n\n## 3. Monorepo Organization\n\nFor multi-package repositories, SpecD supports declared workspaces. Each package can have its own dedicated workspace with isolated specifications and code roots:\n\n```\nmy-monorepo/\n├── specd.yaml                  # Root config declaring all workspaces\n├── packages/\n│   ├── core/\n│   │   ├── specs/              # Specs for the 'core' workspace\n│   │   └── src/\n│   ├── cli/\n│   │   ├── specs/              # Specs for the 'cli' workspace\n│   │   └── src/\n│   └── mcp/\n│       ├── specs/              # Specs for the 'mcp' workspace\n│       └── src/\n└── .specd/                     # Shared project-wide change management\n```\n\nWorkspaces are declared as a map in `specd.yaml`:\n\n```yaml\nworkspaces:\n  default:\n    codeRoot: .\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  core:\n    codeRoot: packages/core\n    specs:\n      adapter: fs\n      fs:\n        path: packages/core/specs\n\n  cli:\n    codeRoot: packages/cli\n    specs:\n      adapter: fs\n      fs:\n        path: packages/cli/specs\n\n  mcp:\n    codeRoot: packages/mcp\n    specs:\n      adapter: fs\n      fs:\n        path: packages/mcp/specs\n```\n\nSee the [Workspaces Guide](./workspaces.md) for detailed configuration, cross-workspace dependencies, and ownership rules.\n",
    "lineCount": 138,
    "byteLength": 5978,
    "outline": [
      {
        "index": 1,
        "heading": "Project Structure & Organization",
        "level": 1,
        "startLine": 1,
        "endLine": 138,
        "lines": 138,
        "content": "# Project Structure & Organization\n\nSpecD establishes clear boundaries between living specifications, active change workflows, and your application source code.\n\n---\n\n## High-Level Repository Layout\n\nA typical repository managed by SpecD looks like this:\n\n```\nmy-project/\n├── specd.yaml                  # Project configuration root\n├── specs/                      # Living specifications (source of truth)\n│   └── auth/\n│       └── login/\n│           ├── spec.md         # Requirements and normative constraints\n│           ├── verify.md       # Acceptance criteria and test scenarios\n│           └── spec-lock.json  # Persisted semantic state (deps, code links, optimizations)\n├── .specd/                     # Managed by SpecD CLI and agent workflows\n│   ├── changes/                # Active changes currently in progress\n│   │   └── 20260923-add-auth/\n│   │       ├── manifest.yaml   # Machine-readable change metadata & hash baselines\n│   │       ├── proposal.md     # Problem statement and intent\n│   │       ├── specs/          # Proposed new specs or deltas\n│   │       ├── design.md       # Technical design and trade-offs\n│   │       └── tasks.md        # Implementation task checklist\n│   ├── drafts/                 # Shelved or paused changes\n│   ├── discarded/              # Abandoned changes (audit history)\n│   ├── archive/                # Completed and verified changes (permanent history)\n│   └── metadata/               # Gitignored local metadata and search cache\n└── src/                        # Your application source code\n```\n\nThe `specs/` directory holds your specifications. The `.specd/` directory is managed by SpecD and tracks all change state. Your application code sits alongside both — SpecD does not impose any structure on your application code.\n\n---\n\n## 1. The `specs/` Directory\n\nThe `specs/` directory contains living specifications representing the current agreed-upon behavior of your system.\n\n- **Organized by Capability:** Each capability lives in its own directory (e.g. `specs/billing/invoices/`, `specs/auth/login/`).\n- **Paired Documents:** Each capability contains `spec.md` (requirements) and `verify.md` (acceptance criteria).\n- **Semantic Sidecar (`spec-lock.json`):** Persists durable machine-readable metadata alongside each spec, including schema identity, curated dependencies (`dependsOn`), code implementation links, and LLM optimization baselines.\n- **Immutable during Active Changes:** You never edit `specs/` directly when implementing a feature or bugfix. You modify specs through active change deltas, which are merged into `specs/` only when the change is archived.\n\n---\n\n## 2. The `.specd/` Directory\n\nThe `.specd/` directory is the operational engine of the SpecD platform. It tracks changes throughout their lifecycle:\n\n### `changes/`\n\nHolds changes currently in progress. Each change directory is named with a timestamped slug (e.g. `.specd/changes/20260923-add-auth/`).\n\nInside an active change:\n\n- `manifest.yaml`: Machine-readable tracking file recording the change name, lifecycle state, targeted spec IDs, artifact hashes, and audit history.\n- `proposal.md`: Explains what is being built and why.\n- `specs/`: Proposed new spec files, or `deltas/` containing structured YAML patches (`*.delta.yaml`) for existing specs.\n- `design.md`: Technical architecture and implementation strategy.\n- `tasks.md`: Implementation task checklist (`- [ ]` to `- [x]`).\n\n### `drafts/`\n\nWhen work on an active change needs to be paused or shelved, running `specd changes draft <name>` moves it to `.specd/drafts/`. Shelved changes are preserved exactly as they were and can be restored at any time via `specd drafts restore <name>`.\n\n### `discarded/`\n\nWhen a proposed change is abandoned, running `specd changes discard <name>` moves it to `.specd/discarded/`. Retaining discarded changes ensures an immutable audit log of ideas and investigations without cluttering the active workspace.\n\n### `archive/`\n\nWhen a change is verified and approved, running `specd changes archive <name>` applies its delta patches to the living `specs/` directory and archives the entire change package under `.specd/archive/`. Archived records serve as a permanent history of system evolution.\n\n### `metadata/`\n\nContains local indices, SQLite code graph caches, and self-healing materialized metadata projections (`.specd/metadata/<spec>.json`). SpecD automatically adds `.specd/metadata/` to `.gitignore`.\n\n---\n\n## 3. Monorepo Organization\n\nFor multi-package repositories, SpecD supports declared workspaces. Each package can have its own dedicated workspace with isolated specifications and code roots:\n\n```\nmy-monorepo/\n├── specd.yaml                  # Root config declaring all workspaces\n├── packages/\n│   ├── core/\n│   │   ├── specs/              # Specs for the 'core' workspace\n│   │   └── src/\n│   ├── cli/\n│   │   ├── specs/              # Specs for the 'cli' workspace\n│   │   └── src/\n│   └── mcp/\n│       ├── specs/              # Specs for the 'mcp' workspace\n│       └── src/\n└── .specd/                     # Shared project-wide change management\n```\n\nWorkspaces are declared as a map in `specd.yaml`:\n\n```yaml\nworkspaces:\n  default:\n    codeRoot: .\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  core:\n    codeRoot: packages/core\n    specs:\n      adapter: fs\n      fs:\n        path: packages/core/specs\n\n  cli:\n    codeRoot: packages/cli\n    specs:\n      adapter: fs\n      fs:\n        path: packages/cli/specs\n\n  mcp:\n    codeRoot: packages/mcp\n    specs:\n      adapter: fs\n      fs:\n        path: packages/mcp/specs\n```\n\nSee the [Workspaces Guide](./workspaces.md) for detailed configuration, cross-workspace dependencies, and ownership rules.\n"
      },
      {
        "index": 2,
        "heading": "High-Level Repository Layout",
        "level": 2,
        "startLine": 7,
        "endLine": 38,
        "lines": 32,
        "content": "## High-Level Repository Layout\n\nA typical repository managed by SpecD looks like this:\n\n```\nmy-project/\n├── specd.yaml                  # Project configuration root\n├── specs/                      # Living specifications (source of truth)\n│   └── auth/\n│       └── login/\n│           ├── spec.md         # Requirements and normative constraints\n│           ├── verify.md       # Acceptance criteria and test scenarios\n│           └── spec-lock.json  # Persisted semantic state (deps, code links, optimizations)\n├── .specd/                     # Managed by SpecD CLI and agent workflows\n│   ├── changes/                # Active changes currently in progress\n│   │   └── 20260923-add-auth/\n│   │       ├── manifest.yaml   # Machine-readable change metadata & hash baselines\n│   │       ├── proposal.md     # Problem statement and intent\n│   │       ├── specs/          # Proposed new specs or deltas\n│   │       ├── design.md       # Technical design and trade-offs\n│   │       └── tasks.md        # Implementation task checklist\n│   ├── drafts/                 # Shelved or paused changes\n│   ├── discarded/              # Abandoned changes (audit history)\n│   ├── archive/                # Completed and verified changes (permanent history)\n│   └── metadata/               # Gitignored local metadata and search cache\n└── src/                        # Your application source code\n```\n\nThe `specs/` directory holds your specifications. The `.specd/` directory is managed by SpecD and tracks all change state. Your application code sits alongside both — SpecD does not impose any structure on your application code.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "1. The `specs/` Directory",
        "level": 2,
        "startLine": 39,
        "endLine": 49,
        "lines": 11,
        "content": "## 1. The `specs/` Directory\n\nThe `specs/` directory contains living specifications representing the current agreed-upon behavior of your system.\n\n- **Organized by Capability:** Each capability lives in its own directory (e.g. `specs/billing/invoices/`, `specs/auth/login/`).\n- **Paired Documents:** Each capability contains `spec.md` (requirements) and `verify.md` (acceptance criteria).\n- **Semantic Sidecar (`spec-lock.json`):** Persists durable machine-readable metadata alongside each spec, including schema identity, curated dependencies (`dependsOn`), code implementation links, and LLM optimization baselines.\n- **Immutable during Active Changes:** You never edit `specs/` directly when implementing a feature or bugfix. You modify specs through active change deltas, which are merged into `specs/` only when the change is archived.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "2. The `.specd/` Directory",
        "level": 2,
        "startLine": 50,
        "endLine": 83,
        "lines": 34,
        "content": "## 2. The `.specd/` Directory\n\nThe `.specd/` directory is the operational engine of the SpecD platform. It tracks changes throughout their lifecycle:\n\n### `changes/`\n\nHolds changes currently in progress. Each change directory is named with a timestamped slug (e.g. `.specd/changes/20260923-add-auth/`).\n\nInside an active change:\n\n- `manifest.yaml`: Machine-readable tracking file recording the change name, lifecycle state, targeted spec IDs, artifact hashes, and audit history.\n- `proposal.md`: Explains what is being built and why.\n- `specs/`: Proposed new spec files, or `deltas/` containing structured YAML patches (`*.delta.yaml`) for existing specs.\n- `design.md`: Technical architecture and implementation strategy.\n- `tasks.md`: Implementation task checklist (`- [ ]` to `- [x]`).\n\n### `drafts/`\n\nWhen work on an active change needs to be paused or shelved, running `specd changes draft <name>` moves it to `.specd/drafts/`. Shelved changes are preserved exactly as they were and can be restored at any time via `specd drafts restore <name>`.\n\n### `discarded/`\n\nWhen a proposed change is abandoned, running `specd changes discard <name>` moves it to `.specd/discarded/`. Retaining discarded changes ensures an immutable audit log of ideas and investigations without cluttering the active workspace.\n\n### `archive/`\n\nWhen a change is verified and approved, running `specd changes archive <name>` applies its delta patches to the living `specs/` directory and archives the entire change package under `.specd/archive/`. Archived records serve as a permanent history of system evolution.\n\n### `metadata/`\n\nContains local indices, SQLite code graph caches, and self-healing materialized metadata projections (`.specd/metadata/<spec>.json`). SpecD automatically adds `.specd/metadata/` to `.gitignore`.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "`changes/`",
        "level": 3,
        "startLine": 54,
        "endLine": 65,
        "lines": 12,
        "content": "### `changes/`\n\nHolds changes currently in progress. Each change directory is named with a timestamped slug (e.g. `.specd/changes/20260923-add-auth/`).\n\nInside an active change:\n\n- `manifest.yaml`: Machine-readable tracking file recording the change name, lifecycle state, targeted spec IDs, artifact hashes, and audit history.\n- `proposal.md`: Explains what is being built and why.\n- `specs/`: Proposed new spec files, or `deltas/` containing structured YAML patches (`*.delta.yaml`) for existing specs.\n- `design.md`: Technical architecture and implementation strategy.\n- `tasks.md`: Implementation task checklist (`- [ ]` to `- [x]`).\n"
      },
      {
        "index": 6,
        "heading": "`drafts/`",
        "level": 3,
        "startLine": 66,
        "endLine": 69,
        "lines": 4,
        "content": "### `drafts/`\n\nWhen work on an active change needs to be paused or shelved, running `specd changes draft <name>` moves it to `.specd/drafts/`. Shelved changes are preserved exactly as they were and can be restored at any time via `specd drafts restore <name>`.\n"
      },
      {
        "index": 7,
        "heading": "`discarded/`",
        "level": 3,
        "startLine": 70,
        "endLine": 73,
        "lines": 4,
        "content": "### `discarded/`\n\nWhen a proposed change is abandoned, running `specd changes discard <name>` moves it to `.specd/discarded/`. Retaining discarded changes ensures an immutable audit log of ideas and investigations without cluttering the active workspace.\n"
      },
      {
        "index": 8,
        "heading": "`archive/`",
        "level": 3,
        "startLine": 74,
        "endLine": 77,
        "lines": 4,
        "content": "### `archive/`\n\nWhen a change is verified and approved, running `specd changes archive <name>` applies its delta patches to the living `specs/` directory and archives the entire change package under `.specd/archive/`. Archived records serve as a permanent history of system evolution.\n"
      },
      {
        "index": 9,
        "heading": "`metadata/`",
        "level": 3,
        "startLine": 78,
        "endLine": 83,
        "lines": 6,
        "content": "### `metadata/`\n\nContains local indices, SQLite code graph caches, and self-healing materialized metadata projections (`.specd/metadata/<spec>.json`). SpecD automatically adds `.specd/metadata/` to `.gitignore`.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "3. Monorepo Organization",
        "level": 2,
        "startLine": 84,
        "endLine": 138,
        "lines": 55,
        "content": "## 3. Monorepo Organization\n\nFor multi-package repositories, SpecD supports declared workspaces. Each package can have its own dedicated workspace with isolated specifications and code roots:\n\n```\nmy-monorepo/\n├── specd.yaml                  # Root config declaring all workspaces\n├── packages/\n│   ├── core/\n│   │   ├── specs/              # Specs for the 'core' workspace\n│   │   └── src/\n│   ├── cli/\n│   │   ├── specs/              # Specs for the 'cli' workspace\n│   │   └── src/\n│   └── mcp/\n│       ├── specs/              # Specs for the 'mcp' workspace\n│       └── src/\n└── .specd/                     # Shared project-wide change management\n```\n\nWorkspaces are declared as a map in `specd.yaml`:\n\n```yaml\nworkspaces:\n  default:\n    codeRoot: .\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n  core:\n    codeRoot: packages/core\n    specs:\n      adapter: fs\n      fs:\n        path: packages/core/specs\n\n  cli:\n    codeRoot: packages/cli\n    specs:\n      adapter: fs\n      fs:\n        path: packages/cli/specs\n\n  mcp:\n    codeRoot: packages/mcp\n    specs:\n      adapter: fs\n      fs:\n        path: packages/mcp/specs\n```\n\nSee the [Workspaces Guide](./workspaces.md) for detailed configuration, cross-workspace dependencies, and ownership rules.\n"
      }
    ]
  },
  {
    "topic": "skills",
    "title": "Skills",
    "description": "What skills are, how to use them, and a catalog of every built-in skill with its purpose.",
    "order": 4,
    "content": "# Skills\n\nSkills are slash commands installed in your coding assistant (Claude, Codex, Copilot, etc.) that guide you and the AI agent through the SpecD workflow. They act as the **interface layer** between you and the SpecD CLI — each skill knows how to load context, call the right CLI commands, write or validate artifacts, and tell the agent exactly what to do next.\n\n> For a deeper explanation of how skills fit into the overall lifecycle, see the [Workflow & Lifecycle guide](./workflow.md).\n\n---\n\n## How skills work\n\nWhen you type `/specd` (or any other skill slash command), your coding assistant loads the skill's instruction file and the agent executes the steps inside it. Skills:\n\n1. **Load shared context** — read `shared.md` and project status from the CLI.\n2. **Call CLI commands** — invoke `specd` under the hood to inspect state, create changes, validate artifacts, and transition lifecycle steps.\n3. **Present results and stop** — skills have mandatory stop-points where they present information and wait for your input before continuing. This keeps you in control.\n4. **Write artifacts** — design docs, task lists, spec deltas, and verify files are authored by the agent following skill instructions.\n\nThe agent interacts with the CLI on your behalf; **you never need to know which CLI command to run**. The skill handles that.\n\n```text\nYou  →  /skill-name  →  Agent  →  specd CLI  →  Workflow engine\n```\n\n---\n\n## Available skills\n\n### `/specd` — Entry point and status\n\nThe main entry point. Checks the current project and change status, detects where you are in the lifecycle, and routes you to the correct next skill.\n\n**When to use:** Start here whenever you open a session, return to a project, or are unsure what to do next.\n\n---\n\n### `/specd-new` — Create a new change\n\nExplores your intent, surfaces affected specs, asks clarifying questions, and creates a new change when the scope is clear.\n\n**When to use:** When you want to start a new unit of work. Does **not** write any design artifacts — that's `/specd-design`.\n\n---\n\n### `/specd-design` — Write design artifacts\n\nWrites one artifact at a time (proposal, spec files, verify scenarios, design doc, tasks) for the active change. Validates each artifact before proceeding. Supports fast-forward mode (`--ff`) to write all remaining artifacts without stopping.\n\n**When to use:** When a change is in `designing` state and you need to author or revise its artifacts.\n\n---\n\n### `/specd-implement` — Write code\n\nImplements the code described by the change's `design.md` and `tasks.md`. Works through tasks one by one, runs tests, and marks tasks complete as it goes.\n\n**When to use:** When a change is in `implementing` state and code needs to be written.\n\n---\n\n### `/specd-verify` — Verify implementation\n\nRuns through the verification scenarios defined in each spec's `verify.md` file. Executes tests, checks output, and reports pass/fail. If all scenarios pass, transitions the change to `done`. If any fail, loops back to implementing.\n\n**When to use:** When a change is in `verifying` state and implementation is complete.\n\n---\n\n### `/specd-archive` — Archive a completed change\n\nReviews deltas and archives the change by merging deltas into canonical project specs. Archiving is the final, irreversible step. The change must already be in `archivable` state.\n\n**When to use:** When a change is `done` (or `archivable`) and you are ready to publish its spec deltas to the repository.\n\n---\n\n### `/specd-compliance` — Audit spec compliance\n\nRead-only auditor that compares the implementation against specs and produces a detailed compliance report. Does **not** modify any code or spec files.\n\n**When to use:** When you want to check that the current code faithfully reflects what the specs say, without making any changes.\n\n---\n\n### `/specd-fasttrack` — Code-first development\n\nFast-track session for code-first features, bugfixes, or spikes. Lets you explore and implement first, then consolidates the evidence into specd design artifacts post-facto. Maintains a live decision journal (`<changePath>/.specd-exploration.md`) so interrupted sessions can always be resumed.\n\n**When to use:** Only when explicitly invoked — for quick bugfixes or exploratory spikes where the design is discovered through implementation rather than planned upfront.\n\n---\n\n## Using skills with agents\n\nAll skills produce output using the SpecD CLI's `--format toon` option where possible, which minimizes token usage while keeping the agent fully informed. The agent instructions embedded in `AGENTS.md` (and `CLAUDE.md`) instruct agents to consult `specd guide [topic]` for on-demand documentation rather than guessing:\n\n```bash\n# Look up any topic from inside a skill session\nspecd guide specs\nspecd guide workflow --section \"Approval Gates\"\nspecd guide search \"delta three-way merge\"\n```\n\n---\n\n## Where skills live\n\nSkills are stored as `.md` files in `.agents/skills/` at the root of the repository:\n\n```\n.agents/skills/\n├── specd/SKILL.md            # /specd\n├── specd-new/SKILL.md        # /specd-new\n├── specd-design/SKILL.md     # /specd-design\n├── specd-implement/SKILL.md  # /specd-implement\n├── specd-verify/SKILL.md     # /specd-verify\n├── specd-archive/SKILL.md    # /specd-archive\n├── specd-compliance/SKILL.md # /specd-compliance\n└── specd-fasttrack/SKILL.md  # /specd-fasttrack\n```\n\nShared context and configuration used by all skills is in `.specd/config/skills/shared/shared.md`.\n\n---\n\n## Next steps\n\n- **[Workflow & Lifecycle](./workflow.md)** — Full walkthrough of lifecycle states, transitions, approval gates, hooks, and a complete end-to-end example showing how skills and CLI interact at each step.\n- **[Changes](./changes.md)** — What a change is, what artifacts it tracks, and how the DAG is organized.\n- **[CLI Reference](./cli.md)** — All CLI commands that skills call under the hood.\n",
    "lineCount": 130,
    "byteLength": 5963,
    "outline": [
      {
        "index": 1,
        "heading": "Skills",
        "level": 1,
        "startLine": 1,
        "endLine": 130,
        "lines": 130,
        "content": "# Skills\n\nSkills are slash commands installed in your coding assistant (Claude, Codex, Copilot, etc.) that guide you and the AI agent through the SpecD workflow. They act as the **interface layer** between you and the SpecD CLI — each skill knows how to load context, call the right CLI commands, write or validate artifacts, and tell the agent exactly what to do next.\n\n> For a deeper explanation of how skills fit into the overall lifecycle, see the [Workflow & Lifecycle guide](./workflow.md).\n\n---\n\n## How skills work\n\nWhen you type `/specd` (or any other skill slash command), your coding assistant loads the skill's instruction file and the agent executes the steps inside it. Skills:\n\n1. **Load shared context** — read `shared.md` and project status from the CLI.\n2. **Call CLI commands** — invoke `specd` under the hood to inspect state, create changes, validate artifacts, and transition lifecycle steps.\n3. **Present results and stop** — skills have mandatory stop-points where they present information and wait for your input before continuing. This keeps you in control.\n4. **Write artifacts** — design docs, task lists, spec deltas, and verify files are authored by the agent following skill instructions.\n\nThe agent interacts with the CLI on your behalf; **you never need to know which CLI command to run**. The skill handles that.\n\n```text\nYou  →  /skill-name  →  Agent  →  specd CLI  →  Workflow engine\n```\n\n---\n\n## Available skills\n\n### `/specd` — Entry point and status\n\nThe main entry point. Checks the current project and change status, detects where you are in the lifecycle, and routes you to the correct next skill.\n\n**When to use:** Start here whenever you open a session, return to a project, or are unsure what to do next.\n\n---\n\n### `/specd-new` — Create a new change\n\nExplores your intent, surfaces affected specs, asks clarifying questions, and creates a new change when the scope is clear.\n\n**When to use:** When you want to start a new unit of work. Does **not** write any design artifacts — that's `/specd-design`.\n\n---\n\n### `/specd-design` — Write design artifacts\n\nWrites one artifact at a time (proposal, spec files, verify scenarios, design doc, tasks) for the active change. Validates each artifact before proceeding. Supports fast-forward mode (`--ff`) to write all remaining artifacts without stopping.\n\n**When to use:** When a change is in `designing` state and you need to author or revise its artifacts.\n\n---\n\n### `/specd-implement` — Write code\n\nImplements the code described by the change's `design.md` and `tasks.md`. Works through tasks one by one, runs tests, and marks tasks complete as it goes.\n\n**When to use:** When a change is in `implementing` state and code needs to be written.\n\n---\n\n### `/specd-verify` — Verify implementation\n\nRuns through the verification scenarios defined in each spec's `verify.md` file. Executes tests, checks output, and reports pass/fail. If all scenarios pass, transitions the change to `done`. If any fail, loops back to implementing.\n\n**When to use:** When a change is in `verifying` state and implementation is complete.\n\n---\n\n### `/specd-archive` — Archive a completed change\n\nReviews deltas and archives the change by merging deltas into canonical project specs. Archiving is the final, irreversible step. The change must already be in `archivable` state.\n\n**When to use:** When a change is `done` (or `archivable`) and you are ready to publish its spec deltas to the repository.\n\n---\n\n### `/specd-compliance` — Audit spec compliance\n\nRead-only auditor that compares the implementation against specs and produces a detailed compliance report. Does **not** modify any code or spec files.\n\n**When to use:** When you want to check that the current code faithfully reflects what the specs say, without making any changes.\n\n---\n\n### `/specd-fasttrack` — Code-first development\n\nFast-track session for code-first features, bugfixes, or spikes. Lets you explore and implement first, then consolidates the evidence into specd design artifacts post-facto. Maintains a live decision journal (`<changePath>/.specd-exploration.md`) so interrupted sessions can always be resumed.\n\n**When to use:** Only when explicitly invoked — for quick bugfixes or exploratory spikes where the design is discovered through implementation rather than planned upfront.\n\n---\n\n## Using skills with agents\n\nAll skills produce output using the SpecD CLI's `--format toon` option where possible, which minimizes token usage while keeping the agent fully informed. The agent instructions embedded in `AGENTS.md` (and `CLAUDE.md`) instruct agents to consult `specd guide [topic]` for on-demand documentation rather than guessing:\n\n```bash\n# Look up any topic from inside a skill session\nspecd guide specs\nspecd guide workflow --section \"Approval Gates\"\nspecd guide search \"delta three-way merge\"\n```\n\n---\n\n## Where skills live\n\nSkills are stored as `.md` files in `.agents/skills/` at the root of the repository:\n\n```\n.agents/skills/\n├── specd/SKILL.md            # /specd\n├── specd-new/SKILL.md        # /specd-new\n├── specd-design/SKILL.md     # /specd-design\n├── specd-implement/SKILL.md  # /specd-implement\n├── specd-verify/SKILL.md     # /specd-verify\n├── specd-archive/SKILL.md    # /specd-archive\n├── specd-compliance/SKILL.md # /specd-compliance\n└── specd-fasttrack/SKILL.md  # /specd-fasttrack\n```\n\nShared context and configuration used by all skills is in `.specd/config/skills/shared/shared.md`.\n\n---\n\n## Next steps\n\n- **[Workflow & Lifecycle](./workflow.md)** — Full walkthrough of lifecycle states, transitions, approval gates, hooks, and a complete end-to-end example showing how skills and CLI interact at each step.\n- **[Changes](./changes.md)** — What a change is, what artifacts it tracks, and how the DAG is organized.\n- **[CLI Reference](./cli.md)** — All CLI commands that skills call under the hood.\n"
      },
      {
        "index": 2,
        "heading": "How skills work",
        "level": 2,
        "startLine": 9,
        "endLine": 25,
        "lines": 17,
        "content": "## How skills work\n\nWhen you type `/specd` (or any other skill slash command), your coding assistant loads the skill's instruction file and the agent executes the steps inside it. Skills:\n\n1. **Load shared context** — read `shared.md` and project status from the CLI.\n2. **Call CLI commands** — invoke `specd` under the hood to inspect state, create changes, validate artifacts, and transition lifecycle steps.\n3. **Present results and stop** — skills have mandatory stop-points where they present information and wait for your input before continuing. This keeps you in control.\n4. **Write artifacts** — design docs, task lists, spec deltas, and verify files are authored by the agent following skill instructions.\n\nThe agent interacts with the CLI on your behalf; **you never need to know which CLI command to run**. The skill handles that.\n\n```text\nYou  →  /skill-name  →  Agent  →  specd CLI  →  Workflow engine\n```\n\n---\n"
      },
      {
        "index": 3,
        "heading": "Available skills",
        "level": 2,
        "startLine": 26,
        "endLine": 91,
        "lines": 66,
        "content": "## Available skills\n\n### `/specd` — Entry point and status\n\nThe main entry point. Checks the current project and change status, detects where you are in the lifecycle, and routes you to the correct next skill.\n\n**When to use:** Start here whenever you open a session, return to a project, or are unsure what to do next.\n\n---\n\n### `/specd-new` — Create a new change\n\nExplores your intent, surfaces affected specs, asks clarifying questions, and creates a new change when the scope is clear.\n\n**When to use:** When you want to start a new unit of work. Does **not** write any design artifacts — that's `/specd-design`.\n\n---\n\n### `/specd-design` — Write design artifacts\n\nWrites one artifact at a time (proposal, spec files, verify scenarios, design doc, tasks) for the active change. Validates each artifact before proceeding. Supports fast-forward mode (`--ff`) to write all remaining artifacts without stopping.\n\n**When to use:** When a change is in `designing` state and you need to author or revise its artifacts.\n\n---\n\n### `/specd-implement` — Write code\n\nImplements the code described by the change's `design.md` and `tasks.md`. Works through tasks one by one, runs tests, and marks tasks complete as it goes.\n\n**When to use:** When a change is in `implementing` state and code needs to be written.\n\n---\n\n### `/specd-verify` — Verify implementation\n\nRuns through the verification scenarios defined in each spec's `verify.md` file. Executes tests, checks output, and reports pass/fail. If all scenarios pass, transitions the change to `done`. If any fail, loops back to implementing.\n\n**When to use:** When a change is in `verifying` state and implementation is complete.\n\n---\n\n### `/specd-archive` — Archive a completed change\n\nReviews deltas and archives the change by merging deltas into canonical project specs. Archiving is the final, irreversible step. The change must already be in `archivable` state.\n\n**When to use:** When a change is `done` (or `archivable`) and you are ready to publish its spec deltas to the repository.\n\n---\n\n### `/specd-compliance` — Audit spec compliance\n\nRead-only auditor that compares the implementation against specs and produces a detailed compliance report. Does **not** modify any code or spec files.\n\n**When to use:** When you want to check that the current code faithfully reflects what the specs say, without making any changes.\n\n---\n\n### `/specd-fasttrack` — Code-first development\n\nFast-track session for code-first features, bugfixes, or spikes. Lets you explore and implement first, then consolidates the evidence into specd design artifacts post-facto. Maintains a live decision journal (`<changePath>/.specd-exploration.md`) so interrupted sessions can always be resumed.\n\n**When to use:** Only when explicitly invoked — for quick bugfixes or exploratory spikes where the design is discovered through implementation rather than planned upfront.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "`/specd` — Entry point and status",
        "level": 3,
        "startLine": 28,
        "endLine": 35,
        "lines": 8,
        "content": "### `/specd` — Entry point and status\n\nThe main entry point. Checks the current project and change status, detects where you are in the lifecycle, and routes you to the correct next skill.\n\n**When to use:** Start here whenever you open a session, return to a project, or are unsure what to do next.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "`/specd-new` — Create a new change",
        "level": 3,
        "startLine": 36,
        "endLine": 43,
        "lines": 8,
        "content": "### `/specd-new` — Create a new change\n\nExplores your intent, surfaces affected specs, asks clarifying questions, and creates a new change when the scope is clear.\n\n**When to use:** When you want to start a new unit of work. Does **not** write any design artifacts — that's `/specd-design`.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "`/specd-design` — Write design artifacts",
        "level": 3,
        "startLine": 44,
        "endLine": 51,
        "lines": 8,
        "content": "### `/specd-design` — Write design artifacts\n\nWrites one artifact at a time (proposal, spec files, verify scenarios, design doc, tasks) for the active change. Validates each artifact before proceeding. Supports fast-forward mode (`--ff`) to write all remaining artifacts without stopping.\n\n**When to use:** When a change is in `designing` state and you need to author or revise its artifacts.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "`/specd-implement` — Write code",
        "level": 3,
        "startLine": 52,
        "endLine": 59,
        "lines": 8,
        "content": "### `/specd-implement` — Write code\n\nImplements the code described by the change's `design.md` and `tasks.md`. Works through tasks one by one, runs tests, and marks tasks complete as it goes.\n\n**When to use:** When a change is in `implementing` state and code needs to be written.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "`/specd-verify` — Verify implementation",
        "level": 3,
        "startLine": 60,
        "endLine": 67,
        "lines": 8,
        "content": "### `/specd-verify` — Verify implementation\n\nRuns through the verification scenarios defined in each spec's `verify.md` file. Executes tests, checks output, and reports pass/fail. If all scenarios pass, transitions the change to `done`. If any fail, loops back to implementing.\n\n**When to use:** When a change is in `verifying` state and implementation is complete.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "`/specd-archive` — Archive a completed change",
        "level": 3,
        "startLine": 68,
        "endLine": 75,
        "lines": 8,
        "content": "### `/specd-archive` — Archive a completed change\n\nReviews deltas and archives the change by merging deltas into canonical project specs. Archiving is the final, irreversible step. The change must already be in `archivable` state.\n\n**When to use:** When a change is `done` (or `archivable`) and you are ready to publish its spec deltas to the repository.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "`/specd-compliance` — Audit spec compliance",
        "level": 3,
        "startLine": 76,
        "endLine": 83,
        "lines": 8,
        "content": "### `/specd-compliance` — Audit spec compliance\n\nRead-only auditor that compares the implementation against specs and produces a detailed compliance report. Does **not** modify any code or spec files.\n\n**When to use:** When you want to check that the current code faithfully reflects what the specs say, without making any changes.\n\n---\n"
      },
      {
        "index": 11,
        "heading": "`/specd-fasttrack` — Code-first development",
        "level": 3,
        "startLine": 84,
        "endLine": 91,
        "lines": 8,
        "content": "### `/specd-fasttrack` — Code-first development\n\nFast-track session for code-first features, bugfixes, or spikes. Lets you explore and implement first, then consolidates the evidence into specd design artifacts post-facto. Maintains a live decision journal (`<changePath>/.specd-exploration.md`) so interrupted sessions can always be resumed.\n\n**When to use:** Only when explicitly invoked — for quick bugfixes or exploratory spikes where the design is discovered through implementation rather than planned upfront.\n\n---\n"
      },
      {
        "index": 12,
        "heading": "Using skills with agents",
        "level": 2,
        "startLine": 92,
        "endLine": 104,
        "lines": 13,
        "content": "## Using skills with agents\n\nAll skills produce output using the SpecD CLI's `--format toon` option where possible, which minimizes token usage while keeping the agent fully informed. The agent instructions embedded in `AGENTS.md` (and `CLAUDE.md`) instruct agents to consult `specd guide [topic]` for on-demand documentation rather than guessing:\n\n```bash\n# Look up any topic from inside a skill session\nspecd guide specs\nspecd guide workflow --section \"Approval Gates\"\nspecd guide search \"delta three-way merge\"\n```\n\n---\n"
      },
      {
        "index": 13,
        "heading": "Where skills live",
        "level": 2,
        "startLine": 105,
        "endLine": 124,
        "lines": 20,
        "content": "## Where skills live\n\nSkills are stored as `.md` files in `.agents/skills/` at the root of the repository:\n\n```\n.agents/skills/\n├── specd/SKILL.md            # /specd\n├── specd-new/SKILL.md        # /specd-new\n├── specd-design/SKILL.md     # /specd-design\n├── specd-implement/SKILL.md  # /specd-implement\n├── specd-verify/SKILL.md     # /specd-verify\n├── specd-archive/SKILL.md    # /specd-archive\n├── specd-compliance/SKILL.md # /specd-compliance\n└── specd-fasttrack/SKILL.md  # /specd-fasttrack\n```\n\nShared context and configuration used by all skills is in `.specd/config/skills/shared/shared.md`.\n\n---\n"
      },
      {
        "index": 14,
        "heading": "Next steps",
        "level": 2,
        "startLine": 125,
        "endLine": 130,
        "lines": 6,
        "content": "## Next steps\n\n- **[Workflow & Lifecycle](./workflow.md)** — Full walkthrough of lifecycle states, transitions, approval gates, hooks, and a complete end-to-end example showing how skills and CLI interact at each step.\n- **[Changes](./changes.md)** — What a change is, what artifacts it tracks, and how the DAG is organized.\n- **[CLI Reference](./cli.md)** — All CLI commands that skills call under the hood.\n"
      }
    ]
  },
  {
    "topic": "workspaces",
    "title": "Workspaces & Multi-Package Organization",
    "description": "Managing monolithic and multi-workspace projects, ownership, and spec locations.",
    "order": 5,
    "content": "# Workspaces\n\nA workspace is the fundamental unit of organization in specd. It groups a set of specs, a code location, and an ownership relationship under a single named entry in `specd.yaml`. Everything in specd that involves specs — context compilation, archiving, change tracking, schema resolution — resolves through workspaces.\n\nThis guide explains what workspaces are, why they exist, when you need more than one, and how they interact with spec IDs, context, and multi-repo setups.\n\n---\n\n## Why workspaces exist\n\nIn a simple project, all specs live in one directory and all code lives in one place. A single `default` workspace handles this perfectly.\n\nBut real projects outgrow that quickly:\n\n**Monorepos with multiple packages.** A monorepo containing `packages/core`, `packages/cli`, and `packages/mcp` needs separate spec directories for each package. Each has its own code root, its own domain, and potentially its own team. Without workspaces, all specs would live in a single flat directory — making it impossible to scope context to the package being worked on.\n\n**Multi-repo microservice architectures.** A payments service needs to read the platform team's API specs for context, but should never modify them. An auth service defines its own specs but needs visibility into the shared contracts. Without workspaces, each repo would be isolated — no cross-repo spec visibility, no coordinated changes.\n\n**Central spec governance.** An architecture team maintains a coordinator repo that governs specs across ten service repos. They need to propose changes to any service's specs, track those changes through the lifecycle, and archive them — all from a single project. Without workspaces, they would need ten separate specd projects with no unified view.\n\n**Mixed ownership.** Some specs are owned by your team. Others are read-only references maintained by another team. You need both in context — but only your specs should be modifiable. Without workspaces, there is no way to express \"include these specs for context but prevent changes to them.\"\n\nWorkspaces solve all of these by letting a single `specd.yaml` declare multiple spec locations, each with its own path, code root, ownership, and context rules.\n\n---\n\n## What is a workspace?\n\nWhen specd needs to work with a spec, it needs to know:\n\n- Where the spec files live on disk\n- Where the implementation code for those specs lives\n- Whether this project is allowed to modify those specs\n- What to call those specs in fully-qualified IDs\n\nA workspace provides all of that. It is not a directory; it is a declaration in `specd.yaml` that names and describes a location for specs and connects it to the rest of the project.\n\nEvery project has at least one workspace: `default`. Most single-repo projects only need that one.\n\n---\n\n## The default workspace\n\n`default` is a reserved name that identifies the local project workspace — the specs this repository owns. Every `specd.yaml` must declare it.\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThe `default` workspace has sensible defaults that make minimal configuration sufficient:\n\n- `codeRoot` — defaults to the project root (the directory containing `specd.yaml`)\n- `ownership` — defaults to `owned`, meaning this project freely creates and modifies specs here\n\nFor most single-repo projects, this is all you ever need. Additional workspaces are for when your project spans multiple spec locations.\n\n---\n\n## Spec IDs and the prefix field\n\nEvery spec in specd has a fully-qualified spec ID in the format `workspace:capability-path`. The workspace name comes first, separated by a colon from the capability path — which mirrors the directory structure inside the workspace's specs directory.\n\nFor example, a spec stored at `specs/auth/login/` in the `default` workspace has the ID `default:auth/login`. A spec in a `payments` workspace at `specs/checkout/` has the ID `payments:checkout`.\n\nBare paths (without a colon) are shorthand for `default:path`. So `auth/login` and `default:auth/login` are equivalent. Internally, specd always uses the fully-qualified form.\n\n### The prefix field\n\nBy default, the capability path starts directly at the first directory inside the workspace's specs root. The optional `prefix` field prepends an additional leading path segment to that capability path, without changing the workspace name:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture` — not `_global:architecture`.\n\nThe workspace is still named `default`, and the workspace name still appears before the colon in spec IDs. The prefix only affects the capability-path portion after the colon.\n\nConcrete example:\n\n- workspace name: `default`\n- `specsPath`: `specs/_global`\n- spec directory on disk: `specs/_global/architecture`\n- relative path inside that workspace root: `architecture`\n\nWithout `prefix`, the spec ID would be `default:architecture`, because `_global` is part of the workspace root and is no longer present in the relative path. `prefix: _global` exists to add that lost leading segment back into the capability-path, producing `default:_global/architecture`.\n\n**When to use a prefix:** when the workspace name and the logical label for your specs diverge — for example, when the `default` workspace holds global constraints that live under a `_global/` directory and should be addressed as such for clarity.\n\nThis is exactly how specd's own project uses it: the `default` workspace has `prefix: _global`, so architecture and conventions specs are referenced as `default:_global/architecture` and `default:_global/conventions`.\n\n---\n\n## Workspace fields\n\nThe full set of fields available on any workspace:\n\n| Field                 | Required           | Default (`default` ws)  | Default (non-`default` ws)            | Description                                            |\n| --------------------- | ------------------ | ----------------------- | ------------------------------------- | ------------------------------------------------------ |\n| `specs`               | always             | —                       | —                                     | Storage adapter and path where spec files live         |\n| `codeRoot`            | non-`default` only | project root            | (must be declared)                    | Directory where implementation code lives              |\n| `schemas`             | no                 | `.specd/schemas`        | (none)                                | Storage adapter and path for named local schemas       |\n| `ownership`           | no                 | `owned`                 | `readOnly`                            | The project's relationship to these specs              |\n| `prefix`              | no                 | (none)                  | (none)                                | Override the qualifier used in spec IDs                |\n| `contextIncludeSpecs` | no                 | (project-level default) | `['*']` (all specs in this workspace) | Include patterns applied when this workspace is active |\n| `contextExcludeSpecs` | no                 | `[]`                    | `[]`                                  | Exclude patterns applied when this workspace is active |\n\nAll relative paths resolve from the directory containing `specd.yaml`.\n\n---\n\n## Ownership: owned, shared, readOnly\n\nThe `ownership` field describes the project's relationship to a workspace's specs. It has three values:\n\n**`owned`** — this project freely creates and modifies specs in this workspace. The default for `default`.\n\n**`shared`** — this project co-owns these specs alongside other teams. Changes may require coordination before being applied. Use this for workspaces where multiple repos contribute specs but none has unilateral authority.\n\n**`readOnly`** — this project reads these specs for context but does not modify them. The default for non-`default` workspaces.\n\n`readOnly` is the most common setting for additional workspaces. It tells specd that you can see the specs in context — and the agent can read them — but the project should not propose modifications to them. This prevents accidental changes to specs owned by another team.\n\nYou can override `readOnly` to `owned` when your coordinator repo is the actual owner of those specs, as in a central spec management setup.\n\n---\n\n## Context and workspaces\n\nContext compilation — assembling the spec content the agent sees at each lifecycle step — is workspace-aware.\n\n### Workspace activation\n\nA workspace is considered active in the context of a change when at least one of the change's specs belongs to that workspace. A change touching `core:compile-context` activates the `core` workspace. A change touching both `default:_global/architecture` and `core:schema-format` activates both `default` (where `_global/*` specs live) and `core` simultaneously.\n\n### Project-level vs workspace-level patterns\n\n`contextIncludeSpecs` and `contextExcludeSpecs` can be declared at two levels with different behaviour:\n\n**Project-level** patterns (at the top of `specd.yaml`) apply to every compiled context, regardless of which change is active. Use these for specs that must always be present: global constraints, cross-cutting architecture specs.\n\n**Workspace-level** patterns (inside a workspace entry) apply only when that workspace is active. Use these for specs that are only relevant when work is happening within that workspace.\n\n```yaml\n# Always include global constraints — regardless of what the change touches\ncontextIncludeSpecs:\n  - 'default:*'\n\nworkspaces:\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    # When the core workspace is active, include all core specs\n    contextIncludeSpecs:\n      - '*'\n    # But exclude draft specs even when active\n    contextExcludeSpecs:\n      - 'drafts/*'\n```\n\n### Pattern syntax\n\n| Pattern              | At project level                                 | At workspace level                               |\n| -------------------- | ------------------------------------------------ | ------------------------------------------------ |\n| `*`                  | All specs in all workspaces                      | All specs in this workspace                      |\n| `workspace:*`        | All specs in the named workspace                 | All specs in the named workspace                 |\n| `prefix/*`           | All specs under `prefix/` in `default`           | All specs under `prefix/` in this workspace      |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace | All specs under `prefix/` in the named workspace |\n| `path/name`          | Exact spec in `default`                          | Exact spec in this workspace                     |\n\n`*` may only appear in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\nAt workspace level, an unqualified pattern like `auth/*` means \"specs under `auth/` in this workspace\" — not the `default` workspace. This is a key difference from project-level patterns, where unqualified paths resolve to `default`.\n\nOne important note: specs added to context via `dependsOn` traversal are never subject to exclude patterns. A declared dependency is always included regardless of what the exclude patterns say.\n\n---\n\n## The monorepo pattern\n\n**The problem:** You have a monorepo with `packages/core`, `packages/cli`, and `packages/mcp`. Each package has its own domain, its own specs, and its own code. When an agent works on a change to `packages/core`, it should see core's specs in full detail and the other packages' specs only as background context. You also want changes that span packages — like updating an architecture spec that affects both core and cli — to be tracked as a single unit of work.\n\n**The solution:** One `specd.yaml` at the monorepo root, multiple workspaces pointing to package subdirectories:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    ownership: owned\n\n  core:\n    prefix: core\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n\n  cli:\n    prefix: cli\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n```\n\nThis is specd's own configuration. Three workspaces, all `owned`, each pointing to a subdirectory of `specs/` and a matching package directory. The `default` workspace uses `prefix: _global`, so those specs gain `_global/` as the leading path segment inside their spec IDs:\n\n- `default:_global/architecture` — an architecture spec\n- `core:schema-format` — a schema format spec in the core package\n- `cli:config` — a config spec for the CLI package\n\nChanges that span packages simply list specs from multiple workspaces:\n\n```yaml\n# A change that touches both global and core specs\nspecIds:\n  - default:_global/architecture\n  - core:compile-context\n```\n\nBoth the `default` and `core` workspaces are active for that change. Context compilation applies project-level patterns always, plus workspace-level patterns from both active workspaces.\n\n### Each package has its own code root\n\n`codeRoot` tells specd where the implementation code for a workspace lives. When an agent compiles context for a change in the `core` workspace, the compiled output tells it to write code under `packages/core`. This ensures the agent knows exactly where each workspace's implementation sits, without having to guess from the workspace name.\n\n`codeRoot` is required for non-`default` workspaces — there is no sensible default for external or package workspaces.\n\n---\n\n## The multi-repo coordinator pattern\n\n**The problem:** You have five microservices in separate repos. The architecture team needs to define cross-service API contracts, track changes to any service's specs, and ensure consistency across the system. Each service team owns their code, but the specs that define the contracts between services need central governance. Some services' specs should be read-only from the coordinator's perspective (the platform team manages those), while others can be freely modified.\n\n**The solution:** A coordinator repository declares each service's spec directory as a workspace. The services do not need their own `specd.yaml` — the coordinator manages everything centrally.\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    codeRoot: ./\n\n  auth:\n    specs:\n      adapter: fs\n      fs:\n        path: ../auth-service/specd/specs\n    codeRoot: ../auth-service\n    ownership: owned\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: ../payments-service/specd/specs\n    codeRoot: ../payments-service\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\nThe coordinator owns `auth` and `payments` specs — it can freely create and modify them. The `platform` workspace is `readOnly` — its specs are available to the agent as context but the coordinator does not modify them.\n\nSeveral things are worth noting about this pattern:\n\n**Paths resolve relative to `specd.yaml`.** `../auth-service/specd/specs` is resolved from the directory containing `specd.yaml`. This means the repos must be cloned in a predictable relative layout. This is typical in monorepo-adjacent setups where service repos are siblings.\n\n**External workspaces are inferred, not declared.** When a workspace's specs path resolves outside the project's repository root, specd marks it as external automatically. You do not declare `isExternal` — it is computed from the path. External workspaces receive `readOnly` as their ownership default.\n\n**Each project's `specd.yaml` is its own source of truth.** specd never reads the service repositories' own `specd.yaml` (if they have one). All properties — paths, schemas, ownership — must be declared in the coordinator's `specd.yaml`. Two projects can declare overlapping workspace paths without conflict; each project has its own independent view.\n\n**Service repos do not need a `specd.yaml`.** They just need their spec directories to be present at the expected path. The coordinator manages the change lifecycle for all of them.\n\n---\n\n## Schema resolution from workspaces\n\nSchemas can be stored locally inside a workspace's `schemas` directory. The resolution prefix determines which workspace specd looks in:\n\n| Reference                       | Where specd looks                       |\n| ------------------------------- | --------------------------------------- |\n| `my-workflow` or `#my-workflow` | `default` workspace's schemas directory |\n| `#billing:my-schema`            | `billing` workspace's schemas directory |\n| `@specd/schema-std`             | npm package in `node_modules`           |\n\nThe `default` workspace has a schemas directory by default (`.specd/schemas`). Non-`default` workspaces have no schemas directory unless you declare one explicitly:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: ../billing/specd/specs\n    schemas:\n      adapter: fs\n      fs:\n        path: ../billing/dev/schemas\n    codeRoot: ../billing\n```\n\nWithout the `schemas` section, any schema reference targeting the `billing` workspace — such as `#billing:my-schema` — produces a `SchemaNotFoundError`.\n\n---\n\n## Archive patterns\n\nWhen changes are archived, their location in the archive directory is controlled by the `pattern` field. Archive patterns support `{{change.name}}`, `{{change.archivedName}}`, and date tokens — not a singular workspace token.\n\nExample — organise by change slug:\n\n```yaml\nstorage:\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{change.name}}/{{change.archivedName}}'\n```\n\nAvailable template variables in archive patterns:\n\n| Variable                  | Value                                                              |\n| ------------------------- | ------------------------------------------------------------------ |\n| `{{change.name}}`         | The change's slug name                                             |\n| `{{change.archivedName}}` | Date-prefixed slug (e.g. `2024-01-15-add-auth-flow`) — the default |\n| `{{year}}`                | Four-digit year at archive time                                    |\n| `{{date}}`                | ISO date at archive time                                           |\n\n`{{change.workspace}}` is not supported. A change may touch multiple workspaces (derived from its `specIds`); there is no primary workspace for path templating.\n\n---\n\n## Where to go next\n\n| Topic                                                      | Document                                                                                            |\n| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |\n| Full field-by-field reference for all workspace options    | [Configuration Guide](configuration.md)                                                             |\n| Getting started and project structure overview             | [Getting Started](getting-started.md)                                                               |\n| How changes move through the lifecycle and span workspaces | [Workflow Reference](workflow.md)                                                                   |\n| All `specd.yaml` configuration options explained           | [Configuring Your Project](configuration.md)                                                        |\n| Annotated multi-workspace coordinator example              | [Configuration Examples](configuration-examples.md#example-3-multi-workspace-monorepo--coordinator) |\n",
    "lineCount": 374,
    "byteLength": 20114,
    "outline": [
      {
        "index": 1,
        "heading": "Workspaces",
        "level": 1,
        "startLine": 1,
        "endLine": 374,
        "lines": 374,
        "content": "# Workspaces\n\nA workspace is the fundamental unit of organization in specd. It groups a set of specs, a code location, and an ownership relationship under a single named entry in `specd.yaml`. Everything in specd that involves specs — context compilation, archiving, change tracking, schema resolution — resolves through workspaces.\n\nThis guide explains what workspaces are, why they exist, when you need more than one, and how they interact with spec IDs, context, and multi-repo setups.\n\n---\n\n## Why workspaces exist\n\nIn a simple project, all specs live in one directory and all code lives in one place. A single `default` workspace handles this perfectly.\n\nBut real projects outgrow that quickly:\n\n**Monorepos with multiple packages.** A monorepo containing `packages/core`, `packages/cli`, and `packages/mcp` needs separate spec directories for each package. Each has its own code root, its own domain, and potentially its own team. Without workspaces, all specs would live in a single flat directory — making it impossible to scope context to the package being worked on.\n\n**Multi-repo microservice architectures.** A payments service needs to read the platform team's API specs for context, but should never modify them. An auth service defines its own specs but needs visibility into the shared contracts. Without workspaces, each repo would be isolated — no cross-repo spec visibility, no coordinated changes.\n\n**Central spec governance.** An architecture team maintains a coordinator repo that governs specs across ten service repos. They need to propose changes to any service's specs, track those changes through the lifecycle, and archive them — all from a single project. Without workspaces, they would need ten separate specd projects with no unified view.\n\n**Mixed ownership.** Some specs are owned by your team. Others are read-only references maintained by another team. You need both in context — but only your specs should be modifiable. Without workspaces, there is no way to express \"include these specs for context but prevent changes to them.\"\n\nWorkspaces solve all of these by letting a single `specd.yaml` declare multiple spec locations, each with its own path, code root, ownership, and context rules.\n\n---\n\n## What is a workspace?\n\nWhen specd needs to work with a spec, it needs to know:\n\n- Where the spec files live on disk\n- Where the implementation code for those specs lives\n- Whether this project is allowed to modify those specs\n- What to call those specs in fully-qualified IDs\n\nA workspace provides all of that. It is not a directory; it is a declaration in `specd.yaml` that names and describes a location for specs and connects it to the rest of the project.\n\nEvery project has at least one workspace: `default`. Most single-repo projects only need that one.\n\n---\n\n## The default workspace\n\n`default` is a reserved name that identifies the local project workspace — the specs this repository owns. Every `specd.yaml` must declare it.\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThe `default` workspace has sensible defaults that make minimal configuration sufficient:\n\n- `codeRoot` — defaults to the project root (the directory containing `specd.yaml`)\n- `ownership` — defaults to `owned`, meaning this project freely creates and modifies specs here\n\nFor most single-repo projects, this is all you ever need. Additional workspaces are for when your project spans multiple spec locations.\n\n---\n\n## Spec IDs and the prefix field\n\nEvery spec in specd has a fully-qualified spec ID in the format `workspace:capability-path`. The workspace name comes first, separated by a colon from the capability path — which mirrors the directory structure inside the workspace's specs directory.\n\nFor example, a spec stored at `specs/auth/login/` in the `default` workspace has the ID `default:auth/login`. A spec in a `payments` workspace at `specs/checkout/` has the ID `payments:checkout`.\n\nBare paths (without a colon) are shorthand for `default:path`. So `auth/login` and `default:auth/login` are equivalent. Internally, specd always uses the fully-qualified form.\n\n### The prefix field\n\nBy default, the capability path starts directly at the first directory inside the workspace's specs root. The optional `prefix` field prepends an additional leading path segment to that capability path, without changing the workspace name:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture` — not `_global:architecture`.\n\nThe workspace is still named `default`, and the workspace name still appears before the colon in spec IDs. The prefix only affects the capability-path portion after the colon.\n\nConcrete example:\n\n- workspace name: `default`\n- `specsPath`: `specs/_global`\n- spec directory on disk: `specs/_global/architecture`\n- relative path inside that workspace root: `architecture`\n\nWithout `prefix`, the spec ID would be `default:architecture`, because `_global` is part of the workspace root and is no longer present in the relative path. `prefix: _global` exists to add that lost leading segment back into the capability-path, producing `default:_global/architecture`.\n\n**When to use a prefix:** when the workspace name and the logical label for your specs diverge — for example, when the `default` workspace holds global constraints that live under a `_global/` directory and should be addressed as such for clarity.\n\nThis is exactly how specd's own project uses it: the `default` workspace has `prefix: _global`, so architecture and conventions specs are referenced as `default:_global/architecture` and `default:_global/conventions`.\n\n---\n\n## Workspace fields\n\nThe full set of fields available on any workspace:\n\n| Field                 | Required           | Default (`default` ws)  | Default (non-`default` ws)            | Description                                            |\n| --------------------- | ------------------ | ----------------------- | ------------------------------------- | ------------------------------------------------------ |\n| `specs`               | always             | —                       | —                                     | Storage adapter and path where spec files live         |\n| `codeRoot`            | non-`default` only | project root            | (must be declared)                    | Directory where implementation code lives              |\n| `schemas`             | no                 | `.specd/schemas`        | (none)                                | Storage adapter and path for named local schemas       |\n| `ownership`           | no                 | `owned`                 | `readOnly`                            | The project's relationship to these specs              |\n| `prefix`              | no                 | (none)                  | (none)                                | Override the qualifier used in spec IDs                |\n| `contextIncludeSpecs` | no                 | (project-level default) | `['*']` (all specs in this workspace) | Include patterns applied when this workspace is active |\n| `contextExcludeSpecs` | no                 | `[]`                    | `[]`                                  | Exclude patterns applied when this workspace is active |\n\nAll relative paths resolve from the directory containing `specd.yaml`.\n\n---\n\n## Ownership: owned, shared, readOnly\n\nThe `ownership` field describes the project's relationship to a workspace's specs. It has three values:\n\n**`owned`** — this project freely creates and modifies specs in this workspace. The default for `default`.\n\n**`shared`** — this project co-owns these specs alongside other teams. Changes may require coordination before being applied. Use this for workspaces where multiple repos contribute specs but none has unilateral authority.\n\n**`readOnly`** — this project reads these specs for context but does not modify them. The default for non-`default` workspaces.\n\n`readOnly` is the most common setting for additional workspaces. It tells specd that you can see the specs in context — and the agent can read them — but the project should not propose modifications to them. This prevents accidental changes to specs owned by another team.\n\nYou can override `readOnly` to `owned` when your coordinator repo is the actual owner of those specs, as in a central spec management setup.\n\n---\n\n## Context and workspaces\n\nContext compilation — assembling the spec content the agent sees at each lifecycle step — is workspace-aware.\n\n### Workspace activation\n\nA workspace is considered active in the context of a change when at least one of the change's specs belongs to that workspace. A change touching `core:compile-context` activates the `core` workspace. A change touching both `default:_global/architecture` and `core:schema-format` activates both `default` (where `_global/*` specs live) and `core` simultaneously.\n\n### Project-level vs workspace-level patterns\n\n`contextIncludeSpecs` and `contextExcludeSpecs` can be declared at two levels with different behaviour:\n\n**Project-level** patterns (at the top of `specd.yaml`) apply to every compiled context, regardless of which change is active. Use these for specs that must always be present: global constraints, cross-cutting architecture specs.\n\n**Workspace-level** patterns (inside a workspace entry) apply only when that workspace is active. Use these for specs that are only relevant when work is happening within that workspace.\n\n```yaml\n# Always include global constraints — regardless of what the change touches\ncontextIncludeSpecs:\n  - 'default:*'\n\nworkspaces:\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    # When the core workspace is active, include all core specs\n    contextIncludeSpecs:\n      - '*'\n    # But exclude draft specs even when active\n    contextExcludeSpecs:\n      - 'drafts/*'\n```\n\n### Pattern syntax\n\n| Pattern              | At project level                                 | At workspace level                               |\n| -------------------- | ------------------------------------------------ | ------------------------------------------------ |\n| `*`                  | All specs in all workspaces                      | All specs in this workspace                      |\n| `workspace:*`        | All specs in the named workspace                 | All specs in the named workspace                 |\n| `prefix/*`           | All specs under `prefix/` in `default`           | All specs under `prefix/` in this workspace      |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace | All specs under `prefix/` in the named workspace |\n| `path/name`          | Exact spec in `default`                          | Exact spec in this workspace                     |\n\n`*` may only appear in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\nAt workspace level, an unqualified pattern like `auth/*` means \"specs under `auth/` in this workspace\" — not the `default` workspace. This is a key difference from project-level patterns, where unqualified paths resolve to `default`.\n\nOne important note: specs added to context via `dependsOn` traversal are never subject to exclude patterns. A declared dependency is always included regardless of what the exclude patterns say.\n\n---\n\n## The monorepo pattern\n\n**The problem:** You have a monorepo with `packages/core`, `packages/cli`, and `packages/mcp`. Each package has its own domain, its own specs, and its own code. When an agent works on a change to `packages/core`, it should see core's specs in full detail and the other packages' specs only as background context. You also want changes that span packages — like updating an architecture spec that affects both core and cli — to be tracked as a single unit of work.\n\n**The solution:** One `specd.yaml` at the monorepo root, multiple workspaces pointing to package subdirectories:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    ownership: owned\n\n  core:\n    prefix: core\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n\n  cli:\n    prefix: cli\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n```\n\nThis is specd's own configuration. Three workspaces, all `owned`, each pointing to a subdirectory of `specs/` and a matching package directory. The `default` workspace uses `prefix: _global`, so those specs gain `_global/` as the leading path segment inside their spec IDs:\n\n- `default:_global/architecture` — an architecture spec\n- `core:schema-format` — a schema format spec in the core package\n- `cli:config` — a config spec for the CLI package\n\nChanges that span packages simply list specs from multiple workspaces:\n\n```yaml\n# A change that touches both global and core specs\nspecIds:\n  - default:_global/architecture\n  - core:compile-context\n```\n\nBoth the `default` and `core` workspaces are active for that change. Context compilation applies project-level patterns always, plus workspace-level patterns from both active workspaces.\n\n### Each package has its own code root\n\n`codeRoot` tells specd where the implementation code for a workspace lives. When an agent compiles context for a change in the `core` workspace, the compiled output tells it to write code under `packages/core`. This ensures the agent knows exactly where each workspace's implementation sits, without having to guess from the workspace name.\n\n`codeRoot` is required for non-`default` workspaces — there is no sensible default for external or package workspaces.\n\n---\n\n## The multi-repo coordinator pattern\n\n**The problem:** You have five microservices in separate repos. The architecture team needs to define cross-service API contracts, track changes to any service's specs, and ensure consistency across the system. Each service team owns their code, but the specs that define the contracts between services need central governance. Some services' specs should be read-only from the coordinator's perspective (the platform team manages those), while others can be freely modified.\n\n**The solution:** A coordinator repository declares each service's spec directory as a workspace. The services do not need their own `specd.yaml` — the coordinator manages everything centrally.\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    codeRoot: ./\n\n  auth:\n    specs:\n      adapter: fs\n      fs:\n        path: ../auth-service/specd/specs\n    codeRoot: ../auth-service\n    ownership: owned\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: ../payments-service/specd/specs\n    codeRoot: ../payments-service\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\nThe coordinator owns `auth` and `payments` specs — it can freely create and modify them. The `platform` workspace is `readOnly` — its specs are available to the agent as context but the coordinator does not modify them.\n\nSeveral things are worth noting about this pattern:\n\n**Paths resolve relative to `specd.yaml`.** `../auth-service/specd/specs` is resolved from the directory containing `specd.yaml`. This means the repos must be cloned in a predictable relative layout. This is typical in monorepo-adjacent setups where service repos are siblings.\n\n**External workspaces are inferred, not declared.** When a workspace's specs path resolves outside the project's repository root, specd marks it as external automatically. You do not declare `isExternal` — it is computed from the path. External workspaces receive `readOnly` as their ownership default.\n\n**Each project's `specd.yaml` is its own source of truth.** specd never reads the service repositories' own `specd.yaml` (if they have one). All properties — paths, schemas, ownership — must be declared in the coordinator's `specd.yaml`. Two projects can declare overlapping workspace paths without conflict; each project has its own independent view.\n\n**Service repos do not need a `specd.yaml`.** They just need their spec directories to be present at the expected path. The coordinator manages the change lifecycle for all of them.\n\n---\n\n## Schema resolution from workspaces\n\nSchemas can be stored locally inside a workspace's `schemas` directory. The resolution prefix determines which workspace specd looks in:\n\n| Reference                       | Where specd looks                       |\n| ------------------------------- | --------------------------------------- |\n| `my-workflow` or `#my-workflow` | `default` workspace's schemas directory |\n| `#billing:my-schema`            | `billing` workspace's schemas directory |\n| `@specd/schema-std`             | npm package in `node_modules`           |\n\nThe `default` workspace has a schemas directory by default (`.specd/schemas`). Non-`default` workspaces have no schemas directory unless you declare one explicitly:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: ../billing/specd/specs\n    schemas:\n      adapter: fs\n      fs:\n        path: ../billing/dev/schemas\n    codeRoot: ../billing\n```\n\nWithout the `schemas` section, any schema reference targeting the `billing` workspace — such as `#billing:my-schema` — produces a `SchemaNotFoundError`.\n\n---\n\n## Archive patterns\n\nWhen changes are archived, their location in the archive directory is controlled by the `pattern` field. Archive patterns support `{{change.name}}`, `{{change.archivedName}}`, and date tokens — not a singular workspace token.\n\nExample — organise by change slug:\n\n```yaml\nstorage:\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{change.name}}/{{change.archivedName}}'\n```\n\nAvailable template variables in archive patterns:\n\n| Variable                  | Value                                                              |\n| ------------------------- | ------------------------------------------------------------------ |\n| `{{change.name}}`         | The change's slug name                                             |\n| `{{change.archivedName}}` | Date-prefixed slug (e.g. `2024-01-15-add-auth-flow`) — the default |\n| `{{year}}`                | Four-digit year at archive time                                    |\n| `{{date}}`                | ISO date at archive time                                           |\n\n`{{change.workspace}}` is not supported. A change may touch multiple workspaces (derived from its `specIds`); there is no primary workspace for path templating.\n\n---\n\n## Where to go next\n\n| Topic                                                      | Document                                                                                            |\n| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |\n| Full field-by-field reference for all workspace options    | [Configuration Guide](configuration.md)                                                             |\n| Getting started and project structure overview             | [Getting Started](getting-started.md)                                                               |\n| How changes move through the lifecycle and span workspaces | [Workflow Reference](workflow.md)                                                                   |\n| All `specd.yaml` configuration options explained           | [Configuring Your Project](configuration.md)                                                        |\n| Annotated multi-workspace coordinator example              | [Configuration Examples](configuration-examples.md#example-3-multi-workspace-monorepo--coordinator) |\n"
      },
      {
        "index": 2,
        "heading": "Why workspaces exist",
        "level": 2,
        "startLine": 9,
        "endLine": 26,
        "lines": 18,
        "content": "## Why workspaces exist\n\nIn a simple project, all specs live in one directory and all code lives in one place. A single `default` workspace handles this perfectly.\n\nBut real projects outgrow that quickly:\n\n**Monorepos with multiple packages.** A monorepo containing `packages/core`, `packages/cli`, and `packages/mcp` needs separate spec directories for each package. Each has its own code root, its own domain, and potentially its own team. Without workspaces, all specs would live in a single flat directory — making it impossible to scope context to the package being worked on.\n\n**Multi-repo microservice architectures.** A payments service needs to read the platform team's API specs for context, but should never modify them. An auth service defines its own specs but needs visibility into the shared contracts. Without workspaces, each repo would be isolated — no cross-repo spec visibility, no coordinated changes.\n\n**Central spec governance.** An architecture team maintains a coordinator repo that governs specs across ten service repos. They need to propose changes to any service's specs, track those changes through the lifecycle, and archive them — all from a single project. Without workspaces, they would need ten separate specd projects with no unified view.\n\n**Mixed ownership.** Some specs are owned by your team. Others are read-only references maintained by another team. You need both in context — but only your specs should be modifiable. Without workspaces, there is no way to express \"include these specs for context but prevent changes to them.\"\n\nWorkspaces solve all of these by letting a single `specd.yaml` declare multiple spec locations, each with its own path, code root, ownership, and context rules.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "What is a workspace?",
        "level": 2,
        "startLine": 27,
        "endLine": 41,
        "lines": 15,
        "content": "## What is a workspace?\n\nWhen specd needs to work with a spec, it needs to know:\n\n- Where the spec files live on disk\n- Where the implementation code for those specs lives\n- Whether this project is allowed to modify those specs\n- What to call those specs in fully-qualified IDs\n\nA workspace provides all of that. It is not a directory; it is a declaration in `specd.yaml` that names and describes a location for specs and connects it to the rest of the project.\n\nEvery project has at least one workspace: `default`. Most single-repo projects only need that one.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "The default workspace",
        "level": 2,
        "startLine": 42,
        "endLine": 63,
        "lines": 22,
        "content": "## The default workspace\n\n`default` is a reserved name that identifies the local project workspace — the specs this repository owns. Every `specd.yaml` must declare it.\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n```\n\nThe `default` workspace has sensible defaults that make minimal configuration sufficient:\n\n- `codeRoot` — defaults to the project root (the directory containing `specd.yaml`)\n- `ownership` — defaults to `owned`, meaning this project freely creates and modifies specs here\n\nFor most single-repo projects, this is all you ever need. Additional workspaces are for when your project spans multiple spec locations.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Spec IDs and the prefix field",
        "level": 2,
        "startLine": 64,
        "endLine": 104,
        "lines": 41,
        "content": "## Spec IDs and the prefix field\n\nEvery spec in specd has a fully-qualified spec ID in the format `workspace:capability-path`. The workspace name comes first, separated by a colon from the capability path — which mirrors the directory structure inside the workspace's specs directory.\n\nFor example, a spec stored at `specs/auth/login/` in the `default` workspace has the ID `default:auth/login`. A spec in a `payments` workspace at `specs/checkout/` has the ID `payments:checkout`.\n\nBare paths (without a colon) are shorthand for `default:path`. So `auth/login` and `default:auth/login` are equivalent. Internally, specd always uses the fully-qualified form.\n\n### The prefix field\n\nBy default, the capability path starts directly at the first directory inside the workspace's specs root. The optional `prefix` field prepends an additional leading path segment to that capability path, without changing the workspace name:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture` — not `_global:architecture`.\n\nThe workspace is still named `default`, and the workspace name still appears before the colon in spec IDs. The prefix only affects the capability-path portion after the colon.\n\nConcrete example:\n\n- workspace name: `default`\n- `specsPath`: `specs/_global`\n- spec directory on disk: `specs/_global/architecture`\n- relative path inside that workspace root: `architecture`\n\nWithout `prefix`, the spec ID would be `default:architecture`, because `_global` is part of the workspace root and is no longer present in the relative path. `prefix: _global` exists to add that lost leading segment back into the capability-path, producing `default:_global/architecture`.\n\n**When to use a prefix:** when the workspace name and the logical label for your specs diverge — for example, when the `default` workspace holds global constraints that live under a `_global/` directory and should be addressed as such for clarity.\n\nThis is exactly how specd's own project uses it: the `default` workspace has `prefix: _global`, so architecture and conventions specs are referenced as `default:_global/architecture` and `default:_global/conventions`.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "The prefix field",
        "level": 3,
        "startLine": 72,
        "endLine": 104,
        "lines": 33,
        "content": "### The prefix field\n\nBy default, the capability path starts directly at the first directory inside the workspace's specs root. The optional `prefix` field prepends an additional leading path segment to that capability path, without changing the workspace name:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n```\n\nWith this configuration, specs under `specs/_global/` are addressed as `default:_global/architecture` — not `_global:architecture`.\n\nThe workspace is still named `default`, and the workspace name still appears before the colon in spec IDs. The prefix only affects the capability-path portion after the colon.\n\nConcrete example:\n\n- workspace name: `default`\n- `specsPath`: `specs/_global`\n- spec directory on disk: `specs/_global/architecture`\n- relative path inside that workspace root: `architecture`\n\nWithout `prefix`, the spec ID would be `default:architecture`, because `_global` is part of the workspace root and is no longer present in the relative path. `prefix: _global` exists to add that lost leading segment back into the capability-path, producing `default:_global/architecture`.\n\n**When to use a prefix:** when the workspace name and the logical label for your specs diverge — for example, when the `default` workspace holds global constraints that live under a `_global/` directory and should be addressed as such for clarity.\n\nThis is exactly how specd's own project uses it: the `default` workspace has `prefix: _global`, so architecture and conventions specs are referenced as `default:_global/architecture` and `default:_global/conventions`.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "Workspace fields",
        "level": 2,
        "startLine": 105,
        "endLine": 122,
        "lines": 18,
        "content": "## Workspace fields\n\nThe full set of fields available on any workspace:\n\n| Field                 | Required           | Default (`default` ws)  | Default (non-`default` ws)            | Description                                            |\n| --------------------- | ------------------ | ----------------------- | ------------------------------------- | ------------------------------------------------------ |\n| `specs`               | always             | —                       | —                                     | Storage adapter and path where spec files live         |\n| `codeRoot`            | non-`default` only | project root            | (must be declared)                    | Directory where implementation code lives              |\n| `schemas`             | no                 | `.specd/schemas`        | (none)                                | Storage adapter and path for named local schemas       |\n| `ownership`           | no                 | `owned`                 | `readOnly`                            | The project's relationship to these specs              |\n| `prefix`              | no                 | (none)                  | (none)                                | Override the qualifier used in spec IDs                |\n| `contextIncludeSpecs` | no                 | (project-level default) | `['*']` (all specs in this workspace) | Include patterns applied when this workspace is active |\n| `contextExcludeSpecs` | no                 | `[]`                    | `[]`                                  | Exclude patterns applied when this workspace is active |\n\nAll relative paths resolve from the directory containing `specd.yaml`.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Ownership: owned, shared, readOnly",
        "level": 2,
        "startLine": 123,
        "endLine": 138,
        "lines": 16,
        "content": "## Ownership: owned, shared, readOnly\n\nThe `ownership` field describes the project's relationship to a workspace's specs. It has three values:\n\n**`owned`** — this project freely creates and modifies specs in this workspace. The default for `default`.\n\n**`shared`** — this project co-owns these specs alongside other teams. Changes may require coordination before being applied. Use this for workspaces where multiple repos contribute specs but none has unilateral authority.\n\n**`readOnly`** — this project reads these specs for context but does not modify them. The default for non-`default` workspaces.\n\n`readOnly` is the most common setting for additional workspaces. It tells specd that you can see the specs in context — and the agent can read them — but the project should not propose modifications to them. This prevents accidental changes to specs owned by another team.\n\nYou can override `readOnly` to `owned` when your coordinator repo is the actual owner of those specs, as in a central spec management setup.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "Context and workspaces",
        "level": 2,
        "startLine": 139,
        "endLine": 192,
        "lines": 54,
        "content": "## Context and workspaces\n\nContext compilation — assembling the spec content the agent sees at each lifecycle step — is workspace-aware.\n\n### Workspace activation\n\nA workspace is considered active in the context of a change when at least one of the change's specs belongs to that workspace. A change touching `core:compile-context` activates the `core` workspace. A change touching both `default:_global/architecture` and `core:schema-format` activates both `default` (where `_global/*` specs live) and `core` simultaneously.\n\n### Project-level vs workspace-level patterns\n\n`contextIncludeSpecs` and `contextExcludeSpecs` can be declared at two levels with different behaviour:\n\n**Project-level** patterns (at the top of `specd.yaml`) apply to every compiled context, regardless of which change is active. Use these for specs that must always be present: global constraints, cross-cutting architecture specs.\n\n**Workspace-level** patterns (inside a workspace entry) apply only when that workspace is active. Use these for specs that are only relevant when work is happening within that workspace.\n\n```yaml\n# Always include global constraints — regardless of what the change touches\ncontextIncludeSpecs:\n  - 'default:*'\n\nworkspaces:\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    # When the core workspace is active, include all core specs\n    contextIncludeSpecs:\n      - '*'\n    # But exclude draft specs even when active\n    contextExcludeSpecs:\n      - 'drafts/*'\n```\n\n### Pattern syntax\n\n| Pattern              | At project level                                 | At workspace level                               |\n| -------------------- | ------------------------------------------------ | ------------------------------------------------ |\n| `*`                  | All specs in all workspaces                      | All specs in this workspace                      |\n| `workspace:*`        | All specs in the named workspace                 | All specs in the named workspace                 |\n| `prefix/*`           | All specs under `prefix/` in `default`           | All specs under `prefix/` in this workspace      |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace | All specs under `prefix/` in the named workspace |\n| `path/name`          | Exact spec in `default`                          | Exact spec in this workspace                     |\n\n`*` may only appear in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\nAt workspace level, an unqualified pattern like `auth/*` means \"specs under `auth/` in this workspace\" — not the `default` workspace. This is a key difference from project-level patterns, where unqualified paths resolve to `default`.\n\nOne important note: specs added to context via `dependsOn` traversal are never subject to exclude patterns. A declared dependency is always included regardless of what the exclude patterns say.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "Workspace activation",
        "level": 3,
        "startLine": 143,
        "endLine": 146,
        "lines": 4,
        "content": "### Workspace activation\n\nA workspace is considered active in the context of a change when at least one of the change's specs belongs to that workspace. A change touching `core:compile-context` activates the `core` workspace. A change touching both `default:_global/architecture` and `core:schema-format` activates both `default` (where `_global/*` specs live) and `core` simultaneously.\n"
      },
      {
        "index": 11,
        "heading": "Project-level vs workspace-level patterns",
        "level": 3,
        "startLine": 147,
        "endLine": 174,
        "lines": 28,
        "content": "### Project-level vs workspace-level patterns\n\n`contextIncludeSpecs` and `contextExcludeSpecs` can be declared at two levels with different behaviour:\n\n**Project-level** patterns (at the top of `specd.yaml`) apply to every compiled context, regardless of which change is active. Use these for specs that must always be present: global constraints, cross-cutting architecture specs.\n\n**Workspace-level** patterns (inside a workspace entry) apply only when that workspace is active. Use these for specs that are only relevant when work is happening within that workspace.\n\n```yaml\n# Always include global constraints — regardless of what the change touches\ncontextIncludeSpecs:\n  - 'default:*'\n\nworkspaces:\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    # When the core workspace is active, include all core specs\n    contextIncludeSpecs:\n      - '*'\n    # But exclude draft specs even when active\n    contextExcludeSpecs:\n      - 'drafts/*'\n```\n"
      },
      {
        "index": 12,
        "heading": "Pattern syntax",
        "level": 3,
        "startLine": 175,
        "endLine": 192,
        "lines": 18,
        "content": "### Pattern syntax\n\n| Pattern              | At project level                                 | At workspace level                               |\n| -------------------- | ------------------------------------------------ | ------------------------------------------------ |\n| `*`                  | All specs in all workspaces                      | All specs in this workspace                      |\n| `workspace:*`        | All specs in the named workspace                 | All specs in the named workspace                 |\n| `prefix/*`           | All specs under `prefix/` in `default`           | All specs under `prefix/` in this workspace      |\n| `workspace:prefix/*` | All specs under `prefix/` in the named workspace | All specs under `prefix/` in the named workspace |\n| `path/name`          | Exact spec in `default`                          | Exact spec in this workspace                     |\n\n`*` may only appear in three positions: alone, as `workspace:*`, or as a path suffix (`prefix/*`).\n\nAt workspace level, an unqualified pattern like `auth/*` means \"specs under `auth/` in this workspace\" — not the `default` workspace. This is a key difference from project-level patterns, where unqualified paths resolve to `default`.\n\nOne important note: specs added to context via `dependsOn` traversal are never subject to exclude patterns. A declared dependency is always included regardless of what the exclude patterns say.\n\n---\n"
      },
      {
        "index": 13,
        "heading": "The monorepo pattern",
        "level": 2,
        "startLine": 193,
        "endLine": 252,
        "lines": 60,
        "content": "## The monorepo pattern\n\n**The problem:** You have a monorepo with `packages/core`, `packages/cli`, and `packages/mcp`. Each package has its own domain, its own specs, and its own code. When an agent works on a change to `packages/core`, it should see core's specs in full detail and the other packages' specs only as background context. You also want changes that span packages — like updating an architecture spec that affects both core and cli — to be tracked as a single unit of work.\n\n**The solution:** One `specd.yaml` at the monorepo root, multiple workspaces pointing to package subdirectories:\n\n```yaml\nworkspaces:\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    ownership: owned\n\n  core:\n    prefix: core\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n\n  cli:\n    prefix: cli\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n```\n\nThis is specd's own configuration. Three workspaces, all `owned`, each pointing to a subdirectory of `specs/` and a matching package directory. The `default` workspace uses `prefix: _global`, so those specs gain `_global/` as the leading path segment inside their spec IDs:\n\n- `default:_global/architecture` — an architecture spec\n- `core:schema-format` — a schema format spec in the core package\n- `cli:config` — a config spec for the CLI package\n\nChanges that span packages simply list specs from multiple workspaces:\n\n```yaml\n# A change that touches both global and core specs\nspecIds:\n  - default:_global/architecture\n  - core:compile-context\n```\n\nBoth the `default` and `core` workspaces are active for that change. Context compilation applies project-level patterns always, plus workspace-level patterns from both active workspaces.\n\n### Each package has its own code root\n\n`codeRoot` tells specd where the implementation code for a workspace lives. When an agent compiles context for a change in the `core` workspace, the compiled output tells it to write code under `packages/core`. This ensures the agent knows exactly where each workspace's implementation sits, without having to guess from the workspace name.\n\n`codeRoot` is required for non-`default` workspaces — there is no sensible default for external or package workspaces.\n\n---\n"
      },
      {
        "index": 14,
        "heading": "Each package has its own code root",
        "level": 3,
        "startLine": 245,
        "endLine": 252,
        "lines": 8,
        "content": "### Each package has its own code root\n\n`codeRoot` tells specd where the implementation code for a workspace lives. When an agent compiles context for a change in the `core` workspace, the compiled output tells it to write code under `packages/core`. This ensures the agent knows exactly where each workspace's implementation sits, without having to guess from the workspace name.\n\n`codeRoot` is required for non-`default` workspaces — there is no sensible default for external or package workspaces.\n\n---\n"
      },
      {
        "index": 15,
        "heading": "The multi-repo coordinator pattern",
        "level": 2,
        "startLine": 253,
        "endLine": 306,
        "lines": 54,
        "content": "## The multi-repo coordinator pattern\n\n**The problem:** You have five microservices in separate repos. The architecture team needs to define cross-service API contracts, track changes to any service's specs, and ensure consistency across the system. Each service team owns their code, but the specs that define the contracts between services need central governance. Some services' specs should be read-only from the coordinator's perspective (the platform team manages those), while others can be freely modified.\n\n**The solution:** A coordinator repository declares each service's spec directory as a workspace. The services do not need their own `specd.yaml` — the coordinator manages everything centrally.\n\n```yaml\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    codeRoot: ./\n\n  auth:\n    specs:\n      adapter: fs\n      fs:\n        path: ../auth-service/specd/specs\n    codeRoot: ../auth-service\n    ownership: owned\n\n  payments:\n    specs:\n      adapter: fs\n      fs:\n        path: ../payments-service/specd/specs\n    codeRoot: ../payments-service\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-repo/specd/specs\n    codeRoot: ../platform-repo\n    ownership: readOnly\n```\n\nThe coordinator owns `auth` and `payments` specs — it can freely create and modify them. The `platform` workspace is `readOnly` — its specs are available to the agent as context but the coordinator does not modify them.\n\nSeveral things are worth noting about this pattern:\n\n**Paths resolve relative to `specd.yaml`.** `../auth-service/specd/specs` is resolved from the directory containing `specd.yaml`. This means the repos must be cloned in a predictable relative layout. This is typical in monorepo-adjacent setups where service repos are siblings.\n\n**External workspaces are inferred, not declared.** When a workspace's specs path resolves outside the project's repository root, specd marks it as external automatically. You do not declare `isExternal` — it is computed from the path. External workspaces receive `readOnly` as their ownership default.\n\n**Each project's `specd.yaml` is its own source of truth.** specd never reads the service repositories' own `specd.yaml` (if they have one). All properties — paths, schemas, ownership — must be declared in the coordinator's `specd.yaml`. Two projects can declare overlapping workspace paths without conflict; each project has its own independent view.\n\n**Service repos do not need a `specd.yaml`.** They just need their spec directories to be present at the expected path. The coordinator manages the change lifecycle for all of them.\n\n---\n"
      },
      {
        "index": 16,
        "heading": "Schema resolution from workspaces",
        "level": 2,
        "startLine": 307,
        "endLine": 336,
        "lines": 30,
        "content": "## Schema resolution from workspaces\n\nSchemas can be stored locally inside a workspace's `schemas` directory. The resolution prefix determines which workspace specd looks in:\n\n| Reference                       | Where specd looks                       |\n| ------------------------------- | --------------------------------------- |\n| `my-workflow` or `#my-workflow` | `default` workspace's schemas directory |\n| `#billing:my-schema`            | `billing` workspace's schemas directory |\n| `@specd/schema-std`             | npm package in `node_modules`           |\n\nThe `default` workspace has a schemas directory by default (`.specd/schemas`). Non-`default` workspaces have no schemas directory unless you declare one explicitly:\n\n```yaml\nworkspaces:\n  billing:\n    specs:\n      adapter: fs\n      fs:\n        path: ../billing/specd/specs\n    schemas:\n      adapter: fs\n      fs:\n        path: ../billing/dev/schemas\n    codeRoot: ../billing\n```\n\nWithout the `schemas` section, any schema reference targeting the `billing` workspace — such as `#billing:my-schema` — produces a `SchemaNotFoundError`.\n\n---\n"
      },
      {
        "index": 17,
        "heading": "Archive patterns",
        "level": 2,
        "startLine": 337,
        "endLine": 364,
        "lines": 28,
        "content": "## Archive patterns\n\nWhen changes are archived, their location in the archive directory is controlled by the `pattern` field. Archive patterns support `{{change.name}}`, `{{change.archivedName}}`, and date tokens — not a singular workspace token.\n\nExample — organise by change slug:\n\n```yaml\nstorage:\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{change.name}}/{{change.archivedName}}'\n```\n\nAvailable template variables in archive patterns:\n\n| Variable                  | Value                                                              |\n| ------------------------- | ------------------------------------------------------------------ |\n| `{{change.name}}`         | The change's slug name                                             |\n| `{{change.archivedName}}` | Date-prefixed slug (e.g. `2024-01-15-add-auth-flow`) — the default |\n| `{{year}}`                | Four-digit year at archive time                                    |\n| `{{date}}`                | ISO date at archive time                                           |\n\n`{{change.workspace}}` is not supported. A change may touch multiple workspaces (derived from its `specIds`); there is no primary workspace for path templating.\n\n---\n"
      },
      {
        "index": 18,
        "heading": "Where to go next",
        "level": 2,
        "startLine": 365,
        "endLine": 374,
        "lines": 10,
        "content": "## Where to go next\n\n| Topic                                                      | Document                                                                                            |\n| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |\n| Full field-by-field reference for all workspace options    | [Configuration Guide](configuration.md)                                                             |\n| Getting started and project structure overview             | [Getting Started](getting-started.md)                                                               |\n| How changes move through the lifecycle and span workspaces | [Workflow Reference](workflow.md)                                                                   |\n| All `specd.yaml` configuration options explained           | [Configuring Your Project](configuration.md)                                                        |\n| Annotated multi-workspace coordinator example              | [Configuration Examples](configuration-examples.md#example-3-multi-workspace-monorepo--coordinator) |\n"
      }
    ]
  },
  {
    "topic": "cli",
    "title": "CLI Guide & Command Interface",
    "description": "Comprehensive guide to the SpecD command-line interface, covering all 12 command families and practical workflows.",
    "order": 6,
    "content": "# CLI Guide & Command Interface\n\nThe SpecD command-line interface (`specd`) is the central engine of the platform. It manages the change lifecycle, compiles context for AI coding assistants, validates artifact syntax and relationships, indexes AST code graphs, manages plugins, and delivers on-demand documentation directly in the terminal.\n\nWhile AI coding assistants typically invoke SpecD through slash skills (`/specd`, `/specd-new`, `/specd-design`, etc.) or MCP tools, those integrations are thin orchestrators calling the CLI directly. You can run every command in your terminal, in shell scripts, or in CI/CD pipelines.\n\n---\n\n## Command Anatomy\n\n```bash\nspecd <command> [subcommand] [arguments...] [flags...]\n```\n\nWhen working within this repository without a global link:\n\n```bash\nnode packages/cli/dist/index.js <command> ...\n```\n\n### Global Options\n\nThese options apply across all command families:\n\n| Flag                | Description                                                           |\n| :------------------ | :-------------------------------------------------------------------- |\n| `--config <path>`   | Path to `specd.yaml` (overrides automatic config discovery).          |\n| `-v, --verbose`     | Increase logging verbosity (`-v` for info, `-vv` for debug/trace).    |\n| `--format <format>` | Output format: `text` (default), `json`, or `toon` (token-optimized). |\n\n### Output Formats\n\n- **`text`** _(default)_: Formatted for human readability with tables, status glyphs, and dynamic terminal-width fitting.\n- **`json`**: Structured JSON representation for scripting and programmatic ingestion.\n- **`toon`**: Ultra-compact token-optimized format specifically engineered to preserve LLM prompt context.\n\n---\n\n## The 12 Command Families\n\nSpecD organizes its capabilities into 12 dedicated command families:\n\n```mermaid\nflowchart LR\n    subgraph Governance & Lifecycle\n        P[project]\n        C[changes]\n        D[drafts]\n        DC[discarded]\n        A[archive]\n    end\n    subgraph Requirements & Specs\n        S[specs]\n        G[guide]\n    end\n    subgraph Architecture & Engine\n        SC[schema]\n        GR[graph]\n        CF[config]\n        ST[storage]\n        PL[plugins]\n    end\n```\n\n---\n\n### 1. `specd project` — Project Operations\n\nInspects project health, compiles context, and monitors change pipelines.\n\n- **`specd project init`** (or root shorthand **`specd init`**): Scaffolds a new project interactively, generating `specd.yaml`.\n- **`specd project status`**: High-level status showing active schema, workspaces, spec counts, active changes, and graph freshness.\n  ```bash\n  specd project status --context --graph --format toon\n  ```\n- **`specd project dashboard`**: Interactive terminal dashboard displaying project metrics, drift detection, and recent changes.\n- **`specd project context`**: Compiles global project context (architecture rules and global specs).\n  ```bash\n  specd project context --mode summary\n  ```\n- **`specd project context-specs`**: Discovers matching spec IDs for project and workspace include/exclude patterns without rendering full markdown.\n- **`specd project metadata`**: Inspects compiled project-level metadata and layout.\n- **`specd project update` / `update-metadata`**: Updates project settings or synchronizes metadata caches.\n\n👉 _See [Project Reference](../cli/project.md) for full options and examples._\n\n---\n\n### 2. `specd changes` — Change Lifecycle Management\n\nManages the core unit of work in SpecD from proposal to verification and archive.\n\n- **`specd changes create <name>`**: Initializes a new change directory under `.specd/changes/`.\n  ```bash\n  specd changes create add-oauth --specs default:auth/oauth\n  ```\n- **`specd changes list`**: Lists all active changes with their current lifecycle state.\n- **`specd changes status <name>`**: Displays the change state, completed artifacts DAG, blockers, and next action.\n  ```bash\n  specd changes status add-oauth\n  ```\n- **`specd changes transition <name> <state>`**: Advances change state (`designing`, `ready`, `implementing`, `verifying`, `done`, `archivable`).\n- **`specd changes validate <name>`**: Validates change artifacts structurally and checks cross-artifact relational integrity.\n- **`specd changes spec-preview <name> <specId>`**: Previews unified spec content after applying delta patches.\n  ```bash\n  specd changes spec-preview add-oauth default:auth/login --diff\n  ```\n- **`specd changes approve spec|signoff <name>`**: Grants human approvals at configured lifecycle gates.\n- **`specd changes context <name> <step>`**: Compiles the token-optimized instruction block and specs for the current step.\n- **`specd changes artifacts <name>`**: Lists artifact statuses (`complete`, `missing`, `drifted`, `skipped`).\n- **`specd changes skip-artifact <name> <artifactId>`**: Marks an optional artifact as skipped.\n- **`specd changes deps <name> [specId]`**: Inspects or modifies declared spec dependencies for the change.\n- **`specd changes draft <name>`**: Shelves an active change into `.specd/drafts/`.\n- **`specd changes discard <name>`**: Abandons an active change, moving it to `.specd/discarded/`.\n- **`specd changes archive <name>`**: Merges validated spec deltas into canonical specs and archives the change.\n- **`specd changes run-hooks <name> <step> [--phase pre|post]`**: Executes lifecycle validation hooks.\n- **`specd changes hook-instruction <name> <step>`**: Returns instructions defined for the current lifecycle hook.\n- **`specd changes artifact-instruction <name> [artifactId]`**: Retrieves the active instruction and template for the next artifact.\n- **`specd changes check-overlap [name]`**: Detects specifications targeted by multiple concurrent active changes.\n- **`specd changes invalidate <name>`**: Rolls back a change to `designing` when upstream specs have drifted.\n- **`specd changes implementation <subcommand> <name>`**: Tracks confirmed implementation files and symbols mapped to the change.\n\n👉 _See [Changes Reference](../cli/changes.md) for full options and examples._\n\n---\n\n### 3. `specd drafts` — Shelved Work\n\nBrowses and restores changes that were paused.\n\n- **`specd drafts list`**: Lists all shelved draft changes, showing date and reasons.\n- **`specd drafts show <name>`**: Inspects artifacts within a shelved change draft.\n- **`specd drafts restore <name>`**: Restores a shelved draft back to active changes in `.specd/changes/`.\n  ```bash\n  specd drafts restore add-oauth\n  ```\n\n👉 _See [Drafts Reference](../cli/drafts.md) for full options and examples._\n\n---\n\n### 4. `specd discarded` — Abandoned Work Audit\n\nMaintains an audit trail of changes that were abandoned.\n\n- **`specd discarded list`**: Lists all abandoned changes.\n- **`specd discarded show <name>`**: Inspects artifacts, proposal, and discard rationale of an abandoned change.\n\n👉 _See [Discarded Reference](../cli/discarded.md) for full options and examples._\n\n---\n\n### 5. `specd archive` — Completed History\n\nPermanent historical archive of merged changes.\n\n- **`specd archive list`**: Lists all completed, archived changes.\n- **`specd archive show <name>`**: Displays the archived change manifest, applied spec deltas, and completion audit record.\n  ```bash\n  specd archive show 20260920-core-architecture --diff\n  ```\n\n👉 _See [Archive Reference](../cli/archive.md) for full options and examples._\n\n---\n\n### 6. `specd specs` — Canonical Specifications\n\nDirect inspection, searching, and validation of specifications in workspaces.\n\n- **`specd specs list`**: Lists all specifications across workspaces.\n  ```bash\n  specd specs list --workspace core\n  ```\n- **`specd specs search <query>`**: Searches specifications by title, description, or requirements.\n- **`specd specs show <specId>`**: Displays full specification content (`spec.md` or `verify.md` with `--verify`).\n- **`specd specs outline <specId>`**: Displays hierarchical section headings and line boundaries.\n- **`specd specs context <specId>`**: Compiles the dependency context block for a single spec.\n- **`specd specs metadata <specId>`**: Inspects compiled requirements, dependencies, and verification scenarios.\n- **`specd specs resolve-path <specId>`**: Prints the exact filesystem path on disk.\n- **`specd specs validate [specId]`**: Validates Markdown AST and schema conformance.\n- **`specd specs init <specId>`**: Scaffolds a new spec directory with `spec.md` and `verify.md`.\n- **`specd specs deps <specId>`**: Adds, removes, or lists dependencies between specifications.\n- **`specd specs implementation <specId>`**: Maps source files and symbols to specification capabilities.\n- **`specd specs optimizations`**: Diagnostics for spec authoring and token usage optimization.\n\n👉 _See [Specs Reference](../cli/specs.md) for full options and examples._\n\n---\n\n### 7. `specd guide` — On-Demand Documentation\n\nAccess and search documentation topics directly within your terminal without opening a browser.\n\n- **`specd guide`**: Catalogs all available guides with titles and descriptions.\n- **`specd guide <topic>`**: Displays the full guide content.\n  ```bash\n  specd guide workflow\n  ```\n- **`specd guide <topic> --meta`**: Inspects document outline, heading depths, and line spans.\n- **`specd guide <topic> --section <name|index>`**: Slices a specific section by heading name or 1-indexed number.\n  ```bash\n  specd guide specs --section 2\n  ```\n- **`specd guide <topic> --start-line 20 --lines 30 --line-numbers`**: Windowed line slicing with 1-indexed line numbers.\n- **`specd guide search <query>`**: BM25 full-text search across all guide sections with contextual snippets.\n  ```bash\n  specd guide search \"approval gates\" --limit 3 --format toon\n  ```\n\n👉 _See [Guide Reference](../cli/guide.md) for full options and examples._\n\n---\n\n### 8. `specd graph` — Codebase Intelligence\n\nAST-based code graph indexing, dependency blast radius, and symbol resolution.\n\n- **`specd graph index`**: Builds or updates the AST dependency graph index.\n  ```bash\n  specd graph index --format toon\n  ```\n- **`specd graph search \"<query>\"`**: Fast fuzzy search across symbols (`--symbols`) or specifications (`--specs`).\n- **`specd graph impact`**: Evaluates blast-radius impact before modifying code:\n  ```bash\n  specd graph impact --symbol \"renderTable\" --direction dependents --format toon\n  specd graph impact --file \"packages/cli/src/helpers/table.ts\" --direction dependents\n  ```\n- **`specd graph stats`**: Displays total symbols, edges, file coverage, and index freshness.\n- **`specd graph hotspots`**: Identifies high-risk, heavily coupled files requiring extra care.\n\n👉 _See [Graph Reference](../cli/graph.md) for full options and examples._\n\n---\n\n### 9. `specd schema` — Workflow Customization\n\nIntrospects, extends, and validates artifact workflows.\n\n- **`specd schema show`**: Displays the active schema definition, declared artifacts, and lifecycle DAG.\n- **`specd schema fork <name>`**: Clones the default schema into a custom local schema for your project.\n- **`specd schema extend`**: Scaffolds a schema extension overlay with custom artifact types or rules.\n- **`specd schema validate`**: Validates a schema definition for circular dependencies and syntax rules.\n\n👉 _See [Schema Reference](../cli/schema.md) for full options and examples._\n\n---\n\n### 10. `specd config` — Configuration Inspection\n\nIntrospects the multi-layer configuration cascade.\n\n- **`specd config show`**: Displays effective configuration merged across defaults, user configs, and `specd.yaml`.\n  ```bash\n  specd config show --resolved --format json\n  ```\n\n👉 _See [Config Reference](../cli/config.md) for full options and examples._\n\n---\n\n### 11. `specd storage` — Cache & Index Storage\n\nManages underlying cache directories and internal indices.\n\n- **`specd storage reindex`**: Rebuilds internal metadata caches and spec indices from scratch.\n  ```bash\n  specd storage reindex --all\n  ```\n\n👉 _See [Storage Reference](../cli/storage.md) for full options and examples._\n\n---\n\n### 12. `specd plugins` — Extensibility & Agent Adapters\n\nInstalls and manages agent skill plugins and workflow integrations.\n\n- **`specd plugins list`**: Lists installed plugins and their active status.\n- **`specd plugins show <name>`**: Displays plugin metadata, registered hooks, and tools.\n- **`specd plugins install <package>`**: Installs and registers a plugin in `specd.yaml`.\n- **`specd plugins update [name]`**: Updates installed plugins to their latest versions.\n- **`specd plugins uninstall <name>`**: Removes a plugin and unregisters its hooks.\n\n👉 _See [Plugins Reference](../cli/plugins.md) for full options and examples._\n\n---\n\n## Practical Recipes\n\n### Recipe 1: Complete Change Lifecycle in 5 Steps\n\n```bash\n# 1. Create a change targeting a spec\nspecd changes create 20260924-user-profile --specs default:users/profile\n\n# 2. Transition to designing and author artifacts\nspecd changes transition 20260924-user-profile designing\n\n# 3. Validate artifacts and transition to implementing\nspecd changes validate 20260924-user-profile\nspecd changes transition 20260924-user-profile ready\nspecd changes transition 20260924-user-profile implementing\n\n# 4. Implement code, check status, verify against spec\nspecd changes status 20260924-user-profile\nspecd changes transition 20260924-user-profile verifying\nspecd changes transition 20260924-user-profile done\n\n# 5. Archive and merge deltas into canonical specs\nspecd changes transition 20260924-user-profile archivable\nspecd changes archive 20260924-user-profile\n```\n\n### Recipe 2: AI Agent Token Optimization\n\nWhen generating prompts or piping commands to AI agents, use `--format toon` to minimize tokens by up to 70%:\n\n```bash\nspecd project status --format toon\nspecd changes context 20260924-user-profile implementing --format toon\nspecd guide search \"approval gates\" --format toon\n```\n\n### Recipe 3: Blast-Radius Impact Analysis Before Refactoring\n\nBefore modifying a function or file, check who depends on it:\n\n```bash\nspecd graph impact --file packages/cli/src/helpers/table.ts --direction dependents --format text\n```\n",
    "lineCount": 332,
    "byteLength": 14099,
    "outline": [
      {
        "index": 1,
        "heading": "CLI Guide & Command Interface",
        "level": 1,
        "startLine": 1,
        "endLine": 332,
        "lines": 332,
        "content": "# CLI Guide & Command Interface\n\nThe SpecD command-line interface (`specd`) is the central engine of the platform. It manages the change lifecycle, compiles context for AI coding assistants, validates artifact syntax and relationships, indexes AST code graphs, manages plugins, and delivers on-demand documentation directly in the terminal.\n\nWhile AI coding assistants typically invoke SpecD through slash skills (`/specd`, `/specd-new`, `/specd-design`, etc.) or MCP tools, those integrations are thin orchestrators calling the CLI directly. You can run every command in your terminal, in shell scripts, or in CI/CD pipelines.\n\n---\n\n## Command Anatomy\n\n```bash\nspecd <command> [subcommand] [arguments...] [flags...]\n```\n\nWhen working within this repository without a global link:\n\n```bash\nnode packages/cli/dist/index.js <command> ...\n```\n\n### Global Options\n\nThese options apply across all command families:\n\n| Flag                | Description                                                           |\n| :------------------ | :-------------------------------------------------------------------- |\n| `--config <path>`   | Path to `specd.yaml` (overrides automatic config discovery).          |\n| `-v, --verbose`     | Increase logging verbosity (`-v` for info, `-vv` for debug/trace).    |\n| `--format <format>` | Output format: `text` (default), `json`, or `toon` (token-optimized). |\n\n### Output Formats\n\n- **`text`** _(default)_: Formatted for human readability with tables, status glyphs, and dynamic terminal-width fitting.\n- **`json`**: Structured JSON representation for scripting and programmatic ingestion.\n- **`toon`**: Ultra-compact token-optimized format specifically engineered to preserve LLM prompt context.\n\n---\n\n## The 12 Command Families\n\nSpecD organizes its capabilities into 12 dedicated command families:\n\n```mermaid\nflowchart LR\n    subgraph Governance & Lifecycle\n        P[project]\n        C[changes]\n        D[drafts]\n        DC[discarded]\n        A[archive]\n    end\n    subgraph Requirements & Specs\n        S[specs]\n        G[guide]\n    end\n    subgraph Architecture & Engine\n        SC[schema]\n        GR[graph]\n        CF[config]\n        ST[storage]\n        PL[plugins]\n    end\n```\n\n---\n\n### 1. `specd project` — Project Operations\n\nInspects project health, compiles context, and monitors change pipelines.\n\n- **`specd project init`** (or root shorthand **`specd init`**): Scaffolds a new project interactively, generating `specd.yaml`.\n- **`specd project status`**: High-level status showing active schema, workspaces, spec counts, active changes, and graph freshness.\n  ```bash\n  specd project status --context --graph --format toon\n  ```\n- **`specd project dashboard`**: Interactive terminal dashboard displaying project metrics, drift detection, and recent changes.\n- **`specd project context`**: Compiles global project context (architecture rules and global specs).\n  ```bash\n  specd project context --mode summary\n  ```\n- **`specd project context-specs`**: Discovers matching spec IDs for project and workspace include/exclude patterns without rendering full markdown.\n- **`specd project metadata`**: Inspects compiled project-level metadata and layout.\n- **`specd project update` / `update-metadata`**: Updates project settings or synchronizes metadata caches.\n\n👉 _See [Project Reference](../cli/project.md) for full options and examples._\n\n---\n\n### 2. `specd changes` — Change Lifecycle Management\n\nManages the core unit of work in SpecD from proposal to verification and archive.\n\n- **`specd changes create <name>`**: Initializes a new change directory under `.specd/changes/`.\n  ```bash\n  specd changes create add-oauth --specs default:auth/oauth\n  ```\n- **`specd changes list`**: Lists all active changes with their current lifecycle state.\n- **`specd changes status <name>`**: Displays the change state, completed artifacts DAG, blockers, and next action.\n  ```bash\n  specd changes status add-oauth\n  ```\n- **`specd changes transition <name> <state>`**: Advances change state (`designing`, `ready`, `implementing`, `verifying`, `done`, `archivable`).\n- **`specd changes validate <name>`**: Validates change artifacts structurally and checks cross-artifact relational integrity.\n- **`specd changes spec-preview <name> <specId>`**: Previews unified spec content after applying delta patches.\n  ```bash\n  specd changes spec-preview add-oauth default:auth/login --diff\n  ```\n- **`specd changes approve spec|signoff <name>`**: Grants human approvals at configured lifecycle gates.\n- **`specd changes context <name> <step>`**: Compiles the token-optimized instruction block and specs for the current step.\n- **`specd changes artifacts <name>`**: Lists artifact statuses (`complete`, `missing`, `drifted`, `skipped`).\n- **`specd changes skip-artifact <name> <artifactId>`**: Marks an optional artifact as skipped.\n- **`specd changes deps <name> [specId]`**: Inspects or modifies declared spec dependencies for the change.\n- **`specd changes draft <name>`**: Shelves an active change into `.specd/drafts/`.\n- **`specd changes discard <name>`**: Abandons an active change, moving it to `.specd/discarded/`.\n- **`specd changes archive <name>`**: Merges validated spec deltas into canonical specs and archives the change.\n- **`specd changes run-hooks <name> <step> [--phase pre|post]`**: Executes lifecycle validation hooks.\n- **`specd changes hook-instruction <name> <step>`**: Returns instructions defined for the current lifecycle hook.\n- **`specd changes artifact-instruction <name> [artifactId]`**: Retrieves the active instruction and template for the next artifact.\n- **`specd changes check-overlap [name]`**: Detects specifications targeted by multiple concurrent active changes.\n- **`specd changes invalidate <name>`**: Rolls back a change to `designing` when upstream specs have drifted.\n- **`specd changes implementation <subcommand> <name>`**: Tracks confirmed implementation files and symbols mapped to the change.\n\n👉 _See [Changes Reference](../cli/changes.md) for full options and examples._\n\n---\n\n### 3. `specd drafts` — Shelved Work\n\nBrowses and restores changes that were paused.\n\n- **`specd drafts list`**: Lists all shelved draft changes, showing date and reasons.\n- **`specd drafts show <name>`**: Inspects artifacts within a shelved change draft.\n- **`specd drafts restore <name>`**: Restores a shelved draft back to active changes in `.specd/changes/`.\n  ```bash\n  specd drafts restore add-oauth\n  ```\n\n👉 _See [Drafts Reference](../cli/drafts.md) for full options and examples._\n\n---\n\n### 4. `specd discarded` — Abandoned Work Audit\n\nMaintains an audit trail of changes that were abandoned.\n\n- **`specd discarded list`**: Lists all abandoned changes.\n- **`specd discarded show <name>`**: Inspects artifacts, proposal, and discard rationale of an abandoned change.\n\n👉 _See [Discarded Reference](../cli/discarded.md) for full options and examples._\n\n---\n\n### 5. `specd archive` — Completed History\n\nPermanent historical archive of merged changes.\n\n- **`specd archive list`**: Lists all completed, archived changes.\n- **`specd archive show <name>`**: Displays the archived change manifest, applied spec deltas, and completion audit record.\n  ```bash\n  specd archive show 20260920-core-architecture --diff\n  ```\n\n👉 _See [Archive Reference](../cli/archive.md) for full options and examples._\n\n---\n\n### 6. `specd specs` — Canonical Specifications\n\nDirect inspection, searching, and validation of specifications in workspaces.\n\n- **`specd specs list`**: Lists all specifications across workspaces.\n  ```bash\n  specd specs list --workspace core\n  ```\n- **`specd specs search <query>`**: Searches specifications by title, description, or requirements.\n- **`specd specs show <specId>`**: Displays full specification content (`spec.md` or `verify.md` with `--verify`).\n- **`specd specs outline <specId>`**: Displays hierarchical section headings and line boundaries.\n- **`specd specs context <specId>`**: Compiles the dependency context block for a single spec.\n- **`specd specs metadata <specId>`**: Inspects compiled requirements, dependencies, and verification scenarios.\n- **`specd specs resolve-path <specId>`**: Prints the exact filesystem path on disk.\n- **`specd specs validate [specId]`**: Validates Markdown AST and schema conformance.\n- **`specd specs init <specId>`**: Scaffolds a new spec directory with `spec.md` and `verify.md`.\n- **`specd specs deps <specId>`**: Adds, removes, or lists dependencies between specifications.\n- **`specd specs implementation <specId>`**: Maps source files and symbols to specification capabilities.\n- **`specd specs optimizations`**: Diagnostics for spec authoring and token usage optimization.\n\n👉 _See [Specs Reference](../cli/specs.md) for full options and examples._\n\n---\n\n### 7. `specd guide` — On-Demand Documentation\n\nAccess and search documentation topics directly within your terminal without opening a browser.\n\n- **`specd guide`**: Catalogs all available guides with titles and descriptions.\n- **`specd guide <topic>`**: Displays the full guide content.\n  ```bash\n  specd guide workflow\n  ```\n- **`specd guide <topic> --meta`**: Inspects document outline, heading depths, and line spans.\n- **`specd guide <topic> --section <name|index>`**: Slices a specific section by heading name or 1-indexed number.\n  ```bash\n  specd guide specs --section 2\n  ```\n- **`specd guide <topic> --start-line 20 --lines 30 --line-numbers`**: Windowed line slicing with 1-indexed line numbers.\n- **`specd guide search <query>`**: BM25 full-text search across all guide sections with contextual snippets.\n  ```bash\n  specd guide search \"approval gates\" --limit 3 --format toon\n  ```\n\n👉 _See [Guide Reference](../cli/guide.md) for full options and examples._\n\n---\n\n### 8. `specd graph` — Codebase Intelligence\n\nAST-based code graph indexing, dependency blast radius, and symbol resolution.\n\n- **`specd graph index`**: Builds or updates the AST dependency graph index.\n  ```bash\n  specd graph index --format toon\n  ```\n- **`specd graph search \"<query>\"`**: Fast fuzzy search across symbols (`--symbols`) or specifications (`--specs`).\n- **`specd graph impact`**: Evaluates blast-radius impact before modifying code:\n  ```bash\n  specd graph impact --symbol \"renderTable\" --direction dependents --format toon\n  specd graph impact --file \"packages/cli/src/helpers/table.ts\" --direction dependents\n  ```\n- **`specd graph stats`**: Displays total symbols, edges, file coverage, and index freshness.\n- **`specd graph hotspots`**: Identifies high-risk, heavily coupled files requiring extra care.\n\n👉 _See [Graph Reference](../cli/graph.md) for full options and examples._\n\n---\n\n### 9. `specd schema` — Workflow Customization\n\nIntrospects, extends, and validates artifact workflows.\n\n- **`specd schema show`**: Displays the active schema definition, declared artifacts, and lifecycle DAG.\n- **`specd schema fork <name>`**: Clones the default schema into a custom local schema for your project.\n- **`specd schema extend`**: Scaffolds a schema extension overlay with custom artifact types or rules.\n- **`specd schema validate`**: Validates a schema definition for circular dependencies and syntax rules.\n\n👉 _See [Schema Reference](../cli/schema.md) for full options and examples._\n\n---\n\n### 10. `specd config` — Configuration Inspection\n\nIntrospects the multi-layer configuration cascade.\n\n- **`specd config show`**: Displays effective configuration merged across defaults, user configs, and `specd.yaml`.\n  ```bash\n  specd config show --resolved --format json\n  ```\n\n👉 _See [Config Reference](../cli/config.md) for full options and examples._\n\n---\n\n### 11. `specd storage` — Cache & Index Storage\n\nManages underlying cache directories and internal indices.\n\n- **`specd storage reindex`**: Rebuilds internal metadata caches and spec indices from scratch.\n  ```bash\n  specd storage reindex --all\n  ```\n\n👉 _See [Storage Reference](../cli/storage.md) for full options and examples._\n\n---\n\n### 12. `specd plugins` — Extensibility & Agent Adapters\n\nInstalls and manages agent skill plugins and workflow integrations.\n\n- **`specd plugins list`**: Lists installed plugins and their active status.\n- **`specd plugins show <name>`**: Displays plugin metadata, registered hooks, and tools.\n- **`specd plugins install <package>`**: Installs and registers a plugin in `specd.yaml`.\n- **`specd plugins update [name]`**: Updates installed plugins to their latest versions.\n- **`specd plugins uninstall <name>`**: Removes a plugin and unregisters its hooks.\n\n👉 _See [Plugins Reference](../cli/plugins.md) for full options and examples._\n\n---\n\n## Practical Recipes\n\n### Recipe 1: Complete Change Lifecycle in 5 Steps\n\n```bash\n# 1. Create a change targeting a spec\nspecd changes create 20260924-user-profile --specs default:users/profile\n\n# 2. Transition to designing and author artifacts\nspecd changes transition 20260924-user-profile designing\n\n# 3. Validate artifacts and transition to implementing\nspecd changes validate 20260924-user-profile\nspecd changes transition 20260924-user-profile ready\nspecd changes transition 20260924-user-profile implementing\n\n# 4. Implement code, check status, verify against spec\nspecd changes status 20260924-user-profile\nspecd changes transition 20260924-user-profile verifying\nspecd changes transition 20260924-user-profile done\n\n# 5. Archive and merge deltas into canonical specs\nspecd changes transition 20260924-user-profile archivable\nspecd changes archive 20260924-user-profile\n```\n\n### Recipe 2: AI Agent Token Optimization\n\nWhen generating prompts or piping commands to AI agents, use `--format toon` to minimize tokens by up to 70%:\n\n```bash\nspecd project status --format toon\nspecd changes context 20260924-user-profile implementing --format toon\nspecd guide search \"approval gates\" --format toon\n```\n\n### Recipe 3: Blast-Radius Impact Analysis Before Refactoring\n\nBefore modifying a function or file, check who depends on it:\n\n```bash\nspecd graph impact --file packages/cli/src/helpers/table.ts --direction dependents --format text\n```\n"
      },
      {
        "index": 2,
        "heading": "Command Anatomy",
        "level": 2,
        "startLine": 9,
        "endLine": 38,
        "lines": 30,
        "content": "## Command Anatomy\n\n```bash\nspecd <command> [subcommand] [arguments...] [flags...]\n```\n\nWhen working within this repository without a global link:\n\n```bash\nnode packages/cli/dist/index.js <command> ...\n```\n\n### Global Options\n\nThese options apply across all command families:\n\n| Flag                | Description                                                           |\n| :------------------ | :-------------------------------------------------------------------- |\n| `--config <path>`   | Path to `specd.yaml` (overrides automatic config discovery).          |\n| `-v, --verbose`     | Increase logging verbosity (`-v` for info, `-vv` for debug/trace).    |\n| `--format <format>` | Output format: `text` (default), `json`, or `toon` (token-optimized). |\n\n### Output Formats\n\n- **`text`** _(default)_: Formatted for human readability with tables, status glyphs, and dynamic terminal-width fitting.\n- **`json`**: Structured JSON representation for scripting and programmatic ingestion.\n- **`toon`**: Ultra-compact token-optimized format specifically engineered to preserve LLM prompt context.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "Global Options",
        "level": 3,
        "startLine": 21,
        "endLine": 30,
        "lines": 10,
        "content": "### Global Options\n\nThese options apply across all command families:\n\n| Flag                | Description                                                           |\n| :------------------ | :-------------------------------------------------------------------- |\n| `--config <path>`   | Path to `specd.yaml` (overrides automatic config discovery).          |\n| `-v, --verbose`     | Increase logging verbosity (`-v` for info, `-vv` for debug/trace).    |\n| `--format <format>` | Output format: `text` (default), `json`, or `toon` (token-optimized). |\n"
      },
      {
        "index": 4,
        "heading": "Output Formats",
        "level": 3,
        "startLine": 31,
        "endLine": 38,
        "lines": 8,
        "content": "### Output Formats\n\n- **`text`** _(default)_: Formatted for human readability with tables, status glyphs, and dynamic terminal-width fitting.\n- **`json`**: Structured JSON representation for scripting and programmatic ingestion.\n- **`toon`**: Ultra-compact token-optimized format specifically engineered to preserve LLM prompt context.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "The 12 Command Families",
        "level": 2,
        "startLine": 39,
        "endLine": 288,
        "lines": 250,
        "content": "## The 12 Command Families\n\nSpecD organizes its capabilities into 12 dedicated command families:\n\n```mermaid\nflowchart LR\n    subgraph Governance & Lifecycle\n        P[project]\n        C[changes]\n        D[drafts]\n        DC[discarded]\n        A[archive]\n    end\n    subgraph Requirements & Specs\n        S[specs]\n        G[guide]\n    end\n    subgraph Architecture & Engine\n        SC[schema]\n        GR[graph]\n        CF[config]\n        ST[storage]\n        PL[plugins]\n    end\n```\n\n---\n\n### 1. `specd project` — Project Operations\n\nInspects project health, compiles context, and monitors change pipelines.\n\n- **`specd project init`** (or root shorthand **`specd init`**): Scaffolds a new project interactively, generating `specd.yaml`.\n- **`specd project status`**: High-level status showing active schema, workspaces, spec counts, active changes, and graph freshness.\n  ```bash\n  specd project status --context --graph --format toon\n  ```\n- **`specd project dashboard`**: Interactive terminal dashboard displaying project metrics, drift detection, and recent changes.\n- **`specd project context`**: Compiles global project context (architecture rules and global specs).\n  ```bash\n  specd project context --mode summary\n  ```\n- **`specd project context-specs`**: Discovers matching spec IDs for project and workspace include/exclude patterns without rendering full markdown.\n- **`specd project metadata`**: Inspects compiled project-level metadata and layout.\n- **`specd project update` / `update-metadata`**: Updates project settings or synchronizes metadata caches.\n\n👉 _See [Project Reference](../cli/project.md) for full options and examples._\n\n---\n\n### 2. `specd changes` — Change Lifecycle Management\n\nManages the core unit of work in SpecD from proposal to verification and archive.\n\n- **`specd changes create <name>`**: Initializes a new change directory under `.specd/changes/`.\n  ```bash\n  specd changes create add-oauth --specs default:auth/oauth\n  ```\n- **`specd changes list`**: Lists all active changes with their current lifecycle state.\n- **`specd changes status <name>`**: Displays the change state, completed artifacts DAG, blockers, and next action.\n  ```bash\n  specd changes status add-oauth\n  ```\n- **`specd changes transition <name> <state>`**: Advances change state (`designing`, `ready`, `implementing`, `verifying`, `done`, `archivable`).\n- **`specd changes validate <name>`**: Validates change artifacts structurally and checks cross-artifact relational integrity.\n- **`specd changes spec-preview <name> <specId>`**: Previews unified spec content after applying delta patches.\n  ```bash\n  specd changes spec-preview add-oauth default:auth/login --diff\n  ```\n- **`specd changes approve spec|signoff <name>`**: Grants human approvals at configured lifecycle gates.\n- **`specd changes context <name> <step>`**: Compiles the token-optimized instruction block and specs for the current step.\n- **`specd changes artifacts <name>`**: Lists artifact statuses (`complete`, `missing`, `drifted`, `skipped`).\n- **`specd changes skip-artifact <name> <artifactId>`**: Marks an optional artifact as skipped.\n- **`specd changes deps <name> [specId]`**: Inspects or modifies declared spec dependencies for the change.\n- **`specd changes draft <name>`**: Shelves an active change into `.specd/drafts/`.\n- **`specd changes discard <name>`**: Abandons an active change, moving it to `.specd/discarded/`.\n- **`specd changes archive <name>`**: Merges validated spec deltas into canonical specs and archives the change.\n- **`specd changes run-hooks <name> <step> [--phase pre|post]`**: Executes lifecycle validation hooks.\n- **`specd changes hook-instruction <name> <step>`**: Returns instructions defined for the current lifecycle hook.\n- **`specd changes artifact-instruction <name> [artifactId]`**: Retrieves the active instruction and template for the next artifact.\n- **`specd changes check-overlap [name]`**: Detects specifications targeted by multiple concurrent active changes.\n- **`specd changes invalidate <name>`**: Rolls back a change to `designing` when upstream specs have drifted.\n- **`specd changes implementation <subcommand> <name>`**: Tracks confirmed implementation files and symbols mapped to the change.\n\n👉 _See [Changes Reference](../cli/changes.md) for full options and examples._\n\n---\n\n### 3. `specd drafts` — Shelved Work\n\nBrowses and restores changes that were paused.\n\n- **`specd drafts list`**: Lists all shelved draft changes, showing date and reasons.\n- **`specd drafts show <name>`**: Inspects artifacts within a shelved change draft.\n- **`specd drafts restore <name>`**: Restores a shelved draft back to active changes in `.specd/changes/`.\n  ```bash\n  specd drafts restore add-oauth\n  ```\n\n👉 _See [Drafts Reference](../cli/drafts.md) for full options and examples._\n\n---\n\n### 4. `specd discarded` — Abandoned Work Audit\n\nMaintains an audit trail of changes that were abandoned.\n\n- **`specd discarded list`**: Lists all abandoned changes.\n- **`specd discarded show <name>`**: Inspects artifacts, proposal, and discard rationale of an abandoned change.\n\n👉 _See [Discarded Reference](../cli/discarded.md) for full options and examples._\n\n---\n\n### 5. `specd archive` — Completed History\n\nPermanent historical archive of merged changes.\n\n- **`specd archive list`**: Lists all completed, archived changes.\n- **`specd archive show <name>`**: Displays the archived change manifest, applied spec deltas, and completion audit record.\n  ```bash\n  specd archive show 20260920-core-architecture --diff\n  ```\n\n👉 _See [Archive Reference](../cli/archive.md) for full options and examples._\n\n---\n\n### 6. `specd specs` — Canonical Specifications\n\nDirect inspection, searching, and validation of specifications in workspaces.\n\n- **`specd specs list`**: Lists all specifications across workspaces.\n  ```bash\n  specd specs list --workspace core\n  ```\n- **`specd specs search <query>`**: Searches specifications by title, description, or requirements.\n- **`specd specs show <specId>`**: Displays full specification content (`spec.md` or `verify.md` with `--verify`).\n- **`specd specs outline <specId>`**: Displays hierarchical section headings and line boundaries.\n- **`specd specs context <specId>`**: Compiles the dependency context block for a single spec.\n- **`specd specs metadata <specId>`**: Inspects compiled requirements, dependencies, and verification scenarios.\n- **`specd specs resolve-path <specId>`**: Prints the exact filesystem path on disk.\n- **`specd specs validate [specId]`**: Validates Markdown AST and schema conformance.\n- **`specd specs init <specId>`**: Scaffolds a new spec directory with `spec.md` and `verify.md`.\n- **`specd specs deps <specId>`**: Adds, removes, or lists dependencies between specifications.\n- **`specd specs implementation <specId>`**: Maps source files and symbols to specification capabilities.\n- **`specd specs optimizations`**: Diagnostics for spec authoring and token usage optimization.\n\n👉 _See [Specs Reference](../cli/specs.md) for full options and examples._\n\n---\n\n### 7. `specd guide` — On-Demand Documentation\n\nAccess and search documentation topics directly within your terminal without opening a browser.\n\n- **`specd guide`**: Catalogs all available guides with titles and descriptions.\n- **`specd guide <topic>`**: Displays the full guide content.\n  ```bash\n  specd guide workflow\n  ```\n- **`specd guide <topic> --meta`**: Inspects document outline, heading depths, and line spans.\n- **`specd guide <topic> --section <name|index>`**: Slices a specific section by heading name or 1-indexed number.\n  ```bash\n  specd guide specs --section 2\n  ```\n- **`specd guide <topic> --start-line 20 --lines 30 --line-numbers`**: Windowed line slicing with 1-indexed line numbers.\n- **`specd guide search <query>`**: BM25 full-text search across all guide sections with contextual snippets.\n  ```bash\n  specd guide search \"approval gates\" --limit 3 --format toon\n  ```\n\n👉 _See [Guide Reference](../cli/guide.md) for full options and examples._\n\n---\n\n### 8. `specd graph` — Codebase Intelligence\n\nAST-based code graph indexing, dependency blast radius, and symbol resolution.\n\n- **`specd graph index`**: Builds or updates the AST dependency graph index.\n  ```bash\n  specd graph index --format toon\n  ```\n- **`specd graph search \"<query>\"`**: Fast fuzzy search across symbols (`--symbols`) or specifications (`--specs`).\n- **`specd graph impact`**: Evaluates blast-radius impact before modifying code:\n  ```bash\n  specd graph impact --symbol \"renderTable\" --direction dependents --format toon\n  specd graph impact --file \"packages/cli/src/helpers/table.ts\" --direction dependents\n  ```\n- **`specd graph stats`**: Displays total symbols, edges, file coverage, and index freshness.\n- **`specd graph hotspots`**: Identifies high-risk, heavily coupled files requiring extra care.\n\n👉 _See [Graph Reference](../cli/graph.md) for full options and examples._\n\n---\n\n### 9. `specd schema` — Workflow Customization\n\nIntrospects, extends, and validates artifact workflows.\n\n- **`specd schema show`**: Displays the active schema definition, declared artifacts, and lifecycle DAG.\n- **`specd schema fork <name>`**: Clones the default schema into a custom local schema for your project.\n- **`specd schema extend`**: Scaffolds a schema extension overlay with custom artifact types or rules.\n- **`specd schema validate`**: Validates a schema definition for circular dependencies and syntax rules.\n\n👉 _See [Schema Reference](../cli/schema.md) for full options and examples._\n\n---\n\n### 10. `specd config` — Configuration Inspection\n\nIntrospects the multi-layer configuration cascade.\n\n- **`specd config show`**: Displays effective configuration merged across defaults, user configs, and `specd.yaml`.\n  ```bash\n  specd config show --resolved --format json\n  ```\n\n👉 _See [Config Reference](../cli/config.md) for full options and examples._\n\n---\n\n### 11. `specd storage` — Cache & Index Storage\n\nManages underlying cache directories and internal indices.\n\n- **`specd storage reindex`**: Rebuilds internal metadata caches and spec indices from scratch.\n  ```bash\n  specd storage reindex --all\n  ```\n\n👉 _See [Storage Reference](../cli/storage.md) for full options and examples._\n\n---\n\n### 12. `specd plugins` — Extensibility & Agent Adapters\n\nInstalls and manages agent skill plugins and workflow integrations.\n\n- **`specd plugins list`**: Lists installed plugins and their active status.\n- **`specd plugins show <name>`**: Displays plugin metadata, registered hooks, and tools.\n- **`specd plugins install <package>`**: Installs and registers a plugin in `specd.yaml`.\n- **`specd plugins update [name]`**: Updates installed plugins to their latest versions.\n- **`specd plugins uninstall <name>`**: Removes a plugin and unregisters its hooks.\n\n👉 _See [Plugins Reference](../cli/plugins.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 6,
        "heading": "1. `specd project` — Project Operations",
        "level": 3,
        "startLine": 67,
        "endLine": 88,
        "lines": 22,
        "content": "### 1. `specd project` — Project Operations\n\nInspects project health, compiles context, and monitors change pipelines.\n\n- **`specd project init`** (or root shorthand **`specd init`**): Scaffolds a new project interactively, generating `specd.yaml`.\n- **`specd project status`**: High-level status showing active schema, workspaces, spec counts, active changes, and graph freshness.\n  ```bash\n  specd project status --context --graph --format toon\n  ```\n- **`specd project dashboard`**: Interactive terminal dashboard displaying project metrics, drift detection, and recent changes.\n- **`specd project context`**: Compiles global project context (architecture rules and global specs).\n  ```bash\n  specd project context --mode summary\n  ```\n- **`specd project context-specs`**: Discovers matching spec IDs for project and workspace include/exclude patterns without rendering full markdown.\n- **`specd project metadata`**: Inspects compiled project-level metadata and layout.\n- **`specd project update` / `update-metadata`**: Updates project settings or synchronizes metadata caches.\n\n👉 _See [Project Reference](../cli/project.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 7,
        "heading": "2. `specd changes` — Change Lifecycle Management",
        "level": 3,
        "startLine": 89,
        "endLine": 126,
        "lines": 38,
        "content": "### 2. `specd changes` — Change Lifecycle Management\n\nManages the core unit of work in SpecD from proposal to verification and archive.\n\n- **`specd changes create <name>`**: Initializes a new change directory under `.specd/changes/`.\n  ```bash\n  specd changes create add-oauth --specs default:auth/oauth\n  ```\n- **`specd changes list`**: Lists all active changes with their current lifecycle state.\n- **`specd changes status <name>`**: Displays the change state, completed artifacts DAG, blockers, and next action.\n  ```bash\n  specd changes status add-oauth\n  ```\n- **`specd changes transition <name> <state>`**: Advances change state (`designing`, `ready`, `implementing`, `verifying`, `done`, `archivable`).\n- **`specd changes validate <name>`**: Validates change artifacts structurally and checks cross-artifact relational integrity.\n- **`specd changes spec-preview <name> <specId>`**: Previews unified spec content after applying delta patches.\n  ```bash\n  specd changes spec-preview add-oauth default:auth/login --diff\n  ```\n- **`specd changes approve spec|signoff <name>`**: Grants human approvals at configured lifecycle gates.\n- **`specd changes context <name> <step>`**: Compiles the token-optimized instruction block and specs for the current step.\n- **`specd changes artifacts <name>`**: Lists artifact statuses (`complete`, `missing`, `drifted`, `skipped`).\n- **`specd changes skip-artifact <name> <artifactId>`**: Marks an optional artifact as skipped.\n- **`specd changes deps <name> [specId]`**: Inspects or modifies declared spec dependencies for the change.\n- **`specd changes draft <name>`**: Shelves an active change into `.specd/drafts/`.\n- **`specd changes discard <name>`**: Abandons an active change, moving it to `.specd/discarded/`.\n- **`specd changes archive <name>`**: Merges validated spec deltas into canonical specs and archives the change.\n- **`specd changes run-hooks <name> <step> [--phase pre|post]`**: Executes lifecycle validation hooks.\n- **`specd changes hook-instruction <name> <step>`**: Returns instructions defined for the current lifecycle hook.\n- **`specd changes artifact-instruction <name> [artifactId]`**: Retrieves the active instruction and template for the next artifact.\n- **`specd changes check-overlap [name]`**: Detects specifications targeted by multiple concurrent active changes.\n- **`specd changes invalidate <name>`**: Rolls back a change to `designing` when upstream specs have drifted.\n- **`specd changes implementation <subcommand> <name>`**: Tracks confirmed implementation files and symbols mapped to the change.\n\n👉 _See [Changes Reference](../cli/changes.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 8,
        "heading": "3. `specd drafts` — Shelved Work",
        "level": 3,
        "startLine": 127,
        "endLine": 141,
        "lines": 15,
        "content": "### 3. `specd drafts` — Shelved Work\n\nBrowses and restores changes that were paused.\n\n- **`specd drafts list`**: Lists all shelved draft changes, showing date and reasons.\n- **`specd drafts show <name>`**: Inspects artifacts within a shelved change draft.\n- **`specd drafts restore <name>`**: Restores a shelved draft back to active changes in `.specd/changes/`.\n  ```bash\n  specd drafts restore add-oauth\n  ```\n\n👉 _See [Drafts Reference](../cli/drafts.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 9,
        "heading": "4. `specd discarded` — Abandoned Work Audit",
        "level": 3,
        "startLine": 142,
        "endLine": 152,
        "lines": 11,
        "content": "### 4. `specd discarded` — Abandoned Work Audit\n\nMaintains an audit trail of changes that were abandoned.\n\n- **`specd discarded list`**: Lists all abandoned changes.\n- **`specd discarded show <name>`**: Inspects artifacts, proposal, and discard rationale of an abandoned change.\n\n👉 _See [Discarded Reference](../cli/discarded.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 10,
        "heading": "5. `specd archive` — Completed History",
        "level": 3,
        "startLine": 153,
        "endLine": 166,
        "lines": 14,
        "content": "### 5. `specd archive` — Completed History\n\nPermanent historical archive of merged changes.\n\n- **`specd archive list`**: Lists all completed, archived changes.\n- **`specd archive show <name>`**: Displays the archived change manifest, applied spec deltas, and completion audit record.\n  ```bash\n  specd archive show 20260920-core-architecture --diff\n  ```\n\n👉 _See [Archive Reference](../cli/archive.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 11,
        "heading": "6. `specd specs` — Canonical Specifications",
        "level": 3,
        "startLine": 167,
        "endLine": 190,
        "lines": 24,
        "content": "### 6. `specd specs` — Canonical Specifications\n\nDirect inspection, searching, and validation of specifications in workspaces.\n\n- **`specd specs list`**: Lists all specifications across workspaces.\n  ```bash\n  specd specs list --workspace core\n  ```\n- **`specd specs search <query>`**: Searches specifications by title, description, or requirements.\n- **`specd specs show <specId>`**: Displays full specification content (`spec.md` or `verify.md` with `--verify`).\n- **`specd specs outline <specId>`**: Displays hierarchical section headings and line boundaries.\n- **`specd specs context <specId>`**: Compiles the dependency context block for a single spec.\n- **`specd specs metadata <specId>`**: Inspects compiled requirements, dependencies, and verification scenarios.\n- **`specd specs resolve-path <specId>`**: Prints the exact filesystem path on disk.\n- **`specd specs validate [specId]`**: Validates Markdown AST and schema conformance.\n- **`specd specs init <specId>`**: Scaffolds a new spec directory with `spec.md` and `verify.md`.\n- **`specd specs deps <specId>`**: Adds, removes, or lists dependencies between specifications.\n- **`specd specs implementation <specId>`**: Maps source files and symbols to specification capabilities.\n- **`specd specs optimizations`**: Diagnostics for spec authoring and token usage optimization.\n\n👉 _See [Specs Reference](../cli/specs.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 12,
        "heading": "7. `specd guide` — On-Demand Documentation",
        "level": 3,
        "startLine": 191,
        "endLine": 214,
        "lines": 24,
        "content": "### 7. `specd guide` — On-Demand Documentation\n\nAccess and search documentation topics directly within your terminal without opening a browser.\n\n- **`specd guide`**: Catalogs all available guides with titles and descriptions.\n- **`specd guide <topic>`**: Displays the full guide content.\n  ```bash\n  specd guide workflow\n  ```\n- **`specd guide <topic> --meta`**: Inspects document outline, heading depths, and line spans.\n- **`specd guide <topic> --section <name|index>`**: Slices a specific section by heading name or 1-indexed number.\n  ```bash\n  specd guide specs --section 2\n  ```\n- **`specd guide <topic> --start-line 20 --lines 30 --line-numbers`**: Windowed line slicing with 1-indexed line numbers.\n- **`specd guide search <query>`**: BM25 full-text search across all guide sections with contextual snippets.\n  ```bash\n  specd guide search \"approval gates\" --limit 3 --format toon\n  ```\n\n👉 _See [Guide Reference](../cli/guide.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 13,
        "heading": "8. `specd graph` — Codebase Intelligence",
        "level": 3,
        "startLine": 215,
        "endLine": 235,
        "lines": 21,
        "content": "### 8. `specd graph` — Codebase Intelligence\n\nAST-based code graph indexing, dependency blast radius, and symbol resolution.\n\n- **`specd graph index`**: Builds or updates the AST dependency graph index.\n  ```bash\n  specd graph index --format toon\n  ```\n- **`specd graph search \"<query>\"`**: Fast fuzzy search across symbols (`--symbols`) or specifications (`--specs`).\n- **`specd graph impact`**: Evaluates blast-radius impact before modifying code:\n  ```bash\n  specd graph impact --symbol \"renderTable\" --direction dependents --format toon\n  specd graph impact --file \"packages/cli/src/helpers/table.ts\" --direction dependents\n  ```\n- **`specd graph stats`**: Displays total symbols, edges, file coverage, and index freshness.\n- **`specd graph hotspots`**: Identifies high-risk, heavily coupled files requiring extra care.\n\n👉 _See [Graph Reference](../cli/graph.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 14,
        "heading": "9. `specd schema` — Workflow Customization",
        "level": 3,
        "startLine": 236,
        "endLine": 248,
        "lines": 13,
        "content": "### 9. `specd schema` — Workflow Customization\n\nIntrospects, extends, and validates artifact workflows.\n\n- **`specd schema show`**: Displays the active schema definition, declared artifacts, and lifecycle DAG.\n- **`specd schema fork <name>`**: Clones the default schema into a custom local schema for your project.\n- **`specd schema extend`**: Scaffolds a schema extension overlay with custom artifact types or rules.\n- **`specd schema validate`**: Validates a schema definition for circular dependencies and syntax rules.\n\n👉 _See [Schema Reference](../cli/schema.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 15,
        "heading": "10. `specd config` — Configuration Inspection",
        "level": 3,
        "startLine": 249,
        "endLine": 261,
        "lines": 13,
        "content": "### 10. `specd config` — Configuration Inspection\n\nIntrospects the multi-layer configuration cascade.\n\n- **`specd config show`**: Displays effective configuration merged across defaults, user configs, and `specd.yaml`.\n  ```bash\n  specd config show --resolved --format json\n  ```\n\n👉 _See [Config Reference](../cli/config.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 16,
        "heading": "11. `specd storage` — Cache & Index Storage",
        "level": 3,
        "startLine": 262,
        "endLine": 274,
        "lines": 13,
        "content": "### 11. `specd storage` — Cache & Index Storage\n\nManages underlying cache directories and internal indices.\n\n- **`specd storage reindex`**: Rebuilds internal metadata caches and spec indices from scratch.\n  ```bash\n  specd storage reindex --all\n  ```\n\n👉 _See [Storage Reference](../cli/storage.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 17,
        "heading": "12. `specd plugins` — Extensibility & Agent Adapters",
        "level": 3,
        "startLine": 275,
        "endLine": 288,
        "lines": 14,
        "content": "### 12. `specd plugins` — Extensibility & Agent Adapters\n\nInstalls and manages agent skill plugins and workflow integrations.\n\n- **`specd plugins list`**: Lists installed plugins and their active status.\n- **`specd plugins show <name>`**: Displays plugin metadata, registered hooks, and tools.\n- **`specd plugins install <package>`**: Installs and registers a plugin in `specd.yaml`.\n- **`specd plugins update [name]`**: Updates installed plugins to their latest versions.\n- **`specd plugins uninstall <name>`**: Removes a plugin and unregisters its hooks.\n\n👉 _See [Plugins Reference](../cli/plugins.md) for full options and examples._\n\n---\n"
      },
      {
        "index": 18,
        "heading": "Practical Recipes",
        "level": 2,
        "startLine": 289,
        "endLine": 332,
        "lines": 44,
        "content": "## Practical Recipes\n\n### Recipe 1: Complete Change Lifecycle in 5 Steps\n\n```bash\n# 1. Create a change targeting a spec\nspecd changes create 20260924-user-profile --specs default:users/profile\n\n# 2. Transition to designing and author artifacts\nspecd changes transition 20260924-user-profile designing\n\n# 3. Validate artifacts and transition to implementing\nspecd changes validate 20260924-user-profile\nspecd changes transition 20260924-user-profile ready\nspecd changes transition 20260924-user-profile implementing\n\n# 4. Implement code, check status, verify against spec\nspecd changes status 20260924-user-profile\nspecd changes transition 20260924-user-profile verifying\nspecd changes transition 20260924-user-profile done\n\n# 5. Archive and merge deltas into canonical specs\nspecd changes transition 20260924-user-profile archivable\nspecd changes archive 20260924-user-profile\n```\n\n### Recipe 2: AI Agent Token Optimization\n\nWhen generating prompts or piping commands to AI agents, use `--format toon` to minimize tokens by up to 70%:\n\n```bash\nspecd project status --format toon\nspecd changes context 20260924-user-profile implementing --format toon\nspecd guide search \"approval gates\" --format toon\n```\n\n### Recipe 3: Blast-Radius Impact Analysis Before Refactoring\n\nBefore modifying a function or file, check who depends on it:\n\n```bash\nspecd graph impact --file packages/cli/src/helpers/table.ts --direction dependents --format text\n```\n"
      },
      {
        "index": 19,
        "heading": "Recipe 1: Complete Change Lifecycle in 5 Steps",
        "level": 3,
        "startLine": 291,
        "endLine": 314,
        "lines": 24,
        "content": "### Recipe 1: Complete Change Lifecycle in 5 Steps\n\n```bash\n# 1. Create a change targeting a spec\nspecd changes create 20260924-user-profile --specs default:users/profile\n\n# 2. Transition to designing and author artifacts\nspecd changes transition 20260924-user-profile designing\n\n# 3. Validate artifacts and transition to implementing\nspecd changes validate 20260924-user-profile\nspecd changes transition 20260924-user-profile ready\nspecd changes transition 20260924-user-profile implementing\n\n# 4. Implement code, check status, verify against spec\nspecd changes status 20260924-user-profile\nspecd changes transition 20260924-user-profile verifying\nspecd changes transition 20260924-user-profile done\n\n# 5. Archive and merge deltas into canonical specs\nspecd changes transition 20260924-user-profile archivable\nspecd changes archive 20260924-user-profile\n```\n"
      },
      {
        "index": 20,
        "heading": "Recipe 2: AI Agent Token Optimization",
        "level": 3,
        "startLine": 315,
        "endLine": 324,
        "lines": 10,
        "content": "### Recipe 2: AI Agent Token Optimization\n\nWhen generating prompts or piping commands to AI agents, use `--format toon` to minimize tokens by up to 70%:\n\n```bash\nspecd project status --format toon\nspecd changes context 20260924-user-profile implementing --format toon\nspecd guide search \"approval gates\" --format toon\n```\n"
      },
      {
        "index": 21,
        "heading": "Recipe 3: Blast-Radius Impact Analysis Before Refactoring",
        "level": 3,
        "startLine": 325,
        "endLine": 332,
        "lines": 8,
        "content": "### Recipe 3: Blast-Radius Impact Analysis Before Refactoring\n\nBefore modifying a function or file, check who depends on it:\n\n```bash\nspecd graph impact --file packages/cli/src/helpers/table.ts --direction dependents --format text\n```\n"
      }
    ]
  },
  {
    "topic": "schemas",
    "title": "Workflow Schemas",
    "description": "Understanding and customizing development workflow schemas and lifecycle phases.",
    "order": 6,
    "content": "# Schemas\n\nA schema defines the shape of your development workflow. It declares which artifact files exist, what order they are produced in, what rules govern them, and which lifecycle phases gate progression from design to archive.\n\nEvery SpecD project has exactly one active schema. You declare it in `specd.yaml` via the `schema` field:\n\n```yaml\nschema: '@specd/schema-std'\n```\n\nSpecD ships a standard schema — `@specd/schema-std` — that works for most projects. This guide explains what that schema does and how to adapt it to your needs.\n\n## The standard schema\n\n`@specd/schema-std` encodes the workflow: **proposal → specs → verify → design → tasks**. Five artifact types, a dependency graph between them, and a lifecycle that takes a change from idea to archived spec.\n\n### Artifacts\n\n#### 1. proposal (scope: change)\n\nThe proposal answers _why_ this change is needed. It is the entry point for every change — no other artifact can be written until a proposal exists.\n\nA proposal covers motivation, current behaviour, proposed solution, the list of specs that will be created or modified, affected code areas, and open questions. The \"Specs affected\" section is the contract that drives everything downstream: every spec listed there must eventually be covered by a `spec.md` file or a delta.\n\nProposals are working documents. They live in the change directory and are not archived into the spec repository — they describe intent, not permanent requirements.\n\n#### 2. specs (scope: spec)\n\nThe spec artifact (`spec.md`) defines _what_ the system should do. One spec file per capability. Specs use SHALL / MUST language, group requirements under `### Requirement: <name>` headings, and explicitly avoid WHEN/THEN scenarios (those belong in `verify.md`).\n\nUnlike `proposal.md`, specs are permanent. They are synced into the `specs/` directory when the change is archived and remain there as the authoritative record of requirements.\n\nSpecs also support delta files: instead of rewriting an existing spec, the agent produces a `.delta.yaml` that expresses the changes as structured operations (add, modify, remove a requirement). SpecD applies deltas deterministically at archive time.\n\nRequires: `proposal`\n\n#### 3. verify (scope: spec)\n\nThe verify artifact (`verify.md`) defines _how to confirm_ the spec is satisfied. It groups GIVEN/WHEN/THEN scenarios under `### Requirement: <name>` headings that mirror `spec.md` exactly. Each scenario describes a concrete check, not a restatement of the requirement.\n\nLike `spec.md`, verify files are permanent and synced to the spec directory at archive time.\n\nRequires: `specs`\n\n#### 4. design (scope: change)\n\nThe design document explains _how to implement_ the change. It analyses the codebase, lists affected files and symbols, defines new constructs with full signatures, documents the implementation approach, and maps every spec requirement and verify scenario to a concrete implementation path.\n\nDesign is a working document — it belongs to the change, not the permanent spec record. It is not optional, but its depth scales with the nature of the change: a large code refactor warrants detailed interface signatures; a documentation change warrants a content analysis.\n\nRequires: `proposal`, `specs`, `verify` — the agent reads all three before writing the design.\n\n#### 5. tasks (scope: change)\n\nTasks is the implementation checklist. Each task is a markdown checkbox (`- [ ]`) with indented context: which file, which symbol, what to change, and the specific approach from `design.md`. The agent works through tasks one by one and marks each complete (`- [x]`) as it goes.\n\nSpecD uses task completion to gate the transition from `implementing` to `verifying` — the transition is blocked while any `- [ ]` items remain. Checking off tasks does not change the artifact's hash (pre-hash cleanup normalises checkboxes), so progress tracking does not trigger re-validation.\n\nRequires: `specs`, `design` — the agent derives tasks from design.md and maps them back to spec requirements.\n\n### Dependency graph\n\n```\nproposal\n  ↓\nspecs\n  ↓\nverify\n  ↓\ndesign   (requires: proposal, specs, verify)\n  ↓\ntasks    (requires: specs, design)\n```\n\nThe graph is acyclic and enforced by SpecD. An artifact's status is `pending` until all of its `requires` are `complete` (or `skipped`, for optional ones). You cannot start writing `verify.md` before `spec.md` is complete; you cannot start design before verify is done; you cannot start tasks before design is done.\n\n## Artifact scope explained\n\nEvery artifact is either `scope: spec` or `scope: change`. This is the single most important property for understanding what survives a change.\n\n**`scope: spec`** — the artifact is a permanent part of the spec record. When a change is archived, SpecD syncs every `scope: spec` artifact from the change directory into the `specs/` repository. The `specd validate` command checks that every spec directory contains all required `scope: spec` artifacts. In the standard schema, `spec.md` and `verify.md` are `scope: spec`.\n\n**`scope: change`** — the artifact is a working document. It is validated during the change but never synced to the spec directory. `proposal.md`, `design.md`, and `tasks.md` are `scope: change` — they capture thinking and process, not permanent requirements.\n\nAt archive time the picture is:\n\n- The change's `specs/` subdirectory (new `spec.md` and `verify.md` files, or delta files) is applied to the spec repository.\n- `proposal.md`, `design.md`, and `tasks.md` remain in the change's archive directory as a record of the work, but they do not become part of the living spec.\n\n## Templates\n\nEach artifact type in `@specd/schema-std` ships with a template — a plain Markdown file used as scaffolding when the agent creates the artifact for the first time.\n\nTemplates are plain text. SpecD performs no variable substitution on them. HTML comments (`<!-- ... -->`) are preserved as-is and serve as guidance hints for the AI agent, pointing it toward intent, format requirements, and common pitfalls.\n\nHere is the shape of each template:\n\n**proposal.md** — opens with an HTML comment reminding the agent to focus on _why_, then provides sections for Motivation, Current behaviour, Proposed solution, Specs affected (split into New specs and Modified specs), Impact, and Open questions.\n\n**spec.md** — opens with an HTML comment on the SHALL/MUST convention and the prohibition on WHEN/THEN, then provides a Purpose section, a Requirements section with one placeholder `### Requirement:` heading, a Constraints section, and a Spec Dependencies section.\n\n**verify.md** — opens with a comment on the GIVEN/WHEN/THEN format and when to use AND/OR, then provides a structure with a `### Requirement:` heading and a `#### Scenario:` block with placeholders for GIVEN, WHEN, and THEN.\n\n**design.md** — opens with a comment on the required depth and concrete specificity, then provides sections for Non-goals, Affected areas, New constructs, Approach, Key decisions, Trade-offs, Migration/Rollback, Testing, and Open questions.\n\n**tasks.md** — opens with a comment on deriving tasks from design.md and including the approach inline, then provides two numbered task group sections with checkbox placeholders.\n\nTemplates can be replaced or extended when you extend or fork the schema. See [Customising your schema](#customising-your-schema) below.\n\n## Artifact rules\n\nEvery artifact can carry `instruction` text and `rules` blocks. These are injected into the AI context when the agent is working on that artifact.\n\n`instruction` is the main prompt for that artifact — what to write, how to structure it, and what to watch out for.\n\n`rules` are pre- and post-instruction blocks injected around the instruction:\n\n```yaml\n- id: tasks\n  scope: change\n  output: tasks.md\n  instruction: |\n    Create the implementation checklist...\n  rules:\n    pre:\n      - id: read-design-first\n        instruction: 'Read design.md in full before creating any tasks.'\n    post:\n      - id: design-tasks-coverage\n        instruction: |\n          Cross-reference design.md against the tasks just written.\n          Every item in Affected areas must map to at least one task.\n```\n\nIn the standard schema, every artifact has a post-rule that cross-references the previous artifact for coverage: proposals check open questions are resolved before proceeding; specs check every listed spec has a file; verify checks every requirement has a scenario; and so on.\n\n`requires` and `optional` control the dependency graph described above. A non-optional artifact cannot hard-depend on an optional one — that would make the non-optional artifact effectively optional too.\n\n## Workflow steps\n\nThe schema also defines the lifecycle phases a change moves through. In `@specd/schema-std`:\n\n| Step           | Requires (artifacts)                   |\n| -------------- | -------------------------------------- |\n| `designing`    | _(none — always available)_            |\n| `ready`        | proposal, specs, verify, design, tasks |\n| `implementing` | proposal, specs, verify, design, tasks |\n| `verifying`    | verify, tasks                          |\n| `archiving`    | specs, tasks                           |\n\nEach step is independently gated by its own `requires`. A step becomes available once all listed artifacts are complete.\n\nSteps can have `pre` and `post` hooks. Hooks are either instructions injected into the AI context or shell commands run at the phase boundary:\n\n```yaml\n- step: implementing\n  requires: [proposal, specs, verify, design, tasks]\n  hooks:\n    pre:\n      - id: implementing-guidance\n        instruction: |\n          Read all change artifacts before starting...\n    post:\n      - id: run-tests\n        run: 'pnpm test'\n```\n\n**Pre-hook failure** — if a `run:` hook exits non-zero, the step is aborted. The agent should fix the problem and retry.\n\n**Post-hook failure** — the step has already completed and is not rolled back. The user is prompted to continue with the remaining hooks or stop.\n\nShell hooks support template variables:\n\n| Variable           | Value                                                  |\n| ------------------ | ------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name                                 |\n| `{{change.path}}`  | Absolute path to the change directory                  |\n| `{{project.root}}` | Absolute path to the directory containing `specd.yaml` |\n\n## Customising your schema\n\nThere are three ways to customise schema behaviour, ranging from a single-line config change to a complete rewrite.\n\n### 1. schemaOverrides in specd.yaml\n\n`schemaOverrides` is the lightest-weight option. It modifies the active schema at load time without touching any file on disk. Use it for project-specific tweaks: an extra hook, an additional rule, a new artifact type that only this project needs.\n\nIt supports five operations:\n\n| Operation | Effect                                                                                   |\n| --------- | ---------------------------------------------------------------------------------------- |\n| `create`  | Adds a new entry (artifact, workflow step). Fails if the ID already exists.              |\n| `append`  | Appends entries to arrays (rules, hooks, metadataExtraction entries).                    |\n| `prepend` | Prepends entries to arrays.                                                              |\n| `set`     | Replaces scalar fields or whole array entries by identity.                               |\n| `remove`  | Removes entries from arrays by identity (`id` for artifacts, `step` for workflow steps). |\n\n**Example: add a post-implementing hook that runs your test suite**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n```\n\n**Example: add a rule to an existing artifact**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-compliance\n              text: >-\n                Cross-reference this design against the global compliance spec before proceeding.\n```\n\n**Example: add a new artifact type (an ADR)**\n\n```yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: Architecture Decision Record\n        requires: [specs]\n```\n\n`schemaOverrides` is the right tool for:\n\n- Adding or removing hooks on existing workflow steps\n- Appending rules or instructions to existing artifacts\n- Adding a new artifact type that is unique to your project\n\n### 2. Extend — specd schema extend\n\n`specd schema extend` creates a new schema package that inherits from a parent and adds to it. The parent schema stays intact; the child schema describes only the delta. This is the right approach when you want to share a schema across multiple projects but each project needs additions.\n\n```sh\nspecd schema extend @specd/schema-std my-workflow\n```\n\nThe generated schema declares its parent and adds or modifies entries. Any artifact or workflow step from the parent is inherited as-is; you only declare what changes.\n\nUse extend when:\n\n- You want to add a new artifact type that multiple projects will share\n- You want to publish the customisation as a reusable plugin\n- The base schema stays structurally intact — you are adding, not restructuring\n\n### 3. Fork — specd schema fork\n\n`specd schema fork` makes a complete copy of a schema. You own every field. Use it when you need structural changes incompatible with the parent — different artifact ordering, renamed lifecycle steps, or a workflow that looks nothing like the original.\n\n```sh\nspecd schema fork @specd/schema-std custom-workflow\n```\n\nUse fork when:\n\n- You want to completely redesign the artifact dependency graph\n- You need to rename lifecycle steps\n- The base schema's structure would need to be dismantled more than extended\n\n### Choosing the right approach\n\n| Situation                                      | Approach          |\n| ---------------------------------------------- | ----------------- |\n| Add a hook after `implementing`                | `schemaOverrides` |\n| Add a rule to the `design` artifact            | `schemaOverrides` |\n| Add a new artifact type for this project only  | `schemaOverrides` |\n| Add a new artifact type shared across projects | extend            |\n| Publish schema additions as a reusable plugin  | extend            |\n| Rename lifecycle steps                         | fork              |\n| Restructure artifact dependencies from scratch | fork              |\n\n## Validation rules\n\nSchemas can declare structural validation rules for artifact content. Validations check that an artifact's AST matches the declared structure — for example, that `spec.md` always has a `## Requirements` section with at least one `### Requirement:` subsection.\n\nThe standard schema ships validations for `spec.md` and `verify.md` out of the box, so malformed artifacts fail before they can be archived.\n\nRules can also enforce **cardinality** (exactly N matches, min/max bounds) and **uniqueness** (no duplicate keys among selected nodes) via the `count` field.\n\nFor the full validation rule syntax and selector system, see [selectors.md](selectors.md).\n\n## Cross-artifact validation\n\nSchemas can also declare **relational** rules that compare multiple artifacts of the same spec. For example, that requirement IDs in `spec.md` and `verify.md` are always mirrored, or that every spec requirement is covered by a YAML model artifact.\n\nThese rules are declared in the top-level `crossArtifactValidations` array and are evaluated only after all participating artifacts pass their local structural validation.\n\nFor the full syntax, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n\n## Metadata extraction\n\nSchemas also declare how to extract structured metadata from artifact content — titles, descriptions, requirements, scenarios, and dependency references. This extracted metadata is what `specd context` uses to compile the structured instruction block for the agent.\n\nFor the full extractor syntax, see [selectors.md](selectors.md).\n\n## Where to go next\n\n- [standard-schema.md](standard-schema.md) — complete reference for `@specd/schema-std` artifacts, DAG, and gates\n- [custom-schemas.md](custom-schemas.md) — authoring custom schemas, `schemaOverrides`, plugins, and forks\n- [selectors.md](selectors.md) — deep dive into selectors, extractors, and validation rules\n- [workflow.md](workflow.md) — lifecycle states, transitions, and approval gates\n- [docs/schemas/schema-format.md](../schemas/schema-format.md) — complete technical reference for the schema YAML format\n- [configuration.md](configuration.md) — `specd.yaml` reference, including `schemaOverrides` and `schemaPlugins`\n",
    "lineCount": 319,
    "byteLength": 17035,
    "outline": [
      {
        "index": 1,
        "heading": "Schemas",
        "level": 1,
        "startLine": 1,
        "endLine": 319,
        "lines": 319,
        "content": "# Schemas\n\nA schema defines the shape of your development workflow. It declares which artifact files exist, what order they are produced in, what rules govern them, and which lifecycle phases gate progression from design to archive.\n\nEvery SpecD project has exactly one active schema. You declare it in `specd.yaml` via the `schema` field:\n\n```yaml\nschema: '@specd/schema-std'\n```\n\nSpecD ships a standard schema — `@specd/schema-std` — that works for most projects. This guide explains what that schema does and how to adapt it to your needs.\n\n## The standard schema\n\n`@specd/schema-std` encodes the workflow: **proposal → specs → verify → design → tasks**. Five artifact types, a dependency graph between them, and a lifecycle that takes a change from idea to archived spec.\n\n### Artifacts\n\n#### 1. proposal (scope: change)\n\nThe proposal answers _why_ this change is needed. It is the entry point for every change — no other artifact can be written until a proposal exists.\n\nA proposal covers motivation, current behaviour, proposed solution, the list of specs that will be created or modified, affected code areas, and open questions. The \"Specs affected\" section is the contract that drives everything downstream: every spec listed there must eventually be covered by a `spec.md` file or a delta.\n\nProposals are working documents. They live in the change directory and are not archived into the spec repository — they describe intent, not permanent requirements.\n\n#### 2. specs (scope: spec)\n\nThe spec artifact (`spec.md`) defines _what_ the system should do. One spec file per capability. Specs use SHALL / MUST language, group requirements under `### Requirement: <name>` headings, and explicitly avoid WHEN/THEN scenarios (those belong in `verify.md`).\n\nUnlike `proposal.md`, specs are permanent. They are synced into the `specs/` directory when the change is archived and remain there as the authoritative record of requirements.\n\nSpecs also support delta files: instead of rewriting an existing spec, the agent produces a `.delta.yaml` that expresses the changes as structured operations (add, modify, remove a requirement). SpecD applies deltas deterministically at archive time.\n\nRequires: `proposal`\n\n#### 3. verify (scope: spec)\n\nThe verify artifact (`verify.md`) defines _how to confirm_ the spec is satisfied. It groups GIVEN/WHEN/THEN scenarios under `### Requirement: <name>` headings that mirror `spec.md` exactly. Each scenario describes a concrete check, not a restatement of the requirement.\n\nLike `spec.md`, verify files are permanent and synced to the spec directory at archive time.\n\nRequires: `specs`\n\n#### 4. design (scope: change)\n\nThe design document explains _how to implement_ the change. It analyses the codebase, lists affected files and symbols, defines new constructs with full signatures, documents the implementation approach, and maps every spec requirement and verify scenario to a concrete implementation path.\n\nDesign is a working document — it belongs to the change, not the permanent spec record. It is not optional, but its depth scales with the nature of the change: a large code refactor warrants detailed interface signatures; a documentation change warrants a content analysis.\n\nRequires: `proposal`, `specs`, `verify` — the agent reads all three before writing the design.\n\n#### 5. tasks (scope: change)\n\nTasks is the implementation checklist. Each task is a markdown checkbox (`- [ ]`) with indented context: which file, which symbol, what to change, and the specific approach from `design.md`. The agent works through tasks one by one and marks each complete (`- [x]`) as it goes.\n\nSpecD uses task completion to gate the transition from `implementing` to `verifying` — the transition is blocked while any `- [ ]` items remain. Checking off tasks does not change the artifact's hash (pre-hash cleanup normalises checkboxes), so progress tracking does not trigger re-validation.\n\nRequires: `specs`, `design` — the agent derives tasks from design.md and maps them back to spec requirements.\n\n### Dependency graph\n\n```\nproposal\n  ↓\nspecs\n  ↓\nverify\n  ↓\ndesign   (requires: proposal, specs, verify)\n  ↓\ntasks    (requires: specs, design)\n```\n\nThe graph is acyclic and enforced by SpecD. An artifact's status is `pending` until all of its `requires` are `complete` (or `skipped`, for optional ones). You cannot start writing `verify.md` before `spec.md` is complete; you cannot start design before verify is done; you cannot start tasks before design is done.\n\n## Artifact scope explained\n\nEvery artifact is either `scope: spec` or `scope: change`. This is the single most important property for understanding what survives a change.\n\n**`scope: spec`** — the artifact is a permanent part of the spec record. When a change is archived, SpecD syncs every `scope: spec` artifact from the change directory into the `specs/` repository. The `specd validate` command checks that every spec directory contains all required `scope: spec` artifacts. In the standard schema, `spec.md` and `verify.md` are `scope: spec`.\n\n**`scope: change`** — the artifact is a working document. It is validated during the change but never synced to the spec directory. `proposal.md`, `design.md`, and `tasks.md` are `scope: change` — they capture thinking and process, not permanent requirements.\n\nAt archive time the picture is:\n\n- The change's `specs/` subdirectory (new `spec.md` and `verify.md` files, or delta files) is applied to the spec repository.\n- `proposal.md`, `design.md`, and `tasks.md` remain in the change's archive directory as a record of the work, but they do not become part of the living spec.\n\n## Templates\n\nEach artifact type in `@specd/schema-std` ships with a template — a plain Markdown file used as scaffolding when the agent creates the artifact for the first time.\n\nTemplates are plain text. SpecD performs no variable substitution on them. HTML comments (`<!-- ... -->`) are preserved as-is and serve as guidance hints for the AI agent, pointing it toward intent, format requirements, and common pitfalls.\n\nHere is the shape of each template:\n\n**proposal.md** — opens with an HTML comment reminding the agent to focus on _why_, then provides sections for Motivation, Current behaviour, Proposed solution, Specs affected (split into New specs and Modified specs), Impact, and Open questions.\n\n**spec.md** — opens with an HTML comment on the SHALL/MUST convention and the prohibition on WHEN/THEN, then provides a Purpose section, a Requirements section with one placeholder `### Requirement:` heading, a Constraints section, and a Spec Dependencies section.\n\n**verify.md** — opens with a comment on the GIVEN/WHEN/THEN format and when to use AND/OR, then provides a structure with a `### Requirement:` heading and a `#### Scenario:` block with placeholders for GIVEN, WHEN, and THEN.\n\n**design.md** — opens with a comment on the required depth and concrete specificity, then provides sections for Non-goals, Affected areas, New constructs, Approach, Key decisions, Trade-offs, Migration/Rollback, Testing, and Open questions.\n\n**tasks.md** — opens with a comment on deriving tasks from design.md and including the approach inline, then provides two numbered task group sections with checkbox placeholders.\n\nTemplates can be replaced or extended when you extend or fork the schema. See [Customising your schema](#customising-your-schema) below.\n\n## Artifact rules\n\nEvery artifact can carry `instruction` text and `rules` blocks. These are injected into the AI context when the agent is working on that artifact.\n\n`instruction` is the main prompt for that artifact — what to write, how to structure it, and what to watch out for.\n\n`rules` are pre- and post-instruction blocks injected around the instruction:\n\n```yaml\n- id: tasks\n  scope: change\n  output: tasks.md\n  instruction: |\n    Create the implementation checklist...\n  rules:\n    pre:\n      - id: read-design-first\n        instruction: 'Read design.md in full before creating any tasks.'\n    post:\n      - id: design-tasks-coverage\n        instruction: |\n          Cross-reference design.md against the tasks just written.\n          Every item in Affected areas must map to at least one task.\n```\n\nIn the standard schema, every artifact has a post-rule that cross-references the previous artifact for coverage: proposals check open questions are resolved before proceeding; specs check every listed spec has a file; verify checks every requirement has a scenario; and so on.\n\n`requires` and `optional` control the dependency graph described above. A non-optional artifact cannot hard-depend on an optional one — that would make the non-optional artifact effectively optional too.\n\n## Workflow steps\n\nThe schema also defines the lifecycle phases a change moves through. In `@specd/schema-std`:\n\n| Step           | Requires (artifacts)                   |\n| -------------- | -------------------------------------- |\n| `designing`    | _(none — always available)_            |\n| `ready`        | proposal, specs, verify, design, tasks |\n| `implementing` | proposal, specs, verify, design, tasks |\n| `verifying`    | verify, tasks                          |\n| `archiving`    | specs, tasks                           |\n\nEach step is independently gated by its own `requires`. A step becomes available once all listed artifacts are complete.\n\nSteps can have `pre` and `post` hooks. Hooks are either instructions injected into the AI context or shell commands run at the phase boundary:\n\n```yaml\n- step: implementing\n  requires: [proposal, specs, verify, design, tasks]\n  hooks:\n    pre:\n      - id: implementing-guidance\n        instruction: |\n          Read all change artifacts before starting...\n    post:\n      - id: run-tests\n        run: 'pnpm test'\n```\n\n**Pre-hook failure** — if a `run:` hook exits non-zero, the step is aborted. The agent should fix the problem and retry.\n\n**Post-hook failure** — the step has already completed and is not rolled back. The user is prompted to continue with the remaining hooks or stop.\n\nShell hooks support template variables:\n\n| Variable           | Value                                                  |\n| ------------------ | ------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name                                 |\n| `{{change.path}}`  | Absolute path to the change directory                  |\n| `{{project.root}}` | Absolute path to the directory containing `specd.yaml` |\n\n## Customising your schema\n\nThere are three ways to customise schema behaviour, ranging from a single-line config change to a complete rewrite.\n\n### 1. schemaOverrides in specd.yaml\n\n`schemaOverrides` is the lightest-weight option. It modifies the active schema at load time without touching any file on disk. Use it for project-specific tweaks: an extra hook, an additional rule, a new artifact type that only this project needs.\n\nIt supports five operations:\n\n| Operation | Effect                                                                                   |\n| --------- | ---------------------------------------------------------------------------------------- |\n| `create`  | Adds a new entry (artifact, workflow step). Fails if the ID already exists.              |\n| `append`  | Appends entries to arrays (rules, hooks, metadataExtraction entries).                    |\n| `prepend` | Prepends entries to arrays.                                                              |\n| `set`     | Replaces scalar fields or whole array entries by identity.                               |\n| `remove`  | Removes entries from arrays by identity (`id` for artifacts, `step` for workflow steps). |\n\n**Example: add a post-implementing hook that runs your test suite**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n```\n\n**Example: add a rule to an existing artifact**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-compliance\n              text: >-\n                Cross-reference this design against the global compliance spec before proceeding.\n```\n\n**Example: add a new artifact type (an ADR)**\n\n```yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: Architecture Decision Record\n        requires: [specs]\n```\n\n`schemaOverrides` is the right tool for:\n\n- Adding or removing hooks on existing workflow steps\n- Appending rules or instructions to existing artifacts\n- Adding a new artifact type that is unique to your project\n\n### 2. Extend — specd schema extend\n\n`specd schema extend` creates a new schema package that inherits from a parent and adds to it. The parent schema stays intact; the child schema describes only the delta. This is the right approach when you want to share a schema across multiple projects but each project needs additions.\n\n```sh\nspecd schema extend @specd/schema-std my-workflow\n```\n\nThe generated schema declares its parent and adds or modifies entries. Any artifact or workflow step from the parent is inherited as-is; you only declare what changes.\n\nUse extend when:\n\n- You want to add a new artifact type that multiple projects will share\n- You want to publish the customisation as a reusable plugin\n- The base schema stays structurally intact — you are adding, not restructuring\n\n### 3. Fork — specd schema fork\n\n`specd schema fork` makes a complete copy of a schema. You own every field. Use it when you need structural changes incompatible with the parent — different artifact ordering, renamed lifecycle steps, or a workflow that looks nothing like the original.\n\n```sh\nspecd schema fork @specd/schema-std custom-workflow\n```\n\nUse fork when:\n\n- You want to completely redesign the artifact dependency graph\n- You need to rename lifecycle steps\n- The base schema's structure would need to be dismantled more than extended\n\n### Choosing the right approach\n\n| Situation                                      | Approach          |\n| ---------------------------------------------- | ----------------- |\n| Add a hook after `implementing`                | `schemaOverrides` |\n| Add a rule to the `design` artifact            | `schemaOverrides` |\n| Add a new artifact type for this project only  | `schemaOverrides` |\n| Add a new artifact type shared across projects | extend            |\n| Publish schema additions as a reusable plugin  | extend            |\n| Rename lifecycle steps                         | fork              |\n| Restructure artifact dependencies from scratch | fork              |\n\n## Validation rules\n\nSchemas can declare structural validation rules for artifact content. Validations check that an artifact's AST matches the declared structure — for example, that `spec.md` always has a `## Requirements` section with at least one `### Requirement:` subsection.\n\nThe standard schema ships validations for `spec.md` and `verify.md` out of the box, so malformed artifacts fail before they can be archived.\n\nRules can also enforce **cardinality** (exactly N matches, min/max bounds) and **uniqueness** (no duplicate keys among selected nodes) via the `count` field.\n\nFor the full validation rule syntax and selector system, see [selectors.md](selectors.md).\n\n## Cross-artifact validation\n\nSchemas can also declare **relational** rules that compare multiple artifacts of the same spec. For example, that requirement IDs in `spec.md` and `verify.md` are always mirrored, or that every spec requirement is covered by a YAML model artifact.\n\nThese rules are declared in the top-level `crossArtifactValidations` array and are evaluated only after all participating artifacts pass their local structural validation.\n\nFor the full syntax, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n\n## Metadata extraction\n\nSchemas also declare how to extract structured metadata from artifact content — titles, descriptions, requirements, scenarios, and dependency references. This extracted metadata is what `specd context` uses to compile the structured instruction block for the agent.\n\nFor the full extractor syntax, see [selectors.md](selectors.md).\n\n## Where to go next\n\n- [standard-schema.md](standard-schema.md) — complete reference for `@specd/schema-std` artifacts, DAG, and gates\n- [custom-schemas.md](custom-schemas.md) — authoring custom schemas, `schemaOverrides`, plugins, and forks\n- [selectors.md](selectors.md) — deep dive into selectors, extractors, and validation rules\n- [workflow.md](workflow.md) — lifecycle states, transitions, and approval gates\n- [docs/schemas/schema-format.md](../schemas/schema-format.md) — complete technical reference for the schema YAML format\n- [configuration.md](configuration.md) — `specd.yaml` reference, including `schemaOverrides` and `schemaPlugins`\n"
      },
      {
        "index": 2,
        "heading": "The standard schema",
        "level": 2,
        "startLine": 13,
        "endLine": 76,
        "lines": 64,
        "content": "## The standard schema\n\n`@specd/schema-std` encodes the workflow: **proposal → specs → verify → design → tasks**. Five artifact types, a dependency graph between them, and a lifecycle that takes a change from idea to archived spec.\n\n### Artifacts\n\n#### 1. proposal (scope: change)\n\nThe proposal answers _why_ this change is needed. It is the entry point for every change — no other artifact can be written until a proposal exists.\n\nA proposal covers motivation, current behaviour, proposed solution, the list of specs that will be created or modified, affected code areas, and open questions. The \"Specs affected\" section is the contract that drives everything downstream: every spec listed there must eventually be covered by a `spec.md` file or a delta.\n\nProposals are working documents. They live in the change directory and are not archived into the spec repository — they describe intent, not permanent requirements.\n\n#### 2. specs (scope: spec)\n\nThe spec artifact (`spec.md`) defines _what_ the system should do. One spec file per capability. Specs use SHALL / MUST language, group requirements under `### Requirement: <name>` headings, and explicitly avoid WHEN/THEN scenarios (those belong in `verify.md`).\n\nUnlike `proposal.md`, specs are permanent. They are synced into the `specs/` directory when the change is archived and remain there as the authoritative record of requirements.\n\nSpecs also support delta files: instead of rewriting an existing spec, the agent produces a `.delta.yaml` that expresses the changes as structured operations (add, modify, remove a requirement). SpecD applies deltas deterministically at archive time.\n\nRequires: `proposal`\n\n#### 3. verify (scope: spec)\n\nThe verify artifact (`verify.md`) defines _how to confirm_ the spec is satisfied. It groups GIVEN/WHEN/THEN scenarios under `### Requirement: <name>` headings that mirror `spec.md` exactly. Each scenario describes a concrete check, not a restatement of the requirement.\n\nLike `spec.md`, verify files are permanent and synced to the spec directory at archive time.\n\nRequires: `specs`\n\n#### 4. design (scope: change)\n\nThe design document explains _how to implement_ the change. It analyses the codebase, lists affected files and symbols, defines new constructs with full signatures, documents the implementation approach, and maps every spec requirement and verify scenario to a concrete implementation path.\n\nDesign is a working document — it belongs to the change, not the permanent spec record. It is not optional, but its depth scales with the nature of the change: a large code refactor warrants detailed interface signatures; a documentation change warrants a content analysis.\n\nRequires: `proposal`, `specs`, `verify` — the agent reads all three before writing the design.\n\n#### 5. tasks (scope: change)\n\nTasks is the implementation checklist. Each task is a markdown checkbox (`- [ ]`) with indented context: which file, which symbol, what to change, and the specific approach from `design.md`. The agent works through tasks one by one and marks each complete (`- [x]`) as it goes.\n\nSpecD uses task completion to gate the transition from `implementing` to `verifying` — the transition is blocked while any `- [ ]` items remain. Checking off tasks does not change the artifact's hash (pre-hash cleanup normalises checkboxes), so progress tracking does not trigger re-validation.\n\nRequires: `specs`, `design` — the agent derives tasks from design.md and maps them back to spec requirements.\n\n### Dependency graph\n\n```\nproposal\n  ↓\nspecs\n  ↓\nverify\n  ↓\ndesign   (requires: proposal, specs, verify)\n  ↓\ntasks    (requires: specs, design)\n```\n\nThe graph is acyclic and enforced by SpecD. An artifact's status is `pending` until all of its `requires` are `complete` (or `skipped`, for optional ones). You cannot start writing `verify.md` before `spec.md` is complete; you cannot start design before verify is done; you cannot start tasks before design is done.\n"
      },
      {
        "index": 3,
        "heading": "Artifacts",
        "level": 3,
        "startLine": 17,
        "endLine": 60,
        "lines": 44,
        "content": "### Artifacts\n\n#### 1. proposal (scope: change)\n\nThe proposal answers _why_ this change is needed. It is the entry point for every change — no other artifact can be written until a proposal exists.\n\nA proposal covers motivation, current behaviour, proposed solution, the list of specs that will be created or modified, affected code areas, and open questions. The \"Specs affected\" section is the contract that drives everything downstream: every spec listed there must eventually be covered by a `spec.md` file or a delta.\n\nProposals are working documents. They live in the change directory and are not archived into the spec repository — they describe intent, not permanent requirements.\n\n#### 2. specs (scope: spec)\n\nThe spec artifact (`spec.md`) defines _what_ the system should do. One spec file per capability. Specs use SHALL / MUST language, group requirements under `### Requirement: <name>` headings, and explicitly avoid WHEN/THEN scenarios (those belong in `verify.md`).\n\nUnlike `proposal.md`, specs are permanent. They are synced into the `specs/` directory when the change is archived and remain there as the authoritative record of requirements.\n\nSpecs also support delta files: instead of rewriting an existing spec, the agent produces a `.delta.yaml` that expresses the changes as structured operations (add, modify, remove a requirement). SpecD applies deltas deterministically at archive time.\n\nRequires: `proposal`\n\n#### 3. verify (scope: spec)\n\nThe verify artifact (`verify.md`) defines _how to confirm_ the spec is satisfied. It groups GIVEN/WHEN/THEN scenarios under `### Requirement: <name>` headings that mirror `spec.md` exactly. Each scenario describes a concrete check, not a restatement of the requirement.\n\nLike `spec.md`, verify files are permanent and synced to the spec directory at archive time.\n\nRequires: `specs`\n\n#### 4. design (scope: change)\n\nThe design document explains _how to implement_ the change. It analyses the codebase, lists affected files and symbols, defines new constructs with full signatures, documents the implementation approach, and maps every spec requirement and verify scenario to a concrete implementation path.\n\nDesign is a working document — it belongs to the change, not the permanent spec record. It is not optional, but its depth scales with the nature of the change: a large code refactor warrants detailed interface signatures; a documentation change warrants a content analysis.\n\nRequires: `proposal`, `specs`, `verify` — the agent reads all three before writing the design.\n\n#### 5. tasks (scope: change)\n\nTasks is the implementation checklist. Each task is a markdown checkbox (`- [ ]`) with indented context: which file, which symbol, what to change, and the specific approach from `design.md`. The agent works through tasks one by one and marks each complete (`- [x]`) as it goes.\n\nSpecD uses task completion to gate the transition from `implementing` to `verifying` — the transition is blocked while any `- [ ]` items remain. Checking off tasks does not change the artifact's hash (pre-hash cleanup normalises checkboxes), so progress tracking does not trigger re-validation.\n\nRequires: `specs`, `design` — the agent derives tasks from design.md and maps them back to spec requirements.\n"
      },
      {
        "index": 4,
        "heading": "1. proposal (scope: change)",
        "level": 4,
        "startLine": 19,
        "endLine": 26,
        "lines": 8,
        "content": "#### 1. proposal (scope: change)\n\nThe proposal answers _why_ this change is needed. It is the entry point for every change — no other artifact can be written until a proposal exists.\n\nA proposal covers motivation, current behaviour, proposed solution, the list of specs that will be created or modified, affected code areas, and open questions. The \"Specs affected\" section is the contract that drives everything downstream: every spec listed there must eventually be covered by a `spec.md` file or a delta.\n\nProposals are working documents. They live in the change directory and are not archived into the spec repository — they describe intent, not permanent requirements.\n"
      },
      {
        "index": 5,
        "heading": "2. specs (scope: spec)",
        "level": 4,
        "startLine": 27,
        "endLine": 36,
        "lines": 10,
        "content": "#### 2. specs (scope: spec)\n\nThe spec artifact (`spec.md`) defines _what_ the system should do. One spec file per capability. Specs use SHALL / MUST language, group requirements under `### Requirement: <name>` headings, and explicitly avoid WHEN/THEN scenarios (those belong in `verify.md`).\n\nUnlike `proposal.md`, specs are permanent. They are synced into the `specs/` directory when the change is archived and remain there as the authoritative record of requirements.\n\nSpecs also support delta files: instead of rewriting an existing spec, the agent produces a `.delta.yaml` that expresses the changes as structured operations (add, modify, remove a requirement). SpecD applies deltas deterministically at archive time.\n\nRequires: `proposal`\n"
      },
      {
        "index": 6,
        "heading": "3. verify (scope: spec)",
        "level": 4,
        "startLine": 37,
        "endLine": 44,
        "lines": 8,
        "content": "#### 3. verify (scope: spec)\n\nThe verify artifact (`verify.md`) defines _how to confirm_ the spec is satisfied. It groups GIVEN/WHEN/THEN scenarios under `### Requirement: <name>` headings that mirror `spec.md` exactly. Each scenario describes a concrete check, not a restatement of the requirement.\n\nLike `spec.md`, verify files are permanent and synced to the spec directory at archive time.\n\nRequires: `specs`\n"
      },
      {
        "index": 7,
        "heading": "4. design (scope: change)",
        "level": 4,
        "startLine": 45,
        "endLine": 52,
        "lines": 8,
        "content": "#### 4. design (scope: change)\n\nThe design document explains _how to implement_ the change. It analyses the codebase, lists affected files and symbols, defines new constructs with full signatures, documents the implementation approach, and maps every spec requirement and verify scenario to a concrete implementation path.\n\nDesign is a working document — it belongs to the change, not the permanent spec record. It is not optional, but its depth scales with the nature of the change: a large code refactor warrants detailed interface signatures; a documentation change warrants a content analysis.\n\nRequires: `proposal`, `specs`, `verify` — the agent reads all three before writing the design.\n"
      },
      {
        "index": 8,
        "heading": "5. tasks (scope: change)",
        "level": 4,
        "startLine": 53,
        "endLine": 60,
        "lines": 8,
        "content": "#### 5. tasks (scope: change)\n\nTasks is the implementation checklist. Each task is a markdown checkbox (`- [ ]`) with indented context: which file, which symbol, what to change, and the specific approach from `design.md`. The agent works through tasks one by one and marks each complete (`- [x]`) as it goes.\n\nSpecD uses task completion to gate the transition from `implementing` to `verifying` — the transition is blocked while any `- [ ]` items remain. Checking off tasks does not change the artifact's hash (pre-hash cleanup normalises checkboxes), so progress tracking does not trigger re-validation.\n\nRequires: `specs`, `design` — the agent derives tasks from design.md and maps them back to spec requirements.\n"
      },
      {
        "index": 9,
        "heading": "Dependency graph",
        "level": 3,
        "startLine": 61,
        "endLine": 76,
        "lines": 16,
        "content": "### Dependency graph\n\n```\nproposal\n  ↓\nspecs\n  ↓\nverify\n  ↓\ndesign   (requires: proposal, specs, verify)\n  ↓\ntasks    (requires: specs, design)\n```\n\nThe graph is acyclic and enforced by SpecD. An artifact's status is `pending` until all of its `requires` are `complete` (or `skipped`, for optional ones). You cannot start writing `verify.md` before `spec.md` is complete; you cannot start design before verify is done; you cannot start tasks before design is done.\n"
      },
      {
        "index": 10,
        "heading": "Artifact scope explained",
        "level": 2,
        "startLine": 77,
        "endLine": 89,
        "lines": 13,
        "content": "## Artifact scope explained\n\nEvery artifact is either `scope: spec` or `scope: change`. This is the single most important property for understanding what survives a change.\n\n**`scope: spec`** — the artifact is a permanent part of the spec record. When a change is archived, SpecD syncs every `scope: spec` artifact from the change directory into the `specs/` repository. The `specd validate` command checks that every spec directory contains all required `scope: spec` artifacts. In the standard schema, `spec.md` and `verify.md` are `scope: spec`.\n\n**`scope: change`** — the artifact is a working document. It is validated during the change but never synced to the spec directory. `proposal.md`, `design.md`, and `tasks.md` are `scope: change` — they capture thinking and process, not permanent requirements.\n\nAt archive time the picture is:\n\n- The change's `specs/` subdirectory (new `spec.md` and `verify.md` files, or delta files) is applied to the spec repository.\n- `proposal.md`, `design.md`, and `tasks.md` remain in the change's archive directory as a record of the work, but they do not become part of the living spec.\n"
      },
      {
        "index": 11,
        "heading": "Templates",
        "level": 2,
        "startLine": 90,
        "endLine": 109,
        "lines": 20,
        "content": "## Templates\n\nEach artifact type in `@specd/schema-std` ships with a template — a plain Markdown file used as scaffolding when the agent creates the artifact for the first time.\n\nTemplates are plain text. SpecD performs no variable substitution on them. HTML comments (`<!-- ... -->`) are preserved as-is and serve as guidance hints for the AI agent, pointing it toward intent, format requirements, and common pitfalls.\n\nHere is the shape of each template:\n\n**proposal.md** — opens with an HTML comment reminding the agent to focus on _why_, then provides sections for Motivation, Current behaviour, Proposed solution, Specs affected (split into New specs and Modified specs), Impact, and Open questions.\n\n**spec.md** — opens with an HTML comment on the SHALL/MUST convention and the prohibition on WHEN/THEN, then provides a Purpose section, a Requirements section with one placeholder `### Requirement:` heading, a Constraints section, and a Spec Dependencies section.\n\n**verify.md** — opens with a comment on the GIVEN/WHEN/THEN format and when to use AND/OR, then provides a structure with a `### Requirement:` heading and a `#### Scenario:` block with placeholders for GIVEN, WHEN, and THEN.\n\n**design.md** — opens with a comment on the required depth and concrete specificity, then provides sections for Non-goals, Affected areas, New constructs, Approach, Key decisions, Trade-offs, Migration/Rollback, Testing, and Open questions.\n\n**tasks.md** — opens with a comment on deriving tasks from design.md and including the approach inline, then provides two numbered task group sections with checkbox placeholders.\n\nTemplates can be replaced or extended when you extend or fork the schema. See [Customising your schema](#customising-your-schema) below.\n"
      },
      {
        "index": 12,
        "heading": "Artifact rules",
        "level": 2,
        "startLine": 110,
        "endLine": 138,
        "lines": 29,
        "content": "## Artifact rules\n\nEvery artifact can carry `instruction` text and `rules` blocks. These are injected into the AI context when the agent is working on that artifact.\n\n`instruction` is the main prompt for that artifact — what to write, how to structure it, and what to watch out for.\n\n`rules` are pre- and post-instruction blocks injected around the instruction:\n\n```yaml\n- id: tasks\n  scope: change\n  output: tasks.md\n  instruction: |\n    Create the implementation checklist...\n  rules:\n    pre:\n      - id: read-design-first\n        instruction: 'Read design.md in full before creating any tasks.'\n    post:\n      - id: design-tasks-coverage\n        instruction: |\n          Cross-reference design.md against the tasks just written.\n          Every item in Affected areas must map to at least one task.\n```\n\nIn the standard schema, every artifact has a post-rule that cross-references the previous artifact for coverage: proposals check open questions are resolved before proceeding; specs check every listed spec has a file; verify checks every requirement has a scenario; and so on.\n\n`requires` and `optional` control the dependency graph described above. A non-optional artifact cannot hard-depend on an optional one — that would make the non-optional artifact effectively optional too.\n"
      },
      {
        "index": 13,
        "heading": "Workflow steps",
        "level": 2,
        "startLine": 139,
        "endLine": 179,
        "lines": 41,
        "content": "## Workflow steps\n\nThe schema also defines the lifecycle phases a change moves through. In `@specd/schema-std`:\n\n| Step           | Requires (artifacts)                   |\n| -------------- | -------------------------------------- |\n| `designing`    | _(none — always available)_            |\n| `ready`        | proposal, specs, verify, design, tasks |\n| `implementing` | proposal, specs, verify, design, tasks |\n| `verifying`    | verify, tasks                          |\n| `archiving`    | specs, tasks                           |\n\nEach step is independently gated by its own `requires`. A step becomes available once all listed artifacts are complete.\n\nSteps can have `pre` and `post` hooks. Hooks are either instructions injected into the AI context or shell commands run at the phase boundary:\n\n```yaml\n- step: implementing\n  requires: [proposal, specs, verify, design, tasks]\n  hooks:\n    pre:\n      - id: implementing-guidance\n        instruction: |\n          Read all change artifacts before starting...\n    post:\n      - id: run-tests\n        run: 'pnpm test'\n```\n\n**Pre-hook failure** — if a `run:` hook exits non-zero, the step is aborted. The agent should fix the problem and retry.\n\n**Post-hook failure** — the step has already completed and is not rolled back. The user is prompted to continue with the remaining hooks or stop.\n\nShell hooks support template variables:\n\n| Variable           | Value                                                  |\n| ------------------ | ------------------------------------------------------ |\n| `{{change.name}}`  | The change's slug name                                 |\n| `{{change.path}}`  | Absolute path to the change directory                  |\n| `{{project.root}}` | Absolute path to the directory containing `specd.yaml` |\n"
      },
      {
        "index": 14,
        "heading": "Customising your schema",
        "level": 2,
        "startLine": 180,
        "endLine": 286,
        "lines": 107,
        "content": "## Customising your schema\n\nThere are three ways to customise schema behaviour, ranging from a single-line config change to a complete rewrite.\n\n### 1. schemaOverrides in specd.yaml\n\n`schemaOverrides` is the lightest-weight option. It modifies the active schema at load time without touching any file on disk. Use it for project-specific tweaks: an extra hook, an additional rule, a new artifact type that only this project needs.\n\nIt supports five operations:\n\n| Operation | Effect                                                                                   |\n| --------- | ---------------------------------------------------------------------------------------- |\n| `create`  | Adds a new entry (artifact, workflow step). Fails if the ID already exists.              |\n| `append`  | Appends entries to arrays (rules, hooks, metadataExtraction entries).                    |\n| `prepend` | Prepends entries to arrays.                                                              |\n| `set`     | Replaces scalar fields or whole array entries by identity.                               |\n| `remove`  | Removes entries from arrays by identity (`id` for artifacts, `step` for workflow steps). |\n\n**Example: add a post-implementing hook that runs your test suite**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n```\n\n**Example: add a rule to an existing artifact**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-compliance\n              text: >-\n                Cross-reference this design against the global compliance spec before proceeding.\n```\n\n**Example: add a new artifact type (an ADR)**\n\n```yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: Architecture Decision Record\n        requires: [specs]\n```\n\n`schemaOverrides` is the right tool for:\n\n- Adding or removing hooks on existing workflow steps\n- Appending rules or instructions to existing artifacts\n- Adding a new artifact type that is unique to your project\n\n### 2. Extend — specd schema extend\n\n`specd schema extend` creates a new schema package that inherits from a parent and adds to it. The parent schema stays intact; the child schema describes only the delta. This is the right approach when you want to share a schema across multiple projects but each project needs additions.\n\n```sh\nspecd schema extend @specd/schema-std my-workflow\n```\n\nThe generated schema declares its parent and adds or modifies entries. Any artifact or workflow step from the parent is inherited as-is; you only declare what changes.\n\nUse extend when:\n\n- You want to add a new artifact type that multiple projects will share\n- You want to publish the customisation as a reusable plugin\n- The base schema stays structurally intact — you are adding, not restructuring\n\n### 3. Fork — specd schema fork\n\n`specd schema fork` makes a complete copy of a schema. You own every field. Use it when you need structural changes incompatible with the parent — different artifact ordering, renamed lifecycle steps, or a workflow that looks nothing like the original.\n\n```sh\nspecd schema fork @specd/schema-std custom-workflow\n```\n\nUse fork when:\n\n- You want to completely redesign the artifact dependency graph\n- You need to rename lifecycle steps\n- The base schema's structure would need to be dismantled more than extended\n\n### Choosing the right approach\n\n| Situation                                      | Approach          |\n| ---------------------------------------------- | ----------------- |\n| Add a hook after `implementing`                | `schemaOverrides` |\n| Add a rule to the `design` artifact            | `schemaOverrides` |\n| Add a new artifact type for this project only  | `schemaOverrides` |\n| Add a new artifact type shared across projects | extend            |\n| Publish schema additions as a reusable plugin  | extend            |\n| Rename lifecycle steps                         | fork              |\n| Restructure artifact dependencies from scratch | fork              |\n"
      },
      {
        "index": 15,
        "heading": "1. schemaOverrides in specd.yaml",
        "level": 3,
        "startLine": 184,
        "endLine": 244,
        "lines": 61,
        "content": "### 1. schemaOverrides in specd.yaml\n\n`schemaOverrides` is the lightest-weight option. It modifies the active schema at load time without touching any file on disk. Use it for project-specific tweaks: an extra hook, an additional rule, a new artifact type that only this project needs.\n\nIt supports five operations:\n\n| Operation | Effect                                                                                   |\n| --------- | ---------------------------------------------------------------------------------------- |\n| `create`  | Adds a new entry (artifact, workflow step). Fails if the ID already exists.              |\n| `append`  | Appends entries to arrays (rules, hooks, metadataExtraction entries).                    |\n| `prepend` | Prepends entries to arrays.                                                              |\n| `set`     | Replaces scalar fields or whole array entries by identity.                               |\n| `remove`  | Removes entries from arrays by identity (`id` for artifacts, `step` for workflow steps). |\n\n**Example: add a post-implementing hook that runs your test suite**\n\n```yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-tests\n              run: 'pnpm test'\n```\n\n**Example: add a rule to an existing artifact**\n\n```yaml\nschemaOverrides:\n  append:\n    artifacts:\n      - id: design\n        rules:\n          post:\n            - id: check-compliance\n              text: >-\n                Cross-reference this design against the global compliance spec before proceeding.\n```\n\n**Example: add a new artifact type (an ADR)**\n\n```yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: Architecture Decision Record\n        requires: [specs]\n```\n\n`schemaOverrides` is the right tool for:\n\n- Adding or removing hooks on existing workflow steps\n- Appending rules or instructions to existing artifacts\n- Adding a new artifact type that is unique to your project\n"
      },
      {
        "index": 16,
        "heading": "2. Extend — specd schema extend",
        "level": 3,
        "startLine": 245,
        "endLine": 260,
        "lines": 16,
        "content": "### 2. Extend — specd schema extend\n\n`specd schema extend` creates a new schema package that inherits from a parent and adds to it. The parent schema stays intact; the child schema describes only the delta. This is the right approach when you want to share a schema across multiple projects but each project needs additions.\n\n```sh\nspecd schema extend @specd/schema-std my-workflow\n```\n\nThe generated schema declares its parent and adds or modifies entries. Any artifact or workflow step from the parent is inherited as-is; you only declare what changes.\n\nUse extend when:\n\n- You want to add a new artifact type that multiple projects will share\n- You want to publish the customisation as a reusable plugin\n- The base schema stays structurally intact — you are adding, not restructuring\n"
      },
      {
        "index": 17,
        "heading": "3. Fork — specd schema fork",
        "level": 3,
        "startLine": 261,
        "endLine": 274,
        "lines": 14,
        "content": "### 3. Fork — specd schema fork\n\n`specd schema fork` makes a complete copy of a schema. You own every field. Use it when you need structural changes incompatible with the parent — different artifact ordering, renamed lifecycle steps, or a workflow that looks nothing like the original.\n\n```sh\nspecd schema fork @specd/schema-std custom-workflow\n```\n\nUse fork when:\n\n- You want to completely redesign the artifact dependency graph\n- You need to rename lifecycle steps\n- The base schema's structure would need to be dismantled more than extended\n"
      },
      {
        "index": 18,
        "heading": "Choosing the right approach",
        "level": 3,
        "startLine": 275,
        "endLine": 286,
        "lines": 12,
        "content": "### Choosing the right approach\n\n| Situation                                      | Approach          |\n| ---------------------------------------------- | ----------------- |\n| Add a hook after `implementing`                | `schemaOverrides` |\n| Add a rule to the `design` artifact            | `schemaOverrides` |\n| Add a new artifact type for this project only  | `schemaOverrides` |\n| Add a new artifact type shared across projects | extend            |\n| Publish schema additions as a reusable plugin  | extend            |\n| Rename lifecycle steps                         | fork              |\n| Restructure artifact dependencies from scratch | fork              |\n"
      },
      {
        "index": 19,
        "heading": "Validation rules",
        "level": 2,
        "startLine": 287,
        "endLine": 296,
        "lines": 10,
        "content": "## Validation rules\n\nSchemas can declare structural validation rules for artifact content. Validations check that an artifact's AST matches the declared structure — for example, that `spec.md` always has a `## Requirements` section with at least one `### Requirement:` subsection.\n\nThe standard schema ships validations for `spec.md` and `verify.md` out of the box, so malformed artifacts fail before they can be archived.\n\nRules can also enforce **cardinality** (exactly N matches, min/max bounds) and **uniqueness** (no duplicate keys among selected nodes) via the `count` field.\n\nFor the full validation rule syntax and selector system, see [selectors.md](selectors.md).\n"
      },
      {
        "index": 20,
        "heading": "Cross-artifact validation",
        "level": 2,
        "startLine": 297,
        "endLine": 304,
        "lines": 8,
        "content": "## Cross-artifact validation\n\nSchemas can also declare **relational** rules that compare multiple artifacts of the same spec. For example, that requirement IDs in `spec.md` and `verify.md` are always mirrored, or that every spec requirement is covered by a YAML model artifact.\n\nThese rules are declared in the top-level `crossArtifactValidations` array and are evaluated only after all participating artifacts pass their local structural validation.\n\nFor the full syntax, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n"
      },
      {
        "index": 21,
        "heading": "Metadata extraction",
        "level": 2,
        "startLine": 305,
        "endLine": 310,
        "lines": 6,
        "content": "## Metadata extraction\n\nSchemas also declare how to extract structured metadata from artifact content — titles, descriptions, requirements, scenarios, and dependency references. This extracted metadata is what `specd context` uses to compile the structured instruction block for the agent.\n\nFor the full extractor syntax, see [selectors.md](selectors.md).\n"
      },
      {
        "index": 22,
        "heading": "Where to go next",
        "level": 2,
        "startLine": 311,
        "endLine": 319,
        "lines": 9,
        "content": "## Where to go next\n\n- [standard-schema.md](standard-schema.md) — complete reference for `@specd/schema-std` artifacts, DAG, and gates\n- [custom-schemas.md](custom-schemas.md) — authoring custom schemas, `schemaOverrides`, plugins, and forks\n- [selectors.md](selectors.md) — deep dive into selectors, extractors, and validation rules\n- [workflow.md](workflow.md) — lifecycle states, transitions, and approval gates\n- [docs/schemas/schema-format.md](../schemas/schema-format.md) — complete technical reference for the schema YAML format\n- [configuration.md](configuration.md) — `specd.yaml` reference, including `schemaOverrides` and `schemaPlugins`\n"
      }
    ]
  },
  {
    "topic": "selectors",
    "title": "Selectors, Extractors, and Validation Rules",
    "description": "Targeting and validating structured document content in SpecD schemas.",
    "order": 7,
    "content": "# Selectors, Extractors, and Validation Rules\n\nSpecD schemas work with the content of your artifact files — not just their existence. To do that, schemas need a way to describe exactly which part of a file they care about: a heading, a list item, a YAML key, a specific sequence entry. That is what selectors do.\n\nSelectors, extractors, and validation rules are three layers that build on each other:\n\n- A **selector** identifies one or more nodes in a file's parsed structure.\n- An **extractor** wraps a selector with post-processing instructions: what text to pull out, how to clean it up, and optionally how to map it into structured objects.\n- A **validation rule** wraps a selector with a structural assertion: does this node exist? does its content match a pattern?\n\nAll three are used across schemas — in `validations`, `deltaValidations`, and `metadataExtraction`. Understanding selectors is the foundation for all of them.\n\n---\n\n## How artifacts are parsed\n\nWhen SpecD reads an artifact file, it parses it into a tree of typed nodes — an Abstract Syntax Tree (AST). Each node has a **type**, a **label** (the identifying text), and a **value** (the content).\n\nThe node types available depend on the file's format:\n\n| Format     | Node types                                                                              |\n| ---------- | --------------------------------------------------------------------------------------- |\n| Markdown   | `document`, `section`, `paragraph`, `list`, `list-item`, `code-block`, `thematic-break` |\n| JSON       | `document`, `object`, `property`, `array`, `array-item`                                 |\n| YAML       | `document`, `mapping`, `pair`, `sequence`, `sequence-item`                              |\n| Plain text | `document`, `paragraph`, `line`                                                         |\n\nThe **label** is what `matches` is tested against: the heading text for a Markdown `section`, the key name for a YAML `pair` or JSON `property`. The **value** is what `contains` is tested against: the paragraph text for a Markdown `paragraph`, the scalar value for a YAML `pair`.\n\n### Markdown example\n\nGiven this Markdown:\n\n```markdown\n## Requirements\n\n### Requirement: Authentication\n\nUsers must be able to log in with email and password.\n```\n\nThe AST contains:\n\n- A `section` node with `label: \"Requirements\"` at level 2\n  - A `section` node with `label: \"Requirement: Authentication\"` at level 3\n    - A `paragraph` node with `value: \"Users must be able to log in with email and password.\"`\n\n### YAML example\n\nGiven this YAML:\n\n```yaml\nworkflow:\n  - name: Run tests\n    command: pnpm test\n  - name: Deploy\n    command: ./deploy.sh\n```\n\nThe AST contains:\n\n- A `mapping` at document root\n  - A `pair` with `label: \"workflow\"`\n    - A `sequence` containing:\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Run tests\"`\n        - A `pair` with `label: \"command\"`, `value: \"pnpm test\"`\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Deploy\"`\n        - ...\n\n---\n\n## Selectors\n\nA selector is an object that describes the nodes to match. All selector fields are optional except `type`.\n\n### Selector fields\n\n| Field      | Type     | Description                                                                                                                                                                                           |\n| ---------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `type`     | string   | The node type to match. Required. Must be one of the type names for the target format (see table above).                                                                                              |\n| `matches`  | string   | Regex matched **case-insensitively** against the node's `label`. Plain strings match anywhere in the label — use anchors for exact matches.                                                           |\n| `contains` | string   | Regex matched **case-insensitively** against the node's `value`. Useful for finding leaf nodes by content rather than name.                                                                           |\n| `parent`   | selector | Constrains the search to nodes whose nearest ancestor matches this selector. Used to disambiguate nodes with the same label at different levels.                                                      |\n| `index`    | integer  | For `array-item` and `sequence-item` nodes: targets the item at this zero-based position. Mutually exclusive with `where`.                                                                            |\n| `where`    | object   | For `array-item` and `sequence-item` nodes that are objects: targets the item whose fields all match the given key–value pairs. Values are case-insensitive regexes. Mutually exclusive with `index`. |\n| `level`    | integer  | For Markdown `section` nodes only: matches sections at exactly this heading level (1 = `#`, 2 = `##`, etc.).                                                                                          |\n\n### Matching with `matches`\n\n`matches` is a case-insensitive regular expression tested against the node's label. Without anchors, it matches anywhere in the label text.\n\n| Pattern                        | What it matches                                                         |\n| ------------------------------ | ----------------------------------------------------------------------- |\n| `'Login'`                      | Any label containing `Login` (e.g. `Login`, `User Login`, `Login flow`) |\n| `'^Requirements$'`             | Exactly the string `Requirements`                                       |\n| `'^Requirement:'`              | Any label starting with `Requirement:`                                  |\n| `'_url$'`                      | Any label ending with `_url`                                            |\n| `'^Scenario: .+ \\(skipped\\)$'` | Labels like `Scenario: Login (skipped)`                                 |\n\n### Narrowing with `parent`\n\nWhen the same heading appears at multiple levels of a document, `parent` disambiguates. Without `parent`, all matching nodes are returned regardless of where they appear.\n\n```yaml\n# Matches every section whose label starts with \"Requirement:\"\n# anywhere in the document — including sections nested under other headings\nselector:\n  type: section\n  matches: '^Requirement:'\n\n# Matches only those sections nested directly under the Requirements section\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\n### Targeting by position with `index`\n\nFor ordered collections (`array-item`, `sequence-item`), `index` selects a specific item by its zero-based position:\n\n```yaml\n# The first item in a sequence\nselector:\n  type: sequence-item\n  index: 0\n```\n\n### Targeting by field values with `where`\n\nFor object-typed sequence or array items, `where` matches items whose named fields satisfy all conditions. Values are case-insensitive regexes:\n\n```yaml\n# The sequence item whose \"op\" field matches \"added\" or \"modified\"\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n\n# The sequence item whose \"name\" field is exactly \"Run tests\"\nselector:\n  type: sequence-item\n  where:\n    name: '^Run tests$'\n```\n\n### Targeting by heading level with `level`\n\nIn Markdown, `level` restricts a `section` selector to a specific heading depth:\n\n```yaml\n# Only the H1 heading (the document title)\nselector:\n  type: section\n  level: 1\n\n# Only H3 headings — not H2 or H4\nselector:\n  type: section\n  level: 3\n```\n\n---\n\n## Practical selector examples\n\nThese examples come directly from the standard schema (`schema-std`).\n\n### Selecting the document title (H1 heading)\n\n```yaml\nselector:\n  type: section\n  level: 1\n```\n\nMatches the first (and typically only) H1 heading in a Markdown spec file.\n\n### Selecting a section by exact heading name\n\n```yaml\nselector:\n  type: section\n  matches: '^Purpose$'\n```\n\nMatches a section whose heading is exactly `Purpose`. Case-insensitive, so `purpose` and `PURPOSE` also match.\n\n### Selecting all Requirement sections under Requirements\n\n```yaml\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\nMatches every `### Requirement: ...` heading that is a direct child of the `## Requirements` section. Without the `parent` constraint, this would also match any `Requirement:` heading that appears elsewhere in the document.\n\n### Selecting list items within a section\n\n```yaml\nselector:\n  type: list-item\n  parent:\n    type: section\n    matches: '^Constraints$'\n```\n\nMatches every bullet point under a `## Constraints` heading.\n\n### Selecting a YAML sequence item by field value\n\n```yaml\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n```\n\nMatches every sequence item (object) whose `op` field is `added` or `modified`. Used in `deltaValidations` to assert that modified delta entries meet structural requirements.\n\n---\n\n## Extractors: pulling data from the AST\n\nAn extractor pairs a selector with instructions for what to do with the matched nodes. Extractors are used in `metadataExtraction` to pull structured content out of artifact files.\n\nEvery extractor has a `selector` field plus a post-processing pipeline. The pipeline runs in this order:\n\n1. **Select** nodes using `selector`\n2. **Extract** text using `extract`\n3. **Strip** unwanted patterns using `strip`\n4. **Capture** a portion of the text using `capture`\n5. **Transform** the result using a named callback with `transform` (callbacks may be sync or async)\n6. **Group** results using `groupBy`\n\nOr, for complex structured data:\n\n- **Map to objects** using `fields`\n\n### Extract modes\n\nThe `extract` field controls what text is pulled from each matched node:\n\n| Value       | What it returns                                                                                                       |\n| ----------- | --------------------------------------------------------------------------------------------------------------------- |\n| `'content'` | The full rendered text of the node's subtree (default). For a Markdown section, this is everything below the heading. |\n| `'label'`   | Only the node's identifying text: the heading text, key name, or item label.                                          |\n| `'both'`    | The label followed by the content, separated.                                                                         |\n\n### Simple extraction: a section's content\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Purpose$'\n  extract: content\n```\n\nReturns the full text of everything inside the `Purpose` section — paragraphs, lists, everything below the heading.\n\n### Extracting just the label\n\n```yaml\nextractor:\n  selector:\n    type: section\n    level: 1\n  extract: label\n```\n\nReturns the heading text of the H1 section — the document title — without any of the section's content.\n\n### Stripping a prefix from labels\n\nWhen labels contain a structured prefix you want to remove:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  extract: label\n  strip: '^Requirement:\\s*'\n```\n\nEach matched label (`Requirement: Authentication`, `Requirement: Session expiry`) has the `Requirement: ` prefix removed, leaving `Authentication` and `Session expiry`.\n\n### Capturing a portion of the text\n\n`capture` is a regex with a capture group. Only the captured portion is kept:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Spec Dependencies$'\n  extract: content\n  capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n  transform:\n    name: resolveSpecPath\n    args: ['$2']\n```\n\nThis extracts dependency entries from the `Spec Dependencies` section, capturing the visible label as the primary `value` and the optional `href` as `$2`. `resolveSpecPath` first tries the label itself (for canonical entries like ``[`core:core/config`](../config/spec.md)`` or `` `core:core/config` ``), then falls back to the captured `href` when the label is legacy text. Relative candidates are resolved through repository-backed normalization (`resolveFromPath` + `crossWorkspaceHint` routing), so links like `../../_global/architecture/spec.md` can normalize to canonical IDs such as `default:_global/architecture`.\n\n### Grouping results by label\n\n`groupBy: label` groups matched nodes by their label (after `strip` is applied). This is useful when the same heading pattern repeats and you want the results keyed by name:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  groupBy: label\n  strip: '^Requirement:\\s*'\n  extract: content\n```\n\nInstead of returning a flat array of content strings, this returns an object keyed by stripped label:\n\n```json\n{\n  \"Authentication\": \"Users must be able to log in...\",\n  \"Session expiry\": \"Sessions expire after 30 minutes...\"\n}\n```\n\n---\n\n## Structured extraction with `fields`\n\nFor complex data like BDD scenarios, a single matched node needs to produce a structured object with multiple named fields. The `fields` option does this: each key in `fields` declares how to populate one property of the output object.\n\nEach field mapping can use:\n\n| Field            | Description                                                                                                                                  |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |\n| `from`           | Source of the value: `'label'` (the matched node's label), `'parentLabel'` (the parent node's label), or `'content'` (the rendered subtree). |\n| `childSelector`  | A selector applied within the matched node to find child nodes for this field.                                                               |\n| `capture`        | Regex with a capture group applied to the extracted text.                                                                                    |\n| `strip`          | Regex removed from the extracted text.                                                                                                       |\n| `followSiblings` | Regex matching sibling nodes that follow a `childSelector` match. Enables sequential grouping — see below.                                   |\n\n### Example: extracting BDD scenarios\n\nThe standard schema extracts structured scenario objects from `verify.md` files. Each scenario section (`#### Scenario: ...`) under a requirement section needs to produce an object with `name`, `requirement`, `given`, `when`, and `then` fields.\n\nThe `verify.md` looks like this:\n\n```markdown\n### Requirement: Authentication\n\n#### Scenario: Valid login\n\n- GIVEN the user is on the login page\n- WHEN the user enters valid credentials\n- AND the user submits the form\n- THEN the user is redirected to the dashboard\n- AND a session cookie is set\n```\n\nThe extractor:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Scenario:'\n    parent:\n      type: section\n      matches: '^Requirement:'\n  fields:\n    name:\n      from: label\n      strip: '^Scenario:\\s*'\n    requirement:\n      from: parentLabel\n      strip: '^Requirement:\\s*'\n    given:\n      childSelector: { type: list-item, matches: '^GIVEN\\b' }\n      capture: '^GIVEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    when:\n      childSelector: { type: list-item, matches: '^WHEN\\b' }\n      capture: '^WHEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    then:\n      childSelector: { type: list-item, matches: '^THEN\\b' }\n      capture: '^THEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n```\n\nEach matched `Scenario:` section produces one object. For the example above:\n\n```json\n{\n  \"name\": \"Valid login\",\n  \"requirement\": \"Authentication\",\n  \"given\": [\"the user is on the login page\"],\n  \"when\": [\"the user enters valid credentials\", \"the user submits the form\"],\n  \"then\": [\"the user is redirected to the dashboard\", \"a session cookie is set\"]\n}\n```\n\n### The `followSiblings` pattern\n\n`followSiblings` enables collecting `AND`/`OR` continuation lines that follow a primary clause. When a `childSelector` matches a node (e.g. `WHEN the user enters valid credentials`), `followSiblings` matches the subsequent sibling nodes that belong to that clause (`AND the user submits the form`). The matched siblings are appended to the preceding field's result array.\n\nIf `followSiblings` contains a capture group, only the captured portion is used. Without a capture group, the full node text is returned. The collection stops when a sibling does not match `followSiblings` or when another field's `childSelector` matches.\n\n---\n\n## Validation rules\n\nValidation rules assert structural constraints on artifact content. They appear in `validations` (checked against the artifact) and `deltaValidations` (checked against delta files before application).\n\n### Validation rule fields\n\n| Field            | Type     | Description                                                                                                                                               |\n| ---------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `id`             | string   | Optional identifier for this rule. Useful for error messages and for targeting with overrides.                                                            |\n| `selector`       | selector | Identifies the node(s) to validate.                                                                                                                       |\n| `path`           | string   | JSONPath expression for targeting a value in JSON or YAML artifacts. Alternative to `selector`.                                                           |\n| `required`       | boolean  | Whether the matched node must exist. Defaults to `true`. When `false`, the rule produces a warning rather than a hard failure if the node is absent.      |\n| `count`          | object   | Cardinality and uniqueness constraints over the matched nodes. See [Cardinality](#cardinality-with-count) and [Uniqueness](#uniqueness-with-countunique). |\n| `contentMatches` | string   | Regex the rendered node content must match.                                                                                                               |\n| `children`       | array    | Nested validation rules evaluated against the matched node's children.                                                                                    |\n\n### Cross-field validation in structured artifacts\n\nA powerful feature of `contentMatches` is that it operates on the **full rendered text of the matched node's subtree**. For structured formats like YAML or JSON, this enables **cross-field validation** within a single rule.\n\nWhen you select a `sequence-item` in a delta file, `contentMatches` sees the entire YAML block for that entry, including the `op`, `selector`, and `content` fields. You can use a single regex to enforce logical dependencies between these fields — for example, \"IF the selector targets a Requirement, THEN the content must include a Scenario\":\n\n```yaml\ndeltaValidations:\n  - id: verify-requirement-delta-has-scenario\n    type: sequence-item\n    # Logic: IF ((op:added AND content:### Requirement:) OR (op:modified AND matches:Requirement:))\n    #        THEN MUST have #### Scenario:\n    # We use lookaheads (?=...) with [\\s\\S]* to ensure we match across lines regardless of field order.\n    contentMatches: '^(?:(?!(?=[\\s\\S]*op: added)(?=[\\s\\S]*content:[^]*### Requirement:)|(?=[\\s\\S]*op: modified)(?=[\\s\\S]*matches:[^]*Requirement:))[\\s\\S]*|[\\s\\S]*#### Scenario:[\\s\\S]*)$'\n```\n\n### Asserting a section exists\n\n```yaml\nvalidations:\n  - id: has-purpose\n    selector:\n      type: section\n      matches: '^Purpose$'\n    required: true\n```\n\nFails if no `Purpose` section is found in the artifact.\n\n### Asserting nested structure\n\n`children` rules are evaluated within each node matched by the parent rule. This is how you assert that a section not only exists but also contains the right structure:\n\n```yaml\nvalidations:\n  - id: has-requirements\n    selector:\n      type: section\n      matches: '^Requirements$'\n    required: true\n    children:\n      - id: has-requirement-block\n        selector:\n          type: section\n          matches: '^Requirement:'\n        required: true\n        children:\n          - id: has-scenario\n            selector:\n              type: section\n              matches: '^Scenario:'\n            required: true\n```\n\nThis enforces the full structure: a `Requirements` section must exist, it must contain at least one `Requirement:` subsection, and that subsection must contain at least one `Scenario:` subsection.\n\n### Asserting content with `contentMatches`\n\n```yaml\ndeltaValidations:\n  - id: added-has-scenario\n    selector:\n      type: sequence-item\n      where:\n        op: 'added|modified'\n    contentMatches: '#### Scenario:'\n    required: true\n```\n\nEvery delta entry with `op: added` or `op: modified` must have content that includes a `#### Scenario:` heading. This is how the standard schema enforces that verification scenarios are included when specs change.\n\n### `required: false` for warnings\n\nWhen `required: false`, the absence of a matching node produces a warning rather than blocking validation:\n\n```yaml\nvalidations:\n  - id: has-spec-dependencies\n    selector:\n      type: section\n      matches: '^Spec Dependencies$'\n    required: false\n```\n\nUse this for recommended but not mandatory structure.\n\n### Cardinality with `count`\n\n`required` only handles the zero-match case. Use `count` when you need non-zero cardinality constraints — for example, \"exactly one login requirement\" or \"each requirement must have 1–3 scenarios\":\n\n```yaml\nvalidations:\n  - id: unique-login-requirement\n    selector:\n      type: section\n      matches: '^Requirement: Login$'\n    count:\n      exactly: 1\n\n  - id: requirement-scenarios\n    selector:\n      type: section\n      matches: '^Requirement:'\n    children:\n      - id: scenarios\n        selector:\n          type: section\n          matches: '^Scenario:'\n        count:\n          min: 1\n          max: 3\n```\n\n`exactly` is mutually exclusive with `min`/`max`. `children` rules can also declare their own `count`.\n\n### Uniqueness with `count.unique`\n\nInside `count`, an optional `unique` block rejects selected nodes whose derived keys collide. Keys are extracted via a `by` sub-object using the same `from`/`capture`/`strip` model as cross-artifact participant keys:\n\n```yaml\nvalidations:\n  - id: unique-requirement-ids\n    selector:\n      type: section\n      matches: '^Requirement:'\n    count:\n      min: 1\n      unique:\n        by:\n          from: label\n          strip: '^Requirement:\\s*'\n```\n\nThis selects all `Requirement:` sections and rejects any whose label (after stripping the prefix) is not unique. The validation failure message lists the duplicate keys found.\n\nOptional bounds constrain the number of **unique** keys independently of total count: `exactlyUnique`, `minUnique`, `maxUnique`.\n\n### Cross-artifact validation\n\nBeyond per-artifact rules, schemas can declare **relational** checks across multiple artifacts using the top-level `crossArtifactValidations` array. Each rule extracts comparable key collections from two or more artifacts and compares them with a relation operator (`all-equal`, `subset`, `superset`).\n\n```yaml\ncrossArtifactValidations:\n  - id: mirrored-requirements\n    scope: spec\n    participants:\n      - artifact: specs\n        as: specRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n      - artifact: verify\n        as: verifyRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n    relation:\n      kind: all-equal\n      between: [specRequirements, verifyRequirements]\n```\n\nThis rule ensures `specs` and `verify` expose the same requirement IDs. Rules are evaluated only after all participating artifacts pass their local structural validation. When a participant is not yet ready, the rule is deferred with a warning rather than failing.\n\nFor the full syntax including `keySelector`, ordering options, and relation kinds, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n\n---\n\n## Metadata extraction configuration\n\n`metadataExtraction` is a top-level field in `schema.yaml` that wires extractors to named metadata categories. SpecD uses these declarations to pull structured information from artifact files — for context compilation, impact analysis, and tooling.\n\nThe configuration is keyed by category name:\n\n**Scalar categories** (single extractor entry):\n\n| Category      | Description                                           |\n| ------------- | ----------------------------------------------------- |\n| `title`       | The spec title, typically the H1 heading.             |\n| `description` | A prose description, typically the Purpose section.   |\n| `dependsOn`   | Dependency spec paths extracted from link references. |\n| `keywords`    | Keyword terms.                                        |\n\n**Array categories** (one or more extractor entries):\n\n| Category      | Description                                           |\n| ------------- | ----------------------------------------------------- |\n| `rules`       | Structured rule groups extracted from the spec.       |\n| `constraints` | Constraint strings.                                   |\n| `scenarios`   | Structured scenario objects from the verify artifact. |\n| `context`     | Always-included context content.                      |\n\nEach entry has three fields:\n\n| Field       | Required | Description                                                                                |\n| ----------- | -------- | ------------------------------------------------------------------------------------------ |\n| `id`        | no       | Identifier for this entry. Used by `schemaOverrides` to target the entry for modification. |\n| `artifact`  | yes      | The artifact type ID to extract from (e.g. `'specs'`, `'verify'`).                         |\n| `extractor` | yes      | The extraction configuration — a `selector` plus post-processing fields.                   |\n\n### Complete example: the standard schema's `metadataExtraction`\n\nThis is the full `metadataExtraction` configuration from `schema-std`, with explanations:\n\n```yaml\nmetadataExtraction:\n  # Pull the spec title from the H1 heading — label only, no content\n  title:\n    artifact: specs\n    extractor:\n      selector: { type: section, level: 1 }\n      extract: label\n\n  # Pull the spec description from the Purpose section's content\n  description:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Purpose$' }\n      extract: content\n\n  # Extract dependency spec paths from Markdown links in Spec Dependencies\n  # capture pulls only the link targets, transform normalises them to spec IDs\n  dependsOn:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Spec Dependencies$' }\n      extract: content\n      capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n      transform:\n        name: resolveSpecPath\n        args: ['$2']\n\n  # Extract all Requirement sections, keyed by requirement name\n  # strip removes the \"Requirement: \" prefix from each key\n  rules:\n    - id: spec-requirements\n      artifact: specs\n      extractor:\n        selector:\n          type: section\n          matches: '^Requirement:'\n          parent: { type: section, matches: '^Requirements$' }\n        groupBy: label\n        strip: '^Requirement:\\s*'\n        extract: content\n\n  # Extract bullet points from the Constraints section\n  constraints:\n    - id: spec-constraints\n      artifact: specs\n      extractor:\n        selector:\n          type: list-item\n          parent: { type: section, matches: '^Constraints$' }\n        extract: label\n\n  # Extract structured scenario objects from the verify artifact\n  scenarios:\n    - id: verify-scenarios\n      artifact: verify\n      extractor:\n        selector:\n          type: section\n          matches: '^Scenario:'\n          parent: { type: section, matches: '^Requirement:' }\n        fields:\n          name: { from: label, strip: '^Scenario:\\s*' }\n          requirement: { from: parentLabel, strip: '^Requirement:\\s*' }\n          given:\n            childSelector: { type: list-item, matches: '^GIVEN\\b' }\n            capture: '^GIVEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          when:\n            childSelector: { type: list-item, matches: '^WHEN\\b' }\n            capture: '^WHEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          then:\n            childSelector: { type: list-item, matches: '^THEN\\b' }\n            capture: '^THEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n\n  # Always include the Purpose section in compiled context\n  context:\n    - id: spec-overview\n      artifact: specs\n      extractor:\n        selector: { type: section, matches: '^Purpose$' }\n        extract: content\n```\n\n---\n\n## Where to go next\n\n- [Schema format reference](../schemas/schema-format.md) — complete technical reference for `schema.yaml`, including `validations`, `deltaValidations`, and `metadataExtraction` field-by-field\n- [Validations and delta validations example](../schemas/examples/validations-and-delta-validations.md) — annotated examples showing selector patterns in validation rules\n- [Delta files example](../schemas/examples/delta-files.md) — how selectors are used inside delta file entries to target nodes for modification\n",
    "lineCount": 739,
    "byteLength": 30315,
    "outline": [
      {
        "index": 1,
        "heading": "Selectors, Extractors, and Validation Rules",
        "level": 1,
        "startLine": 1,
        "endLine": 739,
        "lines": 739,
        "content": "# Selectors, Extractors, and Validation Rules\n\nSpecD schemas work with the content of your artifact files — not just their existence. To do that, schemas need a way to describe exactly which part of a file they care about: a heading, a list item, a YAML key, a specific sequence entry. That is what selectors do.\n\nSelectors, extractors, and validation rules are three layers that build on each other:\n\n- A **selector** identifies one or more nodes in a file's parsed structure.\n- An **extractor** wraps a selector with post-processing instructions: what text to pull out, how to clean it up, and optionally how to map it into structured objects.\n- A **validation rule** wraps a selector with a structural assertion: does this node exist? does its content match a pattern?\n\nAll three are used across schemas — in `validations`, `deltaValidations`, and `metadataExtraction`. Understanding selectors is the foundation for all of them.\n\n---\n\n## How artifacts are parsed\n\nWhen SpecD reads an artifact file, it parses it into a tree of typed nodes — an Abstract Syntax Tree (AST). Each node has a **type**, a **label** (the identifying text), and a **value** (the content).\n\nThe node types available depend on the file's format:\n\n| Format     | Node types                                                                              |\n| ---------- | --------------------------------------------------------------------------------------- |\n| Markdown   | `document`, `section`, `paragraph`, `list`, `list-item`, `code-block`, `thematic-break` |\n| JSON       | `document`, `object`, `property`, `array`, `array-item`                                 |\n| YAML       | `document`, `mapping`, `pair`, `sequence`, `sequence-item`                              |\n| Plain text | `document`, `paragraph`, `line`                                                         |\n\nThe **label** is what `matches` is tested against: the heading text for a Markdown `section`, the key name for a YAML `pair` or JSON `property`. The **value** is what `contains` is tested against: the paragraph text for a Markdown `paragraph`, the scalar value for a YAML `pair`.\n\n### Markdown example\n\nGiven this Markdown:\n\n```markdown\n## Requirements\n\n### Requirement: Authentication\n\nUsers must be able to log in with email and password.\n```\n\nThe AST contains:\n\n- A `section` node with `label: \"Requirements\"` at level 2\n  - A `section` node with `label: \"Requirement: Authentication\"` at level 3\n    - A `paragraph` node with `value: \"Users must be able to log in with email and password.\"`\n\n### YAML example\n\nGiven this YAML:\n\n```yaml\nworkflow:\n  - name: Run tests\n    command: pnpm test\n  - name: Deploy\n    command: ./deploy.sh\n```\n\nThe AST contains:\n\n- A `mapping` at document root\n  - A `pair` with `label: \"workflow\"`\n    - A `sequence` containing:\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Run tests\"`\n        - A `pair` with `label: \"command\"`, `value: \"pnpm test\"`\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Deploy\"`\n        - ...\n\n---\n\n## Selectors\n\nA selector is an object that describes the nodes to match. All selector fields are optional except `type`.\n\n### Selector fields\n\n| Field      | Type     | Description                                                                                                                                                                                           |\n| ---------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `type`     | string   | The node type to match. Required. Must be one of the type names for the target format (see table above).                                                                                              |\n| `matches`  | string   | Regex matched **case-insensitively** against the node's `label`. Plain strings match anywhere in the label — use anchors for exact matches.                                                           |\n| `contains` | string   | Regex matched **case-insensitively** against the node's `value`. Useful for finding leaf nodes by content rather than name.                                                                           |\n| `parent`   | selector | Constrains the search to nodes whose nearest ancestor matches this selector. Used to disambiguate nodes with the same label at different levels.                                                      |\n| `index`    | integer  | For `array-item` and `sequence-item` nodes: targets the item at this zero-based position. Mutually exclusive with `where`.                                                                            |\n| `where`    | object   | For `array-item` and `sequence-item` nodes that are objects: targets the item whose fields all match the given key–value pairs. Values are case-insensitive regexes. Mutually exclusive with `index`. |\n| `level`    | integer  | For Markdown `section` nodes only: matches sections at exactly this heading level (1 = `#`, 2 = `##`, etc.).                                                                                          |\n\n### Matching with `matches`\n\n`matches` is a case-insensitive regular expression tested against the node's label. Without anchors, it matches anywhere in the label text.\n\n| Pattern                        | What it matches                                                         |\n| ------------------------------ | ----------------------------------------------------------------------- |\n| `'Login'`                      | Any label containing `Login` (e.g. `Login`, `User Login`, `Login flow`) |\n| `'^Requirements$'`             | Exactly the string `Requirements`                                       |\n| `'^Requirement:'`              | Any label starting with `Requirement:`                                  |\n| `'_url$'`                      | Any label ending with `_url`                                            |\n| `'^Scenario: .+ \\(skipped\\)$'` | Labels like `Scenario: Login (skipped)`                                 |\n\n### Narrowing with `parent`\n\nWhen the same heading appears at multiple levels of a document, `parent` disambiguates. Without `parent`, all matching nodes are returned regardless of where they appear.\n\n```yaml\n# Matches every section whose label starts with \"Requirement:\"\n# anywhere in the document — including sections nested under other headings\nselector:\n  type: section\n  matches: '^Requirement:'\n\n# Matches only those sections nested directly under the Requirements section\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\n### Targeting by position with `index`\n\nFor ordered collections (`array-item`, `sequence-item`), `index` selects a specific item by its zero-based position:\n\n```yaml\n# The first item in a sequence\nselector:\n  type: sequence-item\n  index: 0\n```\n\n### Targeting by field values with `where`\n\nFor object-typed sequence or array items, `where` matches items whose named fields satisfy all conditions. Values are case-insensitive regexes:\n\n```yaml\n# The sequence item whose \"op\" field matches \"added\" or \"modified\"\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n\n# The sequence item whose \"name\" field is exactly \"Run tests\"\nselector:\n  type: sequence-item\n  where:\n    name: '^Run tests$'\n```\n\n### Targeting by heading level with `level`\n\nIn Markdown, `level` restricts a `section` selector to a specific heading depth:\n\n```yaml\n# Only the H1 heading (the document title)\nselector:\n  type: section\n  level: 1\n\n# Only H3 headings — not H2 or H4\nselector:\n  type: section\n  level: 3\n```\n\n---\n\n## Practical selector examples\n\nThese examples come directly from the standard schema (`schema-std`).\n\n### Selecting the document title (H1 heading)\n\n```yaml\nselector:\n  type: section\n  level: 1\n```\n\nMatches the first (and typically only) H1 heading in a Markdown spec file.\n\n### Selecting a section by exact heading name\n\n```yaml\nselector:\n  type: section\n  matches: '^Purpose$'\n```\n\nMatches a section whose heading is exactly `Purpose`. Case-insensitive, so `purpose` and `PURPOSE` also match.\n\n### Selecting all Requirement sections under Requirements\n\n```yaml\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\nMatches every `### Requirement: ...` heading that is a direct child of the `## Requirements` section. Without the `parent` constraint, this would also match any `Requirement:` heading that appears elsewhere in the document.\n\n### Selecting list items within a section\n\n```yaml\nselector:\n  type: list-item\n  parent:\n    type: section\n    matches: '^Constraints$'\n```\n\nMatches every bullet point under a `## Constraints` heading.\n\n### Selecting a YAML sequence item by field value\n\n```yaml\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n```\n\nMatches every sequence item (object) whose `op` field is `added` or `modified`. Used in `deltaValidations` to assert that modified delta entries meet structural requirements.\n\n---\n\n## Extractors: pulling data from the AST\n\nAn extractor pairs a selector with instructions for what to do with the matched nodes. Extractors are used in `metadataExtraction` to pull structured content out of artifact files.\n\nEvery extractor has a `selector` field plus a post-processing pipeline. The pipeline runs in this order:\n\n1. **Select** nodes using `selector`\n2. **Extract** text using `extract`\n3. **Strip** unwanted patterns using `strip`\n4. **Capture** a portion of the text using `capture`\n5. **Transform** the result using a named callback with `transform` (callbacks may be sync or async)\n6. **Group** results using `groupBy`\n\nOr, for complex structured data:\n\n- **Map to objects** using `fields`\n\n### Extract modes\n\nThe `extract` field controls what text is pulled from each matched node:\n\n| Value       | What it returns                                                                                                       |\n| ----------- | --------------------------------------------------------------------------------------------------------------------- |\n| `'content'` | The full rendered text of the node's subtree (default). For a Markdown section, this is everything below the heading. |\n| `'label'`   | Only the node's identifying text: the heading text, key name, or item label.                                          |\n| `'both'`    | The label followed by the content, separated.                                                                         |\n\n### Simple extraction: a section's content\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Purpose$'\n  extract: content\n```\n\nReturns the full text of everything inside the `Purpose` section — paragraphs, lists, everything below the heading.\n\n### Extracting just the label\n\n```yaml\nextractor:\n  selector:\n    type: section\n    level: 1\n  extract: label\n```\n\nReturns the heading text of the H1 section — the document title — without any of the section's content.\n\n### Stripping a prefix from labels\n\nWhen labels contain a structured prefix you want to remove:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  extract: label\n  strip: '^Requirement:\\s*'\n```\n\nEach matched label (`Requirement: Authentication`, `Requirement: Session expiry`) has the `Requirement: ` prefix removed, leaving `Authentication` and `Session expiry`.\n\n### Capturing a portion of the text\n\n`capture` is a regex with a capture group. Only the captured portion is kept:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Spec Dependencies$'\n  extract: content\n  capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n  transform:\n    name: resolveSpecPath\n    args: ['$2']\n```\n\nThis extracts dependency entries from the `Spec Dependencies` section, capturing the visible label as the primary `value` and the optional `href` as `$2`. `resolveSpecPath` first tries the label itself (for canonical entries like ``[`core:core/config`](../config/spec.md)`` or `` `core:core/config` ``), then falls back to the captured `href` when the label is legacy text. Relative candidates are resolved through repository-backed normalization (`resolveFromPath` + `crossWorkspaceHint` routing), so links like `../../_global/architecture/spec.md` can normalize to canonical IDs such as `default:_global/architecture`.\n\n### Grouping results by label\n\n`groupBy: label` groups matched nodes by their label (after `strip` is applied). This is useful when the same heading pattern repeats and you want the results keyed by name:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  groupBy: label\n  strip: '^Requirement:\\s*'\n  extract: content\n```\n\nInstead of returning a flat array of content strings, this returns an object keyed by stripped label:\n\n```json\n{\n  \"Authentication\": \"Users must be able to log in...\",\n  \"Session expiry\": \"Sessions expire after 30 minutes...\"\n}\n```\n\n---\n\n## Structured extraction with `fields`\n\nFor complex data like BDD scenarios, a single matched node needs to produce a structured object with multiple named fields. The `fields` option does this: each key in `fields` declares how to populate one property of the output object.\n\nEach field mapping can use:\n\n| Field            | Description                                                                                                                                  |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |\n| `from`           | Source of the value: `'label'` (the matched node's label), `'parentLabel'` (the parent node's label), or `'content'` (the rendered subtree). |\n| `childSelector`  | A selector applied within the matched node to find child nodes for this field.                                                               |\n| `capture`        | Regex with a capture group applied to the extracted text.                                                                                    |\n| `strip`          | Regex removed from the extracted text.                                                                                                       |\n| `followSiblings` | Regex matching sibling nodes that follow a `childSelector` match. Enables sequential grouping — see below.                                   |\n\n### Example: extracting BDD scenarios\n\nThe standard schema extracts structured scenario objects from `verify.md` files. Each scenario section (`#### Scenario: ...`) under a requirement section needs to produce an object with `name`, `requirement`, `given`, `when`, and `then` fields.\n\nThe `verify.md` looks like this:\n\n```markdown\n### Requirement: Authentication\n\n#### Scenario: Valid login\n\n- GIVEN the user is on the login page\n- WHEN the user enters valid credentials\n- AND the user submits the form\n- THEN the user is redirected to the dashboard\n- AND a session cookie is set\n```\n\nThe extractor:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Scenario:'\n    parent:\n      type: section\n      matches: '^Requirement:'\n  fields:\n    name:\n      from: label\n      strip: '^Scenario:\\s*'\n    requirement:\n      from: parentLabel\n      strip: '^Requirement:\\s*'\n    given:\n      childSelector: { type: list-item, matches: '^GIVEN\\b' }\n      capture: '^GIVEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    when:\n      childSelector: { type: list-item, matches: '^WHEN\\b' }\n      capture: '^WHEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    then:\n      childSelector: { type: list-item, matches: '^THEN\\b' }\n      capture: '^THEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n```\n\nEach matched `Scenario:` section produces one object. For the example above:\n\n```json\n{\n  \"name\": \"Valid login\",\n  \"requirement\": \"Authentication\",\n  \"given\": [\"the user is on the login page\"],\n  \"when\": [\"the user enters valid credentials\", \"the user submits the form\"],\n  \"then\": [\"the user is redirected to the dashboard\", \"a session cookie is set\"]\n}\n```\n\n### The `followSiblings` pattern\n\n`followSiblings` enables collecting `AND`/`OR` continuation lines that follow a primary clause. When a `childSelector` matches a node (e.g. `WHEN the user enters valid credentials`), `followSiblings` matches the subsequent sibling nodes that belong to that clause (`AND the user submits the form`). The matched siblings are appended to the preceding field's result array.\n\nIf `followSiblings` contains a capture group, only the captured portion is used. Without a capture group, the full node text is returned. The collection stops when a sibling does not match `followSiblings` or when another field's `childSelector` matches.\n\n---\n\n## Validation rules\n\nValidation rules assert structural constraints on artifact content. They appear in `validations` (checked against the artifact) and `deltaValidations` (checked against delta files before application).\n\n### Validation rule fields\n\n| Field            | Type     | Description                                                                                                                                               |\n| ---------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `id`             | string   | Optional identifier for this rule. Useful for error messages and for targeting with overrides.                                                            |\n| `selector`       | selector | Identifies the node(s) to validate.                                                                                                                       |\n| `path`           | string   | JSONPath expression for targeting a value in JSON or YAML artifacts. Alternative to `selector`.                                                           |\n| `required`       | boolean  | Whether the matched node must exist. Defaults to `true`. When `false`, the rule produces a warning rather than a hard failure if the node is absent.      |\n| `count`          | object   | Cardinality and uniqueness constraints over the matched nodes. See [Cardinality](#cardinality-with-count) and [Uniqueness](#uniqueness-with-countunique). |\n| `contentMatches` | string   | Regex the rendered node content must match.                                                                                                               |\n| `children`       | array    | Nested validation rules evaluated against the matched node's children.                                                                                    |\n\n### Cross-field validation in structured artifacts\n\nA powerful feature of `contentMatches` is that it operates on the **full rendered text of the matched node's subtree**. For structured formats like YAML or JSON, this enables **cross-field validation** within a single rule.\n\nWhen you select a `sequence-item` in a delta file, `contentMatches` sees the entire YAML block for that entry, including the `op`, `selector`, and `content` fields. You can use a single regex to enforce logical dependencies between these fields — for example, \"IF the selector targets a Requirement, THEN the content must include a Scenario\":\n\n```yaml\ndeltaValidations:\n  - id: verify-requirement-delta-has-scenario\n    type: sequence-item\n    # Logic: IF ((op:added AND content:### Requirement:) OR (op:modified AND matches:Requirement:))\n    #        THEN MUST have #### Scenario:\n    # We use lookaheads (?=...) with [\\s\\S]* to ensure we match across lines regardless of field order.\n    contentMatches: '^(?:(?!(?=[\\s\\S]*op: added)(?=[\\s\\S]*content:[^]*### Requirement:)|(?=[\\s\\S]*op: modified)(?=[\\s\\S]*matches:[^]*Requirement:))[\\s\\S]*|[\\s\\S]*#### Scenario:[\\s\\S]*)$'\n```\n\n### Asserting a section exists\n\n```yaml\nvalidations:\n  - id: has-purpose\n    selector:\n      type: section\n      matches: '^Purpose$'\n    required: true\n```\n\nFails if no `Purpose` section is found in the artifact.\n\n### Asserting nested structure\n\n`children` rules are evaluated within each node matched by the parent rule. This is how you assert that a section not only exists but also contains the right structure:\n\n```yaml\nvalidations:\n  - id: has-requirements\n    selector:\n      type: section\n      matches: '^Requirements$'\n    required: true\n    children:\n      - id: has-requirement-block\n        selector:\n          type: section\n          matches: '^Requirement:'\n        required: true\n        children:\n          - id: has-scenario\n            selector:\n              type: section\n              matches: '^Scenario:'\n            required: true\n```\n\nThis enforces the full structure: a `Requirements` section must exist, it must contain at least one `Requirement:` subsection, and that subsection must contain at least one `Scenario:` subsection.\n\n### Asserting content with `contentMatches`\n\n```yaml\ndeltaValidations:\n  - id: added-has-scenario\n    selector:\n      type: sequence-item\n      where:\n        op: 'added|modified'\n    contentMatches: '#### Scenario:'\n    required: true\n```\n\nEvery delta entry with `op: added` or `op: modified` must have content that includes a `#### Scenario:` heading. This is how the standard schema enforces that verification scenarios are included when specs change.\n\n### `required: false` for warnings\n\nWhen `required: false`, the absence of a matching node produces a warning rather than blocking validation:\n\n```yaml\nvalidations:\n  - id: has-spec-dependencies\n    selector:\n      type: section\n      matches: '^Spec Dependencies$'\n    required: false\n```\n\nUse this for recommended but not mandatory structure.\n\n### Cardinality with `count`\n\n`required` only handles the zero-match case. Use `count` when you need non-zero cardinality constraints — for example, \"exactly one login requirement\" or \"each requirement must have 1–3 scenarios\":\n\n```yaml\nvalidations:\n  - id: unique-login-requirement\n    selector:\n      type: section\n      matches: '^Requirement: Login$'\n    count:\n      exactly: 1\n\n  - id: requirement-scenarios\n    selector:\n      type: section\n      matches: '^Requirement:'\n    children:\n      - id: scenarios\n        selector:\n          type: section\n          matches: '^Scenario:'\n        count:\n          min: 1\n          max: 3\n```\n\n`exactly` is mutually exclusive with `min`/`max`. `children` rules can also declare their own `count`.\n\n### Uniqueness with `count.unique`\n\nInside `count`, an optional `unique` block rejects selected nodes whose derived keys collide. Keys are extracted via a `by` sub-object using the same `from`/`capture`/`strip` model as cross-artifact participant keys:\n\n```yaml\nvalidations:\n  - id: unique-requirement-ids\n    selector:\n      type: section\n      matches: '^Requirement:'\n    count:\n      min: 1\n      unique:\n        by:\n          from: label\n          strip: '^Requirement:\\s*'\n```\n\nThis selects all `Requirement:` sections and rejects any whose label (after stripping the prefix) is not unique. The validation failure message lists the duplicate keys found.\n\nOptional bounds constrain the number of **unique** keys independently of total count: `exactlyUnique`, `minUnique`, `maxUnique`.\n\n### Cross-artifact validation\n\nBeyond per-artifact rules, schemas can declare **relational** checks across multiple artifacts using the top-level `crossArtifactValidations` array. Each rule extracts comparable key collections from two or more artifacts and compares them with a relation operator (`all-equal`, `subset`, `superset`).\n\n```yaml\ncrossArtifactValidations:\n  - id: mirrored-requirements\n    scope: spec\n    participants:\n      - artifact: specs\n        as: specRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n      - artifact: verify\n        as: verifyRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n    relation:\n      kind: all-equal\n      between: [specRequirements, verifyRequirements]\n```\n\nThis rule ensures `specs` and `verify` expose the same requirement IDs. Rules are evaluated only after all participating artifacts pass their local structural validation. When a participant is not yet ready, the rule is deferred with a warning rather than failing.\n\nFor the full syntax including `keySelector`, ordering options, and relation kinds, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n\n---\n\n## Metadata extraction configuration\n\n`metadataExtraction` is a top-level field in `schema.yaml` that wires extractors to named metadata categories. SpecD uses these declarations to pull structured information from artifact files — for context compilation, impact analysis, and tooling.\n\nThe configuration is keyed by category name:\n\n**Scalar categories** (single extractor entry):\n\n| Category      | Description                                           |\n| ------------- | ----------------------------------------------------- |\n| `title`       | The spec title, typically the H1 heading.             |\n| `description` | A prose description, typically the Purpose section.   |\n| `dependsOn`   | Dependency spec paths extracted from link references. |\n| `keywords`    | Keyword terms.                                        |\n\n**Array categories** (one or more extractor entries):\n\n| Category      | Description                                           |\n| ------------- | ----------------------------------------------------- |\n| `rules`       | Structured rule groups extracted from the spec.       |\n| `constraints` | Constraint strings.                                   |\n| `scenarios`   | Structured scenario objects from the verify artifact. |\n| `context`     | Always-included context content.                      |\n\nEach entry has three fields:\n\n| Field       | Required | Description                                                                                |\n| ----------- | -------- | ------------------------------------------------------------------------------------------ |\n| `id`        | no       | Identifier for this entry. Used by `schemaOverrides` to target the entry for modification. |\n| `artifact`  | yes      | The artifact type ID to extract from (e.g. `'specs'`, `'verify'`).                         |\n| `extractor` | yes      | The extraction configuration — a `selector` plus post-processing fields.                   |\n\n### Complete example: the standard schema's `metadataExtraction`\n\nThis is the full `metadataExtraction` configuration from `schema-std`, with explanations:\n\n```yaml\nmetadataExtraction:\n  # Pull the spec title from the H1 heading — label only, no content\n  title:\n    artifact: specs\n    extractor:\n      selector: { type: section, level: 1 }\n      extract: label\n\n  # Pull the spec description from the Purpose section's content\n  description:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Purpose$' }\n      extract: content\n\n  # Extract dependency spec paths from Markdown links in Spec Dependencies\n  # capture pulls only the link targets, transform normalises them to spec IDs\n  dependsOn:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Spec Dependencies$' }\n      extract: content\n      capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n      transform:\n        name: resolveSpecPath\n        args: ['$2']\n\n  # Extract all Requirement sections, keyed by requirement name\n  # strip removes the \"Requirement: \" prefix from each key\n  rules:\n    - id: spec-requirements\n      artifact: specs\n      extractor:\n        selector:\n          type: section\n          matches: '^Requirement:'\n          parent: { type: section, matches: '^Requirements$' }\n        groupBy: label\n        strip: '^Requirement:\\s*'\n        extract: content\n\n  # Extract bullet points from the Constraints section\n  constraints:\n    - id: spec-constraints\n      artifact: specs\n      extractor:\n        selector:\n          type: list-item\n          parent: { type: section, matches: '^Constraints$' }\n        extract: label\n\n  # Extract structured scenario objects from the verify artifact\n  scenarios:\n    - id: verify-scenarios\n      artifact: verify\n      extractor:\n        selector:\n          type: section\n          matches: '^Scenario:'\n          parent: { type: section, matches: '^Requirement:' }\n        fields:\n          name: { from: label, strip: '^Scenario:\\s*' }\n          requirement: { from: parentLabel, strip: '^Requirement:\\s*' }\n          given:\n            childSelector: { type: list-item, matches: '^GIVEN\\b' }\n            capture: '^GIVEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          when:\n            childSelector: { type: list-item, matches: '^WHEN\\b' }\n            capture: '^WHEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          then:\n            childSelector: { type: list-item, matches: '^THEN\\b' }\n            capture: '^THEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n\n  # Always include the Purpose section in compiled context\n  context:\n    - id: spec-overview\n      artifact: specs\n      extractor:\n        selector: { type: section, matches: '^Purpose$' }\n        extract: content\n```\n\n---\n\n## Where to go next\n\n- [Schema format reference](../schemas/schema-format.md) — complete technical reference for `schema.yaml`, including `validations`, `deltaValidations`, and `metadataExtraction` field-by-field\n- [Validations and delta validations example](../schemas/examples/validations-and-delta-validations.md) — annotated examples showing selector patterns in validation rules\n- [Delta files example](../schemas/examples/delta-files.md) — how selectors are used inside delta file entries to target nodes for modification\n"
      },
      {
        "index": 2,
        "heading": "How artifacts are parsed",
        "level": 2,
        "startLine": 15,
        "endLine": 73,
        "lines": 59,
        "content": "## How artifacts are parsed\n\nWhen SpecD reads an artifact file, it parses it into a tree of typed nodes — an Abstract Syntax Tree (AST). Each node has a **type**, a **label** (the identifying text), and a **value** (the content).\n\nThe node types available depend on the file's format:\n\n| Format     | Node types                                                                              |\n| ---------- | --------------------------------------------------------------------------------------- |\n| Markdown   | `document`, `section`, `paragraph`, `list`, `list-item`, `code-block`, `thematic-break` |\n| JSON       | `document`, `object`, `property`, `array`, `array-item`                                 |\n| YAML       | `document`, `mapping`, `pair`, `sequence`, `sequence-item`                              |\n| Plain text | `document`, `paragraph`, `line`                                                         |\n\nThe **label** is what `matches` is tested against: the heading text for a Markdown `section`, the key name for a YAML `pair` or JSON `property`. The **value** is what `contains` is tested against: the paragraph text for a Markdown `paragraph`, the scalar value for a YAML `pair`.\n\n### Markdown example\n\nGiven this Markdown:\n\n```markdown\n## Requirements\n\n### Requirement: Authentication\n\nUsers must be able to log in with email and password.\n```\n\nThe AST contains:\n\n- A `section` node with `label: \"Requirements\"` at level 2\n  - A `section` node with `label: \"Requirement: Authentication\"` at level 3\n    - A `paragraph` node with `value: \"Users must be able to log in with email and password.\"`\n\n### YAML example\n\nGiven this YAML:\n\n```yaml\nworkflow:\n  - name: Run tests\n    command: pnpm test\n  - name: Deploy\n    command: ./deploy.sh\n```\n\nThe AST contains:\n\n- A `mapping` at document root\n  - A `pair` with `label: \"workflow\"`\n    - A `sequence` containing:\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Run tests\"`\n        - A `pair` with `label: \"command\"`, `value: \"pnpm test\"`\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Deploy\"`\n        - ...\n\n---\n"
      },
      {
        "index": 3,
        "heading": "Markdown example",
        "level": 3,
        "startLine": 30,
        "endLine": 47,
        "lines": 18,
        "content": "### Markdown example\n\nGiven this Markdown:\n\n```markdown\n## Requirements\n\n### Requirement: Authentication\n\nUsers must be able to log in with email and password.\n```\n\nThe AST contains:\n\n- A `section` node with `label: \"Requirements\"` at level 2\n  - A `section` node with `label: \"Requirement: Authentication\"` at level 3\n    - A `paragraph` node with `value: \"Users must be able to log in with email and password.\"`\n"
      },
      {
        "index": 4,
        "heading": "YAML example",
        "level": 3,
        "startLine": 48,
        "endLine": 73,
        "lines": 26,
        "content": "### YAML example\n\nGiven this YAML:\n\n```yaml\nworkflow:\n  - name: Run tests\n    command: pnpm test\n  - name: Deploy\n    command: ./deploy.sh\n```\n\nThe AST contains:\n\n- A `mapping` at document root\n  - A `pair` with `label: \"workflow\"`\n    - A `sequence` containing:\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Run tests\"`\n        - A `pair` with `label: \"command\"`, `value: \"pnpm test\"`\n      - A `sequence-item` (an object) with child pairs\n        - A `pair` with `label: \"name\"`, `value: \"Deploy\"`\n        - ...\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Selectors",
        "level": 2,
        "startLine": 74,
        "endLine": 168,
        "lines": 95,
        "content": "## Selectors\n\nA selector is an object that describes the nodes to match. All selector fields are optional except `type`.\n\n### Selector fields\n\n| Field      | Type     | Description                                                                                                                                                                                           |\n| ---------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `type`     | string   | The node type to match. Required. Must be one of the type names for the target format (see table above).                                                                                              |\n| `matches`  | string   | Regex matched **case-insensitively** against the node's `label`. Plain strings match anywhere in the label — use anchors for exact matches.                                                           |\n| `contains` | string   | Regex matched **case-insensitively** against the node's `value`. Useful for finding leaf nodes by content rather than name.                                                                           |\n| `parent`   | selector | Constrains the search to nodes whose nearest ancestor matches this selector. Used to disambiguate nodes with the same label at different levels.                                                      |\n| `index`    | integer  | For `array-item` and `sequence-item` nodes: targets the item at this zero-based position. Mutually exclusive with `where`.                                                                            |\n| `where`    | object   | For `array-item` and `sequence-item` nodes that are objects: targets the item whose fields all match the given key–value pairs. Values are case-insensitive regexes. Mutually exclusive with `index`. |\n| `level`    | integer  | For Markdown `section` nodes only: matches sections at exactly this heading level (1 = `#`, 2 = `##`, etc.).                                                                                          |\n\n### Matching with `matches`\n\n`matches` is a case-insensitive regular expression tested against the node's label. Without anchors, it matches anywhere in the label text.\n\n| Pattern                        | What it matches                                                         |\n| ------------------------------ | ----------------------------------------------------------------------- |\n| `'Login'`                      | Any label containing `Login` (e.g. `Login`, `User Login`, `Login flow`) |\n| `'^Requirements$'`             | Exactly the string `Requirements`                                       |\n| `'^Requirement:'`              | Any label starting with `Requirement:`                                  |\n| `'_url$'`                      | Any label ending with `_url`                                            |\n| `'^Scenario: .+ \\(skipped\\)$'` | Labels like `Scenario: Login (skipped)`                                 |\n\n### Narrowing with `parent`\n\nWhen the same heading appears at multiple levels of a document, `parent` disambiguates. Without `parent`, all matching nodes are returned regardless of where they appear.\n\n```yaml\n# Matches every section whose label starts with \"Requirement:\"\n# anywhere in the document — including sections nested under other headings\nselector:\n  type: section\n  matches: '^Requirement:'\n\n# Matches only those sections nested directly under the Requirements section\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\n### Targeting by position with `index`\n\nFor ordered collections (`array-item`, `sequence-item`), `index` selects a specific item by its zero-based position:\n\n```yaml\n# The first item in a sequence\nselector:\n  type: sequence-item\n  index: 0\n```\n\n### Targeting by field values with `where`\n\nFor object-typed sequence or array items, `where` matches items whose named fields satisfy all conditions. Values are case-insensitive regexes:\n\n```yaml\n# The sequence item whose \"op\" field matches \"added\" or \"modified\"\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n\n# The sequence item whose \"name\" field is exactly \"Run tests\"\nselector:\n  type: sequence-item\n  where:\n    name: '^Run tests$'\n```\n\n### Targeting by heading level with `level`\n\nIn Markdown, `level` restricts a `section` selector to a specific heading depth:\n\n```yaml\n# Only the H1 heading (the document title)\nselector:\n  type: section\n  level: 1\n\n# Only H3 headings — not H2 or H4\nselector:\n  type: section\n  level: 3\n```\n\n---\n"
      },
      {
        "index": 6,
        "heading": "Selector fields",
        "level": 3,
        "startLine": 78,
        "endLine": 89,
        "lines": 12,
        "content": "### Selector fields\n\n| Field      | Type     | Description                                                                                                                                                                                           |\n| ---------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `type`     | string   | The node type to match. Required. Must be one of the type names for the target format (see table above).                                                                                              |\n| `matches`  | string   | Regex matched **case-insensitively** against the node's `label`. Plain strings match anywhere in the label — use anchors for exact matches.                                                           |\n| `contains` | string   | Regex matched **case-insensitively** against the node's `value`. Useful for finding leaf nodes by content rather than name.                                                                           |\n| `parent`   | selector | Constrains the search to nodes whose nearest ancestor matches this selector. Used to disambiguate nodes with the same label at different levels.                                                      |\n| `index`    | integer  | For `array-item` and `sequence-item` nodes: targets the item at this zero-based position. Mutually exclusive with `where`.                                                                            |\n| `where`    | object   | For `array-item` and `sequence-item` nodes that are objects: targets the item whose fields all match the given key–value pairs. Values are case-insensitive regexes. Mutually exclusive with `index`. |\n| `level`    | integer  | For Markdown `section` nodes only: matches sections at exactly this heading level (1 = `#`, 2 = `##`, etc.).                                                                                          |\n"
      },
      {
        "index": 7,
        "heading": "Matching with `matches`",
        "level": 3,
        "startLine": 90,
        "endLine": 101,
        "lines": 12,
        "content": "### Matching with `matches`\n\n`matches` is a case-insensitive regular expression tested against the node's label. Without anchors, it matches anywhere in the label text.\n\n| Pattern                        | What it matches                                                         |\n| ------------------------------ | ----------------------------------------------------------------------- |\n| `'Login'`                      | Any label containing `Login` (e.g. `Login`, `User Login`, `Login flow`) |\n| `'^Requirements$'`             | Exactly the string `Requirements`                                       |\n| `'^Requirement:'`              | Any label starting with `Requirement:`                                  |\n| `'_url$'`                      | Any label ending with `_url`                                            |\n| `'^Scenario: .+ \\(skipped\\)$'` | Labels like `Scenario: Login (skipped)`                                 |\n"
      },
      {
        "index": 8,
        "heading": "Narrowing with `parent`",
        "level": 3,
        "startLine": 102,
        "endLine": 121,
        "lines": 20,
        "content": "### Narrowing with `parent`\n\nWhen the same heading appears at multiple levels of a document, `parent` disambiguates. Without `parent`, all matching nodes are returned regardless of where they appear.\n\n```yaml\n# Matches every section whose label starts with \"Requirement:\"\n# anywhere in the document — including sections nested under other headings\nselector:\n  type: section\n  matches: '^Requirement:'\n\n# Matches only those sections nested directly under the Requirements section\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n"
      },
      {
        "index": 9,
        "heading": "Targeting by position with `index`",
        "level": 3,
        "startLine": 122,
        "endLine": 132,
        "lines": 11,
        "content": "### Targeting by position with `index`\n\nFor ordered collections (`array-item`, `sequence-item`), `index` selects a specific item by its zero-based position:\n\n```yaml\n# The first item in a sequence\nselector:\n  type: sequence-item\n  index: 0\n```\n"
      },
      {
        "index": 10,
        "heading": "Targeting by field values with `where`",
        "level": 3,
        "startLine": 133,
        "endLine": 150,
        "lines": 18,
        "content": "### Targeting by field values with `where`\n\nFor object-typed sequence or array items, `where` matches items whose named fields satisfy all conditions. Values are case-insensitive regexes:\n\n```yaml\n# The sequence item whose \"op\" field matches \"added\" or \"modified\"\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n\n# The sequence item whose \"name\" field is exactly \"Run tests\"\nselector:\n  type: sequence-item\n  where:\n    name: '^Run tests$'\n```\n"
      },
      {
        "index": 11,
        "heading": "Targeting by heading level with `level`",
        "level": 3,
        "startLine": 151,
        "endLine": 168,
        "lines": 18,
        "content": "### Targeting by heading level with `level`\n\nIn Markdown, `level` restricts a `section` selector to a specific heading depth:\n\n```yaml\n# Only the H1 heading (the document title)\nselector:\n  type: section\n  level: 1\n\n# Only H3 headings — not H2 or H4\nselector:\n  type: section\n  level: 3\n```\n\n---\n"
      },
      {
        "index": 12,
        "heading": "Practical selector examples",
        "level": 2,
        "startLine": 169,
        "endLine": 230,
        "lines": 62,
        "content": "## Practical selector examples\n\nThese examples come directly from the standard schema (`schema-std`).\n\n### Selecting the document title (H1 heading)\n\n```yaml\nselector:\n  type: section\n  level: 1\n```\n\nMatches the first (and typically only) H1 heading in a Markdown spec file.\n\n### Selecting a section by exact heading name\n\n```yaml\nselector:\n  type: section\n  matches: '^Purpose$'\n```\n\nMatches a section whose heading is exactly `Purpose`. Case-insensitive, so `purpose` and `PURPOSE` also match.\n\n### Selecting all Requirement sections under Requirements\n\n```yaml\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\nMatches every `### Requirement: ...` heading that is a direct child of the `## Requirements` section. Without the `parent` constraint, this would also match any `Requirement:` heading that appears elsewhere in the document.\n\n### Selecting list items within a section\n\n```yaml\nselector:\n  type: list-item\n  parent:\n    type: section\n    matches: '^Constraints$'\n```\n\nMatches every bullet point under a `## Constraints` heading.\n\n### Selecting a YAML sequence item by field value\n\n```yaml\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n```\n\nMatches every sequence item (object) whose `op` field is `added` or `modified`. Used in `deltaValidations` to assert that modified delta entries meet structural requirements.\n\n---\n"
      },
      {
        "index": 13,
        "heading": "Selecting the document title (H1 heading)",
        "level": 3,
        "startLine": 173,
        "endLine": 182,
        "lines": 10,
        "content": "### Selecting the document title (H1 heading)\n\n```yaml\nselector:\n  type: section\n  level: 1\n```\n\nMatches the first (and typically only) H1 heading in a Markdown spec file.\n"
      },
      {
        "index": 14,
        "heading": "Selecting a section by exact heading name",
        "level": 3,
        "startLine": 183,
        "endLine": 192,
        "lines": 10,
        "content": "### Selecting a section by exact heading name\n\n```yaml\nselector:\n  type: section\n  matches: '^Purpose$'\n```\n\nMatches a section whose heading is exactly `Purpose`. Case-insensitive, so `purpose` and `PURPOSE` also match.\n"
      },
      {
        "index": 15,
        "heading": "Selecting all Requirement sections under Requirements",
        "level": 3,
        "startLine": 193,
        "endLine": 205,
        "lines": 13,
        "content": "### Selecting all Requirement sections under Requirements\n\n```yaml\nselector:\n  type: section\n  matches: '^Requirement:'\n  parent:\n    type: section\n    matches: '^Requirements$'\n```\n\nMatches every `### Requirement: ...` heading that is a direct child of the `## Requirements` section. Without the `parent` constraint, this would also match any `Requirement:` heading that appears elsewhere in the document.\n"
      },
      {
        "index": 16,
        "heading": "Selecting list items within a section",
        "level": 3,
        "startLine": 206,
        "endLine": 217,
        "lines": 12,
        "content": "### Selecting list items within a section\n\n```yaml\nselector:\n  type: list-item\n  parent:\n    type: section\n    matches: '^Constraints$'\n```\n\nMatches every bullet point under a `## Constraints` heading.\n"
      },
      {
        "index": 17,
        "heading": "Selecting a YAML sequence item by field value",
        "level": 3,
        "startLine": 218,
        "endLine": 230,
        "lines": 13,
        "content": "### Selecting a YAML sequence item by field value\n\n```yaml\nselector:\n  type: sequence-item\n  where:\n    op: 'added|modified'\n```\n\nMatches every sequence item (object) whose `op` field is `added` or `modified`. Used in `deltaValidations` to assert that modified delta entries meet structural requirements.\n\n---\n"
      },
      {
        "index": 18,
        "heading": "Extractors: pulling data from the AST",
        "level": 2,
        "startLine": 231,
        "endLine": 345,
        "lines": 115,
        "content": "## Extractors: pulling data from the AST\n\nAn extractor pairs a selector with instructions for what to do with the matched nodes. Extractors are used in `metadataExtraction` to pull structured content out of artifact files.\n\nEvery extractor has a `selector` field plus a post-processing pipeline. The pipeline runs in this order:\n\n1. **Select** nodes using `selector`\n2. **Extract** text using `extract`\n3. **Strip** unwanted patterns using `strip`\n4. **Capture** a portion of the text using `capture`\n5. **Transform** the result using a named callback with `transform` (callbacks may be sync or async)\n6. **Group** results using `groupBy`\n\nOr, for complex structured data:\n\n- **Map to objects** using `fields`\n\n### Extract modes\n\nThe `extract` field controls what text is pulled from each matched node:\n\n| Value       | What it returns                                                                                                       |\n| ----------- | --------------------------------------------------------------------------------------------------------------------- |\n| `'content'` | The full rendered text of the node's subtree (default). For a Markdown section, this is everything below the heading. |\n| `'label'`   | Only the node's identifying text: the heading text, key name, or item label.                                          |\n| `'both'`    | The label followed by the content, separated.                                                                         |\n\n### Simple extraction: a section's content\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Purpose$'\n  extract: content\n```\n\nReturns the full text of everything inside the `Purpose` section — paragraphs, lists, everything below the heading.\n\n### Extracting just the label\n\n```yaml\nextractor:\n  selector:\n    type: section\n    level: 1\n  extract: label\n```\n\nReturns the heading text of the H1 section — the document title — without any of the section's content.\n\n### Stripping a prefix from labels\n\nWhen labels contain a structured prefix you want to remove:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  extract: label\n  strip: '^Requirement:\\s*'\n```\n\nEach matched label (`Requirement: Authentication`, `Requirement: Session expiry`) has the `Requirement: ` prefix removed, leaving `Authentication` and `Session expiry`.\n\n### Capturing a portion of the text\n\n`capture` is a regex with a capture group. Only the captured portion is kept:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Spec Dependencies$'\n  extract: content\n  capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n  transform:\n    name: resolveSpecPath\n    args: ['$2']\n```\n\nThis extracts dependency entries from the `Spec Dependencies` section, capturing the visible label as the primary `value` and the optional `href` as `$2`. `resolveSpecPath` first tries the label itself (for canonical entries like ``[`core:core/config`](../config/spec.md)`` or `` `core:core/config` ``), then falls back to the captured `href` when the label is legacy text. Relative candidates are resolved through repository-backed normalization (`resolveFromPath` + `crossWorkspaceHint` routing), so links like `../../_global/architecture/spec.md` can normalize to canonical IDs such as `default:_global/architecture`.\n\n### Grouping results by label\n\n`groupBy: label` groups matched nodes by their label (after `strip` is applied). This is useful when the same heading pattern repeats and you want the results keyed by name:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  groupBy: label\n  strip: '^Requirement:\\s*'\n  extract: content\n```\n\nInstead of returning a flat array of content strings, this returns an object keyed by stripped label:\n\n```json\n{\n  \"Authentication\": \"Users must be able to log in...\",\n  \"Session expiry\": \"Sessions expire after 30 minutes...\"\n}\n```\n\n---\n"
      },
      {
        "index": 19,
        "heading": "Extract modes",
        "level": 3,
        "startLine": 248,
        "endLine": 257,
        "lines": 10,
        "content": "### Extract modes\n\nThe `extract` field controls what text is pulled from each matched node:\n\n| Value       | What it returns                                                                                                       |\n| ----------- | --------------------------------------------------------------------------------------------------------------------- |\n| `'content'` | The full rendered text of the node's subtree (default). For a Markdown section, this is everything below the heading. |\n| `'label'`   | Only the node's identifying text: the heading text, key name, or item label.                                          |\n| `'both'`    | The label followed by the content, separated.                                                                         |\n"
      },
      {
        "index": 20,
        "heading": "Simple extraction: a section's content",
        "level": 3,
        "startLine": 258,
        "endLine": 269,
        "lines": 12,
        "content": "### Simple extraction: a section's content\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Purpose$'\n  extract: content\n```\n\nReturns the full text of everything inside the `Purpose` section — paragraphs, lists, everything below the heading.\n"
      },
      {
        "index": 21,
        "heading": "Extracting just the label",
        "level": 3,
        "startLine": 270,
        "endLine": 281,
        "lines": 12,
        "content": "### Extracting just the label\n\n```yaml\nextractor:\n  selector:\n    type: section\n    level: 1\n  extract: label\n```\n\nReturns the heading text of the H1 section — the document title — without any of the section's content.\n"
      },
      {
        "index": 22,
        "heading": "Stripping a prefix from labels",
        "level": 3,
        "startLine": 282,
        "endLine": 299,
        "lines": 18,
        "content": "### Stripping a prefix from labels\n\nWhen labels contain a structured prefix you want to remove:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  extract: label\n  strip: '^Requirement:\\s*'\n```\n\nEach matched label (`Requirement: Authentication`, `Requirement: Session expiry`) has the `Requirement: ` prefix removed, leaving `Authentication` and `Session expiry`.\n"
      },
      {
        "index": 23,
        "heading": "Capturing a portion of the text",
        "level": 3,
        "startLine": 300,
        "endLine": 317,
        "lines": 18,
        "content": "### Capturing a portion of the text\n\n`capture` is a regex with a capture group. Only the captured portion is kept:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Spec Dependencies$'\n  extract: content\n  capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n  transform:\n    name: resolveSpecPath\n    args: ['$2']\n```\n\nThis extracts dependency entries from the `Spec Dependencies` section, capturing the visible label as the primary `value` and the optional `href` as `$2`. `resolveSpecPath` first tries the label itself (for canonical entries like ``[`core:core/config`](../config/spec.md)`` or `` `core:core/config` ``), then falls back to the captured `href` when the label is legacy text. Relative candidates are resolved through repository-backed normalization (`resolveFromPath` + `crossWorkspaceHint` routing), so links like `../../_global/architecture/spec.md` can normalize to canonical IDs such as `default:_global/architecture`.\n"
      },
      {
        "index": 24,
        "heading": "Grouping results by label",
        "level": 3,
        "startLine": 318,
        "endLine": 345,
        "lines": 28,
        "content": "### Grouping results by label\n\n`groupBy: label` groups matched nodes by their label (after `strip` is applied). This is useful when the same heading pattern repeats and you want the results keyed by name:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Requirement:'\n    parent:\n      type: section\n      matches: '^Requirements$'\n  groupBy: label\n  strip: '^Requirement:\\s*'\n  extract: content\n```\n\nInstead of returning a flat array of content strings, this returns an object keyed by stripped label:\n\n```json\n{\n  \"Authentication\": \"Users must be able to log in...\",\n  \"Session expiry\": \"Sessions expire after 30 minutes...\"\n}\n```\n\n---\n"
      },
      {
        "index": 25,
        "heading": "Structured extraction with `fields`",
        "level": 2,
        "startLine": 346,
        "endLine": 428,
        "lines": 83,
        "content": "## Structured extraction with `fields`\n\nFor complex data like BDD scenarios, a single matched node needs to produce a structured object with multiple named fields. The `fields` option does this: each key in `fields` declares how to populate one property of the output object.\n\nEach field mapping can use:\n\n| Field            | Description                                                                                                                                  |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |\n| `from`           | Source of the value: `'label'` (the matched node's label), `'parentLabel'` (the parent node's label), or `'content'` (the rendered subtree). |\n| `childSelector`  | A selector applied within the matched node to find child nodes for this field.                                                               |\n| `capture`        | Regex with a capture group applied to the extracted text.                                                                                    |\n| `strip`          | Regex removed from the extracted text.                                                                                                       |\n| `followSiblings` | Regex matching sibling nodes that follow a `childSelector` match. Enables sequential grouping — see below.                                   |\n\n### Example: extracting BDD scenarios\n\nThe standard schema extracts structured scenario objects from `verify.md` files. Each scenario section (`#### Scenario: ...`) under a requirement section needs to produce an object with `name`, `requirement`, `given`, `when`, and `then` fields.\n\nThe `verify.md` looks like this:\n\n```markdown\n### Requirement: Authentication\n\n#### Scenario: Valid login\n\n- GIVEN the user is on the login page\n- WHEN the user enters valid credentials\n- AND the user submits the form\n- THEN the user is redirected to the dashboard\n- AND a session cookie is set\n```\n\nThe extractor:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Scenario:'\n    parent:\n      type: section\n      matches: '^Requirement:'\n  fields:\n    name:\n      from: label\n      strip: '^Scenario:\\s*'\n    requirement:\n      from: parentLabel\n      strip: '^Requirement:\\s*'\n    given:\n      childSelector: { type: list-item, matches: '^GIVEN\\b' }\n      capture: '^GIVEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    when:\n      childSelector: { type: list-item, matches: '^WHEN\\b' }\n      capture: '^WHEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    then:\n      childSelector: { type: list-item, matches: '^THEN\\b' }\n      capture: '^THEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n```\n\nEach matched `Scenario:` section produces one object. For the example above:\n\n```json\n{\n  \"name\": \"Valid login\",\n  \"requirement\": \"Authentication\",\n  \"given\": [\"the user is on the login page\"],\n  \"when\": [\"the user enters valid credentials\", \"the user submits the form\"],\n  \"then\": [\"the user is redirected to the dashboard\", \"a session cookie is set\"]\n}\n```\n\n### The `followSiblings` pattern\n\n`followSiblings` enables collecting `AND`/`OR` continuation lines that follow a primary clause. When a `childSelector` matches a node (e.g. `WHEN the user enters valid credentials`), `followSiblings` matches the subsequent sibling nodes that belong to that clause (`AND the user submits the form`). The matched siblings are appended to the preceding field's result array.\n\nIf `followSiblings` contains a capture group, only the captured portion is used. Without a capture group, the full node text is returned. The collection stops when a sibling does not match `followSiblings` or when another field's `childSelector` matches.\n\n---\n"
      },
      {
        "index": 26,
        "heading": "Example: extracting BDD scenarios",
        "level": 3,
        "startLine": 360,
        "endLine": 420,
        "lines": 61,
        "content": "### Example: extracting BDD scenarios\n\nThe standard schema extracts structured scenario objects from `verify.md` files. Each scenario section (`#### Scenario: ...`) under a requirement section needs to produce an object with `name`, `requirement`, `given`, `when`, and `then` fields.\n\nThe `verify.md` looks like this:\n\n```markdown\n### Requirement: Authentication\n\n#### Scenario: Valid login\n\n- GIVEN the user is on the login page\n- WHEN the user enters valid credentials\n- AND the user submits the form\n- THEN the user is redirected to the dashboard\n- AND a session cookie is set\n```\n\nThe extractor:\n\n```yaml\nextractor:\n  selector:\n    type: section\n    matches: '^Scenario:'\n    parent:\n      type: section\n      matches: '^Requirement:'\n  fields:\n    name:\n      from: label\n      strip: '^Scenario:\\s*'\n    requirement:\n      from: parentLabel\n      strip: '^Requirement:\\s*'\n    given:\n      childSelector: { type: list-item, matches: '^GIVEN\\b' }\n      capture: '^GIVEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    when:\n      childSelector: { type: list-item, matches: '^WHEN\\b' }\n      capture: '^WHEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n    then:\n      childSelector: { type: list-item, matches: '^THEN\\b' }\n      capture: '^THEN\\s+(.+)'\n      followSiblings: '^(?:AND|OR)\\b'\n```\n\nEach matched `Scenario:` section produces one object. For the example above:\n\n```json\n{\n  \"name\": \"Valid login\",\n  \"requirement\": \"Authentication\",\n  \"given\": [\"the user is on the login page\"],\n  \"when\": [\"the user enters valid credentials\", \"the user submits the form\"],\n  \"then\": [\"the user is redirected to the dashboard\", \"a session cookie is set\"]\n}\n```\n"
      },
      {
        "index": 27,
        "heading": "The `followSiblings` pattern",
        "level": 3,
        "startLine": 421,
        "endLine": 428,
        "lines": 8,
        "content": "### The `followSiblings` pattern\n\n`followSiblings` enables collecting `AND`/`OR` continuation lines that follow a primary clause. When a `childSelector` matches a node (e.g. `WHEN the user enters valid credentials`), `followSiblings` matches the subsequent sibling nodes that belong to that clause (`AND the user submits the form`). The matched siblings are appended to the preceding field's result array.\n\nIf `followSiblings` contains a capture group, only the captured portion is used. Without a capture group, the full node text is returned. The collection stops when a sibling does not match `followSiblings` or when another field's `childSelector` matches.\n\n---\n"
      },
      {
        "index": 28,
        "heading": "Validation rules",
        "level": 2,
        "startLine": 429,
        "endLine": 609,
        "lines": 181,
        "content": "## Validation rules\n\nValidation rules assert structural constraints on artifact content. They appear in `validations` (checked against the artifact) and `deltaValidations` (checked against delta files before application).\n\n### Validation rule fields\n\n| Field            | Type     | Description                                                                                                                                               |\n| ---------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `id`             | string   | Optional identifier for this rule. Useful for error messages and for targeting with overrides.                                                            |\n| `selector`       | selector | Identifies the node(s) to validate.                                                                                                                       |\n| `path`           | string   | JSONPath expression for targeting a value in JSON or YAML artifacts. Alternative to `selector`.                                                           |\n| `required`       | boolean  | Whether the matched node must exist. Defaults to `true`. When `false`, the rule produces a warning rather than a hard failure if the node is absent.      |\n| `count`          | object   | Cardinality and uniqueness constraints over the matched nodes. See [Cardinality](#cardinality-with-count) and [Uniqueness](#uniqueness-with-countunique). |\n| `contentMatches` | string   | Regex the rendered node content must match.                                                                                                               |\n| `children`       | array    | Nested validation rules evaluated against the matched node's children.                                                                                    |\n\n### Cross-field validation in structured artifacts\n\nA powerful feature of `contentMatches` is that it operates on the **full rendered text of the matched node's subtree**. For structured formats like YAML or JSON, this enables **cross-field validation** within a single rule.\n\nWhen you select a `sequence-item` in a delta file, `contentMatches` sees the entire YAML block for that entry, including the `op`, `selector`, and `content` fields. You can use a single regex to enforce logical dependencies between these fields — for example, \"IF the selector targets a Requirement, THEN the content must include a Scenario\":\n\n```yaml\ndeltaValidations:\n  - id: verify-requirement-delta-has-scenario\n    type: sequence-item\n    # Logic: IF ((op:added AND content:### Requirement:) OR (op:modified AND matches:Requirement:))\n    #        THEN MUST have #### Scenario:\n    # We use lookaheads (?=...) with [\\s\\S]* to ensure we match across lines regardless of field order.\n    contentMatches: '^(?:(?!(?=[\\s\\S]*op: added)(?=[\\s\\S]*content:[^]*### Requirement:)|(?=[\\s\\S]*op: modified)(?=[\\s\\S]*matches:[^]*Requirement:))[\\s\\S]*|[\\s\\S]*#### Scenario:[\\s\\S]*)$'\n```\n\n### Asserting a section exists\n\n```yaml\nvalidations:\n  - id: has-purpose\n    selector:\n      type: section\n      matches: '^Purpose$'\n    required: true\n```\n\nFails if no `Purpose` section is found in the artifact.\n\n### Asserting nested structure\n\n`children` rules are evaluated within each node matched by the parent rule. This is how you assert that a section not only exists but also contains the right structure:\n\n```yaml\nvalidations:\n  - id: has-requirements\n    selector:\n      type: section\n      matches: '^Requirements$'\n    required: true\n    children:\n      - id: has-requirement-block\n        selector:\n          type: section\n          matches: '^Requirement:'\n        required: true\n        children:\n          - id: has-scenario\n            selector:\n              type: section\n              matches: '^Scenario:'\n            required: true\n```\n\nThis enforces the full structure: a `Requirements` section must exist, it must contain at least one `Requirement:` subsection, and that subsection must contain at least one `Scenario:` subsection.\n\n### Asserting content with `contentMatches`\n\n```yaml\ndeltaValidations:\n  - id: added-has-scenario\n    selector:\n      type: sequence-item\n      where:\n        op: 'added|modified'\n    contentMatches: '#### Scenario:'\n    required: true\n```\n\nEvery delta entry with `op: added` or `op: modified` must have content that includes a `#### Scenario:` heading. This is how the standard schema enforces that verification scenarios are included when specs change.\n\n### `required: false` for warnings\n\nWhen `required: false`, the absence of a matching node produces a warning rather than blocking validation:\n\n```yaml\nvalidations:\n  - id: has-spec-dependencies\n    selector:\n      type: section\n      matches: '^Spec Dependencies$'\n    required: false\n```\n\nUse this for recommended but not mandatory structure.\n\n### Cardinality with `count`\n\n`required` only handles the zero-match case. Use `count` when you need non-zero cardinality constraints — for example, \"exactly one login requirement\" or \"each requirement must have 1–3 scenarios\":\n\n```yaml\nvalidations:\n  - id: unique-login-requirement\n    selector:\n      type: section\n      matches: '^Requirement: Login$'\n    count:\n      exactly: 1\n\n  - id: requirement-scenarios\n    selector:\n      type: section\n      matches: '^Requirement:'\n    children:\n      - id: scenarios\n        selector:\n          type: section\n          matches: '^Scenario:'\n        count:\n          min: 1\n          max: 3\n```\n\n`exactly` is mutually exclusive with `min`/`max`. `children` rules can also declare their own `count`.\n\n### Uniqueness with `count.unique`\n\nInside `count`, an optional `unique` block rejects selected nodes whose derived keys collide. Keys are extracted via a `by` sub-object using the same `from`/`capture`/`strip` model as cross-artifact participant keys:\n\n```yaml\nvalidations:\n  - id: unique-requirement-ids\n    selector:\n      type: section\n      matches: '^Requirement:'\n    count:\n      min: 1\n      unique:\n        by:\n          from: label\n          strip: '^Requirement:\\s*'\n```\n\nThis selects all `Requirement:` sections and rejects any whose label (after stripping the prefix) is not unique. The validation failure message lists the duplicate keys found.\n\nOptional bounds constrain the number of **unique** keys independently of total count: `exactlyUnique`, `minUnique`, `maxUnique`.\n\n### Cross-artifact validation\n\nBeyond per-artifact rules, schemas can declare **relational** checks across multiple artifacts using the top-level `crossArtifactValidations` array. Each rule extracts comparable key collections from two or more artifacts and compares them with a relation operator (`all-equal`, `subset`, `superset`).\n\n```yaml\ncrossArtifactValidations:\n  - id: mirrored-requirements\n    scope: spec\n    participants:\n      - artifact: specs\n        as: specRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n      - artifact: verify\n        as: verifyRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n    relation:\n      kind: all-equal\n      between: [specRequirements, verifyRequirements]\n```\n\nThis rule ensures `specs` and `verify` expose the same requirement IDs. Rules are evaluated only after all participating artifacts pass their local structural validation. When a participant is not yet ready, the rule is deferred with a warning rather than failing.\n\nFor the full syntax including `keySelector`, ordering options, and relation kinds, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n\n---\n"
      },
      {
        "index": 29,
        "heading": "Validation rule fields",
        "level": 3,
        "startLine": 433,
        "endLine": 444,
        "lines": 12,
        "content": "### Validation rule fields\n\n| Field            | Type     | Description                                                                                                                                               |\n| ---------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `id`             | string   | Optional identifier for this rule. Useful for error messages and for targeting with overrides.                                                            |\n| `selector`       | selector | Identifies the node(s) to validate.                                                                                                                       |\n| `path`           | string   | JSONPath expression for targeting a value in JSON or YAML artifacts. Alternative to `selector`.                                                           |\n| `required`       | boolean  | Whether the matched node must exist. Defaults to `true`. When `false`, the rule produces a warning rather than a hard failure if the node is absent.      |\n| `count`          | object   | Cardinality and uniqueness constraints over the matched nodes. See [Cardinality](#cardinality-with-count) and [Uniqueness](#uniqueness-with-countunique). |\n| `contentMatches` | string   | Regex the rendered node content must match.                                                                                                               |\n| `children`       | array    | Nested validation rules evaluated against the matched node's children.                                                                                    |\n"
      },
      {
        "index": 30,
        "heading": "Cross-field validation in structured artifacts",
        "level": 3,
        "startLine": 445,
        "endLine": 460,
        "lines": 16,
        "content": "### Cross-field validation in structured artifacts\n\nA powerful feature of `contentMatches` is that it operates on the **full rendered text of the matched node's subtree**. For structured formats like YAML or JSON, this enables **cross-field validation** within a single rule.\n\nWhen you select a `sequence-item` in a delta file, `contentMatches` sees the entire YAML block for that entry, including the `op`, `selector`, and `content` fields. You can use a single regex to enforce logical dependencies between these fields — for example, \"IF the selector targets a Requirement, THEN the content must include a Scenario\":\n\n```yaml\ndeltaValidations:\n  - id: verify-requirement-delta-has-scenario\n    type: sequence-item\n    # Logic: IF ((op:added AND content:### Requirement:) OR (op:modified AND matches:Requirement:))\n    #        THEN MUST have #### Scenario:\n    # We use lookaheads (?=...) with [\\s\\S]* to ensure we match across lines regardless of field order.\n    contentMatches: '^(?:(?!(?=[\\s\\S]*op: added)(?=[\\s\\S]*content:[^]*### Requirement:)|(?=[\\s\\S]*op: modified)(?=[\\s\\S]*matches:[^]*Requirement:))[\\s\\S]*|[\\s\\S]*#### Scenario:[\\s\\S]*)$'\n```\n"
      },
      {
        "index": 31,
        "heading": "Asserting a section exists",
        "level": 3,
        "startLine": 461,
        "endLine": 473,
        "lines": 13,
        "content": "### Asserting a section exists\n\n```yaml\nvalidations:\n  - id: has-purpose\n    selector:\n      type: section\n      matches: '^Purpose$'\n    required: true\n```\n\nFails if no `Purpose` section is found in the artifact.\n"
      },
      {
        "index": 32,
        "heading": "Asserting nested structure",
        "level": 3,
        "startLine": 474,
        "endLine": 500,
        "lines": 27,
        "content": "### Asserting nested structure\n\n`children` rules are evaluated within each node matched by the parent rule. This is how you assert that a section not only exists but also contains the right structure:\n\n```yaml\nvalidations:\n  - id: has-requirements\n    selector:\n      type: section\n      matches: '^Requirements$'\n    required: true\n    children:\n      - id: has-requirement-block\n        selector:\n          type: section\n          matches: '^Requirement:'\n        required: true\n        children:\n          - id: has-scenario\n            selector:\n              type: section\n              matches: '^Scenario:'\n            required: true\n```\n\nThis enforces the full structure: a `Requirements` section must exist, it must contain at least one `Requirement:` subsection, and that subsection must contain at least one `Scenario:` subsection.\n"
      },
      {
        "index": 33,
        "heading": "Asserting content with `contentMatches`",
        "level": 3,
        "startLine": 501,
        "endLine": 515,
        "lines": 15,
        "content": "### Asserting content with `contentMatches`\n\n```yaml\ndeltaValidations:\n  - id: added-has-scenario\n    selector:\n      type: sequence-item\n      where:\n        op: 'added|modified'\n    contentMatches: '#### Scenario:'\n    required: true\n```\n\nEvery delta entry with `op: added` or `op: modified` must have content that includes a `#### Scenario:` heading. This is how the standard schema enforces that verification scenarios are included when specs change.\n"
      },
      {
        "index": 34,
        "heading": "`required: false` for warnings",
        "level": 3,
        "startLine": 516,
        "endLine": 530,
        "lines": 15,
        "content": "### `required: false` for warnings\n\nWhen `required: false`, the absence of a matching node produces a warning rather than blocking validation:\n\n```yaml\nvalidations:\n  - id: has-spec-dependencies\n    selector:\n      type: section\n      matches: '^Spec Dependencies$'\n    required: false\n```\n\nUse this for recommended but not mandatory structure.\n"
      },
      {
        "index": 35,
        "heading": "Cardinality with `count`",
        "level": 3,
        "startLine": 531,
        "endLine": 559,
        "lines": 29,
        "content": "### Cardinality with `count`\n\n`required` only handles the zero-match case. Use `count` when you need non-zero cardinality constraints — for example, \"exactly one login requirement\" or \"each requirement must have 1–3 scenarios\":\n\n```yaml\nvalidations:\n  - id: unique-login-requirement\n    selector:\n      type: section\n      matches: '^Requirement: Login$'\n    count:\n      exactly: 1\n\n  - id: requirement-scenarios\n    selector:\n      type: section\n      matches: '^Requirement:'\n    children:\n      - id: scenarios\n        selector:\n          type: section\n          matches: '^Scenario:'\n        count:\n          min: 1\n          max: 3\n```\n\n`exactly` is mutually exclusive with `min`/`max`. `children` rules can also declare their own `count`.\n"
      },
      {
        "index": 36,
        "heading": "Uniqueness with `count.unique`",
        "level": 3,
        "startLine": 560,
        "endLine": 581,
        "lines": 22,
        "content": "### Uniqueness with `count.unique`\n\nInside `count`, an optional `unique` block rejects selected nodes whose derived keys collide. Keys are extracted via a `by` sub-object using the same `from`/`capture`/`strip` model as cross-artifact participant keys:\n\n```yaml\nvalidations:\n  - id: unique-requirement-ids\n    selector:\n      type: section\n      matches: '^Requirement:'\n    count:\n      min: 1\n      unique:\n        by:\n          from: label\n          strip: '^Requirement:\\s*'\n```\n\nThis selects all `Requirement:` sections and rejects any whose label (after stripping the prefix) is not unique. The validation failure message lists the duplicate keys found.\n\nOptional bounds constrain the number of **unique** keys independently of total count: `exactlyUnique`, `minUnique`, `maxUnique`.\n"
      },
      {
        "index": 37,
        "heading": "Cross-artifact validation",
        "level": 3,
        "startLine": 582,
        "endLine": 609,
        "lines": 28,
        "content": "### Cross-artifact validation\n\nBeyond per-artifact rules, schemas can declare **relational** checks across multiple artifacts using the top-level `crossArtifactValidations` array. Each rule extracts comparable key collections from two or more artifacts and compares them with a relation operator (`all-equal`, `subset`, `superset`).\n\n```yaml\ncrossArtifactValidations:\n  - id: mirrored-requirements\n    scope: spec\n    participants:\n      - artifact: specs\n        as: specRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n      - artifact: verify\n        as: verifyRequirements\n        selector: { type: section, matches: '^Requirement:' }\n        key: { from: label, strip: '^Requirement:\\\\s*' }\n    relation:\n      kind: all-equal\n      between: [specRequirements, verifyRequirements]\n```\n\nThis rule ensures `specs` and `verify` expose the same requirement IDs. Rules are evaluated only after all participating artifacts pass their local structural validation. When a participant is not yet ready, the rule is deferred with a warning rather than failing.\n\nFor the full syntax including `keySelector`, ordering options, and relation kinds, see the [schema format reference](../schemas/schema-format.md#crossartifactvalidations).\n\n---\n"
      },
      {
        "index": 38,
        "heading": "Metadata extraction configuration",
        "level": 2,
        "startLine": 610,
        "endLine": 733,
        "lines": 124,
        "content": "## Metadata extraction configuration\n\n`metadataExtraction` is a top-level field in `schema.yaml` that wires extractors to named metadata categories. SpecD uses these declarations to pull structured information from artifact files — for context compilation, impact analysis, and tooling.\n\nThe configuration is keyed by category name:\n\n**Scalar categories** (single extractor entry):\n\n| Category      | Description                                           |\n| ------------- | ----------------------------------------------------- |\n| `title`       | The spec title, typically the H1 heading.             |\n| `description` | A prose description, typically the Purpose section.   |\n| `dependsOn`   | Dependency spec paths extracted from link references. |\n| `keywords`    | Keyword terms.                                        |\n\n**Array categories** (one or more extractor entries):\n\n| Category      | Description                                           |\n| ------------- | ----------------------------------------------------- |\n| `rules`       | Structured rule groups extracted from the spec.       |\n| `constraints` | Constraint strings.                                   |\n| `scenarios`   | Structured scenario objects from the verify artifact. |\n| `context`     | Always-included context content.                      |\n\nEach entry has three fields:\n\n| Field       | Required | Description                                                                                |\n| ----------- | -------- | ------------------------------------------------------------------------------------------ |\n| `id`        | no       | Identifier for this entry. Used by `schemaOverrides` to target the entry for modification. |\n| `artifact`  | yes      | The artifact type ID to extract from (e.g. `'specs'`, `'verify'`).                         |\n| `extractor` | yes      | The extraction configuration — a `selector` plus post-processing fields.                   |\n\n### Complete example: the standard schema's `metadataExtraction`\n\nThis is the full `metadataExtraction` configuration from `schema-std`, with explanations:\n\n```yaml\nmetadataExtraction:\n  # Pull the spec title from the H1 heading — label only, no content\n  title:\n    artifact: specs\n    extractor:\n      selector: { type: section, level: 1 }\n      extract: label\n\n  # Pull the spec description from the Purpose section's content\n  description:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Purpose$' }\n      extract: content\n\n  # Extract dependency spec paths from Markdown links in Spec Dependencies\n  # capture pulls only the link targets, transform normalises them to spec IDs\n  dependsOn:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Spec Dependencies$' }\n      extract: content\n      capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n      transform:\n        name: resolveSpecPath\n        args: ['$2']\n\n  # Extract all Requirement sections, keyed by requirement name\n  # strip removes the \"Requirement: \" prefix from each key\n  rules:\n    - id: spec-requirements\n      artifact: specs\n      extractor:\n        selector:\n          type: section\n          matches: '^Requirement:'\n          parent: { type: section, matches: '^Requirements$' }\n        groupBy: label\n        strip: '^Requirement:\\s*'\n        extract: content\n\n  # Extract bullet points from the Constraints section\n  constraints:\n    - id: spec-constraints\n      artifact: specs\n      extractor:\n        selector:\n          type: list-item\n          parent: { type: section, matches: '^Constraints$' }\n        extract: label\n\n  # Extract structured scenario objects from the verify artifact\n  scenarios:\n    - id: verify-scenarios\n      artifact: verify\n      extractor:\n        selector:\n          type: section\n          matches: '^Scenario:'\n          parent: { type: section, matches: '^Requirement:' }\n        fields:\n          name: { from: label, strip: '^Scenario:\\s*' }\n          requirement: { from: parentLabel, strip: '^Requirement:\\s*' }\n          given:\n            childSelector: { type: list-item, matches: '^GIVEN\\b' }\n            capture: '^GIVEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          when:\n            childSelector: { type: list-item, matches: '^WHEN\\b' }\n            capture: '^WHEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          then:\n            childSelector: { type: list-item, matches: '^THEN\\b' }\n            capture: '^THEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n\n  # Always include the Purpose section in compiled context\n  context:\n    - id: spec-overview\n      artifact: specs\n      extractor:\n        selector: { type: section, matches: '^Purpose$' }\n        extract: content\n```\n\n---\n"
      },
      {
        "index": 39,
        "heading": "Complete example: the standard schema's `metadataExtraction`",
        "level": 3,
        "startLine": 642,
        "endLine": 733,
        "lines": 92,
        "content": "### Complete example: the standard schema's `metadataExtraction`\n\nThis is the full `metadataExtraction` configuration from `schema-std`, with explanations:\n\n```yaml\nmetadataExtraction:\n  # Pull the spec title from the H1 heading — label only, no content\n  title:\n    artifact: specs\n    extractor:\n      selector: { type: section, level: 1 }\n      extract: label\n\n  # Pull the spec description from the Purpose section's content\n  description:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Purpose$' }\n      extract: content\n\n  # Extract dependency spec paths from Markdown links in Spec Dependencies\n  # capture pulls only the link targets, transform normalises them to spec IDs\n  dependsOn:\n    artifact: specs\n    extractor:\n      selector: { type: section, matches: '^Spec Dependencies$' }\n      extract: content\n      capture: '(?:^|\\n)\\s*-\\s+(?:\\[`?|`)?([^`\\]\\n]+?)(?:(?:`?\\]\\(([^)]+)\\)|`)|(?=\\s*(?:—|$)))'\n      transform:\n        name: resolveSpecPath\n        args: ['$2']\n\n  # Extract all Requirement sections, keyed by requirement name\n  # strip removes the \"Requirement: \" prefix from each key\n  rules:\n    - id: spec-requirements\n      artifact: specs\n      extractor:\n        selector:\n          type: section\n          matches: '^Requirement:'\n          parent: { type: section, matches: '^Requirements$' }\n        groupBy: label\n        strip: '^Requirement:\\s*'\n        extract: content\n\n  # Extract bullet points from the Constraints section\n  constraints:\n    - id: spec-constraints\n      artifact: specs\n      extractor:\n        selector:\n          type: list-item\n          parent: { type: section, matches: '^Constraints$' }\n        extract: label\n\n  # Extract structured scenario objects from the verify artifact\n  scenarios:\n    - id: verify-scenarios\n      artifact: verify\n      extractor:\n        selector:\n          type: section\n          matches: '^Scenario:'\n          parent: { type: section, matches: '^Requirement:' }\n        fields:\n          name: { from: label, strip: '^Scenario:\\s*' }\n          requirement: { from: parentLabel, strip: '^Requirement:\\s*' }\n          given:\n            childSelector: { type: list-item, matches: '^GIVEN\\b' }\n            capture: '^GIVEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          when:\n            childSelector: { type: list-item, matches: '^WHEN\\b' }\n            capture: '^WHEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n          then:\n            childSelector: { type: list-item, matches: '^THEN\\b' }\n            capture: '^THEN\\s+(.+)'\n            followSiblings: '^(?:AND|OR)\\b'\n\n  # Always include the Purpose section in compiled context\n  context:\n    - id: spec-overview\n      artifact: specs\n      extractor:\n        selector: { type: section, matches: '^Purpose$' }\n        extract: content\n```\n\n---\n"
      },
      {
        "index": 40,
        "heading": "Where to go next",
        "level": 2,
        "startLine": 734,
        "endLine": 739,
        "lines": 6,
        "content": "## Where to go next\n\n- [Schema format reference](../schemas/schema-format.md) — complete technical reference for `schema.yaml`, including `validations`, `deltaValidations`, and `metadataExtraction` field-by-field\n- [Validations and delta validations example](../schemas/examples/validations-and-delta-validations.md) — annotated examples showing selector patterns in validation rules\n- [Delta files example](../schemas/examples/delta-files.md) — how selectors are used inside delta file entries to target nodes for modification\n"
      }
    ]
  },
  {
    "topic": "artifacts",
    "title": "Artifacts & Templates",
    "description": "What artifacts are in SpecD, the artifact dependency DAG, drift tracking, and template authoring.",
    "order": 8,
    "content": "# Artifacts & Templates\n\nIn SpecD, **artifacts** are the concrete, typed deliverables produced during an active change. Rather than treating development as an informal chat or an unorganized series of code edits, SpecD models work as a deliberate progression through well-defined documents: proposals, living specifications, verification scenarios, architecture designs, and task checklists.\n\nEvery artifact in SpecD is defined and validated by the project's **schema**.\n\n---\n\n## 1. What is an Artifact?\n\nAn artifact is a versioned deliverable created inside an active change folder (`.specd/changes/<change-name>/`).\n\nEach artifact possesses:\n\n- **An Identifier (`id`)**: A unique name within the schema (e.g. `proposal`, `specs`, `verify`, `design`, `tasks`).\n- **A Scope (`scope`)**:\n  - `scope: change` — A single document for the entire change, located at the root of the change folder (e.g. `proposal.md`, `design.md`, `tasks.md`).\n  - `scope: spec` — One document per targeted specification, located in capability subdirectories (e.g. `specs/<workspace>/<capability>/spec.md` for new specs, or `deltas/<workspace>/<capability>/spec.md.delta.yaml` for existing specs).\n- **A Output Location (`output`)**: The file path pattern where the artifact is written on disk.\n- **A Scaffolding Template (`template`)**: An initial Markdown template file on disk that provides structural headings and guidance comments when the artifact is first created.\n- **Prompt Instructions (`instruction`)**: Contextual instructions injected into the AI agent's prompt when authoring or revising that artifact.\n- **Validation Rules (`rules`)**: Pre- and post-validation checks (such as confirming all open questions are resolved or that task lists are properly formatted).\n\n---\n\n## 2. The Artifact Dependency DAG\n\nArtifacts are not authored in arbitrary order. They form an explicit **Directed Acyclic Graph (DAG)** governed by the `requires` field in the schema:\n\n```\nproposal ──► specs ──► verify ──► design ──► tasks\n```\n\nYou cannot author a `design.md` until the requirements in `spec.md` and `verify.md` are defined and validated. Likewise, you cannot write an actionable `tasks.md` checklist until the architecture and symbol impact are planned in `design.md`.\n\n### Drift Detection & Invalidation\n\nSpecD computes cryptographic SHA-256 hashes of all artifacts. If an author or reviewer goes back and modifies an upstream artifact (for example, revising `proposal.md` after `design.md` was already drafted), SpecD automatically detects the discrepancy:\n\n1. **Drift Warning**: Upstream modifications flag downstream artifacts as `drifted`.\n2. **Lifecycle Gate Protection**: SpecD blocks lifecycle transitions (such as moving to `implementing` or `archivable`) until drifted artifacts are reviewed and re-validated.\n3. **Traceability**: Changes never silently drift out of sync with the specifications that govern them.\n\n---\n\n## 3. How Artifacts are Declared in a Schema\n\nIn a `schema.yaml` file (or in local custom schemas), artifacts are declared under the `artifacts` key:\n\n```yaml\nartifacts:\n  - id: proposal\n    scope: change\n    output: proposal.md\n    description: Initial proposal outlining why the change is needed\n    template: templates/proposal.md\n    requires: []\n    instruction: |\n      Create the proposal document that establishes WHY this change is needed.\n      Address the problem statement, affected specs, and technical context.\n    rules:\n      post:\n        - id: resolve-open-questions\n          instruction: |\n            Review the \"Open questions\" section. Any question affecting downstream\n            artifacts must be resolved before proceeding.\n\n  - id: specs\n    scope: spec\n    output: 'specs/**/spec.md'\n    description: Detailed requirements defining what the system should do\n    template: templates/spec.md\n    requires:\n      - proposal\n    instruction: |\n      Draft the normative specification using RFC 2119 keywords (SHALL, MUST, SHOULD).\n```\n\n### Key properties\n\n| Field              | Type                 | Description                                                                            |\n| :----------------- | :------------------- | :------------------------------------------------------------------------------------- |\n| `id`               | `string`             | Unique artifact identifier (e.g. `proposal`, `design`).                                |\n| `scope`            | `'change' \\| 'spec'` | Whether the artifact is change-level or per-spec.                                      |\n| `output`           | `string`             | File path or glob pattern relative to the change directory.                            |\n| `description`      | `string`             | Human-readable explanation of the artifact's purpose.                                  |\n| `template`         | `string` (optional)  | Relative file path to the template file on disk.                                       |\n| `requires`         | `string[]`           | List of upstream artifact IDs that must be valid before this artifact can be authored. |\n| `instruction`      | `string` (optional)  | Prompt instructions injected into AI agent context.                                    |\n| `deltaInstruction` | `string` (optional)  | Alternate instructions used when authoring a `.delta.yaml` instead of a new file.      |\n| `rules`            | `object` (optional)  | Validation rules (`pre` and `post`) executed during artifact lifecycle checks.         |\n\n---\n\n## 4. Artifact Templates\n\nArtifact templates provide scaffolding when an AI agent or developer creates an artifact for the first time. They enforce consistent formatting, provide embedded guidance, and eliminate blank-canvas ambiguity.\n\n### Template Storage as Files\n\nIn SpecD, artifact templates are **external files stored on disk** relative to the schema directory (or repository root):\n\n```\nmy-schema/\n├── schema.yaml\n└── templates/\n    ├── proposal.md\n    ├── spec.md\n    ├── verify.md\n    ├── design.md\n    └── tasks.md\n```\n\nIn `schema.yaml`, the `template` field points to the file path:\n\n```yaml\ntemplate: templates/proposal.md\n```\n\nSpecD reads the file from disk when initializing the artifact. Template files are plain Markdown and are versioned directly alongside your schema.\n\n### Embedded Agent Guidance\n\nTemplates in `@specd/schema-std` make extensive use of HTML comments (`<!-- ... -->`). These comments serve as **in-situ instructions for AI coding assistants**:\n\n```markdown\n# Proposal: {{change.name}}\n\n## Motivation\n\n<!--\n1-2 sentences on the problem or opportunity.\nWhat is broken or missing? Why now? Focus strictly on WHY, not HOW.\n-->\n\n## Proposed Solution\n\n<!--\nHigh-level description of what we want to build or change.\nOutline the concrete approach and the minimal necessary changes.\n-->\n```\n\nWhen an AI agent scaffolds the document, it reads the guidance comments to understand what belongs in each section, what traps to avoid, and how deep the analysis should be. Agents are instructed not to copy the guidance comments into the final output.\n\n---\n\n## 5. The Standard Schema Artifact Pipeline (`@specd/schema-std`)\n\nThe default schema shipped with SpecD (`@specd/schema-std`) defines five core artifacts:\n\n### 1. `proposal.md` (`scope: change`)\n\nEstablishes **why** the change is needed.\n\n- **Sections**: Motivation, Current Behavior, Proposed Solution, Specs Affected (new vs modified), Technical Context, and Open Questions.\n- **Role**: Serves as the initial alignment contract before any specification work begins.\n\n### 2. `spec.md` (`scope: spec`)\n\nEstablishes **what** the system shall do.\n\n- **Sections**: Purpose, Requirements (`### Requirement: <Title>`), Constraints, and Spec Dependencies.\n- **Role**: Durable, living contract. Uses RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`) and omits implementation or testing details.\n\n### 3. `verify.md` (`scope: spec`)\n\nEstablishes **how to verify** that requirements are satisfied.\n\n- **Sections**: Verification Scenarios (`#### Scenario: <Name>`) structured as **GIVEN / WHEN / THEN** test cases.\n- **Role**: Acceptance criteria paired 1:1 with requirements from `spec.md`.\n\n### 4. `design.md` (`scope: change`)\n\nEstablishes **how** the capability will be implemented in code.\n\n- **Sections**: Architectural Context, Symbols to Create/Modify, Blast Radius Analysis, Algorithms, Decision Records (ADRs), and Test Strategy.\n- **Role**: Implementation contract for the developer or agent before writing code.\n\n### 5. `tasks.md` (`scope: change`)\n\nEstablishes the **execution plan**.\n\n- **Sections**: Checklists (`- [ ]`) grouped by component or phase.\n- **Role**: Tracks granular implementation progress through the `implementing` lifecycle state.\n\n---\n\n## 6. Customizing Artifacts in `specd.yaml`\n\nYou can declare new artifact types or override existing templates in `specd.yaml` using `schemaOverrides`:\n\n### Adding a Custom Artifact\n\nTo add a new deliverable (e.g. a database migration plan or OpenAPI contract):\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: migration-plan\n        scope: change\n        output: migration.md\n        description: Database migration and rollback strategy\n        template: .specd/templates/migration.md\n        requires:\n          - design\n        instruction: |\n          Detail database schema changes, rollback scripts, and zero-downtime execution steps.\n```\n\n### Overriding an Existing Template\n\nTo replace an artifact's template with a company-specific format:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  set:\n    artifacts:\n      - id: proposal\n        template: .specd/templates/enterprise-proposal.md\n```\n\n### Removing a Template\n\nTo have an artifact start as an empty file without initial scaffolding:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  remove:\n    artifacts:\n      - id: tasks\n        template: null\n```\n\n---\n\n## Where to go next\n\n- [Changes Guide](./changes.md) — How changes manage artifacts and track state transitions.\n- [Workflow Guide](./workflow.md) — The lifecycle states (`designing`, `implementing`, `verifying`, `archiving`).\n- [Custom Schemas Guide](./custom-schemas.md) — Authoring custom schemas and schema plugins.\n- [Schema Format Reference](../schemas/schema-format.md) — Full technical reference for all artifact and schema properties.\n",
    "lineCount": 247,
    "byteLength": 10373,
    "outline": [
      {
        "index": 1,
        "heading": "Artifacts & Templates",
        "level": 1,
        "startLine": 1,
        "endLine": 247,
        "lines": 247,
        "content": "# Artifacts & Templates\n\nIn SpecD, **artifacts** are the concrete, typed deliverables produced during an active change. Rather than treating development as an informal chat or an unorganized series of code edits, SpecD models work as a deliberate progression through well-defined documents: proposals, living specifications, verification scenarios, architecture designs, and task checklists.\n\nEvery artifact in SpecD is defined and validated by the project's **schema**.\n\n---\n\n## 1. What is an Artifact?\n\nAn artifact is a versioned deliverable created inside an active change folder (`.specd/changes/<change-name>/`).\n\nEach artifact possesses:\n\n- **An Identifier (`id`)**: A unique name within the schema (e.g. `proposal`, `specs`, `verify`, `design`, `tasks`).\n- **A Scope (`scope`)**:\n  - `scope: change` — A single document for the entire change, located at the root of the change folder (e.g. `proposal.md`, `design.md`, `tasks.md`).\n  - `scope: spec` — One document per targeted specification, located in capability subdirectories (e.g. `specs/<workspace>/<capability>/spec.md` for new specs, or `deltas/<workspace>/<capability>/spec.md.delta.yaml` for existing specs).\n- **A Output Location (`output`)**: The file path pattern where the artifact is written on disk.\n- **A Scaffolding Template (`template`)**: An initial Markdown template file on disk that provides structural headings and guidance comments when the artifact is first created.\n- **Prompt Instructions (`instruction`)**: Contextual instructions injected into the AI agent's prompt when authoring or revising that artifact.\n- **Validation Rules (`rules`)**: Pre- and post-validation checks (such as confirming all open questions are resolved or that task lists are properly formatted).\n\n---\n\n## 2. The Artifact Dependency DAG\n\nArtifacts are not authored in arbitrary order. They form an explicit **Directed Acyclic Graph (DAG)** governed by the `requires` field in the schema:\n\n```\nproposal ──► specs ──► verify ──► design ──► tasks\n```\n\nYou cannot author a `design.md` until the requirements in `spec.md` and `verify.md` are defined and validated. Likewise, you cannot write an actionable `tasks.md` checklist until the architecture and symbol impact are planned in `design.md`.\n\n### Drift Detection & Invalidation\n\nSpecD computes cryptographic SHA-256 hashes of all artifacts. If an author or reviewer goes back and modifies an upstream artifact (for example, revising `proposal.md` after `design.md` was already drafted), SpecD automatically detects the discrepancy:\n\n1. **Drift Warning**: Upstream modifications flag downstream artifacts as `drifted`.\n2. **Lifecycle Gate Protection**: SpecD blocks lifecycle transitions (such as moving to `implementing` or `archivable`) until drifted artifacts are reviewed and re-validated.\n3. **Traceability**: Changes never silently drift out of sync with the specifications that govern them.\n\n---\n\n## 3. How Artifacts are Declared in a Schema\n\nIn a `schema.yaml` file (or in local custom schemas), artifacts are declared under the `artifacts` key:\n\n```yaml\nartifacts:\n  - id: proposal\n    scope: change\n    output: proposal.md\n    description: Initial proposal outlining why the change is needed\n    template: templates/proposal.md\n    requires: []\n    instruction: |\n      Create the proposal document that establishes WHY this change is needed.\n      Address the problem statement, affected specs, and technical context.\n    rules:\n      post:\n        - id: resolve-open-questions\n          instruction: |\n            Review the \"Open questions\" section. Any question affecting downstream\n            artifacts must be resolved before proceeding.\n\n  - id: specs\n    scope: spec\n    output: 'specs/**/spec.md'\n    description: Detailed requirements defining what the system should do\n    template: templates/spec.md\n    requires:\n      - proposal\n    instruction: |\n      Draft the normative specification using RFC 2119 keywords (SHALL, MUST, SHOULD).\n```\n\n### Key properties\n\n| Field              | Type                 | Description                                                                            |\n| :----------------- | :------------------- | :------------------------------------------------------------------------------------- |\n| `id`               | `string`             | Unique artifact identifier (e.g. `proposal`, `design`).                                |\n| `scope`            | `'change' \\| 'spec'` | Whether the artifact is change-level or per-spec.                                      |\n| `output`           | `string`             | File path or glob pattern relative to the change directory.                            |\n| `description`      | `string`             | Human-readable explanation of the artifact's purpose.                                  |\n| `template`         | `string` (optional)  | Relative file path to the template file on disk.                                       |\n| `requires`         | `string[]`           | List of upstream artifact IDs that must be valid before this artifact can be authored. |\n| `instruction`      | `string` (optional)  | Prompt instructions injected into AI agent context.                                    |\n| `deltaInstruction` | `string` (optional)  | Alternate instructions used when authoring a `.delta.yaml` instead of a new file.      |\n| `rules`            | `object` (optional)  | Validation rules (`pre` and `post`) executed during artifact lifecycle checks.         |\n\n---\n\n## 4. Artifact Templates\n\nArtifact templates provide scaffolding when an AI agent or developer creates an artifact for the first time. They enforce consistent formatting, provide embedded guidance, and eliminate blank-canvas ambiguity.\n\n### Template Storage as Files\n\nIn SpecD, artifact templates are **external files stored on disk** relative to the schema directory (or repository root):\n\n```\nmy-schema/\n├── schema.yaml\n└── templates/\n    ├── proposal.md\n    ├── spec.md\n    ├── verify.md\n    ├── design.md\n    └── tasks.md\n```\n\nIn `schema.yaml`, the `template` field points to the file path:\n\n```yaml\ntemplate: templates/proposal.md\n```\n\nSpecD reads the file from disk when initializing the artifact. Template files are plain Markdown and are versioned directly alongside your schema.\n\n### Embedded Agent Guidance\n\nTemplates in `@specd/schema-std` make extensive use of HTML comments (`<!-- ... -->`). These comments serve as **in-situ instructions for AI coding assistants**:\n\n```markdown\n# Proposal: {{change.name}}\n\n## Motivation\n\n<!--\n1-2 sentences on the problem or opportunity.\nWhat is broken or missing? Why now? Focus strictly on WHY, not HOW.\n-->\n\n## Proposed Solution\n\n<!--\nHigh-level description of what we want to build or change.\nOutline the concrete approach and the minimal necessary changes.\n-->\n```\n\nWhen an AI agent scaffolds the document, it reads the guidance comments to understand what belongs in each section, what traps to avoid, and how deep the analysis should be. Agents are instructed not to copy the guidance comments into the final output.\n\n---\n\n## 5. The Standard Schema Artifact Pipeline (`@specd/schema-std`)\n\nThe default schema shipped with SpecD (`@specd/schema-std`) defines five core artifacts:\n\n### 1. `proposal.md` (`scope: change`)\n\nEstablishes **why** the change is needed.\n\n- **Sections**: Motivation, Current Behavior, Proposed Solution, Specs Affected (new vs modified), Technical Context, and Open Questions.\n- **Role**: Serves as the initial alignment contract before any specification work begins.\n\n### 2. `spec.md` (`scope: spec`)\n\nEstablishes **what** the system shall do.\n\n- **Sections**: Purpose, Requirements (`### Requirement: <Title>`), Constraints, and Spec Dependencies.\n- **Role**: Durable, living contract. Uses RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`) and omits implementation or testing details.\n\n### 3. `verify.md` (`scope: spec`)\n\nEstablishes **how to verify** that requirements are satisfied.\n\n- **Sections**: Verification Scenarios (`#### Scenario: <Name>`) structured as **GIVEN / WHEN / THEN** test cases.\n- **Role**: Acceptance criteria paired 1:1 with requirements from `spec.md`.\n\n### 4. `design.md` (`scope: change`)\n\nEstablishes **how** the capability will be implemented in code.\n\n- **Sections**: Architectural Context, Symbols to Create/Modify, Blast Radius Analysis, Algorithms, Decision Records (ADRs), and Test Strategy.\n- **Role**: Implementation contract for the developer or agent before writing code.\n\n### 5. `tasks.md` (`scope: change`)\n\nEstablishes the **execution plan**.\n\n- **Sections**: Checklists (`- [ ]`) grouped by component or phase.\n- **Role**: Tracks granular implementation progress through the `implementing` lifecycle state.\n\n---\n\n## 6. Customizing Artifacts in `specd.yaml`\n\nYou can declare new artifact types or override existing templates in `specd.yaml` using `schemaOverrides`:\n\n### Adding a Custom Artifact\n\nTo add a new deliverable (e.g. a database migration plan or OpenAPI contract):\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: migration-plan\n        scope: change\n        output: migration.md\n        description: Database migration and rollback strategy\n        template: .specd/templates/migration.md\n        requires:\n          - design\n        instruction: |\n          Detail database schema changes, rollback scripts, and zero-downtime execution steps.\n```\n\n### Overriding an Existing Template\n\nTo replace an artifact's template with a company-specific format:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  set:\n    artifacts:\n      - id: proposal\n        template: .specd/templates/enterprise-proposal.md\n```\n\n### Removing a Template\n\nTo have an artifact start as an empty file without initial scaffolding:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  remove:\n    artifacts:\n      - id: tasks\n        template: null\n```\n\n---\n\n## Where to go next\n\n- [Changes Guide](./changes.md) — How changes manage artifacts and track state transitions.\n- [Workflow Guide](./workflow.md) — The lifecycle states (`designing`, `implementing`, `verifying`, `archiving`).\n- [Custom Schemas Guide](./custom-schemas.md) — Authoring custom schemas and schema plugins.\n- [Schema Format Reference](../schemas/schema-format.md) — Full technical reference for all artifact and schema properties.\n"
      },
      {
        "index": 2,
        "heading": "1. What is an Artifact?",
        "level": 2,
        "startLine": 9,
        "endLine": 25,
        "lines": 17,
        "content": "## 1. What is an Artifact?\n\nAn artifact is a versioned deliverable created inside an active change folder (`.specd/changes/<change-name>/`).\n\nEach artifact possesses:\n\n- **An Identifier (`id`)**: A unique name within the schema (e.g. `proposal`, `specs`, `verify`, `design`, `tasks`).\n- **A Scope (`scope`)**:\n  - `scope: change` — A single document for the entire change, located at the root of the change folder (e.g. `proposal.md`, `design.md`, `tasks.md`).\n  - `scope: spec` — One document per targeted specification, located in capability subdirectories (e.g. `specs/<workspace>/<capability>/spec.md` for new specs, or `deltas/<workspace>/<capability>/spec.md.delta.yaml` for existing specs).\n- **A Output Location (`output`)**: The file path pattern where the artifact is written on disk.\n- **A Scaffolding Template (`template`)**: An initial Markdown template file on disk that provides structural headings and guidance comments when the artifact is first created.\n- **Prompt Instructions (`instruction`)**: Contextual instructions injected into the AI agent's prompt when authoring or revising that artifact.\n- **Validation Rules (`rules`)**: Pre- and post-validation checks (such as confirming all open questions are resolved or that task lists are properly formatted).\n\n---\n"
      },
      {
        "index": 3,
        "heading": "2. The Artifact Dependency DAG",
        "level": 2,
        "startLine": 26,
        "endLine": 45,
        "lines": 20,
        "content": "## 2. The Artifact Dependency DAG\n\nArtifacts are not authored in arbitrary order. They form an explicit **Directed Acyclic Graph (DAG)** governed by the `requires` field in the schema:\n\n```\nproposal ──► specs ──► verify ──► design ──► tasks\n```\n\nYou cannot author a `design.md` until the requirements in `spec.md` and `verify.md` are defined and validated. Likewise, you cannot write an actionable `tasks.md` checklist until the architecture and symbol impact are planned in `design.md`.\n\n### Drift Detection & Invalidation\n\nSpecD computes cryptographic SHA-256 hashes of all artifacts. If an author or reviewer goes back and modifies an upstream artifact (for example, revising `proposal.md` after `design.md` was already drafted), SpecD automatically detects the discrepancy:\n\n1. **Drift Warning**: Upstream modifications flag downstream artifacts as `drifted`.\n2. **Lifecycle Gate Protection**: SpecD blocks lifecycle transitions (such as moving to `implementing` or `archivable`) until drifted artifacts are reviewed and re-validated.\n3. **Traceability**: Changes never silently drift out of sync with the specifications that govern them.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "Drift Detection & Invalidation",
        "level": 3,
        "startLine": 36,
        "endLine": 45,
        "lines": 10,
        "content": "### Drift Detection & Invalidation\n\nSpecD computes cryptographic SHA-256 hashes of all artifacts. If an author or reviewer goes back and modifies an upstream artifact (for example, revising `proposal.md` after `design.md` was already drafted), SpecD automatically detects the discrepancy:\n\n1. **Drift Warning**: Upstream modifications flag downstream artifacts as `drifted`.\n2. **Lifecycle Gate Protection**: SpecD blocks lifecycle transitions (such as moving to `implementing` or `archivable`) until drifted artifacts are reviewed and re-validated.\n3. **Traceability**: Changes never silently drift out of sync with the specifications that govern them.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "3. How Artifacts are Declared in a Schema",
        "level": 2,
        "startLine": 46,
        "endLine": 94,
        "lines": 49,
        "content": "## 3. How Artifacts are Declared in a Schema\n\nIn a `schema.yaml` file (or in local custom schemas), artifacts are declared under the `artifacts` key:\n\n```yaml\nartifacts:\n  - id: proposal\n    scope: change\n    output: proposal.md\n    description: Initial proposal outlining why the change is needed\n    template: templates/proposal.md\n    requires: []\n    instruction: |\n      Create the proposal document that establishes WHY this change is needed.\n      Address the problem statement, affected specs, and technical context.\n    rules:\n      post:\n        - id: resolve-open-questions\n          instruction: |\n            Review the \"Open questions\" section. Any question affecting downstream\n            artifacts must be resolved before proceeding.\n\n  - id: specs\n    scope: spec\n    output: 'specs/**/spec.md'\n    description: Detailed requirements defining what the system should do\n    template: templates/spec.md\n    requires:\n      - proposal\n    instruction: |\n      Draft the normative specification using RFC 2119 keywords (SHALL, MUST, SHOULD).\n```\n\n### Key properties\n\n| Field              | Type                 | Description                                                                            |\n| :----------------- | :------------------- | :------------------------------------------------------------------------------------- |\n| `id`               | `string`             | Unique artifact identifier (e.g. `proposal`, `design`).                                |\n| `scope`            | `'change' \\| 'spec'` | Whether the artifact is change-level or per-spec.                                      |\n| `output`           | `string`             | File path or glob pattern relative to the change directory.                            |\n| `description`      | `string`             | Human-readable explanation of the artifact's purpose.                                  |\n| `template`         | `string` (optional)  | Relative file path to the template file on disk.                                       |\n| `requires`         | `string[]`           | List of upstream artifact IDs that must be valid before this artifact can be authored. |\n| `instruction`      | `string` (optional)  | Prompt instructions injected into AI agent context.                                    |\n| `deltaInstruction` | `string` (optional)  | Alternate instructions used when authoring a `.delta.yaml` instead of a new file.      |\n| `rules`            | `object` (optional)  | Validation rules (`pre` and `post`) executed during artifact lifecycle checks.         |\n\n---\n"
      },
      {
        "index": 6,
        "heading": "Key properties",
        "level": 3,
        "startLine": 79,
        "endLine": 94,
        "lines": 16,
        "content": "### Key properties\n\n| Field              | Type                 | Description                                                                            |\n| :----------------- | :------------------- | :------------------------------------------------------------------------------------- |\n| `id`               | `string`             | Unique artifact identifier (e.g. `proposal`, `design`).                                |\n| `scope`            | `'change' \\| 'spec'` | Whether the artifact is change-level or per-spec.                                      |\n| `output`           | `string`             | File path or glob pattern relative to the change directory.                            |\n| `description`      | `string`             | Human-readable explanation of the artifact's purpose.                                  |\n| `template`         | `string` (optional)  | Relative file path to the template file on disk.                                       |\n| `requires`         | `string[]`           | List of upstream artifact IDs that must be valid before this artifact can be authored. |\n| `instruction`      | `string` (optional)  | Prompt instructions injected into AI agent context.                                    |\n| `deltaInstruction` | `string` (optional)  | Alternate instructions used when authoring a `.delta.yaml` instead of a new file.      |\n| `rules`            | `object` (optional)  | Validation rules (`pre` and `post`) executed during artifact lifecycle checks.         |\n\n---\n"
      },
      {
        "index": 7,
        "heading": "4. Artifact Templates",
        "level": 2,
        "startLine": 95,
        "endLine": 147,
        "lines": 53,
        "content": "## 4. Artifact Templates\n\nArtifact templates provide scaffolding when an AI agent or developer creates an artifact for the first time. They enforce consistent formatting, provide embedded guidance, and eliminate blank-canvas ambiguity.\n\n### Template Storage as Files\n\nIn SpecD, artifact templates are **external files stored on disk** relative to the schema directory (or repository root):\n\n```\nmy-schema/\n├── schema.yaml\n└── templates/\n    ├── proposal.md\n    ├── spec.md\n    ├── verify.md\n    ├── design.md\n    └── tasks.md\n```\n\nIn `schema.yaml`, the `template` field points to the file path:\n\n```yaml\ntemplate: templates/proposal.md\n```\n\nSpecD reads the file from disk when initializing the artifact. Template files are plain Markdown and are versioned directly alongside your schema.\n\n### Embedded Agent Guidance\n\nTemplates in `@specd/schema-std` make extensive use of HTML comments (`<!-- ... -->`). These comments serve as **in-situ instructions for AI coding assistants**:\n\n```markdown\n# Proposal: {{change.name}}\n\n## Motivation\n\n<!--\n1-2 sentences on the problem or opportunity.\nWhat is broken or missing? Why now? Focus strictly on WHY, not HOW.\n-->\n\n## Proposed Solution\n\n<!--\nHigh-level description of what we want to build or change.\nOutline the concrete approach and the minimal necessary changes.\n-->\n```\n\nWhen an AI agent scaffolds the document, it reads the guidance comments to understand what belongs in each section, what traps to avoid, and how deep the analysis should be. Agents are instructed not to copy the guidance comments into the final output.\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Template Storage as Files",
        "level": 3,
        "startLine": 99,
        "endLine": 121,
        "lines": 23,
        "content": "### Template Storage as Files\n\nIn SpecD, artifact templates are **external files stored on disk** relative to the schema directory (or repository root):\n\n```\nmy-schema/\n├── schema.yaml\n└── templates/\n    ├── proposal.md\n    ├── spec.md\n    ├── verify.md\n    ├── design.md\n    └── tasks.md\n```\n\nIn `schema.yaml`, the `template` field points to the file path:\n\n```yaml\ntemplate: templates/proposal.md\n```\n\nSpecD reads the file from disk when initializing the artifact. Template files are plain Markdown and are versioned directly alongside your schema.\n"
      },
      {
        "index": 9,
        "heading": "Embedded Agent Guidance",
        "level": 3,
        "startLine": 122,
        "endLine": 147,
        "lines": 26,
        "content": "### Embedded Agent Guidance\n\nTemplates in `@specd/schema-std` make extensive use of HTML comments (`<!-- ... -->`). These comments serve as **in-situ instructions for AI coding assistants**:\n\n```markdown\n# Proposal: {{change.name}}\n\n## Motivation\n\n<!--\n1-2 sentences on the problem or opportunity.\nWhat is broken or missing? Why now? Focus strictly on WHY, not HOW.\n-->\n\n## Proposed Solution\n\n<!--\nHigh-level description of what we want to build or change.\nOutline the concrete approach and the minimal necessary changes.\n-->\n```\n\nWhen an AI agent scaffolds the document, it reads the guidance comments to understand what belongs in each section, what traps to avoid, and how deep the analysis should be. Agents are instructed not to copy the guidance comments into the final output.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "5. The Standard Schema Artifact Pipeline (`@specd/schema-std`)",
        "level": 2,
        "startLine": 148,
        "endLine": 188,
        "lines": 41,
        "content": "## 5. The Standard Schema Artifact Pipeline (`@specd/schema-std`)\n\nThe default schema shipped with SpecD (`@specd/schema-std`) defines five core artifacts:\n\n### 1. `proposal.md` (`scope: change`)\n\nEstablishes **why** the change is needed.\n\n- **Sections**: Motivation, Current Behavior, Proposed Solution, Specs Affected (new vs modified), Technical Context, and Open Questions.\n- **Role**: Serves as the initial alignment contract before any specification work begins.\n\n### 2. `spec.md` (`scope: spec`)\n\nEstablishes **what** the system shall do.\n\n- **Sections**: Purpose, Requirements (`### Requirement: <Title>`), Constraints, and Spec Dependencies.\n- **Role**: Durable, living contract. Uses RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`) and omits implementation or testing details.\n\n### 3. `verify.md` (`scope: spec`)\n\nEstablishes **how to verify** that requirements are satisfied.\n\n- **Sections**: Verification Scenarios (`#### Scenario: <Name>`) structured as **GIVEN / WHEN / THEN** test cases.\n- **Role**: Acceptance criteria paired 1:1 with requirements from `spec.md`.\n\n### 4. `design.md` (`scope: change`)\n\nEstablishes **how** the capability will be implemented in code.\n\n- **Sections**: Architectural Context, Symbols to Create/Modify, Blast Radius Analysis, Algorithms, Decision Records (ADRs), and Test Strategy.\n- **Role**: Implementation contract for the developer or agent before writing code.\n\n### 5. `tasks.md` (`scope: change`)\n\nEstablishes the **execution plan**.\n\n- **Sections**: Checklists (`- [ ]`) grouped by component or phase.\n- **Role**: Tracks granular implementation progress through the `implementing` lifecycle state.\n\n---\n"
      },
      {
        "index": 11,
        "heading": "1. `proposal.md` (`scope: change`)",
        "level": 3,
        "startLine": 152,
        "endLine": 158,
        "lines": 7,
        "content": "### 1. `proposal.md` (`scope: change`)\n\nEstablishes **why** the change is needed.\n\n- **Sections**: Motivation, Current Behavior, Proposed Solution, Specs Affected (new vs modified), Technical Context, and Open Questions.\n- **Role**: Serves as the initial alignment contract before any specification work begins.\n"
      },
      {
        "index": 12,
        "heading": "2. `spec.md` (`scope: spec`)",
        "level": 3,
        "startLine": 159,
        "endLine": 165,
        "lines": 7,
        "content": "### 2. `spec.md` (`scope: spec`)\n\nEstablishes **what** the system shall do.\n\n- **Sections**: Purpose, Requirements (`### Requirement: <Title>`), Constraints, and Spec Dependencies.\n- **Role**: Durable, living contract. Uses RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`) and omits implementation or testing details.\n"
      },
      {
        "index": 13,
        "heading": "3. `verify.md` (`scope: spec`)",
        "level": 3,
        "startLine": 166,
        "endLine": 172,
        "lines": 7,
        "content": "### 3. `verify.md` (`scope: spec`)\n\nEstablishes **how to verify** that requirements are satisfied.\n\n- **Sections**: Verification Scenarios (`#### Scenario: <Name>`) structured as **GIVEN / WHEN / THEN** test cases.\n- **Role**: Acceptance criteria paired 1:1 with requirements from `spec.md`.\n"
      },
      {
        "index": 14,
        "heading": "4. `design.md` (`scope: change`)",
        "level": 3,
        "startLine": 173,
        "endLine": 179,
        "lines": 7,
        "content": "### 4. `design.md` (`scope: change`)\n\nEstablishes **how** the capability will be implemented in code.\n\n- **Sections**: Architectural Context, Symbols to Create/Modify, Blast Radius Analysis, Algorithms, Decision Records (ADRs), and Test Strategy.\n- **Role**: Implementation contract for the developer or agent before writing code.\n"
      },
      {
        "index": 15,
        "heading": "5. `tasks.md` (`scope: change`)",
        "level": 3,
        "startLine": 180,
        "endLine": 188,
        "lines": 9,
        "content": "### 5. `tasks.md` (`scope: change`)\n\nEstablishes the **execution plan**.\n\n- **Sections**: Checklists (`- [ ]`) grouped by component or phase.\n- **Role**: Tracks granular implementation progress through the `implementing` lifecycle state.\n\n---\n"
      },
      {
        "index": 16,
        "heading": "6. Customizing Artifacts in `specd.yaml`",
        "level": 2,
        "startLine": 189,
        "endLine": 240,
        "lines": 52,
        "content": "## 6. Customizing Artifacts in `specd.yaml`\n\nYou can declare new artifact types or override existing templates in `specd.yaml` using `schemaOverrides`:\n\n### Adding a Custom Artifact\n\nTo add a new deliverable (e.g. a database migration plan or OpenAPI contract):\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: migration-plan\n        scope: change\n        output: migration.md\n        description: Database migration and rollback strategy\n        template: .specd/templates/migration.md\n        requires:\n          - design\n        instruction: |\n          Detail database schema changes, rollback scripts, and zero-downtime execution steps.\n```\n\n### Overriding an Existing Template\n\nTo replace an artifact's template with a company-specific format:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  set:\n    artifacts:\n      - id: proposal\n        template: .specd/templates/enterprise-proposal.md\n```\n\n### Removing a Template\n\nTo have an artifact start as an empty file without initial scaffolding:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  remove:\n    artifacts:\n      - id: tasks\n        template: null\n```\n\n---\n"
      },
      {
        "index": 17,
        "heading": "Adding a Custom Artifact",
        "level": 3,
        "startLine": 193,
        "endLine": 212,
        "lines": 20,
        "content": "### Adding a Custom Artifact\n\nTo add a new deliverable (e.g. a database migration plan or OpenAPI contract):\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  create:\n    artifacts:\n      - id: migration-plan\n        scope: change\n        output: migration.md\n        description: Database migration and rollback strategy\n        template: .specd/templates/migration.md\n        requires:\n          - design\n        instruction: |\n          Detail database schema changes, rollback scripts, and zero-downtime execution steps.\n```\n"
      },
      {
        "index": 18,
        "heading": "Overriding an Existing Template",
        "level": 3,
        "startLine": 213,
        "endLine": 225,
        "lines": 13,
        "content": "### Overriding an Existing Template\n\nTo replace an artifact's template with a company-specific format:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  set:\n    artifacts:\n      - id: proposal\n        template: .specd/templates/enterprise-proposal.md\n```\n"
      },
      {
        "index": 19,
        "heading": "Removing a Template",
        "level": 3,
        "startLine": 226,
        "endLine": 240,
        "lines": 15,
        "content": "### Removing a Template\n\nTo have an artifact start as an empty file without initial scaffolding:\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  remove:\n    artifacts:\n      - id: tasks\n        template: null\n```\n\n---\n"
      },
      {
        "index": 20,
        "heading": "Where to go next",
        "level": 2,
        "startLine": 241,
        "endLine": 247,
        "lines": 7,
        "content": "## Where to go next\n\n- [Changes Guide](./changes.md) — How changes manage artifacts and track state transitions.\n- [Workflow Guide](./workflow.md) — The lifecycle states (`designing`, `implementing`, `verifying`, `archiving`).\n- [Custom Schemas Guide](./custom-schemas.md) — Authoring custom schemas and schema plugins.\n- [Schema Format Reference](../schemas/schema-format.md) — Full technical reference for all artifact and schema properties.\n"
      }
    ]
  },
  {
    "topic": "context-compilation",
    "title": "Context Compilation",
    "description": "How SpecD compiles targeted, token-optimized context blocks for AI agents and human developers.",
    "order": 8,
    "content": "# Context Compilation\n\nWhen an AI agent or human developer works on a change, it needs to know which specifications and constraints are relevant. Rather than dumping entire repositories into prompt windows or expecting an agent to guess where boundaries lie, SpecD compiles a structured, deterministic, and token-optimized **context block** at each lifecycle step.\n\n---\n\n## Why Context Compilation Matters\n\nLLMs have finite context windows and suffer from attention degradation when presented with irrelevant noise (\"needle in a haystack\" problem). SpecD solves this by treating requirements as a dependency graph and dynamically assembling only the specs pertinent to the current work.\n\nThe result is a unified instruction block injected directly into agent sessions containing:\n\n1. High-level project context and architecture guidelines.\n2. Active artifact instructions according to the project's schema.\n3. Relevant specification requirements, constraints, and scenarios.\n\n---\n\n## The Compilation Pipeline\n\nContext compilation follows a deterministic 6-step pipeline:\n\n```mermaid\nflowchart TD\n    A[\"1. Project Include / Exclude Patterns\"] --> B[\"2. Workspace Include / Exclude Patterns\"]\n    B --> C[\"3. Change Seeding (--include-change-specs)\"]\n    C --> D[\"4. Dependency Graph Traversal (dependsOn)\"]\n    D --> E[\"5. Display Mode Rendering (list | summary | full | hybrid)\"]\n    E --> F[\"6. Assembled Agent Context Block\"]\n```\n\n### 1. Project-level include/exclude patterns\n\nSpecs configured under `context.include` in `specd.yaml` (e.g. `_global/architecture`, `_global/security`) are considered universally applicable and are automatically included in every compiled context. Conversely, patterns in `context.exclude` are filtered out.\n\n### 2. Workspace-level patterns\n\nEach workspace defined in `specd.yaml` can specify its own include and exclude rules. When compiling context for a workspace or package, only relevant workspace specs participate.\n\n### 3. Change seeding (Change context only)\n\nWhen compiling context for a change via `specd change context <change-id>`, the specs listed in the change manifest (`change.specIds`) are the primary seed.\n\n- By default, SpecD focuses on dependencies and active instructions.\n- Add `--include-change-specs` to force-seed all declared `specIds` directly into the compiled block.\n\n### 4. Dependency graph traversal\n\nStarting from the selected seeds, SpecD queries the `@specd/code-graph` or spec metadata and transitively traverses all `dependsOn` links. For instance, if `auth/oauth` depends on `auth/tokens`, both are collected.\n\n### 5. Display mode rendering\n\nSpecs are formatted according to the configured `contextMode` to minimize token overhead while providing sufficient detail.\n\n### 6. Assembly\n\nThe collected components are assembled into an ordered markdown payload ready to inject into the agent prompt or stdout.\n\n---\n\n## Display Modes\n\nSpecD supports four display modes, configured via `contextMode` in `specd.yaml` or overridden per command invocation:\n\n| Mode                      | Behavior                                                                                                                   | Best Used For                                                               |\n| :------------------------ | :------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------- |\n| **`summary`** _(default)_ | Renders spec ID and summary metadata (`title`, `description`).                                                             | General orienting, planning, and keeping prompts lean.                      |\n| **`list`**                | Emits only spec IDs (`workspace:path`).                                                                                    | Quick listing, shell piping, and minimal footprint lookups.                 |\n| **`full`**                | Emits full content (purpose, requirements, constraints, scenarios) for all matched specs.                                  | Implementation and verification where exact scenario details are necessary. |\n| **`hybrid`**              | Tiered rendering: directly targeted specs (`specIds`) render in `full`, while transitive dependencies render as `summary`. | Focused implementation with contextual awareness of dependencies.           |\n\n> **Note:** For `project context` and `spec context`, `hybrid` automatically behaves as `full`.\n\n---\n\n## Context Commands\n\nSpecD provides dedicated commands for inspecting context across different lifecycle levels:\n\n### 1. Change Context\n\nCompiles context specifically for an active change:\n\n```bash\nspecd change context 20260923-144548-my-feature\nspecd change context 20260923-144548-my-feature --mode full --include-change-specs\n```\n\n### 2. Project Context\n\nCompiles high-level project specs and global rules:\n\n```bash\nspecd project context\nspecd project context --mode summary\n```\n\n### 3. Spec Context\n\nCompiles a single canonical spec and its transitive dependencies:\n\n```bash\nspecd spec context default:auth/login --mode full\n```\n\n### 4. Discovery: `project context-specs`\n\nFor pre-change discovery without markdown rendering, `context-specs` resolves the project and workspace include/exclude patterns and prints matching spec IDs:\n\n```bash\nspecd project context-specs\nspecd project context-specs --workspace packages/core --workspaces-only\n```\n\n---\n\n## Section Filtering Flags\n\nWhen rendering in `full` mode, you can filter specific sections to keep token counts down:\n\n- `--rules`: Emits only the requirements / rules section.\n- `--constraints`: Emits only technical and operational constraints.\n- `--scenarios`: Emits only verification test scenarios (`WHEN ... THEN ...`).\n\n```bash\nspecd spec context default:auth/login --mode full --scenarios\n```\n\n_(In `list` and `summary` modes, these flags are accepted without altering the rendered shape)._\n\n---\n\n## Source-Aware Drill-Down Guidance\n\nWhen output contains summary or list entries, SpecD prints helpful drill-down suggestions based on where the spec originated:\n\n- For specs originating in the change (`source: 'specIds'`):\n  ```bash\n  specd changes spec-preview <change-name> <specId>\n  ```\n- For canonical workspace specs (`source: 'specDependsOn'`, `'includePattern'`, or `'dependsOnTraversal'`):\n  ```bash\n  specd specs context <specId> --mode full\n  ```\n",
    "lineCount": 145,
    "byteLength": 6443,
    "outline": [
      {
        "index": 1,
        "heading": "Context Compilation",
        "level": 1,
        "startLine": 1,
        "endLine": 145,
        "lines": 145,
        "content": "# Context Compilation\n\nWhen an AI agent or human developer works on a change, it needs to know which specifications and constraints are relevant. Rather than dumping entire repositories into prompt windows or expecting an agent to guess where boundaries lie, SpecD compiles a structured, deterministic, and token-optimized **context block** at each lifecycle step.\n\n---\n\n## Why Context Compilation Matters\n\nLLMs have finite context windows and suffer from attention degradation when presented with irrelevant noise (\"needle in a haystack\" problem). SpecD solves this by treating requirements as a dependency graph and dynamically assembling only the specs pertinent to the current work.\n\nThe result is a unified instruction block injected directly into agent sessions containing:\n\n1. High-level project context and architecture guidelines.\n2. Active artifact instructions according to the project's schema.\n3. Relevant specification requirements, constraints, and scenarios.\n\n---\n\n## The Compilation Pipeline\n\nContext compilation follows a deterministic 6-step pipeline:\n\n```mermaid\nflowchart TD\n    A[\"1. Project Include / Exclude Patterns\"] --> B[\"2. Workspace Include / Exclude Patterns\"]\n    B --> C[\"3. Change Seeding (--include-change-specs)\"]\n    C --> D[\"4. Dependency Graph Traversal (dependsOn)\"]\n    D --> E[\"5. Display Mode Rendering (list | summary | full | hybrid)\"]\n    E --> F[\"6. Assembled Agent Context Block\"]\n```\n\n### 1. Project-level include/exclude patterns\n\nSpecs configured under `context.include` in `specd.yaml` (e.g. `_global/architecture`, `_global/security`) are considered universally applicable and are automatically included in every compiled context. Conversely, patterns in `context.exclude` are filtered out.\n\n### 2. Workspace-level patterns\n\nEach workspace defined in `specd.yaml` can specify its own include and exclude rules. When compiling context for a workspace or package, only relevant workspace specs participate.\n\n### 3. Change seeding (Change context only)\n\nWhen compiling context for a change via `specd change context <change-id>`, the specs listed in the change manifest (`change.specIds`) are the primary seed.\n\n- By default, SpecD focuses on dependencies and active instructions.\n- Add `--include-change-specs` to force-seed all declared `specIds` directly into the compiled block.\n\n### 4. Dependency graph traversal\n\nStarting from the selected seeds, SpecD queries the `@specd/code-graph` or spec metadata and transitively traverses all `dependsOn` links. For instance, if `auth/oauth` depends on `auth/tokens`, both are collected.\n\n### 5. Display mode rendering\n\nSpecs are formatted according to the configured `contextMode` to minimize token overhead while providing sufficient detail.\n\n### 6. Assembly\n\nThe collected components are assembled into an ordered markdown payload ready to inject into the agent prompt or stdout.\n\n---\n\n## Display Modes\n\nSpecD supports four display modes, configured via `contextMode` in `specd.yaml` or overridden per command invocation:\n\n| Mode                      | Behavior                                                                                                                   | Best Used For                                                               |\n| :------------------------ | :------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------- |\n| **`summary`** _(default)_ | Renders spec ID and summary metadata (`title`, `description`).                                                             | General orienting, planning, and keeping prompts lean.                      |\n| **`list`**                | Emits only spec IDs (`workspace:path`).                                                                                    | Quick listing, shell piping, and minimal footprint lookups.                 |\n| **`full`**                | Emits full content (purpose, requirements, constraints, scenarios) for all matched specs.                                  | Implementation and verification where exact scenario details are necessary. |\n| **`hybrid`**              | Tiered rendering: directly targeted specs (`specIds`) render in `full`, while transitive dependencies render as `summary`. | Focused implementation with contextual awareness of dependencies.           |\n\n> **Note:** For `project context` and `spec context`, `hybrid` automatically behaves as `full`.\n\n---\n\n## Context Commands\n\nSpecD provides dedicated commands for inspecting context across different lifecycle levels:\n\n### 1. Change Context\n\nCompiles context specifically for an active change:\n\n```bash\nspecd change context 20260923-144548-my-feature\nspecd change context 20260923-144548-my-feature --mode full --include-change-specs\n```\n\n### 2. Project Context\n\nCompiles high-level project specs and global rules:\n\n```bash\nspecd project context\nspecd project context --mode summary\n```\n\n### 3. Spec Context\n\nCompiles a single canonical spec and its transitive dependencies:\n\n```bash\nspecd spec context default:auth/login --mode full\n```\n\n### 4. Discovery: `project context-specs`\n\nFor pre-change discovery without markdown rendering, `context-specs` resolves the project and workspace include/exclude patterns and prints matching spec IDs:\n\n```bash\nspecd project context-specs\nspecd project context-specs --workspace packages/core --workspaces-only\n```\n\n---\n\n## Section Filtering Flags\n\nWhen rendering in `full` mode, you can filter specific sections to keep token counts down:\n\n- `--rules`: Emits only the requirements / rules section.\n- `--constraints`: Emits only technical and operational constraints.\n- `--scenarios`: Emits only verification test scenarios (`WHEN ... THEN ...`).\n\n```bash\nspecd spec context default:auth/login --mode full --scenarios\n```\n\n_(In `list` and `summary` modes, these flags are accepted without altering the rendered shape)._\n\n---\n\n## Source-Aware Drill-Down Guidance\n\nWhen output contains summary or list entries, SpecD prints helpful drill-down suggestions based on where the spec originated:\n\n- For specs originating in the change (`source: 'specIds'`):\n  ```bash\n  specd changes spec-preview <change-name> <specId>\n  ```\n- For canonical workspace specs (`source: 'specDependsOn'`, `'includePattern'`, or `'dependsOnTraversal'`):\n  ```bash\n  specd specs context <specId> --mode full\n  ```\n"
      },
      {
        "index": 2,
        "heading": "Why Context Compilation Matters",
        "level": 2,
        "startLine": 7,
        "endLine": 18,
        "lines": 12,
        "content": "## Why Context Compilation Matters\n\nLLMs have finite context windows and suffer from attention degradation when presented with irrelevant noise (\"needle in a haystack\" problem). SpecD solves this by treating requirements as a dependency graph and dynamically assembling only the specs pertinent to the current work.\n\nThe result is a unified instruction block injected directly into agent sessions containing:\n\n1. High-level project context and architecture guidelines.\n2. Active artifact instructions according to the project's schema.\n3. Relevant specification requirements, constraints, and scenarios.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "The Compilation Pipeline",
        "level": 2,
        "startLine": 19,
        "endLine": 60,
        "lines": 42,
        "content": "## The Compilation Pipeline\n\nContext compilation follows a deterministic 6-step pipeline:\n\n```mermaid\nflowchart TD\n    A[\"1. Project Include / Exclude Patterns\"] --> B[\"2. Workspace Include / Exclude Patterns\"]\n    B --> C[\"3. Change Seeding (--include-change-specs)\"]\n    C --> D[\"4. Dependency Graph Traversal (dependsOn)\"]\n    D --> E[\"5. Display Mode Rendering (list | summary | full | hybrid)\"]\n    E --> F[\"6. Assembled Agent Context Block\"]\n```\n\n### 1. Project-level include/exclude patterns\n\nSpecs configured under `context.include` in `specd.yaml` (e.g. `_global/architecture`, `_global/security`) are considered universally applicable and are automatically included in every compiled context. Conversely, patterns in `context.exclude` are filtered out.\n\n### 2. Workspace-level patterns\n\nEach workspace defined in `specd.yaml` can specify its own include and exclude rules. When compiling context for a workspace or package, only relevant workspace specs participate.\n\n### 3. Change seeding (Change context only)\n\nWhen compiling context for a change via `specd change context <change-id>`, the specs listed in the change manifest (`change.specIds`) are the primary seed.\n\n- By default, SpecD focuses on dependencies and active instructions.\n- Add `--include-change-specs` to force-seed all declared `specIds` directly into the compiled block.\n\n### 4. Dependency graph traversal\n\nStarting from the selected seeds, SpecD queries the `@specd/code-graph` or spec metadata and transitively traverses all `dependsOn` links. For instance, if `auth/oauth` depends on `auth/tokens`, both are collected.\n\n### 5. Display mode rendering\n\nSpecs are formatted according to the configured `contextMode` to minimize token overhead while providing sufficient detail.\n\n### 6. Assembly\n\nThe collected components are assembled into an ordered markdown payload ready to inject into the agent prompt or stdout.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "1. Project-level include/exclude patterns",
        "level": 3,
        "startLine": 32,
        "endLine": 35,
        "lines": 4,
        "content": "### 1. Project-level include/exclude patterns\n\nSpecs configured under `context.include` in `specd.yaml` (e.g. `_global/architecture`, `_global/security`) are considered universally applicable and are automatically included in every compiled context. Conversely, patterns in `context.exclude` are filtered out.\n"
      },
      {
        "index": 5,
        "heading": "2. Workspace-level patterns",
        "level": 3,
        "startLine": 36,
        "endLine": 39,
        "lines": 4,
        "content": "### 2. Workspace-level patterns\n\nEach workspace defined in `specd.yaml` can specify its own include and exclude rules. When compiling context for a workspace or package, only relevant workspace specs participate.\n"
      },
      {
        "index": 6,
        "heading": "3. Change seeding (Change context only)",
        "level": 3,
        "startLine": 40,
        "endLine": 46,
        "lines": 7,
        "content": "### 3. Change seeding (Change context only)\n\nWhen compiling context for a change via `specd change context <change-id>`, the specs listed in the change manifest (`change.specIds`) are the primary seed.\n\n- By default, SpecD focuses on dependencies and active instructions.\n- Add `--include-change-specs` to force-seed all declared `specIds` directly into the compiled block.\n"
      },
      {
        "index": 7,
        "heading": "4. Dependency graph traversal",
        "level": 3,
        "startLine": 47,
        "endLine": 50,
        "lines": 4,
        "content": "### 4. Dependency graph traversal\n\nStarting from the selected seeds, SpecD queries the `@specd/code-graph` or spec metadata and transitively traverses all `dependsOn` links. For instance, if `auth/oauth` depends on `auth/tokens`, both are collected.\n"
      },
      {
        "index": 8,
        "heading": "5. Display mode rendering",
        "level": 3,
        "startLine": 51,
        "endLine": 54,
        "lines": 4,
        "content": "### 5. Display mode rendering\n\nSpecs are formatted according to the configured `contextMode` to minimize token overhead while providing sufficient detail.\n"
      },
      {
        "index": 9,
        "heading": "6. Assembly",
        "level": 3,
        "startLine": 55,
        "endLine": 60,
        "lines": 6,
        "content": "### 6. Assembly\n\nThe collected components are assembled into an ordered markdown payload ready to inject into the agent prompt or stdout.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "Display Modes",
        "level": 2,
        "startLine": 61,
        "endLine": 75,
        "lines": 15,
        "content": "## Display Modes\n\nSpecD supports four display modes, configured via `contextMode` in `specd.yaml` or overridden per command invocation:\n\n| Mode                      | Behavior                                                                                                                   | Best Used For                                                               |\n| :------------------------ | :------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------- |\n| **`summary`** _(default)_ | Renders spec ID and summary metadata (`title`, `description`).                                                             | General orienting, planning, and keeping prompts lean.                      |\n| **`list`**                | Emits only spec IDs (`workspace:path`).                                                                                    | Quick listing, shell piping, and minimal footprint lookups.                 |\n| **`full`**                | Emits full content (purpose, requirements, constraints, scenarios) for all matched specs.                                  | Implementation and verification where exact scenario details are necessary. |\n| **`hybrid`**              | Tiered rendering: directly targeted specs (`specIds`) render in `full`, while transitive dependencies render as `summary`. | Focused implementation with contextual awareness of dependencies.           |\n\n> **Note:** For `project context` and `spec context`, `hybrid` automatically behaves as `full`.\n\n---\n"
      },
      {
        "index": 11,
        "heading": "Context Commands",
        "level": 2,
        "startLine": 76,
        "endLine": 116,
        "lines": 41,
        "content": "## Context Commands\n\nSpecD provides dedicated commands for inspecting context across different lifecycle levels:\n\n### 1. Change Context\n\nCompiles context specifically for an active change:\n\n```bash\nspecd change context 20260923-144548-my-feature\nspecd change context 20260923-144548-my-feature --mode full --include-change-specs\n```\n\n### 2. Project Context\n\nCompiles high-level project specs and global rules:\n\n```bash\nspecd project context\nspecd project context --mode summary\n```\n\n### 3. Spec Context\n\nCompiles a single canonical spec and its transitive dependencies:\n\n```bash\nspecd spec context default:auth/login --mode full\n```\n\n### 4. Discovery: `project context-specs`\n\nFor pre-change discovery without markdown rendering, `context-specs` resolves the project and workspace include/exclude patterns and prints matching spec IDs:\n\n```bash\nspecd project context-specs\nspecd project context-specs --workspace packages/core --workspaces-only\n```\n\n---\n"
      },
      {
        "index": 12,
        "heading": "1. Change Context",
        "level": 3,
        "startLine": 80,
        "endLine": 88,
        "lines": 9,
        "content": "### 1. Change Context\n\nCompiles context specifically for an active change:\n\n```bash\nspecd change context 20260923-144548-my-feature\nspecd change context 20260923-144548-my-feature --mode full --include-change-specs\n```\n"
      },
      {
        "index": 13,
        "heading": "2. Project Context",
        "level": 3,
        "startLine": 89,
        "endLine": 97,
        "lines": 9,
        "content": "### 2. Project Context\n\nCompiles high-level project specs and global rules:\n\n```bash\nspecd project context\nspecd project context --mode summary\n```\n"
      },
      {
        "index": 14,
        "heading": "3. Spec Context",
        "level": 3,
        "startLine": 98,
        "endLine": 105,
        "lines": 8,
        "content": "### 3. Spec Context\n\nCompiles a single canonical spec and its transitive dependencies:\n\n```bash\nspecd spec context default:auth/login --mode full\n```\n"
      },
      {
        "index": 15,
        "heading": "4. Discovery: `project context-specs`",
        "level": 3,
        "startLine": 106,
        "endLine": 116,
        "lines": 11,
        "content": "### 4. Discovery: `project context-specs`\n\nFor pre-change discovery without markdown rendering, `context-specs` resolves the project and workspace include/exclude patterns and prints matching spec IDs:\n\n```bash\nspecd project context-specs\nspecd project context-specs --workspace packages/core --workspaces-only\n```\n\n---\n"
      },
      {
        "index": 16,
        "heading": "Section Filtering Flags",
        "level": 2,
        "startLine": 117,
        "endLine": 132,
        "lines": 16,
        "content": "## Section Filtering Flags\n\nWhen rendering in `full` mode, you can filter specific sections to keep token counts down:\n\n- `--rules`: Emits only the requirements / rules section.\n- `--constraints`: Emits only technical and operational constraints.\n- `--scenarios`: Emits only verification test scenarios (`WHEN ... THEN ...`).\n\n```bash\nspecd spec context default:auth/login --mode full --scenarios\n```\n\n_(In `list` and `summary` modes, these flags are accepted without altering the rendered shape)._\n\n---\n"
      },
      {
        "index": 17,
        "heading": "Source-Aware Drill-Down Guidance",
        "level": 2,
        "startLine": 133,
        "endLine": 145,
        "lines": 13,
        "content": "## Source-Aware Drill-Down Guidance\n\nWhen output contains summary or list entries, SpecD prints helpful drill-down suggestions based on where the spec originated:\n\n- For specs originating in the change (`source: 'specIds'`):\n  ```bash\n  specd changes spec-preview <change-name> <specId>\n  ```\n- For canonical workspace specs (`source: 'specDependsOn'`, `'includePattern'`, or `'dependsOnTraversal'`):\n  ```bash\n  specd specs context <specId> --mode full\n  ```\n"
      }
    ]
  },
  {
    "topic": "deltas",
    "title": "Spec Deltas & Unified Specifications",
    "description": "Understanding YAML delta operations, atomic spec evolution, and previewing changes.",
    "order": 9,
    "content": "# Spec Deltas & Unified Specifications\n\n## What a delta file is\n\nA delta file is a YAML document that expresses changes to an existing spec as a sequence of AST operations. Instead of replacing the entire spec file, the agent produces a `.delta.yaml` that says precisely which sections to add, modify, or remove. SpecD applies the delta deterministically — no LLM is involved in the application step.\n\nDelta files are only produced when `delta: true` is declared on an artifact in the schema. They are only valid for `scope: spec` artifacts.\n\n## File naming and location\n\nA delta file's filename is the target artifact's filename with `.delta.yaml` appended:\n\n| Target artifact | Delta filename            |\n| --------------- | ------------------------- |\n| `spec.md`       | `spec.md.delta.yaml`      |\n| `verify.md`     | `verify.md.delta.yaml`    |\n| `openapi.json`  | `openapi.json.delta.yaml` |\n| `config.yaml`   | `config.yaml.delta.yaml`  |\n\nDelta files live inside the change directory at:\n\n```\ndeltas/<workspace>/<spec-path>/<delta-filename>\n```\n\nFor example, a delta targeting `specs/core/config/spec.md` in the `default` workspace lives at:\n\n```\n.specd/changes/<change-name>/deltas/default/core/config/spec.md.delta.yaml\n```\n\nDelta files are never synced to permanent spec directories — they remain in the change directory as part of its record.\n\n## Delta entry structure\n\nA delta file is a YAML sequence. Each entry is one delta operation.\n\n| Field         | Required for                   | Description                                                                                                                                               |\n| ------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `op`          | all                            | Operation type: `added`, `modified`, `removed`, or `no-op`.                                                                                               |\n| `selector`    | `modified`, `removed`          | Identifies the existing node to target. Not valid on `added` or `no-op`.                                                                                  |\n| `position`    | `added` (optional)             | Where to insert the new node. See [Position](#position). Not valid on `no-op`.                                                                            |\n| `rename`      | `modified` (optional)          | New label for the node's identifying property (heading text, key name). Only valid on `modified`.                                                         |\n| `content`     | `added`, `modified` (optional) | New node content in the artifact's native format. Mutually exclusive with `value`. Not valid on `no-op`.                                                  |\n| `value`       | `added`, `modified` (optional) | Node value as a structured YAML value. For JSON/YAML structured nodes. Mutually exclusive with `content`. Not valid on `no-op`.                           |\n| `strategy`    | optional                       | Array merge strategy: `replace` (default), `append`, or `merge-by`. Only valid when the selector targets an array or sequence node. Not valid on `no-op`. |\n| `mergeKey`    | required with `merge-by`       | Key field used to match objects in `merge-by` strategy. Not valid on `no-op`.                                                                             |\n| `description` | optional on all                | Free-text explanation of what this entry does or why. Ignored during application. Valid on all operation types including `no-op`.                         |\n\n## The four operations\n\n### modified — change an existing node\n\n`modified` updates the body of an existing node identified by `selector`. The node's identifying property (its heading, key name) is preserved unless `rename` is also specified.\n\n`content` contains only the body — the identifying line is not repeated. SpecD parses it and replaces the node's existing body.\n\n```yaml\n# Change the body of an existing requirement section\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n# Rename the section and update its body in one operation\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Cache'\n  rename: 'Requirement: Cache resolved config'\n  content: |\n    The system must cache the resolved config object in memory for the\n    duration of a single command invocation.\n\n# Update a JSON property value\n- op: modified\n  selector:\n    type: property\n    matches: version\n  value: '2.0.0'\n\n# Update a YAML pair value\n- op: modified\n  selector:\n    type: pair\n    matches: model\n    parent:\n      type: pair\n      matches: llm\n  value: 'claude-opus-4-6'\n```\n\n### removed — delete an existing node\n\n`removed` detaches the identified node and all its children from the document. It takes only a `selector` — no `content` or `value`.\n\n```yaml\n# Remove a section that is no longer relevant\n- op: removed\n  selector:\n    type: section\n    matches: 'Requirement: Legacy auth'\n\n# Remove a JSON property\n- op: removed\n  selector:\n    type: property\n    matches: deprecated_field\n\n# Remove a YAML pair\n- op: removed\n  selector:\n    type: pair\n    matches: old_setting\n```\n\n### added — insert a new node\n\n`added` inserts a new node into the document. It does not take a `selector` — the node being added does not yet exist. Use `position` to control where it is inserted.\n\nFor text-based formats (markdown, plain text), `content` includes the identifying line as its first line — the heading, for a section.\n\n````yaml\n# Append a new requirement to the end of the Requirements section (default position)\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml\n    is modified on disk.\n\n# Insert after a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    after:\n      type: section\n      matches: 'Requirement: Load config'\n  content: |\n    ### Requirement: Validate on load\n\n    The system must validate specd.yaml against its schema immediately\n    after loading and before executing any command.\n\n# Insert before a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    before:\n      type: section\n      matches: 'Requirement: Evict cache on change'\n  content: |\n    ### Requirement: Cache TTL\n\n    The cache must expire after 60 seconds to pick up external edits.\n\n# Insert as the first child of the parent\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    first: true\n  content: |\n    ### Requirement: Discover config file\n\n    The system must discover specd.yaml by walking up from the CWD.\n\n# Append at document root level (no parent — appended to end of document)\n- op: added\n  content: |\n    ## Examples\n\n    ### Example: Minimal config\n\n    ```yaml\n    schema: '@specd/schema-std'\n    ```\n````\n\n### no-op — explicitly document that no change is needed\n\n`no-op` declares that the artifact requires no changes for this delta. It is used when a spec is listed in the proposal but a particular artifact does not need modifications — the existing content is already valid.\n\nRather than omitting the delta file (which is ambiguous — was it forgotten or intentionally skipped?), write a `no-op` entry with a `description` explaining why no change is needed.\n\nA `no-op` entry must be the **only** entry in the delta file. Mixing `no-op` with any other operation is an error — SpecD rejects such a file during parsing.\n\nThe only valid fields on a `no-op` entry are `op` and `description`. All other fields (`selector`, `position`, `rename`, `content`, `value`, `strategy`, `mergeKey`) are invalid.\n\nWhen SpecD encounters a `no-op` delta, it skips delta application, delta validations, and structural validations entirely. It proceeds directly to hash computation on the raw delta file content and marks the artifact complete. The `no-op` declares that the existing artifact is already correct.\n\n```yaml\n# verify.md does not need changes — existing scenarios remain valid\n- op: no-op\n  description: 'Constructor signature changed but all existing verify scenarios still apply.'\n```\n\n```yaml\n# spec.md requires no changes — only design.md and tasks.md are updated in this change\n- op: no-op\n  description: 'No spec changes needed. This change only updates the implementation plan.'\n```\n\n## Description field\n\nEvery delta entry, regardless of operation type, accepts an optional `description` field. Use it to record the intent behind the operation — why this node is being changed, what the business reason is, or what downstream effect is expected. The description is ignored during delta application; it exists purely for human and AI readers.\n\n```yaml\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  description: 'Tightened wording to reflect that the walk stops at the active VCS root, not just any ancestor.'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n- op: added\n  description: 'New requirement introduced by ADR-0012 — cache invalidation on file change.'\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml is modified on disk.\n```\n\n## Position\n\n`position` controls where an `added` entry inserts its node. All fields are optional — omitting `position` entirely appends the node at the end of the document.\n\n| Field    | Description                                                                                                                                                     |\n| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `parent` | Scopes the insertion to the children of the matched node. If omitted, insertion is at document root level.                                                      |\n| `after`  | Inserts immediately after the matched sibling within the parent scope. Falls back to appending at the end of scope with a warning if the sibling is not found.  |\n| `before` | Inserts immediately before the matched sibling within the parent scope. Falls back to appending at the end of scope with a warning if the sibling is not found. |\n| `first`  | Inserts as the first child of the parent scope.                                                                                                                 |\n| `last`   | Inserts as the last child of the parent scope. This is the default when `parent` is specified but no placement hint is given.                                   |\n\n`after`, `before`, `first`, and `last` are mutually exclusive. If only `parent` is given, the node is appended as the last child.\n\n## Array merge strategies\n\nWhen the selector targets an array or sequence node (not an individual item), the `strategy` field controls how the new value is merged:\n\n| Strategy            | Behaviour                                                                                                                                                                                                              |\n| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `replace` (default) | Replaces the entire array with the supplied value.                                                                                                                                                                     |\n| `append`            | Appends the supplied items to the end of the existing array.                                                                                                                                                           |\n| `merge-by`          | Merges the supplied items into the existing array by matching objects on `mergeKey`. Items with a matching key are replaced; items with new keys are appended; existing items not in the supplied value are preserved. |\n\n## Conflict detection\n\nSpecD validates the entire delta for conflicts before applying any operation. If any conflict is found, the whole delta is rejected with a `DeltaApplicationError` and no changes are made to the artifact.\n\n| Conflict                                                                               | Error                                              |\n| :------------------------------------------------------------------------------------- | :------------------------------------------------- |\n| Two `modified` or `removed` entries resolve to the same node                           | Cannot apply two operations to the same node.      |\n| A `rename` target already exists as a sibling                                          | Would produce a duplicate node.                    |\n| Two `modified` entries rename to the same target within the same parent scope          | Ambiguous result.                                  |\n| `content` and `value` both present in the same entry                                   | Mutually exclusive.                                |\n| `selector` on an `added` or `no-op` entry                                              | Use `position.parent` instead for `added`.         |\n| `rename` on an `added`, `removed`, or `no-op` entry                                    | Only valid on `modified`.                          |\n| `strategy: merge-by` without `mergeKey`                                                | `mergeKey` is required for `merge-by`.             |\n| `mergeKey` without `strategy: merge-by`                                                | `mergeKey` is only meaningful with `merge-by`.     |\n| `strategy` on a non-array selector                                                     | Strategy is only valid for array/sequence targets. |\n| More than one of `after`, `before`, `first`, `last` in the same `position`             | Mutually exclusive placement hints.                |\n| `position.parent` resolves to no node                                                  | SpecD cannot scope the insertion.                  |\n| `no-op` entry combined with any other entry in the same delta file                     | `no-op` must be the sole entry.                    |\n| `position`, `rename`, `content`, `value`, `strategy`, or `mergeKey` on a `no-op` entry | Only `op` and `description` are valid on `no-op`.  |\n",
    "lineCount": 285,
    "byteLength": 15196,
    "outline": [
      {
        "index": 1,
        "heading": "Spec Deltas & Unified Specifications",
        "level": 1,
        "startLine": 1,
        "endLine": 285,
        "lines": 285,
        "content": "# Spec Deltas & Unified Specifications\n\n## What a delta file is\n\nA delta file is a YAML document that expresses changes to an existing spec as a sequence of AST operations. Instead of replacing the entire spec file, the agent produces a `.delta.yaml` that says precisely which sections to add, modify, or remove. SpecD applies the delta deterministically — no LLM is involved in the application step.\n\nDelta files are only produced when `delta: true` is declared on an artifact in the schema. They are only valid for `scope: spec` artifacts.\n\n## File naming and location\n\nA delta file's filename is the target artifact's filename with `.delta.yaml` appended:\n\n| Target artifact | Delta filename            |\n| --------------- | ------------------------- |\n| `spec.md`       | `spec.md.delta.yaml`      |\n| `verify.md`     | `verify.md.delta.yaml`    |\n| `openapi.json`  | `openapi.json.delta.yaml` |\n| `config.yaml`   | `config.yaml.delta.yaml`  |\n\nDelta files live inside the change directory at:\n\n```\ndeltas/<workspace>/<spec-path>/<delta-filename>\n```\n\nFor example, a delta targeting `specs/core/config/spec.md` in the `default` workspace lives at:\n\n```\n.specd/changes/<change-name>/deltas/default/core/config/spec.md.delta.yaml\n```\n\nDelta files are never synced to permanent spec directories — they remain in the change directory as part of its record.\n\n## Delta entry structure\n\nA delta file is a YAML sequence. Each entry is one delta operation.\n\n| Field         | Required for                   | Description                                                                                                                                               |\n| ------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `op`          | all                            | Operation type: `added`, `modified`, `removed`, or `no-op`.                                                                                               |\n| `selector`    | `modified`, `removed`          | Identifies the existing node to target. Not valid on `added` or `no-op`.                                                                                  |\n| `position`    | `added` (optional)             | Where to insert the new node. See [Position](#position). Not valid on `no-op`.                                                                            |\n| `rename`      | `modified` (optional)          | New label for the node's identifying property (heading text, key name). Only valid on `modified`.                                                         |\n| `content`     | `added`, `modified` (optional) | New node content in the artifact's native format. Mutually exclusive with `value`. Not valid on `no-op`.                                                  |\n| `value`       | `added`, `modified` (optional) | Node value as a structured YAML value. For JSON/YAML structured nodes. Mutually exclusive with `content`. Not valid on `no-op`.                           |\n| `strategy`    | optional                       | Array merge strategy: `replace` (default), `append`, or `merge-by`. Only valid when the selector targets an array or sequence node. Not valid on `no-op`. |\n| `mergeKey`    | required with `merge-by`       | Key field used to match objects in `merge-by` strategy. Not valid on `no-op`.                                                                             |\n| `description` | optional on all                | Free-text explanation of what this entry does or why. Ignored during application. Valid on all operation types including `no-op`.                         |\n\n## The four operations\n\n### modified — change an existing node\n\n`modified` updates the body of an existing node identified by `selector`. The node's identifying property (its heading, key name) is preserved unless `rename` is also specified.\n\n`content` contains only the body — the identifying line is not repeated. SpecD parses it and replaces the node's existing body.\n\n```yaml\n# Change the body of an existing requirement section\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n# Rename the section and update its body in one operation\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Cache'\n  rename: 'Requirement: Cache resolved config'\n  content: |\n    The system must cache the resolved config object in memory for the\n    duration of a single command invocation.\n\n# Update a JSON property value\n- op: modified\n  selector:\n    type: property\n    matches: version\n  value: '2.0.0'\n\n# Update a YAML pair value\n- op: modified\n  selector:\n    type: pair\n    matches: model\n    parent:\n      type: pair\n      matches: llm\n  value: 'claude-opus-4-6'\n```\n\n### removed — delete an existing node\n\n`removed` detaches the identified node and all its children from the document. It takes only a `selector` — no `content` or `value`.\n\n```yaml\n# Remove a section that is no longer relevant\n- op: removed\n  selector:\n    type: section\n    matches: 'Requirement: Legacy auth'\n\n# Remove a JSON property\n- op: removed\n  selector:\n    type: property\n    matches: deprecated_field\n\n# Remove a YAML pair\n- op: removed\n  selector:\n    type: pair\n    matches: old_setting\n```\n\n### added — insert a new node\n\n`added` inserts a new node into the document. It does not take a `selector` — the node being added does not yet exist. Use `position` to control where it is inserted.\n\nFor text-based formats (markdown, plain text), `content` includes the identifying line as its first line — the heading, for a section.\n\n````yaml\n# Append a new requirement to the end of the Requirements section (default position)\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml\n    is modified on disk.\n\n# Insert after a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    after:\n      type: section\n      matches: 'Requirement: Load config'\n  content: |\n    ### Requirement: Validate on load\n\n    The system must validate specd.yaml against its schema immediately\n    after loading and before executing any command.\n\n# Insert before a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    before:\n      type: section\n      matches: 'Requirement: Evict cache on change'\n  content: |\n    ### Requirement: Cache TTL\n\n    The cache must expire after 60 seconds to pick up external edits.\n\n# Insert as the first child of the parent\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    first: true\n  content: |\n    ### Requirement: Discover config file\n\n    The system must discover specd.yaml by walking up from the CWD.\n\n# Append at document root level (no parent — appended to end of document)\n- op: added\n  content: |\n    ## Examples\n\n    ### Example: Minimal config\n\n    ```yaml\n    schema: '@specd/schema-std'\n    ```\n````\n\n### no-op — explicitly document that no change is needed\n\n`no-op` declares that the artifact requires no changes for this delta. It is used when a spec is listed in the proposal but a particular artifact does not need modifications — the existing content is already valid.\n\nRather than omitting the delta file (which is ambiguous — was it forgotten or intentionally skipped?), write a `no-op` entry with a `description` explaining why no change is needed.\n\nA `no-op` entry must be the **only** entry in the delta file. Mixing `no-op` with any other operation is an error — SpecD rejects such a file during parsing.\n\nThe only valid fields on a `no-op` entry are `op` and `description`. All other fields (`selector`, `position`, `rename`, `content`, `value`, `strategy`, `mergeKey`) are invalid.\n\nWhen SpecD encounters a `no-op` delta, it skips delta application, delta validations, and structural validations entirely. It proceeds directly to hash computation on the raw delta file content and marks the artifact complete. The `no-op` declares that the existing artifact is already correct.\n\n```yaml\n# verify.md does not need changes — existing scenarios remain valid\n- op: no-op\n  description: 'Constructor signature changed but all existing verify scenarios still apply.'\n```\n\n```yaml\n# spec.md requires no changes — only design.md and tasks.md are updated in this change\n- op: no-op\n  description: 'No spec changes needed. This change only updates the implementation plan.'\n```\n\n## Description field\n\nEvery delta entry, regardless of operation type, accepts an optional `description` field. Use it to record the intent behind the operation — why this node is being changed, what the business reason is, or what downstream effect is expected. The description is ignored during delta application; it exists purely for human and AI readers.\n\n```yaml\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  description: 'Tightened wording to reflect that the walk stops at the active VCS root, not just any ancestor.'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n- op: added\n  description: 'New requirement introduced by ADR-0012 — cache invalidation on file change.'\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml is modified on disk.\n```\n\n## Position\n\n`position` controls where an `added` entry inserts its node. All fields are optional — omitting `position` entirely appends the node at the end of the document.\n\n| Field    | Description                                                                                                                                                     |\n| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `parent` | Scopes the insertion to the children of the matched node. If omitted, insertion is at document root level.                                                      |\n| `after`  | Inserts immediately after the matched sibling within the parent scope. Falls back to appending at the end of scope with a warning if the sibling is not found.  |\n| `before` | Inserts immediately before the matched sibling within the parent scope. Falls back to appending at the end of scope with a warning if the sibling is not found. |\n| `first`  | Inserts as the first child of the parent scope.                                                                                                                 |\n| `last`   | Inserts as the last child of the parent scope. This is the default when `parent` is specified but no placement hint is given.                                   |\n\n`after`, `before`, `first`, and `last` are mutually exclusive. If only `parent` is given, the node is appended as the last child.\n\n## Array merge strategies\n\nWhen the selector targets an array or sequence node (not an individual item), the `strategy` field controls how the new value is merged:\n\n| Strategy            | Behaviour                                                                                                                                                                                                              |\n| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `replace` (default) | Replaces the entire array with the supplied value.                                                                                                                                                                     |\n| `append`            | Appends the supplied items to the end of the existing array.                                                                                                                                                           |\n| `merge-by`          | Merges the supplied items into the existing array by matching objects on `mergeKey`. Items with a matching key are replaced; items with new keys are appended; existing items not in the supplied value are preserved. |\n\n## Conflict detection\n\nSpecD validates the entire delta for conflicts before applying any operation. If any conflict is found, the whole delta is rejected with a `DeltaApplicationError` and no changes are made to the artifact.\n\n| Conflict                                                                               | Error                                              |\n| :------------------------------------------------------------------------------------- | :------------------------------------------------- |\n| Two `modified` or `removed` entries resolve to the same node                           | Cannot apply two operations to the same node.      |\n| A `rename` target already exists as a sibling                                          | Would produce a duplicate node.                    |\n| Two `modified` entries rename to the same target within the same parent scope          | Ambiguous result.                                  |\n| `content` and `value` both present in the same entry                                   | Mutually exclusive.                                |\n| `selector` on an `added` or `no-op` entry                                              | Use `position.parent` instead for `added`.         |\n| `rename` on an `added`, `removed`, or `no-op` entry                                    | Only valid on `modified`.                          |\n| `strategy: merge-by` without `mergeKey`                                                | `mergeKey` is required for `merge-by`.             |\n| `mergeKey` without `strategy: merge-by`                                                | `mergeKey` is only meaningful with `merge-by`.     |\n| `strategy` on a non-array selector                                                     | Strategy is only valid for array/sequence targets. |\n| More than one of `after`, `before`, `first`, `last` in the same `position`             | Mutually exclusive placement hints.                |\n| `position.parent` resolves to no node                                                  | SpecD cannot scope the insertion.                  |\n| `no-op` entry combined with any other entry in the same delta file                     | `no-op` must be the sole entry.                    |\n| `position`, `rename`, `content`, `value`, `strategy`, or `mergeKey` on a `no-op` entry | Only `op` and `description` are valid on `no-op`.  |\n"
      },
      {
        "index": 2,
        "heading": "What a delta file is",
        "level": 2,
        "startLine": 3,
        "endLine": 8,
        "lines": 6,
        "content": "## What a delta file is\n\nA delta file is a YAML document that expresses changes to an existing spec as a sequence of AST operations. Instead of replacing the entire spec file, the agent produces a `.delta.yaml` that says precisely which sections to add, modify, or remove. SpecD applies the delta deterministically — no LLM is involved in the application step.\n\nDelta files are only produced when `delta: true` is declared on an artifact in the schema. They are only valid for `scope: spec` artifacts.\n"
      },
      {
        "index": 3,
        "heading": "File naming and location",
        "level": 2,
        "startLine": 9,
        "endLine": 33,
        "lines": 25,
        "content": "## File naming and location\n\nA delta file's filename is the target artifact's filename with `.delta.yaml` appended:\n\n| Target artifact | Delta filename            |\n| --------------- | ------------------------- |\n| `spec.md`       | `spec.md.delta.yaml`      |\n| `verify.md`     | `verify.md.delta.yaml`    |\n| `openapi.json`  | `openapi.json.delta.yaml` |\n| `config.yaml`   | `config.yaml.delta.yaml`  |\n\nDelta files live inside the change directory at:\n\n```\ndeltas/<workspace>/<spec-path>/<delta-filename>\n```\n\nFor example, a delta targeting `specs/core/config/spec.md` in the `default` workspace lives at:\n\n```\n.specd/changes/<change-name>/deltas/default/core/config/spec.md.delta.yaml\n```\n\nDelta files are never synced to permanent spec directories — they remain in the change directory as part of its record.\n"
      },
      {
        "index": 4,
        "heading": "Delta entry structure",
        "level": 2,
        "startLine": 34,
        "endLine": 49,
        "lines": 16,
        "content": "## Delta entry structure\n\nA delta file is a YAML sequence. Each entry is one delta operation.\n\n| Field         | Required for                   | Description                                                                                                                                               |\n| ------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `op`          | all                            | Operation type: `added`, `modified`, `removed`, or `no-op`.                                                                                               |\n| `selector`    | `modified`, `removed`          | Identifies the existing node to target. Not valid on `added` or `no-op`.                                                                                  |\n| `position`    | `added` (optional)             | Where to insert the new node. See [Position](#position). Not valid on `no-op`.                                                                            |\n| `rename`      | `modified` (optional)          | New label for the node's identifying property (heading text, key name). Only valid on `modified`.                                                         |\n| `content`     | `added`, `modified` (optional) | New node content in the artifact's native format. Mutually exclusive with `value`. Not valid on `no-op`.                                                  |\n| `value`       | `added`, `modified` (optional) | Node value as a structured YAML value. For JSON/YAML structured nodes. Mutually exclusive with `content`. Not valid on `no-op`.                           |\n| `strategy`    | optional                       | Array merge strategy: `replace` (default), `append`, or `merge-by`. Only valid when the selector targets an array or sequence node. Not valid on `no-op`. |\n| `mergeKey`    | required with `merge-by`       | Key field used to match objects in `merge-by` strategy. Not valid on `no-op`.                                                                             |\n| `description` | optional on all                | Free-text explanation of what this entry does or why. Ignored during application. Valid on all operation types including `no-op`.                         |\n"
      },
      {
        "index": 5,
        "heading": "The four operations",
        "level": 2,
        "startLine": 50,
        "endLine": 215,
        "lines": 166,
        "content": "## The four operations\n\n### modified — change an existing node\n\n`modified` updates the body of an existing node identified by `selector`. The node's identifying property (its heading, key name) is preserved unless `rename` is also specified.\n\n`content` contains only the body — the identifying line is not repeated. SpecD parses it and replaces the node's existing body.\n\n```yaml\n# Change the body of an existing requirement section\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n# Rename the section and update its body in one operation\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Cache'\n  rename: 'Requirement: Cache resolved config'\n  content: |\n    The system must cache the resolved config object in memory for the\n    duration of a single command invocation.\n\n# Update a JSON property value\n- op: modified\n  selector:\n    type: property\n    matches: version\n  value: '2.0.0'\n\n# Update a YAML pair value\n- op: modified\n  selector:\n    type: pair\n    matches: model\n    parent:\n      type: pair\n      matches: llm\n  value: 'claude-opus-4-6'\n```\n\n### removed — delete an existing node\n\n`removed` detaches the identified node and all its children from the document. It takes only a `selector` — no `content` or `value`.\n\n```yaml\n# Remove a section that is no longer relevant\n- op: removed\n  selector:\n    type: section\n    matches: 'Requirement: Legacy auth'\n\n# Remove a JSON property\n- op: removed\n  selector:\n    type: property\n    matches: deprecated_field\n\n# Remove a YAML pair\n- op: removed\n  selector:\n    type: pair\n    matches: old_setting\n```\n\n### added — insert a new node\n\n`added` inserts a new node into the document. It does not take a `selector` — the node being added does not yet exist. Use `position` to control where it is inserted.\n\nFor text-based formats (markdown, plain text), `content` includes the identifying line as its first line — the heading, for a section.\n\n````yaml\n# Append a new requirement to the end of the Requirements section (default position)\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml\n    is modified on disk.\n\n# Insert after a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    after:\n      type: section\n      matches: 'Requirement: Load config'\n  content: |\n    ### Requirement: Validate on load\n\n    The system must validate specd.yaml against its schema immediately\n    after loading and before executing any command.\n\n# Insert before a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    before:\n      type: section\n      matches: 'Requirement: Evict cache on change'\n  content: |\n    ### Requirement: Cache TTL\n\n    The cache must expire after 60 seconds to pick up external edits.\n\n# Insert as the first child of the parent\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    first: true\n  content: |\n    ### Requirement: Discover config file\n\n    The system must discover specd.yaml by walking up from the CWD.\n\n# Append at document root level (no parent — appended to end of document)\n- op: added\n  content: |\n    ## Examples\n\n    ### Example: Minimal config\n\n    ```yaml\n    schema: '@specd/schema-std'\n    ```\n````\n\n### no-op — explicitly document that no change is needed\n\n`no-op` declares that the artifact requires no changes for this delta. It is used when a spec is listed in the proposal but a particular artifact does not need modifications — the existing content is already valid.\n\nRather than omitting the delta file (which is ambiguous — was it forgotten or intentionally skipped?), write a `no-op` entry with a `description` explaining why no change is needed.\n\nA `no-op` entry must be the **only** entry in the delta file. Mixing `no-op` with any other operation is an error — SpecD rejects such a file during parsing.\n\nThe only valid fields on a `no-op` entry are `op` and `description`. All other fields (`selector`, `position`, `rename`, `content`, `value`, `strategy`, `mergeKey`) are invalid.\n\nWhen SpecD encounters a `no-op` delta, it skips delta application, delta validations, and structural validations entirely. It proceeds directly to hash computation on the raw delta file content and marks the artifact complete. The `no-op` declares that the existing artifact is already correct.\n\n```yaml\n# verify.md does not need changes — existing scenarios remain valid\n- op: no-op\n  description: 'Constructor signature changed but all existing verify scenarios still apply.'\n```\n\n```yaml\n# spec.md requires no changes — only design.md and tasks.md are updated in this change\n- op: no-op\n  description: 'No spec changes needed. This change only updates the implementation plan.'\n```\n"
      },
      {
        "index": 6,
        "heading": "modified — change an existing node",
        "level": 3,
        "startLine": 52,
        "endLine": 95,
        "lines": 44,
        "content": "### modified — change an existing node\n\n`modified` updates the body of an existing node identified by `selector`. The node's identifying property (its heading, key name) is preserved unless `rename` is also specified.\n\n`content` contains only the body — the identifying line is not repeated. SpecD parses it and replaces the node's existing body.\n\n```yaml\n# Change the body of an existing requirement section\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n# Rename the section and update its body in one operation\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Cache'\n  rename: 'Requirement: Cache resolved config'\n  content: |\n    The system must cache the resolved config object in memory for the\n    duration of a single command invocation.\n\n# Update a JSON property value\n- op: modified\n  selector:\n    type: property\n    matches: version\n  value: '2.0.0'\n\n# Update a YAML pair value\n- op: modified\n  selector:\n    type: pair\n    matches: model\n    parent:\n      type: pair\n      matches: llm\n  value: 'claude-opus-4-6'\n```\n"
      },
      {
        "index": 7,
        "heading": "removed — delete an existing node",
        "level": 3,
        "startLine": 96,
        "endLine": 119,
        "lines": 24,
        "content": "### removed — delete an existing node\n\n`removed` detaches the identified node and all its children from the document. It takes only a `selector` — no `content` or `value`.\n\n```yaml\n# Remove a section that is no longer relevant\n- op: removed\n  selector:\n    type: section\n    matches: 'Requirement: Legacy auth'\n\n# Remove a JSON property\n- op: removed\n  selector:\n    type: property\n    matches: deprecated_field\n\n# Remove a YAML pair\n- op: removed\n  selector:\n    type: pair\n    matches: old_setting\n```\n"
      },
      {
        "index": 8,
        "heading": "added — insert a new node",
        "level": 3,
        "startLine": 120,
        "endLine": 191,
        "lines": 72,
        "content": "### added — insert a new node\n\n`added` inserts a new node into the document. It does not take a `selector` — the node being added does not yet exist. Use `position` to control where it is inserted.\n\nFor text-based formats (markdown, plain text), `content` includes the identifying line as its first line — the heading, for a section.\n\n````yaml\n# Append a new requirement to the end of the Requirements section (default position)\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml\n    is modified on disk.\n\n# Insert after a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    after:\n      type: section\n      matches: 'Requirement: Load config'\n  content: |\n    ### Requirement: Validate on load\n\n    The system must validate specd.yaml against its schema immediately\n    after loading and before executing any command.\n\n# Insert before a specific sibling\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    before:\n      type: section\n      matches: 'Requirement: Evict cache on change'\n  content: |\n    ### Requirement: Cache TTL\n\n    The cache must expire after 60 seconds to pick up external edits.\n\n# Insert as the first child of the parent\n- op: added\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n    first: true\n  content: |\n    ### Requirement: Discover config file\n\n    The system must discover specd.yaml by walking up from the CWD.\n\n# Append at document root level (no parent — appended to end of document)\n- op: added\n  content: |\n    ## Examples\n\n    ### Example: Minimal config\n\n    ```yaml\n    schema: '@specd/schema-std'\n    ```\n````\n"
      },
      {
        "index": 9,
        "heading": "no-op — explicitly document that no change is needed",
        "level": 3,
        "startLine": 192,
        "endLine": 215,
        "lines": 24,
        "content": "### no-op — explicitly document that no change is needed\n\n`no-op` declares that the artifact requires no changes for this delta. It is used when a spec is listed in the proposal but a particular artifact does not need modifications — the existing content is already valid.\n\nRather than omitting the delta file (which is ambiguous — was it forgotten or intentionally skipped?), write a `no-op` entry with a `description` explaining why no change is needed.\n\nA `no-op` entry must be the **only** entry in the delta file. Mixing `no-op` with any other operation is an error — SpecD rejects such a file during parsing.\n\nThe only valid fields on a `no-op` entry are `op` and `description`. All other fields (`selector`, `position`, `rename`, `content`, `value`, `strategy`, `mergeKey`) are invalid.\n\nWhen SpecD encounters a `no-op` delta, it skips delta application, delta validations, and structural validations entirely. It proceeds directly to hash computation on the raw delta file content and marks the artifact complete. The `no-op` declares that the existing artifact is already correct.\n\n```yaml\n# verify.md does not need changes — existing scenarios remain valid\n- op: no-op\n  description: 'Constructor signature changed but all existing verify scenarios still apply.'\n```\n\n```yaml\n# spec.md requires no changes — only design.md and tasks.md are updated in this change\n- op: no-op\n  description: 'No spec changes needed. This change only updates the implementation plan.'\n```\n"
      },
      {
        "index": 10,
        "heading": "Description field",
        "level": 2,
        "startLine": 216,
        "endLine": 241,
        "lines": 26,
        "content": "## Description field\n\nEvery delta entry, regardless of operation type, accepts an optional `description` field. Use it to record the intent behind the operation — why this node is being changed, what the business reason is, or what downstream effect is expected. The description is ignored during delta application; it exists purely for human and AI readers.\n\n```yaml\n- op: modified\n  selector:\n    type: section\n    matches: 'Requirement: Load config'\n  description: 'Tightened wording to reflect that the walk stops at the active VCS root, not just any ancestor.'\n  content: |\n    The system must load specd.yaml from the nearest ancestor directory\n    that contains the file, stopping at the active VCS root.\n\n- op: added\n  description: 'New requirement introduced by ADR-0012 — cache invalidation on file change.'\n  position:\n    parent:\n      type: section\n      matches: '^Requirements$'\n  content: |\n    ### Requirement: Evict cache on change\n\n    The system must evict the in-memory config cache when specd.yaml is modified on disk.\n```\n"
      },
      {
        "index": 11,
        "heading": "Position",
        "level": 2,
        "startLine": 242,
        "endLine": 255,
        "lines": 14,
        "content": "## Position\n\n`position` controls where an `added` entry inserts its node. All fields are optional — omitting `position` entirely appends the node at the end of the document.\n\n| Field    | Description                                                                                                                                                     |\n| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `parent` | Scopes the insertion to the children of the matched node. If omitted, insertion is at document root level.                                                      |\n| `after`  | Inserts immediately after the matched sibling within the parent scope. Falls back to appending at the end of scope with a warning if the sibling is not found.  |\n| `before` | Inserts immediately before the matched sibling within the parent scope. Falls back to appending at the end of scope with a warning if the sibling is not found. |\n| `first`  | Inserts as the first child of the parent scope.                                                                                                                 |\n| `last`   | Inserts as the last child of the parent scope. This is the default when `parent` is specified but no placement hint is given.                                   |\n\n`after`, `before`, `first`, and `last` are mutually exclusive. If only `parent` is given, the node is appended as the last child.\n"
      },
      {
        "index": 12,
        "heading": "Array merge strategies",
        "level": 2,
        "startLine": 256,
        "endLine": 265,
        "lines": 10,
        "content": "## Array merge strategies\n\nWhen the selector targets an array or sequence node (not an individual item), the `strategy` field controls how the new value is merged:\n\n| Strategy            | Behaviour                                                                                                                                                                                                              |\n| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `replace` (default) | Replaces the entire array with the supplied value.                                                                                                                                                                     |\n| `append`            | Appends the supplied items to the end of the existing array.                                                                                                                                                           |\n| `merge-by`          | Merges the supplied items into the existing array by matching objects on `mergeKey`. Items with a matching key are replaced; items with new keys are appended; existing items not in the supplied value are preserved. |\n"
      },
      {
        "index": 13,
        "heading": "Conflict detection",
        "level": 2,
        "startLine": 266,
        "endLine": 285,
        "lines": 20,
        "content": "## Conflict detection\n\nSpecD validates the entire delta for conflicts before applying any operation. If any conflict is found, the whole delta is rejected with a `DeltaApplicationError` and no changes are made to the artifact.\n\n| Conflict                                                                               | Error                                              |\n| :------------------------------------------------------------------------------------- | :------------------------------------------------- |\n| Two `modified` or `removed` entries resolve to the same node                           | Cannot apply two operations to the same node.      |\n| A `rename` target already exists as a sibling                                          | Would produce a duplicate node.                    |\n| Two `modified` entries rename to the same target within the same parent scope          | Ambiguous result.                                  |\n| `content` and `value` both present in the same entry                                   | Mutually exclusive.                                |\n| `selector` on an `added` or `no-op` entry                                              | Use `position.parent` instead for `added`.         |\n| `rename` on an `added`, `removed`, or `no-op` entry                                    | Only valid on `modified`.                          |\n| `strategy: merge-by` without `mergeKey`                                                | `mergeKey` is required for `merge-by`.             |\n| `mergeKey` without `strategy: merge-by`                                                | `mergeKey` is only meaningful with `merge-by`.     |\n| `strategy` on a non-array selector                                                     | Strategy is only valid for array/sequence targets. |\n| More than one of `after`, `before`, `first`, `last` in the same `position`             | Mutually exclusive placement hints.                |\n| `position.parent` resolves to no node                                                  | SpecD cannot scope the insertion.                  |\n| `no-op` entry combined with any other entry in the same delta file                     | `no-op` must be the sole entry.                    |\n| `position`, `rename`, `content`, `value`, `strategy`, or `mergeKey` on a `no-op` entry | Only `op` and `description` are valid on `no-op`.  |\n"
      }
    ]
  },
  {
    "topic": "code-graph",
    "title": "Code Graph & Blast Radius Analysis",
    "description": "Leveraging AST-based dependency graphs, impact analysis, and symbol indexing.",
    "order": 10,
    "content": "# Code Graph & Blast Radius Analysis\n\nThe SpecD Code Graph is an AST-powered codebase intelligence system. It maps symbols, imports, calls, spec requirements, and implementation links across your repository.\n\nUnlike generic lexical search tools (`grep` or text matching), the Code Graph understands syntactic language constructs and structural dependencies. This enables AI agents and developers to perform graph-first research, assess blast radius, and detect high-risk hotspots before making modifications.\n\n---\n\n## Why Graph-First Research Matters\n\nIn large codebases, modifying a function or changing an interface often triggers unexpected breaking changes in distant modules. AI agents relying on blind grep frequently miss indirect call sites or waste thousands of context tokens reading irrelevant files.\n\nThe Code Graph solves this with:\n\n- **Symbol indexing**: Instant $O(1)$ symbol definition lookups across files and workspaces.\n- **Bi-directional impact analysis**: Tracing who depends on a symbol (`dependents`) and what a symbol relies on (`dependencies`).\n- **High-risk hotspot identification**: Pinpointing files with high coupling and cyclomatic complexity.\n- **Traceable spec links**: Connecting normative requirements directly to the code that realizes them.\n\n---\n\n## Core CLI Commands\n\n### 1. Indexing the Code Graph (`specd graph index`)\n\nBefore querying the graph, ensure the index is fresh:\n\n```bash\nspecd graph index --format toon\n```\n\nThe command scans repository workspaces, parses TypeScript/JavaScript ASTs, and caches the dependency graph under `.specd/config/graph/`.\n\n### 2. Finding Symbols & Definitions (`specd graph search`)\n\nSearch for symbols, function definitions, types, or interfaces:\n\n```bash\n# Search for symbol definitions\nspecd graph search \"ConfigCascade\" --symbols --format toon\n\n# Search for specs mentioning a capability\nspecd graph search \"lifecycle\" --specs --format toon\n```\n\n### 3. Blast Radius Analysis (`specd graph impact`)\n\nCalculate the exact blast radius of changing a file or symbol before writing code:\n\n```bash\n# Analyze downstream dependents (who breaks if this symbol changes?)\nspecd graph impact --symbol \"resolveActiveChain\" --direction dependents --format toon\n\n# Analyze downstream dependents of a specific file\nspecd graph impact --file \"packages/core/src/infrastructure/fs/config-cascade.ts\" --direction dependents --format toon\n\n# Analyze upstream dependencies (what does this file rely on?)\nspecd graph impact --file \"packages/cli/src/commands/guide/index.ts\" --direction dependencies --format toon\n```\n\nOutput highlights the calculated risk level (`LOW`, `MEDIUM`, `HIGH`, `CRITICAL`) and enumerates all affected files.\n\n### 4. Detecting Hotspots (`specd graph hotspots`)\n\nIdentify complex, tightly-coupled files that require extra caution and testing:\n\n```bash\nspecd graph hotspots --min-risk HIGH --format toon\n```\n\n---\n\n## Agent Protocol: The Graph-First Rule\n\nAll AI agents operating in a SpecD repository follow the **Graph-First Protocol**:\n\n1. **Never guess paths or symbol usages with ad-hoc grep first.**\n2. When orienting or planning design changes:\n   - Check index freshness: `specd project status --graph`\n   - Re-index if stale: `specd graph index`\n   - Discover symbol locations: `specd graph search \"<symbol>\" --symbols`\n   - Calculate blast radius: `specd graph impact --symbol \"<symbol>\" --direction dependents`\n3. Include impact findings directly in `design.md` so reviewers and developers understand downstream implications before implementation starts.\n",
    "lineCount": 84,
    "byteLength": 3545,
    "outline": [
      {
        "index": 1,
        "heading": "Code Graph & Blast Radius Analysis",
        "level": 1,
        "startLine": 1,
        "endLine": 84,
        "lines": 84,
        "content": "# Code Graph & Blast Radius Analysis\n\nThe SpecD Code Graph is an AST-powered codebase intelligence system. It maps symbols, imports, calls, spec requirements, and implementation links across your repository.\n\nUnlike generic lexical search tools (`grep` or text matching), the Code Graph understands syntactic language constructs and structural dependencies. This enables AI agents and developers to perform graph-first research, assess blast radius, and detect high-risk hotspots before making modifications.\n\n---\n\n## Why Graph-First Research Matters\n\nIn large codebases, modifying a function or changing an interface often triggers unexpected breaking changes in distant modules. AI agents relying on blind grep frequently miss indirect call sites or waste thousands of context tokens reading irrelevant files.\n\nThe Code Graph solves this with:\n\n- **Symbol indexing**: Instant $O(1)$ symbol definition lookups across files and workspaces.\n- **Bi-directional impact analysis**: Tracing who depends on a symbol (`dependents`) and what a symbol relies on (`dependencies`).\n- **High-risk hotspot identification**: Pinpointing files with high coupling and cyclomatic complexity.\n- **Traceable spec links**: Connecting normative requirements directly to the code that realizes them.\n\n---\n\n## Core CLI Commands\n\n### 1. Indexing the Code Graph (`specd graph index`)\n\nBefore querying the graph, ensure the index is fresh:\n\n```bash\nspecd graph index --format toon\n```\n\nThe command scans repository workspaces, parses TypeScript/JavaScript ASTs, and caches the dependency graph under `.specd/config/graph/`.\n\n### 2. Finding Symbols & Definitions (`specd graph search`)\n\nSearch for symbols, function definitions, types, or interfaces:\n\n```bash\n# Search for symbol definitions\nspecd graph search \"ConfigCascade\" --symbols --format toon\n\n# Search for specs mentioning a capability\nspecd graph search \"lifecycle\" --specs --format toon\n```\n\n### 3. Blast Radius Analysis (`specd graph impact`)\n\nCalculate the exact blast radius of changing a file or symbol before writing code:\n\n```bash\n# Analyze downstream dependents (who breaks if this symbol changes?)\nspecd graph impact --symbol \"resolveActiveChain\" --direction dependents --format toon\n\n# Analyze downstream dependents of a specific file\nspecd graph impact --file \"packages/core/src/infrastructure/fs/config-cascade.ts\" --direction dependents --format toon\n\n# Analyze upstream dependencies (what does this file rely on?)\nspecd graph impact --file \"packages/cli/src/commands/guide/index.ts\" --direction dependencies --format toon\n```\n\nOutput highlights the calculated risk level (`LOW`, `MEDIUM`, `HIGH`, `CRITICAL`) and enumerates all affected files.\n\n### 4. Detecting Hotspots (`specd graph hotspots`)\n\nIdentify complex, tightly-coupled files that require extra caution and testing:\n\n```bash\nspecd graph hotspots --min-risk HIGH --format toon\n```\n\n---\n\n## Agent Protocol: The Graph-First Rule\n\nAll AI agents operating in a SpecD repository follow the **Graph-First Protocol**:\n\n1. **Never guess paths or symbol usages with ad-hoc grep first.**\n2. When orienting or planning design changes:\n   - Check index freshness: `specd project status --graph`\n   - Re-index if stale: `specd graph index`\n   - Discover symbol locations: `specd graph search \"<symbol>\" --symbols`\n   - Calculate blast radius: `specd graph impact --symbol \"<symbol>\" --direction dependents`\n3. Include impact findings directly in `design.md` so reviewers and developers understand downstream implications before implementation starts.\n"
      },
      {
        "index": 2,
        "heading": "Why Graph-First Research Matters",
        "level": 2,
        "startLine": 9,
        "endLine": 21,
        "lines": 13,
        "content": "## Why Graph-First Research Matters\n\nIn large codebases, modifying a function or changing an interface often triggers unexpected breaking changes in distant modules. AI agents relying on blind grep frequently miss indirect call sites or waste thousands of context tokens reading irrelevant files.\n\nThe Code Graph solves this with:\n\n- **Symbol indexing**: Instant $O(1)$ symbol definition lookups across files and workspaces.\n- **Bi-directional impact analysis**: Tracing who depends on a symbol (`dependents`) and what a symbol relies on (`dependencies`).\n- **High-risk hotspot identification**: Pinpointing files with high coupling and cyclomatic complexity.\n- **Traceable spec links**: Connecting normative requirements directly to the code that realizes them.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "Core CLI Commands",
        "level": 2,
        "startLine": 22,
        "endLine": 72,
        "lines": 51,
        "content": "## Core CLI Commands\n\n### 1. Indexing the Code Graph (`specd graph index`)\n\nBefore querying the graph, ensure the index is fresh:\n\n```bash\nspecd graph index --format toon\n```\n\nThe command scans repository workspaces, parses TypeScript/JavaScript ASTs, and caches the dependency graph under `.specd/config/graph/`.\n\n### 2. Finding Symbols & Definitions (`specd graph search`)\n\nSearch for symbols, function definitions, types, or interfaces:\n\n```bash\n# Search for symbol definitions\nspecd graph search \"ConfigCascade\" --symbols --format toon\n\n# Search for specs mentioning a capability\nspecd graph search \"lifecycle\" --specs --format toon\n```\n\n### 3. Blast Radius Analysis (`specd graph impact`)\n\nCalculate the exact blast radius of changing a file or symbol before writing code:\n\n```bash\n# Analyze downstream dependents (who breaks if this symbol changes?)\nspecd graph impact --symbol \"resolveActiveChain\" --direction dependents --format toon\n\n# Analyze downstream dependents of a specific file\nspecd graph impact --file \"packages/core/src/infrastructure/fs/config-cascade.ts\" --direction dependents --format toon\n\n# Analyze upstream dependencies (what does this file rely on?)\nspecd graph impact --file \"packages/cli/src/commands/guide/index.ts\" --direction dependencies --format toon\n```\n\nOutput highlights the calculated risk level (`LOW`, `MEDIUM`, `HIGH`, `CRITICAL`) and enumerates all affected files.\n\n### 4. Detecting Hotspots (`specd graph hotspots`)\n\nIdentify complex, tightly-coupled files that require extra caution and testing:\n\n```bash\nspecd graph hotspots --min-risk HIGH --format toon\n```\n\n---\n"
      },
      {
        "index": 4,
        "heading": "1. Indexing the Code Graph (`specd graph index`)",
        "level": 3,
        "startLine": 24,
        "endLine": 33,
        "lines": 10,
        "content": "### 1. Indexing the Code Graph (`specd graph index`)\n\nBefore querying the graph, ensure the index is fresh:\n\n```bash\nspecd graph index --format toon\n```\n\nThe command scans repository workspaces, parses TypeScript/JavaScript ASTs, and caches the dependency graph under `.specd/config/graph/`.\n"
      },
      {
        "index": 5,
        "heading": "2. Finding Symbols & Definitions (`specd graph search`)",
        "level": 3,
        "startLine": 34,
        "endLine": 45,
        "lines": 12,
        "content": "### 2. Finding Symbols & Definitions (`specd graph search`)\n\nSearch for symbols, function definitions, types, or interfaces:\n\n```bash\n# Search for symbol definitions\nspecd graph search \"ConfigCascade\" --symbols --format toon\n\n# Search for specs mentioning a capability\nspecd graph search \"lifecycle\" --specs --format toon\n```\n"
      },
      {
        "index": 6,
        "heading": "3. Blast Radius Analysis (`specd graph impact`)",
        "level": 3,
        "startLine": 46,
        "endLine": 62,
        "lines": 17,
        "content": "### 3. Blast Radius Analysis (`specd graph impact`)\n\nCalculate the exact blast radius of changing a file or symbol before writing code:\n\n```bash\n# Analyze downstream dependents (who breaks if this symbol changes?)\nspecd graph impact --symbol \"resolveActiveChain\" --direction dependents --format toon\n\n# Analyze downstream dependents of a specific file\nspecd graph impact --file \"packages/core/src/infrastructure/fs/config-cascade.ts\" --direction dependents --format toon\n\n# Analyze upstream dependencies (what does this file rely on?)\nspecd graph impact --file \"packages/cli/src/commands/guide/index.ts\" --direction dependencies --format toon\n```\n\nOutput highlights the calculated risk level (`LOW`, `MEDIUM`, `HIGH`, `CRITICAL`) and enumerates all affected files.\n"
      },
      {
        "index": 7,
        "heading": "4. Detecting Hotspots (`specd graph hotspots`)",
        "level": 3,
        "startLine": 63,
        "endLine": 72,
        "lines": 10,
        "content": "### 4. Detecting Hotspots (`specd graph hotspots`)\n\nIdentify complex, tightly-coupled files that require extra caution and testing:\n\n```bash\nspecd graph hotspots --min-risk HIGH --format toon\n```\n\n---\n"
      },
      {
        "index": 8,
        "heading": "Agent Protocol: The Graph-First Rule",
        "level": 2,
        "startLine": 73,
        "endLine": 84,
        "lines": 12,
        "content": "## Agent Protocol: The Graph-First Rule\n\nAll AI agents operating in a SpecD repository follow the **Graph-First Protocol**:\n\n1. **Never guess paths or symbol usages with ad-hoc grep first.**\n2. When orienting or planning design changes:\n   - Check index freshness: `specd project status --graph`\n   - Re-index if stale: `specd graph index`\n   - Discover symbol locations: `specd graph search \"<symbol>\" --symbols`\n   - Calculate blast radius: `specd graph impact --symbol \"<symbol>\" --direction dependents`\n3. Include impact findings directly in `design.md` so reviewers and developers understand downstream implications before implementation starts.\n"
      }
    ]
  },
  {
    "topic": "configuration-examples",
    "title": "Configuration Examples",
    "description": "Practical specd.yaml configuration examples ranging from minimal single-repo setups to multi-workspace coordinators and an exhaustive reference configuration.",
    "order": 10,
    "content": "# Configuration Examples\n\nThis guide provides practical, ready-to-use `specd.yaml` configuration examples for common project architectures, followed by an exhaustive reference configuration demonstrating all available settings in a single file.\n\n---\n\n## Example 1: Minimal Single-Repo Project\n\nThis is the standard starting point for most projects: a single Git repository, the default published schema (`@specd/schema-std`), and standard local filesystem storage.\n\nRun `specd project init` in a fresh repository to generate this structure automatically.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\n### Key behaviors\n\n- **Schema:** `@specd/schema-std` is resolved from `node_modules/@specd/schema-std/schema.yaml`. Version pinning is handled via `package.json`.\n- **Workspace:** The single `default` workspace stores all specs in `specs/`. `codeRoot` defaults to the repository root (`.`), and `ownership` defaults to `owned`.\n- **Storage:** `.specd/changes` tracks in-progress changes, `.specd/drafts` stores shelved drafts, `.specd/discarded` retains abandoned changes, and `.specd/archive` stores permanent records of completed changes.\n- **Approvals:** Both approval gates (`spec` and `signoff`) are disabled by default (`false`). Transitions require no manual sign-off.\n\n---\n\n## Example 2: Single-Repo with Local Custom Schema\n\nUse this setup when your team has custom workflow steps, unique artifact types, or specialized validation rules defined locally in the repository rather than from an npm package.\n\n### Directory structure\n\n```\n.specd/\n└── schemas/\n    └── custom-workflow/\n        ├── schema.yaml\n        └── templates/\n            ├── proposal.md\n            ├── spec.md\n            └── tasks.md\n```\n\n### `specd.yaml`\n\n```yaml\nschema: 'custom-workflow' # Resolves to .specd/schemas/custom-workflow/schema.yaml\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Optional: this is the default path for 'default'\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\n### Key behaviors\n\n- **Schema reference:** Bare names (e.g. `'custom-workflow'`) or hashed names (e.g. `'#custom-workflow'`) resolve against `schemas.adapter.fs.path` of the `default` workspace.\n- **Local versioning:** The schema is committed and versioned alongside your application code. Updates take effect immediately for new changes.\n\n---\n\n## Example 3: Multi-Workspace Monorepo & Coordinator\n\nUse this pattern in a monorepo where a top-level `default` workspace coordinates multiple packages (e.g. `packages/core`, `packages/cli`, `apps/web`), each having its own independent spec folder and code root.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  # The default workspace covers root-level scripts, configs, and shared global specs.\n  # Because codeRoot defaults to '.' (the repo root), it physically encloses all child packages.\n  # We MUST exclude child directories in graph.excludePaths so the graph engine does not\n  # discover their files twice (once under default and once under the child workspace).\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    codeRoot: .\n    ownership: owned\n    graph:\n      excludePaths:\n        - node_modules/\n        - .git/\n        - .specd/\n        - dist/\n        - build/\n        - coverage/\n        - packages/ # Indexed independently by child workspaces below\n        - apps/ # Indexed independently by child workspaces below\n        - specs/\n        - specd-sdd/\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:composition'\n\n  cli:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-core/specs\n    codeRoot: ../platform-core\n    ownership: readOnly # Read-only: context only, modifications rejected\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.archivedName}}'\n```\n\n### Key behaviors\n\n- **Monorepo root exclusion pattern:** In monorepos where `workspaces.default.codeRoot` is `.` (the project root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` (such as `packages/core` or `packages/cli`), their source files would be indexed twice, causing symbol duplicate errors and inflated graph databases. Listing `packages/` and `apps/` in `workspaces.default.graph.excludePaths` ensures child packages are only indexed under their dedicated workspaces.\n- **`ownership: readOnly`:** The coordinator can include specs from `platform` into compiled context for AI agents, but SpecD rejects changes that propose mutations to `platform` specs.\n- **`prefix: _global`:** Specs located under `specs/_global/architecture.md` are referenced with ID `default:_global/architecture`.\n- **Targeted context filters:** When `core` is active in a change, its `composition` specs are automatically pulled into context.\n- **Yearly archive folders:** Changes archive into `.specd/archive/2026/2026-09-24-my-change/`.\n\n---\n\n## Example 4: Enterprise Governance, Approvals & Lifecycle Hooks\n\nUse this setup for mission-critical repositories where changes require human sign-off gates, audit trails with privacy obfuscation, and automated shell verification at lifecycle transitions.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\n# Privacy protection for public or semi-private repositories\nprivacy:\n  mode: mask # Partially mask author names and email addresses in manifests\n  excludeActors: # Retain explicit automation bot identities verbatim\n    - 'specd'\n    - 'system@getspecd.dev'\n    - 'github-actions[bot]'\n  allowedMetadataKeys:\n    - 'department'\n\n# Global context guidance for agents\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Do not modify public API signatures without backward compatibility.'\n\n# Mandatory approval gates\napprovals:\n  spec: true # Human reviewer must approve specs before 'implementing' state\n  signoff: true # Human reviewer must approve verification before 'archivable' state\n\n# Artifact invalidation strategy\ninvalidationPolicy: downstream # Automatically reopen drifted files and their DAG descendants\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.name}}/{{change.archivedName}}'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n# Inline schema overrides for lifecycle automation hooks\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: verify-approval-instruction\n              instruction: |\n                Confirm human spec approval is recorded in the change manifest before writing code.\n      - step: archiving\n        hooks:\n          pre:\n            - id: run-test-suite\n              run: 'pnpm test'\n          post:\n            - id: notify-audit-log\n              run: 'node scripts/audit-log.js --change \"{{change.name}}\" --path \"{{change.path}}\"'\n```\n\n---\n\n## Example 5: Layered Configuration Cascade (`specd.local.yaml`)\n\nUse the configuration cascade to provide personal developer overrides or CI-specific variations without altering the shared `specd.yaml`.\n\n### Base configuration (`specd.yaml`)\n\n```yaml\nschema: '@specd/schema-std'\n\nlogging:\n  level: info\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n  legacy:\n    specs:\n      adapter: fs\n      fs:\n        path: legacy/specs/\n    codeRoot: legacy/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n\ncontext:\n  - id: global-rules\n    instruction: 'Standard repository conventions apply.'\n```\n\n### Local developer override (`specd.local.yaml`)\n\nThis file is automatically gitignored by `specd project init`:\n\n```yaml\nextends: true # Deep-merge on top of base specd.yaml\n\nlogging:\n  level: debug # Enable verbose debug logs locally\n\ncontext:\n  - instruction: 'Local dev environment: run integration tests against Docker.'\n\nremove:\n  workspaces:\n    - legacy # Do not index or load legacy workspace on this dev machine\n```\n\n---\n\n## Example 6: Exhaustive Reference Configuration\n\nBelow is a complete `specd.yaml` showcasing **every configuration field** supported by the SpecD schema, with detailed comments explaining types, defaults, and runtime effects.\n\n```yaml\n# ==============================================================================\n# SpecD Master Configuration Reference (Exhaustive)\n# ==============================================================================\n\n# 1. Schema Selection (Required)\n# Reference to the workflow schema package, local schema (#name), or file path.\n# Overridden by environment variable: SPECD_SCHEMA\nschema: '@specd/schema-std'\n\n# 2. State & Runtime Paths\n# Relative directory paths resolved against the directory containing specd.yaml.\n# specdPath: location of workflow directories (changes, drafts, archive)\nspecdPath: '.specd' # Default: '.specd'\n\n# configPath: location of engine operational data (graph databases, logs, tmp)\nconfigPath: '.specd/config' # Default: '.specd/config'\n\n# 3. VCS Identity Provider\n# Forces a specific VCS identity provider ('git', 'hg'). Bypasses auto-detection.\n# Overridden by environment variable: SPECD_ACTOR_PROVIDER\nactorProvider: 'git'\n\n# 4. Identity Privacy & Obfuscation\n# Protects contributor names and emails in public change manifests and archives.\n# Overridden by environment variables: SPECD_PRIVACY_MODE, SPECD_PRIVACY_SALT\nprivacy:\n  mode: mask # 'hash' | 'mask' | 'anonymous' (Default: null / none)\n  salt: 'secret-hash-salt' # Required when mode is 'hash'\n  # Actors exempt from obfuscation. Setting this REPLACES the built-in defaults:\n  excludeActors:\n    - 'specd' # Built-in default\n    - 'system@getspecd.dev' # Built-in default\n    - 'ci-bot'\n  # Whitelist of metadata keys preserved under privacy mode (all others stripped):\n  allowedMetadataKeys:\n    - 'team'\n    - 'department'\n\n# 5. Logging Configuration\n# Controls disk logging threshold at {configPath}/log/specd.log.\n# Overridden by environment variable: SPECD_LOG_LEVEL\nlogging:\n  level: info # 'trace' | 'debug' | 'info' | 'warn' | 'error' | 'silent' (Default: 'info')\n\n# 6. Global Prompt Context\n# Content injected at the beginning of prompt context delivered to AI agents.\ncontext:\n  - file: AGENTS.md # File path relative to specd.yaml (injected verbatim)\n  - id: global-guidance # Optional identifier used for removal in cascade layers\n    instruction: 'Prefer editing existing functions over introducing redundant abstractions.'\n\n# 7. Global Spec Inclusion & Exclusion Patterns\n# Patterns applied to all compiled contexts across all active changes.\ncontextIncludeSpecs:\n  - 'default:_global/*' # Include all global constraints in every change context\ncontextExcludeSpecs:\n  - 'default:_global/archive-policy' # Exclude specific specs unless directly targeted\n\n# 8. Context Presentation & Optimization\n# Spec rendering format in compiled agent prompt context:\n# 'summary' (compact catalogue), 'hybrid' (full active specs, summarized deps),\n# 'full' (all specs verbatim), 'list' (IDs only).\n# Overridden by environment variable: SPECD_CONTEXT_MODE\ncontextMode: summary # Default: 'summary'\n\n# When true, gates optimizer agent writes to metadata cache.\n# Overridden by environment variable: SPECD_LLM_OPTIMIZED\nllmOptimizedContext: false # Default: false\n\n# 9. Invalidation Propagation Policy\n# Strategy used when change artifacts drift from baseline or are invalidated.\n# 'none', 'surgical' (target only), 'downstream' (target + DAG descendants), 'global' (all).\ninvalidationPolicy: downstream # Default: 'downstream'\n\n# 10. Approval Gates\n# When true, requires explicit human review commands before advancing states:\n# 'spec': blocks ready -> implementing until 'specd changes approve-spec'\n# 'signoff': blocks done -> archivable until 'specd changes signoff'\napprovals:\n  spec: false # Default: false\n  signoff: false # Default: false\n\n# 11. Project-Level Code Graph Indexing\ngraph:\n  includePaths:\n    - 'tools/**/*.ts' # Root-level files indexed under reserved 'root:' namespace\n  # Setting excludePaths REPLACES the built-in defaults ('node_modules/', '.git/', 'dist/', etc.):\n  excludePaths:\n    - 'node_modules/'\n    - '.git/'\n    - '.specd/'\n    - 'dist/'\n    - 'build/'\n    - 'coverage/'\n    - '**/*.spec.ts'\n\n# 12. Workspaces Configuration\n# Defines repositories and sub-directories containing specs and source code.\nworkspaces:\n  default:\n    prefix: _global # Logical prefix prepended to all spec IDs in this workspace\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global # Path to specs folder\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Path to local schemas (Default: .specd/schemas)\n    codeRoot: . # Implementation source code root (Default: '.' for default)\n    ownership: owned # 'owned' | 'shared' | 'readOnly' (Default: 'owned')\n    contextIncludeSpecs:\n      - 'default:*'\n    contextExcludeSpecs:\n      - 'default:_global/deprecated/*'\n    graph:\n      respectGitignore: true # When false, gitignored files are indexed (Default: true)\n      # In monorepos where default codeRoot is '.', exclude child workspace folders to avoid duplicate indexing:\n      excludePaths:\n        - 'packages/'\n        - 'apps/'\n        - 'temp/'\n      allowedPaths: # When defined, restricts discovery to matching paths\n        - 'src/**'\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/core/specs\n    codeRoot: packages/core # Required for non-default workspaces\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:ports/*'\n\n  external-service:\n    specs:\n      adapter: fs\n      fs:\n        path: ../service/specd/specs\n    codeRoot: ../service\n    ownership: readOnly # Read-only workspace: context-only, cannot be modified\n\n# 13. Storage Configuration\n# Persistent storage directories for the change lifecycle stages.\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes # Active changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts # Shelved drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded # Abandoned changes\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive # Completed changes\n      pattern: '{{year}}/{{change.archivedName}}' # Directory template for archives\n\n# 14. Schema Plugins\n# Schema extension packages loaded and merged before schemaOverrides.\nschemaPlugins:\n  - '@specd/plugin-schema-enterprise'\n\n# 15. Inline Schema Overrides\n# Declarative modifications to the active schema without forking.\n# Supported operations: create, remove, set, append, prepend.\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: audit-pre-check\n              instruction: 'Confirm task list is up to date before editing code.'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-check\n              run: 'pnpm test'\n          post:\n            - id: post-archive-log\n              run: 'echo \"Change {{change.name}} successfully archived.\"'\n\n# 16. Installed Agent Plugins\n# Coding assistant plugins registered in this project.\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-claude'\n    - name: '@specd/plugin-agent-copilot'\n      config:\n        model: 'gpt-4o'\n```\n",
    "lineCount": 554,
    "byteLength": 16753,
    "outline": [
      {
        "index": 1,
        "heading": "Configuration Examples",
        "level": 1,
        "startLine": 1,
        "endLine": 554,
        "lines": 554,
        "content": "# Configuration Examples\n\nThis guide provides practical, ready-to-use `specd.yaml` configuration examples for common project architectures, followed by an exhaustive reference configuration demonstrating all available settings in a single file.\n\n---\n\n## Example 1: Minimal Single-Repo Project\n\nThis is the standard starting point for most projects: a single Git repository, the default published schema (`@specd/schema-std`), and standard local filesystem storage.\n\nRun `specd project init` in a fresh repository to generate this structure automatically.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\n### Key behaviors\n\n- **Schema:** `@specd/schema-std` is resolved from `node_modules/@specd/schema-std/schema.yaml`. Version pinning is handled via `package.json`.\n- **Workspace:** The single `default` workspace stores all specs in `specs/`. `codeRoot` defaults to the repository root (`.`), and `ownership` defaults to `owned`.\n- **Storage:** `.specd/changes` tracks in-progress changes, `.specd/drafts` stores shelved drafts, `.specd/discarded` retains abandoned changes, and `.specd/archive` stores permanent records of completed changes.\n- **Approvals:** Both approval gates (`spec` and `signoff`) are disabled by default (`false`). Transitions require no manual sign-off.\n\n---\n\n## Example 2: Single-Repo with Local Custom Schema\n\nUse this setup when your team has custom workflow steps, unique artifact types, or specialized validation rules defined locally in the repository rather than from an npm package.\n\n### Directory structure\n\n```\n.specd/\n└── schemas/\n    └── custom-workflow/\n        ├── schema.yaml\n        └── templates/\n            ├── proposal.md\n            ├── spec.md\n            └── tasks.md\n```\n\n### `specd.yaml`\n\n```yaml\nschema: 'custom-workflow' # Resolves to .specd/schemas/custom-workflow/schema.yaml\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Optional: this is the default path for 'default'\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\n### Key behaviors\n\n- **Schema reference:** Bare names (e.g. `'custom-workflow'`) or hashed names (e.g. `'#custom-workflow'`) resolve against `schemas.adapter.fs.path` of the `default` workspace.\n- **Local versioning:** The schema is committed and versioned alongside your application code. Updates take effect immediately for new changes.\n\n---\n\n## Example 3: Multi-Workspace Monorepo & Coordinator\n\nUse this pattern in a monorepo where a top-level `default` workspace coordinates multiple packages (e.g. `packages/core`, `packages/cli`, `apps/web`), each having its own independent spec folder and code root.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  # The default workspace covers root-level scripts, configs, and shared global specs.\n  # Because codeRoot defaults to '.' (the repo root), it physically encloses all child packages.\n  # We MUST exclude child directories in graph.excludePaths so the graph engine does not\n  # discover their files twice (once under default and once under the child workspace).\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    codeRoot: .\n    ownership: owned\n    graph:\n      excludePaths:\n        - node_modules/\n        - .git/\n        - .specd/\n        - dist/\n        - build/\n        - coverage/\n        - packages/ # Indexed independently by child workspaces below\n        - apps/ # Indexed independently by child workspaces below\n        - specs/\n        - specd-sdd/\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:composition'\n\n  cli:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-core/specs\n    codeRoot: ../platform-core\n    ownership: readOnly # Read-only: context only, modifications rejected\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.archivedName}}'\n```\n\n### Key behaviors\n\n- **Monorepo root exclusion pattern:** In monorepos where `workspaces.default.codeRoot` is `.` (the project root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` (such as `packages/core` or `packages/cli`), their source files would be indexed twice, causing symbol duplicate errors and inflated graph databases. Listing `packages/` and `apps/` in `workspaces.default.graph.excludePaths` ensures child packages are only indexed under their dedicated workspaces.\n- **`ownership: readOnly`:** The coordinator can include specs from `platform` into compiled context for AI agents, but SpecD rejects changes that propose mutations to `platform` specs.\n- **`prefix: _global`:** Specs located under `specs/_global/architecture.md` are referenced with ID `default:_global/architecture`.\n- **Targeted context filters:** When `core` is active in a change, its `composition` specs are automatically pulled into context.\n- **Yearly archive folders:** Changes archive into `.specd/archive/2026/2026-09-24-my-change/`.\n\n---\n\n## Example 4: Enterprise Governance, Approvals & Lifecycle Hooks\n\nUse this setup for mission-critical repositories where changes require human sign-off gates, audit trails with privacy obfuscation, and automated shell verification at lifecycle transitions.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\n# Privacy protection for public or semi-private repositories\nprivacy:\n  mode: mask # Partially mask author names and email addresses in manifests\n  excludeActors: # Retain explicit automation bot identities verbatim\n    - 'specd'\n    - 'system@getspecd.dev'\n    - 'github-actions[bot]'\n  allowedMetadataKeys:\n    - 'department'\n\n# Global context guidance for agents\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Do not modify public API signatures without backward compatibility.'\n\n# Mandatory approval gates\napprovals:\n  spec: true # Human reviewer must approve specs before 'implementing' state\n  signoff: true # Human reviewer must approve verification before 'archivable' state\n\n# Artifact invalidation strategy\ninvalidationPolicy: downstream # Automatically reopen drifted files and their DAG descendants\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.name}}/{{change.archivedName}}'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n# Inline schema overrides for lifecycle automation hooks\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: verify-approval-instruction\n              instruction: |\n                Confirm human spec approval is recorded in the change manifest before writing code.\n      - step: archiving\n        hooks:\n          pre:\n            - id: run-test-suite\n              run: 'pnpm test'\n          post:\n            - id: notify-audit-log\n              run: 'node scripts/audit-log.js --change \"{{change.name}}\" --path \"{{change.path}}\"'\n```\n\n---\n\n## Example 5: Layered Configuration Cascade (`specd.local.yaml`)\n\nUse the configuration cascade to provide personal developer overrides or CI-specific variations without altering the shared `specd.yaml`.\n\n### Base configuration (`specd.yaml`)\n\n```yaml\nschema: '@specd/schema-std'\n\nlogging:\n  level: info\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n  legacy:\n    specs:\n      adapter: fs\n      fs:\n        path: legacy/specs/\n    codeRoot: legacy/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n\ncontext:\n  - id: global-rules\n    instruction: 'Standard repository conventions apply.'\n```\n\n### Local developer override (`specd.local.yaml`)\n\nThis file is automatically gitignored by `specd project init`:\n\n```yaml\nextends: true # Deep-merge on top of base specd.yaml\n\nlogging:\n  level: debug # Enable verbose debug logs locally\n\ncontext:\n  - instruction: 'Local dev environment: run integration tests against Docker.'\n\nremove:\n  workspaces:\n    - legacy # Do not index or load legacy workspace on this dev machine\n```\n\n---\n\n## Example 6: Exhaustive Reference Configuration\n\nBelow is a complete `specd.yaml` showcasing **every configuration field** supported by the SpecD schema, with detailed comments explaining types, defaults, and runtime effects.\n\n```yaml\n# ==============================================================================\n# SpecD Master Configuration Reference (Exhaustive)\n# ==============================================================================\n\n# 1. Schema Selection (Required)\n# Reference to the workflow schema package, local schema (#name), or file path.\n# Overridden by environment variable: SPECD_SCHEMA\nschema: '@specd/schema-std'\n\n# 2. State & Runtime Paths\n# Relative directory paths resolved against the directory containing specd.yaml.\n# specdPath: location of workflow directories (changes, drafts, archive)\nspecdPath: '.specd' # Default: '.specd'\n\n# configPath: location of engine operational data (graph databases, logs, tmp)\nconfigPath: '.specd/config' # Default: '.specd/config'\n\n# 3. VCS Identity Provider\n# Forces a specific VCS identity provider ('git', 'hg'). Bypasses auto-detection.\n# Overridden by environment variable: SPECD_ACTOR_PROVIDER\nactorProvider: 'git'\n\n# 4. Identity Privacy & Obfuscation\n# Protects contributor names and emails in public change manifests and archives.\n# Overridden by environment variables: SPECD_PRIVACY_MODE, SPECD_PRIVACY_SALT\nprivacy:\n  mode: mask # 'hash' | 'mask' | 'anonymous' (Default: null / none)\n  salt: 'secret-hash-salt' # Required when mode is 'hash'\n  # Actors exempt from obfuscation. Setting this REPLACES the built-in defaults:\n  excludeActors:\n    - 'specd' # Built-in default\n    - 'system@getspecd.dev' # Built-in default\n    - 'ci-bot'\n  # Whitelist of metadata keys preserved under privacy mode (all others stripped):\n  allowedMetadataKeys:\n    - 'team'\n    - 'department'\n\n# 5. Logging Configuration\n# Controls disk logging threshold at {configPath}/log/specd.log.\n# Overridden by environment variable: SPECD_LOG_LEVEL\nlogging:\n  level: info # 'trace' | 'debug' | 'info' | 'warn' | 'error' | 'silent' (Default: 'info')\n\n# 6. Global Prompt Context\n# Content injected at the beginning of prompt context delivered to AI agents.\ncontext:\n  - file: AGENTS.md # File path relative to specd.yaml (injected verbatim)\n  - id: global-guidance # Optional identifier used for removal in cascade layers\n    instruction: 'Prefer editing existing functions over introducing redundant abstractions.'\n\n# 7. Global Spec Inclusion & Exclusion Patterns\n# Patterns applied to all compiled contexts across all active changes.\ncontextIncludeSpecs:\n  - 'default:_global/*' # Include all global constraints in every change context\ncontextExcludeSpecs:\n  - 'default:_global/archive-policy' # Exclude specific specs unless directly targeted\n\n# 8. Context Presentation & Optimization\n# Spec rendering format in compiled agent prompt context:\n# 'summary' (compact catalogue), 'hybrid' (full active specs, summarized deps),\n# 'full' (all specs verbatim), 'list' (IDs only).\n# Overridden by environment variable: SPECD_CONTEXT_MODE\ncontextMode: summary # Default: 'summary'\n\n# When true, gates optimizer agent writes to metadata cache.\n# Overridden by environment variable: SPECD_LLM_OPTIMIZED\nllmOptimizedContext: false # Default: false\n\n# 9. Invalidation Propagation Policy\n# Strategy used when change artifacts drift from baseline or are invalidated.\n# 'none', 'surgical' (target only), 'downstream' (target + DAG descendants), 'global' (all).\ninvalidationPolicy: downstream # Default: 'downstream'\n\n# 10. Approval Gates\n# When true, requires explicit human review commands before advancing states:\n# 'spec': blocks ready -> implementing until 'specd changes approve-spec'\n# 'signoff': blocks done -> archivable until 'specd changes signoff'\napprovals:\n  spec: false # Default: false\n  signoff: false # Default: false\n\n# 11. Project-Level Code Graph Indexing\ngraph:\n  includePaths:\n    - 'tools/**/*.ts' # Root-level files indexed under reserved 'root:' namespace\n  # Setting excludePaths REPLACES the built-in defaults ('node_modules/', '.git/', 'dist/', etc.):\n  excludePaths:\n    - 'node_modules/'\n    - '.git/'\n    - '.specd/'\n    - 'dist/'\n    - 'build/'\n    - 'coverage/'\n    - '**/*.spec.ts'\n\n# 12. Workspaces Configuration\n# Defines repositories and sub-directories containing specs and source code.\nworkspaces:\n  default:\n    prefix: _global # Logical prefix prepended to all spec IDs in this workspace\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global # Path to specs folder\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Path to local schemas (Default: .specd/schemas)\n    codeRoot: . # Implementation source code root (Default: '.' for default)\n    ownership: owned # 'owned' | 'shared' | 'readOnly' (Default: 'owned')\n    contextIncludeSpecs:\n      - 'default:*'\n    contextExcludeSpecs:\n      - 'default:_global/deprecated/*'\n    graph:\n      respectGitignore: true # When false, gitignored files are indexed (Default: true)\n      # In monorepos where default codeRoot is '.', exclude child workspace folders to avoid duplicate indexing:\n      excludePaths:\n        - 'packages/'\n        - 'apps/'\n        - 'temp/'\n      allowedPaths: # When defined, restricts discovery to matching paths\n        - 'src/**'\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/core/specs\n    codeRoot: packages/core # Required for non-default workspaces\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:ports/*'\n\n  external-service:\n    specs:\n      adapter: fs\n      fs:\n        path: ../service/specd/specs\n    codeRoot: ../service\n    ownership: readOnly # Read-only workspace: context-only, cannot be modified\n\n# 13. Storage Configuration\n# Persistent storage directories for the change lifecycle stages.\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes # Active changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts # Shelved drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded # Abandoned changes\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive # Completed changes\n      pattern: '{{year}}/{{change.archivedName}}' # Directory template for archives\n\n# 14. Schema Plugins\n# Schema extension packages loaded and merged before schemaOverrides.\nschemaPlugins:\n  - '@specd/plugin-schema-enterprise'\n\n# 15. Inline Schema Overrides\n# Declarative modifications to the active schema without forking.\n# Supported operations: create, remove, set, append, prepend.\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: audit-pre-check\n              instruction: 'Confirm task list is up to date before editing code.'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-check\n              run: 'pnpm test'\n          post:\n            - id: post-archive-log\n              run: 'echo \"Change {{change.name}} successfully archived.\"'\n\n# 16. Installed Agent Plugins\n# Coding assistant plugins registered in this project.\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-claude'\n    - name: '@specd/plugin-agent-copilot'\n      config:\n        model: 'gpt-4o'\n```\n"
      },
      {
        "index": 2,
        "heading": "Example 1: Minimal Single-Repo Project",
        "level": 2,
        "startLine": 7,
        "endLine": 52,
        "lines": 46,
        "content": "## Example 1: Minimal Single-Repo Project\n\nThis is the standard starting point for most projects: a single Git repository, the default published schema (`@specd/schema-std`), and standard local filesystem storage.\n\nRun `specd project init` in a fresh repository to generate this structure automatically.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\n### Key behaviors\n\n- **Schema:** `@specd/schema-std` is resolved from `node_modules/@specd/schema-std/schema.yaml`. Version pinning is handled via `package.json`.\n- **Workspace:** The single `default` workspace stores all specs in `specs/`. `codeRoot` defaults to the repository root (`.`), and `ownership` defaults to `owned`.\n- **Storage:** `.specd/changes` tracks in-progress changes, `.specd/drafts` stores shelved drafts, `.specd/discarded` retains abandoned changes, and `.specd/archive` stores permanent records of completed changes.\n- **Approvals:** Both approval gates (`spec` and `signoff`) are disabled by default (`false`). Transitions require no manual sign-off.\n\n---\n"
      },
      {
        "index": 3,
        "heading": "`specd.yaml`",
        "level": 3,
        "startLine": 13,
        "endLine": 43,
        "lines": 31,
        "content": "### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n"
      },
      {
        "index": 4,
        "heading": "Key behaviors",
        "level": 3,
        "startLine": 44,
        "endLine": 52,
        "lines": 9,
        "content": "### Key behaviors\n\n- **Schema:** `@specd/schema-std` is resolved from `node_modules/@specd/schema-std/schema.yaml`. Version pinning is handled via `package.json`.\n- **Workspace:** The single `default` workspace stores all specs in `specs/`. `codeRoot` defaults to the repository root (`.`), and `ownership` defaults to `owned`.\n- **Storage:** `.specd/changes` tracks in-progress changes, `.specd/drafts` stores shelved drafts, `.specd/discarded` retains abandoned changes, and `.specd/archive` stores permanent records of completed changes.\n- **Approvals:** Both approval gates (`spec` and `signoff`) are disabled by default (`false`). Transitions require no manual sign-off.\n\n---\n"
      },
      {
        "index": 5,
        "heading": "Example 2: Single-Repo with Local Custom Schema",
        "level": 2,
        "startLine": 53,
        "endLine": 111,
        "lines": 59,
        "content": "## Example 2: Single-Repo with Local Custom Schema\n\nUse this setup when your team has custom workflow steps, unique artifact types, or specialized validation rules defined locally in the repository rather than from an npm package.\n\n### Directory structure\n\n```\n.specd/\n└── schemas/\n    └── custom-workflow/\n        ├── schema.yaml\n        └── templates/\n            ├── proposal.md\n            ├── spec.md\n            └── tasks.md\n```\n\n### `specd.yaml`\n\n```yaml\nschema: 'custom-workflow' # Resolves to .specd/schemas/custom-workflow/schema.yaml\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Optional: this is the default path for 'default'\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n\n### Key behaviors\n\n- **Schema reference:** Bare names (e.g. `'custom-workflow'`) or hashed names (e.g. `'#custom-workflow'`) resolve against `schemas.adapter.fs.path` of the `default` workspace.\n- **Local versioning:** The schema is committed and versioned alongside your application code. Updates take effect immediately for new changes.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "Directory structure",
        "level": 3,
        "startLine": 57,
        "endLine": 69,
        "lines": 13,
        "content": "### Directory structure\n\n```\n.specd/\n└── schemas/\n    └── custom-workflow/\n        ├── schema.yaml\n        └── templates/\n            ├── proposal.md\n            ├── spec.md\n            └── tasks.md\n```\n"
      },
      {
        "index": 7,
        "heading": "`specd.yaml`",
        "level": 3,
        "startLine": 70,
        "endLine": 104,
        "lines": 35,
        "content": "### `specd.yaml`\n\n```yaml\nschema: 'custom-workflow' # Resolves to .specd/schemas/custom-workflow/schema.yaml\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Optional: this is the default path for 'default'\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n```\n"
      },
      {
        "index": 8,
        "heading": "Key behaviors",
        "level": 3,
        "startLine": 105,
        "endLine": 111,
        "lines": 7,
        "content": "### Key behaviors\n\n- **Schema reference:** Bare names (e.g. `'custom-workflow'`) or hashed names (e.g. `'#custom-workflow'`) resolve against `schemas.adapter.fs.path` of the `default` workspace.\n- **Local versioning:** The schema is committed and versioned alongside your application code. Updates take effect immediately for new changes.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "Example 3: Multi-Workspace Monorepo & Coordinator",
        "level": 2,
        "startLine": 112,
        "endLine": 202,
        "lines": 91,
        "content": "## Example 3: Multi-Workspace Monorepo & Coordinator\n\nUse this pattern in a monorepo where a top-level `default` workspace coordinates multiple packages (e.g. `packages/core`, `packages/cli`, `apps/web`), each having its own independent spec folder and code root.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  # The default workspace covers root-level scripts, configs, and shared global specs.\n  # Because codeRoot defaults to '.' (the repo root), it physically encloses all child packages.\n  # We MUST exclude child directories in graph.excludePaths so the graph engine does not\n  # discover their files twice (once under default and once under the child workspace).\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    codeRoot: .\n    ownership: owned\n    graph:\n      excludePaths:\n        - node_modules/\n        - .git/\n        - .specd/\n        - dist/\n        - build/\n        - coverage/\n        - packages/ # Indexed independently by child workspaces below\n        - apps/ # Indexed independently by child workspaces below\n        - specs/\n        - specd-sdd/\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:composition'\n\n  cli:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-core/specs\n    codeRoot: ../platform-core\n    ownership: readOnly # Read-only: context only, modifications rejected\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.archivedName}}'\n```\n\n### Key behaviors\n\n- **Monorepo root exclusion pattern:** In monorepos where `workspaces.default.codeRoot` is `.` (the project root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` (such as `packages/core` or `packages/cli`), their source files would be indexed twice, causing symbol duplicate errors and inflated graph databases. Listing `packages/` and `apps/` in `workspaces.default.graph.excludePaths` ensures child packages are only indexed under their dedicated workspaces.\n- **`ownership: readOnly`:** The coordinator can include specs from `platform` into compiled context for AI agents, but SpecD rejects changes that propose mutations to `platform` specs.\n- **`prefix: _global`:** Specs located under `specs/_global/architecture.md` are referenced with ID `default:_global/architecture`.\n- **Targeted context filters:** When `core` is active in a change, its `composition` specs are automatically pulled into context.\n- **Yearly archive folders:** Changes archive into `.specd/archive/2026/2026-09-24-my-change/`.\n\n---\n"
      },
      {
        "index": 10,
        "heading": "`specd.yaml`",
        "level": 3,
        "startLine": 116,
        "endLine": 192,
        "lines": 77,
        "content": "### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\nworkspaces:\n  # The default workspace covers root-level scripts, configs, and shared global specs.\n  # Because codeRoot defaults to '.' (the repo root), it physically encloses all child packages.\n  # We MUST exclude child directories in graph.excludePaths so the graph engine does not\n  # discover their files twice (once under default and once under the child workspace).\n  default:\n    prefix: _global\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global\n    codeRoot: .\n    ownership: owned\n    graph:\n      excludePaths:\n        - node_modules/\n        - .git/\n        - .specd/\n        - dist/\n        - build/\n        - coverage/\n        - packages/ # Indexed independently by child workspaces below\n        - apps/ # Indexed independently by child workspaces below\n        - specs/\n        - specd-sdd/\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/core\n    codeRoot: packages/core\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:composition'\n\n  cli:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/cli\n    codeRoot: packages/cli\n    ownership: owned\n\n  platform:\n    specs:\n      adapter: fs\n      fs:\n        path: ../platform-core/specs\n    codeRoot: ../platform-core\n    ownership: readOnly # Read-only: context only, modifications rejected\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.archivedName}}'\n```\n"
      },
      {
        "index": 11,
        "heading": "Key behaviors",
        "level": 3,
        "startLine": 193,
        "endLine": 202,
        "lines": 10,
        "content": "### Key behaviors\n\n- **Monorepo root exclusion pattern:** In monorepos where `workspaces.default.codeRoot` is `.` (the project root), SpecD's file discovery traverses the entire project tree under `default`. If child workspaces define their own `codeRoot` (such as `packages/core` or `packages/cli`), their source files would be indexed twice, causing symbol duplicate errors and inflated graph databases. Listing `packages/` and `apps/` in `workspaces.default.graph.excludePaths` ensures child packages are only indexed under their dedicated workspaces.\n- **`ownership: readOnly`:** The coordinator can include specs from `platform` into compiled context for AI agents, but SpecD rejects changes that propose mutations to `platform` specs.\n- **`prefix: _global`:** Specs located under `specs/_global/architecture.md` are referenced with ID `default:_global/architecture`.\n- **Targeted context filters:** When `core` is active in a change, its `composition` specs are automatically pulled into context.\n- **Yearly archive folders:** Changes archive into `.specd/archive/2026/2026-09-24-my-change/`.\n\n---\n"
      },
      {
        "index": 12,
        "heading": "Example 4: Enterprise Governance, Approvals & Lifecycle Hooks",
        "level": 2,
        "startLine": 203,
        "endLine": 282,
        "lines": 80,
        "content": "## Example 4: Enterprise Governance, Approvals & Lifecycle Hooks\n\nUse this setup for mission-critical repositories where changes require human sign-off gates, audit trails with privacy obfuscation, and automated shell verification at lifecycle transitions.\n\n### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\n# Privacy protection for public or semi-private repositories\nprivacy:\n  mode: mask # Partially mask author names and email addresses in manifests\n  excludeActors: # Retain explicit automation bot identities verbatim\n    - 'specd'\n    - 'system@getspecd.dev'\n    - 'github-actions[bot]'\n  allowedMetadataKeys:\n    - 'department'\n\n# Global context guidance for agents\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Do not modify public API signatures without backward compatibility.'\n\n# Mandatory approval gates\napprovals:\n  spec: true # Human reviewer must approve specs before 'implementing' state\n  signoff: true # Human reviewer must approve verification before 'archivable' state\n\n# Artifact invalidation strategy\ninvalidationPolicy: downstream # Automatically reopen drifted files and their DAG descendants\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.name}}/{{change.archivedName}}'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n# Inline schema overrides for lifecycle automation hooks\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: verify-approval-instruction\n              instruction: |\n                Confirm human spec approval is recorded in the change manifest before writing code.\n      - step: archiving\n        hooks:\n          pre:\n            - id: run-test-suite\n              run: 'pnpm test'\n          post:\n            - id: notify-audit-log\n              run: 'node scripts/audit-log.js --change \"{{change.name}}\" --path \"{{change.path}}\"'\n```\n\n---\n"
      },
      {
        "index": 13,
        "heading": "`specd.yaml`",
        "level": 3,
        "startLine": 207,
        "endLine": 282,
        "lines": 76,
        "content": "### `specd.yaml`\n\n```yaml\nschema: '@specd/schema-std'\n\n# Privacy protection for public or semi-private repositories\nprivacy:\n  mode: mask # Partially mask author names and email addresses in manifests\n  excludeActors: # Retain explicit automation bot identities verbatim\n    - 'specd'\n    - 'system@getspecd.dev'\n    - 'github-actions[bot]'\n  allowedMetadataKeys:\n    - 'department'\n\n# Global context guidance for agents\ncontext:\n  - file: AGENTS.md\n  - instruction: 'Do not modify public API signatures without backward compatibility.'\n\n# Mandatory approval gates\napprovals:\n  spec: true # Human reviewer must approve specs before 'implementing' state\n  signoff: true # Human reviewer must approve verification before 'archivable' state\n\n# Artifact invalidation strategy\ninvalidationPolicy: downstream # Automatically reopen drifted files and their DAG descendants\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n      pattern: '{{year}}/{{change.name}}/{{change.archivedName}}'\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n\n# Inline schema overrides for lifecycle automation hooks\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: verify-approval-instruction\n              instruction: |\n                Confirm human spec approval is recorded in the change manifest before writing code.\n      - step: archiving\n        hooks:\n          pre:\n            - id: run-test-suite\n              run: 'pnpm test'\n          post:\n            - id: notify-audit-log\n              run: 'node scripts/audit-log.js --change \"{{change.name}}\" --path \"{{change.path}}\"'\n```\n\n---\n"
      },
      {
        "index": 14,
        "heading": "Example 5: Layered Configuration Cascade (`specd.local.yaml`)",
        "level": 2,
        "startLine": 283,
        "endLine": 350,
        "lines": 68,
        "content": "## Example 5: Layered Configuration Cascade (`specd.local.yaml`)\n\nUse the configuration cascade to provide personal developer overrides or CI-specific variations without altering the shared `specd.yaml`.\n\n### Base configuration (`specd.yaml`)\n\n```yaml\nschema: '@specd/schema-std'\n\nlogging:\n  level: info\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n  legacy:\n    specs:\n      adapter: fs\n      fs:\n        path: legacy/specs/\n    codeRoot: legacy/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n\ncontext:\n  - id: global-rules\n    instruction: 'Standard repository conventions apply.'\n```\n\n### Local developer override (`specd.local.yaml`)\n\nThis file is automatically gitignored by `specd project init`:\n\n```yaml\nextends: true # Deep-merge on top of base specd.yaml\n\nlogging:\n  level: debug # Enable verbose debug logs locally\n\ncontext:\n  - instruction: 'Local dev environment: run integration tests against Docker.'\n\nremove:\n  workspaces:\n    - legacy # Do not index or load legacy workspace on this dev machine\n```\n\n---\n"
      },
      {
        "index": 15,
        "heading": "Base configuration (`specd.yaml`)",
        "level": 3,
        "startLine": 287,
        "endLine": 330,
        "lines": 44,
        "content": "### Base configuration (`specd.yaml`)\n\n```yaml\nschema: '@specd/schema-std'\n\nlogging:\n  level: info\n\nworkspaces:\n  default:\n    specs:\n      adapter: fs\n      fs:\n        path: specs/\n  legacy:\n    specs:\n      adapter: fs\n      fs:\n        path: legacy/specs/\n    codeRoot: legacy/\n\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive\n\ncontext:\n  - id: global-rules\n    instruction: 'Standard repository conventions apply.'\n```\n"
      },
      {
        "index": 16,
        "heading": "Local developer override (`specd.local.yaml`)",
        "level": 3,
        "startLine": 331,
        "endLine": 350,
        "lines": 20,
        "content": "### Local developer override (`specd.local.yaml`)\n\nThis file is automatically gitignored by `specd project init`:\n\n```yaml\nextends: true # Deep-merge on top of base specd.yaml\n\nlogging:\n  level: debug # Enable verbose debug logs locally\n\ncontext:\n  - instruction: 'Local dev environment: run integration tests against Docker.'\n\nremove:\n  workspaces:\n    - legacy # Do not index or load legacy workspace on this dev machine\n```\n\n---\n"
      },
      {
        "index": 17,
        "heading": "Example 6: Exhaustive Reference Configuration",
        "level": 2,
        "startLine": 351,
        "endLine": 554,
        "lines": 204,
        "content": "## Example 6: Exhaustive Reference Configuration\n\nBelow is a complete `specd.yaml` showcasing **every configuration field** supported by the SpecD schema, with detailed comments explaining types, defaults, and runtime effects.\n\n```yaml\n# ==============================================================================\n# SpecD Master Configuration Reference (Exhaustive)\n# ==============================================================================\n\n# 1. Schema Selection (Required)\n# Reference to the workflow schema package, local schema (#name), or file path.\n# Overridden by environment variable: SPECD_SCHEMA\nschema: '@specd/schema-std'\n\n# 2. State & Runtime Paths\n# Relative directory paths resolved against the directory containing specd.yaml.\n# specdPath: location of workflow directories (changes, drafts, archive)\nspecdPath: '.specd' # Default: '.specd'\n\n# configPath: location of engine operational data (graph databases, logs, tmp)\nconfigPath: '.specd/config' # Default: '.specd/config'\n\n# 3. VCS Identity Provider\n# Forces a specific VCS identity provider ('git', 'hg'). Bypasses auto-detection.\n# Overridden by environment variable: SPECD_ACTOR_PROVIDER\nactorProvider: 'git'\n\n# 4. Identity Privacy & Obfuscation\n# Protects contributor names and emails in public change manifests and archives.\n# Overridden by environment variables: SPECD_PRIVACY_MODE, SPECD_PRIVACY_SALT\nprivacy:\n  mode: mask # 'hash' | 'mask' | 'anonymous' (Default: null / none)\n  salt: 'secret-hash-salt' # Required when mode is 'hash'\n  # Actors exempt from obfuscation. Setting this REPLACES the built-in defaults:\n  excludeActors:\n    - 'specd' # Built-in default\n    - 'system@getspecd.dev' # Built-in default\n    - 'ci-bot'\n  # Whitelist of metadata keys preserved under privacy mode (all others stripped):\n  allowedMetadataKeys:\n    - 'team'\n    - 'department'\n\n# 5. Logging Configuration\n# Controls disk logging threshold at {configPath}/log/specd.log.\n# Overridden by environment variable: SPECD_LOG_LEVEL\nlogging:\n  level: info # 'trace' | 'debug' | 'info' | 'warn' | 'error' | 'silent' (Default: 'info')\n\n# 6. Global Prompt Context\n# Content injected at the beginning of prompt context delivered to AI agents.\ncontext:\n  - file: AGENTS.md # File path relative to specd.yaml (injected verbatim)\n  - id: global-guidance # Optional identifier used for removal in cascade layers\n    instruction: 'Prefer editing existing functions over introducing redundant abstractions.'\n\n# 7. Global Spec Inclusion & Exclusion Patterns\n# Patterns applied to all compiled contexts across all active changes.\ncontextIncludeSpecs:\n  - 'default:_global/*' # Include all global constraints in every change context\ncontextExcludeSpecs:\n  - 'default:_global/archive-policy' # Exclude specific specs unless directly targeted\n\n# 8. Context Presentation & Optimization\n# Spec rendering format in compiled agent prompt context:\n# 'summary' (compact catalogue), 'hybrid' (full active specs, summarized deps),\n# 'full' (all specs verbatim), 'list' (IDs only).\n# Overridden by environment variable: SPECD_CONTEXT_MODE\ncontextMode: summary # Default: 'summary'\n\n# When true, gates optimizer agent writes to metadata cache.\n# Overridden by environment variable: SPECD_LLM_OPTIMIZED\nllmOptimizedContext: false # Default: false\n\n# 9. Invalidation Propagation Policy\n# Strategy used when change artifacts drift from baseline or are invalidated.\n# 'none', 'surgical' (target only), 'downstream' (target + DAG descendants), 'global' (all).\ninvalidationPolicy: downstream # Default: 'downstream'\n\n# 10. Approval Gates\n# When true, requires explicit human review commands before advancing states:\n# 'spec': blocks ready -> implementing until 'specd changes approve-spec'\n# 'signoff': blocks done -> archivable until 'specd changes signoff'\napprovals:\n  spec: false # Default: false\n  signoff: false # Default: false\n\n# 11. Project-Level Code Graph Indexing\ngraph:\n  includePaths:\n    - 'tools/**/*.ts' # Root-level files indexed under reserved 'root:' namespace\n  # Setting excludePaths REPLACES the built-in defaults ('node_modules/', '.git/', 'dist/', etc.):\n  excludePaths:\n    - 'node_modules/'\n    - '.git/'\n    - '.specd/'\n    - 'dist/'\n    - 'build/'\n    - 'coverage/'\n    - '**/*.spec.ts'\n\n# 12. Workspaces Configuration\n# Defines repositories and sub-directories containing specs and source code.\nworkspaces:\n  default:\n    prefix: _global # Logical prefix prepended to all spec IDs in this workspace\n    specs:\n      adapter: fs\n      fs:\n        path: specs/_global # Path to specs folder\n    schemas:\n      adapter: fs\n      fs:\n        path: .specd/schemas # Path to local schemas (Default: .specd/schemas)\n    codeRoot: . # Implementation source code root (Default: '.' for default)\n    ownership: owned # 'owned' | 'shared' | 'readOnly' (Default: 'owned')\n    contextIncludeSpecs:\n      - 'default:*'\n    contextExcludeSpecs:\n      - 'default:_global/deprecated/*'\n    graph:\n      respectGitignore: true # When false, gitignored files are indexed (Default: true)\n      # In monorepos where default codeRoot is '.', exclude child workspace folders to avoid duplicate indexing:\n      excludePaths:\n        - 'packages/'\n        - 'apps/'\n        - 'temp/'\n      allowedPaths: # When defined, restricts discovery to matching paths\n        - 'src/**'\n\n  core:\n    specs:\n      adapter: fs\n      fs:\n        path: packages/core/specs\n    codeRoot: packages/core # Required for non-default workspaces\n    ownership: owned\n    contextIncludeSpecs:\n      - 'core:ports/*'\n\n  external-service:\n    specs:\n      adapter: fs\n      fs:\n        path: ../service/specd/specs\n    codeRoot: ../service\n    ownership: readOnly # Read-only workspace: context-only, cannot be modified\n\n# 13. Storage Configuration\n# Persistent storage directories for the change lifecycle stages.\nstorage:\n  changes:\n    adapter: fs\n    fs:\n      path: .specd/changes # Active changes\n  drafts:\n    adapter: fs\n    fs:\n      path: .specd/drafts # Shelved drafts\n  discarded:\n    adapter: fs\n    fs:\n      path: .specd/discarded # Abandoned changes\n  archive:\n    adapter: fs\n    fs:\n      path: .specd/archive # Completed changes\n      pattern: '{{year}}/{{change.archivedName}}' # Directory template for archives\n\n# 14. Schema Plugins\n# Schema extension packages loaded and merged before schemaOverrides.\nschemaPlugins:\n  - '@specd/plugin-schema-enterprise'\n\n# 15. Inline Schema Overrides\n# Declarative modifications to the active schema without forking.\n# Supported operations: create, remove, set, append, prepend.\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          pre:\n            - id: audit-pre-check\n              instruction: 'Confirm task list is up to date before editing code.'\n      - step: archiving\n        hooks:\n          pre:\n            - id: pre-archive-check\n              run: 'pnpm test'\n          post:\n            - id: post-archive-log\n              run: 'echo \"Change {{change.name}} successfully archived.\"'\n\n# 16. Installed Agent Plugins\n# Coding assistant plugins registered in this project.\nplugins:\n  agents:\n    - name: '@specd/plugin-agent-claude'\n    - name: '@specd/plugin-agent-copilot'\n      config:\n        model: 'gpt-4o'\n```\n"
      }
    ]
  },
  {
    "topic": "standard-schema",
    "title": "Standard Schema Reference",
    "description": "Deep-dive into @specd/schema-std artifacts, DAG validation, rules, and transition hooks.",
    "order": 11,
    "content": "# Standard Schema Reference\n\n`@specd/schema-std` is the default workflow schema for SpecD projects. It defines an opinionated, production-grade spec-driven development process: **proposal → specs → verify → design → tasks**.\n\nThis reference describes each artifact in `@specd/schema-std`, its scope and lifecycle role, dependency constraints, templates, and transition gates.\n\n---\n\n## Artifact DAG\n\n```\nproposal [scope: change]\n  ↓\nspecs [scope: spec]\n  ↓\nverify [scope: spec]\n  ↓\ndesign [scope: change]  (requires: proposal, specs, verify)\n  ↓\ntasks [scope: change]   (requires: specs, design)\n```\n\nThe DAG is strictly acyclic. An artifact cannot be drafted or validated until all prerequisite artifacts in its dependency chain are complete (or explicitly skipped if optional).\n\n---\n\n## Artifacts in `@specd/schema-std`\n\n### 1. proposal (`proposal.md`)\n\n- **Scope**: `change` (working document, not synced to specs on archive)\n- **Requires**: None (entrypoint artifact for every change)\n- **Output**: `<change>/proposal.md`\n- **Purpose**: Establishes the business motivation and technical intent before any spec or code is touched.\n- **Sections**:\n  - `## Motivation`: The problem being solved and value delivered.\n  - `## Current behaviour`: Existing behavior or limitations.\n  - `## Proposed solution`: High-level approach and architecture changes.\n  - `## Specs affected`: Exhaustive list of specs created (`New specs`) or modified (`Modified specs`).\n  - `## Impact`: Blast radius across workspaces, packages, and public contracts.\n  - `## Open questions`: Unresolved design trade-offs.\n\n### 2. specs (`spec.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent specification synced to `specs/` upon archive)\n- **Requires**: `proposal`\n- **Output**: `<change>/specs/<capability>/spec.md` or `<change>/deltas/<capability>/spec.md.delta.yaml`\n- **Purpose**: Formal normative contract defining what the system SHALL do.\n- **Conventions**:\n  - Normative RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`).\n  - Requirements grouped under `### Requirement: <Title>` headings.\n  - No implementation details (languages, classes, functions) or test scenarios.\n\n### 3. verify (`verify.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent verification scenarios synced to `specs/` upon archive)\n- **Requires**: `specs`\n- **Output**: `<change>/specs/<capability>/verify.md` or `<change>/deltas/<capability>/verify.md.delta.yaml`\n- **Purpose**: Concrete test scenarios verifying requirement satisfaction.\n- **Conventions**:\n  - One-to-one parity with `spec.md`: every `### Requirement: <Title>` in `spec.md` has a corresponding heading in `verify.md`.\n  - Structured GIVEN/WHEN/THEN behavior scenarios under `#### Scenario: <Name>`.\n\n### 4. design (`design.md`)\n\n- **Scope**: `change` (working document)\n- **Requires**: `proposal`, `specs`, `verify`\n- **Output**: `<change>/design.md`\n- **Purpose**: Concrete technical specification of _how_ the implementation will be built.\n- **Sections**:\n  - `## Overview`: Technical summary and architecture diagrams.\n  - `## Affected areas`: Files, packages, and modules touched.\n  - `## New constructs`: Class, function, interface, and error signatures.\n  - `## Approach & Execution flow`: Algorithms and step-by-step flow.\n  - `## Key decisions`: Architecture Decision Records (ADRs) inline with rationale and alternatives.\n  - `## Trade-offs`: Explicit compromises made.\n  - `## Testing`: Unit, integration, and E2E test strategy.\n\n### 5. tasks (`tasks.md`)\n\n- **Scope**: `change` (working document with live progress checkboxes)\n- **Requires**: `specs`, `design`\n- **Output**: `<change>/tasks.md`\n- **Purpose**: Granular, ordered implementation checklist driving execution.\n- **Conventions**:\n  - Grouped into logical task phases (e.g. `## 1. Domain Layer`, `## 2. CLI Adapter`).\n  - Checkboxes (`- [ ]` / `- [x]`) with target files, symbols, approach, and requirement references.\n  - Gated transition: Transition to `verifying` is blocked while any `- [ ]` remains.\n\n---\n\n## Workflow Step Gating\n\n`@specd/schema-std` configures required artifacts for each lifecycle transition:\n\n| State          | Required Artifacts                               | Description                                                                      |\n| :------------- | :----------------------------------------------- | :------------------------------------------------------------------------------- |\n| `drafting`     | None                                             | Initial state after change creation.                                             |\n| `designing`    | None                                             | Authoring proposals, specs, verify, design, and tasks.                           |\n| `ready`        | `proposal`, `specs`, `verify`, `design`, `tasks` | All planning artifacts complete and validated.                                   |\n| `implementing` | `proposal`, `specs`, `verify`, `design`, `tasks` | Coding and task checklist advancement.                                           |\n| `verifying`    | `verify`, `tasks`                                | Testing and scenario verification. Blocked if any `- [ ]` remains in `tasks.md`. |\n| `archiving`    | `specs`, `tasks`                                 | Delta merging and syncing into `specs/`.                                         |\n\n---\n\n## Cross-Artifact Validations\n\nThe standard schema enforces relational integrity across artifacts:\n\n1. **`specs-verify-requirement-parity`**:\n   Ensures that every requirement declared in `spec.md` has a matching requirement heading with verification scenarios in `verify.md`.\n2. **`proposal-specs-coverage`**:\n   Ensures that every spec identifier listed under `## Specs affected` in `proposal.md` has a corresponding spec file or delta in the change.\n3. **`design-tasks-coverage`**:\n   Ensures that files and constructs identified in `design.md` are covered by implementation items in `tasks.md`.\n",
    "lineCount": 117,
    "byteLength": 5956,
    "outline": [
      {
        "index": 1,
        "heading": "Standard Schema Reference",
        "level": 1,
        "startLine": 1,
        "endLine": 117,
        "lines": 117,
        "content": "# Standard Schema Reference\n\n`@specd/schema-std` is the default workflow schema for SpecD projects. It defines an opinionated, production-grade spec-driven development process: **proposal → specs → verify → design → tasks**.\n\nThis reference describes each artifact in `@specd/schema-std`, its scope and lifecycle role, dependency constraints, templates, and transition gates.\n\n---\n\n## Artifact DAG\n\n```\nproposal [scope: change]\n  ↓\nspecs [scope: spec]\n  ↓\nverify [scope: spec]\n  ↓\ndesign [scope: change]  (requires: proposal, specs, verify)\n  ↓\ntasks [scope: change]   (requires: specs, design)\n```\n\nThe DAG is strictly acyclic. An artifact cannot be drafted or validated until all prerequisite artifacts in its dependency chain are complete (or explicitly skipped if optional).\n\n---\n\n## Artifacts in `@specd/schema-std`\n\n### 1. proposal (`proposal.md`)\n\n- **Scope**: `change` (working document, not synced to specs on archive)\n- **Requires**: None (entrypoint artifact for every change)\n- **Output**: `<change>/proposal.md`\n- **Purpose**: Establishes the business motivation and technical intent before any spec or code is touched.\n- **Sections**:\n  - `## Motivation`: The problem being solved and value delivered.\n  - `## Current behaviour`: Existing behavior or limitations.\n  - `## Proposed solution`: High-level approach and architecture changes.\n  - `## Specs affected`: Exhaustive list of specs created (`New specs`) or modified (`Modified specs`).\n  - `## Impact`: Blast radius across workspaces, packages, and public contracts.\n  - `## Open questions`: Unresolved design trade-offs.\n\n### 2. specs (`spec.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent specification synced to `specs/` upon archive)\n- **Requires**: `proposal`\n- **Output**: `<change>/specs/<capability>/spec.md` or `<change>/deltas/<capability>/spec.md.delta.yaml`\n- **Purpose**: Formal normative contract defining what the system SHALL do.\n- **Conventions**:\n  - Normative RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`).\n  - Requirements grouped under `### Requirement: <Title>` headings.\n  - No implementation details (languages, classes, functions) or test scenarios.\n\n### 3. verify (`verify.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent verification scenarios synced to `specs/` upon archive)\n- **Requires**: `specs`\n- **Output**: `<change>/specs/<capability>/verify.md` or `<change>/deltas/<capability>/verify.md.delta.yaml`\n- **Purpose**: Concrete test scenarios verifying requirement satisfaction.\n- **Conventions**:\n  - One-to-one parity with `spec.md`: every `### Requirement: <Title>` in `spec.md` has a corresponding heading in `verify.md`.\n  - Structured GIVEN/WHEN/THEN behavior scenarios under `#### Scenario: <Name>`.\n\n### 4. design (`design.md`)\n\n- **Scope**: `change` (working document)\n- **Requires**: `proposal`, `specs`, `verify`\n- **Output**: `<change>/design.md`\n- **Purpose**: Concrete technical specification of _how_ the implementation will be built.\n- **Sections**:\n  - `## Overview`: Technical summary and architecture diagrams.\n  - `## Affected areas`: Files, packages, and modules touched.\n  - `## New constructs`: Class, function, interface, and error signatures.\n  - `## Approach & Execution flow`: Algorithms and step-by-step flow.\n  - `## Key decisions`: Architecture Decision Records (ADRs) inline with rationale and alternatives.\n  - `## Trade-offs`: Explicit compromises made.\n  - `## Testing`: Unit, integration, and E2E test strategy.\n\n### 5. tasks (`tasks.md`)\n\n- **Scope**: `change` (working document with live progress checkboxes)\n- **Requires**: `specs`, `design`\n- **Output**: `<change>/tasks.md`\n- **Purpose**: Granular, ordered implementation checklist driving execution.\n- **Conventions**:\n  - Grouped into logical task phases (e.g. `## 1. Domain Layer`, `## 2. CLI Adapter`).\n  - Checkboxes (`- [ ]` / `- [x]`) with target files, symbols, approach, and requirement references.\n  - Gated transition: Transition to `verifying` is blocked while any `- [ ]` remains.\n\n---\n\n## Workflow Step Gating\n\n`@specd/schema-std` configures required artifacts for each lifecycle transition:\n\n| State          | Required Artifacts                               | Description                                                                      |\n| :------------- | :----------------------------------------------- | :------------------------------------------------------------------------------- |\n| `drafting`     | None                                             | Initial state after change creation.                                             |\n| `designing`    | None                                             | Authoring proposals, specs, verify, design, and tasks.                           |\n| `ready`        | `proposal`, `specs`, `verify`, `design`, `tasks` | All planning artifacts complete and validated.                                   |\n| `implementing` | `proposal`, `specs`, `verify`, `design`, `tasks` | Coding and task checklist advancement.                                           |\n| `verifying`    | `verify`, `tasks`                                | Testing and scenario verification. Blocked if any `- [ ]` remains in `tasks.md`. |\n| `archiving`    | `specs`, `tasks`                                 | Delta merging and syncing into `specs/`.                                         |\n\n---\n\n## Cross-Artifact Validations\n\nThe standard schema enforces relational integrity across artifacts:\n\n1. **`specs-verify-requirement-parity`**:\n   Ensures that every requirement declared in `spec.md` has a matching requirement heading with verification scenarios in `verify.md`.\n2. **`proposal-specs-coverage`**:\n   Ensures that every spec identifier listed under `## Specs affected` in `proposal.md` has a corresponding spec file or delta in the change.\n3. **`design-tasks-coverage`**:\n   Ensures that files and constructs identified in `design.md` are covered by implementation items in `tasks.md`.\n"
      },
      {
        "index": 2,
        "heading": "Artifact DAG",
        "level": 2,
        "startLine": 9,
        "endLine": 26,
        "lines": 18,
        "content": "## Artifact DAG\n\n```\nproposal [scope: change]\n  ↓\nspecs [scope: spec]\n  ↓\nverify [scope: spec]\n  ↓\ndesign [scope: change]  (requires: proposal, specs, verify)\n  ↓\ntasks [scope: change]   (requires: specs, design)\n```\n\nThe DAG is strictly acyclic. An artifact cannot be drafted or validated until all prerequisite artifacts in its dependency chain are complete (or explicitly skipped if optional).\n\n---\n"
      },
      {
        "index": 3,
        "heading": "Artifacts in `@specd/schema-std`",
        "level": 2,
        "startLine": 27,
        "endLine": 91,
        "lines": 65,
        "content": "## Artifacts in `@specd/schema-std`\n\n### 1. proposal (`proposal.md`)\n\n- **Scope**: `change` (working document, not synced to specs on archive)\n- **Requires**: None (entrypoint artifact for every change)\n- **Output**: `<change>/proposal.md`\n- **Purpose**: Establishes the business motivation and technical intent before any spec or code is touched.\n- **Sections**:\n  - `## Motivation`: The problem being solved and value delivered.\n  - `## Current behaviour`: Existing behavior or limitations.\n  - `## Proposed solution`: High-level approach and architecture changes.\n  - `## Specs affected`: Exhaustive list of specs created (`New specs`) or modified (`Modified specs`).\n  - `## Impact`: Blast radius across workspaces, packages, and public contracts.\n  - `## Open questions`: Unresolved design trade-offs.\n\n### 2. specs (`spec.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent specification synced to `specs/` upon archive)\n- **Requires**: `proposal`\n- **Output**: `<change>/specs/<capability>/spec.md` or `<change>/deltas/<capability>/spec.md.delta.yaml`\n- **Purpose**: Formal normative contract defining what the system SHALL do.\n- **Conventions**:\n  - Normative RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`).\n  - Requirements grouped under `### Requirement: <Title>` headings.\n  - No implementation details (languages, classes, functions) or test scenarios.\n\n### 3. verify (`verify.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent verification scenarios synced to `specs/` upon archive)\n- **Requires**: `specs`\n- **Output**: `<change>/specs/<capability>/verify.md` or `<change>/deltas/<capability>/verify.md.delta.yaml`\n- **Purpose**: Concrete test scenarios verifying requirement satisfaction.\n- **Conventions**:\n  - One-to-one parity with `spec.md`: every `### Requirement: <Title>` in `spec.md` has a corresponding heading in `verify.md`.\n  - Structured GIVEN/WHEN/THEN behavior scenarios under `#### Scenario: <Name>`.\n\n### 4. design (`design.md`)\n\n- **Scope**: `change` (working document)\n- **Requires**: `proposal`, `specs`, `verify`\n- **Output**: `<change>/design.md`\n- **Purpose**: Concrete technical specification of _how_ the implementation will be built.\n- **Sections**:\n  - `## Overview`: Technical summary and architecture diagrams.\n  - `## Affected areas`: Files, packages, and modules touched.\n  - `## New constructs`: Class, function, interface, and error signatures.\n  - `## Approach & Execution flow`: Algorithms and step-by-step flow.\n  - `## Key decisions`: Architecture Decision Records (ADRs) inline with rationale and alternatives.\n  - `## Trade-offs`: Explicit compromises made.\n  - `## Testing`: Unit, integration, and E2E test strategy.\n\n### 5. tasks (`tasks.md`)\n\n- **Scope**: `change` (working document with live progress checkboxes)\n- **Requires**: `specs`, `design`\n- **Output**: `<change>/tasks.md`\n- **Purpose**: Granular, ordered implementation checklist driving execution.\n- **Conventions**:\n  - Grouped into logical task phases (e.g. `## 1. Domain Layer`, `## 2. CLI Adapter`).\n  - Checkboxes (`- [ ]` / `- [x]`) with target files, symbols, approach, and requirement references.\n  - Gated transition: Transition to `verifying` is blocked while any `- [ ]` remains.\n\n---\n"
      },
      {
        "index": 4,
        "heading": "1. proposal (`proposal.md`)",
        "level": 3,
        "startLine": 29,
        "endLine": 42,
        "lines": 14,
        "content": "### 1. proposal (`proposal.md`)\n\n- **Scope**: `change` (working document, not synced to specs on archive)\n- **Requires**: None (entrypoint artifact for every change)\n- **Output**: `<change>/proposal.md`\n- **Purpose**: Establishes the business motivation and technical intent before any spec or code is touched.\n- **Sections**:\n  - `## Motivation`: The problem being solved and value delivered.\n  - `## Current behaviour`: Existing behavior or limitations.\n  - `## Proposed solution`: High-level approach and architecture changes.\n  - `## Specs affected`: Exhaustive list of specs created (`New specs`) or modified (`Modified specs`).\n  - `## Impact`: Blast radius across workspaces, packages, and public contracts.\n  - `## Open questions`: Unresolved design trade-offs.\n"
      },
      {
        "index": 5,
        "heading": "2. specs (`spec.md` or `.delta.yaml`)",
        "level": 3,
        "startLine": 43,
        "endLine": 53,
        "lines": 11,
        "content": "### 2. specs (`spec.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent specification synced to `specs/` upon archive)\n- **Requires**: `proposal`\n- **Output**: `<change>/specs/<capability>/spec.md` or `<change>/deltas/<capability>/spec.md.delta.yaml`\n- **Purpose**: Formal normative contract defining what the system SHALL do.\n- **Conventions**:\n  - Normative RFC 2119 keywords (`SHALL`, `MUST`, `SHOULD`).\n  - Requirements grouped under `### Requirement: <Title>` headings.\n  - No implementation details (languages, classes, functions) or test scenarios.\n"
      },
      {
        "index": 6,
        "heading": "3. verify (`verify.md` or `.delta.yaml`)",
        "level": 3,
        "startLine": 54,
        "endLine": 63,
        "lines": 10,
        "content": "### 3. verify (`verify.md` or `.delta.yaml`)\n\n- **Scope**: `spec` (permanent verification scenarios synced to `specs/` upon archive)\n- **Requires**: `specs`\n- **Output**: `<change>/specs/<capability>/verify.md` or `<change>/deltas/<capability>/verify.md.delta.yaml`\n- **Purpose**: Concrete test scenarios verifying requirement satisfaction.\n- **Conventions**:\n  - One-to-one parity with `spec.md`: every `### Requirement: <Title>` in `spec.md` has a corresponding heading in `verify.md`.\n  - Structured GIVEN/WHEN/THEN behavior scenarios under `#### Scenario: <Name>`.\n"
      },
      {
        "index": 7,
        "heading": "4. design (`design.md`)",
        "level": 3,
        "startLine": 64,
        "endLine": 78,
        "lines": 15,
        "content": "### 4. design (`design.md`)\n\n- **Scope**: `change` (working document)\n- **Requires**: `proposal`, `specs`, `verify`\n- **Output**: `<change>/design.md`\n- **Purpose**: Concrete technical specification of _how_ the implementation will be built.\n- **Sections**:\n  - `## Overview`: Technical summary and architecture diagrams.\n  - `## Affected areas`: Files, packages, and modules touched.\n  - `## New constructs`: Class, function, interface, and error signatures.\n  - `## Approach & Execution flow`: Algorithms and step-by-step flow.\n  - `## Key decisions`: Architecture Decision Records (ADRs) inline with rationale and alternatives.\n  - `## Trade-offs`: Explicit compromises made.\n  - `## Testing`: Unit, integration, and E2E test strategy.\n"
      },
      {
        "index": 8,
        "heading": "5. tasks (`tasks.md`)",
        "level": 3,
        "startLine": 79,
        "endLine": 91,
        "lines": 13,
        "content": "### 5. tasks (`tasks.md`)\n\n- **Scope**: `change` (working document with live progress checkboxes)\n- **Requires**: `specs`, `design`\n- **Output**: `<change>/tasks.md`\n- **Purpose**: Granular, ordered implementation checklist driving execution.\n- **Conventions**:\n  - Grouped into logical task phases (e.g. `## 1. Domain Layer`, `## 2. CLI Adapter`).\n  - Checkboxes (`- [ ]` / `- [x]`) with target files, symbols, approach, and requirement references.\n  - Gated transition: Transition to `verifying` is blocked while any `- [ ]` remains.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "Workflow Step Gating",
        "level": 2,
        "startLine": 92,
        "endLine": 106,
        "lines": 15,
        "content": "## Workflow Step Gating\n\n`@specd/schema-std` configures required artifacts for each lifecycle transition:\n\n| State          | Required Artifacts                               | Description                                                                      |\n| :------------- | :----------------------------------------------- | :------------------------------------------------------------------------------- |\n| `drafting`     | None                                             | Initial state after change creation.                                             |\n| `designing`    | None                                             | Authoring proposals, specs, verify, design, and tasks.                           |\n| `ready`        | `proposal`, `specs`, `verify`, `design`, `tasks` | All planning artifacts complete and validated.                                   |\n| `implementing` | `proposal`, `specs`, `verify`, `design`, `tasks` | Coding and task checklist advancement.                                           |\n| `verifying`    | `verify`, `tasks`                                | Testing and scenario verification. Blocked if any `- [ ]` remains in `tasks.md`. |\n| `archiving`    | `specs`, `tasks`                                 | Delta merging and syncing into `specs/`.                                         |\n\n---\n"
      },
      {
        "index": 10,
        "heading": "Cross-Artifact Validations",
        "level": 2,
        "startLine": 107,
        "endLine": 117,
        "lines": 11,
        "content": "## Cross-Artifact Validations\n\nThe standard schema enforces relational integrity across artifacts:\n\n1. **`specs-verify-requirement-parity`**:\n   Ensures that every requirement declared in `spec.md` has a matching requirement heading with verification scenarios in `verify.md`.\n2. **`proposal-specs-coverage`**:\n   Ensures that every spec identifier listed under `## Specs affected` in `proposal.md` has a corresponding spec file or delta in the change.\n3. **`design-tasks-coverage`**:\n   Ensures that files and constructs identified in `design.md` are covered by implementation items in `tasks.md`.\n"
      }
    ]
  },
  {
    "topic": "custom-schemas",
    "title": "Authoring Custom Schemas",
    "description": "Creating and publishing custom SpecD workflow schemas, extensions, and schema plugins.",
    "order": 12,
    "content": "# Authoring Custom Schemas\n\nWhile `@specd/schema-std` fits most software engineering workflows, teams often need customized processes: adding Architecture Decision Records (ADRs), integrating security audits, enforcing database migration reviews, or custom validation rules.\n\nSpecD provides three progressive mechanisms to customize workflows:\n\n1. **`schemaOverrides`** in `specd.yaml` (lightweight inline customization)\n2. **`schemaPlugins`** (composable schema layers)\n3. **`specd schema extend` / `specd schema fork`** (custom schema packages)\n\n---\n\n## 1. Inline Customization with `schemaOverrides`\n\n`schemaOverrides` allows you to modify the active schema directly in `specd.yaml` without publishing a separate package.\n\nSupported operations:\n\n- `create`: Add new artifact types or workflow steps.\n- `append`: Add rules, hooks, or validators to existing lists.\n- `prepend`: Insert rules or hooks at the start of lists.\n- `set`: Replace fields or objects by key.\n- `remove`: Remove entries by `id` or `step`.\n\n### Example: Adding an ADR Artifact Type\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: 'Architecture Decision Record for significant design choices'\n        requires:\n          - specs\n        template: |\n          # ADR: {{change.name}}\n\n          ## Context\n          ## Decision\n          ## Consequences\n```\n\n### Example: Adding Quality Gate Hooks\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-typecheck\n              run: 'pnpm typecheck'\n            - id: run-unit-tests\n              run: 'pnpm test'\n```\n\n---\n\n## 2. Composable Schema Plugins (`schemaPlugins`)\n\nWhen multiple repositories share standard additions (e.g. corporate security rules, compliance checklists), you can package them as schema plugins.\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaPlugins:\n  - '@acme/specd-compliance-plugin'\n  - '@acme/specd-security-plugin'\n\nschemaOverrides:\n  # Local project overrides applied on top of plugins\n  append:\n    workflow:\n      - step: archiving\n        hooks:\n          pre:\n            - id: notify-slack\n              run: './scripts/notify.sh'\n```\n\nPlugins are applied in declaration order before `schemaOverrides`.\n\n---\n\n## 3. Creating Schema Packages (`extend` vs `fork`)\n\n### Extending a Schema (`specd schema extend`)\n\nUse `specd schema extend` when your custom workflow is based on an existing schema:\n\n```bash\nspecd schema extend @specd/schema-std my-workflow-schema\n```\n\nThis creates a new package where:\n\n- The base schema is declared as parent.\n- Only differences (new artifacts, modified rules) are specified.\n- Upstream updates to the parent schema are inherited automatically.\n\n### Forking a Schema (`specd schema fork`)\n\nUse `specd schema fork` when you need a fundamentally different workflow (e.g., non-code project, hardware spec workflow, or completely renamed lifecycle steps):\n\n```bash\nspecd schema fork @specd/schema-std standalone-schema\n```\n\nThis copies the entire schema definition, giving you full ownership of every artifact, rule, and lifecycle transition.\n\n---\n\n## Choosing the Right Approach\n\n| Use Case                                                         | Recommended Approach              |\n| :--------------------------------------------------------------- | :-------------------------------- |\n| Add a test hook or lint rule to your project                     | `schemaOverrides` in `specd.yaml` |\n| Add an optional project-specific artifact (e.g. `openapi.yaml`)  | `schemaOverrides`                 |\n| Share custom rules across multiple company repositories          | `schemaPlugins`                   |\n| Build an organization-wide standard based on `@specd/schema-std` | `specd schema extend`             |\n| Complete redesign of lifecycle states and dependencies           | `specd schema fork`               |\n",
    "lineCount": 131,
    "byteLength": 4059,
    "outline": [
      {
        "index": 1,
        "heading": "Authoring Custom Schemas",
        "level": 1,
        "startLine": 1,
        "endLine": 131,
        "lines": 131,
        "content": "# Authoring Custom Schemas\n\nWhile `@specd/schema-std` fits most software engineering workflows, teams often need customized processes: adding Architecture Decision Records (ADRs), integrating security audits, enforcing database migration reviews, or custom validation rules.\n\nSpecD provides three progressive mechanisms to customize workflows:\n\n1. **`schemaOverrides`** in `specd.yaml` (lightweight inline customization)\n2. **`schemaPlugins`** (composable schema layers)\n3. **`specd schema extend` / `specd schema fork`** (custom schema packages)\n\n---\n\n## 1. Inline Customization with `schemaOverrides`\n\n`schemaOverrides` allows you to modify the active schema directly in `specd.yaml` without publishing a separate package.\n\nSupported operations:\n\n- `create`: Add new artifact types or workflow steps.\n- `append`: Add rules, hooks, or validators to existing lists.\n- `prepend`: Insert rules or hooks at the start of lists.\n- `set`: Replace fields or objects by key.\n- `remove`: Remove entries by `id` or `step`.\n\n### Example: Adding an ADR Artifact Type\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: 'Architecture Decision Record for significant design choices'\n        requires:\n          - specs\n        template: |\n          # ADR: {{change.name}}\n\n          ## Context\n          ## Decision\n          ## Consequences\n```\n\n### Example: Adding Quality Gate Hooks\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-typecheck\n              run: 'pnpm typecheck'\n            - id: run-unit-tests\n              run: 'pnpm test'\n```\n\n---\n\n## 2. Composable Schema Plugins (`schemaPlugins`)\n\nWhen multiple repositories share standard additions (e.g. corporate security rules, compliance checklists), you can package them as schema plugins.\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaPlugins:\n  - '@acme/specd-compliance-plugin'\n  - '@acme/specd-security-plugin'\n\nschemaOverrides:\n  # Local project overrides applied on top of plugins\n  append:\n    workflow:\n      - step: archiving\n        hooks:\n          pre:\n            - id: notify-slack\n              run: './scripts/notify.sh'\n```\n\nPlugins are applied in declaration order before `schemaOverrides`.\n\n---\n\n## 3. Creating Schema Packages (`extend` vs `fork`)\n\n### Extending a Schema (`specd schema extend`)\n\nUse `specd schema extend` when your custom workflow is based on an existing schema:\n\n```bash\nspecd schema extend @specd/schema-std my-workflow-schema\n```\n\nThis creates a new package where:\n\n- The base schema is declared as parent.\n- Only differences (new artifacts, modified rules) are specified.\n- Upstream updates to the parent schema are inherited automatically.\n\n### Forking a Schema (`specd schema fork`)\n\nUse `specd schema fork` when you need a fundamentally different workflow (e.g., non-code project, hardware spec workflow, or completely renamed lifecycle steps):\n\n```bash\nspecd schema fork @specd/schema-std standalone-schema\n```\n\nThis copies the entire schema definition, giving you full ownership of every artifact, rule, and lifecycle transition.\n\n---\n\n## Choosing the Right Approach\n\n| Use Case                                                         | Recommended Approach              |\n| :--------------------------------------------------------------- | :-------------------------------- |\n| Add a test hook or lint rule to your project                     | `schemaOverrides` in `specd.yaml` |\n| Add an optional project-specific artifact (e.g. `openapi.yaml`)  | `schemaOverrides`                 |\n| Share custom rules across multiple company repositories          | `schemaPlugins`                   |\n| Build an organization-wide standard based on `@specd/schema-std` | `specd schema extend`             |\n| Complete redesign of lifecycle states and dependencies           | `specd schema fork`               |\n"
      },
      {
        "index": 2,
        "heading": "1. Inline Customization with `schemaOverrides`",
        "level": 2,
        "startLine": 13,
        "endLine": 66,
        "lines": 54,
        "content": "## 1. Inline Customization with `schemaOverrides`\n\n`schemaOverrides` allows you to modify the active schema directly in `specd.yaml` without publishing a separate package.\n\nSupported operations:\n\n- `create`: Add new artifact types or workflow steps.\n- `append`: Add rules, hooks, or validators to existing lists.\n- `prepend`: Insert rules or hooks at the start of lists.\n- `set`: Replace fields or objects by key.\n- `remove`: Remove entries by `id` or `step`.\n\n### Example: Adding an ADR Artifact Type\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: 'Architecture Decision Record for significant design choices'\n        requires:\n          - specs\n        template: |\n          # ADR: {{change.name}}\n\n          ## Context\n          ## Decision\n          ## Consequences\n```\n\n### Example: Adding Quality Gate Hooks\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-typecheck\n              run: 'pnpm typecheck'\n            - id: run-unit-tests\n              run: 'pnpm test'\n```\n\n---\n"
      },
      {
        "index": 3,
        "heading": "Example: Adding an ADR Artifact Type",
        "level": 3,
        "startLine": 25,
        "endLine": 48,
        "lines": 24,
        "content": "### Example: Adding an ADR Artifact Type\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaOverrides:\n  create:\n    artifacts:\n      - id: adr\n        scope: spec\n        output: 'specs/**/adr.md'\n        optional: true\n        description: 'Architecture Decision Record for significant design choices'\n        requires:\n          - specs\n        template: |\n          # ADR: {{change.name}}\n\n          ## Context\n          ## Decision\n          ## Consequences\n```\n"
      },
      {
        "index": 4,
        "heading": "Example: Adding Quality Gate Hooks",
        "level": 3,
        "startLine": 49,
        "endLine": 66,
        "lines": 18,
        "content": "### Example: Adding Quality Gate Hooks\n\n```yaml\n# specd.yaml\nschemaOverrides:\n  append:\n    workflow:\n      - step: implementing\n        hooks:\n          post:\n            - id: run-typecheck\n              run: 'pnpm typecheck'\n            - id: run-unit-tests\n              run: 'pnpm test'\n```\n\n---\n"
      },
      {
        "index": 5,
        "heading": "2. Composable Schema Plugins (`schemaPlugins`)",
        "level": 2,
        "startLine": 67,
        "endLine": 93,
        "lines": 27,
        "content": "## 2. Composable Schema Plugins (`schemaPlugins`)\n\nWhen multiple repositories share standard additions (e.g. corporate security rules, compliance checklists), you can package them as schema plugins.\n\n```yaml\n# specd.yaml\nschema: '@specd/schema-std'\n\nschemaPlugins:\n  - '@acme/specd-compliance-plugin'\n  - '@acme/specd-security-plugin'\n\nschemaOverrides:\n  # Local project overrides applied on top of plugins\n  append:\n    workflow:\n      - step: archiving\n        hooks:\n          pre:\n            - id: notify-slack\n              run: './scripts/notify.sh'\n```\n\nPlugins are applied in declaration order before `schemaOverrides`.\n\n---\n"
      },
      {
        "index": 6,
        "heading": "3. Creating Schema Packages (`extend` vs `fork`)",
        "level": 2,
        "startLine": 94,
        "endLine": 121,
        "lines": 28,
        "content": "## 3. Creating Schema Packages (`extend` vs `fork`)\n\n### Extending a Schema (`specd schema extend`)\n\nUse `specd schema extend` when your custom workflow is based on an existing schema:\n\n```bash\nspecd schema extend @specd/schema-std my-workflow-schema\n```\n\nThis creates a new package where:\n\n- The base schema is declared as parent.\n- Only differences (new artifacts, modified rules) are specified.\n- Upstream updates to the parent schema are inherited automatically.\n\n### Forking a Schema (`specd schema fork`)\n\nUse `specd schema fork` when you need a fundamentally different workflow (e.g., non-code project, hardware spec workflow, or completely renamed lifecycle steps):\n\n```bash\nspecd schema fork @specd/schema-std standalone-schema\n```\n\nThis copies the entire schema definition, giving you full ownership of every artifact, rule, and lifecycle transition.\n\n---\n"
      },
      {
        "index": 7,
        "heading": "Extending a Schema (`specd schema extend`)",
        "level": 3,
        "startLine": 96,
        "endLine": 109,
        "lines": 14,
        "content": "### Extending a Schema (`specd schema extend`)\n\nUse `specd schema extend` when your custom workflow is based on an existing schema:\n\n```bash\nspecd schema extend @specd/schema-std my-workflow-schema\n```\n\nThis creates a new package where:\n\n- The base schema is declared as parent.\n- Only differences (new artifacts, modified rules) are specified.\n- Upstream updates to the parent schema are inherited automatically.\n"
      },
      {
        "index": 8,
        "heading": "Forking a Schema (`specd schema fork`)",
        "level": 3,
        "startLine": 110,
        "endLine": 121,
        "lines": 12,
        "content": "### Forking a Schema (`specd schema fork`)\n\nUse `specd schema fork` when you need a fundamentally different workflow (e.g., non-code project, hardware spec workflow, or completely renamed lifecycle steps):\n\n```bash\nspecd schema fork @specd/schema-std standalone-schema\n```\n\nThis copies the entire schema definition, giving you full ownership of every artifact, rule, and lifecycle transition.\n\n---\n"
      },
      {
        "index": 9,
        "heading": "Choosing the Right Approach",
        "level": 2,
        "startLine": 122,
        "endLine": 131,
        "lines": 10,
        "content": "## Choosing the Right Approach\n\n| Use Case                                                         | Recommended Approach              |\n| :--------------------------------------------------------------- | :-------------------------------- |\n| Add a test hook or lint rule to your project                     | `schemaOverrides` in `specd.yaml` |\n| Add an optional project-specific artifact (e.g. `openapi.yaml`)  | `schemaOverrides`                 |\n| Share custom rules across multiple company repositories          | `schemaPlugins`                   |\n| Build an organization-wide standard based on `@specd/schema-std` | `specd schema extend`             |\n| Complete redesign of lifecycle states and dependencies           | `specd schema fork`               |\n"
      }
    ]
  }
] as const

export const GUIDES_INDEX: Readonly<Record<string, number>> = {
  "index": 0,
  "getting-started": 1,
  "installation": 2,
  "philosophy": 3,
  "workflow": 4,
  "specs": 5,
  "changes": 6,
  "configuration": 7,
  "project-structure": 8,
  "skills": 9,
  "workspaces": 10,
  "cli": 11,
  "schemas": 12,
  "selectors": 13,
  "artifacts": 14,
  "context-compilation": 15,
  "deltas": 16,
  "code-graph": 17,
  "configuration-examples": 18,
  "standard-schema": 19,
  "custom-schemas": 20
} as const
