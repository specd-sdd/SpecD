Generated: 2026-04-28

# Exploration Context — pluralize-cli-resource-commands

## Problem statement

The user reported an inconsistency in CLI naming: there is `drafts` (plural), but other commands (for countable entities) remain singular like `change` or `spec`. They want naming coherence by moving to plural resource names where appropriate, while preserving singular command forms as backward-compatible aliases.

## Why this matters

Current mixed singular/plural naming can increase cognitive load and make command discovery less predictable. A consistent resource naming strategy improves usability, documentation clarity, and skill prompt reliability.

## Approach / solution outline

- Introduce plural canonical command forms for countable resource groups currently exposed in singular.
- Keep singular forms as compatibility aliases (no immediate breaking change).
- Update user-facing docs and skill guidance to prefer the new plural canonical forms.
- Ensure help output and references converge on canonical plural nomenclature.

## Affected areas discussed

- CLI command naming and routing (package likely: `packages/cli`).
- Existing drafts command family already plural (`drafts`) and treated as reference style.
- Documentation references to command examples and CLI usage (`docs/` likely impacted).
- Agent skills content that embeds command examples/instructions (notably specd skills files under `.codex/skills/` and potentially shared notes).

## Spec IDs discussed and selected

Initial specs added to change on creation:

- `cli:cli/change-draft`
- `cli:cli/drafts-list`
- `cli:cli/drafts-show`
- `cli:cli/drafts-restore`

Additional likely relevant CLI specs discovered during exploration (not yet added in this step):

- `cli:cli/change-list`
- `cli:cli/change-status`
- `cli:cli/change-create`
- `cli:cli/change-edit`
- `cli:cli/change-transition`
- `cli:cli/change-validate`
- `cli:cli/change-context`
- `cli:cli/change-archive`
- `cli:cli/change-approve`
- `cli:cli/change-run-hooks`
- `cli:cli/change-hook-instruction`
- `cli:cli/change-artifact-instruction`
- `cli:cli/change-spec-preview`

Rationale: user explicitly called out singular `change` and `spec` as likely candidates; full exact target set still to be finalized in design.

## Design decisions confirmed

- Canonical direction: plural for countable command resources.
- Backward compatibility: keep singular commands as aliases.
- Non-goal (implied): do not force immediate user migration by removing old commands.

## Agreements reached

- User approved creating a change immediately with this intent.
- Scope should include docs and skills nomenclature updates, not only CLI parser changes.

## Rejected/Deferred alternatives

- Keeping mixed naming as-is was implicitly rejected by user concern.
- Immediate breaking rename (drop singular forms) not requested; compatibility-first preferred.

## Open questions for /specd-design

- Exact canonical plural map for each command group (`change` -> `changes`, `spec` -> `specs`, others?).
- Which singular forms remain as hidden aliases vs visible/deprecated aliases.
- How deprecation messaging should appear (if at all) in help output.
- Whether internal API/use-case naming remains singular while CLI surface becomes plural.
- Exact docs and skill files to update and migration wording strategy.

## Key codebase observations

- Project context confirms spec-driven workflow and command changes must flow through specs/artifacts.
- Workspaces ownership from `project status --format toon`: all relevant workspaces are `owned` (including `cli`), so writable scope is available.
- Relevant specs discovered through graph search include drafts and change command families.
- Graph freshness reported stale (`stale: true`), so deeper impact analysis may need reindex/refresh during implementation if precision is required.

## User preferences

- Prioritize consistency in command naming.
- Preserve backward compatibility.
- Include docs and skills updates as first-class scope.

## Tangential topics mentioned

- User uncertainty whether other countable nouns beyond `change`/`spec` should be included; indicates expectation to inspect for other similar inconsistencies.

## Conversation flow (chronological)

1. User invoked `$specd-new` and described concern about inconsistency: `drafts` plural vs other singular command groups.
2. User proposed preferred strategy: switch singular countable command groups to plural; keep singular for backward compatibility.
3. User added explicit scope extension: update docs and skills terminology.
4. Agent loaded specd-new shared instructions and project context, listed/searched specs, checked workspace ownership.
5. Agent proposed change name/description/initial specs and asked for confirmation.
6. User confirmed with "si".
7. Agent created change `pluralize-cli-resource-commands`.

## Anchors

- Change path: `/Users/monki/Documents/Proyectos/specd/.specd/changes/20260428-102814-pluralize-cli-resource-commands`
- Commands used during exploration:
  - `node packages/cli/dist/index.js project context --format text`
  - `node packages/cli/dist/index.js spec list --format text --summary`
  - `node packages/cli/dist/index.js graph search "draft command cli" --format toon`
  - `node packages/cli/dist/index.js project status --format toon`
  - `node packages/cli/dist/index.js change create pluralize-cli-resource-commands ... --format toon`
