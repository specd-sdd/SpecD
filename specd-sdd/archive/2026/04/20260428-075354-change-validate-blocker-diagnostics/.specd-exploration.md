Generated: 2026-04-28

# Exploration Context

## Problem statement

The user reported that `change validate` still shows a generic dependency-block message and expected it to report the real blocker status similarly to `change transition`, especially when a prior DAG blocker exists.

## Approach / solution outline

Create a focused change to align diagnostics between validation and transition flows.
Expected implementation direction:

- Enhance core validation dependency-block failure details to include effective status context.
- For review-related recursive blockers, include parent blocker information similar to transition diagnostics.
- Ensure CLI `change validate` renders the improved message from core output.

## Affected areas discussed

- Core use case: `packages/core/src/application/use-cases/validate-artifacts.ts`
- Transition diagnostics reference: `packages/core/src/domain/errors/invalid-state-transition-error.ts`
- CLI rendering: `packages/cli/src/commands/change/validate.ts`
- Related tests:
  - `packages/core/test/application/use-cases/validate-artifacts.spec.ts`
  - `packages/cli/test/commands/change-validate.spec.ts`

## Spec IDs in scope

- `cli:cli/change-validate`
- `core:core/validate-artifacts`

## Key observations from investigation

- Current dependency-block failure in validate is:
  - `Artifact '<id>' is blocked by incomplete dependency '<dep>'`
- This message does not include dependency effective status.
- `change transition` already has richer status-aware diagnostics, including specific handling for `pending-parent-artifact-review` and parent blocker context.

## Design decisions and agreements

- User explicitly requested the new behavior to be similar to `change transition`.
- A new change was preferred over continuing existing active changes.

## Rejected alternatives

- Reusing an existing active change: rejected by user (requested "uno nuevo").
- Limiting the fix to CLI-only text templating: likely insufficient because blocker status data should originate from core validation logic.

## Open questions

- None blocking creation. Exact wording can be finalized during design/implementation to match transition-style diagnostics without breaking existing output contracts unexpectedly.

## User preferences

- Diagnostics should reflect real blocker state instead of generic "incomplete" wording.
- Keep behavior aligned with existing transition diagnostics.

## Conversation flow summary

1. User suspected prior commits changed validation output behavior.
2. Investigation confirmed richer diagnostics were implemented for transitions, not for `change validate` dependency-block failures.
3. User requested implementation of similar behavior for `change validate`.
4. User requested creating a new change rather than reusing active ones.
5. New change was created with focused scope on CLI/core validation specs.
