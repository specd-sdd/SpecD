Generated: 2026-04-27

# Exploration Context

## Problem Statement

The current way of determining whether a given artifact generates task checkboxes is derived implicitly from elsewhere. The goal is to make this explicit by adding a boolean `hasTasks` field to the schema definitions of artifacts.

## Approach / Solution Outline

1. **Schema Update**: Introduce an optional `hasTasks` boolean property in `ArtifactYaml` within `schema.yaml` structures (e.g., in `@specd/core` schema formats).
2. **Derivation Logic**: Replace the current implicit task-checking logic with a direct read of the `hasTasks` property from the schema configuration.
3. **CLI Feedback**: Update the output of `specd change status` so that its DAG visualization explicitly shows a `[hasTasks]` tag next to artifacts that have this feature enabled.

## Affected Areas

- **core:** `ArtifactYaml` schema definition, likely inside `core/src/infrastructure/schema-yaml-parser.ts` or `core/src/domain/value-objects/schema.ts` / `artifact-type.ts`.
- **cli:** Output formatting logic for the DAG in `change status`, potentially inside `cli/src/commands/change-status.ts` or related display utilities.

## Spec IDs in Scope

- `core:core/schema-format`: Needs updating to describe the new `hasTasks` property.
- `cli:cli/change-status`: Needs updating to specify that `[hasTasks]` is displayed in the DAG.

## Agreements Reached

- Added the two relevant specs to the initial change scope: `core:core/schema-format` and `cli:cli/change-status`.
- Both workspaces are locally owned.

## Next Steps

- Run `/specd-design schema-artifact-hastasks` to begin drafting the actual spec changes and implementation tasks based on this exploration.
