# Exploration: change-edit-description

**Generated:** 2026-04-17

## Problem statement

La opción `--description` en `specd change edit` no funciona. El usuario ejecuta:

```bash
specd change edit <name> --description "Nueva descripción"
```

Pero la descripción del change no se actualiza.

## Approach / solution

Investigar por qué la opción `--description` no tiene efecto y luego implementarla en:

1. CLI command (`packages/cli/src/commands/change/edit.ts`)
2. Use case input interface (`packages/core/src/application/use-cases/edit-change.ts`)
3. Execute logic para persistir la descripción

## Affected areas

- `packages/cli/src/commands/change/edit.ts` — linea 91 pasa `description` al execute
- `packages/core/src/application/use-cases/edit-change.ts` — la interfaz `EditChangeInput` no tiene `description`

## Spec IDs

- `cli:cli/change-edit` — ya asignado al change

## Open questions

- ¿La descripción debe хранить invalidate el change? (creo que no, solo cambia metadata)
- ¿Necesita还有其他 validación?

## Key codebase observations

- `EditChangeInput` (edit-change.ts:11-18) solo tiene `name`, `addSpecIds`, `removeSpecIds`
- `EditChange.execute()` (linea 76-78) retorna early si no hay cambios de specs
- La opción `--description` es un **zombie option** — existe en CLI pero se ignora

## User preferences

- Implementar la funcionalidad para que `--description` actually actualice la descripción
