Generated: 2026-04-20

# Exploration Context — move-change-locks-to-config-tmp

## Problem statement

The user asked where the `change-locks` directory is generated, then requested changing that behavior so lock directories point to `tmp` inside the configured `configPath`.

User intent in Spanish:

- "puedes comprobar donde se genera el directorio change-locks?"
- "pues esto hay que cambiarlo para que apunte al tmp dentro del path de config"
- "quiero hacer uno nuevo"
- "ok" (confirmation to create this new change)

## What the user wants

Move lock storage for change mutation locking away from current storage-root sibling behavior and into config temp area:

- target area requested: "tmp dentro del path de config"
- implied desired base: `<configPath>/tmp/...`

## Current behavior observed (code anchors)

`change-locks` is currently derived from `changesPath` parent, not from `configPath`:

- `FsChangeRepository` sets `_locksPath = path.join(path.dirname(this._changesPath), 'change-locks')`
- file: `/Users/monki/Documents/Proyectos/specd/packages/core/src/infrastructure/fs/change-repository.ts`
- anchor line observed: ~111

Creation happens when acquiring mutation lock:

- `await fs.mkdir(this._locksPath, { recursive: true })`
- file: `/Users/monki/Documents/Proyectos/specd/packages/core/src/infrastructure/fs/change-repository.ts`
- anchor line observed: ~539

Lock flow:

- `mutate()` -> `_withChangeLock()` -> `_acquireLock()`
- `mutate` anchor: ~141
- `_withChangeLock` anchor: ~510
- `_lockDirPath` anchor: ~528

For this repo config:

- `storage.changes.fs.path` in `specd.yaml` is `.specd/changes`
- therefore current lock path resolves to `.specd/change-locks`
- current directory exists in workspace at `/Users/monki/Documents/Proyectos/specd/.specd/change-locks`

## Related config and composition anchors

Resolved project config currently reports:

- `configPath`: `/Users/monki/Documents/Proyectos/specd/.specd/config`
- `storage.changesPath`: `/Users/monki/Documents/Proyectos/specd/.specd/changes`

Likely composition point for wiring repository options:

- `/Users/monki/Documents/Proyectos/specd/packages/core/src/composition/change-repository.ts`
- `/Users/monki/Documents/Proyectos/specd/packages/core/src/composition/kernel-internals.ts`

## Specs considered relevant

Initial specs attached to this change:

- `core:core/change-repository-port`
- `core:core/storage`
- `core:core/config`

Read during exploration:

- `core:core/change-repository-port` (contract of mutate serialization and repository behavior)
- `core:core/storage` (fs adapter storage behavior and constraints)
- `core:core/config` (project config and `configPath` concept)

## Decisions and agreements reached

1. We created a NEW change (user explicitly requested a new one) instead of continuing existing active change.
2. Change name and scope agreed:
   - Name: `move-change-locks-to-config-tmp`
   - Description: `Move change lock directories under configPath/tmp instead of the storage root.`
   - Specs: `core:core/change-repository-port`, `core:core/storage`, `core:core/config`
3. No artifacts were authored yet (this is still discovery/new-change setup).

## Change creation result

Created successfully:

- name: `move-change-locks-to-config-tmp`
- state: `drafting`
- changePath: `/Users/monki/Documents/Proyectos/specd/.specd/changes/20260420-081323-move-change-locks-to-config-tmp`

CLI warning at creation:

- spec overlap detected:
  - `core:core/config` also targeted by existing change `context-display-mode-config` (state `designing`)

## Existing project state noticed during entry

There is already an active change:

- `context-display-mode-config` in `designing`
- includes spec `core:core/config` (same spec overlap as warning above)

## Hooks status during setup

For this new change (`drafting` pre phase):

- `change run-hooks ... drafting --phase pre` => no hooks to run
- `change hook-instruction ... drafting --phase pre` => no instructions

## Open questions to resolve in design

1. Exact lock path convention under config tmp:
   - Option A: `<configPath>/tmp/change-locks/<change>.lock`
   - Option B: `<configPath>/tmp/locks/changes/<change>.lock`
     Current user phrasing suggests Option A, but design should make it explicit.
2. Backward compatibility strategy:
   - Do we need fallback cleanup/read behavior for old `.specd/change-locks` directories, or just stop writing there?
3. Multi-project safety:
   - confirm whether `configPath` is always project-specific and writable in all supported setups.
4. Testing updates:
   - existing tests that assert lock dir under tmp root will need path expectation updates.

## Key codebase observations relevant for implementation

- `FsChangeRepository` currently computes lock path internally from `changesPath` only; no explicit `locksPath` option in constructor config.
- To move to `configPath/tmp`, composition may need to pass additional resolved path or repository may need access to config path-derived temporary directory.
- `config show --format json` confirms `configPath` is already computed/resolved by config loader and available in runtime config outputs.

## User preferences and constraints observed

- User prefers direct, concrete change to behavior and requested creating a separate new change.
- Language preference in interaction: Spanish.

## Rejected/alternative directions discussed

- Continuing existing change `context-display-mode-config` was not chosen; user requested a new change.
- No other naming/scope alternatives were debated after proposal was accepted.

## Tangential topics captured

- During initial investigation we inspected where `change-locks` is created and validated current location in filesystem.
- We observed temporary indexing artifacts under `.specd/config/tmp/...` in user IDE context, which motivated desired target area.

## Conversation flow (chronological)

1. User asked where `change-locks` is generated.
2. Investigation traced generation to `FsChangeRepository` lock acquisition and showed current resolved location.
3. User requested changing behavior to point to `tmp` inside config path.
4. Workflow entry through `specd` was run and current active change state was presented.
5. User requested creating a new change.
6. `specd-new` flow was executed, relevant specs/context were loaded, proposal was presented.
7. User confirmed ("ok").
8. New change `move-change-locks-to-config-tmp` was created; overlap warning with `core:core/config` surfaced.
9. Drafting pre hooks/instructions were checked (none).
10. This exploration file was written for handoff to `/specd-design`.
