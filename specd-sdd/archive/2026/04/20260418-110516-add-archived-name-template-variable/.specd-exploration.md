# Exploration Context

Generated: 2026-04-18

## Problem Statement

The user requested verification of whether the recent hook-related code change should have required a spec update. The concrete change in code added `change.archivedName` to template variables used in archived post-hook execution (`RunStepHooks` archive fallback path). This variable is used by the post-archive changeset hook path.

The user explicitly wants this framed as "adding the variable" rather than generic "alignment", and wants the change description to reflect that this is needed for changeset behavior.

## Approach / Solution Outline

Create a focused specd change that:

- Adds `change.archivedName` as an officially supported template variable for archived-change flows.
- Updates specs that define hook variable construction and template-variable registry/contracts.
- Leaves hook script behavior discussions for design phase scope decision (documented below), while ensuring the variable contract itself is explicit and testable.

## Change Created

- Name: `add-archived-name-template-variable`
- Description: `Add change.archivedName as a supported hook/template variable for archived-change flows, including the post-archive changeset generation path.`
- Path: `/Users/monki/Documents/Proyectos/specd/.specd/changes/20260418-110516-add-archived-name-template-variable`
- Initial specs:
- `core:core/run-step-hooks`
- `core:core/template-variables`
- Initial state: `drafting`

## Affected Areas Discussed

- Code:
- `packages/core/src/application/use-cases/run-step-hooks.ts`
- `packages/core/test/application/use-cases/run-step-hooks.spec.ts`
- `scripts/hooks/post-archive-changeset.js`
- Specs:
- `specs/core/run-step-hooks/spec.md`
- `specs/core/template-variables/spec.md`
- Potentially (decision pending during design): a spec governing `post-archive-changeset` hook behavior if the team wants formal contract coverage for script semantics.

## Key Codebase Observations

- `RunStepHooks` archived fallback currently builds variables including:
- `change.name`
- `change.workspace`
- `change.path`
- `change.archivedName` (newly added in code)
- Current spec text in `run-step-hooks` documents only `name/workspace/path` and does not list `archivedName`.
- Current spec text in `template-variables` lists only:
- `change.name`
- `change.workspace`
- `change.path`
- no explicit `change.archivedName`.
- `scripts/hooks/post-archive-changeset.js` currently contains behavior changes (source selection for release info, workspace package map extension, failure handling with `process.exit(1)`), and references archive naming conventions. The historic spec directory `specs/_global/post-archive-changeset/` is removed in current tree state, so hook-script behavior is currently under-specified unless re-specified elsewhere.

## Decisions and Agreements

- User confirmed opening a new change via `specd-new` workflow.
- User requested naming/framing to emphasize **adding** `change.archivedName` variable (not merely "alignment").
- Description must explicitly mention downstream use in changeset generation path.
- User approved proposed name and description.

## Rejected / Adjusted Alternatives

- Initial wording proposal used "align hook/template variable specs"; user requested stronger product intent wording: "add variable" because the variable enables real behavior (changeset flow) rather than only documentation cleanup.

## Open Questions for /specd-design

- Should this change include only core variable-contract specs (`run-step-hooks`, `template-variables`) or also introduce/restore a dedicated spec for post-archive changeset hook script behavior?
- If hook script behavior is included, what is authoritative source precedence for bump mapping:
- `.changeset-release.yaml`
- archived `manifest.json`
- inferred bump logic from content
- What failure contract should be normative for the script:
- fail-open (`exit 0`) vs fail-fast (`exit 1`) and how this interacts with archive lifecycle guarantees.

## Dependencies Context

- Existing conceptual dependency: `core:core/run-step-hooks` relies on variable semantics defined in `core:core/template-variables`.
- No new dependency registration was performed during creation; reassess in design if explicit `change deps` updates are beneficial.

## Hooks Executed During Creation

- `change run-hooks <name> drafting --phase pre`: no hooks to run
- `change hook-instruction <name> drafting --phase pre --format text`: no instructions

## Conversation Flow Summary

- User asked if hook changes required spec updates.
- Analysis found mismatch: code exposes `change.archivedName`; specs do not document it.
- User asked to proceed through `specd-new` and then requested wording focused on adding the variable and linking motivation to changeset usage.
- Proposed change name/description/spec scope was updated accordingly.
- User approved creation.

## Tangential Topics Mentioned

- Graph search command currently returned an error (`fatal: no such column: step`) during discovery; graph was re-indexed successfully but search command issue remains an operational concern outside this change’s direct scope.
- Existing unstaged local changes in unrelated hook files were part of prior workflow context but are not part of this newly created change scope.
