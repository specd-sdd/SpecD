# Fast-track Journal: lifecycle-polish-followups

> **MANDATORY DIRECTIVES FOR `/specd-design`**:
> When generating change artifacts during the design phase:
>
> 1. **Primary Implementation Scope (Tracked Links)**:
>    - Do NOT rely solely on `git diff` (intermediate commits might have occurred during the session).
>    - You MUST inspect the tracked implementation files and confirmed links via `specd changes implementation list <name>` and `specd changes implementation review <name> --format toon` as the authoritative scope of code modifications.
> 2. **Spec Evaluation & Selection**: Critically evaluate the suggested specs under `## Suggested Specs for Design` or search the workspace specs to find better-fitting alternatives (or determine if a brand-new capability spec is required). Register the final, optimal set of specs via `specd changes edit <name> --add-spec <spec-id>`.
> 3. **Proposal artifact**: Transcribe the motivation, root cause, and agreed solution into the formal proposal document.
> 4. **Specification & Verification artifacts (or deltas)**: Formalize the behavior into normative requirements and verification scenarios according to the schema instructions. Incorporate any broken/modified contracts identified below.
> 5. **Design / Architecture artifact**: Document the concrete architecture, touched files/symbols from the tracked implementation links, and technical choices.
> 6. **Task-bearing artifact (`[hasTasks]`)**:
>    - **DO NOT start tasks from scratch.** The fast-track journal explicitly lists all tasks that have already been implemented and verified during this session under `## Completed Work`.
>    - You MUST carry over all items from **Completed Work** below and generate them in the task-bearing artifact **already marked as completed (`- [x]`)**, following the artifact's instructions and template.
>    - Carry over all items from **Pending Work & Gaps** into the task-bearing artifact **marked as open/pending (`- [ ]`)**, following the artifact's instructions and template.
>    - Preserve the task metadata and structure required by the schema (e.g. file, symbol, approach, and requirement mapping).
>    - This ensures the task artifact accurately preserves the full audit trail of what was built during the fast-track session alongside any remaining polish/tests.
> 7. **Completeness, Edge Cases & Omissions (Capture what was left out)**:
>    - The fast-track session focused on the primary solution. The design skill MUST critically identify any omitted edge cases, boundary conditions, error handling paths, or missing unit/integration tests.
>    - Add new verification scenarios for these edge cases into the verification artifact.
>    - Add concrete follow-up tasks into the task-bearing artifact marked as pending/open tasks.
> 8. **Codebase-Wide Adoption & Affected Areas (MUST)**:
>    - You MUST investigate if other parts of the codebase should adopt, reuse, or be updated to use this new feature, capability, refactor, or fix (e.g. eliminating duplicated logic, replacing legacy call patterns, or standardizing behavior across packages/workspaces).
>    - Search the codebase (`specd graph search`, grep) for adoption candidates.
>    - Include concrete adoption/migration tasks in the task-bearing artifact and document them in the design artifact.
> 9. **Blast Radius & Downstream Regression Search (MUST)**:
>    - You MUST run code graph impact analysis (`specd graph impact`) on all tracked files and symbols to identify callers or modules across the codebase that could suffer side effects or regressions.
>    - Ensure any affected downstream areas have corresponding regression test tasks and design mitigations documented.

## Motivation & Problem

- Remediate the confirmed lifecycle-transition and CLI UX contract defects, the related workflow-template issues, and the spec-compliance hygiene findings in one code-first session before formal artifacts are authored.

## Approaches Explored & Agreed Strategy

- Chosen: one unscoped fast-track change covering runtime, CLI, skills templates, tests, and spec hygiene; use focused regression tests to prove each runtime contract before formalizing specs.
- Rejected: reopening the archived workflow-transition-checks change, because a new bounded follow-up preserves traceability without mutating historical workflow.
- Rejected: splitting hygiene first, because implementation evidence will clarify the intentional contracts that specs must describe.

## Suggested Specs for Design

<!-- Discovered during exploration; the design skill must critically evaluate these suggestions or search for better-fitting alternatives before registering via specd changes edit <name> --add-spec -->

- `core:transition-change` (suggested: modified)
- `core:lifecycle-engine` (suggested: modified)
- `core:transition-checks` (suggested: modified)
- `cli:change-status` (suggested: modified)
- `skills:skill-templates-source` (suggested: modified)

## Spec Contracts & Impact Analysis

<!-- Record specs read and any contracts modified or broken -->

### Active Specs Inspected

- Project context: `default:_global/architecture`, `default:_global/conventions`, `default:_global/testing`, and related global directives.
- Candidate lifecycle and delivery specs identified by graph search; full governing context remains to be loaded per target file.

### Broken or Modified Contracts (if any)

<!-- e.g. `<workspace:spec-id>`: Requirement: <name> — previous behavior vs new behavior and why -->

- `core:transition-change`: historic parked approval states can surface `invalid-transition` before the promised `approval-required` repair guidance.
- `core:lifecycle-engine`: `transitionBlockers` does not project the artifact ID from a failed `workflow.requires` predicate.
- `cli:change-status`: text rendering can expose `undefined` for missing display status; structured status shape requires a single documented contract.
- `skills:skill-templates-source`: fast-track stop-gate and impact terminology need alignment with normative requirements.

## Codebase Adoption & Affected Areas (Initial Findings)

<!-- Other places in the codebase noted during exploration that might need or adopt this change -->

- Areas identified to adopt this change: Core lifecycle projections, CLI status projection, and rendered workflow skill templates.
- Downstream affected modules: CLI/API consumers of typed transition failures and JSON status; installed skill consumers.

## Decisions & Code Actions

- 2026-09-07: Created `lifecycle-polish-followups` without preassigned specs and activated implementation tracking. Reason: user requested one new fast-track change covering all findings before formal spec authoring. Affected scope: new change directory only.
- 2026-09-07: Recorded the initial approach and candidate governing specs. Reason: preserve the rationale before runtime investigation and edits.
- 2026-09-07: Ran graph impact before target investigation. `core:src/application/use-cases/transition-change.ts` has CRITICAL blast radius (86 direct dependents); `cli:src/commands/change/status.ts` is also CRITICAL and covered by `cli:change-status`. The graph has no indexed Markdown entry for the fast-track template, so source inspection is the permitted fallback for that template. Reason: constrain changes and regression tests to the evidence-backed consumer surface.
- 2026-09-07: Loaded compiled context for `core:transition-change`, `core:lifecycle-engine`, `core:transition-checks`, `cli:change-status`, and `skills:skill-templates-source`; refreshed the stale transition-change optimization first. Confirmed the required runtime behavior and the template contract. Planned edits: fail parked non-drain requests before `protocol.edge`; derive transition blocker IDs from failed predicate details; use canonical/effective status fallback in CLI text and structured projection; make Step 2's user-choice an explicit stop; replace blast-radius “downstream” language with “dependents”.
- 2026-09-07: Edited `TransitionChange._assertDrainAndGateTargets` so historic parked non-drain transitions fail as `approval-required` before predicate evaluation; edited `evaluateLifecycleVerdict` so predicate-backed `transitionBlockers` preserve the failed required artifact; edited CLI status rendering/JSON projection to fall back from absent `displayStatus`; edited the fast-track template to add an explicit Step 2 stop and dependents terminology. Reason: restore typed repair guidance, stable status output, and template/spec alignment.
- 2026-09-07: Added regression cases for parked spec/signoff non-drain failures, predicate-projected transition blocker artifact IDs, absent CLI display status, and fast-track Step 2 stop/terminology. Reason: make the externally visible contracts reproducible before formal spec artifacts are authored.
- 2026-09-07: Registered Core, CLI, and skills source/test files in implementation tracking, using stable symbols `TransitionChange`, `evaluateLifecycleVerdict`, and `registerChangeStatus` for runtime entries. Reason: retain the authoritative implementation scope independently from the working diff.
- 2026-09-07: Verification passed: Core targeted suite reported 25/25 files and 122/122 tests; CLI status suite reported 81/81 files and 914/914 tests; skills template suite reported 8/8 files and 53/53 tests. The new parked-state, blocker-detail, display fallback, and Step 2 stop regressions all passed.
- 2026-09-07: Reindexed the code graph after source edits and confirmed it is current. Additional P1 impact shows `resolveWorkflowCheckRegistry` is HIGH risk and reaches all lifecycle factories; archive snapshot composition shares that surface. Reason: avoid making a default-wiring change without inspecting all production callers.
- 2026-09-07: Inspected overlap and snapshot composition. `resolveGetStatusDeps` and `resolveArchiveChangeDeps` explicitly enable overlap detection; transition correctly does not need archive overlap I/O. No runtime overlap change is needed, but the composition invariant needs formal documentation. `resolveArchiveBatchSnapshotPort` fallback recreates `FsArchiveBatchSnapshot` per method, which loses its in-memory snapshot manifest; unify it with a lazy shared instance and test rollback behavior.
- 2026-09-07: Edited `resolveArchiveBatchSnapshotPort` to lazily create one fallback `FsArchiveBatchSnapshot` instance and reuse it for every port method. Added a rollback regression that snapshots a new spec directory, records a created file, restores, and verifies that the file is removed. Reason: keep snapshot state coherent without requiring explicit layouts.
- 2026-09-07: Registered archive snapshot composition and its regression test with `core:archive-change` in implementation tracking. Reason: preserve the fallback adapter as a confirmed implementation surface for formal design.
- 2026-09-07: Snapshot fallback regression passed: 2/2 suites, 3/3 tests. It verifies that a created file recorded after snapshot is removed by restore through the same fallback port.
- 2026-09-07: Reindexed again after the snapshot edit; graph reports current and complete. Audited the remaining memoization/JSON-shape concerns: no second `CountTasks` call was found in `TransitionChange`; active status uses `lifecycle.*` while drafts intentionally expose empty legacy top-level arrays and omit a schema DAG. These are formal contract/spec-hygiene items, not additional runtime defects.
- 2026-09-07: TypeScript typecheck completed with no errors for the workspace command covering the touched packages. Reason: validate strict TypeScript compatibility after Core, CLI, and template edits.
- 2026-09-07: Audited implementation tracking: 10 confirmed links with all stable symbols resolved and a fresh, complete graph. The three optimized-context `spec-lock.json` files are incidental sidecars, not implementation scope. The runtime/template/test files are reviewed and ready to resolve in tracking; formal spec scope remains intentionally deferred to design.

## Consolidation & Audit (Pending)

## Consolidation & Audit Summary

### 1. Root Cause & Solution Summary

- Historic parked approval states evaluated `protocol.edge` before returning typed approval guidance. They now reject non-drain requests as `approval-required` before predicate evaluation.
- Predicate-projected lifecycle blockers discarded `workflow.requires.details.artifactId`; they now preserve it.
- CLI status now falls back to effective/canonical state when `displayStatus` is absent.
- Archive snapshot fallback now retains one lazy snapshot instance across operations.
- Fast-track Step 2 now has an explicit stop; blast-radius wording uses dependents.

### 2. Tracked Implementation Summary

- Resolved: 10 Core, CLI, and skills source/test files. Stable links: `TransitionChange`, `evaluateLifecycleVerdict`, `registerChangeStatus`, and `resolveArchiveBatchSnapshotPort`.
- Ignored incidental optimizer sidecars: architecture, logging, and transition-change `spec-lock.json`.

### 3. Suggested Specs for Design (Final Review)

- `core:transition-change`: parked approval failure contract.
- `core:lifecycle-engine`: blocker projection contract.
- `core:transition-checks`: approval/error precedence and single-pass evaluation context.
- `core:archive-change`: fallback snapshot lifetime.
- `cli:change-status`: display fallback and active-vs-draft JSON shape.
- `skills:skill-templates-source`: Step 2 stop and dependents terminology.

### 4. Spec Contract Impact Summary

- Runtime behavior is now aligned with existing transition intent.
- Formal deltas must resolve hygiene discrepancies: hooks wording, metadata extraction wording, task-bearing artifact wording, GetStatus schema-failure wording, archive command naming, ConfigWriter initialization/plugins wording, and artifact-instruction timing.
- Document that overlap detection is explicitly injected for production status/archive composition; status shape is lifecycle-nested for active changes and legacy-empty top-level arrays for drafts, with draft DAG omission intentional.

### 5. Codebase Adoption & Propagation Candidates (MUST Check in Design)

- No additional runtime caller needs overlap detection beyond status/archive composition.
- Installed/rendered fast-track skills inherit template changes; ensure generated bundles retain the new stop and terminology.

### 6. Completed Work (Tasks already implemented & verified)

- [x] `TransitionChange`: typed parked spec/signoff non-drain errors; Core regression coverage.
- [x] `evaluateLifecycleVerdict`: preserve required artifact IDs in transition blockers; domain regression coverage.
- [x] CLI status: canonical fallback for absent display status; command regression coverage.
- [x] Archive snapshot composition: lazy shared fallback instance; rollback regression coverage.
- [x] Fast-track template: Step 2 stop and dependents language; template regression coverage.
- [x] Targeted verification: Core 122/122, CLI 914/914, skills 53/53; TypeScript typecheck clean.

### 7. Pending Work & Gaps (Tasks remaining to be done)

- [ ] Author formal spec/verify/design/task artifacts and register the five suggested specs.
- [ ] Resolve the documented spec-hygiene contradictions listed above without changing unrelated runtime behavior.
- [ ] Review active/draft CLI JSON documentation and formalize the selected shape.

### 8. Spec & Verification Guidance

- Add normative scenarios for parked non-drain typed failures, predicate blocker artifact IDs, display fallback, snapshot rollback, and fast-track Step 2 confirmation.
- Add constraints distinguishing production overlap injection from non-production registry default behavior.
