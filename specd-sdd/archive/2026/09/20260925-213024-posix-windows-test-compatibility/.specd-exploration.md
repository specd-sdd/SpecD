# Exploration: posix-windows-test-compatibility

Generated: 2026-09-25

## Problem statement

User wants to modify the global testing spec so it is explicit that tests MUST be compatible with both POSIX and Windows environments — not only narrow Windows-safe fixture rules (tmpdir / chmod).

Why: CI already runs the same job on `macos-latest`, `ubuntu-latest`, and `windows-latest` (`default:_global/continuous-integration`). The testing conventions currently call out Windows-safe fixtures but do not state a general cross-platform (POSIX + Windows) compatibility requirement for tests as a whole.

## Approach / solution outline

- Update existing spec `default:_global/testing` (do not create a new spec).
- Strengthen requirements/constraints (and matching `verify.md` scenarios) so tests are required to work on POSIX and Windows.
- Likely broaden or complement the existing "Fixtures are valid on Windows" requirement into a clearer POSIX+Windows / cross-platform testing requirement covering path construction, separators, permissions, shell assumptions, etc. Exact wording deferred to `/specd-design`.
- Spec-only change expected unless design discovers concrete test code that must be fixed to comply; user asked specifically for the testing _spec_ clarification.

## Affected areas

- Spec: `default:_global/testing` (workspace `default`, owned)
- Related context (not in change scope unless design expands): `default:_global/continuous-integration` (already requires multi-OS CI)
- Spec deps already on testing: `default:_global/architecture`, `default:_global/conventions`
- Code: none attached yet; packages run Vitest tests under `test/**/*.spec.ts`

## Spec IDs

- In change: `default:_global/testing`
- Mentioned as related context: `default:_global/continuous-integration`
- Existing deps (already registered on the testing spec): `default:_global/architecture`, `default:_global/conventions`

## Design decisions / agreements

- Create a **new** change (user chose "algo nuevo" over continuing existing changes).
- Name: `posix-windows-test-compatibility`
- Description: Require tests to be POSIX- and Windows-compatible in the global testing conventions
- Initial specs: only `default:_global/testing`
- User confirmed creation with "si"
- Language: artifacts/code/docs in English (project rule)
- Goal is to make compatibility requirement **clear** in the testing spec (POSIX + Windows)

## Existing spec snapshot (anchors)

Current `default:_global/testing` already has:

- Requirement: "Fixtures are valid on Windows"
  - MUST use `os.tmpdir()`, build file URLs from local path
  - MUST NOT hardcode `/tmp`, `/var/www`, `file:///tmp/...`
  - MUST NOT use `chmod 0o000` as unreadability on Windows; produce unreadable file another way or skip where permission bit has no effect
- Constraints do **not** yet include a general "tests must pass on POSIX and Windows" rule
- verify.md has scenarios for OS tmp paths and chmod-on-Windows only

Gap to close: elevate from fixture-specific Windows rules to an explicit POSIX+Windows compatibility requirement for tests generally.

## Rejected alternatives

- Continuing an unrelated active change — none matched this intent (`implementation-snapshot`, `code-graph-symbol-semantic-context`, `report-overlap-dependency-changes`, `language-agnostic-member-symbol-references`).
- Restoring drafts — not relevant (`lift-validate-all-batch-to-core`, `graph-change-context`).
- Creating a separate new spec — not proposed; updating the existing testing conventions spec is the right place.

## Open questions for `/specd-design`

- Exact requirement wording: broaden "Fixtures are valid on Windows" vs add a new "Tests are POSIX- and Windows-compatible" requirement?
- Scope of concrete rules: path separators (`path.join` / `path.sep`), drive letters, case sensitivity, line endings (`\r\n`), shell/`sh` assumptions, Unix-only APIs (`chmod`, symlinks), absolute path string assertions?
- Should `verify.md` add scenarios that mirror CI matrix (macOS/Linux/Windows) or keep platform-agnostic WHEN/THEN?
- Include `default:_global/continuous-integration` as context-only or as no-op / dependency for design narrative?
- Any code/test fixes in this change, or spec-only?

## Key codebase / project observations

- Project: TypeScript pnpm monorepo; Vitest only; tests in `test/` mirroring `src/`.
- Graph freshness at exploration: generally fresh; one search returned `CONTENT_KNOWN_STALE` warning — re-index if design needs deep code search.
- CI already mandates identical checks on three OS runners; this change aligns the _testing conventions_ language with that reality.
- Workspace `default` is `owned` — safe to modify `default:_global/testing`.

## User preferences

- Spanish conversation OK for routing; artifacts/specs remain English.
- Narrow intent: modify testing spec for POSIX + Windows compatibility clarity.
- Preferred new change over piggybacking on existing work.

## Tangential topics

- Existing Windows fixture rules stay relevant; they should be preserved or folded into the broader requirement, not deleted without replacement.
- Unrelated active design work elsewhere in the repo should not be mixed into this change.

## Conversation flow

1. User: need to modify testing spec to state tests must be POSIX- and Windows-compatible.
2. `/specd` showed project state; no existing change matched; user chose "algo nuevo".
3. `/specd-new` investigated `default:_global/testing` and related CI/Windows mentions; proposed name/description/specs.
4. User confirmed "si" → change created.

## Change location

- changePath: `/Users/monki/Documents/Proyectos/specd/specd-sdd/changes/20260925-213024-posix-windows-test-compatibility`
- state after create: `drafting`
