Generated: 2026-06-30

# Exploration: align-studio-specs-post-merge

## Problem

Track B merged (`0cc90abb`, `main` @ `fed3b7e4`). `packages/api` and `apps/specd-studio-desktop` bootstrap via `createSdkContext` from `@specd/sdk`. Studio specs still describe `createKernel` direct wiring and `@specd/core` in delivery layer prose.

## Approach

Spec-only alignment (wave 1): composition, lifecycle, sample handlers. Deltas document implemented behaviour. No code changes unless verify finds drift.

## Code anchors

- `packages/api/src/composition/create-api-server.ts` — `createSdkContext`
- `packages/api/src/composition/create-api-context.ts` — `ApiServerState extends SdkHostContext`
- `apps/specd-studio-desktop/src/main/ipc-handlers.ts` — `createSdkContext`

## Out of scope (wave 2+)

Remaining `api:*` handlers/presenters, other `studio-desktop:*` boilerplate, `client:*` boundary specs.
