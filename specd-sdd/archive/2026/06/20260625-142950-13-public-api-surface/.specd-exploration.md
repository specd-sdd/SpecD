Generated: 2026-06-25

# 13-public-api-surface — exploration

## Source

`core-refactor-on-main.md` phase **A3**.

## Problem statement

`@specd/core` currently `export *` includes adapters, ports, domain internals — contradicts `default:_global/architecture`. After refactor, curate public barrels.

## Approach

### @specd/core public

Export: `createKernel`, `createConfigLoader`, `createConfigWriter`, domain use-case factories, `SpecdConfig`, `*Input`/`*Result`, `SpecdError`, `CORE_VERSION`

Do **not** export: ports, adapters (`FsConfigWriter`, `GitVcsAdapter`), composition internals, use case classes, removed APIs (`createInitProject`, `createAddPlugin`, `createRemovePlugin`, `kernel.project.init|addPlugin|removePlugin`)

**Retire (no code, plugin-era obsolete):** `RecordSkillInstall`, `GetSkillsManifest` specs + any `createRecordSkillInstall` / `createGetSkillsManifest` docs — skills tracking is `plugins.*` via `createConfigWriter().addPlugin`, not a separate manifest use case.

Implementation: `packages/core/src/public.ts` + package.json exports `"."` → public, `"./internal"` → full barrel

### @specd/code-graph public

Export: provider factory, G1 use cases, operation types, errors
Do not export: indexer internals, store adapters

### @specd/sdk

Re-export only public barrels + `orchestration/` — no `export *` of internals

### Migration

Grep monorepo for host imports of adapters/ports from `@specd/core`; fix before/archive with A2b complete

## Specs attached

- `default:_global/architecture`, `core:composition`, `code-graph:composition`

## Sequencing

- **Last** — after `12-cli-mcp-sdk-migration` when hosts no longer need internal imports
- Overlap with multiple drafting changes on composition specs
- **After** `09-core-approval-gates-baked` — public kernel surface documents `changes.approveSpec` / `changes.approveSignoff`; no `specs.approve*` paths

## Prerequisite from 09-core-approval-gates-baked

When curating `@specd/core` public barrel and `core:kernel` docs:

- Export path convention: approval gates are **`kernel.changes.approveSpec`** / **`kernel.changes.approveSignoff`**
- Remove or avoid documenting deprecated `kernel.specs.approve*` paths
- Overlap on `core:kernel`, `core:composition` with this change — reconcile deltas after 09 archives

## Acceptance

Zero host imports of `@specd/core` infrastructure from `packages/cli`, `packages/mcp`, `apps/`

## Conversation context

User requested full refactor split per `core-refactor-on-main.md` on `main` branch before Studio feature merge
