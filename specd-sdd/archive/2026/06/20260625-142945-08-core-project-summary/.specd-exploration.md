Generated: 2026-06-25

# 08-core-project-summary — exploration

## Source

`core-refactor-on-main.md` phase **P2**.

## Problem statement

`project status` CLI manually parallelizes `list*` calls and spec counts. Need `GetProjectSummary` use case without graph concerns.

## Approach

New use case `GetProjectSummary`:

```ts
interface GetProjectSummaryResult {
  activeCount: number
  draftCount: number
  discardedCount: number
  archivedCount: number
  specsByWorkspace: Record<string, number>
  workspaceCount: number
}
```

- Wire in kernel `kernel.project.getProjectSummary` (or similar)
- Factory `createGetProjectSummary(config)`
- CLI `project/status.ts`: use summary for counts; graph part deferred to SDK (`buildProjectStatusSnapshot` in `11-sdk-host-facade` + `12-cli-mcp-sdk-migration`)

## Specs attached

- `cli:project-status`
- **New spec needed:** `core:get-project-summary`

## Sequencing

- After P0–P1 wave; parallel with G1
- Required before `11-sdk-host-facade` `buildProjectStatusSnapshot` is complete
- Overlap: `cli:project-status` also in `12-cli-mcp-sdk-migration` — archive this first

## Out of scope

- Graph health slice — `10-code-graph-host-use-cases` + SDK
