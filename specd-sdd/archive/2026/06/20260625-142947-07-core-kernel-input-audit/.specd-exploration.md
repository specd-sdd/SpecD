Generated: 2026-06-25

# 07-core-kernel-input-audit — exploration

## Source

`core-refactor-on-main.md` phase **P1d**.

## Problem statement

After P1a, P1c, P1e, and P3, verify no kernel use case `*Input` still asks for values derivable from `SpecdConfig` baked at construction.

## Approach

1. Re-audit all kernel use case inputs against rules in doc § "Regla general (cualquier use case)"
2. Document exceptions with explicit rationale
3. Fix any remaining violations (may be no-op if prior changes complete)
4. Update `default:_global/architecture` if kernel contract needs clarification

## Known violations (pre-audit, from doc)

| Use case / surface         | Issue                                         | Fixed in                     |
| -------------------------- | --------------------------------------------- | ---------------------------- |
| CompileContext             | `config` in execute                           | P1a                          |
| GetProjectContext          | `config` in execute                           | P1a                          |
| ListPlugins                | kernel + disk re-read                         | P1c                          |
| Config mutation on kernel  | `project.init` / `addPlugin` / `removePlugin` | P1e → `createConfigWriter()` |
| TransitionChange           | approvals in execute                          | P3                           |
| ApproveSpec/ApproveSignoff | approvals in execute                          | P3                           |
| GetStatus                  | —                                             | already conforme             |

**Stale (no implementation):** `RecordSkillInstall`, `GetSkillsManifest` — specs/deps linger from pre-plugin-era; terminal refactor should **retire specs**, not audit as kernel use cases. Plugin declarations + `createConfigWriter().addPlugin` replaced skills manifest.

## Specs attached

- `core:kernel`, `default:_global/architecture`

## Sequencing

- **After** `03-core-host-orchestration-context`, `05-core-config-list-plugins`, `06-core-config-editing-boundary`, and ideally `09-core-approval-gates-baked`
- Gate before P2 if audit finds blockers

## Deliverable

Audit report in design.md + spec deltas only if gaps found
