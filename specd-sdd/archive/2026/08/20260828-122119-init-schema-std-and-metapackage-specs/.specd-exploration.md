# Exploration Context: init-schema-std-and-metapackage-specs

Generated: 2026-08-28

## Problem Statement

The `@specd/schema-std` and `@specd/specd` (metapackage) workspaces currently have 0 specs in `specs/schema-std` and `specs/specd`. They represent key packages in the SpecD monorepo:

1. `schema-std`: provides the standard schema YAML definition and markdown artifact templates used by SpecD core and CLI workflows.
2. `specd`: acts as the ecosystem metapackage (umbrella package) aggregating all core packages, tools, and plugins under a single entry point, along with versioning and changeset management.

This change initializes formal specification suites for both workspaces to satisfy the repository's spec-driven development requirements.

## Scope and Affected Areas

- **Workspaces:**
  - `schema-std` (`packages/schema-std`, specs in `specs/schema-std`)
  - `specd-metapackage` (`packages/specd`, specs in `specs/specd`)
- **Key Code & Asset Anchors:**
  - `packages/schema-std/schema.yaml`: canonical schema specification (artifacts, workflows, steps, transitions, rules, hooks)
  - `packages/schema-std/templates/`: template files (`proposal.md`, `design.md`, `tasks.md`, `spec.md`, `verify.md`)
  - `packages/specd/package.json`: metapackage dependencies and package publishing config
  - `dev/scripts/`: preflight checks, prepublish checks, plugin manifest sync, changeset hooks
  - `package.json`: root changeset scripts (`changeset`, `release:version`, `release:tag`, `release:publish`)

## Specs in Scope

1. `schema-std:standard-schema`: Defines standard schema structure, valid configurations, workflow steps, transitions, rules, hooks, and packaging.
2. `schema-std:templates`: Defines standard artifact template contracts, structure, placeholder standards, and consistency rules across change/spec scopes.
3. `specd-metapackage:metapackage`: Defines purpose of the metapackage umbrella distribution, dependency aggregation across official `@specd/*` modules, version synchronization via Changesets, and integration with `dev/scripts/` hooks and validation checks.

## Spec Dependencies

- `schema-std:templates` depends on `schema-std:standard-schema`.

## Key Design Decisions & Agreements

- Keep the metapackage spec concise and focused on its role, dependency bundling, versioning, and changeset release workflows driven by `dev/scripts/`.
- For `schema-std`, explicitly capture what it contains (`schema.yaml`, `templates/`) and the specific structural and semantic requirements each template and schema section must satisfy.

## Conversation Flow

- User identified that `schema-std` and `specd` packages lacked specs and requested a change to initialize specs for both.
- Agent analyzed workspace structure (`packages/schema-std`, `packages/specd`, `dev/scripts/`), verified 0 existing specs, and formulated proposed change name and spec IDs.
- User confirmed the proposed change.
- Agent created change `init-schema-std-and-metapackage-specs` and registered dependencies.

## Next Steps

- Run `/specd-design init-schema-std-and-metapackage-specs` to draft proposal, design, tasks, and initial spec artifacts.
