# Exploration Context

Generated: 2026-05-05

## Problem statement

The user reported that `graph impact` returns fewer results than text search (`grep`/`rg`) for `ArtifactParser`, and specifically highlighted that `generate-spec-metadata` appears to use parser-related types but is absent from impact results.

Goal: analyze and fix the gap in code-graph impact analysis so semantically relevant files are surfaced when symbols are referenced through type aliases/registry indirection.

## Approach / solution outline

- Reproduce mismatch between:
  - `node packages/cli/dist/index.js graph impact --symbol "ArtifactParser"`
  - `rg -n "ArtifactParser"`
- Validate concrete missing case in `packages/core/src/application/use-cases/generate-spec-metadata.ts`.
- Investigate code-graph symbol/reference extraction and impact traversal for type-only references and alias expansion (`ArtifactParserRegistry = ReadonlyMap<string, ArtifactParser>`).
- Add/adjust behavior in code-graph so impact traversal includes semantically linked dependents when type alias indirection is involved.
- Add regression tests.

## Affected areas discussed

- Package: `packages/code-graph`
- Potential modules likely involved:
  - symbol extraction/indexing
  - dependency edge construction
  - traversal logic used by impact analysis
- Repro target in consumer code:
  - `packages/core/src/application/use-cases/generate-spec-metadata.ts`
  - `packages/core/src/application/ports/artifact-parser.ts`

## Spec IDs

Change created with initial specs:

- `code-graph:traversal`
- `code-graph:symbol-model`

Additional possibly relevant spec to consider during design/implementation:

- `code-graph:indexer`

## Key observations from reproduction

Observed in this workspace during exploration:

1. Graph impact result for `ArtifactParser`:

- `directDependents=12`, `indirectDependents=8`, `affectedFiles=20`.

2. Text search result for `ArtifactParser`:

- `214` textual matches across `62` files.
- Distribution from `rg -l`:
  - `packages`: 34 files
  - `specs`: 21 files
  - `docs`: 7 files

3. Missing example:

- `packages/core/src/application/use-cases/generate-spec-metadata.ts` contains `ArtifactParserRegistry` imports/usages and runtime parser retrieval (`this._parsers.get(format)`), but does not appear in `graph impact --symbol ArtifactParser` output.

4. Even `graph impact --symbol "ArtifactParserRegistry"` did not include `generate-spec-metadata.ts` in affected files (during this exploration run), suggesting potential missing edge modeling for this symbol relation.

5. `graph search --symbols` for `GenerateSpecMetadata` returns symbols from that file, so the file is indexed; the gap appears to be in relationship extraction/traversal, not file discovery.

## Agreements reached

- User requested creating a dedicated change to analyze and find/fix the error.
- Change should focus on impact analysis behavior rather than textual search behavior.

## Rejected alternatives

- No rejected architectural alternatives yet. Only initial diagnosis completed.

## Open questions

- Is omission of `generate-spec-metadata.ts` intended by current impact semantics, or a bug?
- Should impact semantics include type-only alias expansions by default, or behind an option?
- Are there existing constraints in `code-graph:traversal` or `code-graph:symbol-model` that intentionally exclude this relation?
- Is the issue in index-time edge creation, query-time traversal filters, or symbol disambiguation logic?

## User preferences

- User prefers practical, concrete analysis from real command outputs.
- User explicitly asked to create a change for this problem.

## Tangential topics discussed

- Difference between semantic impact and textual search scope.
- Potential index freshness considerations.

## Conversation flow summary

1. User asked why `graph impact` can return fewer results than grep.
2. We ran both commands and compared concrete counts.
3. User pointed to `generate-spec-metadata` as a missing impact case.
4. We verified the file contains `ArtifactParserRegistry` usage and is indexed, yet absent in impact outputs for related symbols.
5. User requested creating a change to analyze and find the error.
6. This change was created with code-graph specs in scope.
