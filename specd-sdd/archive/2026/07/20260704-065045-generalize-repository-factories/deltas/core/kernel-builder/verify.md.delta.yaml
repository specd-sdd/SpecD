- op: added
  position:
    parent:
      type: section
      matches: 'Requirement: Builder accumulates additive kernel registrations'
    after:
      type: section
      matches: 'Scenario: Registrations accumulate before build'
  content: |
    #### Scenario: Repository overrides in builder

    - **GIVEN** a mock `ChangeRepository` set on the builder
    - **WHEN** `build()` is called
    - **THEN** the returned kernel reuses that mock instance
