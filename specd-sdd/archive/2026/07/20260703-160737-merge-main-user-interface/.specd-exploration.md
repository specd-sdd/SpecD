Generated: 2026-07-03

# Problem Statement

User wants to merge local `main` into branch `feat/user-interface` and first understand:

- what is new on `main`
- what needs to be merged
- what conflicts are likely
- what resolution strategy should be used

Later, user approved creating a specd change to track this merge-oriented work.

# Approach / Solution Outline

- Use `specd` discovery flow, not ad hoc repo edits.
- Use graph-first and project context first.
- Compare current branch against local `main`.
- Separate bulk incoming changes from actual overlap/conflict surface.
- Ignore generated metadata conflicts for manual analysis because user explicitly said they can be regenerated later with `generate-metadata --write`.
- Focus manual resolution on functional code/spec wiring in `core`, `sdk`, and `cli`.

# Affected Areas

## Branch / Git

- Branch under analysis: `feat/user-interface`
- Merge target/source of interest: local `main`
- `git status --short --branch` showed branch ahead of `origin/feat/user-interface` by 1 commit
- Untracked local path observed during analysis: `specd-sdd/changes/`

## Mainline commits not in branch

Two commits from `main` not present in branch at time of analysis:

- `5ed2b5f7` — `fix(all): align project status repository wiring`
- `5756f9d5` — `feat(all): canonicalize spec dependency metadata`

## Branch-side commits not in main

Many branch-only commits exist, mostly Studio/API/UI/SDK work already on `feat/user-interface`. This matters because the branch is large, but the actual new merge pressure from `main` is narrow and concentrated.

## Functional overlap of interest

Incoming `main` changes touch:

- `packages/core/src/application/ports/spec-repository.ts`
- `packages/core/src/application/use-cases/_shared/depends-on-traversal.ts`
- `packages/core/src/application/use-cases/compile-context.ts`
- `packages/core/src/application/use-cases/generate-spec-metadata.ts`
- `packages/core/src/application/use-cases/get-project-context.ts`
- `packages/core/src/application/use-cases/get-spec-context.ts`
- `packages/core/src/application/use-cases/invalidate-spec-metadata.ts`
- `packages/core/src/application/use-cases/list-specs.ts`
- `packages/core/src/application/use-cases/validate-specs.ts`
- `packages/core/src/composition/kernel.ts`
- `packages/core/src/composition/shared-repository-wiring.ts`
- `packages/core/src/composition/use-cases/get-spec-context.ts`
- `packages/core/src/composition/use-cases/get-status.ts`
- `packages/core/src/composition/use-cases/list-changes.ts`
- `packages/core/src/composition/use-cases/list-discarded.ts`
- `packages/core/src/composition/use-cases/list-drafts.ts`
- `packages/core/src/composition/use-cases/list-workspaces.ts`
- `packages/core/src/composition/use-cases/validate-specs.ts`
- `packages/core/src/domain/services/parse-metadata.ts`
- `packages/core/src/infrastructure/fs/spec-repository.ts`
- `packages/sdk/src/orchestration/build-project-status-snapshot.ts`
- `packages/cli/test/commands/project/status.spec.ts`
- corresponding specs under `specs/core/*`, `specs/sdk/*`, `specs/cli/*`

## Actual overlap discovered between branch and incoming main

Computed overlap list during analysis:

- `.specd/metadata/core/edit-change/metadata.json`
- `.specd/metadata/default/_global/architecture/metadata.json`
- `.specd/metadata/sdk/composition/metadata.json`
- `packages/core/src/composition/kernel.ts`
- `packages/core/test/application/use-cases/helpers.ts`
- `packages/core/test/infrastructure/fs/change-repository.spec.ts`

After user guidance, metadata overlaps are to be ignored for manual conflict reasoning.

# Spec IDs

Change created with these specs:

- `core:compile-context`
- `core:edit-change`
- `core:get-project-context`
- `core:get-project-summary`
- `core:get-spec-context`
- `core:get-status`
- `core:list-changes`
- `core:list-discarded`
- `core:list-drafts`
- `core:list-workspaces`
- `core:spec-repository-port`
- `core:validate-specs`
- `sdk:build-project-status-snapshot`
- `cli:project-status`

Additional relevant specs surfaced by incoming `main` work:

- `core:create-change`
- `core:generate-metadata`
- `core:save-spec-metadata`
- `core:spec-lock`
- `core:spec-metadata`

# Design Decisions

- Treat this as merge-analysis and merge-preparation work, not direct implementation yet.
- Ignore generated metadata conflicts during human analysis.
- Plan to regenerate metadata afterward with `generate-metadata --write`.
- Use `main` semantics as source of truth where incoming commits redefine canonical persisted dependency and project-status wiring contracts.
- For functional merge conflicts, preserve branch-specific behavior where still needed and layer in `main`’s new wiring rather than discarding either side blindly.

# Agreements Reached

- User confirmed metadata conflicts can be ignored.
- User explicitly said: `los metadatas puedes ignorarlos, los regeneramos con generate-metadata --write, nos centramos en lo demás`
- User approved creating the change after proposal.

# Steps / Plan Defined

Chronological work plan agreed during discussion:

1. Inspect what is new on local `main`.
2. Identify what must be merged into `feat/user-interface`.
3. Analyze possible conflicts.
4. Decide likely resolution approach before performing merge work.
5. Ignore metadata-only conflicts.
6. Focus on functional conflicts in `core`/`sdk`/`cli`, especially composition and context/status flows.
7. Regenerate metadata after merge resolution.

# Rejected Alternatives

- Deep manual review of metadata diffs was effectively rejected after user clarified they should be ignored and regenerated later.
- Treating the huge Studio/API/UI delta as the main merge risk was discarded after branch-vs-main comparison showed the new mainline delta is much narrower and concentrated in `core`/`project status` work.

# Open Questions

- Whether local untracked `specd-sdd/changes/` content should be kept, moved, or stashed before actual merge execution.
- Whether any spec scope should be expanded later to include `core:create-change`, `core:generate-metadata`, `core:save-spec-metadata`, `core:spec-lock`, and `core:spec-metadata` if implementation goes beyond the currently scoped review.
- Whether merge execution itself will be performed inside this change or only analyzed here and merged manually afterward.

# Key Codebase Observations

## Project / graph state

- `project status --format toon --graph` reported:
  - `active: 1`
  - `drafts: 2`
  - graph `stale: false`
  - graph freshness `2026-07-02T06:23:49.845Z`

## Merge size

- `git rev-list --count HEAD..main` returned `2`
- `git rev-list --count main..HEAD` returned `349`
- `git diff --name-only HEAD...main | wc -l` returned `185`

Interpretation:

- only two new `main` commits need to be integrated
- branch is far ahead in total work
- merge is conceptually narrow but sits on high-coupling files

## Conflict-bearing file

The only clearly functional content conflict from `merge-tree` is:

- `packages/core/src/composition/kernel.ts`

Observed conflict shape in `kernel.ts`:

- branch side still wires `validateBatch: new ValidateChangeBatch(...)`
- incoming `main` adds expanded wiring for:
  - `new ValidateSpecs(i.specs, schemaProvider, i.parsers, i.hasher, i.registry.extractorTransforms, workspaceRoutes)`
  - `new GetSpecContext(listWorkspaces, i.hasher, schemaProvider, i.parsers, i.registry.extractorTransforms, workspaceRoutes)`

Likely intended resolution:

- keep branch `validateBatch`
- also keep/accept `main`’s expanded constructor arguments for `ValidateSpecs` and `GetSpecContext`

## Non-conflicting but overlapping tests

Even if not marked as hard conflicts, these should be reviewed after merge:

- `packages/core/test/application/use-cases/helpers.ts`
- `packages/core/test/infrastructure/fs/change-repository.spec.ts`

## Graph impact / risk

Graph impact results captured during analysis:

- `core:src/composition/kernel.ts`
  - risk level: `CRITICAL`
  - extremely high dependent surface across core use cases and composition
- `core:src/application/use-cases/get-project-context.ts`
  - risk level: `CRITICAL`
  - large dependency chain across context compilation and spec traversal
- `sdk:src/orchestration/build-project-status-snapshot.ts`
  - risk level: `HIGH`
  - affects CLI project status, API handler-project, and desktop IPC handlers

Practical implication:

- merge verification must prioritize kernel wiring, project/spec context, and project status orchestration

# User Preferences

- Wants analysis before action.
- Wants local `main` merged into current branch.
- Wants likely conflict resolution thought through up front.
- Explicitly prefers to ignore metadata conflicts and regenerate them later.

# Tangential Topics

- During specd context loading, optimized project/spec context was stale or missing for some global specs, requiring metadata refresh logic to proceed with specd workflow.
- This was operational overhead, not core user intent.
- Generated local metadata changes from that refresh were reverted afterward to avoid contaminating the repository state.

# Conversation Flow

1. User invoked `specd-new` and asked to analyze merging local `main` into current branch, including new changes, conflicts, and planned resolution.
2. Shared specd context, project status, and graph status were loaded first.
3. Project context reported stale optimized context warnings; optimizer instructions were read and metadata refresh attempted.
4. Refresh hit permissions around `.specd/config/log/specd.log`; elevated execution was used to complete required metadata updates.
5. Global specs and project context were loaded, then git branch comparisons and diffs against `main` were analyzed.
6. Analysis showed only two new `main` commits, both focused on canonical spec dependency metadata and project-status repository wiring.
7. Bulk branch work was identified as pre-existing Studio/API/UI work, not the new merge payload from `main`.
8. Overlap and `merge-tree` analysis narrowed the meaningful functional conflict surface to `packages/core/src/composition/kernel.ts` plus some review-worthy tests; metadata conflicts were also identified.
9. User clarified metadata can be ignored and regenerated later with `generate-metadata --write`.
10. Based on that clarification, a focused change proposal was made around merge-functional integration only.
11. User approved creation of the change.
