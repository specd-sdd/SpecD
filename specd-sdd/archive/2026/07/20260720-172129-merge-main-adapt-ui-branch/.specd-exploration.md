# Exploration: merge-main-adapt-ui-branch

Generated: 2026-07-20

## Problem statement

`feat/user-interface` must absorb canonical `main` without losing Studio/UI contracts from this branch or the composition/SDK/graph contracts from main. After merge + conflict resolution, adapt this branch to main’s newer seams. Separately (as part of the same change’s later design/implement work), introduce Electron SQLite graph storage via an injectable database-module factory — **not** by forking code-graph composition again.

## Approach / solution outline

1. **Merge first:** `main` → `feat/user-interface` (main is canonical for shared core/graph/SDK).
2. **Resolve conflicts** preserving both sides’ contracts (no silent drops of Studio behaviour or main’s new graph/SDK semantics).
3. **Post-merge adaptation:** re-align this branch’s Studio/desktop/API wiring to main’s new composition seams; inventory new/changed specs from main and add them to this change (or related active changes) with correct artifacts/deltas.
4. **Electron SQLite:** new workspace package (candidate name `@specd/code-graph-sqlite-electron`) exposing backend id `sqlite-electron` via `createSqliteGraphStoreFactory({ loadDatabaseModule })` loading an Electron-vendored `better-sqlite3`. Wire desktop through SDK `graph` options (`graphStoreId` + `graphStoreFactories`).
5. **Leave `@specd/code-graph-electron` alone:** do not delete, do not use as composition fork for this work; keep it for later evaluation.

## Merge analysis (discovery anchors)

- Branch: `feat/user-interface`
- Last historical merge commit noted: `eb0857bf` (2026-07-06, `chore(all): merge main into feat-user-interface`)
- Merge-base with local `main` at discovery: `aa9c8ae3`
- Ahead/behind (vs `origin/main` when fetch failed on SSH): ~352 ahead / ~4 behind; local `main` had additional tip commits beyond that (~9 commits after merge-base)
- Main themes since merge-base (must land and adapt to):
  - SDK graph provider factory archive: `withOpenGraphProvider`, busy/stale health, provider lifecycle hardening
  - **`createSqliteGraphStoreFactory` + injectable `loadDatabaseModule`** on `SQLiteGraphStore` (deferred load until `open()`)
  - SDK host start-dir bootstrap + warning contract
  - Spec preview diff generation moved into core + shown during validate
  - Live hook progress streaming
- Overlap / conflict-prone files (~44 both-touched): includes `packages/sdk/src/.../host-context`, `packages/code-graph/.../sqlite-graph-store.ts`, `packages/code-graph/src/composition/index.ts`, `packages/cli/.../change/validate.ts`, `specs/sdk/host-context/*`, metadata, etc.
- Active changes that overlap main’s work (watch during/after merge):
  - `move-diff-to-core` (main already moved preview diff into core)
  - `deprecate-ladybug-store` (ladybug/composition/SDK graph host surface)
  - Others exist but less central to this merge theme

## Electron / storage adapter decisions

### Confirmed

- **No Ladybug Electron factory in scope for now.** User noted for later: main has `createSqliteGraphStoreFactory` but **no** `createLadybugGraphStoreFactory`. Ladybug on main still hardcodes `loadLbugModule = async () => import('lbug')` with no public factory / constructor injection. Specs on main only export `createSqliteGraphStoreFactory` in composition package exports.
- **Do not mash `@specd/code-graph-electron`.** Keep package; stop treating it as the path forward for this work; do not delete.
- **New package** for Electron SQLite store adapter (preferred candidate: `@specd/code-graph-sqlite-electron`).
- Backend id: **`sqlite-electron`** (cannot overwrite builtin `sqlite` — registry rejects collisions via `GraphStoreRegistryError.alreadyRegistered`).
- Implementation reuse: same `SQLiteGraphStore` class from `@specd/code-graph`; only inject vendored Database module via `loadDatabaseModule`.
- Desktop wiring target: SDK host graph options, e.g. `createSdkHostContext(config, { graph: { graphStoreId: 'sqlite-electron', graphStoreFactories: { 'sqlite-electron': createElectronSqliteGraphStoreFactory() } } })` (exact API names after merge).
- Current desktop still imports `createCodeGraphProvider` from `@specd/code-graph-electron` (`apps/specd-studio-desktop/src/main/ipc-handlers.ts`) — that is the composition-fork path to move away from after merge/adapt.

### Rejected / deferred

- Revising `@specd/code-graph-electron` in place as the new adapter home — rejected; user wants a **new package** so the old one is not overwritten.
- Parallel full composition re-export package as the long-term Electron graph entry — rejected in favour of storage-factory injection.
- Ladybug Electron adapter — deferred (not needed yet).
- Spec IDs for this change — **explicitly deferred until after merge**, then add via `changes edit --add-spec` based on what actually needs adaptation.

## Spec IDs

- **Attached to change at creation:** none
- **Likely candidates after merge (not registered yet):**
  - New workspace spec under the new package (e.g. `code-graph-sqlite-electron:…`)
  - `code-graph:composition`, `code-graph:sqlite-graph-store` (seam from main; may be no-op or small delta)
  - `sdk:host-context`, possibly `sdk:with-open-graph-provider` / `sdk:composition`
  - `studio-desktop:main-kernel-lifecycle` (or whatever owns local graph provider wiring)
  - Possibly revise/retire usage expectations of `code-graph-electron:composition` without deleting the package

## Key codebase observations

### This branch (`feat/user-interface`)

- `packages/code-graph-electron/src/index.ts` re-exports `../../code-graph/src/index.js` plus vendored sqlite runtime — composition fork.
- Vendored loader: `packages/code-graph-electron/src/runtime/vendored-better-sqlite3.ts` (`createRequire` → `vendor/better-sqlite3/lib/index.js`).
- Builtin factories in branch still inline `new SQLiteGraphStore(storagePath)` / `new LadybugGraphStore(storagePath)` in `create-code-graph-provider.ts` — **no** injectable sqlite factory yet (that lands from main).
- Workspace already registered: `code-graph-electron` (owned), with spec `code-graph-electron:composition`.
- GraphStoreFactory impact is CRITICAL (wide dependents) — treat registry/factory changes carefully after merge.

### Main (canonical seams to adopt)

- `packages/code-graph/src/composition/create-sqlite-graph-store-factory.ts`
  - `SqliteDatabaseModule` with `default` constructor
  - `createSqliteGraphStoreFactory({ loadDatabaseModule? })`
- `SQLiteGraphStore` constructor accepts `loadDatabaseModule`; default `import('better-sqlite3')`; used in `open()`.
- Public barrel exports `createSqliteGraphStoreFactory`.
- Composition options renamed toward `CodeGraphCompositionOptions` on main (branch still has `CodeGraphFactoryOptions` naming in places — expect rename conflicts).
- Ladybug: internal `loadLbugModule` field only; no equivalent public factory.

### Intended Electron factory shape (post-merge)

```ts
import { createSqliteGraphStoreFactory } from '@specd/code-graph'
// vendored Database default from new package runtime
export function createElectronSqliteGraphStoreFactory() {
  return createSqliteGraphStoreFactory({
    loadDatabaseModule: async () => ({ default: VendoredDatabase }),
  })
}
```

New package owns: vendor sync/rebuild scripts (can start from code-graph-electron patterns without modifying that package), runtime loader, factory export. Does **not** re-export full code-graph composition.

## Agreements reached

- Main is canonical for the merge.
- Specs for this change are chosen **after** merge, based on real adaptation needs.
- Preserve contracts from both branches.
- New package for sqlite-electron; leave code-graph-electron untouched for now.
- Ladybug Electron out of scope for now (remembered).
- This change covers merge + resolve + adapt + new sqlite-electron package work; artifact writing is `/specd-design`, not this skill.

## Steps / plan defined

1. Create change with **no initial specs** ✓
2. Later (user-driven): perform git merge of `main`, resolve conflicts.
3. Inventory new/changed specs and APIs from main; add specs to this change; write design/tasks.
4. Adapt Studio/desktop/SDK wiring.
5. Add new package + `sqlite-electron` factory; wire desktop; leave old electron package unused.

## Open questions

- Final npm/workspace name: candidate `@specd/code-graph-sqlite-electron` (user leaned this way; confirm at design time).
- Whether vendor tree is copied/adapted from `code-graph-electron` scripts or newly authored in the new package (prefer copy patterns, not mutate old package).
- How much of `code-graph-electron:composition` needs a future deprecation/delta vs simply stopping consumption.
- Coordination with active changes `move-diff-to-core` and `deprecate-ladybug-store` after merge (overlap risk).
- Local `main` vs `origin/main` divergence: fetch failed via SSH during discovery; re-fetch before merge.

## User preferences

- Do not rush spec selection before merge.
- Do not delete or repurpose `@specd/code-graph-electron` for this.
- Prefer new package over overwriting the old one.
- Ladybug Electron later, not now.
- Full merge + adaptation is in scope, not “only the electron adapter”.

## Tangential topics

- Exploration doc `EXPLORATION_STORAGE_ABSTRACTION.md` on main was about Issue #10 change-artifact checkout/sync storage — **different** problem from Electron graph SQLite; deleted on main tip; not the driver for this change.
- Active Studio polling change `harden-studio-project-poll` unrelated but coexists on branch.
- Graph-first / hexagonal architecture constraints remain binding (`default:_global/architecture`).

## Conversation flow

1. User started `/specd-new` wanting merge analysis + Electron storage adapters instead of code-graph-electron composition path.
2. Discovery: merge delta, overlap files, existing electron package, main’s injectable sqlite factory, missing ladybug factory.
3. User asked if ladybug has equivalent factory → answered no; deferred.
4. User confirmed injectable `loadDatabaseModule` approach; leave code-graph-electron unused/undeleted; figure sqlite-electron with vendored module.
5. User asked for a **new package** so the old one is not mashed.
6. User clarified scope: merge, resolve, adapt branch to main, handle new specs/artifacts post-merge, preserve both contracts — not only the electron package.
7. User confirmed create change with **no initial specs**; specs after merge.

## Suggested next skill

`/specd-design merge-main-adapt-ui-branch` — but only after (or as part of design prep with explicit user go-ahead to) merge main and inventory adaptation targets. Do not write artifacts in `/specd-new`.
