Generated: 2026-07-03

# Exploration Context

## Problem statement

The user wants a new change focused on the main `project status` bug and on other routes that may fail for the same reason: incomplete repository wiring outside the kernel. The specific persistence/write-on-read problem must not be handled in this change because it will be addressed separately in `fix-change-repository-write-on-read`.

The immediate motivation came from reviewing the existing change `fix-project-status-artifact-drift`, whose artifacts were judged too broad and not sufficiently coherent for the concrete bug. The new change should isolate the main defect and fix similar breakage caused by the same wiring pattern.

## Approach / solution outline

- Create a new change instead of continuing `fix-project-status-artifact-drift`.
- Scope the change to the `project status` read path and directly related composition factories that build repositories differently from the kernel.
- Include nearby routes that show the same incomplete bootstrap pattern and may fail similarly.
- Explicitly exclude write-on-read persistence fixes.
- Do not use this new change for a repo-wide composition refactor.

## Affected areas

### Packages

- `@specd/core`
- `@specd/sdk`
- `@specd/cli`

### Key files observed

- `packages/core/src/composition/use-cases/get-project-summary.ts`
- `packages/core/src/composition/use-cases/list-workspaces.ts`
- `packages/core/src/composition/use-cases/list-changes.ts`
- `packages/core/src/composition/use-cases/list-drafts.ts`
- `packages/core/src/composition/use-cases/list-discarded.ts`
- `packages/core/src/composition/use-cases/get-status.ts`
- `packages/sdk/src/orchestration/build-project-status-snapshot.ts`
- `packages/cli/src/commands/project/status.ts`

### Key behavior observed

- `createGetProjectSummary` wires `createListChanges`, `createListDrafts`, `createListDiscarded`, `createListArchived`, and `createListWorkspaces`.
- `createListChanges`, `createListDrafts`, and `createListDiscarded` construct `ChangeRepository` from basic storage paths when called from config, instead of using kernel-equivalent wiring.
- `createGetStatus` also builds `ChangeRepository` directly and independently wires related dependencies.
- `createListWorkspaces` constructs `SpecRepository` directly with a simplified `metadataPath` pattern.
- `buildProjectStatusSnapshot` uses kernel use cases, but `project status` depends on the quality of the underlying core factories and orchestration.

## Spec IDs

### Attached to this change

- `core:get-project-summary`
- `core:list-workspaces`
- `core:list-changes`
- `core:list-drafts`
- `core:list-discarded`
- `core:get-status`
- `sdk:build-project-status-snapshot`
- `cli:project-status`

### Relevant context specs discussed

- `default:_global/architecture`
- `default:_global/conventions`
- `default:_global/docs`
- `default:_global/error-handling-conventions`
- `default:_global/eslint`
- `default:_global/logging`
- `default:_global/spec-layout`
- `default:_global/testing`

## Design decisions

- New change name chosen: `fix-project-status-repository-wiring`.
- Description chosen: align `project status` and directly affected read/composition factories with kernel repository wiring so incomplete bootstrap does not cause artifact drift or similar read-path failures, excluding write-on-read persistence fixes.
- The bug should be treated as a repository wiring/bootstrap issue, not as a broad composition cleanup.
- Similar routes may be included only when they fail through the same incomplete wiring pattern.
- The change should not become a repo-wide refactor of all composition factories.

## Agreements reached

- The old change `fix-project-status-artifact-drift` is too broad for the main bug and should not be used as the vehicle for this narrower fix.
- The new change should still fix other routes that are wrong or vulnerable to a similar bug because of missing kernel-equivalent wiring.
- The separate change `fix-change-repository-write-on-read` will handle write-on-read persistence concerns.
- The new change should be created first, then continue with design later.

## Steps / plan defined

1. Review the existing `fix-project-status-artifact-drift` artifacts.
2. Conclude that the existing scope is too broad and lacks tight verification of the observed bug.
3. Create a new change only for the main bug and sibling routes with the same wiring defect.
4. Exclude write-on-read from this new change.
5. Continue later with `/specd-design` on the new change.

## Rejected alternatives

- Continue using `fix-project-status-artifact-drift` as-is.
  - Rejected because the change sprawls across too many specs and composition paths, overlaps with archived work, and focuses too much on generic wiring/API deltas instead of the concrete observable bug.
- Expand to all composition factories immediately.
  - Rejected because that would recreate the same scope problem and blur the main defect with a general refactor.
- Include write-on-read persistence fixes here.
  - Rejected because the user explicitly wants that handled in `fix-change-repository-write-on-read`.

## Open questions

- Which additional sibling routes beyond the initial attached specs should remain in scope once design inspects the exact kernel/non-kernel divergence?
- Whether `list-archived` needs any change for consistency, even though the user-facing discussion centered on `project status`, `get-status`, and list factories backed by `ChangeRepository`/`SpecRepository`.

## Key codebase observations

- `createListChanges`, `createListDrafts`, and `createListDiscarded` each call `createChangeRepository('fs', context, options)` with only path-level options.
- `createGetStatus` builds `ChangeRepository`, `ArchiveRepository`, schema registry, and implementation tracking directly in its composition factory.
- `createListWorkspaces` creates spec repositories using `metadataPath: path.join(ws.specsPath, '..', '.specd', 'metadata')`.
- `createGetProjectSummary` is a thin orchestrator but inherits any mis-wiring from its constituent list factories.
- `buildProjectStatusSnapshot` is the SDK read path used by the CLI `project status` command.
- Graph analysis showed `createGetProjectSummary`, `createListWorkspaces`, and `createGetStatus` are high-risk/critical symbols with broad impact.
- `project status --format toon` reported the graph as stale after the working tree changed during this session, but this did not block change creation.

## User preferences

- Fix the main bug now.
- Also fix anything else that is actually wrong or presents a similar bug due to missing wiring.
- Do not pull write-on-read into this change.
- Avoid broad, unfocused changes.

## Tangential topics

- The previous change `fix-project-status-artifact-drift` remains active and overlaps with the new change on all initial specs.
- The overlap warning at creation time is expected:
  - `cli:project-status`
  - `core:get-project-summary`
  - `core:get-status`
  - `core:list-changes`
  - `core:list-discarded`
  - `core:list-drafts`
  - `core:list-workspaces`
  - `sdk:build-project-status-snapshot`

## Conversation flow

1. The user asked for an analysis of the artifacts of `fix-project-status-artifact-drift`.
2. The review concluded that the change diagnosis was good but the artifacts were too broad, too wiring-heavy, and missing direct verification of the observable bug.
3. The user decided to create a new change only for the main `project status` bug, while treating write-on-read separately in `fix-change-repository-write-on-read`.
4. The user invoked the `specd-new` skill and asked to create the new change.
5. An initial narrower proposal was made for `project status` and direct dependencies.
6. The user clarified that similar bugs caused by missing wiring should also be fixed now.
7. The scope was expanded only to sibling routes sharing the same incomplete repository bootstrap pattern.
8. The change `fix-project-status-repository-wiring` was created with the selected initial specs.
