Generated: 2026-07-13

# Exploration Context

## Problem statement

The user wants to merge `main` into `feat/user-interface` and prepare the branch so it does not diverge from `main`'s newer working conventions.

This is not only a conflict-resolution task. The branch must also be aligned to the newer canonical approaches that landed on `main` after the previous merge-from-main work already done in this branch.

The user explicitly wants:

- analysis of incoming changes from `main`
- analysis of likely merge conflicts against this branch
- identification of specs that must change to merge both code lines correctly
- review of branch-local implementations that should be adjusted to the newer conventions established on `main`
- adoption of the forms/patterns from `main` where they are now the better or canonical approach

## Scope agreed

The change scope agreed with the user is:

- merge post-2026-07-03 `main` changes into `feat/user-interface`
- align this branch with `main`'s current composition conventions
- align this branch with `main`'s current config-loading conventions
- align this branch with `main`'s current VCS-neutral conventions
- align this branch with `main`'s current SDK host-context conventions
- reduce workflow divergence between this branch and `main`

The goal is not only "make merge pass", but "make this branch converge toward `main`'s way of doing things".

## Proposed and confirmed change

- Name: `align-user-interface-with-main-conventions`
- Description: `Merge post-2026-07-03 mainline changes into feat/user-interface and align this branch with main's current composition, config-loading, VCS, and SDK conventions`

## Specs attached to the change

- `default:_global/architecture`
- `default:_global/conventions`
- `core:composition`
- `core:kernel`
- `core:config`
- `core:config-loader`
- `core:compile-context`
- `core:get-project-context`
- `core:get-spec-context`
- `core:get-status`
- `core:validate-specs`
- `sdk:composition`
- `sdk:host-context`

## Why these specs were chosen

- `default:_global/architecture`
  - `main` introduces composition-level refactors in `core` that affect architectural boundaries and composition ownership.
- `default:_global/conventions`
  - The user explicitly requested alignment with the newer conventions from `main`, not only code compatibility.
- `core:composition`
  - `main` refactors composition factories, composition resolver usage, and canonical create/resolve flows.
- `core:kernel`
  - `kernel.ts` is a central hotspot and a direct overlap point between `main` and this branch.
- `core:config`
  - `main` changes how config adapters and storage bindings are represented and surfaced.
- `core:config-loader`
  - `main` moves config discovery/loading toward VCS-neutral behavior and `createDefaultConfigLoader`.
- `core:compile-context`
  - this branch and `main` both touch project/spec context related behavior.
- `core:get-project-context`
  - high overlap and high blast radius.
- `core:get-spec-context`
  - high overlap and high blast radius.
- `core:get-status`
  - affected by composition/kernel/status snapshot changes.
- `core:validate-specs`
  - touched by both sides and `main` adds composition-resolution changes around dependency resolution.
- `sdk:composition`
  - overlap exists in SDK export surface and composition exposure.
- `sdk:host-context`
  - direct conflict area; `main` renames/standardizes config-loader usage and this propagates widely.

## Main findings from repository analysis

### Branch and base

- Current branch: `feat/user-interface`
- `main` and `origin/main` resolve to commit `aa9c8ae3a3435fb8638ab97ce8bc918d888f5807`
- merge-base between `main` and `HEAD`: `971da76885d65ba238cec79cc9986c24ba695b04`

### Project state at analysis time

- Active changes: 1
- Drafts: 2
- Graph was stale initially and was reindexed during this exploration
- All workspaces listed by `project status` are `owned`

### Diff volume

From merge-base to `main`:

- `749 files changed, 50557 insertions(+), 8176 deletions(-)`

From merge-base to this branch `HEAD`:

- `2115 files changed, 373670 insertions(+), 1416 deletions(-)`

Interpretation:

- `main` brings a moderate but deep set of changes into shared core areas
- this branch adds a very large amount of new API/client/ui/studio code
- the real merge risk is not the total size of this branch, but the overlapping shared core/sdk/cli/spec areas

## Important incoming mainline changes identified

Three post-previous-merge mainline changes were the main source of new overlap:

1. `decouple-composition-factories`
2. `generalize-repository-factories`
3. `eliminate-git-hardcoding`

These changes collectively move `main` toward:

- canonical `createX(deps)` plus config-based convenience entry points
- composition resolved through shared resolver helpers instead of duplicated inline wiring
- repository factory generalization
- removal of git-specific assumptions
- VCS-neutral root/identity/config-discovery behavior
- SDK/CLI host-context alignment around the newer config-loader surface

## Prior related work found in this branch

An archived earlier merge-from-main effort already exists:

- `specd-sdd/archive/2026/07/20260703-160737-merge-main-user-interface/`

This is important context:

- this branch has already absorbed an earlier slice of `main`
- the current work is a follow-up alignment against later `main` changes
- the current work should treat that archived change as historical context, not duplicate it blindly

## Overlap / likely conflict files

The following overlap list came from the intersection of both sides' diffs and from `git merge-tree` inspection.

### Shared code/docs/config files changed on both sides

- `packages/cli/src/index.ts`
- `packages/core/src/application/specd-config.ts`
- `packages/core/src/composition/kernel.ts`
- `packages/core/src/composition/use-cases/get-project-context.ts`
- `packages/core/src/composition/use-cases/get-spec-context.ts`
- `packages/core/src/composition/use-cases/index.ts`
- `packages/core/src/composition/use-cases/validate-specs.ts`
- `packages/core/src/infrastructure/fs/archive-repository.ts`
- `packages/core/src/infrastructure/fs/change-repository.ts`
- `packages/core/src/infrastructure/fs/config-loader.ts`
- `packages/core/src/public.ts`
- `packages/core/test/application/use-cases/archive-change-batch-restore.spec.ts`
- `packages/sdk/src/core-reexports.ts`
- `specd.yaml`
- `docs/config/config-reference.md`
- `docs/guide/configuration.md`

### Removed in main but still part of this branch line

- `packages/core/src/composition/graph-store-factory.ts`
- `packages/core/src/composition/kernel-internals.ts`
- `packages/core/src/composition/kernel-registries.ts`
- `packages/core/src/infrastructure/git/actor-resolver.ts`
- `packages/core/src/infrastructure/hg/actor-resolver.ts`
- `packages/core/src/infrastructure/svn/actor-resolver.ts`
- `packages/core/test/composition/kernel-internals.spec.ts`
- `packages/core/test/infrastructure/git/actor-resolver.spec.ts`

Interpretation:

- these are not just textual conflicts
- they indicate `main` replaced or removed concepts this branch may still rely on
- implementation in this branch should prefer the newer `main` architecture where feasible, rather than restoring deleted `main` structures

## Specs and metadata overlap

### Shared spec files changed on both sides

- `specs/_global/architecture/spec.md`
- `specs/_global/architecture/verify.md`
- `specs/core/compile-context/spec-lock.json`
- `specs/core/compile-context/spec.md`
- `specs/core/compile-context/verify.md`
- `specs/core/config/spec.md`
- `specs/core/config/verify.md`
- `specs/core/edit-change/spec-lock.json`
- `specs/core/edit-change/verify.md`
- `specs/core/get-project-context/spec-lock.json`
- `specs/core/get-project-context/spec.md`
- `specs/core/get-project-context/verify.md`
- `specs/core/get-project-summary/spec-lock.json`
- `specs/core/get-spec-context/spec-lock.json`
- `specs/core/get-spec-context/spec.md`
- `specs/core/get-spec-context/verify.md`
- `specs/core/get-status/spec-lock.json`
- `specs/core/kernel/spec.md`
- `specs/core/kernel/verify.md`
- `specs/core/list-workspaces/spec-lock.json`
- `specs/core/validate-specs/spec-lock.json`
- `specs/sdk/composition/spec-lock.json`
- `specs/sdk/host-context/spec-lock.json`
- `specs/sdk/host-context/spec.md`
- `specs/sdk/host-context/verify.md`

### Shared metadata files changed on both sides

- `.specd/metadata/core/change-manifest/metadata.json`
- `.specd/metadata/core/change-repository-port/metadata.json`
- `.specd/metadata/core/change/metadata.json`
- `.specd/metadata/core/compile-context/metadata.json`
- `.specd/metadata/core/config-loader/metadata.json`
- `.specd/metadata/core/config/metadata.json`
- `.specd/metadata/core/draft-change/metadata.json`
- `.specd/metadata/core/edit-change/metadata.json`
- `.specd/metadata/core/get-project-context/metadata.json`
- `.specd/metadata/core/get-project-summary/metadata.json`
- `.specd/metadata/core/get-spec-context/metadata.json`
- `.specd/metadata/core/get-status/metadata.json`
- `.specd/metadata/core/kernel/metadata.json`
- `.specd/metadata/core/list-workspaces/metadata.json`
- `.specd/metadata/core/storage/metadata.json`
- `.specd/metadata/core/validate-artifacts/metadata.json`
- `.specd/metadata/core/validate-specs/metadata.json`
- `.specd/metadata/default/_global/architecture/metadata.json`
- `.specd/metadata/sdk/composition/metadata.json`
- `.specd/metadata/sdk/host-context/metadata.json`

Interpretation:

- after implementation/spec alignment, metadata regeneration will almost certainly be needed
- these metadata overlaps reflect semantic drift, not just regenerated hashes

## Concrete conflict examples observed

### SDK host context

`main` version of `packages/sdk/src/composition/host-context.ts`:

- imports `createDefaultConfigLoader`
- awaits the loader factory
- documents the new canonical loader naming

This branch version:

- imports `createConfigLoader`
- uses the older naming/style

This is a direct example of "branch must align to newer main conventions", not just "keep both".

### Core kernel

`main` version of `packages/core/src/composition/kernel.ts`:

- uses `createCompositionResolver`
- imports many `createX` / `resolveXDeps` composition helpers
- references `composition-resolver.ts`
- uses `CompositionRegistryView`
- represents the newer composition architecture

This branch version:

- still depends on `kernel-internals.ts` and `kernel-registries.ts`
- still exposes branch-local registry/internals style
- includes studio/log/read-only/artifact additions not present in the older main composition style

Interpretation:

- the branch likely needs to preserve its extra capabilities
- but the way they are wired should be reviewed against `main`'s newer resolver-based composition approach
- avoid keeping the old branch-only composition style if `main` now provides the canonical direction

### Config loader

`main` version of `packages/core/src/infrastructure/fs/config-loader.ts`:

- imports `ConfigLoader` as a value type only
- supports structured adapter bindings beyond plain strings
- introduces `StorageDirectoryNotFoundError`
- removes direct git coupling in favor of broader adapter semantics

This branch version:

- imports `git` exec helper directly
- uses older simpler adapter-binding schema
- is still framed around git-bounded discovery

Interpretation:

- this is a major convention mismatch
- branch-side code should probably move toward the newer `main` VCS-neutral loader model

## High-risk graph findings

### `core:src/composition/kernel.ts`

- risk level: `CRITICAL`
- direct dependents: 78
- transitive dependents: 136

Why it matters:

- kernel composition changes ripple across lifecycle, project, spec, archive, status, and validation flows

### `core:src/infrastructure/fs/config-loader.ts`

- risk level: `CRITICAL`
- direct dependents: 16
- transitive dependents: 257

Why it matters:

- config-loader changes affect kernel bootstrap, repositories, CLI context, SDK host context, and project discovery flows

### `sdk:src/composition/host-context.ts`

- risk level: `CRITICAL`
- direct dependents: 8
- transitive dependents: 72

Affected surfaces explicitly include:

- CLI graph commands
- CLI project/status/context flows
- API host bootstrap
- desktop studio IPC host ownership

### `core:src/composition/use-cases/get-project-context.ts`

- risk level: `CRITICAL`
- large dependent surface across compile-context, summary, specs, metadata, preview, validation

### `core:src/composition/use-cases/validate-specs.ts`

- risk level: `CRITICAL`
- large dependent surface across validation, preview, summary, metadata, list/status/context

## Areas likely needing design decisions

### 1. Composition convergence

Open design question:

- how much of this branch's extra kernel features should be re-expressed using `main`'s newer composition resolver/factory style versus preserved behind compatibility wrappers

Current leaning from analysis:

- prefer `main`'s newer resolver-based composition direction
- preserve branch-specific features only where they add capability not present on `main`

### 2. VCS-neutral bootstrap

Open design question:

- should all branch-side config/bootstrap/host code be migrated to the newer VCS-neutral conventions from `main`

Current leaning from analysis:

- yes, where feasible
- user explicitly asked to adopt the newer ways from `main`

### 3. SDK host/context surface

Open design question:

- whether branch-side Studio/API/CLI host bootstrapping should standardize on the newer loader naming and boot path from `main`

Current leaning from analysis:

- yes
- `sdk:host-context` is one of the clearest divergence points

### 4. Deleted abstractions

Open design question:

- whether any deleted `main` abstractions (`kernel-internals`, per-VCS actor resolver classes, graph-store factory) still carry branch-only requirements that need rehoming

Current leaning from analysis:

- do not resurrect deleted mainline abstractions by default
- instead, map branch requirements onto `main`'s replacement abstractions

## Candidate secondary specs mentioned but not added initially

These came up during analysis as potentially relevant depending on final design direction:

- `core:vcs-adapter-port`
- `core:vcs-adapter`
- `core:vcs-actor-resolver`
- `core:actor-resolver`
- `core:actor-provider`
- `cli:entrypoint`
- `cli:host-context`
- `code-graph:indexer`
- `code-graph:workspace-integration`
- `core:list-workspaces`

Reason not all were added now:

- initial scope was kept focused on the core architectural/spec areas already proven to overlap directly
- these may need to be added during `/specd-design` if the final proposal concludes they must be changed explicitly

## User preferences and constraints

- The user wants analysis before implementation
- The user wants the branch aligned to the newer conventions from `main`
- The user does not want branch and `main` to diverge in their ways of working
- The user prefers adopting the forms that now come from `main`
- The user wants to inspect things like conflicts, incoming changes, affected specs, and better patterns before merge work proceeds

## Rejected or deprioritized alternatives

### "Only resolve textual merge conflicts"

Rejected in spirit by the user's follow-up.

Reason:

- user explicitly extended scope to include convergence with newer `main` conventions
- simple conflict fixing would preserve drift

### "Keep branch-local approach when both work"

Rejected in principle unless branch-specific capability requires it.

Reason:

- user explicitly asked to use the forms from `main`
- goal is to reduce divergence in ways of working

### "Attach every potentially touched spec immediately"

Not chosen at this stage.

Reason:

- a focused but clearly relevant initial scope is more manageable
- additional specs can be added during design if needed

## Implementation-relevant observations for later phases

- The graph was stale at session start and was reindexed successfully.
- `project status --graph` showed all workspaces as `owned`; no read-only workspace blocked the proposed spec scope.
- `main` and `origin/main` were identical at analysis time.
- This branch was `ahead 5` relative to `origin/feat/user-interface`.
- There were no visible working tree modifications in `git status --short --branch` beyond branch/ahead info at the moment this exploration was run.
- `changes create` succeeded immediately and created:
  - `/Users/monki/Documents/Proyectos/specd-worktrees/feat-user-interface/specd-sdd/changes/20260713-154752-align-user-interface-with-main-conventions`
- drafting pre-hooks returned:
  - `no hooks to run`
- drafting pre-hook instructions returned:
  - `no instructions`

## Suggested starting points for `/specd-design`

Good first design questions:

1. Define the canonical merge strategy for branch-specific kernel/SDK additions against `main`'s resolver-based composition.
2. Decide whether to add VCS-related specs explicitly to scope.
3. Clarify whether `specd.yaml`/config adapter binding changes require expanding `core:config` or `core:config-loader` deltas further.
4. Decide whether Studio/API/CLI host surfaces should all standardize on the same `openSpecdHost`/loader conventions from `main`.
5. Review the archived earlier merge-main change for reusable rationale vs obsolete assumptions.

## Conversation flow

1. User invoked `specd-new` and asked for merge analysis: what changed in `main`, possible conflicts, affected specs, etc.
2. Shared specd skill context and project context were loaded.
3. Project graph was found stale and was reindexed before deeper analysis.
4. Repository-level diff and merge-base analysis were run between `main` and this branch.
5. `merge-tree` and diff-intersection analysis isolated actual overlap areas.
6. Graph impact checks showed `kernel.ts`, `config-loader.ts`, `sdk host-context`, `get-project-context`, and `validate-specs` as critical blast-radius areas.
7. An initial change proposal focused on merge reconciliation was presented.
8. User extended the requirement: this branch must also align to new conventions from `main`, avoiding divergent working patterns.
9. Based on that, the proposal was updated to include convention alignment and a slightly broader spec scope.
10. User approved the proposal with `ok`.
11. The change was created successfully and this exploration context was saved for later `/specd-design` work.
