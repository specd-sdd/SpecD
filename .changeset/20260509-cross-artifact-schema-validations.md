---
'@specd/schema-std': minor
---

20260509 - cross-artifact-schema-validations: Update standard schema to include cross-artifact validation between `specs` and `verify` artifacts, ensuring requirement parity across artifacts. Incorporates `count` and `unique` constraints for requirement and scenario blocks within the standard schema to enforce structural consistency.

Specs affected:

- `core:schema-format`
