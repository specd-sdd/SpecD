---
'@specd/cli': minor
---

20260826 - cli-specs-list-count: Add --count flag to specd specs list CLI command to retrieve total spec count globally and per workspace, supporting text, json, and toon formats with mutual exclusivity for --summary.

Specs affected:

- `cli:spec-list`
